name: Suspicious IIS Module Registration
id: fd93db7d-4c18-4b04-8c12-29fe6f0caf94
description: |
  Detects the IIS worker process w3wp.exe spawning a child that registers a new IIS
  module, where the child command line EITHER contains 'appcmd.exe add module', OR
  is powershell.exe running 'system.enterpriseservices.internal.publish', OR runs
  gacutil with '/I'. Registering a managed or native module is a legitimate
  administrative action when an application is being deployed or updated, but the
  same operation lets an operator install a passive backdoor that loads into the
  worker process on every HTTP request and persists across IIS restarts and OS
  updates. The investigation should determine which registration branch fired, what
  module and DLL were registered, whether a configuration change and a corresponding
  module load followed, and whether the worker process spawning a registration tool
  is expected on this server.
type: detection
detection_id: 043c4b8b-3a54-4780-9682-081cb6b8185c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting child of w3wp.exe match one of the IIS module registration patterns this rule detects?
  context: >-
    Before investing investigation time, confirm the alert is a true positive. The
    rule fires when w3wp.exe is the parent AND the child command line matches at
    least ONE branch: it contains 'appcmd.exe add module', OR it is powershell.exe
    running 'system.enterpriseservices.internal.publish', OR it runs gacutil with
    '/I'. Note which branch fired — it determines what module or assembly was
    registered and which downstream evidence (config change, DLL load) is worth
    chasing. Confirm ParentImage is w3wp.exe and read the full command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to register IIS modules?
  context: >-
    Legitimate application deployment registers IIS modules, but normally from a
    deployment pipeline, an admin session, or a maintenance window — not from the
    web worker process under the application-pool identity. Knowing whether this
    server is an IIS host under active deployment and whether this account performs
    module registration is often the fastest way to separate a routine deployment
    from operator persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Is w3wp.exe expected to spawn a registration tool here, and what started w3wp.exe itself?
  context: >-
    The w3wp.exe parent is the core signal. The worker process spawning appcmd,
    powershell, or gacutil means a registration command ran inside the IIS request
    pipeline rather than from an administrative context. Tracing the worker's own
    origin and command line shows which application pool and site it serves, which
    helps decide whether a deployment legitimately drives registration from the
    worker or whether an HTTP request reached code execution and pivoted to
    persistence.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What module, assembly, and DLL did the registration command install? Decode the full command line.
  context: >-
    The full command line names what was registered. For 'appcmd add module', read
    the module name and the image DLL path it points at. For the
    system.enterpriseservices.internal.publish branch, identify the assembly being
    published into the GAC. For gacutil '/I', read the assembly path being
    installed. A DLL or assembly path under a webroot directory such as
    inetpub\wwwroot\bin or App_Bin, or any non-Microsoft signed DLL, is the artifact
    to pivot on. This query scopes to registration commands on this host within a
    tight window so a multi-step deployment is visible as one operation rather than
    a single line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_register:
        CommandLine|contains:
          - 'appcmd.exe add module'
          - ' system.enterpriseservices.internal.publish'
          - 'gacutil'
      condition: selection and selection_register
    fields:
      - CommandLine
      - Image
      - CurrentDirectory
- source: template:initial_access.delivery_vector
  question: What HTTP request or site activity drove the worker process to run a registration command?
  context: >-
    When the IIS worker process reaches code execution, the trigger is usually an
    inbound HTTP request against a vulnerable application or an exposed management
    surface rather than email or a document. Identify the application pool, site,
    and the request that preceded the spawn so the entry point can be patched and
    other servers running the same application can be checked. The worker's command
    line and parent context point at which site and pool to pull web access logs
    for.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did this same w3wp.exe worker spawn around the alert?
  context: >-
    If the worker process reached code execution, the registration command is rarely
    the only thing it ran. Other children of the same worker instance may include
    reconnaissance, credential access, or a download that staged the module DLL.
    Multiple distinct children from the same worker strongly suggests interactive
    operator activity rather than a scripted deployment.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1505.004.config_modification
  question: Was applicationHost.config modified or a DLL dropped to a webroot or inetsrv path around this time?
  context: >-
    Module registration ultimately writes a new entry into applicationHost.config
    under modules or globalModules, and the referenced DLL is typically dropped into
    a writable path first. File events on
    C:\Windows\System32\inetsrv\config\applicationHost.config, on the inetsrv
    directory, or on webroot bin directories such as inetpub\wwwroot\bin and
    App_Bin corroborate that a module was actually installed rather than the command
    failing. A new add element referencing an unfamiliar DLL path is the persistence
    artifact.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_paths:
        TargetFilename|contains:
          - '\inetsrv\config\applicationHost.config'
          - '\inetsrv\'
          - '\inetpub\wwwroot\bin\'
          - '\App_Bin\'
      condition: selection and selection_paths
    fields:
      - TargetFilename
      - Image
- source: attack:T1505.004.module_load
  question: Did w3wp.exe load a newly registered or unsigned DLL from a webroot path after the registration?
  context: >-
    A registered module only becomes a working backdoor once the worker process
    loads it, so the canonical post-installation indicator is w3wp.exe loading a DLL
    from a webroot subdirectory such as inetpub\wwwroot\bin or App_Bin, or loading a
    non-Microsoft signed DLL from any inetsrv path. Correlating a module-load event
    on the worker with the registration command confirms the module is live in the
    request pipeline. If image-load telemetry is not collected in this environment,
    treat zero results as a visibility gap rather than evidence that no module
    loaded.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\w3wp.exe'
      selection_paths:
        ImageLoaded|contains:
          - '\inetpub\wwwroot\bin\'
          - '\App_Bin\'
          - '\inetsrv\'
      condition: selection and selection_paths
    fields:
      - ImageLoaded
      - Image
- source: template:powershell.encoded_command_decode
  question: If the powershell.exe publish branch fired, was the command encoded or obfuscated, and what does it decode to?
  context: >-
    The system.enterpriseservices.internal.publish branch runs through
    powershell.exe, which is frequently wrapped in -EncodedCommand, string
    concatenation, or backtick escaping to hide what assembly is being published.
    Decode any base64 or obfuscated content to recover the real assembly path and
    publish call. If this branch did not fire, the registration ran through appcmd
    or gacutil and the command line is already readable.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full publish script content?
  context: >-
    Script block logging records the deobfuscated script content PowerShell actually
    executed, even when the command line is encoded. For the publish branch this is
    ground truth about the assembly being registered and any surrounding logic. If
    PowerShell operational logs are not ingested in this environment, treat the
    absence of results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \w3wp.exe
      selection_cli_1:
        CommandLine|contains: appcmd.exe add module
      selection_cli_2:
        CommandLine|contains: ' system.enterpriseservices.internal.publish'
        Image|endswith: \powershell.exe
      selection_cli_3:
        CommandLine|contains|all:
        - gacutil
        - ' /I'
      _pb_scope:
        User|expand: '%User%'
      condition: (selection_parent and 1 of selection_cli_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \w3wp.exe
      selection_cli_1:
        CommandLine|contains: appcmd.exe add module
      selection_cli_2:
        CommandLine|contains: ' system.enterpriseservices.internal.publish'
        Image|endswith: \powershell.exe
      selection_cli_3:
        CommandLine|contains|all:
        - gacutil
        - ' /I'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_parent and 1 of selection_cli_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user account do across the environment around the time of the alert?
  context: >-
    Tracking the account's activity across hosts in the alert window reveals whether
    the registration is isolated or part of broader activity, such as the same
    account touching other IIS servers. If multiple hosts show related activity from
    this account, the scope of the operation is larger than one server.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \w3wp.exe
      selection_cli_1:
        CommandLine|contains: appcmd.exe add module
      selection_cli_2:
        CommandLine|contains: ' system.enterpriseservices.internal.publish'
        Image|endswith: \powershell.exe
      selection_cli_3:
        CommandLine|contains|all:
        - gacutil
        - ' /I'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_parent and 1 of selection_cli_*) and _pb_scope
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this server around the same time?
  context: >-
    Operators rarely rely on a single foothold. After registering an IIS module,
    they commonly stack backup persistence using scheduled tasks, services, registry
    Run keys, BITS jobs, or WMI. Multiple persistence installations in the same
    timeframe indicate intentional stacking rather than an isolated deployment
    action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this server in the hours before this alert?
  context: >-
    Persistence rarely happens in isolation. Earlier detections on the same
    server — web exploitation, reconnaissance, or credential access — may reveal the
    stages that preceded the module registration and help reconstruct when the
    server was first reached.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1505.004
      name: IIS Components
      role: primary
    - id: T1505
      name: Server Software Component
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: registration_command_execution
      description: "Worker process w3wp.exe spawns a registration tool (appcmd add module / publish / gacutil /I) that installs an IIS module"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 1b
      label: registration_decode
      description: "Decode the registration command to identify the module, assembly, and DLL installed"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: config_modification
      description: "applicationHost.config gains a new module entry and the referenced DLL is dropped to a webroot/inetsrv path"
      mapped_to: Q7
      source: attack:T1505.004.config_modification
    - stage: 3
      label: module_load
      description: "w3wp.exe loads the newly registered DLL, making the module live in the HTTP request pipeline"
      mapped_to: Q8
      source: attack:T1505.004.module_load
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1505.004
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1505.004
      covered_by: Q8
      source: attack:T1505.004.module_load
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1505.004
      covered_by: Q7
      source: attack:T1505.004.config_modification
      coverage_type: direct
    - dimension: user_context
      technique: T1505.004
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1505
      covered_by: Q6
      source: template:parent_child.sibling_processes
      coverage_type: direct
    - dimension: time_correlation
      technique: T1505
      covered_by: Q16
      source: template:cue_host_context.precursor
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1505
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: partial
  mutable_dispositions:
    - element: WatchedPaths
      dimension: tuning_parameter
      specific_values: "%windir%\\System32\\inetsrv\\, inetpub\\wwwroot\\bin\\, App_Bin\\, applicationHost.config"
      disposition: query_injection
      target_question: Q7
      notes: "inetsrv/applicationHost.config and webroot bin paths injected into Q7 file_event TargetFilename|contains and Q8 image_load ImageLoaded|contains"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "w3wp.exe (parent), AppCmd.exe / powershell.exe / gacutil (registration tools)"
      disposition: query_injection
      target_question: Q8
      notes: "w3wp.exe pinned as Image in the image_load query; parent_process_legitimacy (Q3) and baseline_svc (Q12) pivot on the w3wp.exe parent"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "No concrete threshold given; chained-event correlation realized as +/-4h on module_load and +/-15m on config_modification"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: "No concrete user list; realized as user-pivot baseline (Q11) plus service-principal substitute (Q12) for app-pool identity"
    - element: DLLNameEntropyThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Entropy/name heuristic not directly queryable; surfaced as 'unsigned / non-Microsoft DLL from webroot' guidance in Q8 context"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (ParentImage in detection) — parent_child.parent_process_legitimacy (Q3) subsumes process_chain"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.mechanism_decode
      reason: "parent_child technique set active — parent_process_legitimacy (Q3) is the more specific parent-decode question; cue_process_tool.mechanism_decode (Q4) covers the child-command decode"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_user_actor.baseline / baseline_svc pair
      reason: "user_actor pair — non-deduplicable; both emitted (Q11 user-pivot, Q12 parent+binary substitute) because the alert may fire under the IIS app-pool service principal"
      kept: both
    - dropped: cue_user_actor.clustering / clustering_svc pair
      reason: "user_actor pair — non-deduplicable; both emitted (Q13 user-pivot, Q14 parent+binary substitute)"
      kept: both
  skipped_templates:
    - template: cue_parent_process.baseline
      reason: "Overlaps cue_user_actor.baseline_svc (Q12), which already aggregates the w3wp.exe parent + child-binary pairing across hosts; dropped to stay within the question budget without losing parent-baseline coverage"
    - template: cue_parent_process.follow_on
      reason: "Subsumed by parent_child.sibling_processes (Q6) which enumerates children of the exact w3wp.exe instance via ParentProcessGuid — more precise than the parent-binary follow_on"
    - template: cue_parent_process.clustering
      reason: "Subsumed by sibling_processes (Q6) and host_context coverage; same-time activity from the worker is captured by the ParentProcessGuid-scoped sibling query"
    - template: cue_parent_process.scope
      reason: "Cross-host parent-child distribution is already answered by cue_user_actor.clustering_svc (Q14), which pivots on parent+binary across hosts in the alert window"
    - template: cue_process_tool.baseline
      reason: "Replaced by cue_user_actor.baseline_svc (Q12) which baselines the parent+binary launch mechanism; a host+Image baseline on powershell.exe would be dominated by routine usage per the powershell-noise hint"
    - template: cue_host_context.clustering
      reason: "Host-wide aggregated activity overlaps sibling_processes (Q6) and precursor (Q16); dropped to keep the playbook focused within budget"
    - template: powershell.powershell_security_bypass
      reason: "The publish branch is a deployment cmdlet, not an interactive PowerShell payload; bypass/AMSI-tamper indicators are weakly relevant and would add a low-signal slot. Defense-tampering precursors are still surfaced via the precursor alert query (Q16)"
    - template: organizational_context.change_ticket
      reason: "recommended priority; coverage budget used by higher-value IIS-specific chain questions. host_user_context (Q2) and baseline (Q11) already establish whether the action was authorized/expected"
    - template: organizational_context.human_recall
      reason: "recommended priority; redundant with host_user_context (Q2) for this rule's disposition needs"
    - template: organizational_context.timing_alignment
      reason: "recommended priority; timing-vs-maintenance judgment is folded into host_user_context (Q2) and precursor (Q16) rather than a standalone slot"
    - template: organizational_context.procedural_consistency
      reason: "recommended priority; the deployment-procedure question is folded into host_user_context (Q2) ('expected to register IIS modules') for this rule"
    - template: initial_access.campaign_scope
      reason: "Cross-host same-rule correlation overlaps cue_user_actor.clustering_svc (Q14) for this parent+binary signature; the worker-spawn launch pivot is more specific than a generic rule.uuid fan-out for a server-side persistence rule"
    - template: initial_access.application_patch_status
      reason: "recommended initial-access check; patch verification is implied by the delivery_vector question (Q5) which directs the analyst to the triggering web request and application, without a separate parent-only slot"
    - template: persistence_scope.file_based_persistence
      reason: "Startup-folder / Run-key file drops are not the persistence vector here; the relevant file-event coverage (applicationHost.config + webroot DLL drop) is provided by the IIS-specific Q7"
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: declined
      reason: >-
        Acknowledged as a replacement directive, not an independent action item:
        obfuscation.encoded_payload_decode is not present in this rule's template
        set, and the replacement powershell.encoded_command_decode is included as
        Q9. No separate obfuscation question was authored or dropped, so there is no
        distinct decline beyond honoring the replacement.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - initial_access
    - persistence_scope
    - parent_child
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 11
      file_event: 1
      image_load: 1
      powershell: 1
      sigma_alert: 1
