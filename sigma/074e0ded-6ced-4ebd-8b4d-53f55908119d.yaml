name: AWL Bypass with Winrm.vbs and Malicious WsmPty.xsl/WsmTxt.xsl
id: 412535ad-e19e-4ae9-98bb-868727599c55
description: |
  Detects a script host (cscript.exe or wscript.exe, possibly renamed and relocated) executing winrm.vbs with a
  -format:pretty or -format:text argument from a directory outside C:\Windows\System32 or C:\Windows\SysWOW64. The rule
  fires when the command line contains "winrm" AND a format argument (pretty or text, with or without quotes) AND the
  script-host Image does not start from a system folder. winrm.vbs is a Microsoft-signed VBScript that loads WsmPty.xsl or
  WsmTxt.xsl from the script host's own directory; an attacker who copies a script host to a writable directory and drops a
  malicious WsmPty.xsl/WsmTxt.xsl alongside it can have winrm.vbs execute attacker-controlled JScript inside the signed
  chain, bypassing application allow-listing. The investigation determines whether the script host and its on-disk XSL are
  legitimate catalog-signed copies, what JScript any non-catalog XSL would run, and whether the execution led to child
  processes or follow-on activity.
type: detection
detection_id: 074e0ded-6ced-4ebd-8b4d-53f55908119d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Did a script host run winrm.vbs with a format:pretty or format:text argument from a non-system folder?
  context: Before investing investigation time, confirm the alert is a true positive. Review the exact command line and the
    script-host Image path against the rule's criteria. The rule fires only when the command line contains "winrm" AND a
    format argument (pretty or text, quoted or unquoted) AND the executing Image does not start from C:\Windows\System32 or
    C:\Windows\SysWOW64. Confirm all three hold and note the full Image path, since a host running from outside a system
    folder is the core anomaly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to administer WinRM from a command-line script host?
  context: Legitimate WinRM administration via winrm.vbs runs from the signed copy in C:\Windows\System32, not from a copied
    or relocated script host. Knowing whether the user role and host function involve scripted remote-management tooling is
    often the fastest way to separate routine administration from a proxy-execution bypass.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this winrm.vbs invocation do? Decode the format argument, the winrm sub-operation, and the host
    path.
  context: The full command line reveals the specific action. Decode the format argument (pretty loads WsmPty.xsl, text loads
    WsmTxt.xsl from the script host's directory), the winrm sub-command (get, enumerate, invoke), and the directory the
    script host ran from. A host path outside System32/SysWOW64 means winrm.vbs reads an XSL from an attacker-writable
    location rather than the catalog-signed System32 copy. A path segment named cscript.exe holding a renamed host satisfies
    winrm.vbs's substring check while running a different binary.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: winrm
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this script host and is that parent chain expected?
  context: The parent process reveals how the script host was invoked. Interactive administration typically descends from
    explorer.exe or a console shell; invocation from office applications, mshta.exe, a download utility, or an unexpected
    automation parent points to scripted or operator-driven abuse rather than routine WinRM management.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run a script host from this directory?
  context: A script host executing from outside the system folders is the anomaly this rule flags. Zero or very low historical
    counts for this Image path on this host indicate first-time or rare execution and raise the likelihood that the host was
    copied into a writable directory specifically to read an attacker-controlled XSL.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and
    does the current alert fit that pattern?
  context: Even legitimate WinRM scripting can match loosely. The query mirrors the rule's selection structure scoped to this
    host, so the resulting parent and user distribution reflects how this specific firing pattern (winrm plus a format
    argument from a non-system folder) has historically run here. A new parent process, an unexpected user account, or an
    unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      contains_format_pretty_arg:
        CommandLine|contains:
        - format:pretty
        - format:"pretty"
        - format:"text"
        - format:text
      image_from_system_folder:
        Image|startswith:
        - C:\Windows\System32\
        - C:\Windows\SysWOW64\
      contains_winrm:
        CommandLine|contains: winrm
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (contains_winrm and (contains_format_pretty_arg and not image_from_system_folder)) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did this script host spawn afterward?
  context: A malicious WsmPty.xsl or WsmTxt.xsl embeds a JScript block that runs arbitrary code; a proof-of-concept payload
    spawns cmd.exe, while production payloads bridge to unsigned .NET execution. Child processes such as cmd.exe,
    powershell.exe, or a network or download utility descending from this script host reveal the actual payload the XSL
    transform executed.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.clustering
  question: What other tools ran on this host around the same time?
  context: Proxy-execution bypasses rarely stand alone. Activity surrounding the script-host execution may reveal the staging
    of the copied host and XSL, reconnaissance, privilege escalation, or persistence that provides broader context for the
    investigation.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like
    consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    firing pattern rather than generic script-host usage. Many hosts firing the same pattern points to scripted deployment or
    vendor automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to
    calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      contains_format_pretty_arg:
        CommandLine|contains:
        - format:pretty
        - format:"pretty"
        - format:"text"
        - format:text
      image_from_system_folder:
        Image|startswith:
        - C:\Windows\System32\
        - C:\Windows\SysWOW64\
      contains_winrm:
        CommandLine|contains: winrm
      condition: contains_winrm and (contains_format_pretty_arg and not image_from_system_folder)
    fields:
    - host.name
- source: attack:T1216.file_event
  question: What WsmPty.xsl, WsmTxt.xsl, or script-host file was written into the directory this host ran from?
  context: The bypass requires two files on disk in an attacker-writable directory, a copied script host (cscript.exe or a
    renamed wscript.exe) and a malicious WsmPty.xsl or WsmTxt.xsl that winrm.vbs reads in place of the catalog-signed System32
    copy. Surfacing recent writes of these filenames, and who wrote them, ties the execution to the staging step. A
    WsmPty.xsl or WsmTxt.xsl whose hash differs from the System32 catalog-signed version is the suspicious artifact; legitimate
    copies rarely change.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_files:
        TargetFilename|endswith:
        - \WsmPty.xsl
        - \WsmTxt.xsl
        - \cscript.exe
        - \wscript.exe
      condition: selection_host and selection_files
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run a command-line script host for WinRM administration?
  context: Compare this user's current action against their historical behavior. A user who has never run this script host or
    administered WinRM from the command line is more suspicious than one who does so regularly, and user role should align
    with scripted remote-management duties.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and script-host combination historically routine on this host or across the environment, or is the
    parent unusual for this host?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is
    the launch mechanism, which parent process spawned this script host, and whether that pair is historically common. A new
    or rare parent for this host on this machine or environment-wide is the deviation worth chasing; a pair already
    accounting for thousands of prior events is vendor or operating-system automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity on multiple endpoints. If the
    same user triggered similar script-host activity elsewhere, the scope of the operation may be larger than this single
    host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and script-host combination fire on other hosts in the same window, indicating a coordinated
    rollout, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism, parent process plus spawned script host, fired on other hosts in
    the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context, what led to
    this event and what happened after, and whether it is part of a larger sequence. Look for the staging of files into a
    writable directory, reconnaissance, privilege escalation, or persistence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host, such as defense tampering, reconnaissance,
    or file-staging activity, may reveal the attack stages that preceded this event and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution, and application allow-list bypasses are often paired with attempts
    to weaken endpoint defenses. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad
    exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful signal that protection looks healthy; kernel-level driver
    tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation;
    review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1216
      name: System Script Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_script_proxy_execution
      description: A Microsoft-signed script host runs winrm.vbs with a format argument from a non-system folder, causing
        winrm.vbs to load an attacker-controlled WsmPty.xsl/WsmTxt.xsl from the host's directory.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_line_decode
      description: Decode the format argument, the winrm sub-operation, and the host directory to determine which XSL is read
        and from where (attacker-writable vs catalog-signed System32).
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_staging_on_disk
      description: The copied script host and the malicious WsmPty.xsl/WsmTxt.xsl are written into an attacker-writable
        directory prior to execution.
      mapped_to: Q10
      source: attack:T1216.file_event
    - stage: 4
      label: unsigned_child_execution
      description: The XSL JScript block runs arbitrary code, spawning a child process (cmd.exe, powershell.exe, or a .NET
        bridge) from the signed parent.
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1216
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1216
      covered_by: Q7
      source: template:cue_process_tool.follow_on
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1216
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (wscript.exe, cscript.exe, pubprn.vbs, installutil.exe — environment-specific paths)"
      disposition: context
      target_question: Q4
      notes: Parent script-host interpreters discussed in process_chain context; the rule's anomaly is the host Image path,
        scoped via ParentProcessGuid in the query.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (delta between signed-script execution and child creation)"
      disposition: context
      target_question: Q7
      notes: follow_on uses a +4h window for child-process correlation; file_event staging uses -24h. No queryable threshold.
    - element: ChildCommandLineRegex
      dimension: tuning_parameter
      specific_values: "rationale only (download cradle / decode regex — no fixed values supplied)"
      disposition: context
      target_question: Q7
      notes: follow_on surfaces child CommandLine for manual inspection; the brief supplies a regex rationale, not concrete
        literals, so no CommandLine|contains was injected.
    - element: SignedToUnsignedTransition
      dimension: tuning_parameter
      specific_values: "rationale only (signed parent -> unsigned/unknown child)"
      disposition: context
      target_question: Q10
      notes: Catalog-signature mismatch of WsmPty.xsl/WsmTxt.xsl and the relocated host is described in mechanism_decode and
        file_event context; signing verification is a manual/host step, not directly queryable in process_creation.
  dedup_decisions:
    - dropped: none
      reason: "cue_user_actor user-pivot + service_principal_substitute pairs emitted in full (baseline Q11/baseline_svc Q12,
        clustering Q13/clustering_svc Q14) — non-deduplicable per AGENTS.md (subject vs mechanism identity)."
      kept: all four user_actor questions
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1216) is not covered — pull in cue_image_load.* or author a
        custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule's mechanism is command-line script proxy execution: a signed winrm.vbs reads an attacker-controlled XSL and
        runs embedded JScript via the msxml/WSH transform engine. There is no malicious PE/DLL image_load to observe — the
        unsigned code runs in-script inside the already-loaded, catalog-signed script-host modules, so an image_load query on
        cscript.exe/wscript.exe would return only their normal signed module set with no separation between benign and abuse
        firings. The load-bearing artifact is the on-disk XSL/host file, which Q10 (attack:T1216.file_event) covers directly,
        and the in-script payload surfaces as a spawned child in Q7 (follow_on). No compensating attack:T1216.image_load
        question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1216) is not covered and no template family exists —
        author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        The bypass does not involve cross-process handle access or memory manipulation of another process — the signed script
        host executes attacker JScript within its own process via the XSL transform, then spawns ordinary children. There is no
        target process being opened (no GrantedAccess mask to key on), so a process_access query would have no selective
        signal distinguishing this rule's firing from background telemetry. The relevant evidence (host relocation + XSL drop,
        and any spawned payload) is covered by Q3, Q7, and Q10. No compensating attack:T1216.process_access question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      file_event: 1
      sigma_alert: 1
