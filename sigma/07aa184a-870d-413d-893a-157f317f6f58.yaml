name: Suspicious Reconnaissance Activity Via GatherNetworkInfo.VBS
id: 520616ec-5048-473c-8c8d-e58b291de3e7
description: |
  Detects a command line referencing the built-in script gatherNetworkInfo.vbs when it is NOT
  launched through the expected script hosts cscript.exe or wscript.exe. The script is a
  Microsoft-signed VBScript at C:\Windows\System32\gatherNetworkInfo.vbs that performs
  comprehensive host enumeration, dumping event logs, network configuration, and hardware
  details into C:\Windows\System32\config\. Because the rule fires only when the invoker is
  something other than the normal script hosts, the alert points to an unusual launch path for
  built-in reconnaissance. Determine what process invoked the script, whether the user and host
  routinely run it, and whether the activity sits inside a broader enumeration or staging
  sequence.
type: detection
detection_id: 07aa184a-870d-413d-893a-157f317f6f58
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line reference gatherNetworkInfo.vbs while NOT being launched by cscript.exe or wscript.exe?
  context: Before investing investigation time, confirm the alert is a true positive. The rule
    fires when a command line contains gatherNetworkInfo.vbs AND the invoking image is not
    cscript.exe or wscript.exe. Read the full command line and the invoking process to verify
    both conditions hold, since the normal script-host invocation is explicitly excluded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run built-in host enumeration scripts?
  context: Legitimate administrative or imaging activity can reference gatherNetworkInfo.vbs.
    Knowing whether the user role and host function are consistent with running built-in
    reconnaissance scripts is often the fastest path to distinguishing routine operations from
    operator-driven enumeration. A standard user workstation invoking this script outside an
    imaging or support context is harder to explain than a build or provisioning host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this invocation of gatherNetworkInfo.vbs do? Decode the full command line and any arguments.
  context: The full command line reveals how the script was driven and what surrounded it. Decode
    any flags, redirection, chained commands, or output paths. The script writes its enumeration
    results into C:\Windows\System32\config\, so look for follow-on handling of those files. Note
    the invoking binary, since the rule fires precisely because the launcher is not the normal
    script host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: gatherNetworkInfo.vbs
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - Image
- source: template:obfuscation.encoded_payload_decode
  question: Is the command line or the surrounding script content encoded or obfuscated, and what does it reconstruct to?
  context: VBScript-based execution frequently hides intent behind string concatenation or Chr()
    chains, and a launcher that is not the standard script host may pass encoded or assembled
    arguments. Reconstruct any Chr() chains, concatenated strings, or encoded fragments in the
    command line to recover the literal operation. The goal is to determine whether the
    invocation is a plain reference to the built-in script or a wrapper assembling additional
    behavior around it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this invocation, and is that parent chain expected for a built-in script?
  context: The parent process reveals how the script was driven. Because the normal script hosts
    are excluded by the rule, the invoking and parent chain are the central question. Legitimate
    imaging or provisioning often runs from setup or maintenance processes; parents such as
    mshta.exe, an Office application, a download cradle, or an unfamiliar interpreter point toward
    operator-driven execution.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single discovery action may be legitimate troubleshooting, but multiple
    reconnaissance commands in rapid succession is a signature pattern of post-compromise
    enumeration. Built-in host recon scripts are frequently paired with native discovery binaries
    that dump network configuration, ARP and DNS state, running processes, services, and account
    information. Several distinct recon binaries within the window alongside this script point to
    automated enumeration rather than isolated administration.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even built-in scripts can be abused. The query mirrors the rule's selection structure
    scoped to this host, so the resulting parent and user distribution reflects how this specific
    firing pattern has historically run here rather than generic VBScript usage. Deviations such
    as a new parent process, an unexpected user account, or an unusual command-line structure are the
    anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: gatherNetworkInfo.vbs
      filter:
        Image|endswith:
        - \cscript.exe
        - \wscript.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What processes ran immediately after this invocation, and do they handle the enumeration output?
  context: The script writes consolidated enumeration results into C:\Windows\System32\config\.
    Operator-driven recon typically follows collection with staging — archive creation, file
    copying, encoding, or upload tooling. Child and sibling processes spawned shortly after the
    invocation reveal whether the gathered data is being compressed, moved, or exfiltrated, which
    sharply raises severity over a bare enumeration.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: gatherNetworkInfo.vbs
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.clustering
  question: What other tools ran on this host around the same time?
  context: Attacks typically involve multiple tools in sequence. Activity surrounding the detected
    invocation may reveal archive and compression utilities, transfer tooling, or remote-execution
    helpers that turn host enumeration into a collection and staging operation. A cluster of
    built-in utilities running together is a stronger signal than the script appearing alone.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: gatherNetworkInfo.vbs
      filter:
        Image|endswith:
        - \cscript.exe
        - \wscript.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: gatherNetworkInfo.vbs
      filter:
        Image|endswith:
        - \cscript.exe
        - \wscript.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or enumeration
    spread across multiple endpoints. If the same user invoked this script or related recon on
    several hosts, the scope of the activity is broader than a single workstation and points
    toward a coordinated enumeration sweep.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: gatherNetworkInfo.vbs
      filter:
        Image|endswith:
        - \cscript.exe
        - \wscript.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution. The query surfaces direct attempts to
    disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI
    and ETW. Zero rows is a useful signal that protection looks healthy, though kernel-level
    driver tampering and in-memory patches will not appear here. Populated rows shift the
    disposition toward operator preparation; review the parent process and signing chain to
    identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of this firing pattern rather than generic VBScript usage. Many hosts firing
    the same pattern points to scripted deployment or imaging automation; one host or a small
    unrelated set points to manual operator activity. Use the host distribution and parent
    consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: gatherNetworkInfo.vbs
      filter:
        Image|endswith:
        - \cscript.exe
        - \wscript.exe
      condition: selection and not filter
    fields:
    - host.name
- source: attack:T1059.005.image_load
  question: Did the invoking process load the VBScript scripting engine modules, confirming actual VBScript interpretation rather than a passing string reference?
  context: A command line that merely names gatherNetworkInfo.vbs does not prove the script was
    interpreted. When VBScript actually executes, the host process loads scripting engine modules
    such as vbscript.dll and scrrun.dll. Correlating the invoking process to a load of those
    modules in the same window confirms the script ran and identifies which process hosted the
    interpreter, which matters because the normal script hosts are excluded by the rule. Absence
    of these loads suggests the command line referenced the script without interpreting it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ImageLoaded|endswith:
          - '\vbscript.dll'
          - '\scrrun.dll'
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - ProcessGuid
- source: attack:T1615.security
  question: Did object-access auditing on this host record enumeration of Group Policy or directory objects around the same time?
  context: Host enumeration scripts of this kind are often paired with broader discovery that
    reaches into Group Policy and directory objects. Object-access audit events record handle
    requests against directory service objects, which surface enumeration of groupPolicyContainer
    and related attributes that process-creation telemetry alone does not show. Correlating those
    access events with this invocation helps establish whether the activity is confined to local
    host data collection or extends into domain-level Group Policy discovery. If object-access
    auditing is not enabled in this environment, treat zero results as a visibility gap rather
    than evidence nothing occurred.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4661
      condition: selection
    fields:
      - SubjectUserName
      - ObjectType
      - ObjectName
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1615
      name: Group Policy Discovery
      role: primary
    - id: T1059.005
      name: Visual Basic
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: script_invocation
      description: "Built-in VBScript invoked via a non-standard launcher to enumerate host network/hardware/event-log state"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: interpreter_execution
      description: "Invoking process loads the VBScript scripting engine, confirming actual script interpretation"
      mapped_to: Q16
      source: attack:T1059.005.image_load
    - stage: 3
      label: discovery_breadth
      description: "Enumeration extends across native recon binaries and, for T1615, Group Policy / directory objects"
      mapped_to: Q6
      source: template:discovery_recon.discovery_burst_detection
    - stage: 4
      label: collection_staging
      description: "Enumeration output handled by follow-on archive/transfer tooling"
      mapped_to: Q8
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: time_correlation
      technique: T1615
      covered_by: Q6
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1615
      covered_by: Q7
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: user_context
      technique: T1615
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1059.005
      covered_by: Q4
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.005
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcess
      dimension: parent_process_analysis
      specific_values: "mshta.exe / Office apps spawning script hosts (rationale; rule excludes cscript/wscript)"
      disposition: context
      target_question: Q5
      notes: "Parent-chain question frames non-standard launchers as the central pivot since the normal script hosts are filtered out."
    - element: ModuleName
      dimension: tuning_parameter
      specific_values: "vbscript.dll, scrrun.dll"
      disposition: query_injection
      target_question: Q16
      notes: "Injected as ImageLoaded|endswith list in the authored image_load question (hint-driven)."
    - element: CommandLinePatterns
      dimension: tuning_parameter
      specific_values: "gatherNetworkInfo.vbs (rule selection value)"
      disposition: query_injection
      target_question: Q3
      notes: "CommandLine|contains gatherNetworkInfo.vbs used to scope mechanism_decode and baseline queries since rule is CommandLine-only."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (admin vs standard user)"
      disposition: context
      target_question: Q2
      notes: "Captured in host_user_context and user baseline framing."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window)"
      disposition: context
      target_question: Q6
      notes: "Discovery-burst and clustering windows operationalize the correlation window."
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (high-entropy/encoding indicator)"
      disposition: context
      target_question: Q4
      notes: "Obfuscation decode question covers Chr()/concatenation reconstruction; no numeric threshold queryable in process_creation."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rationale only (encoded/uncommon script execution)"
      disposition: context
      target_question: Q3
      notes: "Mechanism-decode and obfuscation-decode together cover command-line analysis."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (managed/unmanaged workstation variance)"
      disposition: context
      target_question: Q5
      notes: "Parent-chain question covers parent variance."
  dedup_decisions:
    - dropped: template:discovery_recon.discovery_user_context
      reason: "overlaps universal.host_user_context (per template note); kept the universal triage version"
      kept: template:universal.host_user_context
    - dropped: template:discovery_recon.discovery_parent_chain
      reason: "duplicates cue_process_tool.process_chain (parent-of-invocation pivot); process_chain kept as it walks the tree"
      kept: template:cue_process_tool.process_chain
    - dropped: template:cue_process_tool.baseline
      reason: "host-baseline subsumed by baseline_deviation (REFLECTED, rule-pattern-scoped) which is more precise for this firing pattern"
      kept: template:cue_process_tool.baseline_deviation
    - dropped: template:cue_host_context.clustering
      reason: "duplicates cue_process_tool.clustering host-around-time view"
      kept: template:cue_process_tool.clustering
    - dropped: template:cue_host_context.precursor
      reason: "trimmed to stay within 14-18; endpoint_protection_health covers pre-alert tampering context"
      kept: template:universal.endpoint_protection_health
    - pair: [template:cue_user_actor.baseline, template:cue_user_actor.baseline_svc]
      reason: "user_actor pair — non-deduplicable"
    - pair: [template:cue_user_actor.clustering, template:cue_user_actor.clustering_svc]
      reason: "user_actor pair — non-deduplicable"
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted-on
      reason: >-
        ADAPT_STRUCTURALLY queries (mechanism_decode Q3, follow_on Q8, user baseline Q10,
        baseline_svc Q11, clustering_svc Q13) rewritten to use CommandLine|contains
        gatherNetworkInfo.vbs instead of Image|endswith|expand %Image%, since the rule carries no
        Image selection. Listed here for audit; acted-on hints are not declines.
    - hint: "logsource 'image_load' (recommended by techniques: T1059.005) is not covered — pull in cue_image_load.* or author a custom question with category: image_load"
      disposition: acted-on
      reason: >-
        Authored Q16 (attack:T1059.005.image_load) querying ImageLoaded for vbscript.dll/scrrun.dll
        on the host to confirm actual VBScript interpretation. Listed for audit; acted-on, not declined.
    - hint: "logsource 'security' (recommended by techniques: T1615) is not covered and no template family exists — author a custom question with category: security"
      disposition: acted-on
      reason: >-
        Authored Q17 (attack:T1615.security) querying Security EventID 4661 object-access auditing
        for Group Policy / directory object enumeration. Listed for audit; acted-on, not declined.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - discovery_recon
    - obfuscation
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 14
      image_load: 1
      security: 1
