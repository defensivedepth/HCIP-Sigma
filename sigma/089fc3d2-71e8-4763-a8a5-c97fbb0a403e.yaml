name: Regsvr32 DLL Execution With Suspicious File Extension
id: 2f004dc0-c255-4811-9bc3-f8f748c0087f
description: |
  Detects regsvr32.exe (or a binary whose original filename is REGSVR32.EXE) invoked with a command line ending in a file extension associated with images, documents, or temporary data rather than .dll or .ocx. The rule fires only when both conditions match: the process is regsvr32.exe AND the command line ends in one of the masquerading extensions. Loading code through regsvr32 with a non-DLL extension is a proxy-execution and masquerading pattern used to disguise a malicious DLL as benign data; legitimate COM registration almost always references a .dll or .ocx. Determine what the loaded file actually is, what spawned regsvr32, whether the host or user routinely runs this pattern, and whether the load was followed by network activity or child processes.
type: detection
detection_id: 089fc3d2-71e8-4763-a8a5-c97fbb0a403e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the regsvr32 command line end in one of the masquerading extensions
    this rule detects, and is the process regsvr32.exe?
  context: Before investing investigation time, confirm the alert is a true positive
    by reviewing the exact command line and process details. The rule fires only when
    the process is regsvr32.exe (by Image or OriginalFileName) AND the command line
    ends in a non-DLL extension such as .jpg, .png, .dat, .tmp, or .txt. Confirm both
    halves match the loaded file argument.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to register COM objects or load DLLs via
    regsvr32?
  context: Legitimate administrative activity and software installers do invoke regsvr32,
    but almost always against a .dll or .ocx in a system or vendor install path. A
    user or host with no role that involves COM registration loading a file with an
    image or temp extension is harder to explain as routine operations.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What file is regsvr32 actually loading, and what does the full command
    line reveal about the load?
  context: The full command line reveals which file was passed to regsvr32 and any
    flags (/i, /u, /s, /n) that change registration behavior. Decode the file path
    and extension to identify whether the target lives in a temporary or user-profile
    directory and whether the extension masks an actual DLL. A file with an image
    or document extension loaded as code by regsvr32 is the masquerading behavior
    this rule fires on.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|endswith:
          - .bin
          - .bmp
          - .cr2
          - .dat
          - .eps
          - .gif
          - .ico
          - .jpeg
          - .jpg
          - .log
          - .nef
          - .orf
          - .png
          - .raw
          - .rtf
          - .sr2
          - .temp
          - .tif
          - .tiff
          - .tmp
          - .txt
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned regsvr32, and is that parent chain expected?
  context: The parent process reveals how regsvr32 was invoked. Legitimate COM registration
    is usually launched by installers, explorer.exe, or services.exe. Parents such
    as office applications, wscript.exe, mshta.exe, or a macro host point to delivery
    through a document or script that dropped a disguised DLL and registered it.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.010.image_load
  question: What modules did regsvr32 load into memory after it started, and were
    any unsigned or loaded from a user-writable path?
  context: Regsvr32 registers a DLL by loading it and calling its entry point, so
    the image-load events for the regsvr32 process show the actual module behind the
    disguised extension. A module loaded from a temporary or user-profile directory,
    or one that is unsigned or has a mismatched original filename, confirms that the
    file with the image or document extension is executable code rather than data.
    If image-load telemetry is not collected in this environment, treat zero results
    as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.010.network_connection
  question: Did regsvr32 make any outbound network connections around the time of
    this load?
  context: Regsvr32 can fetch and execute a remote scriptlet or the registered DLL
    can call out to a command channel after loading. Legitimate COM registration does
    not initiate network traffic, so any outbound connection from regsvr32 is a strong
    pivot toward staging or command-and-control. Note the destination address and
    port for correlation with other host activity.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.follow_on
  question: What child processes did regsvr32 spawn afterward?
  context: The DLL loaded by regsvr32 may spawn follow-on processes for the next stage
    of an operation. Legitimate registration spawns nothing; unexpected children such
    as cmd.exe, powershell.exe, rundll32.exe, or a dropped executable indicate the
    registered module executed post-load activity.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this regsvr32 pattern, what parents
    and users normally drive it, and does the current alert fit?
  context: Even legitimate software can occasionally register a DLL with an unusual
    extension. The query mirrors the rule's selection scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing pattern
    has run here before. A new parent process, an unexpected user account, or a first-time
    appearance is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this regsvr32 pattern in the past week, and does
    the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so
    the result is a fleet-wide count of this firing pattern rather than generic regsvr32
    usage. Many hosts firing the same pattern points to a scripted deployment or vendor
    installer; one host or a small unrelated set points to manual operator activity
    or targeted delivery. Use the host spread and parent consistency to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or
    activity spanning multiple endpoints. If the same user triggered similar activity
    on other hosts, the scope of the operation may be larger than this single load.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals
    what led to the regsvr32 load and what happened after, and whether it is part
    of a larger sequence. Look for a preceding document or script host, reconnaissance,
    privilege escalation, or persistence mechanisms.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host
    in the hour before the alert?
  context: Tampering typically precedes payload execution. The query surfaces direct
    attempts to disable AV/EDR services, flip Defender flags, add broad exclusions,
    clear logs, or patch AMSI/ETW. Zero rows is a useful signal that protection looks
    healthy; kernel-level driver tampering and in-memory patches will not appear here.
    Populated rows shift the disposition toward operator preparation; review the parent
    process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:lolbin.technique_class_lateral
  question: Were any other proxy-execution LOLBins used on this host recently?
  context: Attackers who use one proxy-execution binary often use several. If regsvr32
    loaded a disguised DLL, check whether rundll32, mshta, msbuild, installutil, or
    similar binaries also ran on this host to execute code. Finding multiple proxy-execution
    LOLBins performing the same kind of code-loading strengthens the case for malicious
    activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\msiexec.exe'
          - '\installutil.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\control.exe'
          - '\infdefaultinstall.exe'
          - '\msdt.exe'
          - '\mavinject.exe'
          - '\wmic.exe'
          - '\xwizard.exe'
          - '\ie4uinit.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was regsvr32 executed from an expected system path?
  context: Regsvr32 executed from outside System32 or SysWOW64 may indicate the binary
    was copied or renamed to evade path-based allowlists. A regsvr32.exe running from
    a user's temp or profile directory is more suspicious than one in its normal system
    location. Compare the Image path against OriginalFileName to confirm the binary
    identity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this regsvr32 pattern over the past 90 days,
    and what command-line structures are typical?
  context: A LOLBin pattern with no prior history on a host is more suspicious than
    one used regularly under stable parents. The query mirrors the rule's selection
    scoped to this host, so the count reflects this specific firing pattern rather
    than generic regsvr32 usage. The command-line projection lets you compare typical
    invocation patterns and loaded-file extensions against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \regsvr32.exe
      - OriginalFileName: REGSVR32.EXE
      selection_cli:
        CommandLine|endswith:
        - .bin
        - .bmp
        - .cr2
        - .dat
        - .eps
        - .gif
        - .ico
        - .jpeg
        - .jpg
        - .log
        - .nef
        - .orf
        - .png
        - .raw
        - .rtf
        - .sr2
        - .temp
        - .tif
        - .tiff
        - .tmp
        - .txt
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.010
      name: Regsvr32
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: regsvr32_invocation
      description: regsvr32.exe process creation with a non-DLL file argument
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: Decode the command line to identify the loaded file, path, and registration flags
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: module_load
      description: Module/DLL load by regsvr32 shortly after invocation
      mapped_to: Q5
      source: attack:T1218.010.image_load
    - stage: 4
      label: network_retrieval
      description: Optional outbound network connection initiated by regsvr32
      mapped_to: Q6
      source: attack:T1218.010.network_connection
  dimension_coverage:
    - dimension: time_correlation
      technique: T1218.010
      covered_by: Q5
      source: attack:T1218.010.image_load
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.010
      covered_by: Q8
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q18
      source: template:lolbin.historical_lolbin_baseline
      coverage_type: direct
  mutable_dispositions:
    - element: AllowedDLLPaths
      dimension: tuning_parameter
      specific_values: "C:\\Windows\\System32 (expected DLL-load directory)"
      disposition: context
      target_question: Q19
      notes: Expected-path framing carried in renamed_binary_check context (System32/SysWOW64); no fixed allowlist value to inject.
    - element: ScriptletExtensions
      dimension: tuning_parameter
      specific_values: ".sct, .ocx (suspicious extensions for regsvr32)"
      disposition: context
      target_question: Q3
      notes: Rule already enforces a concrete suspicious-extension list in selection_cli; mechanism_decode query carries that exact list, and context names the masquerading-extension angle.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "correlate process creation with subsequent module loads / network connections"
      disposition: new_question
      target_question: Q5
      notes: image_load (Q5, +/-5m) and network_connection (Q6, +/-10m) questions correlate the regsvr32 process with its module load and outbound traffic via ProcessGuid.
    - element: ParentProcessWhitelist
      dimension: parent_process_analysis
      specific_values: "explorer.exe / installers (expected parents)"
      disposition: query_injection
      target_question: Q4
      notes: process_chain (Q4) and baseline_deviation (Q8) surface ParentImage so an unexpected parent for regsvr32 stands out.
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profile unexpected parent-child)"
      disposition: query_injection
      target_question: Q4
      notes: ParentImage projected by process_chain; baseline_deviation aggregates ParentImage over the rule pattern.
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "regsvr32.exe (signed proxy binary)"
      disposition: context
      target_question: Q19
      notes: Rule's binary is regsvr32; renamed_binary_check confirms it ran from its signed system location vs. a relocated copy.
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "suspicious args: URLs, script paths, non-DLL extensions"
      disposition: query_injection
      target_question: Q3
      notes: mechanism_decode query filters CommandLine|endswith to the rule's masquerade-extension list, keeping the question/query alignment honest.
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legit enterprise domains)"
      disposition: context
      target_question: Q6
      notes: network_connection question projects DestinationIp/Port/Hostname so the analyst can compare against known-good destinations; no fixed allowlist injected.
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot)"
      kept: both (Q10, Q11)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot)"
      kept: both (Q13, Q14)
    - dropped: cue_process_tool.clustering
      reason: "host-scoped 'what other tools ran' (Image only) subsumed by cue_host_context.clustering which projects Image+ParentImage for the same window"
      kept: cue_host_context.clustering (Q15)
    - dropped: cue_process_tool.baseline
      reason: "generic host+binary 90d baseline subsumed by the reflected lolbin.historical_lolbin_baseline, which counts the rule's specific firing pattern on this host over 90d"
      kept: lolbin.historical_lolbin_baseline (Q18)
  skipped_templates:
    - template: cue_host_context.precursor
      reason: "tier-3 supporting pattern; prior-alert timeline coverage trimmed to stay within the 14-18 question budget while keeping both mandatory user_actor pairs and all must_include lolbin templates"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' comes from one historical reference's downstream payload, not from this rule's detection. The rule fires on regsvr32 loading a non-DLL file; it has no impact-stage selection. Keeping questions technique-focused per the technique-focused-rule guidance."
      - id: network_scope
        confidence: low
        reason: "Same incidental 'ransomware' keyword source. network_scope's host-spread/lateral angle is already covered technique-appropriately by the reflected scope (Q9) and user clustering questions (Q13/Q14); a generic network-scope set would duplicate without adding precision."
  technique_sets_applied:
    - universal
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      image_load: 1
      network_connection: 1
