name: Control Panel Items
id: f7e5b4dc-8fb2-41ce-9e5d-3e2b5e1f46aa
description: |
  Detects abuse of Control Panel items for code execution and persistence. The rule fires on either of two patterns:
  reg.exe registering a new Control Panel add-in under CurrentVersion\Control Panel\CPLs, or execution of a command line
  ending in .cpl from a non-System32 location (excluding the known igfxCPL.cpl regsvr32 case). Registering a CPL DLL causes
  it to load into explorer.exe when Control Panel opens, providing both proxy execution through a trusted process and a
  persistence foothold; launching an out-of-tree .cpl directly proxies a DLL payload through control.exe or rundll32.exe.
  Determine which branch fired, what registry value or .cpl path is involved, whether the referenced DLL exists and is
  trustworthy, and whether the activity matches authorized administration.
type: detection
detection_id: 0ba863e6-def5-4e50-9cea-4dd8c7dc46a4
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-08'
questions:
- source: template:universal.verify_trigger_match
  question: Did this alert fire on reg.exe registering a Control Panel add-in, or on a .cpl command line running outside System32?
  context: Confirm which branch matched before investing time. One branch is reg.exe adding a value under CurrentVersion\Control
    Panel\CPLs; the other is a command line ending in .cpl that does not resolve to System32 and is not the known igfxCPL.cpl
    regsvr32 case. Note which branch fired — the registry branch points to add-in registration (persistence), while the .cpl
    branch points to direct proxy execution of a DLL, and downstream questions weight differently for each.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to register Control Panel items or run .cpl files from non-standard locations?
  context: Legitimate administrative activity can register CPLs or invoke control panel applets, but it is uncommon outside
    of software installers and vendor tooling. Knowing whether the user role and host function are consistent with control
    panel modification is often the fastest path to separating routine operations from abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Is the parent expected to launch reg.exe or a .cpl execution, and what started the parent?
  context: The parent-child relationship is the core signal. Tracing the parent's own origin reveals whether the chain is
    legitimate (an installer or admin shell) or suspicious (a script host, Office application, or mshta.exe spawning the
    control panel activity). Unexpected parents for reg.exe or for a .cpl invocation indicate the action was driven by
    something other than a user opening Control Panel.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent spawn?
  context: If a suspicious parent spawned the detected activity, it likely spawned other processes too. Examining siblings
    reveals the full scope of activity from that parent — multiple suspicious children from one parent points to scripted
    orchestration rather than an isolated control panel action.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:registry.registry_key_context
  question: What value name and DLL path were written under the Control Panel CPLs key, and does that DLL live in a trusted location?
  context: For the registry branch, the value written under HKCU or HKLM Software\Microsoft\Windows\CurrentVersion\Control
    Panel\CPLs names the DLL that will load into explorer.exe when Control Panel opens. Read the value name and the data
    (the DLL path) directly rather than parsing reg.exe's command line. A DLL under a user-writable directory such as a
    profile, temp, or public folder is far more concerning than a vendor path under Program Files.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session?
  context: An operator who registers a Control Panel add-in often makes several registry changes — adding the CPL value
    plus disabling protections or setting other autorun keys. Finding a cluster of reg.exe write operations from the same
    host within a short window reveals whether multiple objectives were pursued alongside the control panel change.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:registry.registry_value_persistence_check
  question: Does the registered CPL DLL still exist on disk, and what is its hash and signing status?
  context: Control Panel add-in persistence is only effective if the DLL named in the CPLs value is present at that path.
    Confirming the file exists, then analyzing its hash and signature, determines whether the persistence mechanism is live
    and whether the loaded code is trusted. The same check applies to the DLL or .cpl referenced by the direct execution branch.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_reg_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_reg_cli:
        CommandLine|contains|all:
        - add
        - CurrentVersion\Control Panel\CPLs
      selection_cpl:
        CommandLine|endswith: .cpl
      filter_cpl_sys:
        CommandLine|contains:
        - \System32\
        - '%System%'
        - '|C:\Windows\system32|'
      filter_cpl_igfx:
        CommandLine|contains|all:
        - 'regsvr32 '
        - ' /s '
        - igfxCPL.cpl
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_reg_* or (selection_cpl and not 1 of filter_cpl_*)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_reg_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_reg_cli:
        CommandLine|contains|all:
        - add
        - CurrentVersion\Control Panel\CPLs
      selection_cpl:
        CommandLine|endswith: .cpl
      filter_cpl_sys:
        CommandLine|contains:
        - \System32\
        - '%System%'
        - '|C:\Windows\system32|'
      filter_cpl_igfx:
        CommandLine|contains|all:
        - 'regsvr32 '
        - ' /s '
        - igfxCPL.cpl
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_reg_* or (selection_cpl and not 1 of filter_cpl_*)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user triggered similar control panel or registry activity on multiple hosts, the scope of compromise may be
    larger than the single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_reg_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_reg_cli:
        CommandLine|contains|all:
        - add
        - CurrentVersion\Control Panel\CPLs
      selection_cpl:
        CommandLine|endswith: .cpl
      filter_cpl_sys:
        CommandLine|contains:
        - \System32\
        - '%System%'
        - '|C:\Windows\system32|'
      filter_cpl_igfx:
        CommandLine|contains|all:
        - 'regsvr32 '
        - ' /s '
        - igfxCPL.cpl
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_reg_* or (selection_cpl and not 1 of filter_cpl_*)) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or
    credential access — may reveal the attack stages that preceded the control panel activity and help reconstruct the full
    timeline leading to this event.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:lolbin.technique_class_lateral
  question: Were other proxy-execution LOLBins used on this host recently?
  context: Operators who abuse one trusted signed binary to proxy code often reach for several. If a CPL was registered or
    a .cpl was executed to run a DLL, check whether rundll32, regsvr32, mshta, control, or similar proxy-execution binaries
    were also used nearby. Multiple proxy-execution LOLBins performing the same objective strengthen the case for malicious
    activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\control.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\installutil.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\infdefaultinstall.exe'
          - '\msdt.exe'
          - '\mavinject.exe'
          - '\xwizard.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was the alerting binary executed from its expected path?
  context: A trusted binary executed from outside System32 or SysWOW64 may have been copied or renamed to evade path-based
    allowlists, and a .cpl invoked from a user-writable directory is exactly what the execution branch targets. Compare the
    Image path and OriginalFileName against the expected system location.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this Control Panel pattern over the past 90 days, and what command-line structures are typical?
  context: A control panel registration or .cpl execution pattern with no prior history on a host is more suspicious than
    one seen regularly under stable parents. This count reflects the rule's specific firing pattern rather than generic use
    of reg.exe, and the command-line projection lets you compare typical invocation patterns against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_reg_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_reg_cli:
        CommandLine|contains|all:
        - add
        - CurrentVersion\Control Panel\CPLs
      selection_cpl:
        CommandLine|endswith: .cpl
      filter_cpl_sys:
        CommandLine|contains:
        - \System32\
        - '%System%'
        - '|C:\Windows\system32|'
      filter_cpl_igfx:
        CommandLine|contains|all:
        - 'regsvr32 '
        - ' /s '
        - igfxCPL.cpl
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_reg_* or (selection_cpl and not 1 of filter_cpl_*)) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired this Control Panel pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This count mirrors the rule's selection structure with no host scope, so it reflects the rule's firing pattern
    fleet-wide rather than generic reg.exe usage. Many hosts firing the same pattern points to scripted deployment or vendor
    automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_reg_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_reg_cli:
        CommandLine|contains|all:
        - add
        - CurrentVersion\Control Panel\CPLs
      selection_cpl:
        CommandLine|endswith: .cpl
      filter_cpl_sys:
        CommandLine|contains:
        - \System32\
        - '%System%'
        - '|C:\Windows\system32|'
      filter_cpl_igfx:
        CommandLine|contains|all:
        - 'regsvr32 '
        - ' /s '
        - igfxCPL.cpl
      condition: all of selection_reg_* or (selection_cpl and not 1 of filter_cpl_*)
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.002
      name: Control Panel
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
    - id: T1546
      name: Event Triggered Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: cpl_registration_or_execution
      description: "reg.exe registers a CPL add-in under CurrentVersion\\Control Panel\\CPLs, or a .cpl command line executes outside System32"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: registry_value_decode
      description: "Read the CPL registry value name and DLL path (the payload that loads into explorer.exe)"
      mapped_to: Q5
      source: template:registry.registry_key_context
    - stage: 3
      label: payload_on_disk
      description: "Suspicious child execution / DLL payload presence — verify the referenced DLL exists and assess trust"
      mapped_to: Q7
      source: template:registry.registry_value_persistence_check
    - stage: 4
      label: parent_attribution
      description: "Profile the parent that drove the control panel activity (regsvr32/rundll32/control not launched by explorer.exe)"
      mapped_to: Q3
      source: template:parent_child.parent_process_legitimacy
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1218.002
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.002
      covered_by: Q16
      source: template:cue_process_tool.scope
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q5
      source: template:registry.registry_key_context
      coverage_type: direct
    - dimension: time_correlation
      technique: T1546
      covered_by: Q6
      source: template:registry.registry_related_modifications
      coverage_type: direct
    - dimension: user_context
      technique: T1546
      covered_by: Q8
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: CPLPathRegex
      dimension: tuning_parameter
      specific_values: "rationale only (CPL path allowlist tuning)"
      disposition: context
      target_question: Q5
      notes: "Read TargetObject/Details to assess whether the DLL path is user-writable vs vendor; no concrete value to inject"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (regsvr32/rundll32/control not launched by explorer.exe)"
      disposition: context
      target_question: Q3
      notes: "Parent legitimacy question profiles the spawning parent; no fixed allowlist value to inject"
    - element: NewFileTimeWindow
      dimension: tuning_parameter
      specific_values: "rationale only (CPL drop-to-execute delta)"
      disposition: context
      target_question: Q7
      notes: "File-presence check uses +15m range to catch newly dropped DLL; threshold not directly queryable"
    - element: RegistryKeyAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (known-good CPL registry entries)"
      disposition: context
      target_question: Q6
      notes: "Related-registry sweep surfaces other reg writes for allowlist comparison"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (msiexec.exe, regsvr32.exe, etc.)"
      disposition: context
      target_question: Q13
      notes: "Proxy-execution LOLBin sweep enumerates trusted signed binaries used for proxy execution"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (URLs, script paths, DLL entrypoints)"
      disposition: context
      target_question: Q1
      notes: "verify_trigger_match surfaces the full CommandLine for the analyst to assess DLL entrypoint / .cpl path"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (legitimate enterprise domains)"
      disposition: context
      target_question: Q1
      notes: "Rule has no network selection; no network_connection question authored — domain allowlist not applicable"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (SYSTEM/LOCAL SERVICE vs user-initiated)"
      disposition: context
      target_question: Q8
      notes: "User baseline plus service-principal substitute address whether the triggering identity is a service principal"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (trigger-to-execution within ~5 min)"
      disposition: context
      target_question: Q6
      notes: "Related-registry sweep uses +/-30m to correlate trigger definition and surrounding changes"
    - element: PathAnomalyThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (binary path deviation scoring)"
      disposition: context
      target_question: Q14
      notes: "renamed_binary_check compares Image path against expected system location"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_child technique set active — parent_process_legitimacy is more specific and walks the grandparent chain"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. mechanism identity pivots); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. mechanism identity pivots); both emitted"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context (structured TargetObject/Details beats parsing reg.exe CommandLine)"
    - template: cue_process_tool.baseline
      reason: "Superseded by lolbin.historical_lolbin_baseline (REFLECTED, rule-pattern-scoped) and cue_user_actor.baseline; generic binary-frequency count adds no coverage"
    - template: cue_process_tool.baseline_deviation
      reason: "REFLECTED host-pattern parent/user distribution overlaps lolbin.historical_lolbin_baseline (same rule-pattern host scope) and cue_user_actor.baseline_svc parent pivot; dropped to stay within 18-question cap"
    - template: cue_process_tool.follow_on
      reason: "parent_child.sibling_processes and lolbin proxy-execution sweep cover spawned-activity scope; follow_on by Image is broader and less precise"
    - template: cue_process_tool.clustering
      reason: "cue_host_context coverage and registry_related_modifications provide host-window context; generic tool clustering adds no unique angle"
    - template: cue_host_context.clustering
      reason: "registry_related_modifications and cue_host_context.precursor already supply host-window and prior-alert context; dropped to stay within cap"
    - template: universal.endpoint_protection_health
      reason: "cue_host_context.precursor present and rule is persistence/proxy-execution rather than execution/credential-centric, so prior-alert context carries the tamper signal; generic host-tamper sweep not essential here"
    - template: organizational_context.change_ticket
      reason: "Four organizational_context recommended templates would crowd out chain/dimension coverage; user/admin recall and change-window judgment folded into host_user_context and user baseline"
    - template: organizational_context.human_recall
      reason: "Same as change_ticket — organizational judgment covered by host_user_context; recommended-only, dropped to stay within cap"
    - template: organizational_context.timing_alignment
      reason: "Recommended-only; timing judgment subsumed by precursor and related-registry temporal windows"
    - template: organizational_context.procedural_consistency
      reason: "Recommended-only; procedural judgment folded into host_user_context (is this user/host expected to register CPLs)"
    - template: persistence_scope.process_based_persistence
      reason: "registry_related_modifications already sweeps reg/regedit write operations; the rule's persistence vector is the CPL registry value, covered by registry_key_context + registry_value_persistence_check"
    - template: persistence_scope.file_based_persistence
      reason: "registry_value_persistence_check (file_event on the referenced DLL path) covers the on-disk payload; Startup-folder drops are a different mechanism than CPL add-in registration"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218.002) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule fires on process_creation evidence — reg.exe registering the CPLs
        value, or a .cpl command line — not on the subsequent DLL load into
        explorer.exe. The registered DLL's load event is the downstream effect; the
        on-disk DLL identity and trust are surfaced more precisely by
        registry_value_persistence_check (file_event on the referenced path with
        Hashes) than by an image_load volume query against explorer.exe, which would
        return broad load noise without isolating the CPL add-in. No compensating
        attack:T1218.002.image_load question authored.
    - hint: "logsource 'security' (recommended by techniques: T1546) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The security-log analytic for T1546 targets scheduled-task (4698) and
        registry-audit (4657) trigger creation; this rule's persistence vector is a
        Control Panel CPLs registry value written via reg.exe, which is detected and
        investigated through process_creation and registry_set (registry_key_context).
        A security-channel question would chase WMI/scheduled-task triggers this rule
        does not detect. Registry intent is covered by registry_key_context and
        registry_related_modifications.
    - hint: "logsource 'wmi' (recommended by techniques: T1546) is not covered and no template family exists — author a custom question with `category: wmi`"
      disposition: declined
      reason: >-
        The WMI analytic for T1546 covers __EventFilter / CommandLineEventConsumer
        subscriptions, an event-triggered persistence mechanism unrelated to Control
        Panel add-in registration. This rule's detection logic constrains the analysis
        to reg.exe CPL registration and .cpl execution; a WMI-subscription question
        would be off-target for the persistence vector this rule actually detects.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - registry
    - persistence_scope
    - parent_child
    - lolbin
  generation_stats:
    total_questions: 16
    source_counts:
      template: 16
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      registry_set: 1
      file_event: 1
      sigma_alert: 1
