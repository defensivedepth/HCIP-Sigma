name: Suspicious Reg Add BitLocker
id: 95483deb-2baa-42fd-89c2-11fe35d97ced
description: |
  Detects reg.exe adding values under the BitLocker policy key (\SOFTWARE\Policies\Microsoft\FVE)
  using the /v and /f switches together with BitLocker startup-authentication value names
  such as EnableBDEWithNoTPM, UseAdvancedStartup, UseTPM, or RecoveryKeyMessage. Administrators
  and management tooling do legitimately set these policies, so the same command can be routine
  configuration. When it appears outside a change window, it can be the staging step that lets
  an operator turn on full-volume encryption to deny access to data, so the investigation should
  determine who ran it, what other recovery-inhibiting or destructive actions followed, and
  whether the same activity is spreading to other hosts.
type: detection
detection_id: 0e0255bf-2548-47b8-9582-c0955c9283f5
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show reg.exe adding a BitLocker policy value under \SOFTWARE\Policies\Microsoft\FVE with /v and /f?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line.
    The rule fires only when a single reg add command writes to the FVE policy key with the /v and /f switches AND one of the
    BitLocker startup value names is present. Read the full command line and confirm all of those parts are there rather than
    an unrelated reg.exe invocation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to change BitLocker policy via the registry?
  context: Legitimate BitLocker enrollment is normally driven by Group Policy, MDM, or an imaging task, not by an interactive
    reg.exe command from an arbitrary account. Knowing whether this user administers encryption policy and whether this host
    is a management or imaging system is often the fastest way to separate routine configuration from operator staging.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:registry.registry_key_context
  question: Which specific BitLocker policy value was written, and what data was set?
  context: The value name and data reveal intent. EnableBDEWithNoTPM and UseAdvancedStartup allow encryption on machines without
    a TPM or with a PIN/USB key the operator controls, and RecoveryKeyMessage/RecoveryKeyMessageSource can plant attacker-controlled
    recovery text. Read the exact value name and the data written to determine whether the change weakens startup authentication
    or prepares operator-controlled encryption rather than enforcing a standard policy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session?
  context: An operator who edits one BitLocker policy value often edits several, and may also disable security tooling or change
    firewall rules in the same window. Finding the full cluster of reg.exe write operations from this host reveals whether this
    was a single policy tweak or part of a broader configuration change.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:ransomware_impact.recovery_options_assessment
  question: What recovery-inhibiting commands ran on this host?
  context: 'Operators preparing impact chain multiple recovery-inhibiting commands: deleting shadow copies (vssadmin, wmic),
    disabling boot recovery (bcdedit), deleting backups (wbadmin), or disabling the Windows Recovery Environment (reagentc).
    This query surfaces those commands across the host. The analyst should then confirm whether each succeeded by checking
    system state directly (vssadmin list shadows, bcdedit /enum, reagentc /info, wbadmin get versions) and verify offline or
    cloud backup status through the organization''s backup systems.'
  range: +24h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recovery:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\reagentc.exe'
          - '\diskshadow.exe'
      selection_cmdline:
        CommandLine|contains:
          - 'delete shadows'
          - 'shadowcopy delete'
          - 'recoveryenabled'
          - 'bootstatuspolicy'
          - 'delete catalog'
          - 'delete systemstatebackup'
          - 'boot.ini'
          - 'winre.wim'
          - '/disable'
      condition: selection and (selection_recovery or selection_cmdline)
    fields:
      - Image
      - CommandLine
- source: template:ransomware_impact.destruction_scope_on_host
  question: What other destructive or recovery-inhibiting actions occurred on this host?
  context: 'Impact operations typically chain several destructive actions: deleting shadow copies AND disabling recovery AND
    stopping security services AND clearing event logs. Finding the complete set of destructive commands, and whether they
    share one scripting parent, reveals whether this is an automated destruction sequence rather than an isolated administrative
    change.'
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_destructive:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\cipher.exe'
          - '\fsutil.exe'
          - '\icacls.exe'
          - '\takeown.exe'
          - '\attrib.exe'
          - '\sc.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\powershell.exe'
          - '\taskkill.exe'
      condition: selection and selection_destructive
    fields:
      - Image
      - User
- source: attack:T1486.file_event
  question: Are files being created with unusual extensions or ransom-note names on this host after the policy change?
  context: Full-volume encryption staging is often followed within minutes by file encryption or ransom-note drops. This query
    watches file creation on the host for non-standard or randomly generated extensions and for filenames that look like ransom
    notes, concentrated in user document folders, network shares, or system paths. A burst of such writes in a short window
    indicates encryption has started; the value of this check is the file extension and target folder, not the process that
    wrote them.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_ransom_note:
        TargetFilename|contains:
          - 'readme'
          - 'how_to'
          - 'how-to'
          - 'decrypt'
          - 'recover'
          - 'restore-my'
          - 'ransom'
      condition: selection and selection_ransom_note
    fields:
      - TargetFilename
      - Image
- source: template:ransomware_impact.network_wide_impact
  question: Is this same BitLocker policy activity firing on other hosts in the environment?
  context: Impact operators often push the same encryption-staging command across many hosts in rapid succession. Finding this
    same rule firing on multiple hosts indicates network-wide execution and sets the scope for containment and incident response.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.uuid|expand: '%rule.uuid%'
      condition: selection
    fields:
      - host.name
      - rule.name
      - event.severity_label
- source: template:cue_process_tool.process_chain
  question: What process spawned this reg.exe instance and is that parent chain expected?
  context: The parent process reveals how the command was launched. Group Policy or imaging-driven BitLocker policy changes
    have predictable service or management parents. An interactive shell, a script host, or an Office application as the parent
    points to hands-on-keyboard activity or macro-driven execution rather than managed configuration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - REG
        - ADD
        - \SOFTWARE\Policies\Microsoft\FVE
        - /v
        - /f
        CommandLine|contains:
        - EnableBDEWithNoTPM
        - UseAdvancedStartup
        - UseTPM
        - UseTPMKey
        - UseTPMKeyPIN
        - RecoveryKeyMessageSource
        - UseTPMPIN
        - RecoveryKeyMessage
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - REG
        - ADD
        - \SOFTWARE\Policies\Microsoft\FVE
        - /v
        - /f
        CommandLine|contains:
        - EnableBDEWithNoTPM
        - UseAdvancedStartup
        - UseTPM
        - UseTPMKey
        - UseTPMKeyPIN
        - RecoveryKeyMessageSource
        - UseTPMPIN
        - RecoveryKeyMessage
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user staged similar encryption-policy activity on several hosts, the scope of the operation may be much larger
    than this one alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - REG
        - ADD
        - \SOFTWARE\Policies\Microsoft\FVE
        - /v
        - /f
        CommandLine|contains:
        - EnableBDEWithNoTPM
        - UseAdvancedStartup
        - UseTPM
        - UseTPMKey
        - UseTPMKeyPIN
        - RecoveryKeyMessageSource
        - UseTPMPIN
        - RecoveryKeyMessage
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this BitLocker policy pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even routine policy changes can be abused. The query mirrors the rule's selection scoped to this host, so the resulting
    parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process,
    an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - REG
        - ADD
        - \SOFTWARE\Policies\Microsoft\FVE
        - /v
        - /f
        CommandLine|contains:
        - EnableBDEWithNoTPM
        - UseAdvancedStartup
        - UseTPM
        - UseTPMKey
        - UseTPMKeyPIN
        - RecoveryKeyMessageSource
        - UseTPMPIN
        - RecoveryKeyMessage
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this BitLocker policy pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of this firing pattern
    rather than generic reg.exe usage. Many hosts firing the same pattern points to scripted deployment or management automation;
    one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - REG
        - ADD
        - \SOFTWARE\Policies\Microsoft\FVE
        - /v
        - /f
        CommandLine|contains:
        - EnableBDEWithNoTPM
        - UseAdvancedStartup
        - UseTPM
        - UseTPMKey
        - UseTPMKeyPIN
        - RecoveryKeyMessageSource
        - UseTPMPIN
        - RecoveryKeyMessage
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host, such as defense tampering, reconnaissance,
    or credential access, may reveal the attack stages that preceded this event and help reconstruct the timeline leading to
    encryption staging.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1486
      name: Data Encrypted for Impact
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: encryption_config_tampering
      description: "Actor writes BitLocker policy values via reg.exe to enable or weaken full-volume encryption startup authentication"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 1b
      label: registry_value_decode
      description: "Decode the exact FVE value name and data written to determine what the policy change does"
      mapped_to: Q3
      source: template:registry.registry_key_context
    - stage: 2
      label: recovery_inhibition
      description: "Shadow copy deletion, boot-recovery disabling, backup deletion preceding encryption"
      mapped_to: Q5
      source: template:ransomware_impact.recovery_options_assessment
    - stage: 3
      label: file_encryption_and_ransom_note
      description: "High-frequency file writes with uncommon extensions and ransom-note creation"
      mapped_to: Q7
      source: attack:T1486.file_event
    - stage: 4
      label: network_wide_execution
      description: "Same encryption-staging activity executed across multiple hosts"
      mapped_to: Q8
      source: template:ransomware_impact.network_wide_impact
  dimension_coverage:
    - dimension: command_analysis
      technique: T1486
      covered_by: Q5
      source: template:ransomware_impact.recovery_options_assessment
      coverage_type: direct
    - dimension: network_indicators
      technique: T1486
      covered_by: Q8
      source: template:ransomware_impact.network_wide_impact
      coverage_type: indirect
    - dimension: time_correlation
      technique: T1486
      covered_by: Q7
      source: attack:T1486.file_event
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1486
      covered_by: Q7
      source: attack:T1486.file_event
      coverage_type: direct
    - dimension: user_context
      technique: T1486
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: FileExtension
      dimension: tuning_parameter
      specific_values: "non-standard or randomly generated file extensions"
      disposition: new_question
      target_question: Q7
      notes: "file_event question watches file creation for ransom-note/encrypted-output naming; extension is the surfaced field"
    - element: TargetFolder
      dimension: tuning_parameter
      specific_values: "user document folders, network shares, system paths like %System32%"
      disposition: context
      target_question: Q7
      notes: "Folder concentration described in Q7 context; query surfaces TargetFilename so analyst sees the folder"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rapid writes/renames within seconds across high file count"
      disposition: context
      target_question: Q7
      notes: "Burst-in-short-window framing in Q7 context; +30m forward range captures the encryption phase"
    - element: CommandLine
      dimension: command_analysis
      specific_values: "vssadmin delete shadows /all /quiet and similar recovery-inhibiting commands"
      disposition: query_injection
      target_question: Q5
      notes: "recovery_options_assessment KEEP_EXACT query already enumerates vssadmin/bcdedit/wbadmin and delete shadows patterns"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by registry.registry_key_context per brief hint — registry_set fields give the value/data directly instead of parsing reg.exe CommandLine"
      kept: registry.registry_key_context
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pairing
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism identity)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pairing
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism identity)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context"
    - template: cue_process_tool.baseline
      reason: "Image|endswith %Image% baseline on reg.exe is too noisy (reg.exe is ubiquitous) and conflicts with the CommandLine-only-scoping hint; the BitLocker-pattern baseline is covered by the user-pivot baseline (Q11) and the reflected host_pattern question (Q16)"
    - template: cue_process_tool.follow_on
      reason: "reg.exe writes a value and exits; it does not spawn children, so a child-process query of reg.exe returns nothing useful. Recovery/destruction follow-on is covered by Q5 and Q6"
    - template: cue_process_tool.clustering
      reason: "host-wide process clustering subsumed by destruction_scope_on_host (Q6) and host_context.precursor (Q15) which target the relevant follow-on activity for an impact rule"
    - template: cue_host_context.clustering
      reason: "generic host-activity aggregation subsumed by destruction_scope_on_host (Q6) for this impact rule"
    - template: network_scope.same_action_other_hosts
      reason: "cross-host same-action coverage provided by ransomware_impact.network_wide_impact (Q8); the network_scope Image-only variant is less precise for a CommandLine-only rule"
    - template: network_scope.actor_activity_network_wide
      reason: "duplicates cue_user_actor.clustering (Q13) — user activity across all hosts in the alert window"
    - template: network_scope.actor_authentication_trail
      reason: "authentication-trail pivot is lateral-movement focused; for a local encryption-staging rule the relevant cross-host signal is the same-pattern fleet count (Q16) and network_wide_impact (Q8). Declined to keep the set impact-focused"
  reflection_overrides: []
  hint_dispositions:
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: declined
      reason: >-
        Acted on for the scoping-sensitive baseline (Q11 user baseline rewritten to scope on
        CommandLine|contains|all FVE policy pattern instead of Image, and the reflected host_pattern/fleet_pattern
        questions Q16/Q17 already embed the rule's CommandLine selection). The remaining %Image%/%ParentName%
        placeholders that survive (Q12 baseline_svc, Q14 clustering_svc) are alert-derived launch-mechanism pivots,
        not detection-scoping clauses, and they hydrate reg.exe from the alert event; recorded here as a partial
        decline because those two service-principal pivots intentionally keep Image to answer the launch-mechanism
        question the pair exists to answer.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - registry
    - ransomware_impact
    - network_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      registry_set: 1
      file_event: 1
      sigma_alert: 2
