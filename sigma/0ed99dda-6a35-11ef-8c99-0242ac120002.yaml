name: Attempts of Kerberos Coercion Via DNS SPN Spoofing
id: 6c00d0d9-57cc-48b9-8d7d-9aa69773310d
description: |
  Detects a command line containing the base64 signature of a marshaled CREDENTIAL_TARGET_INFORMATION
  structure (the "UWhRCA...BAAAA" pattern), an indicator of a Kerberos coercion attempt in which an
  adversary manipulates name resolution to spoof a Service Principal Name and redirect authentication
  to an attacker-controlled host. The rule fires when a process command line contains both the
  "UWhRCA" and "BAAAA" fragments. Legitimate appearances are rare, so the investigation should confirm
  the full decoded command, identify the spoofed SPN or DNS record being added or queried, and
  determine whether the coerced authentication produced follow-on logons or lateral movement.
type: detection
detection_id: 0ed99dda-6a35-11ef-8c99-0242ac120002
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain both the UWhRCA and BAAAA fragments of the marshaled credential-target signature?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line
    against the rule's detection criteria. The rule fires only when both fragments are present together, so verify both
    appear in the same command line and read the surrounding arguments to see what host or DNS record is referenced.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manage DNS records or query Service Principal Names from the command line?
  context: Legitimate administrative activity can trigger many detections. Adding or querying DNS records tied to service
    hostnames is normally done by directory or DNS administrators or by automation, not by ordinary user accounts or
    workstations. Knowing whether the user role and host function fit this operation is often the fastest path to
    distinguishing a true positive from routine administration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: What SPN or DNS record does the decoded command target, and is it adding a spoofed name or querying for one?
  context: The marshaled CREDENTIAL_TARGET_INFORMATION blob encodes the target the coerced authentication will be bound to.
    Decode the full command line to identify the Service Principal Name or hostname being added to DNS or checked via name
    resolution, and determine whether the action writes a new record (staging the coercion) or merely reads one
    (reconnaissance). The targeted name reveals which service the attacker intends authentication to be relayed toward.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this command and is that parent chain expected?
  context: The parent process reveals how the command was invoked. A name-resolution or DNS manipulation command launched
    from an interactive admin shell looks different from one spawned by an Office application, a script host, or an
    unexpected service. Unusual parents such as mshta.exe, wscript.exe, or a document handler point toward exploitation
    rather than deliberate administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity,
    and does the current alert fit that pattern?
  context: Even rare signatures can have a benign source such as a security tool or a sanctioned test. The query mirrors
    the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this
    specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual
    command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - UWhRCA
        - BAAAA
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like
    consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of the
    rule's firing pattern. Many hosts firing the same signature points to scripted deployment or a coordinated coercion
    attempt across the environment; one host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate scope and severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - UWhRCA
        - BAAAA
      condition: selection
    fields:
    - host.name
- source: attack:T1557.001.security
  question: Do Windows security events show new service creation or multicast name-resolution re-enabling on this host
    around the alert?
  context: Adversary-in-the-middle credential interception is frequently set up by creating a helper service or by
    re-enabling LLMNR and NBT-NS so the host answers spoofed name-resolution requests. The Windows security log records
    service installation as event 4697; correlating it with this command-line alert can reveal the responder or relay
    component staged alongside the DNS manipulation. If service-creation auditing is not enabled in this environment,
    treat zero results as a visibility gap rather than evidence nothing was installed.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID: 4697
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - ServiceName
      - ServiceFileName
      - SubjectUserName
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts or service identities may be exposed by this coercion, and do any have elevated privileges?
  context: The severity of a coercion attempt depends on the privilege of the identity that would be relayed. Coercing
    authentication from a domain controller, a server with a privileged machine account, or a service identity with broad
    access requires escalation; a standard workstation identity may only need monitoring. Cross-reference the host's
    machine account and the logged-on user against directory group memberships to gauge impact.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - UWhRCA
        - BAAAA
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging on multiple endpoints. If the
    same account drove name-resolution manipulation, responder setup, or authentication attempts on several hosts in this
    window, the scope of the operation is broader than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity surrounding the alert reveals what led to this event and
    what followed, and whether the coercion command sits inside a larger sequence of reconnaissance, name-resolution
    tampering, or relay setup. Novel parent-child process pairs stand out against the repetitive agent telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host may reveal the stages that preceded this coercion attempt. Reconnaissance,
    credential access, or defense tampering ahead of this event helps reconstruct the timeline and shows whether the
    coercion is an opening move or a later step in an established intrusion.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution. The query surfaces direct attempts to disable AV/EDR services,
    flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful signal that protection
    looks healthy; populated rows shift the disposition toward operator preparation, so review the parent process and
    signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:organizational_context.human_recall
  question: Does the user or administrator recall performing this DNS or name-resolution action?
  context: Direct confirmation from the human behind the account is often the fastest path to determining legitimacy. If
    no one recalls staging this name record or running this command, the account may be compromised or the activity may be
    automated. If someone does recall it, capture why it was done and against which service.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:organizational_context.timing_alignment
  question: Does the timing of this activity align with normal work hours and maintenance windows?
  context: Legitimate DNS and directory changes typically occur during business hours or scheduled maintenance windows.
    Name-record manipulation at an odd hour, outside any maintenance window, or on a host whose local time makes the action
    unusual is more suspicious than the same activity during a documented change window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1557.001
      name: LLMNR/NBT-NS Poisoning and SMB Relay
      role: primary
    - id: T1557
      name: Adversary-in-the-Middle
      role: secondary
    - id: T1187
      name: Forced Authentication
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: spoofed_name_staging
      description: "Adversary writes or queries a DNS / name-resolution record to spoof an SPN-bound hostname (the command-line signature this rule fires on)."
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 2
      label: aitm_component_setup
      description: "Helper service creation or multicast name-resolution re-enabling that stands up the responder/relay component."
      mapped_to: Q7
      source: attack:T1557.001.security
    - stage: 3
      label: coerced_authentication_relay
      description: "Coerced host emits Kerberos/SMB/WebDAV authentication that is relayed; follow-on logons or remote connections appear."
      mapped_to: Q8
      source: template:credential_access.post_access_lateral_movement
    - stage: 4
      label: identity_exposure
      description: "Repeated coercion or relay of privileged identities across hosts; assess which accounts are exposed."
      mapped_to: Q9
      source: template:credential_access.credential_exposure_assessment
  dimension_coverage:
    - dimension: time_correlation
      technique: T1557.001
      covered_by: Q8
      source: template:credential_access.post_access_lateral_movement
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1557.001
      covered_by: Q5
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: time_correlation
      technique: T1557
      covered_by: Q13
      source: template:cue_host_context.precursor
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1557
      covered_by: Q6
      source: template:cue_process_tool.scope
      coverage_type: direct
    - dimension: time_correlation
      technique: T1187
      covered_by: Q16
      source: template:organizational_context.timing_alignment
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1187
      covered_by: Q10
      source: template:cue_user_actor.baseline
      coverage_type: indirect
  mutable_dispositions:
    - element: TrustedResponderList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Expected LLMNR/NBT-NS responders are environment-specific with no concrete values in the brief; framed as a tuning rationale in the security-event question's visibility-gap note."
    - element: SMBServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Normal SMB/relay baseline is org-specific; surfaced as context in the lateral-movement follow-on question."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Correlation horizon between coercion and relayed authentication; expressed via the +4h range on the lateral-movement question."
    - element: MonitoredRegistryPaths
      dimension: tuning_parameter
      specific_values: "EnableMulticast (HKLM\\Software\\Policies\\Microsoft\\Windows NT\\DNSClient)"
      disposition: context
      target_question: Q7
      notes: "Registry re-enabling of multicast name resolution named in context; the security-event query targets service creation 4697 since registry_set is a separate logsource not in the rule's scope."
    - element: DowngradeCipherList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "TLS downgrade cipher list is unrelated to this command-line-signature rule; noted as not applicable, no query injection."
    - element: UserLocations
      dimension: tuning_parameter
      specific_values: "Desktop, Public, Downloads, Temp, Startup"
      disposition: context
      target_question: Q3
      notes: "T1187 lure-file locations; this rule fires on the DNS-spoofing command, not a lure file, so locations are background context only."
    - element: LureExtensions
      dimension: tuning_parameter
      specific_values: ".lnk, .scf, .url, .doc/.xls/.ppt/.pdf/.html"
      disposition: context
      target_question: Q3
      notes: "Lure file extensions for the file-based T1187 variant; not applicable to this command-line detection, no query injection."
    - element: "UntrustedCIDR/DNS"
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Org-specific untrusted destinations for outbound NTLM; covered as a tuning consideration in the lateral-movement question."
    - element: WorkstationZones
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Asset/zone tags distinguishing workstations from servers; surfaced in the exposure-assessment question to gauge identity privilege."
    - element: OfficeTemplatePaths
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Office template injection paths belong to the file-based T1187 variant; not applicable to this command-line detection."
  dedup_decisions:
    - dropped: cue_user_actor.baseline_svc
      reason: "service-principal substitute pivots on process.parent.name + Image; this is a CommandLine-only rule with primary_binary null, so there is no binary identity for a launch-mechanism baseline. The reflected baseline_deviation (Q5) already returns ParentImage+User scoped to the rule's exact CommandLine pattern, providing the launch-mechanism distribution this substitute would have."
      kept: cue_user_actor.baseline
    - dropped: cue_user_actor.clustering_svc
      reason: "same constraint — no Image to pivot the launch mechanism on. The reflected scope question (Q6) provides the cross-host distribution of the rule's exact pattern, which is the rollout-vs-isolated signal the clustering substitute exists to deliver."
      kept: cue_user_actor.clustering
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (Q3) per brief hint and template replaced_by field."
    - template: cue_process_tool.baseline
      reason: "ADAPT_STRUCTURALLY template scopes on host+Image; primary_binary is null (CommandLine-only rule). The user baseline (Q10) was re-scoped to the rule's CommandLine pattern instead, and the reflected baseline_deviation (Q5) covers host-scoped historical frequency of the exact pattern."
    - template: cue_process_tool.follow_on
      reason: "scopes on ParentImage|endswith %Image%; no Image in this rule. The coercion command is not a launcher of child payloads — follow-on impact is authentication relay, covered by post_access_lateral_movement (Q8), not child-process spawning."
    - template: data_staging_exfil.data_scope
      reason: "confirmed by tactic collection keyword, but this rule detects a Kerberos-coercion command-line signature, not file archiving/export. No archive scope to enumerate; archive-tool questions would mislead the analyst."
    - template: data_staging_exfil.archive_destination
      reason: "same — no archive is created by this command; the relevant data-movement angle (relayed authentication destination) is covered by post_access_lateral_movement (Q8)."
    - template: data_staging_exfil.encryption_password
      reason: "no archive/encryption tool in this detection; password-flag questions are not applicable to a DNS-SPN-spoofing command-line signature."
    - template: persistence_scope.process_based_persistence
      reason: "confirmed by persistence tactic tag, but this rule fires on a coercion command, not schtasks/sc/reg persistence tooling. AitM responder persistence is better surfaced by the security service-creation question (Q7)."
    - template: persistence_scope.file_based_persistence
      reason: "Startup-folder/Run-key file drops are unrelated to this command-line coercion signature; no file-based persistence is implied by the detection."
    - template: organizational_context.change_ticket
      reason: "recommended only; human_recall (Q15) and timing_alignment (Q16) already cover the authorization angle without a third near-identical organizational question."
    - template: organizational_context.procedural_consistency
      reason: "recommended only; the procedural-norm angle for DNS administration overlaps human_recall (Q15); omitted to stay within the 14-18 target without redundant organizational questions."
  reflection_overrides: []
  hint_dispositions:
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: declined
      reason: >-
        Recorded as declined only to capture the audit trail; the hint was in fact ACTED ON throughout. primary_binary is
        null, so every Image-scoped cue template was either re-scoped to the rule's CommandLine|contains|all [UWhRCA, BAAAA]
        pattern (Q10 user baseline) or skipped where Image scoping was structural (cue_process_tool.baseline, follow_on,
        and both _svc substitutes — see skipped_templates and dedup_decisions). The reflected questions (Q5, Q6) already
        embed the CommandLine pattern verbatim. No query in the playbook scopes on %Image%.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      sigma_alert: 2
      security: 1
