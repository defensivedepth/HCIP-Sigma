name: Remote Access Tool - AnyDesk Silent Installation
id: b67a9552-7191-43cb-8435-fc0ec60faf50
description: |
  Detects a command line that performs a silent, unattended AnyDesk installation by combining the --install, --start-with-win, and --silent flags together. The same automated-deployment primitive that IT teams use for mass rollout is reused by operators to drop a remote access tool as a hands-off persistence backdoor: --start-with-win registers the AnyDesk service to auto-start at boot, and a silent install leaves no interactive prompt. Investigation should determine whether the install is a sanctioned deployment or an unauthorized remote-access foothold by checking the full command line, the launch chain, approval records, the resulting service and outbound connections, and the cross-host spread of the same pattern.
type: detection
detection_id: 114e7f1c-f137-48c8-8f54-3088c24ce4b9
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-04
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain the silent-install flags this rule detects (--install, --start-with-win, and --silent together)?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line against the rule's detection criteria. The rule fires only when all three flags appear together, which is the unattended-deployment primitive rather than an interactive install.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to deploy remote access software?
  context: Legitimate AnyDesk deployment is usually performed by IT staff or automated software-distribution tooling on managed endpoints. A silent install initiated by a standard user, a developer workstation, or a server with no support role is a stronger signal of unauthorized installation than the same action on an admin's deployment host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What does the full AnyDesk command line do beyond the three detected flags?
  context: The detected flags confirm a silent autostart install, but the full command line often carries more — an install directory in a user-writable or temp path, --create-shortcuts/--create-desktop-icon, or a piped --set-password and --register-licence that establish an attacker-known credential and license. Decode every argument and the install destination to understand the complete operation, including any sequential AnyDesk invocations on this host that together form one deployment.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
          - '--install'
          - '--start-with-win'
          - '--silent'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:remote_access_tool.tool_authorization
  question: Is AnyDesk on the organization's approved remote access software list?
  context: Organizations typically sanction a single remote support tool for IT use. AnyDesk not appearing on the approved list -- even though it is well-known commercial software -- may represent shadow IT or operator-deployed persistence. An unauthorized remote access tool should be reviewed regardless of the product's reputation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a current helpdesk ticket, change record, or support session that explains this AnyDesk install?
  context: Legitimate remote access tool deployment usually correlates with an active support ticket, a scheduled maintenance window, or a documented rollout. If no support or change context exists for this silent install, the tool may have been planted by an operator for persistent access or by a user circumventing controls.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned the AnyDesk installer, and is that parent chain expected for a software deployment?
  context: The parent process reveals how the install was launched. Sanctioned deployment is typically driven by software-distribution agents, services, or an admin shell. A silent install spawned from a script wrapper such as cmd.exe or powershell.exe running a batch deployment, from an Office application, or from a browser download directory points to operator tradecraft rather than managed rollout.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1219.tuning_parameter
  question: Was an AnyDesk auto-start service created on this host around the time of the install?
  context: The --start-with-win flag registers AnyDesk to launch at boot, which surfaces as a new service installation (System event 7045) and is the persistence side-effect that turns a remote tool into a standing backdoor. A service created from a user-writable or temp install path, or one not matching a sanctioned deployment, indicates the install is intended to survive reboots for ongoing access.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 7045
      condition: selection
    fields:
      - ServiceName
      - ImagePath
      - ServiceType
      - AccountName
- source: attack:T1219.002.file_event
  question: What AnyDesk files and install-directory artifacts were written to this host?
  context: A silent install drops the AnyDesk binary and configuration into an install directory and per-user/program-data paths. Files written to a user-writable or temporary location rather than the standard program-files path, or configuration written immediately before an outbound connection, distinguish an operator drop from a managed deployment and reveal residual artifacts left behind by the install.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains: 'AnyDesk'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:remote_access_tool.connection_destination
  question: What external endpoints did AnyDesk connect to after the install?
  context: Remote access tools establish outbound sessions to relay servers and cloud infrastructure. Connections to AnyDesk's own relay infrastructure are consistent with normal operation, while connections to unfamiliar addresses or non-standard high ports suggest a custom relay or attacker-controlled endpoint. The presence of any outbound session confirms the installed tool is actively reachable.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|contains: 'AnyDesk'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:cue_process_tool.follow_on
  question: What child processes did the AnyDesk install or service spawn afterward?
  context: A silent deployment script often chains additional steps after the install -- piping a password via echo into --set-password, registering a license, or launching a shell. Child processes such as cmd.exe or powershell.exe spawned in the same window reveal whether this was a single managed install or part of a scripted backdoor setup.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|contains: 'AnyDesk'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - --install
        - --start-with-win
        - --silent
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - --install
        - --start-with-win
        - --silent
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of this install?
  context: Tracking the user's activity across all hosts reveals whether this is an isolated deployment or part of broader hands-on activity. If the same user installed remote access software or ran administrative tooling on multiple endpoints in the same window, the scope of access being established may be larger than one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - --install
        - --start-with-win
        - --silent
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired the silent-install pattern, what parents and users drove it, and does the current alert fit?
  context: Even a sanctioned tool can be abused. This query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific silent-install pattern has historically run here. A new parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - --install
        - --start-with-win
        - --silent
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this silent-install pattern in the past week, and does the cross-host spread look like automated rollout or one-off activity?
  context: This query mirrors the rule's selection with no host scope, returning a fleet-wide count of the silent-install pattern. Many hosts firing the same pattern points to scripted or vendor deployment, while one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - --install
        - --start-with-win
        - --silent
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this install?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or credential access -- may reveal the stages that preceded an operator deploying a remote access foothold and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219.002
      name: Remote Desktop Software
      role: primary
    - id: T1219
      name: Remote Access Tools
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: agent_execution
      description: "Initial execution of the remote-control agent under user context (silent AnyDesk install)"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: persistence_service
      description: "Persistence via service or autorun (--start-with-win registers AnyDesk auto-start service, System EID 7045)"
      mapped_to: Q7
      source: attack:T1219.tuning_parameter
    - stage: 3
      label: outbound_session
      description: "Long-lived outbound connection/tunnel to external infrastructure"
      mapped_to: Q9
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: interactive_control
      description: "Interactive control signals such as shell or file-manager child processes spawned by the RAT parent"
      mapped_to: Q10
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: time_correlation
      technique: T1219.002
      covered_by: Q7
      source: attack:T1219.tuning_parameter
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219.002
      covered_by: Q8
      source: attack:T1219.002.file_event
      coverage_type: direct
    - dimension: process_identification
      technique: T1219
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1219
      covered_by: Q15
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219
      covered_by: Q9
      source: template:remote_access_tool.connection_destination
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: Image
      dimension: tuning_parameter
      specific_values: "AnyDesk binaries (ConnectWise, Zoho Assist named as variants)"
      disposition: context
      target_question: Q4
      notes: "Rule is CommandLine-only with no Image field; AnyDesk-specific framing kept in tool_authorization and connection_destination context. Other RMM binaries are out of scope for this rule's firing pattern."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "configurable/random high ports (7070, 5650)"
      disposition: query_injection
      target_question: Q9
      notes: "connection_destination query projects DestinationPort so non-standard high ports surface; context notes high-port relays."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only (parent varies by enterprise context)"
      disposition: new_question
      target_question: Q6
      notes: "Covered by process_chain question on parent of the installer."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (default 15m, adjust per environment)"
      disposition: context
      target_question: Q7
      notes: "Install-to-service and install-to-beacon correlation framed in service (+/-1h) and connection (+/-15m) ranges."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (help-desk/jump/admin vs standard endpoints)"
      disposition: context
      target_question: Q11
      notes: "User-role differentiation framed in user baseline and host_user_context."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only (known-good remote support tools)"
      disposition: context
      target_question: Q4
      notes: "Approved-software-list check carries the allowlist concept; deviations chased via baseline_deviation."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "rationale only (user-writable or temp install paths)"
      disposition: context
      target_question: Q8
      notes: "file_event question surfaces install-directory artifacts and flags user-writable/temp paths; CurrentDirectory also projected in mechanism_decode."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (vendor support clouds/CDNs)"
      disposition: context
      target_question: Q9
      notes: "connection_destination context distinguishes vendor relay infra from unfamiliar endpoints."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only (acceptable vs suspicious child shells from GUI parents)"
      disposition: context
      target_question: Q10
      notes: "follow_on question surfaces child shells (cmd.exe/powershell.exe) spawned by AnyDesk parent."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only (session duration, connection counts, bytes ratio)"
      disposition: context
      target_question: Q9
      notes: "Thresholds are not directly queryable; outbound-session framing in connection_destination context."
  dedup_decisions:
    - dropped: null
      reason: "user_actor pair — non-deduplicable (baseline + baseline_svc, clustering + clustering_svc both emitted per AGENTS.md service-principal rule)"
      kept: cue_user_actor.baseline, cue_user_actor.baseline_svc, cue_user_actor.clustering, cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "Host-frequency baseline of the binary is subsumed by the reflected baseline_deviation (Q15), which scopes the rule's actual silent-install pattern to this host rather than generic binary usage. CommandLine-only rule has no Image to baseline cleanly."
    - template: cue_process_tool.clustering
      reason: "Host-wide tool clustering overlaps cue_host_context.clustering; precursor (Q17) plus user-clustering provide the surrounding-activity view. Dropped to stay within the 14-18 question budget."
    - template: cue_host_context.clustering
      reason: "Surrounding-host activity overlaps user clustering (Q13) and precursor alert correlation (Q17); dropped to stay within question budget while keeping the host timeline via precursor."
  reflection_overrides: []
  hint_dispositions:
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted-on
      reason: >-
        ADAPT_STRUCTURALLY templates that defaulted to Image|endswith|expand
        '%Image%' scoping (mechanism_decode Q3, user baseline Q11, baseline_svc
        Q12, clustering_svc Q14) were rewritten to CommandLine|contains|all of
        the rule's three flags (--install, --start-with-win, --silent). Reflected
        templates (Q16, Q17/scope) already carry the CommandLine pattern verbatim.
        Network/file/follow-on queries use Image|contains 'AnyDesk' /
        ParentImage|contains 'AnyDesk' / TargetFilename|contains 'AnyDesk' for
        cross-logsource pivots since no Image placeholder is available.
    - hint: "logsource 'file_event' (recommended by techniques: T1219.002) is not covered — pull in cue_artifact.* or author a custom question with `category: file_event`"
      disposition: acted-on
      reason: >-
        Authored Q8 (attack:T1219.002.file_event) with category: file_event,
        scoping host.name + TargetFilename|contains 'AnyDesk' to surface
        install-directory artifacts and residual files, covering the
        tuning_parameter (InstallPathRegex) dimension via file evidence.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      file_event: 1
      network_connection: 1
      system: 1
      sigma_alert: 1
