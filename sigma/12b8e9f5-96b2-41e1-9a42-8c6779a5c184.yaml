name: Potentially Suspicious Execution Of PDQDeployRunner
id: 0e9433f3-8231-4864-bb0d-f3c2b59523e3
description: |
  Detects PDQDeployRunner (the component of the PDQ Deploy software-deployment stack that runs commands and packages on remote machines) spawning a child that either has a name in a set of commonly abused interpreters and LOLBins, or runs from a user-writable staging directory, or carries a command line with download, encoding, or in-memory execution markers. Legitimately, PDQDeploy pushes installers and maintenance scripts fleet-wide, so administrative use can match this pattern. Abused, the same deployment channel becomes a remote-execution and lateral-movement mechanism for dropping and running tooling across many hosts at once. Determine whether the spawned command is expected deployment content for this environment, decode any encoded or download-bearing arguments, and check whether the same parent-child pattern is spreading across hosts.
type: detection
detection_id: 12b8e9f5-96b2-41e1-9a42-8c6779a5c184
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did PDQDeployRunner spawn a child that matches one of the rule's suspicious branches?
  context: >-
    Confirm the alert before investing time. The rule fires when the parent Image contains
    "\PDQDeployRunner-" AND the child matches at least one branch — its name ends in an
    abused interpreter or LOLBin (bash.exe, certutil.exe, cmd.exe, csc.exe, cscript.exe,
    dllhost.exe, mshta.exe, msiexec.exe, regsvr32.exe, rundll32.exe, scriptrunner.exe,
    wmic.exe, wscript.exe, wsl.exe), OR it runs from a staging path (ProgramData, Users\Public,
    Windows\TEMP, AppData\Local\Temp), OR the command line carries a download/encoding/in-memory
    marker (-decode, -enc, -encodedcommand, -w hidden, DownloadString, FromBase64String, http,
    iex, Invoke-). Note which branch fired — that determines which downstream questions carry the
    most weight.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to receive deployment-driven command execution from PDQDeployRunner?
  context: >-
    Deployment tooling normally runs under a service account on managed endpoints, pushing
    installers and scripts. If the host is in scope for PDQ Deploy and the spawned command is
    routine maintenance content, the alert is likely benign. An unmanaged host, an unexpected
    target, or a command that does not resemble packaged software is the deviation worth chasing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the spawned tool do? Decode its full command line.
  context: >-
    The child's command line reveals the operation pushed through the deployment channel.
    Parse every flag, path, and argument; resolve any download URL, decode base64 or hex content,
    and identify what the interpreter or LOLBin was instructed to run. Multi-step tools may issue
    several sequential invocations on the host within the window, so review the full set rather
    than the single triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:obfuscation.encoded_payload_decode
  question: Is the spawned command line encoded or obfuscated? What does it decode to, and what does the decoded content do?
  context: >-
    Several of the rule's markers point at hidden payloads — certutil -decode of a staged file,
    PowerShell -enc/-encodedcommand base64, FromBase64String reconstruction, DownloadString /
    iex download cradles, and -w hidden execution. Decode the content to its readable form and
    identify the actual intent: a download cradle, an in-memory loader, a reverse shell, or
    credential or data handling. For certutil, identify the encoded source file and the decoded
    output path; iterate decoding when the payload is layered.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:parent_child.parent_process_legitimacy
  question: What started the PDQDeployRunner parent instance, and is that origin expected?
  context: >-
    The parent-child relationship is the core signal here. PDQDeployRunner is normally launched
    by the PDQ Deploy service stack on the managed endpoint. Tracing the parent's own origin
    (the grandparent) confirms whether the chain is the expected deployment service or an
    unexpected launcher standing in for it.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did this same PDQDeployRunner instance spawn?
  context: >-
    A deployment push that runs one suspicious command often runs several in sequence. Listing the
    other children of this exact parent instance reveals the full set of commands executed through
    this deployment, distinguishing a single installer step from a chain of tooling staged and run
    together.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Does PDQDeployRunner on this host normally spawn this child binary?
  context: >-
    PDQ Deploy routinely spawns installer engines (msiexec.exe) and packaged scripts, so some
    parent-child pairings are expected on a managed host. A child binary this parent has never
    or rarely spawned here — an interpreter or LOLBin outside the host's normal deployment
    content — is the discriminator between routine deployment and abuse of the channel.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is this PDQDeployRunner-to-child spawning pattern occurring on other hosts?
  context: >-
    Deployment tooling pushes to many endpoints at once, so the same parent-child pattern landing
    on multiple hosts can be a legitimate fleet-wide rollout — or a single operation distributing
    tooling across the environment through the deployment channel. Cross-host distribution helps
    separate routine packaged deployment from a coordinated push, and shows the scope.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run the spawned binary at all?
  context: >-
    Independent of the deployment parent, a child binary that has never run on this host is more
    notable than one that runs regularly. Zero or very low historical counts indicate first-time
    or rare execution of this interpreter or LOLBin on the endpoint.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Does the account that owns this execution normally run this binary?
  context: >-
    Compare the owning account's current action against its history. A deployment service account
    has a narrow, repetitive toolset; an interactive account that has never run this interpreter
    or LOLBin is a stronger deviation. Align the account's role with the activity it triggered.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: >-
    When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism — which parent spawned this binary, and whether
    that pair is historically common. A new or rare parent for this binary, here or environment-wide,
    is the deviation; a pair that already accounts for many prior events is established deployment
    automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this account do across the network around this time?
  context: >-
    Tracking the owning account's activity across all hosts in the window reveals whether the
    execution is isolated or part of broader movement. The same account triggering similar activity
    on multiple endpoints widens the likely scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+binary combination fire on other hosts in the same window — a coordinated rollout, or isolated to this host?
  context: >-
    Under a service principal, "what else did this account do across the network" returns
    agent-telemetry noise. The meaningful question is whether the same launch mechanism — the
    PDQDeployRunner parent spawning this binary — appeared on other hosts in the alert window.
    Multiple hosts at once points to scheduled or pushed deployment; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining what ran on the host just before and after the
    deployment-driven execution reveals the immediate context — what staged it, what followed, and
    whether reconnaissance, privilege escalation, or persistence accompanied it.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host — defense tampering, reconnaissance, credential access —
    may reveal the stages that preceded a deployment-channel execution and help reconstruct the
    timeline leading to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: >-
    Operators commonly weaken protection before running tooling. The query surfaces direct attempts
    to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch
    AMSI/ETW. Zero rows is a useful "protection looks healthy" signal — kernel-level driver tampering
    and in-memory patches will not appear here. Populated rows shift the disposition toward operator
    preparation; review the writing process and its signing chain.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:persistence_scope.process_based_persistence
  question: Were process-based persistence mechanisms installed on this host around the same time?
  context: >-
    A deployment channel that runs tooling on a remote host can also be used to plant persistence.
    After the detected execution, check for scheduled tasks, services, registry Run-key writes,
    BITS jobs, or WMI subscriptions installed in the same timeframe. Several persistence
    installations clustered together indicate intentional stacking rather than an isolated
    administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: false
  techniques: []
  attack_chain_coverage: []
  dimension_coverage: []
  mutable_dispositions: []
  dedup_decisions:
    - dropped: cue_parent_process.mechanism_decode
      reason: "parent_child technique set active — parent_child.parent_process_legitimacy is more specific and supersedes the generic parent mechanism_decode (AGENTS.md §4)"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "redundant with parent_child.sibling_processes — sibling_processes scopes to the exact ParentProcessGuid instance (more precise) and covers 'what else did this parent spawn'"
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "near-identical query to cue_host_context.clustering (host + parent activity, +/-15m); host_context.clustering covers around-the-time host activity"
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.baseline (paired with baseline_svc)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering (paired with clustering_svc)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain (parent cue subsumes it; parent_child.parent_process_legitimacy + sibling_processes cover the parent chain)"
    - template: persistence_scope.file_based_persistence
      reason: "recommended-priority; rule is execution-focused (deployment tool spawning interpreters/LOLBins), not file persistence. process_based_persistence already covers persistence stacking after the execution; file_event sweep would add length without a load-bearing angle for this rule"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - obfuscation
    - parent_child
    - persistence_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 17
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 16
      sigma_alert: 1
