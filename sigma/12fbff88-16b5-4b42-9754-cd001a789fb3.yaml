name: CodePage Modification Via MODE.COM To Russian Language
id: d5fa50e9-c7d8-4f51-a417-169cc10ffa75
description: |
  Detects use of the mode.com utility to change the console code page to a Cyrillic
  encoding (1251 or 866) via the "con cp select=" subcommand. The rule fires when the
  process is mode.com (by Image suffix or OriginalFileName) AND the command line contains
  ' con ', ' cp ', and ' select=' AND ends with =1251 or =866.
  Setting the console code page to a Cyrillic value is sometimes done by ransomware staging
  scripts so operator-supplied or ransom-note text renders correctly, and the change often
  sits next to shadow-copy deletion and persistence writes; it can also be a legitimate action
  by a Russian-speaking user adjusting their console encoding. Determine whether the code-page
  change is an isolated user action or part of a destructive sequence on the host.
type: detection
detection_id: 12fbff88-16b5-4b42-9754-cd001a789fb3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-04'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show mode.com setting the console code page to a Cyrillic value?
  context: Confirm the alert is a true positive by reviewing the exact command line and process
    details against the rule's criteria — the process is mode.com (by path or embedded
    OriginalFileName) and the command line carries ' con ', ' cp ', ' select=' and ends in
    =1251 or =866. Note which Cyrillic code page was selected, as 1251 is the Windows ANSI
    Cyrillic value and 866 is the OEM Russian value.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user/host expected to change the console code page to a Cyrillic encoding?
  context: A console-locale change to Cyrillic on a system whose installed locale is something
    else is the anomaly. If the user is a Russian-speaking operator working in a Cyrillic
    console, this can be routine; if the host's locale and the user's role have no Cyrillic
    relationship, the change is harder to explain and warrants tracing the surrounding activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this mode.com invocation do? Decode the code-page selection and any adjacent arguments.
  context: The full command line reveals which code page was selected and how the command was
    formed. Decode the select= value and confirm it targets a Cyrillic page (1251 ANSI or 866
    OEM). Because a single staging script may issue mode.com more than once, this scopes to all
    mode.com executions on the host in a short window so multiple sequential invocations that form
    one logical operation are visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - ' select='
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:masquerading.true_identity
  question: What is the actual identity of this binary based on its metadata and hash?
  context: mode.com is a Microsoft-signed system binary, so the embedded OriginalFileName should
    read MODE.COM and the Image should sit under the system directory. Compare the Image path on
    disk against OriginalFileName to confirm this is the real utility and not a renamed binary
    masquerading as it, and submit the file hash for reputation checking to rule out a replaced
    or trojanized copy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:masquerading.binary_origin
  question: How did this mode.com binary arrive on the host, and is it running from the expected system directory?
  context: The legitimate utility ships with the OS under the system directory. If a file named
    mode.com was written to a user-writable location (Temp, AppData, Downloads, Desktop) or
    replaced in place, the file-creation event names the process that dropped it — a download tool,
    an archive extractor, a remote-admin copy, or a dropper. A binary in its canonical system path
    points to the genuine OS tool rather than a planted copy.
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_name%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:masquerading.renamed_binary_activity
  question: What did mode.com spawn after execution, and is the same invocation present on other hosts?
  context: A console code-page change is a preparatory step rather than an end in itself. Examine
    any child processes of mode.com to see what followed, and check whether the same code-page
    invocation appears on other hosts, which would suggest a scripted rollout rather than an
    isolated action.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned mode.com and is that parent chain expected?
  context: The parent process reveals how the code-page change was driven. A cmd.exe parent that
    issued the command as part of a batch script, especially one spawned in turn from an
    unexpected process, differs sharply from an interactive shell a user typed the command into.
    Walk up the chain to see what launched the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering and recovery-inhibition steps often precede or accompany impact activity. The
    query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad
    exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful signal that protection looks
    healthy; populated rows shift the disposition toward operator preparation, so review the parent
    process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: attack:T1036.follow_on_impact
  question: Did shadow-copy deletion or recovery-inhibition commands run on this host near the code-page change?
  context: A Cyrillic console code-page change is sometimes issued by a script immediately before
    recovery-inhibition steps such as deleting volume shadow copies, disabling boot recovery, or
    clearing the backup catalog. Finding these commands in the same window on the same host moves
    the disposition from a benign locale change toward pre-encryption staging and is a high-value
    pivot for prioritization.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_recovery:
        - CommandLine|contains|all:
            - 'vssadmin'
            - 'delete'
            - 'shadows'
        - CommandLine|contains|all:
            - 'wmic'
            - 'shadowcopy'
            - 'delete'
        - CommandLine|contains:
            - 'bcdedit'
            - 'wbadmin delete'
            - 'Delete-ShadowCopy'
            - 'recoveryenabled no'
      condition: selection_host and selection_recovery
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: template:cue_process_tool.baseline
  question: Does this host normally run mode.com to change the console code page?
  context: A code-page change via mode.com that has never been seen on this host is more suspicious
    than one that recurs as part of routine operations. Zero or very low historical counts indicate
    first-time or rare execution of this pattern on the host.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - ' select='
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drove it, and does the current alert fit?
  context: Even a routine utility can be abused. This query mirrors the rule's selection structure
    scoped to this host, so the resulting parent/user distribution reflects how this specific
    code-page-change pattern (not generic mode.com usage) has historically run here. A new parent
    process, an unexpected user account, or an unusual command structure is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \mode.com
      - OriginalFileName: MODE.COM
      selection_cli:
        CommandLine|contains|all:
        - ' con '
        - ' cp '
        - ' select='
        CommandLine|endswith:
        - =1251
        - =866
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of this code-page-change pattern (not generic mode.com usage). Many hosts firing
    the same pattern points to scripted deployment or imaging; one host or a small unrelated set
    points to manual operator activity. Use the host distribution and parent consistency to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \mode.com
      - OriginalFileName: MODE.COM
      selection_cli:
        CommandLine|contains|all:
        - ' con '
        - ' cp '
        - ' select='
        CommandLine|endswith:
        - =1251
        - =866
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \mode.com
      - OriginalFileName: MODE.COM
      selection_cli:
        CommandLine|contains|all:
        - ' con '
        - ' cp '
        - ' select='
        CommandLine|endswith:
        - =1251
        - =866
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \mode.com
      - OriginalFileName: MODE.COM
      selection_cli:
        CommandLine|contains|all:
        - ' con '
        - ' cp '
        - ' select='
        CommandLine|endswith:
        - =1251
        - =866
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated
    action across multiple endpoints. If the same user triggered similar activity on other hosts,
    the scope of the event may be larger than a single console-locale change.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \mode.com
      - OriginalFileName: MODE.COM
      selection_cli:
        CommandLine|contains|all:
        - ' con '
        - ' cp '
        - ' select='
        CommandLine|endswith:
        - =1251
        - =866
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the code-page change reveals
    the immediate context: what led to it, what happened after, and whether it is part of a larger
    sequence. Look for reconnaissance, persistence writes to the Startup folder, or file-encryption
    activity surrounding the event.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential
    access — may reveal the stages that preceded this event and help reconstruct the full timeline.
    A code-page change that sits at the tail of a chain of prior alerts reads very differently from
    one that fires with no surrounding detections.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1036
      name: Masquerading
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: binary_identity_or_rename
      description: "Adversary deploys or invokes a binary (here the signed mode.com) whose name/metadata is checked against its real identity to spot masquerading"
      mapped_to: Q4
      source: template:masquerading.true_identity
    - stage: 2
      label: file_creation_origin
      description: "File creation of the binary precedes execution; trace how/where the binary landed"
      mapped_to: Q5
      source: template:masquerading.binary_origin
    - stage: 3
      label: execution_inconsistent_with_usage
      description: "Execution (the code-page change) inconsistent with the system's installed locale and followed by further activity"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 4
      label: follow_on_impact
      description: "Code-page change followed by recovery-inhibition / impact staging in the same window"
      mapped_to: Q9
      source: attack:T1036.follow_on_impact
  dimension_coverage:
    - dimension: execution_context
      technique: T1036
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1036
      covered_by: Q9
      source: attack:T1036.follow_on_impact
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1036
      covered_by: Q4
      source: template:masquerading.true_identity
      coverage_type: direct
  mutable_dispositions:
    - element: OriginalFilenameMismatch
      dimension: tuning_parameter
      specific_values: "Compare Image basename vs PE OriginalFilename (MODE.COM)"
      disposition: query_injection
      target_question: Q4
      notes: "true_identity (KEEP_EXACT) projects Image + OriginalFileName so the analyst can compare on-disk name against embedded metadata; context names the MODE.COM expected value."
    - element: KnownSystemUtilityPaths
      dimension: tuning_parameter
      specific_values: "Expected system-directory install path for mode.com"
      disposition: context
      target_question: Q5
      notes: "binary_origin question/context calls out the canonical system path vs user-writable drop locations; baseline (Q10) groups by user to surface unusual hosts."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "Correlation window between code-page change and follow-on/sibling activity"
      disposition: query_injection
      target_question: Q9
      notes: "follow_on_impact uses +/-15m host-scoped window to correlate the code-page change with recovery-inhibition commands; host_context.clustering (Q17) and renamed_binary_activity (Q6, +4h) provide additional temporal correlation."
  dedup_decisions:
    - dropped: cue_process_tool.follow_on
      reason: "child-process-of-binary query (ParentImage|endswith %Image%) is subsumed by masquerading.renamed_binary_activity (Q6), which is the more specific technique-set version of the same query"
      kept: masquerading.renamed_binary_activity
    - dropped: cue_process_tool.clustering
      reason: "host-scoped 'what else ran around this time' duplicates cue_host_context.clustering (Q17); kept the host_context version"
      kept: cue_host_context.clustering
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline (Q13) + baseline_svc (Q14) and clustering (Q15) + clustering_svc (Q16) pivot on subject vs launch-mechanism identity and degenerate under different conditions; both emitted per AGENTS.md"
      kept: cue_user_actor.baseline / baseline_svc / clustering / clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: medium
        reason: "Keyword match on 'ransomware' in description/refs is real, but the rule itself fires only on the code-page change, not on encryption/impact behavior. Rather than pull in the full ransomware_impact set, the single highest-value impact pivot (recovery-inhibition / shadow-copy deletion) is covered by the authored attack:T1036.follow_on_impact question (Q9), keeping the playbook centered on what the rule detects."
      - id: network_scope
        confidence: medium
        reason: "Triggered by the same incidental 'ransomware' keyword. The rule has no network indicator and process_creation provides no destination data; cross-host scope is already covered by scope (Q12), clustering_svc (Q16), and renamed_binary_activity (Q6). A network_scope set would add no convertible signal for this process-creation, command-line-only rule."
  technique_sets_applied:
    - universal
    - masquerading
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      file_event: 1
      sigma_alert: 1
