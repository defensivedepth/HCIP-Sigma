name: Use of UltraVNC Remote Access Software
id: 19dd4791-c837-4b60-ad26-686560a3860d
description: |
  Detects execution of the UltraVNC VNCViewer client, identified by its embedded
  binary metadata (Description, Product, Company, or OriginalFileName). Remote access
  tools like VNC are used legitimately for IT support and administration, but the same
  software can give an operator an interactive command-and-control channel into a host.
  The investigation should determine whether the tool is approved for this environment,
  whether a support session or ticket explains it, where it connects, and whether it
  was dropped to disk and run as part of a larger remote-access chain.
type: detection
detection_id: 145322e4-0fd3-486b-81ca-9addc75736d8
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the process metadata match UltraVNC VNCViewer — Description VNCViewer, Product UltraVNC VNCViewer, Company UltraVNC, or OriginalFileName VNCViewer.exe?
  context: The rule fires when any one of these embedded metadata fields identifies the binary as UltraVNC VNCViewer, so a single matching field is enough. Confirm the alert is a true positive by reviewing the event's metadata and noting which field matched, since a renamed-on-disk binary still carries its original VNCViewer.exe metadata.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - OriginalFileName
      - Company
      - Description
      - Product
- source: template:universal.host_user_context
  question: Is this user and host expected to run VNC remote access software?
  context: Legitimate VNC usage is common on IT-support hosts, jump boxes, and admin workstations and rare on standard user endpoints. Knowing whether the user role and host function are consistent with running a remote-access client is often the fastest path to separating sanctioned support activity from unauthorized access.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:remote_access_tool.tool_authorization
  question: Is UltraVNC on the organization's approved remote access software list?
  context: Organizations typically sanction a specific remote-access tool for IT support and treat anything else as shadow IT or attacker-deployed access. Even well-known software like UltraVNC should be checked against the approved list regardless of its reputation, since the tool's legitimacy depends on whether this environment authorized it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a current helpdesk ticket or support session explaining this VNC access?
  context: Legitimate remote-access usage usually correlates with an active support ticket, a scheduled maintenance window, or a user request. If no support context exists for this session, the tool may have been installed by an operator for persistent interactive access or by a user circumventing controls.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the VNCViewer command invoke? Decode the command-line arguments, target host, and any connection or password flags.
  context: The full command line reveals what the viewer was told to do — which host or display it connects to, whether a saved connection file or stored credentials were passed, and whether listen mode was enabled to accept inbound sessions. Decode each argument to understand the direction and target of the remote session.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned VNCViewer and is that parent chain expected?
  context: The parent process reveals how the tool was launched. Interactive use typically shows explorer.exe or a shell; a service or autorun parent points to persistence, and script-host or Office-application parents point to scripted or macro-driven deployment. Note what spawned the parent and what else the parent spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:remote_access_tool.connection_destination
  question: What external endpoint is VNCViewer connecting to?
  context: VNC clients establish outbound sessions to the host or relay they control. The destination reveals whether the connection targets an internal host being administered, an expected support relay, or an unfamiliar external address. Connections to arbitrary external IPs, or non-standard high ports outside the usual VNC range, raise the likelihood of an attacker-controlled endpoint.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: attack:T1219.002.install_artifact
  question: Was the VNCViewer binary recently written to disk on this host, and where did it land?
  context: Remote-access tooling is frequently dropped to disk shortly before it runs, and the write location separates sanctioned deployment from operator staging. A binary installed under a standard program directory by a known installer differs from one written to a user-writable or temp path. Correlate the file-write time and writer against the execution time to establish the install-to-run sequence.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith: '\vncviewer.exe'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.follow_on
  question: What child processes did VNCViewer spawn afterward?
  context: An interactive remote session is often used to launch further activity. Child processes such as cmd.exe, powershell.exe, or file-transfer utilities spawned by the viewer indicate hands-on-keyboard control rather than a passive viewing session and mark where post-access activity begins.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run VNC remote access software?
  context: Compare this user's current action against their historical behavior. A user who has never run a VNC client is more suspicious than one who uses it routinely as part of an IT-support role. The user's role and typical toolset should align with running remote-access software.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-VNCViewer launch combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute the behavior. The meaningful pivot is the launch mechanism — which parent process spawned the viewer, and whether that pair is historically common. A new or rare parent for this binary is the deviation worth chasing; a pair that already accounts for thousands of prior events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar remote-access activity on several hosts, the scope of the access may be wider than this one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-VNCViewer combination fire on other hosts in the same window — a coordinated rollout, or isolated to this host?
  context: Under a service principal, asking what the user did network-wide returns agent-telemetry noise. The meaningful question is whether the same launch mechanism — parent process plus spawned binary — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed deployment; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.baseline
  question: Does this host normally run VNCViewer?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. Zero or very low historical counts over the trailing window indicate first-time or rare execution of the remote-access tool on this endpoint.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the VNCViewer metadata pattern, what parents and users normally drive that activity, and does the current alert fit?
  context: Even sanctioned remote-access tools can be abused. The query mirrors the rule's metadata selection scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual launch shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Description: VNCViewer
      - Product: UltraVNC VNCViewer
      - Company: UltraVNC
      - OriginalFileName: VNCViewer.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to the remote session and what followed it — reconnaissance, privilege escalation, persistence installation, or data staging that frames the broader context. Novel parent-child pairs stand out against high-count routine telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the VNCViewer metadata pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's metadata selection with no host scope, giving a fleet-wide count of this specific firing pattern rather than generic use of the binary. Many hosts firing the same pattern points to scripted or vendor deployment; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Description: VNCViewer
      - Product: UltraVNC VNCViewer
      - Company: UltraVNC
      - OriginalFileName: VNCViewer.exe
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the stages that preceded the remote session and help reconstruct the timeline. The sequence of alerts on a host often tells the operational story leading up to interactive access.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219.002
      name: Remote Desktop Software
      role: primary
    - id: T1219
      name: Remote Access Tools
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tool_execution
      description: "Initial execution of the remote-control/assist client under user context"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: install_artifact
      description: "RMM/RAT binary written to disk prior to execution (install-to-run)"
      mapped_to: Q8
      source: attack:T1219.002.install_artifact
    - stage: 3
      label: outbound_session
      description: "Long-lived outbound connection / remote session to external infrastructure"
      mapped_to: Q7
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: interactive_control
      description: "Interactive control signals — shell or file-manager children spawned by the RAT parent"
      mapped_to: Q9
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: time_correlation
      technique: T1219.002
      covered_by: Q8
      source: attack:T1219.002.install_artifact
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219.002
      covered_by: Q7
      source: template:remote_access_tool.connection_destination
      coverage_type: direct
    - dimension: process_identification
      technique: T1219
      covered_by: Q11
      source: template:cue_user_actor.baseline_svc
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q10
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: Image
      dimension: tuning_parameter
      specific_values: "rationale only (e.g., ConnectWise, Zoho Assist — other RMM binaries)"
      disposition: context
      target_question: Q3
      notes: "Rule is tool-specific (UltraVNC); other RMM binaries are out of this rule's scope. Authorization framing in tool_authorization carries the 'check against approved list regardless of tool' intent."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "configurable / random high ports (e.g., 7070, 5650)"
      disposition: context
      target_question: Q7
      notes: "connection_destination query is KEEP_EXACT and already projects DestinationPort; non-standard high-port note added to context."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only — expected parent varies by enterprise context"
      disposition: query_injection
      target_question: Q11
      notes: "Parent-as-launch-mechanism pivot is the spine of baseline_svc/clustering_svc and process_chain."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — correlation window (default 15m, adjust per env)"
      disposition: context
      target_question: Q8
      notes: "install-to-run correlation framed in context; -24h file_event window plus +/-15m host clustering bracket the chain timing."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — differentiate help-desk/jump/admin from standard endpoints"
      disposition: context
      target_question: Q2
      notes: "host_user_context and user baseline carry the role-vs-endpoint differentiation."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only — known-good remote support tools"
      disposition: context
      target_question: Q3
      notes: "Approved-software-list check in tool_authorization is the allowlist disposition."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "rationale only — alert on execution from user-writable/temp paths"
      disposition: context
      target_question: Q8
      notes: "install_artifact context directs analyst to separate standard program-dir installs from user-writable/temp drops."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only — vendor support clouds/CDNs"
      disposition: context
      target_question: Q7
      notes: "connection_destination context distinguishes expected relay infra from unfamiliar external addresses."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only — acceptable vs suspicious child shells from GUI parent"
      disposition: context
      target_question: Q9
      notes: "follow_on surfaces children of the viewer; context flags shells/transfer utilities as hands-on-keyboard."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only — session duration/connection-count/byte-ratio thresholds"
      disposition: context
      target_question: Q7
      notes: "Egress-shape rationale; no per-event byte fields queryable from process_creation/network_connection selection here."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Within-phase overlap with cue_host_context.clustering (both 'what else ran on this host around the same time'). Kept host_context version — groups by Image+ParentImage for richer novel-pair surfacing."
      kept: cue_host_context.clustering
    - dropped: ""
      reason: "user_actor pair — non-deduplicable. cue_user_actor.baseline (Q10) + baseline_svc (Q11) emitted as a pair; user-pivot and parent+binary substitute pivot on different identity dimensions."
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: ""
      reason: "user_actor pair — non-deduplicable. cue_user_actor.clustering (Q12) + clustering_svc (Q13) emitted as a pair; user-pivot degenerates to agent-telemetry noise under a service principal, substitute pivots on launch mechanism."
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. Rule is command-and-control (remote-access tool execution), not execution/credential-access/impact, so tamper-as-precursor is not the disposition driver. cue_host_context.precursor (Q18) already surfaces prior tamper/defense-evasion alerts on the host. defense_tampering set is not confirmed. Dropped to stay within the 18-question cap."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      file_event: 1
      sigma_alert: 1
