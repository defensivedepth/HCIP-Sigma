name: Potentially Suspicious Child Process Of WinRAR.EXE
id: 50b15b50-7523-4abc-9680-06a368d24828
description: |
  Detects WinRAR.exe spawning a command interpreter or script host — cmd.exe, cscript.exe,
  mshta.exe, powershell.exe, pwsh.exe, regsvr32.exe, rundll32.exe, or wscript.exe — identified
  by either the child Image name or its OriginalFileName. An archive utility launching an
  interpreter is a strong sign that opening or extracting an archive triggered code execution,
  the pattern behind archive-handling exploitation such as the extension-spoofing flaw where a
  crafted archive runs a same-named script instead of the decoy the user clicked. The same
  spawn can also be benign — a self-extracting archive or an installer packaged in an archive.
  The investigation should confirm the child binary and parent are what the rule matched,
  decode what the child executed, recover the archive that was opened, and determine whether
  the activity is isolated or part of a wider delivery wave.
type: detection
detection_id: 146aace8-9bd6-42ba-be7a-0070d8027b76
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting event match the rule — WinRAR.exe as the parent spawning one of the listed interpreters or script hosts?
  context: Before investing investigation time, confirm the true positive. Check that ParentImage
    ends in \WinRAR.exe and the child is one of cmd.exe, cscript.exe, mshta.exe, powershell.exe,
    pwsh.exe, regsvr32.exe, rundll32.exe, or wscript.exe — matched on either the Image name or
    its OriginalFileName, which catches a renamed binary. Note which interpreter fired, since
    that determines which downstream decode and baseline questions carry the most weight.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to extract archives that launch interpreters?
  context: Developers, IT staff, and power users routinely open self-extracting archives and
    installers that legitimately spawn helpers. The same spawn from a general user's workstation,
    triggered by an archive received over email or downloaded from the web, is far more concerning.
    Knowing the account role and the host's function is often the fastest way to separate routine
    extraction from delivery-driven execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the spawned interpreter run? Decode its full command line and working directory.
  context: The child's command line is where the payload lives. Decode any flags, encoded blobs,
    file paths, or URLs to learn what the interpreter actually executed — a download cradle, an
    in-memory loader, a registered COM object via rundll32, or a script read from a temp path.
    The working directory frequently points back at the extraction folder the archive landed in.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: If the child was PowerShell, was the command encoded or obfuscated, and what does it decode to?
  context: When the interpreter is powershell.exe or pwsh.exe, attackers frequently hide intent
    behind -EncodedCommand (-enc), string concatenation, backtick escaping, or variable
    substitution. Base64-encoded commands are the most common form. Decoding reveals the actual
    payload — a download cradle, a reverse shell, or in-memory execution of a .NET assembly. If
    the child is not PowerShell, this decode does not apply and the command-line decode of the
    spawned interpreter is the relevant artifact.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:parent_child.parent_process_legitimacy
  question: What started WinRAR itself, and was it opening an archive interactively or driven by another process?
  context: The parent-child relationship is the core signal. Tracing WinRAR's own origin shows
    whether a user double-clicked an archive in a mail client or browser download folder, or
    whether a script or loader launched WinRAR non-interactively. The grandparent process and
    WinRAR's command line — which often names the archive path — distinguish a user opening a
    file from automated extraction in an attack chain.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same WinRAR instance spawn?
  context: A crafted archive can drop and launch several stages. Examining every child of this
    exact WinRAR instance reveals the full scope of what the extraction set in motion — multiple
    interpreters or helpers from the same parent points to orchestrated execution rather than an
    isolated spawn.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:initial_access.delivery_vector
  question: What archive was opened, and how did it reach this host?
  context: Archive-handling exploitation begins with a crafted archive delivered by email,
    download, or chat. WinRAR's command line and the parent that launched it usually name the
    archive path; from there, determine whether it arrived as a mail attachment, a browser
    download, or a copied file. Identifying the archive and its source enables blocking it and
    searching for other recipients who received the same file.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: attack:T1203.application_crash
  question: Did WinRAR crash or exit abnormally around the time it spawned the interpreter?
  context: Exploitation for client execution often manifests as the client application crashing
    or terminating abnormally as the malformed input is processed, immediately before or after an
    unexpected child is launched. An application-error event for WinRAR.exe close to the spawn
    time supports memory-corruption or processing-error exploitation rather than a benign
    self-extractor; its absence does not rule out logic-flaw exploitation such as extension
    spoofing, which executes cleanly.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: application
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Channel: Application
      filter:
        - Provider_Name|contains: 'Error'
        - Data|contains: 'WinRAR'
      condition: selection and filter
    fields:
      - Provider_Name
      - Data
- source: attack:T1203.file_path_analysis
  question: What files did the spawned interpreter write or read in user-writable locations?
  context: Archive exploitation drops payloads and decoys into writable paths — the extraction
    folder, %TEMP%, %APPDATA%, Downloads, or OneDrive — and the interpreter then reads or executes
    them. Reviewing file events tied to the interpreter recovers the dropped script, loader, or
    same-named decoy that the extension-spoofing pattern relies on, and shows where the payload
    landed for containment.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1203.network_egress
  question: Did the spawned interpreter make outbound network connections shortly after launch?
  context: Successful client-execution exploitation commonly fetches a second stage or beacons to
    command-and-control within a short window of the child spawning. Connections initiated by the
    interpreter to external addresses — especially soon after extraction and to destinations not
    on known update or content-delivery infrastructure — point to payload retrieval or C2 rather
    than benign extraction.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        Initiated: 'true'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:initial_access.campaign_scope
  question: Did this same detection fire on other hosts or for other users?
  context: A crafted archive distributed to many recipients rarely lands on a single host. If one
    user opened a weaponized archive, others may have received the same file. Correlating this
    detection across the environment reveals the breadth of the delivery and identifies additional
    hosts that need investigation.
  range: -24h
  query: |
    aggregation: true
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.uuid|expand: '%rule.uuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:initial_access.application_patch_status
  question: Is the WinRAR installation on this host up to date?
  context: Archive-handling exploitation often abuses a known vulnerability in the archive utility
    itself, such as the extension-spoofing code-execution flaw (CVE-2023-38831). An outdated WinRAR
    build confirms a known exploit was possible and flags other hosts on the same version as at
    risk; a current build shifts weight toward a self-extracting archive, a packaged installer, or
    user-driven extraction of a malicious script.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Operators frequently disable or weaken protection before running a payload delivered
    through an archive. This surfaces direct attempts to stop AV/EDR services, flip Defender flags,
    add exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy"
    signal — kernel-level driver tampering and in-memory patches will not appear here. Populated
    rows shift the disposition toward operator preparation; review the writer's parent process and
    signing chain.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_parent_process.baseline
  question: Does WinRAR normally spawn this interpreter on this host?
  context: WinRAR launching a command interpreter or script host is rare in most environments;
    when it happens routinely it is usually a specific self-extracting installer the user runs
    regularly. Historical frequency of this exact WinRAR-to-interpreter pairing on the host
    distinguishes an established, benign extraction workflow from a first-time spawn worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run this interpreter?
  context: Compare the account's current action against its history. A user who has never run this
    interpreter is more suspicious than one who does so as part of their role. Zero or very low
    counts across hosts indicate the user does not normally use this tool; align the result with
    what is known about the account's function.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-and-binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal (SYSTEM, LOCAL SERVICE, NETWORK SERVICE,
    or a computer account), user identity does not attribute the behavior. The meaningful pivot is
    the launch mechanism — that WinRAR.exe spawned this interpreter — and whether that pair is
    historically common. A new or rare WinRAR-to-interpreter pair is the deviation worth chasing;
    a pair already accounting for thousands of events is established automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the account's activity across all hosts in the surrounding window reveals
    lateral movement or coordinated activity on multiple endpoints. If the same user triggered
    similar archive-extraction or interpreter activity elsewhere, the scope is larger than this
    one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-and-binary combination fire on other hosts in the same window, or is it isolated to this host?
  context: Under a service principal, "what else did this user do" returns agent-telemetry noise
    because every host runs SYSTEM-owned agents constantly. The meaningful question is whether the
    same launch mechanism — WinRAR.exe spawning this interpreter — fired on other hosts in the
    alert window. Multiple hosts point to a delivery wave or pushed automation; a single host
    points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1203
      name: Exploitation for Client Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: client_app_abnormal
      description: "Client app (WinRAR) processes crafted input and crashes/exits abnormally or otherwise mishandles the archive"
      mapped_to: Q8
      source: attack:T1203.application_crash
    - stage: 2
      label: file_drop
      description: "Payload/decoy files dropped or read in user-writable paths during extraction"
      mapped_to: Q9
      source: attack:T1203.file_path_analysis
    - stage: 3
      label: unexpected_child
      description: "WinRAR spawns an unexpected interpreter/script host (the detection point)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 4
      label: c2_egress
      description: "Spawned interpreter establishes outbound C2-like connections shortly after"
      mapped_to: Q10
      source: attack:T1203.network_egress
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1203
      covered_by: Q9
      source: attack:T1203.file_path_analysis
      coverage_type: direct
    - dimension: time_correlation
      technique: T1203
      covered_by: Q10
      source: attack:T1203.network_egress
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1203
      covered_by: Q1
      source: template:universal.verify_trigger_match
      coverage_type: partial
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "~15m correlation window between crash / write / child / network"
      disposition: query_injection
      target_question: Q10
      notes: "The 15m correlation window is applied directly as the range on the application_crash (+/-15m), file_path_analysis (+/-15m), and network_egress (+/-15m) questions that bracket the spawn, keeping the crash/write/child/network chain inside the analytic's window."
    - element: HighRiskChildren
      dimension: tuning_parameter
      specific_values: "powershell.exe, cmd.exe, wscript.exe, mshta.exe, rundll32.exe, regsvr32.exe, cscript.exe, pwsh.exe"
      disposition: context
      target_question: Q1
      notes: "The rule's selection_binaries IS this high-risk-child list; verify_trigger_match (Q1) enumerates exactly these eight interpreters, so the tuning value is already enforced by the rule's own detection. No separate query needed."
    - element: UserPaths
      dimension: file_path_analysis
      specific_values: "Downloads, %TEMP%, %APPDATA%, OneDrive, extraction folder"
      disposition: context
      target_question: Q9
      notes: "Writable-path list named in the file_path_analysis context; the query scopes file events to the spawned interpreter's Image rather than enumerating paths, so the analyst sees every write/read the child performed without a brittle path allowlist."
    - element: AllowedPlugins
      dimension: tuning_parameter
      specific_values: "rationale only (known add-ins / updater binaries to suppress)"
      disposition: context
      target_question: Q14
      notes: "Suppression rationale, not a queryable value for this rule. cue_parent_process.baseline frames the routine-vs-novel WinRAR-to-interpreter pairing that this allowlist concept addresses."
    - element: EgressAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (known update/CDN domains, proxy egress CIDRs)"
      disposition: context
      target_question: Q10
      notes: "Suppression rationale. network_egress context directs the analyst to discount destinations on known update/content-delivery infrastructure rather than embedding an environment-coupled allowlist in the query."
  dedup_decisions:
    - dropped: cue_parent_process.follow_on
      reason: "near-duplicate of parent_child.sibling_processes (children of the same parent); sibling_processes scopes by exact ParentProcessGuid instance"
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "near-duplicate of cue_host_context activity sweep / sibling_processes; same-time parent activity is covered by sibling_processes (exact parent instance) and clustering_svc (cross-host parent+binary)"
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.scope
      reason: "near-identical query to cue_user_actor.clustering_svc (process.parent.name + Image|endswith, cross-host aggregation); clustering_svc is a mandatory service-principal pair member"
      kept: cue_user_actor.clustering_svc
    - dropped: cue_parent_process.mechanism_decode
      reason: "the parent is WinRAR (named in the rule) and parent_child.parent_process_legitimacy already looks up the parent's own event and grandparent; a separate parent decode adds nothing"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_process_tool.baseline
      reason: "cue_parent_process.baseline is more specific — it conditions on the WinRAR-to-interpreter pair rather than the interpreter alone, which is the discriminating relationship here"
      kept: cue_parent_process.baseline
    - dropped: none
      reason: "user_actor pair — non-deduplicable per AGENTS.md; baseline + baseline_svc and clustering + clustering_svc both emitted as separate questions"
      kept: cue_user_actor.baseline / cue_user_actor.baseline_svc / cue_user_actor.clustering / cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain (parent cue subsumes it)"
    - template: powershell.script_block_logging
      reason: "budget (18-question cap) — the child is one of eight interpreters and only two are PowerShell; encoded_command_decode already covers the PowerShell deobfuscation angle conditionally, and script-block 4104 coverage would sit idle for the six non-PowerShell children"
    - template: powershell.powershell_security_bypass
      reason: "budget (18-question cap) — endpoint_protection_health already sweeps host-wide tamper/AMSI/ETW activity in the -1h precursor window, which subsumes the PowerShell-specific bypass list for this multi-interpreter rule"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - initial_access
    - parent_child
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 14
      application: 1
      file_event: 1
      network_connection: 1
      sigma_alert: 1
