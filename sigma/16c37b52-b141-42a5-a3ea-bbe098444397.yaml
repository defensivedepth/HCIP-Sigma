name: Suspect Svchost Activity
id: d6bd2f17-87cf-4e28-8d58-bfdfc3d48820
description: |
  Detects svchost.exe running with no command-line arguments. Legitimate svchost.exe is always launched by services.exe with a "-k <service group>" argument, so a bare svchost process with only its own image path on the command line is abnormal. The rule fires when the command line ends in "svchost.exe" and the image ends in \svchost.exe, AND the parent is not rpcnet.exe/rpcnetp.exe and the command line is not empty. This shape is consistent with a process created in a suspended state for hollowing or remote-thread injection, where another process allocates memory in svchost and writes code into it. Investigation should determine which process created this svchost, whether memory/handle access or unusual DLL loads occurred against it, and whether the host shows surrounding injection or post-exploitation activity.
type: detection
detection_id: 16c37b52-b141-42a5-a3ea-bbe098444397
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did svchost.exe run with no service-group argument, and is its parent something other than rpcnet.exe/rpcnetp.exe?
  context: Confirm the alert is a true positive. The rule fires when the image ends in \svchost.exe and the command line is just the image path with no "-k" service-group argument, AND the parent is not the lojack-style rpcnet.exe/rpcnetp.exe. Verify the exact command line and image path against these criteria before investing further.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this host expected to produce a svchost.exe process with no service-group argument?
  context: Genuine svchost processes are spawned by services.exe with a "-k" argument identifying the service group. A bare svchost with no arguments is not part of normal Windows service startup on any host, so there is rarely a benign business explanation. Establish whether any local software or imaging process is known to produce this shape before treating it as injection.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.process_chain
  question: What process created this argument-less svchost, and is that parent expected to spawn svchost?
  context: Legitimate svchost is created only by services.exe. Any other parent — explorer.exe, an Office application, a script host, a packer, or an unsigned binary — is the central anomaly, because injection-style creation of a suspended svchost is performed by the injecting process itself. Identify the parent and walk up its own parent chain to see what launched it.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What is the full creation context of this bare svchost — its image path, working directory, and integrity level?
  context: The triggering event has no command-line payload to decode, so the operational meaning is in the surrounding fields. Confirm the image path is the genuine system32/syswow64 svchost and not a relocated copy, and review the working directory and any sibling svchost executions in the same window. A svchost created from a non-system path, or one with no inbound service-control activity, points away from normal service startup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - ParentImage
- source: attack:T1055.access_verification
  question: Did another process open a handle to this svchost with memory-write or thread-creation access in the surrounding window?
  context: Process injection acquires a handle to the target with rights such as PROCESS_VM_WRITE, PROCESS_VM_OPERATION, or PROCESS_CREATE_THREAD before writing code and starting a remote thread. svchost, lsass.exe, and explorer.exe are common injection targets because they run continuously and blend in. Review process-access events targeting this svchost for high-value access masks (for example 0x1F0FFF full access, or masks granting VM_WRITE/CREATE_THREAD) and identify the source process. If process-access telemetry is not collected in this environment, treat the absence as a visibility gap rather than evidence nothing occurred.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        TargetImage|endswith: \svchost.exe
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - SourceImage
      - TargetImage
      - GrantedAccess
      - CallTrace
- source: attack:T1055.image_load
  question: Did this svchost load any DLLs from unexpected paths or any unsigned modules after creation?
  context: Reflective or remote DLL injection loads code into the target that does not come from the normal service-DLL set under system32. Review modules loaded by this svchost for unusual paths (user-writable directories, temp, AppData), unsigned modules, or DLLs with no matching service registration. A bare svchost that goes on to load a non-system DLL is a strong follow-on signal for injection. If module-load telemetry is not ingested here, zero results is a visibility gap, not a clean result.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        Image|endswith: \svchost.exe
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1055.time_correlation
  question: What process activity clustered on this host in the minutes immediately around the svchost creation?
  context: Injection unfolds as a short sequence — a parent allocates memory, writes the payload, and starts a thread in the target, often within seconds. Examining the tight window around the bare svchost creation surfaces the process that performed the allocation/write/thread steps and any tool that was running at the same moment. A narrow time correlation separates the injection actor from unrelated background activity.
  range: +/-5m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.follow_on
  question: What child processes did this svchost spawn after creation?
  context: A normally functioning svchost service host hosts service DLLs rather than spawning interactive children. Once code is injected, the target frequently spawns cmd.exe, powershell.exe, rundll32.exe, or other post-exploitation tooling. Unexpected children of this svchost reveal the actual impact of the injected code.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Has this host produced an argument-less svchost before, or is this a first-time occurrence?
  context: A bare svchost with no service-group argument has no legitimate recurring cause, so historical counts should be near zero on a clean host. A first-time occurrence raises suspicion; a recurring pattern may point to a specific local software product that should be identified and confirmed benign.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users drove it, and does the current alert fit that pattern?
  context: The query mirrors the rule's selection structure scoped to this host, so the resulting parent/user distribution reflects how this specific firing pattern — bare svchost, no service-group argument — has historically run here. A new parent process or an unexpected user account relative to that history is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|endswith: svchost.exe
        Image|endswith: \svchost.exe
      filter:
      - ParentImage|endswith:
        - \rpcnet.exe
        - \rpcnetp.exe
      - CommandLine: null
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user account normally run processes that result in this svchost pattern?
  context: Compare the alerting user's current activity against their historical behavior across the environment. A user who has never been associated with this pattern is more suspicious than one for whom it recurs. If the alert fires under a service principal, this pivot will be uninformative — rely on the parent+binary baseline instead.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+svchost combination historically routine on this host or across the environment, or is the parent unusual for svchost?
  context: When the alert fires under SYSTEM or another service principal, user identity does not attribute the behavior; the meaningful pivot is the launch mechanism. A parent that already accounts for many prior svchost creations is automation or OS behavior, while a new or rare parent for svchost is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of this alert?
  context: Tracking the user's activity across all hosts reveals lateral movement or parallel activity on multiple endpoints. If the same user is associated with similar activity on other hosts in this window, the scope may be larger than a single host. Under a service principal this returns agent-telemetry noise — use the parent+binary cross-host view instead.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+svchost combination fire on other hosts in the same window, indicating a coordinated rollout rather than isolated activity?
  context: Under a service principal, a user-wide pivot returns continuous agent telemetry. The meaningful question is whether the same launch mechanism — parent process plus spawned svchost — appeared on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, or persistence mechanisms surrounding the svchost creation.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the stages that preceded this event and help reconstruct the timeline. Injection into a system process is frequently a mid-chain step rather than the initial access.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal — kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1055
      name: Process Injection
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: process_handle_acquisition
      description: "Injecting process opens a handle to the target (svchost) with memory-write/thread-creation access rights"
      mapped_to: Q5
      source: attack:T1055.access_verification
    - stage: 2
      label: memory_write_and_thread
      description: "Allocation/write into target memory and remote thread creation, observable as a tight cluster of activity around target creation"
      mapped_to: Q7
      source: attack:T1055.time_correlation
    - stage: 3
      label: unusual_dll_load
      description: "Injected/reflective DLLs loaded into the target process from unexpected paths or unsigned modules"
      mapped_to: Q6
      source: attack:T1055.image_load
  dimension_coverage:
    - dimension: access_verification
      technique: T1055
      covered_by: Q5
      source: attack:T1055.access_verification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1055
      covered_by: Q7
      source: attack:T1055.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1055
      covered_by: Q5
      source: attack:T1055.access_verification
      coverage_type: partial
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "PROCESS_VM_WRITE / PROCESS_CREATE_THREAD; full access 0x1F0FFF"
      disposition: new_question
      target_question: Q5
      notes: "Authored attack:T1055.access_verification (process_access logsource) returning GrantedAccess on handles into svchost; access-mask values cited in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: new_question
      target_question: Q7
      notes: "Authored attack:T1055.time_correlation with a tight +/-5m window around svchost creation to bound the allocate/write/thread sequence."
    - element: InjectedProcessList
      dimension: tuning_parameter
      specific_values: "lsass.exe, explorer.exe (target here is svchost.exe)"
      disposition: context
      target_question: Q5
      notes: "High-value injection targets (lsass, explorer, svchost) cited in access_verification context; not directly queryable since the rule already fixes the target as svchost."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Identical intent and query shape (host-scoped aggregation of activity in window) to cue_host_context.clustering — kept the host_context version."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.scope
      reason: "Fleet-wide rule-pattern count (REFLECTED fleet_pattern) overlaps with the cross-host parent+binary view in cue_user_actor.clustering_svc; dropped to stay within the 18-question cap while keeping the more specific parent+binary cross-host pivot."
      kept: cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 14
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      process_access: 1
      image_load: 1
      sigma_alert: 1
