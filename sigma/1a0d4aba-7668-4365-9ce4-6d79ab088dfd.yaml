name: Ping Hex IP
id: 8fedde51-b17f-41b0-93cd-6acdef408993
description: |
  Detects ping.exe invoked with a hexadecimal-encoded IP address (an 8-hex-digit
  literal such as 0x7F000001). Windows ping accepts a 32-bit integer in place of
  dotted-decimal notation, so encoding the destination as hex hides the real target
  IP from casual inspection of the command line and from naive string-matching.
  Legitimate use is rare — administrators almost never address hosts in hex — so the
  pattern most often appears in offensive tooling that resolves or pings a hidden
  destination. Decode the hex value to the actual IP, determine what that destination
  is and whether a connection followed, and establish how ping was launched and by whom.
type: detection
detection_id: 1a0d4aba-7668-4365-9ce4-6d79ab088dfd
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show ping.exe with an 8-digit hexadecimal IP (0x followed by 8 hex characters)?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires when the image ends in ping.exe and the command line contains a hex literal of the form 0x then eight hex digits, which is how a 32-bit IP address is encoded in hexadecimal.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run ping with a hex-encoded address?
  context: Legitimate administrative use of ping uses hostnames or dotted-decimal IPs; hex-encoded targets are not a normal operator habit. Knowing whether the user role and host function involve any tooling that would produce this form is often the fastest path to distinguishing a true positive from a one-off curiosity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What was the full ping command line on this host, including every argument and any chained commands?
  context: The full command line reveals the specific action taken. Read every flag and parameter beyond the hex address — count, timeout, source interface — and check whether ping was one segment of a chained command (for example joined with &, &&, or a pipe) that does additional work once the destination is reached. This scopes to ping.exe executions on the host so multiple sequential invocations that form one logical operation are visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:obfuscation.encoded_payload_decode
  question: What dotted-decimal IP does the hex value decode to, and what is that destination?
  context: The 8-hex-digit literal is a 32-bit integer encoding of an IPv4 address. Convert each byte to decimal to recover the real target — for example 0x7F000001 decodes to 127.0.0.1 and 0x08080808 to 8.8.8.8. Identify whether the decoded address is loopback, private (RFC1918), or a routable external host, and whether it corresponds to known infrastructure. The hex form can vary in case and byte order, so reconstruct the address deliberately rather than assuming a layout.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1140.decoded_destination
  question: Did a network connection to the decoded destination IP follow the ping on this host?
  context: Encoding the IP hides the destination, but the operation only matters if traffic reaches it. After decoding the hex to a dotted-decimal address, look for outbound connections to that IP from this host in the surrounding window — a connection following the ping points to reachability checking ahead of command-and-control or staging, while no connection suggests a probe or test. Substitute the decoded address for the related-IP placeholder when running this.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationIp|expand: '%related_ip%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - Initiated
- source: template:cue_process_tool.process_chain
  question: What process spawned ping.exe and is that parent chain expected?
  context: The parent process reveals how ping was invoked. A user-driven ping typically descends from an interactive shell or explorer.exe; ping launched by mshta.exe, wscript.exe, an Office application, or an unexpected script host points to automated or scripted invocation worth tracing further up the tree.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did ping.exe spawn afterward on this host?
  context: ping.exe normally spawns nothing. Children such as cmd.exe, powershell.exe, or certutil.exe following the ping indicate the hex-IP ping was a step within a larger chain — for example a reachability check preceding a download or further execution.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run ping.exe?
  context: A binary that runs regularly on a host is less notable than one that is rare here. Zero or very low historical counts indicate first-time or unusual use of ping on this host, which raises the weight of a hex-encoded destination.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even common tools can be abused. The query mirrors the rule's selection — ping.exe with a hex-encoded IP — scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here rather than generic ping usage. A new parent process, an unexpected user account, or an unusual command shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \ping.exe
        CommandLine|re: 0x[a-fA-F0-9]{8}
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of hex-encoded ping activity rather than generic ping usage. Many hosts firing the same pattern points to scripted deployment or a tool packaged into automation; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \ping.exe
        CommandLine|re: 0x[a-fA-F0-9]{8}
      condition: selection
    fields:
    - host.name
- source: template:cue_process_tool.clustering
  question: What other tools ran on this host around the same time as the ping?
  context: A hex-IP ping is rarely the whole story. Activity surrounding it may reveal reconnaissance, a download cradle, privilege escalation, or staging that frames the ping as a reachability check within a larger sequence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
- source: template:cue_user_actor.baseline
  question: Does this user normally run ping or similar network utilities?
  context: Compare this user's current action against their historical behavior. A user who has never run ping — let alone with a hex-encoded address — is more notable than one who routinely uses network utilities. User role and typical toolset should align with the activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and ping.exe combination historically routine on this host or across the environment, or is the parent unusual for ping?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned ping, and is that pair historically common. A new or rare parent for ping on this host or environment-wide is the deviation worth chasing; a pair that already accounts for many prior events is more likely automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated action across multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the operation may be larger than this one alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and ping.exe combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise because every host runs system-owned agents constantly. The meaningful question is whether the same launch mechanism — parent process plus spawned ping — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the stages that preceded this event and help reconstruct the full timeline around the hex-IP ping.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1140
      name: Deobfuscate/Decode Files or Information
      role: primary
    - id: T1027
      name: Obfuscated Files or Information
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: obfuscated_destination
      description: "An adversary encodes the target IP as a hex literal to hide the destination from casual command-line inspection"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: decode_destination
      description: "The hex literal is decoded to the actual IPv4 address to reveal the real target"
      mapped_to: Q4
      source: template:obfuscation.encoded_payload_decode
    - stage: 3
      label: decoded_action
      description: "Traffic reaches the decoded destination, or the ping is chained to follow-on execution"
      mapped_to: Q5
      source: attack:T1140.decoded_destination
  dimension_coverage:
    - dimension: command_analysis
      technique: T1140
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: execution_context
      technique: T1140
      covered_by: Q5
      source: attack:T1140.decoded_destination
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1140
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1140
      covered_by: Q11
      source: template:cue_process_tool.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1140
      covered_by: Q4
      source: template:obfuscation.encoded_payload_decode
      coverage_type: indirect
    - dimension: user_context
      technique: T1140
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q4
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ToolName
      dimension: tuning_parameter
      specific_values: "certutil, powershell, copy, expand, 7zip"
      disposition: context
      target_question: Q3
      notes: "Rule constrains the tool to ping.exe; the generic decode-tool list is not applicable. Q3 reads the actual ping command line."
    - element: FileExtensionFilter
      dimension: tuning_parameter
      specific_values: ".txt, .cer, .enc, .b64, .zip"
      disposition: context
      target_question: Q4
      notes: "No file artifact in this rule — the obfuscated object is a hex IP in the command line, not a staged file. Decode angle handled as IP reconstruction."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "0x[a-fA-F0-9]{8}"
      disposition: query_injection
      target_question: Q9
      notes: "Rule regex embedded verbatim in the reflected baseline_deviation (Q9) and scope (Q10) queries; also drives Q4 decode framing."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Short follow-window between decode and action covered by Q5 (+/-1h network correlation) and Q11 clustering."
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Entropy thresholds do not apply to a fixed 8-hex-digit IP literal; obfuscation_check is satisfied by decoding the hex to a dotted-decimal IP in Q4."
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Parent legitimacy assessed in Q6 (process_chain) and the parent+binary svc-baseline (Q13)."
  dedup_decisions:
    - dropped: cue_host_context.clustering
      reason: "Duplicates cue_process_tool.clustering (Q11) — both surface other process_creation activity on the host in the same window; the process_tool variant is kept as the primary-cue version"
      kept: cue_process_tool.clustering
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q12 user-pivot, Q13 parent+binary substitute) per service-principal handling"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q14 user-pivot, Q15 parent+binary substitute) per service-principal handling"
      kept: both
  skipped_templates:
    - template: universal.evidence_preservation
      reason: "applicability has_file_reference is set because the brief derived a file reference from CommandLine, but the rule writes no file — the obfuscated object is a hex IP literal in the ping command line. A file_event query for %file_paths% would search for a file that does not exist. The decode/destination analysis is handled by Q4 and Q5 instead."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. defense_tampering is not confirmed for this rule, and a hex-encoded ping is a deobfuscation/reachability action rather than a hands-on payload-execution or credential-access step where tamper-as-precursor is the live disposition question. cue_host_context.precursor (Q16) already surfaces prior tamper alerts on the host — the (b) drop condition in the template notes."
  hint_dispositions:
    - hint: "logsource 'process_access' (recommended by techniques: T1140) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        T1140's process_access analytic targets in-memory decode where one
        process reads/decodes content from another (EventCode=10 cross-process
        access). This rule fires on a self-contained ping.exe command line whose
        only obfuscated object is a hex IP literal in its own arguments — there
        is no second process being read, so a process_access query would return
        unrelated handle-open noise. The decode is reconstructed directly from
        the command line in Q4 (hex-to-IP), making process_access coverage
        inapplicable to this rule's mechanism. No compensating
        attack:T1140.process_access question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - obfuscation
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      network_connection: 1
      sigma_alert: 1
