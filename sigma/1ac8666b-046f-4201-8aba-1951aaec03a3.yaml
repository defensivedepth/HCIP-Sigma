name: Command Line Execution with Suspicious URL and AppData Strings
id: fa2eebf2-2f71-4ec7-9556-f830ca45ff79
description: |
  Detects a cmd.exe command line that simultaneously contains an HTTP URL ('http' and '://') and an '%AppData%'
  reference -- the AND combination commonly produced by script-based droppers that orchestrate a download-and-run
  chain (a script host spawns cmd.exe, which builds an obfuscated PowerShell or native download command that
  fetches a payload over HTTP and drops it into the per-user AppData directory). Legitimate cmd.exe invocations
  rarely combine a remote URL with an AppData write path in one command line, but some installers and helper
  scripts can. The investigation should decode the full command line, identify the download mechanism and source
  URL, confirm whether a payload landed on disk under AppData, and establish the parent chain and user context to
  separate a real download-and-execute chain from benign automation.
type: detection
detection_id: 1ac8666b-046f-4201-8aba-1951aaec03a3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does this cmd.exe command line contain an HTTP URL and an %AppData% reference together?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when a
    single cmd.exe command line contains 'http' AND '://' AND '%AppData%' together. Read the exact command line and
    confirm all three are present in one invocation rather than coincidentally split across unrelated arguments.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run cmd.exe commands that download files into AppData?
  context: Legitimate administrative activity and some installers can trigger this. Knowing whether the user role
    and host function are consistent with command-line downloads into a per-user AppData directory is often the
    fastest path to distinguishing a true positive from routine operations.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this command do? Decode the full command line, including any caret or escape obfuscation.
  context: The full command line reveals the download mechanism and the payload destination. Droppers frequently
    break PowerShell keywords and switches apart with caret ('^') characters that cmd.exe strips before passing to
    the child interpreter; reconstruct the cleaned command. Identify the source URL, the download method (for
    example New-Object Net.WebClient DownloadFile, Invoke-WebRequest, bitsadmin, certutil), and the exact AppData
    drop path.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \cmd.exe
        CommandLine|contains|all:
        - http
        - '://'
        - '%AppData%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Did cmd.exe spawn an obfuscated PowerShell downloader, and what does the deobfuscated command do?
  context: This pattern typically chains cmd.exe into a caret-fragmented or otherwise obfuscated PowerShell command
    that pulls a payload over HTTP. Reconstruct the cleaned PowerShell command from the alerting cmd.exe command
    line, identify the cradle (for example a Net.WebClient DownloadFile or DownloadString call), and note the source
    URL and the AppData destination so the download can be tracked to disk.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1059.003.command_analysis
  question: Does the command line carry shell-chaining or obfuscation markers such as carets, ampersand chaining,
    or /c and /k switches?
  context: cmd.exe abuse is characterized by anomalous command-line structure -- caret splitting between keyword
    letters, '&&' or '&' chaining of multiple commands, and /c or /k driving a one-liner. These markers distinguish
    a scripted dropper orchestration from an interactive operator typing a single command. Note which markers are
    present, since they indicate runtime command assembly designed to defeat static parsing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this cmd.exe, and is that parent chain expected?
  context: The parent reveals how the command was invoked. cmd.exe launched from a script host such as wscript.exe
    or cscript.exe, from a browser, or from an Office application is characteristic of a delivered-file execution
    chain, whereas explorer.exe or an interactive shell parent is more consistent with user-driven activity. Identify
    the immediate parent and what spawned it.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1105.network_transfer
  question: Did the spawning process tree make outbound network connections to the URL host around the alert time?
  context: The download stage of this chain reaches out over HTTP to fetch the payload. Pivot from the alerting
    host to outbound connections in the alert window to confirm a transfer occurred and to capture the destination
    address and port, which can be enriched against threat intelligence. Exclude routine system-owned connections to
    focus on the interpreter-driven traffic.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
      filter_browsers:
        Image|endswith:
        - \chrome.exe
        - \msedge.exe
        - \firefox.exe
      condition: selection and not filter_browsers
    fields:
      - Image
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:download_tool.download_success_verification
  question: Was the download successful? Check for file creation at the AppData destination.
  context: A download command does not guarantee the file was retrieved. Network errors, blocked URLs, or security
    controls may have prevented the transfer. Confirming file creation under the AppData path from the command line
    establishes whether the chain progressed past delivery into execution.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:universal.evidence_preservation
  question: Is the downloaded payload still on disk under AppData for analysis?
  context: If the dropped file references a path the command wrote to (a downloaded payload, often with an unusual
    or extensionless name under AppData), checking whether it still exists enables hash lookup, sandbox submission,
    or content review.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded payload from a safe environment?
  context: If the downloaded file is still on disk, submitting its hash to threat intelligence services or
    detonating it in an isolated environment reveals whether it is a known malicious payload, a legitimate tool, or
    something novel that requires deeper analysis. The drop path is typically under the per-user AppData directory.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the AppData target directory?
  context: Droppers often write multiple files to the same staging directory, or the downloaded file triggers
    further writes (extraction, decompression, additional downloads). Examining directory-level file activity reveals
    the broader scope of the delivery operation.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_dirs%'
      filter_self:
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection and not filter_self
    fields:
      - TargetFilename
      - Image
- source: attack:T1105.executed_payload
  question: Did the downloaded payload under AppData get executed afterward?
  context: This chain typically ends with Start-Process or a direct launch of the file dropped into AppData. Look
    for process creation where the image path resolves to the per-user AppData directory in the period after the
    download, which confirms the chain progressed from delivery into execution.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|contains:
        - \AppData\Roaming\
        - \AppData\Local\
      condition: selection
    fields:
      - Image
      - ParentImage
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this download-into-AppData pattern, what parents and users drove it, and
    does the current alert fit?
  context: Even common shells can be abused. The query mirrors the rule's selection scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new
    parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \cmd.exe
        CommandLine|contains|all:
        - http
        - '://'
        - '%AppData%'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \cmd.exe
        CommandLine|contains|all:
        - http
        - ://
        - '%AppData%'
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \cmd.exe
        CommandLine|contains|all:
        - http
        - ://
        - '%AppData%'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple
    endpoints. If the same user triggered similar activity on several hosts, the scope of the delivery operation may
    be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \cmd.exe
        CommandLine|contains|all:
        - http
        - ://
        - '%AppData%'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- script execution, reconnaissance,
    or defense tampering -- may reveal the stages that preceded this download chain and help reconstruct the full
    timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.003
      name: Windows Command Shell
      role: primary
    - id: T1059.001
      name: PowerShell
      role: secondary
    - id: T1105
      name: Ingress Tool Transfer
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: shell_orchestration
      description: "cmd.exe is launched (typically from a script host) and assembles a download command, often with caret/escape obfuscation"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: parent_invocation
      description: "Anomalous parent spawns cmd.exe (script host, browser, Office) — T1059.003 parent_process_analysis"
      mapped_to: Q6
      source: template:cue_process_tool.process_chain
    - stage: 3
      label: powershell_downloader
      description: "cmd.exe spawns an obfuscated PowerShell command line carrying a download cradle — T1059.001"
      mapped_to: Q4
      source: template:powershell.encoded_command_decode
    - stage: 4
      label: network_transfer
      description: "The interpreter initiates an outbound HTTP connection to fetch the payload — T1105 network_connection"
      mapped_to: Q7
      source: attack:T1105.network_transfer
    - stage: 5
      label: file_drop
      description: "Downloaded payload is written to disk under the AppData directory — T1105 file_event"
      mapped_to: Q8
      source: template:download_tool.download_success_verification
    - stage: 6
      label: payload_execution
      description: "The dropped payload under AppData is executed (Start-Process / direct launch)"
      mapped_to: Q12
      source: attack:T1105.executed_payload
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.003
      covered_by: Q5
      source: attack:T1059.003.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.003
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.003
      covered_by: Q18
      source: template:cue_host_context.precursor
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1059.003
      covered_by: Q8
      source: template:download_tool.download_success_verification
      coverage_type: direct
    - dimension: user_context
      technique: T1059.003
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059
      covered_by: Q13
      source: template:cue_process_tool.baseline_deviation
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1059
      covered_by: Q5
      source: attack:T1059.003.command_analysis
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q18
      source: template:cue_host_context.precursor
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q4
      source: template:powershell.encoded_command_decode
      coverage_type: partial
    - dimension: file_path_analysis
      technique: T1105
      covered_by: Q11
      source: template:download_tool.directory_activity
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1105
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1105
      covered_by: Q7
      source: attack:T1105.network_transfer
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "carets/'^' obfuscation, '&&'/'&' chaining, /c and /k switches; rule anchors http + :// + %AppData%"
      disposition: query_injection
      target_question: Q3
      notes: "mechanism_decode query injects the rule's CommandLine|contains|all anchors (http, ://, %AppData%); Q5 enumerates caret/chaining/switch markers in context"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "script hosts (wscript.exe, cscript.exe), browsers, Office apps as unusual cmd.exe parents"
      disposition: context
      target_question: Q6
      notes: "process_chain question names script-host/browser/Office parents in context; baseline_svc/clustering_svc pivot on parent name"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — off-hours / non-maintenance windows"
      disposition: context
      target_question: Q18
      notes: "tuning rationale, no concrete values; precursor and baseline_deviation windows carry temporal scoping"
    - element: ScriptStoragePath
      dimension: tuning_parameter
      specific_values: "%TEMP%, C:\\Users\\Public, AppData drop paths"
      disposition: context
      target_question: Q8
      notes: "rule's AppData anchor IS the storage-path signal; download_success_verification and evidence_preservation scope to %file_paths% under AppData"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — admin users outside baseline"
      disposition: context
      target_question: Q14
      notes: "user baseline question covers historical user activity; host_user_context covers role expectation"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only — rare .NET assemblies in image_load"
      disposition: context
      target_question: Q4
      notes: "image_load logsource not in playbook (see hint_dispositions); .NET cradle covered via process_creation evidence in encoded_command_decode"
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only — 4104 script-block length threshold"
      disposition: context
      target_question: Q4
      notes: "script-block logging not authored as separate question; obfuscated PowerShell content reconstructed from cmd.exe command line"
    - element: DestinationIPCategory
      dimension: tuning_parameter
      specific_values: "internal vs external IP blocks"
      disposition: context
      target_question: Q7
      notes: "network_transfer question surfaces DestinationIp/Hostname/Port for internal-vs-external triage"
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: "uncommon drop paths (AppData, C:\\Users\\Public)"
      disposition: query_injection
      target_question: Q11
      notes: "directory_activity scopes to %file_dirs% (the AppData staging dir); executed_payload injects \\AppData\\Roaming\\ and \\AppData\\Local\\"
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "user_actor baseline (Q14) and baseline_deviation (Q13) cover historical frequency; cue_process_tool.baseline on cmd.exe (a noisy binary) would return host-wide volume without the rule's pattern filter"
      kept: cue_user_actor.baseline + cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "follow-on child processes of cmd.exe are covered by the PowerShell-downloader stage (Q4) and the executed-payload stage (Q12), which are more specific to this chain"
      kept: attack:T1105.executed_payload
    - dropped: cue_process_tool.clustering
      reason: "host-wide what-else-ran is covered by cue_host_context.clustering scope; dropped to stay within 18-question budget and because user-actor clustering + precursor provide temporal context"
      kept: cue_host_context.precursor
    - dropped: cue_process_tool.scope
      reason: "fleet-wide rule-pattern count dropped for question budget; baseline_deviation (host) + user clustering_svc (cross-host same-window) provide the cross-host distribution signal"
      kept: cue_user_actor.clustering_svc
    - dropped: cue_host_context.clustering
      reason: "host-around-alert activity overlaps with user clustering and precursor; dropped for budget — precursor (sigma alerts) gives the higher-signal host timeline"
      kept: cue_host_context.precursor
    - dropped: powershell.script_block_logging
      reason: "the obfuscated PowerShell here is constructed inside the cmd.exe command line and runs as a child; script-block 4104 content is valuable but the cleaned command is reconstructable from the alerting cmd.exe command line (Q4) within budget"
      kept: powershell.encoded_command_decode
    - dropped: powershell.powershell_security_bypass
      reason: "rule anchors on URL+AppData download, not on AMSI/policy bypass; bypass-precursor coverage subsumed by precursor (Q18) which surfaces any prior tamper/bypass alerts on the host"
      kept: cue_host_context.precursor
    - dropped: universal.endpoint_protection_health
      reason: "rule detects a download-and-run chain, not defense tampering; tamper-precursor signal covered by precursor (Q18); dropped for 18-question budget"
      kept: cue_host_context.precursor
    - decision: user_actor pair — non-deduplicable
      kept: cue_user_actor.baseline (Q14) + cue_user_actor.baseline_svc (Q15); cue_user_actor.clustering (Q16) + cue_user_actor.clustering_svc (Q17)
      reason: "user-pivot and service-principal-substitute variants pivot on distinct identity dimensions; emit both per AGENTS.md"
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "deduped — see dedup_decisions (cmd.exe noisy binary; pattern-scoped baseline_deviation preferred)"
    - template: cue_process_tool.follow_on
      reason: "deduped — covered by powershell_downloader and executed_payload chain stages"
    - template: cue_process_tool.clustering
      reason: "deduped — host-wide clustering subsumed by precursor + user clustering"
    - template: cue_process_tool.scope
      reason: "deduped — cross-host signal via baseline_deviation + clustering_svc; dropped for budget"
    - template: cue_host_context.clustering
      reason: "deduped — overlaps precursor + user clustering"
    - template: powershell.script_block_logging
      reason: "deduped — cleaned PowerShell reconstructable from cmd.exe command line within budget"
    - template: powershell.powershell_security_bypass
      reason: "deduped — not the rule's behavior; bypass-precursor covered by precursor question"
    - template: universal.endpoint_protection_health
      reason: "deduped — rule is a download chain, not tampering; tamper-precursor covered by precursor"
  reflection_overrides: []
  hint_dispositions:
    - hint: "cmd.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: acted_on
      reason: >-
        Acted on. mechanism_decode (Q3) scopes by host.name AND the rule's
        CommandLinePattern (Image|endswith \cmd.exe with CommandLine|contains|all
        http/://%AppData%) rather than bare Image scope, so the result is the
        rule's firing pattern, not all cmd.exe on the host.
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted_on
      reason: >-
        Acted on. powershell.encoded_command_decode is authored as Q4 (the
        obfuscated-PowerShell-downloader question); no obfuscation.encoded_payload_decode
        template was emitted.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted_on
      reason: >-
        Acted on. download_success_verification (Q8), evidence_preservation (Q9),
        and payload_analysis (Q10) all scope to %file_paths%; directory_activity
        (Q11) uses %file_dirs%. The AppData drop path is extracted from the
        alerting command line and substituted into these placeholders.
    - hint: "logsource 'image_load' (recommended by techniques: T1059.003,T1059.001) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule constrains the trigger to a cmd.exe command line whose payload is
        a download-and-run chain; the load-bearing evidence is the command line
        and the resulting file/network artifacts, not .NET module loads. image_load
        on cmd.exe/powershell.exe returns high-volume mscorlib/system assembly
        loads with no precision for this download pattern. The .NET-hosted cradle
        is decoded from process_creation evidence in the obfuscated-PowerShell
        question (Q4), which is sufficient for triage. No compensating
        attack:T1059.001.image_load question authored.
    - hint: "logsource 'network_connection' (recommended by techniques: T1105) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted_on
      reason: >-
        Acted on. Authored attack:T1105.network_transfer (Q7) with
        category: network_connection to confirm the outbound HTTP transfer and
        capture DestinationIp/Hostname/Port for the download stage.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - download_tool
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 12
      file_event: 4
      network_connection: 1
      sigma_alert: 1
