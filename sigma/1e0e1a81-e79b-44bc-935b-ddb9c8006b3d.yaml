name: Potential Script Proxy Execution Via CL_Mutexverifiers.ps1
id: 00dc400c-340a-464b-80b8-e5d3e77f2c20
description: |
  Detects powershell.exe (or pwsh.exe) spawning a child powershell.exe with the command line ' -nologo -windowstyle minimized -file ' AND a file argument located under \AppData\Local\Temp\ or \Windows\Temp\. This launch pattern matches the proxy-execution behavior of the Microsoft-signed CL_Mutexverifiers.ps1 diagnostic script, which exposes a runAfterCancelProcess function that launches an arbitrary file. The signed-script lineage lets an actor run untrusted payloads under the cover of a trusted parent, so the investigation centers on what the proxied script does, where the Temp file came from, and whether the parent-child powershell chain is expected for this user and host.
type: detection
detection_id: 1e0e1a81-e79b-44bc-935b-ddb9c8006b3d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did powershell spawn a child powershell with the minimized -file launch and a Temp-path script argument?
  context: Confirm the alert is a true positive by checking the triggering event has a powershell.exe/pwsh.exe parent, a powershell.exe child, the ' -nologo -windowstyle minimized -file ' command-line fragment, and a file argument under \AppData\Local\Temp\ or \Windows\Temp\. All of these must be present together for the rule to fire.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:parent_child.parent_process_legitimacy
  question: Is the parent powershell expected to spawn a child powershell this way, and what started the parent?
  context: The parent-child powershell relationship is the core signal here. Tracing the parent's own origin (grandparent process) shows whether the chain is a legitimate diagnostic or script-host workflow or whether something like a macro, scheduled task, or remote shell launched the parent that then proxied the Temp script.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run nested powershell diagnostic scripts from Temp?
  context: Legitimate diagnostic and IT-automation workflows can launch signed PowerShell scripts from Temp. Knowing whether this user role and host function routinely run nested powershell that loads scripts from Temp is often the fastest way to separate routine activity from proxy-execution abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What does the proxied Temp script and its full command line do? Decode every argument and the -file path.
  context: The full command line names the exact .ps1 file under Temp that the child powershell loads. Read the file path, any additional arguments, and the script contents to determine whether it is a benign diagnostic routine or a download cradle, loader, or reverse shell. The CommandLine filter scopes this to the minimized -file launch pattern rather than all powershell on the host, which is too common to enumerate.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \powershell.exe
        CommandLine|contains: ' -nologo -windowstyle minimized -file '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Is the proxied script or its command line encoded or obfuscated, and what does it decode to?
  context: Proxy-execution payloads frequently hide intent with base64 (-EncodedCommand / -enc), string concatenation, backtick escaping, or variable substitution. Decoding reveals the real payload — download cradles, reverse shells, or in-memory loading of .NET assemblies. If the launched .ps1 is itself plain text, inspect the script body for IEX, Net.WebClient, Invoke-WebRequest, or Reflection.Assembly::Load patterns.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1216.file_event
  question: When and by what process was the proxied .ps1 file written into the Temp directory?
  context: System script proxy execution depends on a script file being staged on disk first. Identifying which process wrote the .ps1 under \AppData\Local\Temp\ or \Windows\Temp\, and when, shows whether the file is a legitimate diagnostic artifact or was dropped by a download, archive extraction, or another stage shortly before execution. A signed parent launching an unsigned or freshly written Temp script is the signed-to-unsigned transition this technique relies on.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - \AppData\Local\Temp\
          - \Windows\Temp\
        TargetFilename|endswith: .ps1
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated content of the proxied script?
  context: Script block logging records the deobfuscated script content PowerShell actually executes, even when the command line or file is encoded or obfuscated. This is ground truth for what the proxied Temp script ran. If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before this execution?
  context: Proxy execution through a signed script often follows steps that weaken PowerShell defenses — AMSI bypass (patching amsi.dll in memory), execution-policy changes, constrained language mode escape, or a downgrade to PowerShell v2 which lacks logging. Finding these precursor events strengthens the case that the Temp-script launch is malicious.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent powershell instance spawn?
  context: If the parent powershell proxied one script, it may have spawned additional children — reconnaissance, credential access, or follow-on stages. Examining siblings of the exact parent instance reveals the full scope of activity orchestrated through this chain.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Does this host normally see powershell spawning a child powershell?
  context: A powershell process launching another powershell is an unusual lineage outside of specific automation. Historical frequency of this parent-child pairing on this host distinguishes a routine diagnostic or build workflow from a novel nested-powershell chain worth scrutinizing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is this nested-powershell spawning pattern occurring on other hosts?
  context: The same powershell-spawns-powershell pairing appearing across multiple hosts may indicate a pushed automation rollout or a multi-host operation rather than an isolated event. A single occurrence is more likely targeted. Enterprise-wide comparison helps calibrate scope.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pwsh:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \powershell.exe
        CommandLine|contains: ' -nologo -windowstyle minimized -file '
      selection_temp:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Windows\Temp\
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pwsh:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \powershell.exe
        CommandLine|contains: ' -nologo -windowstyle minimized -file '
      selection_temp:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Windows\Temp\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar nested-powershell activity on multiple hosts, the scope of the operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pwsh:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \powershell.exe
        CommandLine|contains: ' -nologo -windowstyle minimized -file '
      selection_temp:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Windows\Temp\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, or persistence activity near the nested-powershell launch.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the attack stages that preceded the proxied-script execution and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1216
      name: System Script Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: script_staging
      description: "Untrusted script file is staged on disk under a Temp directory before execution"
      mapped_to: Q6
      source: attack:T1216.file_event
    - stage: 2
      label: signed_parent_proxy_launch
      description: "Microsoft-signed/script-host powershell parent proxies execution of the Temp script via a child powershell with the minimized -file launch"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: payload_execution
      description: "Proxied script executes its payload (download cradle, loader, or in-memory .NET execution)"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1216
      covered_by: Q2
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1216
      covered_by: Q9
      source: template:parent_child.sibling_processes
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1216
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "script interpreters (wscript.exe, cscript.exe, pubprn.vbs, installutil.exe); here constrained to powershell.exe/pwsh.exe parent"
      disposition: context
      target_question: Q2
      notes: "Rule fixes the parent to powershell.exe/pwsh.exe; parent legitimacy and origin tracing covered in parent_process_legitimacy"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — delta between signed-script execution and child process creation"
      disposition: context
      target_question: Q9
      notes: "Reflected in sibling-process +/-15m and file_event -24h staging windows; no specific threshold to query"
    - element: ChildCommandLineRegex
      dimension: tuning_parameter
      specific_values: "download cradle / PowerShell decode patterns; rule's distinctive pattern ' -nologo -windowstyle minimized -file ' + Temp paths"
      disposition: query_injection
      target_question: Q4
      notes: "CommandLine|contains ' -nologo -windowstyle minimized -file ' injected into mechanism_decode selection to scope off noisy powershell.exe"
    - element: SignedToUnsignedTransition
      dimension: tuning_parameter
      specific_values: "rationale only — signed Microsoft parent launching unsigned/unknown child or script"
      disposition: context
      target_question: Q6
      notes: "Signed-parent-to-unsigned-Temp-script transition described in file_event staging context and mechanism_decode"
  dedup_decisions:
    - dropped: cue_parent_process.mechanism_decode
      reason: "parent_child technique set active — parent_process_legitimacy is the more specific parent-lookup question (per AGENTS.md dedup rule)"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "parent_child.sibling_processes covers children of the exact parent instance via ParentProcessGuid; follow_on's parent-name scope is broader and redundant here"
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "parent-scoped same-time activity is subsumed by host_context.clustering (host-wide +/-15m) plus sibling_processes; avoids a third near-identical parent-scope query"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "host baseline keyed on Image=powershell.exe is too common to be meaningful; parent_process.baseline (powershell-spawns-powershell pairing) is the discriminating baseline"
      kept: cue_parent_process.baseline
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q12 user-pivot, Q13 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q14 user-pivot, Q15 parent+binary substitute)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain"
    - template: universal.endpoint_protection_health
      reason: "powershell.powershell_security_bypass (Q8) and cue_host_context.precursor (Q17) already surface prior tamper/bypass activity on the host; this defense-evasion proxy rule does not turn on whether AV/EDR was disabled first, so the generic host-tamper sweep does not earn a slot"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1216) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        Rule detects T1216 purely through process lineage (powershell parent
        spawning powershell child with a Temp -file argument). image_load (Sysmon
        EventCode 7) would address in-memory .NET assembly hosting, which the
        rule's detection does not constrain — there is no signed-DLL or
        assembly-load criterion in the selections. The proxied-payload analysis
        is carried by Q5 (encoded_command_decode) and Q7 (script_block_logging)
        via process_creation and PowerShell-operational evidence, which is
        sufficient for triage of this launch pattern. No compensating
        attack:T1216.image_load question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1216) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (Sysmon EventCode 10) addresses cross-process memory
        access / handle requests, which would be relevant if the rule detected
        injection or credential-dumping by the proxied script. This rule's
        detection is limited to the powershell-spawns-powershell launch shape and
        the Temp file argument; it provides no GrantedAccess or target-process
        criterion to scope a process_access query against, and any such query
        would return host-wide handle noise unrelated to the trigger. Follow-on
        child activity from the proxied script is instead covered by Q9
        (sibling_processes) and Q16 (host clustering). No custom
        attack:T1216.process_access question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - parent_child
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 3
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 14
      file_event: 1
      powershell: 1
      sigma_alert: 1
