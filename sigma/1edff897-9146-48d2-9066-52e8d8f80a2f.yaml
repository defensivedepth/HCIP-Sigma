name: Suspicious Invoke-WebRequest Execution With DirectIP
id: c825270c-a3cb-4b82-92ce-db418f9d36a5
description: |
  Detects PowerShell (powershell.exe, pwsh.exe, or powershell_ise.exe) invoking a download
  cmdlet or command-line transfer tool -- Invoke-WebRequest, Invoke-RestMethod, their irm/iwr
  aliases, curl, or wget -- against a target addressed by a direct numeric IP rather than a
  hostname. The rule fires only when all three conditions hold together: the executing image
  is a PowerShell interpreter AND the command line carries one of the download verbs AND the
  target URL begins with a numeric IP. Hostname-less, IP-direct fetches bypass DNS-based
  reputation and blocklists and are a common ingress-tool-transfer pattern, but the same syntax
  appears in legitimate automation, scripted health checks, and internal tooling. The
  investigation should determine what was fetched, whether it landed on disk, what the
  PowerShell parent chain was, and whether the destination IP is internal or external.
type: detection
detection_id: 1edff897-9146-48d2-9066-52e8d8f80a2f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show a PowerShell download cmdlet fetching a direct numeric IP?
  context: Before investing investigation time, confirm the alert is a true positive. The rule
    fires only when all three conditions hold together -- the image is a PowerShell interpreter,
    the command line contains a download verb (Invoke-WebRequest, Invoke-RestMethod, irm, iwr,
    curl, or wget), and the target URL begins with a numeric IP rather than a hostname. Read the
    exact command line and confirm all three are present.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to fetch files over the command line via PowerShell?
  context: Legitimate administrative scripting, package installers, and health checks routinely
    pull files with Invoke-WebRequest or curl. Knowing whether this user role and host function
    normally perform command-line downloads is often the fastest path to distinguishing routine
    automation from operator-driven ingress.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.evidence_preservation
  question: Is the downloaded file still on disk for analysis?
  context: If the fetch succeeded, the retrieved payload may still be on disk. Confirming it exists
    enables deeper analysis -- hash lookups, sandbox submission, or content review. The destination
    path is typically the value passed to -OutFile or the redirect target in the command line.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this PowerShell command do? Decode the full command line, including
    the destination IP and output path.
  context: The full command line reveals the specific action taken. Decode the download verb, the
    direct-IP URL, any -OutFile or redirect destination, hidden-window or no-profile flags, and
    any chained commands. This host+binary scope captures any sequential PowerShell invocations
    around the alert that together form one logical operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - 'Invoke-WebRequest'
          - 'Invoke-RestMethod'
          - 'iwr '
          - ' irm '
          - 'curl '
          - 'wget '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Was the command encoded or obfuscated? What does the download cradle decode to?
  context: The detected pattern is a plain-text download cradle, but it may be wrapped in
    base64 -EncodedCommand, backtick escaping, string concatenation, or variable substitution to
    hide the destination. Decode any encoding and identify the actual URL being fetched, the file
    being written, and whether the result is piped to Invoke-Expression for in-memory execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell or endpoint security controls bypassed or weakened before this execution?
  context: Operators frequently disable or bypass protection before pulling a payload. Common
    precursors include AMSI bypass, execution-policy changes (-ExecutionPolicy Bypass or -ep bypass),
    Defender real-time-monitoring disablement, and PowerShell v2 downgrade to evade logging. Finding
    these precursor events on the host strengthens the case that the download is operator-driven.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full script content?
  context: Script block logging records the deobfuscated script content PowerShell actually
    executes, even when the command line is encoded or obfuscated. This provides ground truth about
    what ran. If PowerShell operational logs are not ingested in this environment, treat zero
    results as a visibility gap rather than evidence that nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: The parent process reveals how the download was invoked. Interactive use typically has
    explorer.exe or a console as the parent; scripted automation has a service or scheduler.
    Unusual parents -- Office applications, mshta.exe, wscript.exe, or a server-side application
    process spawning PowerShell -- point to exploitation or a malicious launcher rather than an
    administrator at a prompt.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1105.network_connection
  question: Did this host make an outbound connection to the direct IP, and is that destination
    internal or external?
  context: The detected command line names a numeric-IP target; this confirms whether the fetch
    actually reached the network and where it went. An external, previously-unseen destination is
    the concerning case for ingress tool transfer; an internal IP block often reflects a sanctioned
    distribution point or internal automation. Correlate the connection to the alerting PowerShell
    instance and note whether the destination falls inside or outside the organization's address
    space.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \powershell.exe
          - \pwsh.exe
          - \powershell_ise.exe
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
- source: template:download_tool.download_success_verification
  question: Was the download successful? Check for file creation by the PowerShell instance.
  context: A download command does not guarantee the file was retrieved. Network errors, blocked
    IPs, or security controls may have prevented the transfer. Confirming file creation at the
    destination path establishes whether the operation progressed past delivery into a staged
    payload on disk.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded payload from a safe environment?
  context: If the fetched file is still on disk, submitting its hash to threat intelligence or
    detonating it in a sandbox reveals whether it is a known malicious payload, a legitimate tool
    such as a remote-management installer, or something novel requiring deeper analysis. Direct-IP
    delivery is frequently used to stage installers and second-stage tooling.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the download's target directory?
  context: Operators often stage multiple files to the same directory, or the fetched file triggers
    further writes -- extraction, decompression, or additional downloads. Uncommon drop locations
    such as C:\Users\Public, %TEMP%, or ProgramData are worth particular attention. Directory-level
    file activity reveals the broader scope of the delivery operation.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the cross-host
    distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's full selection structure with no host scope, so the result
    is a fleet-wide count of this exact firing pattern -- a PowerShell interpreter fetching a
    direct-IP URL via a download verb -- not generic PowerShell usage. Many hosts firing the same
    pattern points to scripted deployment or vendor automation; one host or a small unrelated set
    points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - ' irm '
        - 'iwr '
        - 'wget '
      selection_ip:
        CommandLine|contains:
        - ://1
        - ://2
        - ://3
        - ://4
        - ://5
        - ://6
        - ://7
        - ://8
        - ://9
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - ' irm '
        - 'iwr '
        - 'wget '
      selection_ip:
        CommandLine|contains:
        - ://1
        - ://2
        - ://3
        - ://4
        - ://5
        - ://6
        - ://7
        - ://8
        - ://9
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - ' irm '
        - 'iwr '
        - 'wget '
      selection_ip:
        CommandLine|contains:
        - ://1
        - ://2
        - ://3
        - ://4
        - ://5
        - ://6
        - ://7
        - ://8
        - ://9
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity
    spanning multiple endpoints. If the same user triggered similar download activity on multiple
    hosts, the scope of the operation may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - ' irm '
        - 'iwr '
        - 'wget '
      selection_ip:
        CommandLine|contains:
        - ://1
        - ://2
        - ://3
        - ://4
        - ://5
        - ://6
        - ://7
        - ://8
        - ://9
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1105
      name: Ingress Tool Transfer
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: external_network_fetch
      description: "Unusual or uncommon process initiates a network connection to an external
        destination (direct-IP URL) to retrieve a tool."
      mapped_to: Q9
      source: attack:T1105.network_connection
    - stage: 2
      label: file_drop
      description: "Network transfer completes and a file (downloaded tool) is created on disk."
      mapped_to: Q10
      source: template:download_tool.download_success_verification
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1105
      covered_by: Q12
      source: template:download_tool.directory_activity
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1105
      covered_by: Q8
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1105
      covered_by: Q9
      source: attack:T1105.network_connection
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (tune for known-good updaters, e.g., ChromeUpdate, OneDrive)"
      disposition: context
      target_question: Q8
      notes: "No concrete values to inject; process_chain question surfaces ParentImage so the
        analyst can tune known-good updaters. Parent+binary pair also examined in Q15/Q17 (svc
        substitutes)."
    - element: DestinationIPCategory
      dimension: tuning_parameter
      specific_values: "rationale only (allow filtering internal vs external IP blocks)"
      disposition: new_question
      target_question: Q9
      notes: "Authored network_connection question that asks the analyst to classify the destination
        IP as internal vs external -- the exact internal/external distinction the mutable element
        describes."
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: "C:\\Users\\Public, %TEMP% (example uncommon drop paths)"
      disposition: context
      target_question: Q12
      notes: "Path patterns are derived at runtime from %file_paths%/%file_dirs%; the directory
        activity question's context names C:\\Users\\Public, %TEMP%, ProgramData as drop locations
        to focus on. No fixed path to inject into selection since the rule derives paths from
        CommandLine."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "Historical-frequency baseline of the binary is covered by cue_user_actor.baseline
        (user+binary 90d) and the fleet-pattern scope question; a third host+binary 90d aggregation
        adds no distinct angle for a rule whose interpreters are ubiquitous."
      kept: cue_user_actor.baseline / cue_process_tool.scope
    - dropped: cue_process_tool.baseline_deviation
      reason: "REFLECTED host-pattern deviation overlaps cue_process_tool.scope (REFLECTED
        fleet-pattern, same embedded detection block); kept the fleet variant for cross-host
        calibration and the parent/user deviation angle is carried by cue_user_actor.baseline_svc.
        Dropped to stay within the 18-question cap."
      kept: cue_process_tool.scope
    - dropped: cue_process_tool.follow_on
      reason: "Child-process follow-on for a download cradle is better answered by the file-drop
        chain (download_success_verification, directory_activity); the meaningful post-download
        artifact here is a written file, not a spawned child."
      kept: template:download_tool.download_success_verification
    - dropped: cue_process_tool.clustering
      reason: "Host-wide concurrent-process clustering duplicates cue_host_context.clustering, which
        was itself dropped for cap; cross-host user/parent clustering is retained via
        cue_user_actor.clustering and clustering_svc."
      kept: cue_user_actor.clustering / cue_user_actor.clustering_svc
    - dropped: cue_host_context.clustering
      reason: "Generic host-wide activity aggregation; the cross-host user/parent clustering pair
        and the fleet scope question already provide surrounding-activity context. Dropped to stay
        within the 18-question cap."
      kept: cue_user_actor.clustering / cue_process_tool.scope
    - dropped: cue_host_context.precursor
      reason: "Prior-alert timeline is partially covered by powershell_security_bypass (prior
        tamper/bypass on host, -1h). Dropped to stay within the 18-question cap on a single-technique
        rule; the bypass-precursor question carries the load-bearing precursor signal for a download
        cradle."
      kept: template:powershell.powershell_security_bypass
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair emitted as Q14 and Q15 -- non-deduplicable, kept BOTH per AGENTS.md
        (subject vs. mechanism identity pivots)."
      kept: cue_user_actor.baseline AND cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor clustering pair emitted as Q16 and Q17 -- non-deduplicable, kept BOTH per
        AGENTS.md (user-pivot degenerates under service principals)."
      kept: cue_user_actor.clustering AND cue_user_actor.clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. Safe-to-drop condition (b)-equivalent: a more specific
        selected question, powershell.powershell_security_bypass, already surfaces prior AV/EDR and
        AMSI/Defender tamper-and-bypass activity on this host in the -1h window. This is a
        PowerShell-interpreter execution rule, so the PowerShell-scoped bypass sweep is the more
        precise precursor question; the generic AV/EDR service-name sweep adds no distinct angle here.
        Recorded per the universal-question no-silent-drop requirement."
    - template: obfuscation.encoded_payload_decode
      reason: "Replaced by powershell.encoded_command_decode per brief hint; PowerShell-specific
        decode question (Q5) supersedes the generic obfuscation decode."
  hint_dispositions:
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted_on
      reason: "Included powershell.encoded_command_decode as Q5; obfuscation.encoded_payload_decode
        recorded as skipped (replaced)."
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted_on
      reason: "evidence_preservation (Q3), download_success_verification (Q10), payload_analysis
        (Q11), and directory_activity (Q12) all scope file_event queries via %file_paths% /
        %file_dirs% rather than a fixed path, since the rule derives the destination from the
        command line."
    - hint: "logsource 'network_connection' (recommended by techniques: T1105) is not covered — pull
        in cue_network_endpoint.* or author a custom question with category: network_connection"
      disposition: acted_on
      reason: "Authored attack:T1105.network_connection (Q9) with category: network_connection,
        scoped to the host and PowerShell interpreters, surfacing DestinationIp/Port and the
        internal-vs-external classification. Covers chain stage 1 and the DestinationIPCategory
        mutable element."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - download_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 11
      file_event: 4
      network_connection: 1
      powershell: 1
