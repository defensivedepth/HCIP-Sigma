name: Private Keys Reconnaissance Via CommandLine Tools
id: 0ed7e3e2-6955-4689-bda4-57792d6ee71b
description: |
  Detects command-line searches for private key and certificate files by their extensions (.key, .pem, .pfx, .ppk, .p12, .pgp, .gpg, .cer, .p7b, .asc). The rule fires when a key-extension reference appears on the command line AND the invoking tool is EITHER cmd.exe (with dir) OR powershell.exe/pwsh.exe (with Get-ChildItem) OR findstr.exe. Listing or grepping for key material is a credential-access reconnaissance step that often precedes exfiltration of the located files; it can also be benign administration or backup tooling. Determine whether the search was operator-driven or automated, what extensions and paths were targeted, whether any matching files exist on the host, and whether the located key material was subsequently read, copied, or sent off-host.
type: detection
detection_id: 213d6a77-3d55-4ce8-ba74-fcfef741974e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line search for private-key extensions using cmd dir, PowerShell Get-ChildItem, or findstr?
  context: Confirm the alert is a true positive by reviewing the exact command line, Image, and OriginalFileName against the rule's criteria. The rule fires when a key extension (.key, .pem, .pfx, .ppk, .p12, .pgp, .gpg, .cer, .p7b, .asc) appears on the command line AND one of three branches matched — cmd.exe with a dir search, powershell.exe/pwsh.exe with Get-ChildItem, or findstr.exe. Note which branch fired and which extensions were named, because the search tool and the targeted extensions pattern every downstream question.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to search the filesystem for private-key and certificate files?
  context: Listing key material by extension is normal for certificate administrators, backup jobs, PKI tooling, and developers managing SSH/code-signing keys, but unusual for general user accounts. Knowing whether the user role and host function justify a key-file search is often the fastest path to distinguishing routine operations from reconnaissance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1552.004.credential_target_identification
  question: Which key-file extensions and directories did the search target, and does the scope look enumerative?
  context: Decode the full command line to see exactly which extensions were named and which paths were enumerated. A recursive search across user profiles, .ssh directories, or system certificate stores covering several extensions at once points to credential reconnaissance; a single targeted lookup of one known file is closer to routine administration. The extensions present (e.g. .ppk and .pem for SSH keys, .pfx and .p12 for code-signing or TLS) narrow which credential type was sought.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1552.004.process_identification
  question: Is the searching process a genuine cmd.exe, powershell.exe, pwsh.exe, or findstr.exe, or a renamed binary?
  context: The rule matches on both Image path and OriginalFileName, so a renamed copy of a search tool placed elsewhere will still fire. Compare the Image path against OriginalFileName and the on-disk location — a findstr.exe or powershell.exe running from a user-writable or temp directory, or whose OriginalFileName disagrees with its filename, indicates masquerading rather than the in-box utility, which raises the weight of the reconnaissance finding.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: attack:T1552.004.file_path_analysis
  question: Do any private-key or certificate files matching the searched extensions actually exist on this host?
  context: The command line only proves a search ran, not that key material was present. Pivoting to file events for the matching extensions shows whether the search would have returned hits — confirmed key files on disk make the reconnaissance actionable and identify exactly which credentials are at risk and may need rotation.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_ext:
        TargetFilename|endswith:
          - .key
          - .pgp
          - .gpg
          - .ppk
          - .p12
          - .pem
          - .pfx
          - .cer
          - .p7b
          - .asc
      condition: selection and selection_ext
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this search tool, and is that parent chain expected?
  context: The parent process reveals how the search was invoked. An interactive shell launched from explorer.exe or a known administration console suggests operator or maintenance activity; spawning from a script host, Office application, remote-execution service, or a freshly written binary suggests the search is one step of a scripted intrusion.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What did the searching process do next, and were any located key files read or copied?
  context: A bare directory listing is reconnaissance; the disposition turns on what followed. Child processes that copy, archive, encode, or transmit the located files (copy, robocopy, certutil, compression utilities, or a transfer client) escalate this from a search to active credential collection and staging.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1552.004.security_share_access
  question: Were the located key files accessed over an SMB network share around the time of the search?
  context: Key material is frequently pulled from file shares rather than read locally. Windows Security event 5145 records detailed share-access checks; reviewing share accesses to objects with key extensions in the alert window shows whether the reconnaissance was followed by remote retrieval of the files from a server, which would not appear in local process or file telemetry on the alerting host.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: security
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 5145
      selection_ext:
        RelativeTargetName|contains:
          - .key
          - .pgp
          - .gpg
          - .ppk
          - .p12
          - .pem
          - .pfx
          - .cer
          - .p7b
          - .asc
      condition: selection and selection_ext
    fields:
      - RelativeTargetName
      - ShareName
      - SubjectUserName
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which keys or certificates would the searched paths expose, and do any protect privileged access?
  context: The severity depends on what the located key material protects. A domain code-signing certificate, a CA key, an SSH key for a privileged jump host, or a VPN client certificate warrants escalation and rotation; a self-signed developer key may only need monitoring. Cross-reference the targeted paths and any matching files against the systems and identities those credentials authenticate to.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously matched the rule's key-search pattern, what parents and users drove it, and does the current alert fit?
  context: Even routine PKI and backup tooling can search for key files. The query mirrors the rule's full selection structure scoped to this host, so the resulting parent/user distribution reflects how this specific key-search pattern has historically run here. A new parent process, an unexpected user account, or an unusual command-line structure relative to that history is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmd_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cmd_cli:
        CommandLine|contains: 'dir '
      selection_pwsh_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_pwsh_cli:
        CommandLine|contains: 'Get-ChildItem '
      selection_findstr:
      - Image|endswith: \findstr.exe
      - OriginalFileName: FINDSTR.EXE
      selection_ext:
        CommandLine|contains:
        - .key
        - .pgp
        - .gpg
        - .ppk
        - .p12
        - .pem
        - .pfx
        - .cer
        - .p7b
        - .asc
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection_ext and (all of selection_cmd_* or all of selection_pwsh_* or selection_findstr)) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmd_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cmd_cli:
        CommandLine|contains: 'dir '
      selection_pwsh_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_pwsh_cli:
        CommandLine|contains: 'Get-ChildItem '
      selection_findstr:
      - Image|endswith: \findstr.exe
      - OriginalFileName: FINDSTR.EXE
      selection_ext:
        CommandLine|contains:
        - .key
        - .pgp
        - .gpg
        - .ppk
        - .p12
        - .pem
        - .pfx
        - .cer
        - .p7b
        - .asc
      _pb_scope:
        User|expand: '%User%'
      condition: (selection_ext and (all of selection_cmd_* or all of selection_pwsh_* or selection_findstr)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmd_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cmd_cli:
        CommandLine|contains: 'dir '
      selection_pwsh_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_pwsh_cli:
        CommandLine|contains: 'Get-ChildItem '
      selection_findstr:
      - Image|endswith: \findstr.exe
      - OriginalFileName: FINDSTR.EXE
      selection_ext:
        CommandLine|contains:
        - .key
        - .pgp
        - .gpg
        - .ppk
        - .p12
        - .pem
        - .pfx
        - .cer
        - .p7b
        - .asc
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_ext and (all of selection_cmd_* or all of selection_pwsh_* or selection_findstr)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether the key search was an isolated action or part of broader hands-on activity. The same user searching for key material or running other reconnaissance on multiple endpoints in the same window widens the likely scope of compromise.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmd_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cmd_cli:
        CommandLine|contains: 'dir '
      selection_pwsh_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_pwsh_cli:
        CommandLine|contains: 'Get-ChildItem '
      selection_findstr:
      - Image|endswith: \findstr.exe
      - OriginalFileName: FINDSTR.EXE
      selection_ext:
        CommandLine|contains:
        - .key
        - .pgp
        - .gpg
        - .ppk
        - .p12
        - .pem
        - .pfx
        - .cer
        - .p7b
        - .asc
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_ext and (all of selection_cmd_* or all of selection_pwsh_* or selection_findstr)) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this search?
  context: A key-file search rarely stands alone. Earlier detections on the same host — execution from a script host, defense tampering, or other reconnaissance — may reveal the access stage that preceded this credential-hunting step and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1552.004
      name: Private Keys
      role: primary
    - id: T1552
      name: Unsecured Credentials
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: search_invocation
      description: A process searches the filesystem for private-key files by extension via cmd dir, PowerShell Get-ChildItem, or findstr
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: target_decode
      description: Decode which extensions and directories were enumerated to characterize the search scope
      mapped_to: Q3
      source: attack:T1552.004.credential_target_identification
    - stage: 3
      label: key_material_present
      description: Determine whether matching private-key files actually exist on the host (file_event / share access)
      mapped_to: Q5
      source: attack:T1552.004.file_path_analysis
    - stage: 4
      label: collection_and_exfil
      description: Located key files are read, copied, compressed, or transmitted; or used for follow-on authentication
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1552.004
      covered_by: Q5
      source: attack:T1552.004.file_path_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1552.004
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: process_identification
      technique: T1552.004
      covered_by: Q4
      source: attack:T1552.004.process_identification
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1552.004
      covered_by: Q3
      source: attack:T1552.004.credential_target_identification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1552
      covered_by: Q3
      source: attack:T1552.004.credential_target_identification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1552
      covered_by: Q14
      source: template:cue_user_actor.clustering
      coverage_type: direct
    - dimension: user_context
      technique: T1552
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: ".pem, .pfx, .ppk, .p12, .key, C:\\Users\\*\\.ssh\\ and other key extensions/cert dirs"
      disposition: query_injection
      target_question: Q5
      notes: Key extensions injected into file_event selection_ext and Security 5145 RelativeTargetName selection; decoded scope addressed in Q3 context.
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "benign cert-mgmt tools e.g. certutil.exe, ssh.exe (exclusion rationale)"
      disposition: context
      target_question: Q6
      notes: Rationale only (which parents are benign); folded into process_chain context rather than a hardcoded exclusion list.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — threshold for multiple-file access indicating automation"
      disposition: context
      target_question: Q14
      notes: No concrete threshold value; clustering window question carries the automation-vs-manual framing.
    - element: SuspiciousProcessList
      dimension: tuning_parameter
      specific_values: "reg.exe, cmd.exe, powershell.exe (rule already constrains to cmd/pwsh/findstr)"
      disposition: context
      target_question: Q3
      notes: Rule's own selections already enumerate the searching binaries; no additional injection needed.
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification — used as Q3 (re-sourced as attack:T1552.004 to carry the decoded-target framing)"
      kept: credential_access.credential_target_identification
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q12 user-pivot, Q13 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q14 user-pivot, Q15 parent+binary substitute)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "Subsumed by cue_process_tool.baseline_deviation (Q11), which scopes to the rule's actual key-search pattern on this host rather than generic searches-binary frequency; included instead to avoid two near-identical 90d/14d host-frequency questions and stay under the 18-question cap."
    - template: cue_process_tool.clustering
      reason: "Generic host-wide tool clustering overlaps cue_host_context.precursor (Q16, prior alerts on host) and cue_user_actor.clustering (Q14, user activity in window); dropped to stay under cap without losing distinct coverage."
    - template: cue_process_tool.scope
      reason: "Fleet-wide rule-pattern count overlaps cue_user_actor.clustering_svc (Q15), which already surfaces same-window cross-host firing of the same parent+binary launch; dropped to stay under the 18-question cap."
    - template: cue_host_context.clustering
      reason: "Host-wide Image+ParentImage activity overlaps cue_host_context.precursor (Q16) and the follow_on / process_chain questions; dropped to stay under cap."
    - template: powershell.encoded_command_decode
      reason: "Rule's PowerShell branch is plain-text Get-ChildItem recon, not encoded or obfuscated execution — there is no -EncodedCommand or cradle payload to decode here. Decoded-target characterization is covered by Q3."
    - template: powershell.script_block_logging
      reason: "Search command is a single readable Get-ChildItem; script-block logging adds no deobfuscation value over the already-readable command line surfaced by Q1/Q3, per template note for short single-cmdlet commands."
    - template: powershell.powershell_security_bypass
      reason: "Rule detects credential-access reconnaissance, not PowerShell payload execution; no AMSI/execution-policy bypass precedes a Get-ChildItem listing. The generic host-tamper precursor signal is carried by cue_host_context.precursor (Q16)."
    - template: universal.evidence_preservation
      reason: "Detection references file paths only inside the CommandLine of a search — no payload file is created by the rule. The on-disk-presence question is reframed as attack:T1552.004.file_path_analysis (Q5), which scopes file_event to the searched key extensions rather than a single %file_paths% target."
    - template: universal.endpoint_protection_health
      reason: "Recon-only rule (directory listing for key files); no payload execution whose disposition turns on prior AV/EDR tampering. cue_host_context.precursor (Q16) already surfaces any prior tamper alerts on the host."
  reflection_overrides: []
  hint_dispositions:
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: declined
      reason: >-
        The rule never captures a concrete file path — it matches key
        extensions appearing inside a search command's CommandLine, so there
        is no %file_paths% value to expand at hydration. Q5
        (attack:T1552.004.file_path_analysis) instead scopes file_event on this
        host to the literal key extensions (.pem, .pfx, .ppk, .p12, .key, etc.)
        to test whether matching key material actually exists, which is the
        on-disk question %file_paths% would otherwise serve.
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: declined
      reason: >-
        Neither encoded-command nor obfuscation decoding applies: the rule's
        PowerShell branch fires on a plain-text Get-ChildItem search for key
        extensions, with no -EncodedCommand, concatenation, or cradle to decode.
        The decoded-target characterization the analyst needs (which extensions
        and directories) is provided by Q3
        (attack:T1552.004.credential_target_identification).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 16
    source_counts:
      template: 12
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 12
      file_event: 1
      security: 1
      sigma_alert: 2
