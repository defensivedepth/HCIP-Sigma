name: Remote File Download Via Desktopimgdownldr Utility
id: 22f6dd73-62fc-4a10-8ad8-518873ce250e
description: |
  Detects desktopimgdownldr.exe, a signed Windows lockscreen/desktop-image helper, invoked with a /lockscreenurl:http argument. The rule fires when the process Image AND its parent both end in desktopimgdownldr.exe AND the command line contains /lockscreenurl:http, treating the invocation as a living-off-the-land file-download alternative to certutil. Note that the outbound transfer is not performed by desktopimgdownldr.exe itself; the actual TCP connection is made by svchost.exe hosting the BITS COM service, so the network and file evidence must be hunted on those processes correlated by time and user. The investigation should determine what the supplied URL points to, whether a file landed on disk, what spawned the chain, and whether the activity is a one-off versus a fleet-wide pattern.
type: detection
detection_id: 214641c2-c579-4ecb-8427-0cf19df6842e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects — desktopimgdownldr.exe with a /lockscreenurl:http argument?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule requires the process Image to end in desktopimgdownldr.exe, the parent to also be desktopimgdownldr.exe, and the command line to contain /lockscreenurl:http. Read the full /lockscreenurl value and any /eventName argument from the triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to download files via the desktopimgdownldr lockscreen utility?
  context: Legitimate use of desktopimgdownldr.exe is almost entirely administrative image provisioning, and even then the /lockscreenurl:http download path is rare. Knowing whether the user role and host function involve lockscreen or desktop-image management is often the fastest way to separate a benign admin action from living-off-the-land file transfer.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: What spawned the desktopimgdownldr chain, and is that grandparent expected to launch lockscreen-image tooling?
  context: The rule fires when desktopimgdownldr.exe runs with a desktopimgdownldr.exe parent, so the meaningful origin is what spawned that parent. A grandparent such as a script interpreter, an Office application, or an unexpected shell points to operator-driven download abuse; a routine provisioning or management process points to legitimate image deployment. If ParentProcessGuid resolves to NODATA, pivot on the parent binary and time window on this host instead.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent spawn around the alert?
  context: If the parent that launched this desktopimgdownldr invocation also spawned other processes, those siblings reveal the full scope of activity from that execution. Multiple tools launched from the same parent in a short window suggests scripted or hands-on-keyboard orchestration rather than a single benign image download.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What does the /lockscreenurl value point to, and what is the full command line including any SYSTEMROOT redirection or /eventName argument?
  context: The full command line reveals the download target and the operating mode. Decode the URL from /lockscreenurl to identify the destination host, port, and file. Watch for a preceding SYSTEMROOT redirect to a writable temp directory, which is the standard-user variant that skips the registry write and lockscreen replacement, and for an /eventName argument that controls the saved filename. The host-scoped window captures any companion desktopimgdownldr invocations that form one logical operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1105.network_connection
  question: Was an outbound connection to the /lockscreenurl destination made by svchost.exe hosting the BITS service around the alert?
  context: The transfer is not performed by desktopimgdownldr.exe; it queues a BITS job and the actual TCP connection is made by svchost.exe running the BITS service. Confirming an outbound connection from svchost to the destination host in the alert window establishes that the download attempt actually reached the network. A connection to an external address is the expected pattern for ingress tool transfer; internal-only destinations may be benign image provisioning. If no svchost outbound connection appears, the job may have failed, been blocked, or targeted an internal host.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \svchost.exe
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:download_tool.download_success_verification
  question: Was the download successful? Check for file creation at the lockscreen destination path.
  context: A queued download does not guarantee the file was retrieved. Network errors, blocked URLs, or security controls may have stopped the transfer. Under intended use the file lands at C:\Windows\Personalization\LockScreenImage; the standard-user variant redirects it under a writable temp directory. Confirming file creation at the resolved path establishes whether the activity progressed past delivery.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded file from a safe environment?
  context: If the file delivered through desktopimgdownldr is still on disk, computing its hash and submitting it to threat intelligence services or detonating it in a sandbox reveals whether it is a benign image, a known malicious payload, or something novel. The /lockscreenurl mechanism does not validate that the target is actually an image, so treat the file as untrusted regardless of extension.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the download target directory?
  context: Operators often stage multiple files in the same directory, or the first download triggers further writes such as extraction or additional downloads. Examining directory-level file activity around the alert reveals the broader scope of the delivery operation beyond the single file named in the command line.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:universal.evidence_preservation
  question: Is the downloaded file still on disk for analysis?
  context: If the file referenced through the /lockscreenurl download is still present, it can be hashed, submitted to threat intelligence, or reviewed for content. Capture it before any cleanup, since the admin cleanup variant of this technique deletes the PersonalizationCSP registry key and may remove traces of the operation.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.baseline
  question: Does this host normally run desktopimgdownldr.exe?
  context: desktopimgdownldr.exe is a niche provisioning helper that most endpoints never execute. A host with zero or very low historical executions over 90 days makes this a first-time or rare invocation, which raises the weight of the download. A host that runs it routinely points toward managed image deployment.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \desktopimgdownldr.exe
        ParentImage|endswith: \desktopimgdownldr.exe
        CommandLine|contains: /lockscreenurl:http
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \desktopimgdownldr.exe
        ParentImage|endswith: \desktopimgdownldr.exe
        CommandLine|contains: /lockscreenurl:http
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts in the alert window reveals lateral movement or parallel activity on multiple endpoints. If the same user triggered comparable tool execution elsewhere, the scope of the operation may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \desktopimgdownldr.exe
        ParentImage|endswith: \desktopimgdownldr.exe
        CommandLine|contains: /lockscreenurl:http
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining process activity around the alert reveals what led up to the download and what followed — reconnaissance, additional tool transfers, or persistence. Aggregating by process and parent collapses routine agent noise into high-count rows while novel pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, defense tampering, or initial-access activity — may reveal the stages that preceded this download and help reconstruct the timeline. Ingress tool transfer is typically a mid-chain step that follows an initial foothold, so prior alerts often explain how the operator arrived here.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_parent_process.scope
  question: Is this desktopimgdownldr parent-child spawning pattern occurring on other hosts?
  context: The same parent-child relationship appearing on multiple hosts may indicate a campaign or a pushed automation pattern rather than a one-off. A single occurrence is more likely targeted or accidental. An environment-wide comparison helps calibrate severity and identify the scope.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1105
      name: Ingress Tool Transfer
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: process_initiation
      description: "Unusual process (desktopimgdownldr.exe) is launched to initiate an ingress tool transfer"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 1b
      label: command_decode
      description: "Decode the /lockscreenurl target and operating mode of the download invocation"
      mapped_to: Q5
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: network_connection
      description: "Process (svchost/BITS on behalf of desktopimgdownldr) initiates a network connection to an external destination"
      mapped_to: Q6
      source: attack:T1105.network_connection
    - stage: 3
      label: file_creation
      description: "File creation at the destination path (tool downloaded)"
      mapped_to: Q7
      source: template:download_tool.download_success_verification
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1105
      covered_by: Q7
      source: template:download_tool.download_success_verification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1105
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1105
      covered_by: Q6
      source: attack:T1105.network_connection
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (tune for known-good updaters)"
      disposition: context
      target_question: Q3
      notes: "No specific values given; parent legitimacy and baseline parent+binary questions (Q3, Q14, Q18) carry the parent_process_analysis angle. desktopimgdownldr's own parent is the rule's ParentImage."
    - element: DestinationIPCategory
      dimension: tuning_parameter
      specific_values: "rationale only (internal vs external IP blocks)"
      disposition: context
      target_question: Q6
      notes: "No literal IP blocks supplied. Internal-vs-external destination framing injected into the svchost/BITS network-connection context as the tuning lever for this dimension."
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: "C:\\Windows\\Personalization\\LockScreenImage (default path from db_refs); writable temp under standard-user SYSTEMROOT redirect"
      disposition: context
      target_question: Q7
      notes: "The default download path and the temp-redirect variant are stated in the file-creation and decode question context. File queries use %file_paths%/%file_dirs% per hint 2."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (hint 1) — parent_child.parent_process_legitimacy subsumes process_chain"
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.mechanism_decode
      reason: "parent_child technique set active — parent_process_legitimacy is the more specific parent question"
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "overlaps parent_child.sibling_processes (other children of the parent); sibling_processes uses ParentProcessGuid precision"
      kept: template:parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "parent-scoped 'other activity' subsumed by sibling_processes plus cue_host_context.clustering"
      kept: template:cue_host_context.clustering
    - dropped: cue_parent_process.baseline
      reason: "host-frequency of desktopimgdownldr already answered by cue_process_tool.baseline; parent here is the same binary, so the parent-child baseline is degenerate"
      kept: template:cue_process_tool.baseline
    - dropped: cue_user_actor.baseline / baseline_svc / clustering / clustering_svc pairing
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute variants emitted (Q13/Q14 baseline pair, Q15/Q16 clustering pair)"
      kept: all four
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: >-
        RECOMMENDED tier, not must_include. This is an ingress-tool-transfer / command-and-control
        rule, not an execution or credential-access rule where the disposition turns on whether
        protection was weakened first. Drop condition (b) from the template notes_to_llm applies:
        cue_host_context.precursor (Q17) is present and surfaces prior tamper-as-precursor alerts on
        the host, and the rule is not execution/credential-centric. Dropped to stay within the
        18-question cap; prior-alert context carries the tamper signal sufficiently for triage.
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'network_connection' (recommended by techniques: T1105) is not covered — pull in cue_network_endpoint.* or author a custom question with category: network_connection"
      disposition: acted_on
      reason: >-
        Acted on. Authored Q6 (attack:T1105.network_connection) as a custom network_connection
        question. Per db_refs the outbound transfer is made by svchost.exe hosting the BITS service,
        not by desktopimgdownldr.exe, so the query scopes to host + svchost.exe rather than the
        alerting binary. This is the load-bearing network pivot for the ingress-tool-transfer chain.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - download_tool
    - parent_child
    - cue_parent_process
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 12
      file_event: 4
      network_connection: 1
      sigma_alert: 1
