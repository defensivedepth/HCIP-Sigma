name: Vulnerable Driver Blocklist Registry Tampering Via CommandLine
id: 55abe369-720b-41b3-be14-9cae13cb5009
description: |
  Detects command-line tampering with the Windows Vulnerable Driver Blocklist registry configuration, using powershell.exe, pwsh.exe, or reg.exe to write the VulnerableDriverBlocklistEnable value under \Control\CI\Config. The blocklist prevents the loading of known-exploitable signed kernel drivers; disabling it is a common precursor to bring-your-own-vulnerable-driver (BYOVD) activity, where an attacker loads a signed-but-vulnerable driver to run code in kernel mode and disable endpoint protection. The rule fires when all of [a matching binary, a registry-write verb (add / New-ItemProperty / Set-ItemProperty / si), and both the \Control\CI\Config path and the VulnerableDriverBlocklistEnable value name] are present in the command line. Investigation should determine what value was written (enable vs. disable), who and what launched the change, and whether a driver load or further defense tampering followed.
type: detection
detection_id: 22154f0e-5132-4a54-aa78-cc62f6def531
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-08'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line modify VulnerableDriverBlocklistEnable under \Control\CI\Config using powershell.exe, pwsh.exe, or reg.exe?
  context: Confirm the alert is a true positive before investing time. The rule fires only when a matching binary, a registry-write verb (add, New-ItemProperty, Set-ItemProperty, or si), and both the \Control\CI\Config path and the VulnerableDriverBlocklistEnable value name all appear together. Read the full command line and the registry value being written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to modify kernel driver blocklist policy from the command line?
  context: Vulnerable Driver Blocklist configuration is managed centrally through Windows servicing and App Control policy, not by ad-hoc command-line edits. Knowing whether the user role and host function involve security-policy administration is usually the fastest way to separate a sanctioned change from tampering.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: What value was written to VulnerableDriverBlocklistEnable, and does it enable or disable the blocklist?
  context: The data written determines impact. A value of 0 disables the Vulnerable Driver Blocklist, leaving the host able to load known-exploitable signed drivers; a value of 1 re-enables it. Decode the full command line to confirm the value name, the data written, and which verb (add / Set-ItemProperty / New-ItemProperty / si) performed the change.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:registry.registry_key_context
  question: What registry key path and value data were actually written by this process?
  context: When the registry write is captured as a structured registry event, the target object and value data are available directly rather than parsed from the command line. Confirm the modification landed under \Control\CI\Config\VulnerableDriverBlocklistEnable and check the written data, since the command line and the committed value can differ if the write failed or was redirected.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:cue_process_tool.process_chain
  question: What process spawned this powershell.exe, pwsh.exe, or reg.exe instance, and is that parent chain expected?
  context: The parent process reveals how the registry edit was invoked. A change driven by a deployment or configuration-management parent looks different from one launched by an interactive shell, a script host (wscript.exe, mshta.exe), or an Office application. Walk up to the parent and note what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1562.001.driver_load
  question: Was a kernel driver or new service loaded on this host after the blocklist change?
  context: Disabling the Vulnerable Driver Blocklist is typically a setup step for loading a signed-but-vulnerable driver to gain kernel-mode execution (BYOVD). After a disable, look for new kernel service creation (sc create, New-Service) or driver-install tooling on the host. A driver load shortly after the blocklist was disabled is the payoff the tampering enabled.
  range: +4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_driver:
        - Image|endswith:
            - \sc.exe
            - \drvinst.exe
            - \rundll32.exe
        - CommandLine|contains:
            - 'New-Service'
            - 'sc create'
            - 'sc.exe create'
            - 'type= kernel'
            - 'type=kernel'
            - '.sys'
      condition: selection and selection_driver
    fields:
      - Image
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is the Vulnerable Driver Blocklist currently enabled again, or does the host remain in the disabled state?
  context: Tampering may be transient (disabled for one action, then restored) or persistent. If the blocklist remains disabled, the host can still load known-exploitable drivers and the setting should be restored before the investigation closes. Check the live registry value and policy status.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the blocklist was modified?
  context: Defenses are weakened for a reason. Examining process activity in the window after the change reveals what the tampering was meant to enable, such as driver installation, payload execution, or credential access that would otherwise be blocked or logged.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host around the same time?
  context: An actor who disables one control often disables several. Modifying the driver blocklist alongside stopping AV/EDR services, clearing event logs, or deleting shadow copies indicates a coordinated effort to remove protection. Finding multiple tampering events from the same user or parent reveals the full scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
        - reg.exe
      selection_cli_1:
        CommandLine|contains:
        - 'add '
        - 'New-ItemProperty '
        - 'Set-ItemProperty '
        - 'si '
      selection_cli_2:
        CommandLine|contains|all:
        - \Control\CI\Config
        - VulnerableDriverBlocklistEnable
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
        - reg.exe
      selection_cli_1:
        CommandLine|contains:
        - 'add '
        - 'New-ItemProperty '
        - 'Set-ItemProperty '
        - 'si '
      selection_cli_2:
        CommandLine|contains|all:
        - \Control\CI\Config
        - VulnerableDriverBlocklistEnable
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the user's activity across all hosts reveals whether this was an isolated change or part of broader hands-on activity. The same user driving similar changes on multiple endpoints widens the potential scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
        - reg.exe
      selection_cli_1:
        CommandLine|contains:
        - 'add '
        - 'New-ItemProperty '
        - 'Set-ItemProperty '
        - 'si '
      selection_cli_2:
        CommandLine|contains|all:
        - \Control\CI\Config
        - VulnerableDriverBlocklistEnable
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this same blocklist-tampering pattern, what parents and users drove it, and does the current alert fit?
  context: Even routine binaries can be abused. This query mirrors the rule's selection scoped to this host, so the parent/user distribution reflects how this specific firing pattern — not generic powershell or reg usage — has historically run here. A new parent, an unexpected user, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
        - reg.exe
      selection_cli_1:
        CommandLine|contains:
        - 'add '
        - 'New-ItemProperty '
        - 'Set-ItemProperty '
        - 'si '
      selection_cli_2:
        CommandLine|contains|all:
        - \Control\CI\Config
        - VulnerableDriverBlocklistEnable
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this same blocklist-tampering pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection with no host scope, giving a fleet-wide count of this specific firing pattern rather than generic binary usage. Many hosts firing the same pattern points to scripted deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use host distribution and parent/binary consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
        - reg.exe
      selection_cli_1:
        CommandLine|contains:
        - 'add '
        - 'New-ItemProperty '
        - 'Set-ItemProperty '
        - 'si '
      selection_cli_2:
        CommandLine|contains|all:
        - \Control\CI\Config
        - VulnerableDriverBlocklistEnable
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — reconnaissance, credential access, or other defense tampering — may reveal the attack stages that preceded the blocklist change and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Examining activity surrounding the alert reveals the immediate context: what led to the registry change, what happened after, and whether it is part of a larger sequence. Novel parent-child pairs stand out against repetitive system telemetry.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tamper_invocation
      description: "Actor invokes powershell.exe/pwsh.exe/reg.exe with a registry-write verb against \\Control\\CI\\Config"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: blocklist_value_write
      description: "VulnerableDriverBlocklistEnable value is written (disable vs. enable)"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 3
      label: registry_commit
      description: "The registry modification is committed to \\Control\\CI\\Config"
      mapped_to: Q4
      source: template:registry.registry_key_context
    - stage: 4
      label: driver_load_payoff
      description: "A vulnerable/unsigned kernel driver or service is loaded once the blocklist is disabled (BYOVD)"
      mapped_to: Q6
      source: attack:T1562.001.driver_load
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q8
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q10
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q10
      notes: "Tuning rationale (expected admin tools) — no concrete values; informs the baseline/legitimacy framing."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Correlation-window rationale; realized as the +1h post-tamper range and +/-1h other-tamper range."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Security-service name list is a tuning rationale; the other-tampering query carries service-stop indicators rather than a per-deployment name list."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Authorized-tool exclusion rationale; realized via the parent-chain legitimacy question."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Service-criticality refinement rationale; no concrete values to inject."
  dedup_decisions:
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable; emitted alongside cue_user_actor.baseline_svc (subject vs. mechanism pivot)"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable; emitted alongside cue_user_actor.clustering_svc (subject vs. mechanism pivot)"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
    - dropped: cue_process_tool.baseline
      reason: "host-binary frequency over 90d subsumed by cue_process_tool.baseline_deviation, which scopes to the rule's actual firing pattern on this host rather than generic powershell/reg usage"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "generic child-of-binary query subsumed by attack:T1562.001.driver_load (driver/service follow-on) and defense_tampering.activity_after_tampering (post-tamper host activity)"
      kept: attack:T1562.001.driver_load + defense_tampering.activity_after_tampering
    - dropped: cue_process_tool.clustering
      reason: "tool co-occurrence subsumed by cue_host_context.clustering (same +/-15m host aggregation) and defense_tampering.other_tampering_activity"
      kept: cue_host_context.clustering
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context (and defense_tampering.what_was_disabled covers the CommandLine decode of the tamper value)"
    - template: powershell.encoded_command_decode
      reason: "Rule detects plain-text reg-write verbs (add/Set-ItemProperty) and literal value names; no -enc/-EncodedCommand or obfuscation to decode."
    - template: powershell.script_block_logging
      reason: "Detection is a short, fully readable registry-write command; 4104 script-block content adds no ground truth the CommandLine does not already carry."
    - template: powershell.powershell_security_bypass
      reason: "Rule does not detect a PowerShell control bypass; prior-tamper context is already carried by defense_tampering.other_tampering_activity and cue_host_context.precursor."
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled / other_tampering_activity questions supersede the generic host-tamper sweep (per the template's own drop guidance)."
    - template: registry.registry_related_modifications
      reason: "Other reg.exe/PowerShell registry writes on the host are already surfaced by defense_tampering.other_tampering_activity (which includes reg/powershell write verbs) within an overlapping window."
    - template: persistence_scope.process_based_persistence
      reason: "Rule is not a persistence rule (applicability.is_persistence_rule: false); persistence_scope set was pulled in by the reg.exe binary, but blocklist tampering is defense-evasion, not foothold installation. Driver/service follow-on is covered by attack:T1562.001.driver_load."
    - template: persistence_scope.file_based_persistence
      reason: "Startup-folder / Run-key file drops are unrelated to driver-blocklist tampering; rule is not a persistence rule. Slot used for the BYOVD driver-load follow-on instead."
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: declined
      reason: >-
        cue_process_tool.mechanism_decode was dropped entirely (replaced_by
        registry.registry_key_context). The surviving Phase 2 decode
        (defense_tampering.what_was_disabled) is ProcessGuid-scoped, not
        Image-scoped, so the powershell.exe over-breadth this hint warns about
        does not apply. The two reflected host/fleet baseline queries that do
        touch the binary already AND in the rule's full CommandLine selection
        (\Control\CI\Config + VulnerableDriverBlocklistEnable), so they are not
        bare Image scopes either.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - registry
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      sigma_alert: 1
