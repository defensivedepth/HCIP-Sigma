name: Process Reconnaissance Via Wmic.EXE
id: e88d4668-00c5-40c5-9a94-99bdbd15edaf
description: |
  Detects wmic.exe being run with a command line that contains 'process'
  while not also containing both 'call' and 'create' together. This pattern
  corresponds to listing or querying running processes through WMI rather than
  using wmic to spawn a new process, and it is a common host-reconnaissance and
  software-inventory step. Administrators, inventory agents, and software
  packaging also enumerate processes this way, so the investigation should
  decode exactly what was queried, establish whether the user, host, and parent
  chain match expected operations, and check whether the enumeration sits inside
  a broader sequence of discovery or follow-on tooling.
type: detection
detection_id: 221b251a-357a-49a9-920a-271802777cc0
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line run wmic with the process flag and without a call create together?
  context: Confirm the alert is a true positive by reviewing the exact command line against the
    rule's criteria. The rule fires when wmic.exe (by OriginalFileName or an Image ending in
    \WMIC.exe) is run with a command line that contains 'process' AND does not contain both
    'call' and 'create'. Verify the process keyword is present, confirm this is an enumeration
    rather than a wmic process call create spawn, and note the specific alias, sub-command, or
    filter used to query processes.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to enumerate processes through wmic?
  context: Legitimate process and inventory queries are common from management agents, software
    packaging, and administrators, but interactive process enumeration via wmic from a regular
    user workstation is less routine. Knowing whether the user role and host function include
    WMI-driven reconnaissance is often the fastest way to separate routine operations from
    discovery activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this wmic process query do? Decode the full command line.
  context: The full command line reveals what was enumerated. Decode the alias or WMI path to see
    whether it listed all processes, filtered for a specific process name, requested particular
    properties, or escaped to a schema class such as Win32_Process, and check for a /node argument
    naming a remote host, which would indicate the query targeted another machine. Process
    enumeration is often a precursor to selecting a target for injection, credential access, or
    defense evasion. The query scopes to wmic executions on this host in the surrounding window,
    constrained to the process keyword, so a sequence of related enumeration commands is visible.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains: process
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1047.network_connection
  question: Did wmic open a remote connection to another host around the time of this command?
  context: WMI queries can run against a remote target. When wmic is pointed at another host with
    /node, it opens an outbound DCOM/RPC connection from this machine rather than reading the
    local process list. A connection from wmic.exe to a remote endpoint in the alert window
    indicates the enumeration was aimed at another host, widening the scope; correlate the
    destination against any host named in the command line and watch for the same wmic process
    reaching several distinct remote hosts, which points to scripted remote discovery.
  range: +/-5m
  query: |
    aggregation: true
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1047.wmi
  question: Did the WMI provider record query or method activity on this host in the alert window?
  context: WMI activity logs record the provider operations that back a wmic invocation,
    independent of the process command line. Operational WMI events around the alert confirm the
    enumeration was carried out through the WMI provider and can show queries that originated
    remotely, which a local process event alone does not reveal. If WMI activity logs are not
    collected in this environment, treat zero results as a visibility gap rather than evidence
    nothing ran, and rely on the process and network evidence instead.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: wmi
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - Operation
      - PossibleCause
- source: template:cue_process_tool.process_chain
  question: What process spawned this wmic instance and is that parent chain expected?
  context: The parent process reveals how wmic was invoked. Enumeration driven by an interactive
    shell, a scripting host such as wscript.exe or mshta.exe, or an Office application is more
    concerning than a launch from a management agent or a scheduled inventory task. Walk the chain
    back from the immediate parent to understand who initiated the query.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this wmic instance spawn afterward?
  context: Reconnaissance is usually a setup step for further action. Children such as cmd.exe,
    powershell.exe, or certutil.exe spawned by this wmic process, or additional discovery and
    transfer tooling immediately after, point to a hands-on sequence rather than a single isolated
    inventory query.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the process activity surrounding the wmic
    process query reveals what led to it and what followed, and whether it is one step in a larger
    sequence of reconnaissance, privilege escalation, or staging. Process enumeration often runs
    alongside other discovery commands; novel parent-child pairs at low counts stand out against
    repetitive agent telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.baseline
  question: Does this host normally run wmic?
  context: A binary that has never been seen on a host is more suspicious than one that runs
    regularly. Zero or very low historical counts of wmic on this host indicate first-time or rare
    use and raise the weight of the detected enumeration.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this wmic process-query pattern, what parents and
    users normally drive it, and does the current alert fit?
  context: Even routine tooling can be abused. The query mirrors the rule's selection scoped to
    this host, so the parent and user distribution reflects how this specific process-enumeration
    pattern has historically run here rather than generic wmic usage. A new parent process, an
    unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
        CommandLine|contains: process
      filter_main_creation:
        CommandLine|contains|all:
        - call
        - create
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection* and not 1 of filter_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run wmic?
  context: Compare this user's current action against their historical behavior. A user who has
    never run wmic to enumerate processes is more notable than one who does so regularly, and the
    result should be weighed against the user's role and expected toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and wmic combination historically routine on this host or across the
    environment, or is the parent unusual for wmic?
  context: When the alert fires under a service principal, user identity does not attribute the
    behavior. The meaningful pivot is the launch mechanism, which parent process spawned wmic and
    whether that pair is historically common. A new or rare parent for wmic on this host or
    environment-wide is the deviation worth chasing; a pair that already accounts for thousands of
    prior events is vendor or operating-system automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or actions on
    multiple endpoints. If the same user drove similar enumeration or other discovery tooling on
    several machines, the scope of the activity is larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and wmic combination fire on other hosts in the same window?
  context: Under a service principal, asking what else the user did across the network returns
    agent-telemetry noise because every host runs SYSTEM-owned agents constantly. The meaningful
    question is whether the same launch mechanism, the parent process spawning wmic, fired on
    other hosts in the alert window. Multiple hosts in the same window points to scheduled or
    pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Reconnaissance rarely happens in isolation. Earlier detections on the same host, such
    as initial access, defense tampering, or other discovery commands, may reveal the stages that
    preceded this enumeration and help place it in the attack timeline. Process discovery is often
    one step in a chain that continues toward credential access or lateral movement.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:lolbin.technique_class_lateral
  question: Were any other discovery or proxy-execution LOLBins used on this host recently?
  context: Operators who use one living-off-the-land binary often use several. wmic is used here
    for discovery and is also a proxy-execution LOLBin, so check whether other enumeration and
    proxy-execution binaries such as rundll32, regsvr32, mshta, msbuild, or installutil were also
    run on the host. Finding multiple LOLBins working toward the same objective strengthens the
    case for operator activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\installutil.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\control.exe'
          - '\infdefaultinstall.exe'
          - '\msdt.exe'
          - '\mavinject.exe'
          - '\xwizard.exe'
          - '\ie4uinit.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was wmic executed from its expected System32 or SysWOW64 path?
  context: A LOLBin executed from a non-standard path may have been copied or renamed to evade
    path-based allowlists. wmic.exe running from a user's temp or AppData directory, rather than
    System32 or the wbem subdirectory under SysWOW64, is more suspicious than one in its normal
    location. Compare Image to OriginalFileName to confirm the on-disk name and path match the
    genuine binary.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this wmic process-query pattern over the past 90 days,
    and what command-line shapes are typical?
  context: A pattern with no prior history on a host is more suspicious than one used regularly
    under stable parents. The query mirrors the rule's selection scoped to this host, so the count
    reflects this specific process-enumeration pattern rather than generic wmic usage. The
    command-line projection lets the analyst compare typical invocation shapes against the
    alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
        CommandLine|contains: process
      filter_main_creation:
        CommandLine|contains|all:
        - call
        - create
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection* and not 1 of filter_*) and _pb_scope
    fields:
    - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1047
      name: Windows Management Instrumentation
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: process_creation
      description: WMIC process is created on the host to query the process list
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_execution
      description: The process-enumeration query is executed via the WMI command line
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: network_connection
      description: A corresponding outbound connection is opened when the WMI query targets a remote host
      mapped_to: Q4
      source: attack:T1047.network_connection
  dimension_coverage:
    - dimension: time_correlation
      technique: T1047
      covered_by: Q4
      source: attack:T1047.network_connection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1047
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1047
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: WMIQueryScope
      dimension: tuning_parameter
      specific_values: "\\root\\cimv2, \\root\\subscription"
      disposition: context
      target_question: Q3
      notes: "Namespace scope is not present in a process_creation command line for wmic process
        queries unless a WMI path is used; decode of the alias/path/name filter is directed in
        mechanism_decode context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Correlation window for process creation vs remote connection is realized as the
        +/-5m range on the network_connection question."
    - element: UserContext
      dimension: user_context
      specific_values: "interactive vs SYSTEM/low-privileged"
      disposition: context
      target_question: Q11
      notes: "Interactive vs service-principal execution is split across the user baseline and the
        parent+binary service-principal substitute pair."
    - element: RemoteDestinationThreshold
      dimension: tuning_parameter
      specific_values: "unique remote hosts contacted within a window"
      disposition: new_question
      target_question: Q4
      notes: "Aggregated network_connection query surfaces distinct DestinationIp/DestinationHostname
        from wmic so a fan-out of remote queries is visible; threshold itself stated in context."
    - element: SuspiciousCommandPatterns
      dimension: tuning_parameter
      specific_values: "wmic process call, powershell Invoke-WmiMethod"
      disposition: query_injection
      target_question: Q3
      notes: "mechanism_decode query carries CommandLine|contains: process from the rule selection;
        verify_trigger_match re-states the process keyword and the not-call-create filter. The
        wmic process call execution variant is filtered out of this rule by construction."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "near-identical to cue_host_context.clustering (what else ran on this host around the
        alert, aggregated by Image); kept host_context version"
      kept: cue_host_context.clustering
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline (Q11) and
        cue_user_actor.baseline_svc (Q12) both emitted (subject vs launch-mechanism pivot)"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.clustering (Q13) and
        cue_user_actor.clustering_svc (Q14) both emitted (subject vs launch-mechanism pivot)"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "Dropped (recommended-tier, not must_include). This rule detects passive process
        enumeration (wmic process, with the call create execution variant filtered OUT), not
        payload execution, credential access, or impact. Tamper-as-precursor is not a live question
        for a read-only discovery action — an operator does not typically disable AV before listing
        processes. The defense_tampering technique set is not confirmed for this rule. Prior-host
        context is instead carried by cue_host_context.precursor (Q15), which is the more relevant
        timeline question for a discovery rule. Recorded here per the no-silent-drop rule for
        universal questions."
    - template: persistence_scope.process_based_persistence
      reason: "Trimmed for the 14-18 target; this rule is execution/discovery via WMI, not a
        persistence-installation rule (is_persistence_rule: false), so the persistence-stacking
        question is recommended-not-required and lower value here."
    - template: persistence_scope.file_based_persistence
      reason: "Trimmed for the 14-18 target; same rationale — file-drop persistence is parallel to
        process enumeration and not the behavior this rule detects."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'network_connection' (recommended by techniques: T1047) is not covered —
        pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted_on
      reason: >-
        Acted on, not declined: authored Q4 (attack:T1047.network_connection) with
        category: network_connection to catch the outbound DCOM/RPC connection wmic opens when the
        query targets a remote host via /node. Listed here for the audit trail.
    - hint: "logsource 'wmi' (recommended by techniques: T1047) is not covered and no template
        family exists — author a custom question with `category: wmi`"
      disposition: acted_on
      reason: >-
        Acted on, not declined: authored Q5 (attack:T1047.wmi) with category: wmi to confirm the
        process query was carried out through the WMI provider and to catch remotely originated
        queries the local process event cannot show. Listed here for the audit trail.
    - hint: "LOLBin rule — check for renamed binary and other LOLBins achieving same objective"
      disposition: acted_on
      reason: >-
        Acted on, not declined: Q17 (lolbin.renamed_binary_check) verifies wmic ran from
        System32/SysWOW64, and Q16 (lolbin.technique_class_lateral) checks for other discovery and
        proxy-execution LOLBins on the host. Listed here for the audit trail.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      wmi: 1
      sigma_alert: 1
