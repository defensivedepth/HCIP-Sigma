name: Renamed NirCmd.EXE Execution
id: 49a8da74-936a-4361-a7bf-deb08155f81a
description: |
  Detects a process whose embedded PE metadata identifies it as NirCmd (OriginalFileName = NirCmd.exe)
  but whose on-disk filename does NOT end in nircmd.exe or nircmdc.exe — i.e., the utility has been renamed.
  NirCmd is a legitimate, signed command-line tool that performs many UI-less Windows actions (registry writes,
  run-as-SYSTEM elevation, process termination, screenshot capture, system shutdown, remote execution from a
  host list). It is renamed to camouflage those high-impact primitives in process audits, so the rename itself
  is the signal regardless of the subcommand. Investigation should confirm the metadata/filename mismatch,
  determine how the renamed binary arrived, decode what subcommands it ran, and establish whether the same
  renamed binary or launch pattern appears elsewhere in the environment.
type: detection
detection_id: 264982dc-dbad-4dce-b707-1e0d3e0f73d9
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does this process carry NirCmd PE metadata while running under a filename other than nircmd.exe or nircmdc.exe?
  context: Confirm the alert is a true positive before investing time. The rule fires when the embedded
    OriginalFileName equals NirCmd.exe AND the on-disk Image does not end in \nircmd.exe or \nircmdc.exe — a
    renamed copy. Review the OriginalFileName and the Image path together and verify the mismatch is real and
    not a benign tool that happens to share the metadata.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to run NirCmd-style administrative utilities?
  context: Legitimate IT scripting and admin automation does use NirCmd for volume control, display settings,
    or scripted housekeeping. Knowing whether the user role and host function routinely involve UI-less admin
    tooling is often the fastest path to separating routine operations from a renamed binary planted by an
    operator.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: A renamed NirCmd can stop services and terminate processes (killprocess, service stop), so an operator
    may weaken protection before or alongside hands-on actions. This query surfaces direct attempts to disable
    AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a
    useful "protection looks healthy" signal — kernel-level driver tampering and in-memory patches will not
    appear here. Populated rows shift the disposition toward operator preparation; review the parent process and
    signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:masquerading.true_identity
  question: What is the actual identity of this binary based on its metadata and hash?
  context: The file is named something other than nircmd.exe but its OriginalFileName says NirCmd.exe. Compare
    the Image path on disk against the embedded OriginalFileName to confirm the true tool, and use the file hash
    for a reputation lookup to determine whether this is the genuine signed NirSoft binary, a known-bad sample,
    or a novel file. The signing party and signature status indicate whether the metadata can be trusted.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:masquerading.binary_origin
  question: How did this renamed binary arrive on the host?
  context: 'Renamed binaries do not appear spontaneously — they were copied, downloaded, or dropped by a parent
    process. Tracing the file creation event by basename reveals the delivery mechanism: a download tool, a
    dropper, manual copy, or lateral movement. A landing path under Desktop, Downloads, Temp, or AppData points
    to download or extraction; a system directory points to compromise of a privileged process or an installer.
    The Image field in the file event shows which process wrote the file.'
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_name%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:masquerading.renamed_binary_activity
  question: What did this renamed binary do after execution, and is the same renamed binary present on other hosts?
  context: The point of renaming is to evade notice while performing the tool's function. Child processes, file
    operations, and network connections from the renamed binary reveal its actual objective. The same renamed
    binary appearing on multiple hosts points to deliberate distribution rather than an isolated event. Pivot on
    OriginalFileName or the file hash to find the renamed copy elsewhere in the environment.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What NirCmd subcommands and arguments did this binary run? Decode the full command line.
  context: NirCmd's risk is in the subcommand. Decode the arguments to identify high-impact primitives —
    elevatecmd or runassystem (run as SYSTEM), regsetval (registry writes), killprocess or service stop,
    exitwin poweroff/reboot, savescreenshot, multiremote (execute across a host list), or cmdshortcut. The query
    scopes to all executions of this renamed binary on this host within the window, since a single logical
    operation may span several sequential invocations.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this renamed binary, and is that parent chain expected?
  context: The parent process reveals how the binary was invoked. Legitimate admin scripting often has
    predictable parents (explorer.exe for user actions, a scheduler or scripting host for automation). Unusual
    parents such as wscript.exe, mshta.exe, a browser, or an Office application point to delivery via a script
    or document rather than deliberate administrative use.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1202.network_connection
  question: Did this renamed binary or its child processes open outbound network connections after execution?
  context: Indirect-execution utilities frequently chain into outbound transfers — fetching follow-on payloads
    or beaconing to a controller. Correlate network connections from the renamed binary and any process it
    spawned to determine whether execution led to egress. Compare destinations against known-good egress for
    this host; connections to unfamiliar external endpoints raise the priority of the investigation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
      filter_known_good:
        Image|endswith:
          - \svchost.exe
          - \MsMpEng.exe
      condition: selection and not filter_known_good
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run a binary carrying NirCmd metadata?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. A zero
    or very low historical count for NirCmd-metadata executions on this host indicates first-time or rare use,
    consistent with a planted copy rather than established admin tooling.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even legitimate tools can be abused. The query mirrors the rule's selection structure scoped to this
    host, so the parent/user distribution reflects how the rule's specific firing pattern — a renamed NirCmd —
    has historically run here. A new parent process, an unexpected user account, or an unusual command shape is
    the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: NirCmd.exe
      filter_main_img:
        Image|endswith:
        - \nircmd.exe
        - \nircmdc.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide
    count of renamed-NirCmd executions (not generic NirCmd usage). Many hosts firing the same pattern points to
    scripted deployment or vendor automation; one host or a small unrelated set points to manual operator
    activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: NirCmd.exe
      filter_main_img:
        Image|endswith:
        - \nircmd.exe
        - \nircmdc.exe
      condition: selection and not 1 of filter_main_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run NirCmd-style utilities?
  context: Compare this user's current action against their historical behavior. A user who has never run this
    tool is more suspicious than one who does so regularly, and the user's role should align with running
    UI-less admin utilities. A zero or low count across all hosts over the baseline window suggests this is not
    routine for them.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism: which parent spawned this binary, and is that pair historically
    common. A new or rare parent for this binary is the deviation worth chasing; a pair that already accounts
    for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple
    endpoints. If the same user drove similar activity on several hosts in this window, the scope of the event
    may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+binary combination fire on other hosts in the same window — is this a coordinated automation rollout or isolated activity?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry
    noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same
    launch mechanism — parent process plus spawned binary — fired on other hosts in the alert window. Multiple
    hosts points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining surrounding activity reveals the immediate context: what
    led to this event, what happened after, and whether it is part of a larger sequence. Look for
    reconnaissance, privilege escalation, or persistence around the alert window.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may
    reveal the attack stages that preceded this event and help reconstruct the timeline. Renamed tooling is
    often a mid-chain step that follows delivery and precedes hands-on actions.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1202
      name: Indirect Command Execution
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: indirection_utility_launch
      description: "A renamed indirection/admin utility (NirCmd) is launched; the rename itself is the trigger."
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: secondary_command_execution
      description: "The utility runs subcommands and/or spawns secondary programs (cmd, powershell, arbitrary EXE)."
      mapped_to: Q7
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: child_process_spawn
      description: "Utility spawns a secondary program — child-process evidence of indirect execution."
      mapped_to: Q6
      source: template:masquerading.renamed_binary_activity
    - stage: 4
      label: outbound_network
      description: "The utility or its child opens outbound network connections."
      mapped_to: Q9
      source: attack:T1202.network_connection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059
      covered_by: Q7
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059
      covered_by: Q8
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059
      covered_by: Q17
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059
      covered_by: Q9
      source: attack:T1202.network_connection
      coverage_type: indirect
    - dimension: user_context
      technique: T1059
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rationale only (encoded/uncommon script patterns — environment-specific)"
      disposition: context
      target_question: Q7
      notes: "Rule has no commandline_values; NirCmd subcommand surface (elevatecmd, runassystem, regsetval, killprocess, exitwin, multiremote, savescreenshot, cmdshortcut) enumerated in mechanism_decode context for manual decode."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (varies across managed/unmanaged hosts)"
      disposition: context
      target_question: Q8
      notes: "Parent-chain question surfaces ParentImage; unusual scripting-host/Office/browser parents called out in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (work-hours / admin maintenance windows)"
      disposition: context
      target_question: Q17
      notes: "Host-clustering +/-15m and precursor -6h windows provide temporal correlation; threshold not directly queryable."
    - element: AllowedUtilities
      dimension: tuning_parameter
      specific_values: "forfiles, wsl, ssh (jumphost allow-list)"
      disposition: context
      target_question: Q12
      notes: "Suppression allow-list is environment-specific tuning; fleet-scope question lets analyst judge whether cross-host spread is sanctioned automation."
    - element: HighRiskChildren
      dimension: tuning_parameter
      specific_values: "powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, curl.exe, bitsadmin.exe"
      disposition: context
      target_question: Q6
      notes: "High-risk child images named in renamed_binary_activity context; query returns all children for the analyst to spot these. KEEP_EXACT template not modified."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (raise severity for interactive standard user on workstation)"
      disposition: context
      target_question: Q2
      notes: "host_user_context question asks whether user/host expected to run NirCmd-style tooling."
    - element: DestCIDRs
      dimension: tuning_parameter
      specific_values: "rationale only (known-good egress for SSH/WSL)"
      disposition: context
      target_question: Q9
      notes: "Network question advises comparing destinations against known-good egress; CIDR allow-list is environment-specific."
  dedup_decisions:
    - dropped: cue_process_tool.follow_on
      reason: "masquerading.renamed_binary_activity uses the identical ParentImage|endswith|expand %Image% child-process query and is more specific (renamed-binary framing + scope thread)."
      kept: masquerading.renamed_binary_activity
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline (Q13) + baseline_svc (Q14) emitted as a pair; clustering (Q15) + clustering_svc (Q16) emitted as a pair. user-pivot and service-principal-substitute pivot on different identity dimensions."
      kept: cue_user_actor.baseline + baseline_svc + clustering + clustering_svc
  skipped_templates: []
  reflection_overrides: []
  reflection_notes:
    - templates:
        - template:cue_process_tool.baseline_deviation
        - template:cue_process_tool.scope
      note: >-
        Both reflected detection blocks shipped BYTE-IDENTICAL to the brief
        query_template (verified: detection and fields match exactly; no
        modification). The alignment linter flags these as
        'reflected_regression' because it only confirms alignment via a
        CommandLine-family constraint, and this rule's detection is
        OriginalFileName + Image|endswith with NO CommandLine values
        (rule.commandline_values is empty). Adding a CommandLine constraint to
        silence the warning would break the byte-identity guarantee and
        misrepresent what the rule fires on. The warnings are a heuristic
        false-positive for CommandLine-free rules; the reflected queries are
        correct by construction. No reflection_override recorded because nothing
        was overridden.
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - masquerading
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 16
    logsource_diversity:
      process_creation: 15
      file_event: 1
      network_connection: 1
      sigma_alert: 1
