name: HackTool - Dumpert Process Dumper Execution
id: 2c7f30bd-0326-4139-9511-7d2ccfaeac31
description: |
  Detects execution of the Dumpert LSASS memory dumper, identified either by the known Dumpert DLL hash or by a command line that references Dumpert.dll. Dumpert uses direct system calls and API unhooking to read lsass.exe process memory and write a minidump while evading user-mode API hooks, so the resulting dump is typically created on disk in a temporary location and then exfiltrated. This is an offensive credential-access tool; legitimate use is essentially limited to authorized penetration tests or red-team engagements, so the investigation should confirm whether an engagement is active, determine what the tool did to LSASS, identify which accounts may now be exposed, and check for follow-on lateral movement.
type: detection
detection_id: 2704ab9e-afe2-4854-a3b1-0c0706d03578
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does this event match the known Dumpert hash or a command line referencing Dumpert.dll?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires on either of two conditions
    the known Dumpert DLL hash (MD5 09D278F9DE118EF09163C6140255C690) or a command line that contains the string Dumpert.dll, such as a rundll32 invocation loading the Outflank-Dumpert DLL. Review the exact command line and hash against these criteria and note which condition matched.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Hashes
- source: template:universal.host_user_context
  question: Is this user and host expected to load Dumpert.dll or dump LSASS memory?
  context: Legitimate administrative activity rarely involves an LSASS memory dumper. Knowing whether the user role and host function would ever plausibly run a credential-dumping tool is often the fastest path to distinguishing an authorized test from an intrusion. A general workstation or server with no security-testing role running this tool is a strong escalation signal.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this Dumpert execution part of an authorized penetration test or red team engagement?
  context: Offensive LSASS dumpers are used in authorized security assessments. Before investing further investigation time, check whether a pentest or red team exercise is currently active on this host or network segment. An authorized engagement with matching tool usage and timing resolves the alert without further analysis; absence of any engagement makes this a hands-on-keyboard credential-access event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.credential_target_identification
  question: Was LSASS memory successfully accessed or dumped, and by what method?
  context: Dumpert targets LSASS process memory specifically, using direct system calls and API unhooking rather than the more common comsvcs.dll MiniDump or procdump paths. Identify whether the dump completed and what method was used the technique reveals attacker sophistication, and a successful LSASS dump means any credentials cached in that process (logon passwords, NTLM hashes, Kerberos tickets) may now be in attacker hands.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: What loader and arguments invoked Dumpert, and which dumping capability was triggered?
  context: This tool ships as both an executable and a DLL; the DLL form is typically loaded through rundll32 with an exported Dump function, and an sRDI/shellcode form can be injected into an existing process. The command line, module name, and exported function reveal which capability ran and whether the dump was written to disk or kept in memory, which determines the scope of potential credential compromise.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.001.access_verification
  question: Which processes opened a full-access handle to lsass.exe on this host around the alert?
  context: The credential-dumping chain begins with a process opening a handle to lsass.exe with full access rights (AccessMask 0x1F0FFF) before reading or dumping its memory. A handle request with that mask from a process that is not a known security or system tool corroborates the dump attempt and identifies the requesting process even when the command line is obfuscated. The +/-5 minute window correlates the handle open with the dump activity that follows.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        TargetImage|endswith: '\lsass.exe'
        GrantedAccess: '0x1F0FFF'
      filter_host:
        host.name|expand: '%hostname%'
      condition: selection and filter_host
    fields:
      - SourceImage
      - GrantedAccess
      - CallTrace
- source: template:cue_process_tool.process_chain
  question: What process spawned the Dumpert loader, and is that parent chain expected?
  context: The parent process reveals how the tool was launched and whether a known good tool is being allowlisted incorrectly. A handle to LSASS from a security or backup agent has a predictable parent; a dumper launched from a web shell, a remote-execution service, an Office application, or a script host (mshta, wscript, cmd, powershell) points to operator activity. Note the parent's own parentage to reconstruct how execution reached this host.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:hacking_tool.tool_specific_artifacts
  question: Was an LSASS minidump file written to a temporary directory on this host?
  context: Dumpert writes the LSASS minidump to disk before exfiltration, commonly into temporary paths such as the user or system TEMP folder or C:\Windows\Temp. A new .dmp or minidump-sized file in those locations around the alert time confirms the dump completed even if the dumping process has exited, and the file path and timestamp anchor the credential-exposure window. Absence of a dump file may mean an in-memory-only variant or that file telemetry is incomplete.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_path:
        TargetFilename|contains:
          - '\Temp\'
          - '\AppData\Local\Temp\'
          - '\Windows\Temp\'
      condition: selection_host and selection_path
    fields:
      - TargetFilename
      - Image
- source: attack:T1003.001.registry_credential_access
  question: Were registry values written or modified on this host that are indicative of credential scraping around the alert?
  context: The staged credential-theft chain sometimes pairs LSASS access with registry modification such as flipping the RunAsPPL/LSASS protection value, enabling WDigest credential caching (UseLogonCredential), or staging configuration before dumping. Registry writes to the LSA, WDigest, or SAM-related keys close in time to the LSASS access strengthen the case that this is deliberate credential scraping rather than an isolated tool launch.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_key:
        TargetObject|contains:
          - '\Control\Lsa\'
          - '\SecurityProviders\WDigest\'
          - '\Control\SecurityProviders\'
      condition: selection_host and selection_key
    fields:
      - TargetObject
      - Details
      - Image
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes credential access. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful protection-looks-healthy signal kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_user_actor.baseline
  question: Does this user normally run LSASS-dumping or other offensive tooling?
  context: Compare this user's current action against their historical behavior. A user who has never run a credential-dumping tool is more suspicious than a security tester who does so regularly. The query counts how often this user has driven the same firing pattern across all hosts; zero or very low counts indicate first-time or out-of-role activity. Combine with knowledge of the user's role.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: 'Dumpert.dll'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and Dumpert-loading combination historically routine on this host or across the environment, or is the parent unusual?
  context: When the alert fires under a service principal (SYSTEM, LOCAL SERVICE, NETWORK SERVICE, or a computer account), user identity does not attribute behavior; the meaningful pivot is the launch mechanism. A new or rare parent process for this Dumpert load on this host or environment-wide is the deviation worth chasing, whereas a parent that already accounts for many prior events is more likely automation. This is a distinct question from whether the user is routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith|expand: '%ParentImage%'
        CommandLine|contains: 'Dumpert.dll'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered comparable activity on several hosts in the alert window, the scope of compromise may be larger than the single triggering host. Credential dumping is frequently followed by the operator pivoting with the harvested credentials.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and Dumpert-loading combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise because every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism (parent process plus the Dumpert load) fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger or hands-on operator.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith|expand: '%ParentImage%'
        CommandLine|contains: 'Dumpert.dll'
      condition: selection
    fields:
      - host.name
- source: template:credential_access.post_access_lateral_movement
  question: Were any new logon sessions or remote connections initiated after this event?
  context: Credential theft is typically followed by lateral movement the operator uses harvested credentials to access other systems. Look specifically for follow-on alerts indicating remote sessions, SMB or admin-share use, remote command execution, or new authentication shortly after the dump. Their presence indicates the stolen credentials are already being exercised and raises the urgency of containment.
  range: +4h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be compromised, and do any have elevated privileges?
  context: The severity of an LSASS dump depends on which credentials were resident in memory on this host at the time. Any account with an active or cached logon session privileged users, domain or enterprise admins, and service accounts with broad rights may now be exposed. Domain admin or service-account exposure requires incident escalation and credential rotation; a standalone standard-user session may only need monitoring.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of the rule's firing pattern (the Dumpert hash or Dumpert.dll command line), not generic binary usage. Many hosts firing the same pattern points to scripted deployment or a sanctioned tool; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Hashes|contains: MD5=09D278F9DE118EF09163C6140255C690
      - CommandLine|contains: Dumpert.dll
      condition: selection
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even when a tool has run before on a host, the firing context matters. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how the rule's specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual command-line shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Hashes|contains: MD5=09D278F9DE118EF09163C6140255C690
      - CommandLine|contains: Dumpert.dll
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.001
      name: LSASS Memory
      role: primary
    - id: T1003
      name: OS Credential Dumping
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: lsass_handle_open
      description: "A process opens a handle to lsass.exe with full access (0x1F0FFF) prior to reading/dumping its memory"
      mapped_to: Q6
      source: attack:T1003.001.access_verification
    - stage: 2
      label: memory_dump_file_creation
      description: "The tool dumps LSASS memory and writes a minidump file to disk (TEMP / Windows\\Temp) before exfiltration"
      mapped_to: Q8
      source: template:hacking_tool.tool_specific_artifacts
    - stage: 3
      label: registry_modification
      description: "Registry modification indicative of credential scraping (LSA/WDigest/security-provider keys) correlated with the LSASS access"
      mapped_to: Q9
      source: attack:T1003.001.registry_credential_access
  dimension_coverage:
    - dimension: access_verification
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.access_verification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1003.001
      covered_by: Q5
      source: template:hacking_tool.tool_capability_used
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1003.001
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.access_verification
      coverage_type: partial
    - dimension: tuning_parameter
      technique: T1003.001
      covered_by: Q8
      source: template:hacking_tool.tool_specific_artifacts
      coverage_type: direct
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF"
      disposition: query_injection
      target_question: Q6
      notes: "Injected into the authored process_access question as GrantedAccess: '0x1F0FFF' with TargetImage ending in \\lsass.exe."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "5 minutes (access -> dump/registry)"
      disposition: context
      target_question: Q6
      notes: "5-minute correlation window between LSASS handle-open and the dump/registry activity recorded in Q6 context; Q6 range is +/-5m and Q9 (registry) range is +/-5m to enforce the same window."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (allowlist known AV/EDR tools)"
      disposition: context
      target_question: Q7
      notes: "Parent-process legitimacy assessment is the body of Q7; allowlisting of legitimate security tools described in context rather than as a query filter (allowlist is environment-specific)."
    - element: DumpFilePath
      dimension: tuning_parameter
      specific_values: "%TEMP%, C:\\Windows\\Temp"
      disposition: query_injection
      target_question: Q8
      notes: "Injected into Q8 file_event query as TargetFilename|contains entries: \\Temp\\, \\AppData\\Local\\Temp\\, \\Windows\\Temp\\."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rundll32, procdump, comsvcs.dll, Invoke-Mimikatz (Dumpert loads via rundll32 + Dumpert.dll)"
      disposition: context
      target_question: Q5
      notes: "Dumpert-specific loader pattern (rundll32 + Dumpert.dll DLL form, sRDI/shellcode form) described in Q4/Q5 context; the rule already keys on CommandLine|contains Dumpert.dll, so no additional CommandLine filter needed on the entity-scoped Q5."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification (brief hint) — Phase 2 satisfied by Q4"
      kept: credential_access.credential_target_identification
    - dropped: cue_process_tool.baseline
      reason: "Image-based host baseline superseded by REFLECTED scope (Q17) and baseline_deviation (Q18), which embed the rule's CommandLine/hash pattern verbatim and are filter-correct for this CommandLine-only rule; an Image|endswith baseline has no %Image% from this detection (primary_binary null)."
      kept: cue_process_tool.scope, cue_process_tool.baseline_deviation
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification"
    - template: cue_process_tool.follow_on
      reason: "Recommended tier-2 cue dropped to stay within the 18-question cap. Child-process follow-on for an in-memory LSASS dumper that exits quickly adds little over the lateral-movement (Q15) and host-clustering coverage; access_verification (Q6) and the dump-file artifact (Q8) are the load-bearing post-execution evidence here."
    - template: cue_process_tool.clustering
      reason: "Recommended tier-2 cue dropped at the 18-question cap; surrounding-tool context is partially covered by the user clustering questions (Q13/Q14) and the precursor/lateral-movement alert correlation."
    - template: cue_host_context.clustering
      reason: "Recommended tier-2 cue dropped at the 18-question cap; broad host-activity context overlaps cue_user_actor.clustering (Q13) and the alert-correlation questions (Q15)."
    - template: cue_host_context.precursor
      reason: "Recommended tier-3 cue dropped at the 18-question cap; prior-alert context on this host is partly captured by post_access_lateral_movement (Q15, same alert-correlation logsource over a forward window) and endpoint_protection_health (Q10, tamper-as-precursor in the -1h window). Note: NOT used as the basis for dropping endpoint_protection_health — that universal question is included."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1003.001) is not covered and no template family exists — author a custom question with category: security"
      disposition: declined
      reason: >-
        The Security-log signal for this technique is EventCode 4673 (sensitive
        privilege use / handle to LSASS), which detects the same staged event as
        the authored process_access question (Q6, GrantedAccess 0x1F0FFF to
        \lsass.exe via Sysmon EventCode 10). 4673 SeDebugPrivilege auditing is
        rarely enabled and high-volume when it is, so it would add noise without
        new coverage. The access_verification dimension and the
        handle-open chain stage are already covered directly by Q6. No
        compensating attack:T1003.001.security question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - hacking_tool
    - cue_process_tool
    - cue_user_actor
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 16
    logsource_diversity:
      process_creation: 13
      process_access: 1
      file_event: 1
      registry_set: 1
      sigma_alert: 2
