name: Suspicious Rundll32 Setupapi.dll Activity
id: dc378404-c9cc-404b-980f-d09b53ca004f
description: |
  Detects runonce.exe spawned by rundll32.exe when the rundll32 command line
  invokes setupapi.dll with the InstallHinfSection function. InstallHinfSection
  processes an INF file and always launches runonce.exe regardless of the INF
  content, which is what makes this parent-child pair the signal. INF directives
  can register or unregister scriptlet DLLs, write Run and RunOnce registry
  values, drop files, install drivers, or fetch and execute local or remote
  script and payload code through a Microsoft-signed binary chain. Legitimate
  driver installation tooling also uses setupapi.dll with INF files, so the
  investigation should determine which INF file was processed, what directives
  it carried, and whether the parent rundll32 invocation and the INF source are
  consistent with expected administrative activity.
type: detection
detection_id: 285b85b1-a555-4095-8652-a8a4106af63f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alert show runonce.exe spawned by rundll32.exe with setupapi.dll and InstallHinfSection in the parent command line?
  context: Confirm the alert is a true positive by reviewing the parent rundll32 command line and the spawned runonce.exe against the rule's criteria. The parent command line should contain both setupapi.dll and InstallHinfSection, and it normally also names the INF file path and a section directive. Note the INF path, section name, and any URL or scriptlet reference in the parent command line for downstream analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
      - Image
- source: template:universal.host_user_context
  question: Is this user and host expected to process INF files through rundll32 setupapi.dll?
  context: Driver installation, device provisioning, and some software installers legitimately invoke InstallHinfSection. Knowing whether this host is a workstation, server, or build machine and whether the user performs driver or device administration is often the fastest way to separate routine installation activity from abuse of the INF execution path.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Is rundll32.exe expected to spawn runonce.exe here, and what started the rundll32 process?
  context: The rundll32-to-runonce pairing through InstallHinfSection is the core signal for this detection. Tracing the rundll32 process back to its own parent shows whether the INF processing was launched by an interactive user action, an installer, a scripting host, or an Office or browser process. A grandparent like a script interpreter or document handler points toward INF abuse rather than legitimate driver installation.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: attack:T1218.011.command_analysis
  question: What INF file path, section directive, and any script or URL reference appear in the rundll32 setupapi.dll command line?
  context: The parent rundll32 command line carries the operational detail of this technique. Decode the INF file path and the named section, then determine whether the path points to a user-writable or temporary directory and whether the command line or the referenced INF includes a remote URL, a scriptlet DLL such as scrobj.dll, or an UnregisterDlls or RegisterDlls directive. INF processing can run local or remote scriptlet code and write RunOnce registry values for persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
          - setupapi.dll
          - InstallHinfSection
      condition: selection
    fields:
      - ParentCommandLine
      - CommandLine
      - CurrentDirectory
- source: template:parent_child.sibling_processes
  question: What other child processes did the same rundll32 instance spawn around this time?
  context: If the INF processing was malicious, runonce.exe may not be the only thing the rundll32 instance launched, and the INF itself may chain into additional processes. Examining siblings of the runonce.exe child reveals scriptlet hosts, command interpreters, or payload binaries started through the same INF directive set.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.011.image_load
  question: What DLLs did rundll32.exe and runonce.exe load on this host around the time of the alert?
  context: InstallHinfSection maps DLLs into memory and INF RegisterDlls or UnregisterDlls directives invoke scriptlet handlers such as scrobj.dll. Reviewing the image loads of the rundll32 and runonce processes shows whether a scriptlet COM DLL, an unsigned module, or a DLL from a user-writable path was pulled in, which distinguishes script and payload execution from a benign driver installation. If image-load telemetry is not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \rundll32.exe
          - \runonce.exe
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.011.file_event
  question: What files did the rundll32 and runonce processes write or drop on this host around the time of the alert?
  context: INF directives can copy files, drop scriptlet or payload content, and stage executables before they run. Files written by the rundll32 or runonce processes, especially scriptlet, executable, or INF files landing in temporary or user-writable directories, indicate the INF carried more than driver metadata. If file-event telemetry is not collected here, treat zero results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \rundll32.exe
          - \runonce.exe
      condition: selection
    fields:
      - Image
      - TargetFilename
- source: attack:T1218.011.network_connection
  question: Did rundll32.exe or runonce.exe make any external network connections around the time of the alert?
  context: INF directives can reference remote resources, fetching a scriptlet, HTA, or DLL payload from an external host before executing it through the signed binary chain. Outbound connections from rundll32 or runonce to an external address, particularly one retrieving script or payload content, move the disposition toward remote payload execution rather than local driver installation. If network telemetry is not collected here, treat zero results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \rundll32.exe
          - \runonce.exe
        Initiated: 'true'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1218.011.time_correlation
  question: Were registry Run or RunOnce key modifications recorded on this host in the minutes around the alert?
  context: A documented use of the InstallHinfSection path is writing Run or RunOnce registry values to establish persistence. Correlating the INF processing with registry persistence writes in the same short window ties the execution to a persistence outcome. If registry telemetry is not collected here, treat zero results as a visibility gap rather than evidence no key was written.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - \CurrentVersion\Run
          - \CurrentVersion\RunOnce
      condition: selection
    fields:
      - Image
      - TargetObject
      - Details
- source: attack:T1218.011.parent_process_analysis
  question: Does rundll32.exe normally spawn runonce.exe on this host, or is this parent-child pairing new?
  context: A frequency count of the rundll32-to-runonce pairing on this host separates routine driver and device installation activity from a first-time or rare occurrence. Zero or very low historical counts indicate a novel spawning relationship worth deeper inspection, while a steady history points to recurring automation or maintenance.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ParentCommandLine
- source: attack:T1218.tuning_parameter
  question: How often has rundll32 setupapi.dll InstallHinfSection run across the environment, and which hosts and INF paths account for it?
  context: A fleet-wide view of this specific InstallHinfSection invocation distinguishes broad consistent automation, such as a deployment tool installing drivers across many hosts, from a one-off or host-isolated occurrence. Concentration on a single host or an unusual INF path stands out against an established install baseline.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \rundll32.exe
        Image|endswith: \runonce.exe
        ParentCommandLine|contains|all:
          - setupapi.dll
          - InstallHinfSection
      condition: selection
    fields:
      - host.name
      - ParentCommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \runonce.exe
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - setupapi.dll
        - InstallHinfSection
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \runonce.exe
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - setupapi.dll
        - InstallHinfSection
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the activity may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \runonce.exe
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - setupapi.dll
        - InstallHinfSection
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host, such as defense tampering, reconnaissance, or credential access, may reveal the attack stages that preceded this event and help reconstruct the timeline around the INF execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.011
      name: Rundll32
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: rundll32_inf_invocation
      description: rundll32.exe invoked with setupapi.dll,InstallHinfSection processing an INF file
      mapped_to: Q4
      source: attack:T1218.011.command_analysis
    - stage: 2
      label: dll_mapping_scriptlet
      description: DLLs mapped into memory and scriptlet handlers (scrobj.dll) invoked via INF RegisterDlls/UnregisterDlls
      mapped_to: Q6
      source: attack:T1218.011.image_load
    - stage: 3
      label: payload_file_drop
      description: Suspicious DLLs/scripts/payloads dropped or accessed from disk via INF directives
      mapped_to: Q7
      source: attack:T1218.011.file_event
    - stage: 4
      label: external_payload_retrieval
      description: rundll32/runonce reach out to external domains to fetch .sct/.hta/.dll
      mapped_to: Q8
      source: attack:T1218.011.network_connection
    - stage: 5
      label: persistence_runonce
      description: INF writes Run/RunOnce registry values for persistence; runonce.exe launched
      mapped_to: Q9
      source: attack:T1218.011.time_correlation
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1218.011
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1218.011
      covered_by: Q9
      source: attack:T1218.011.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.011
      covered_by: Q11
      source: attack:T1218.tuning_parameter
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q4
      source: attack:T1218.011.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q10
      source: attack:T1218.011.parent_process_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q11
      source: attack:T1218.tuning_parameter
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "correlate rundll32 invocation with DLL load / network / registry activity within a short window"
      disposition: query_injection
      target_question: Q6, Q7, Q8, Q9
      notes: "+/-15m correlation windows on image_load, file_event, network_connection, and registry_set queries scoped to the rundll32/runonce processes"
    - element: ParentProcessFilter
      dimension: parent_process_analysis
      specific_values: "suspicious parents (explorer.exe, office apps) vs trusted installers"
      disposition: new_question
      target_question: Q3, Q10
      notes: "Q3 traces rundll32's own parent; Q10 baselines the rundll32->runonce pairing frequency on the host"
    - element: AllowedDLLs
      dimension: tuning_parameter
      specific_values: "rationale only — baseline of legitimate DLLs rundll32 runs"
      disposition: context
      target_question: Q6
      notes: "No environment-specific allowlist available at generation time; Q6 surfaces loaded DLLs with signing for the analyst to compare against expected modules"
    - element: ExternalIPRange
      dimension: tuning_parameter
      specific_values: "rationale only — scope of anomalous external ranges"
      disposition: context
      target_question: Q8
      notes: "No environment allowlist at generation time; Q8 surfaces destination IP/port/hostname for the analyst to evaluate"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only — unexpected parent-child profiling"
      disposition: new_question
      target_question: Q3
      notes: "Q3 examines what spawned rundll32 to profile the parent chain"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only — known signed proxy binaries (msiexec, regsvr32)"
      disposition: context
      target_question: Q4
      notes: "Rule already constrains to rundll32->runonce signed chain; Q4 examines the INF/section content invoked through it"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, script paths, DLL entrypoints in the command line"
      disposition: new_question
      target_question: Q4
      notes: "Q4 decodes INF path, section directive, scriptlet/URL references in ParentCommandLine; query retains rule's setupapi.dll+InstallHinfSection constraint"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only — suppress legitimate enterprise domains"
      disposition: context
      target_question: Q8
      notes: "No environment allowlist at generation time; Q8 surfaces destination hostname for the analyst to evaluate against known-good domains"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue and parent_child technique set active — parent_process_legitimacy (Q3) subsumes the process-chain question"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_process_tool.mechanism_decode
      reason: "host+binary CommandLine decode on runonce.exe carries no operational detail (runonce takes no meaningful arguments); the technique's operational detail lives in the rundll32 ParentCommandLine, covered by attack:T1218.011.command_analysis (Q4)"
      kept: attack:T1218.011.command_analysis
    - dropped: cue_parent_process.mechanism_decode
      reason: "overlaps parent_child.parent_process_legitimacy (Q3) which also retrieves the parent's own process_creation event and asks what started the parent"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair
      reason: "user_actor pair — non-deduplicable; emitted both (Q12 user-pivot, Q13 launch-mechanism substitute) per service-principal handling"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair
      reason: "user_actor pair — non-deduplicable; emitted both (Q14 user-pivot, Q15 launch-mechanism substitute) per service-principal handling"
      kept: both
  skipped_templates:
    - template: cue_parent_process.baseline
      reason: "host-level rundll32->runonce parent-child frequency covered by attack:T1218.011.parent_process_analysis (Q10) which mirrors this query shape"
    - template: cue_parent_process.follow_on
      reason: "follow-on children of the parent covered by parent_child.sibling_processes (Q5) scoped to the exact ParentProcessGuid for precision"
    - template: cue_parent_process.clustering
      reason: "around-the-time parent activity covered by the +/-15m image_load/file_event/network_connection correlation questions (Q6-Q8) plus cue_host_context activity context"
    - template: cue_parent_process.scope
      reason: "cross-host occurrence of this parent-child pair covered more precisely by attack:T1218.tuning_parameter (Q11) which also constrains the setupapi.dll+InstallHinfSection command line"
    - template: cue_process_tool.baseline
      reason: "host frequency of runonce.exe is dominated by benign OS RunOnce processing; the discriminating baseline is the rundll32->runonce pairing, covered by Q10"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering set not confirmed for this rule and cue_host_context.precursor (Q16) already surfaces prior tamper/recon alerts on the host; this rule is a proxy-execution detection, not a payload-execution or credential-access rule where tamper-as-precursor is the disposition-turning question"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - parent_child
  generation_stats:
    total_questions: 16
    source_counts:
      template: 9
      attack: 7
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 11
      image_load: 1
      file_event: 1
      network_connection: 1
      registry_set: 1
      sigma_alert: 1
