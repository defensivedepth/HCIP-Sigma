name: Indirect Command Execution From Script File Via Bash.EXE
id: 4fd89197-2f8a-4806-8034-dabd759a99eb
description: |
  Detects the Windows Subsystem for Linux launcher bash.exe running without any command-line flags, which causes it to
  execute the contents of a bash script file directly. Because the actual command logic lives inside the script rather than
  on the command line, this form of invocation can run Linux or Windows binaries while bypassing controls that inspect
  process command lines, making it a defense-evasion path. The rule fires when bash.exe (by Image path or OriginalFileName)
  runs AND none of the filters for a normal flagged invocation, bare interactive launch, or empty command line match.
  Investigation should determine what the script did, what child processes or network connections followed, and whether
  this user and host legitimately use WSL.
type: detection
detection_id: 2d22a514-e024-4428-9dba-41505bd63a5b
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did bash.exe run without command-line flags, so it executed a script file directly?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when bash.exe (matched
    by Image path under System32 or SysWOW64, or by OriginalFileName) runs AND the command line is not a flagged invocation
    (bash.exe - / bash -), not a bare interactive launch (bash.exe / bash), and not empty. Review the exact command line and
    process details to confirm the flagless, script-driven form actually matched.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to run the Windows Subsystem for Linux and execute bash scripts?
  context: Legitimate administrative or developer activity can trigger this detection. WSL and bash.exe are common on
    developer workstations and some admin jumphosts but unusual on general-purpose endpoints or servers. Knowing whether
    this user role and host function include Linux-subsystem usage is often the fastest path to separating routine activity
    from abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did the bash invocation actually run? Identify the script file and the command line on this host.
  context: The full command line and working directory reveal which script file bash.exe executed and from where. Because
    the flagless form runs the script's contents rather than an inline command, identify the referenced script path, decode
    any wrapped or chained commands, and note any file paths or network endpoints the script targets.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned bash.exe, and is that parent chain expected?
  context: The parent process reveals how bash.exe was invoked. Interactive launches typically descend from explorer.exe or
    a terminal host; script-runner, Office applications, mshta.exe, wscript.exe, or remote-management parents point to
    automated or operator-driven execution that warrants closer review.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1202.child_execution
  question: What child processes did bash.exe spawn after it ran?
  context: Indirect execution matters because of what it launches next. The script run by bash.exe may spawn secondary
    programs such as powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, curl.exe, or bitsadmin.exe,
    or arbitrary attacker binaries. Children that perform download, scripting, or further execution shift the disposition
    toward abuse.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1202.network_connection
  question: Did bash.exe or its child processes open outbound network connections around the time of the alert?
  context: The Linux subsystem can carry outbound traffic — reverse shells, data exfiltration over /dev/tcp, or download of
    a second-stage payload — initiated by the script rather than visible on the command line. Connections from bash.exe or
    its children to external or unexpected destinations indicate the script reached out beyond local execution.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - Initiated
- source: attack:T1202.ssh_config_precursor
  question: Was the user's SSH client configuration file written or modified on this host before the alert?
  context: Some indirect-execution paths persist by adding LocalCommand or ProxyCommand entries to the SSH client config so
    that a command runs whenever a connection is made. A recent write to a per-user .ssh config file is a precursor worth
    correlating with this execution; an unmodified config makes the SSH-persistence path less likely.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.baseline
  question: Does this host normally run bash.exe?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. Zero or very low
    historical counts indicate first-time or rare WSL usage on this endpoint; a steady count suggests an established
    developer or admin workflow.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users normally drive it, and does the
    current alert fit?
  context: Even routine tools can be abused. The query mirrors the rule's selection structure scoped to this host, so the
    resulting parent and user distribution reflects how the rule's specific flagless-bash firing pattern has historically run
    here. A new parent process, an unexpected user account, or an unusual command-line structure is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith:
        - :\Windows\System32\bash.exe
        - :\Windows\SysWOW64\bash.exe
      - OriginalFileName: Bash.exe
      filter_main_cli_flag:
        CommandLine|contains:
        - bash.exe -
        - bash -
      filter_main_no_cli:
        CommandLine: null
      filter_main_empty:
        CommandLine: ''
      filter_main_no_flag:
        CommandLine:
        - bash.exe
        - bash
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith:
        - :\Windows\System32\bash.exe
        - :\Windows\SysWOW64\bash.exe
      - OriginalFileName: Bash.exe
      filter_main_cli_flag:
        CommandLine|contains:
        - bash.exe -
        - bash -
      filter_main_no_cli:
        CommandLine: null
      filter_main_empty:
        CommandLine: ''
      filter_main_no_flag:
        CommandLine:
        - bash.exe
        - bash
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith:
        - :\Windows\System32\bash.exe
        - :\Windows\SysWOW64\bash.exe
      - OriginalFileName: Bash.exe
      filter_main_cli_flag:
        CommandLine|contains:
        - bash.exe -
        - bash -
      filter_main_no_cli:
        CommandLine: null
      filter_main_empty:
        CommandLine: ''
      filter_main_no_flag:
        CommandLine:
        - bash.exe
        - bash
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user triggered similar activity on several hosts, the scope of the activity may be larger than a single
    endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith:
        - :\Windows\System32\bash.exe
        - :\Windows\SysWOW64\bash.exe
      - OriginalFileName: Bash.exe
      filter_main_cli_flag:
        CommandLine|contains:
        - bash.exe -
        - bash -
      filter_main_no_cli:
        CommandLine: null
      filter_main_empty:
        CommandLine: ''
      filter_main_no_flag:
        CommandLine:
        - bash.exe
        - bash
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context — what led to
    this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, persistence, or data staging near the bash.exe execution.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the
    stages that preceded this execution and help reconstruct the timeline. The alert sequence often tells the story of how
    the activity unfolded.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1202
      name: Indirect Command Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: indirection_launcher
      description: "User or service launches the indirection utility (bash.exe) so its command logic lives in a script, not the command line"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 1b
      label: script_decode
      description: "Identify the script file and command logic the flagless bash invocation executed"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: secondary_execution
      description: "The launcher spawns a secondary program/command (powershell, cmd, curl, arbitrary EXE)"
      mapped_to: Q5
      source: attack:T1202.child_execution
    - stage: 2b
      label: outbound_network
      description: "The launcher or its child opens outbound network connections (reverse shell, exfil, payload download)"
      mapped_to: Q6
      source: attack:T1202.network_connection
    - stage: 3
      label: ssh_config_precursor
      description: "Optional precursor modification of SSH config to persist LocalCommand/ProxyCommand"
      mapped_to: Q7
      source: attack:T1202.ssh_config_precursor
  dimension_coverage:
    - dimension: time_correlation
      technique: T1202
      covered_by: Q6
      source: attack:T1202.network_connection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1202
      covered_by: Q5
      source: attack:T1202.child_execution
      coverage_type: direct
    - dimension: user_context
      technique: T1202
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "10-30 minutes correlation window between launcher and child/network activity"
      disposition: context
      target_question: Q6
      notes: "Child-execution (+4h) and network (+/-15m) questions correlate launcher with downstream activity; window encoded in range fields rather than a queryable field."
    - element: HighRiskChildren
      dimension: tuning_parameter
      specific_values: "powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, curl.exe, bitsadmin.exe"
      disposition: context
      target_question: Q5
      notes: "Enumerated in Q5 context as the high-risk child images that shift disposition toward abuse. Query returns all children of bash.exe (parent-scoped) so analyst sees these and any other spawned binary; not narrowed to the list to avoid missing arbitrary EXEs."
    - element: AllowedUtilities
      dimension: tuning_parameter
      specific_values: "forfiles, wsl, ssh (admin/jumphost allowlist)"
      disposition: context
      target_question: Q2
      notes: "Allowlist concept addressed via host/user-expectation framing in Q2 (is WSL expected here); rule is already bash-specific so a utility allowlist is not a queryable element."
    - element: UserContext
      dimension: user_context
      specific_values: "standard/interactive user on a workstation vs server/CI agent"
      disposition: context
      target_question: Q2
      notes: "Q2 host/user-context question asks whether this user/host is expected to run WSL; severity-raising standard-user-on-workstation framing carried in context."
    - element: DestCIDRs
      dimension: tuning_parameter
      specific_values: "known-good egress networks for SSH/WSL to suppress expected admin automation"
      disposition: context
      target_question: Q6
      notes: "Q6 network question surfaces DestinationIp/Port so the analyst can compare against known-good egress; specific CIDRs are environment-local and not injectable at generation time."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject-pivot vs launch-mechanism pivot, degenerate under different conditions)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject-pivot vs launch-mechanism pivot, degenerate under service principals)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "Superseded by attack:T1202.child_execution (Q5), which carries the same parent-scoped child query plus the T1202 chain-stage framing and the HighRiskChildren mutable-element enumeration."
    - template: cue_process_tool.clustering
      reason: "Host-scoped surrounding-activity coverage provided by cue_host_context.clustering (Q15); the cue_process_tool.clustering host-wide Image aggregation is the same query shape and would duplicate it."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 13
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      network_connection: 1
      file_event: 1
      sigma_alert: 1
