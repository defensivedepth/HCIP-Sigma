name: Potential Adplus.EXE Abuse
id: e259047a-d160-4da2-b95a-42fdee44ccce
description: |
  Detects execution of adplus.exe, a Windows SDK debugging tool, with command-line flags that drive process attachment
  and memory dumping or arbitrary command execution under a trusted Microsoft-signed binary. The rule fires when the image
  is adplus.exe (by name or original filename) AND the command line carries one of the process/dump/config flags. Because
  adplus can dump lsass.exe memory and run commands via a config file, the central question is whether this is a developer
  debugging a process or an operator staging credential theft under cover of a signed binary.
type: detection
detection_id: 2f869d59-7f6a-4931-992c-cce556ff2d53
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects, naming adplus.exe with one of its process or config flags?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when the image is
    adplus.exe (or its original filename) AND the command line contains one of the process-attach or config flags such as
    -hang, -pn, -pmn, -p, -po, -c, or -sc. Read the exact command line and confirm which flag fired and what process or
    config file it points at.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run a debugging tool against process memory?
  context: Legitimate adplus usage comes from developers, support engineers, or crash-analysis tooling. Knowing whether the
    user role and host function involve debugging is often the fastest way to separate routine diagnostics from an operator
    using a signed debugger to scrape credentials.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: What process was adplus targeting, and was lsass.exe the dump target?
  context: Adplus attaches to a process by name (-pn) or PID (-p/-pmn) and writes a memory dump, or runs a config file (-c)
    that can dump memory or execute commands. The dump target determines exposure. An adplus invocation naming lsass.exe is
    a direct credential-store hit; a config-file invocation hides the target inside the referenced .xml, so the config path
    itself must be retrieved and read. Decode the flag and its argument to identify exactly what was attached to or executed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.001.access_verification
  question: Did a handle with full access to lsass.exe get opened on this host around the time of the alert?
  context: Memory dumping requires opening a handle to the target process. A handle requesting full access (0x1F0FFF, or
    the read/query-limited subsets used to bypass tuning) to lsass.exe from a non-security process is the access primitive
    behind credential scraping. Allowlisted AV/EDR tools open lsass routinely; an adplus-owned or operator-owned process
    holding that handle near the alert is the deviation worth chasing.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetImage|endswith: \lsass.exe
      condition: selection
    fields:
      - SourceImage
      - GrantedAccess
      - CallTrace
      - User
- source: attack:T1003.001.tuning_parameter
  question: Was a memory dump file written on this host shortly after the alert, and where?
  context: A successful dump leaves a file on disk. Adplus writes to an output directory passed with -o, commonly under
    user-writable or temp locations such as %TEMP% or C:\Windows\Temp. A new .dmp or large binary written to such a path
    in the minutes after the alert corroborates that memory was actually captured and tells the responder what to contain
    and where the credentials now reside.
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      filter_ext:
        TargetFilename|endswith:
          - .dmp
          - .dump
      condition: selection and filter_ext
    fields:
      - Image
      - TargetFilename
      - User
- source: attack:T1003.001.command_analysis
  question: Did other known credential-dumping command patterns run on this host around the same time?
  context: Adplus is one of many ways to reach lsass. Operators frequently chain or fall back to other dumpers, so nearby
    use of comsvcs.dll MiniDump, procdump, rundll32 against lsass, or PowerShell mimikatz-style cradles raises confidence
    that this is staged credential theft rather than isolated debugging. This surfaces those adjacent command structures on the
    same host.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_cli:
        CommandLine|contains:
          - 'comsvcs.dll'
          - 'MiniDump'
          - 'procdump'
          - 'lsass'
          - 'Invoke-Mimikatz'
          - 'sekurlsa'
      condition: selection and selection_cli
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned adplus and is that parent chain expected for a debugging tool?
  context: The parent process reveals how adplus was invoked. Interactive debugging typically descends from explorer.exe,
    a console host, or a development tool. Parents such as wscript.exe, mshta.exe, a web shell, or an Office application
    point to scripted or exploitation-driven launch rather than a developer at the keyboard.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did adplus spawn afterward?
  context: With the -sc and -c flags, adplus can launch arbitrary commands and binaries from the context of a signed
    debugger. Children such as cmd.exe, powershell.exe, certutil.exe, or unexpected binaries indicate the tool was used for
    execution rather than, or in addition to, memory capture.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run adplus?
  context: Adplus ships with the Windows SDK Debugging Tools and is absent from most production endpoints. Zero or very low
    historical counts indicate first-time or rare execution and raise the weight of every other deviation; routine appearance
    on a developer or build host lowers it.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and
    does the current alert fit that pattern?
  context: Even a legitimately installed debugger can be abused. The query mirrors the rule's selection structure scoped to
    this host, so the resulting parent/user distribution reflects how the rule's specific firing pattern has historically run
    here. A new parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \adplus.exe
      - OriginalFileName: Adplus.exe
      selection_cli:
        CommandLine|contains:
        - ' -hang '
        - ' -pn '
        - ' -pmn '
        - ' -p '
        - ' -po '
        - ' -c '
        - ' -sc '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \adplus.exe
      - OriginalFileName: Adplus.exe
      selection_cli:
        CommandLine|contains:
        - ' -hang '
        - ' -pn '
        - ' -pmn '
        - ' -p '
        - ' -po '
        - ' -c '
        - ' -sc '
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \adplus.exe
      - OriginalFileName: Adplus.exe
      selection_cli:
        CommandLine|contains:
        - ' -hang '
        - ' -pn '
        - ' -pmn '
        - ' -p '
        - ' -po '
        - ' -c '
        - ' -sc '
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user drove similar debugging or dumping activity on several hosts, the scope of compromise may be larger than
    this one alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \adplus.exe
      - OriginalFileName: Adplus.exe
      selection_cli:
        CommandLine|contains:
        - ' -hang '
        - ' -pn '
        - ' -pmn '
        - ' -p '
        - ' -po '
        - ' -c '
        - ' -sc '
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be compromised, and do any have elevated privileges?
  context: The severity of credential theft depends on the privilege of the accounts whose secrets were in lsass at dump
    time. Domain admin or service-account credentials require incident escalation; a standard user account may only need
    monitoring. Cross-reference the accounts active on this host with directory group memberships.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or
    privilege escalation — may reveal the stages that preceded a credential-dump attempt and help reconstruct the full
    timeline leading to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1003.001
    name: LSASS Memory
    role: primary
  - id: T1003
    name: OS Credential Dumping
    role: secondary
  attack_chain_coverage:
  - stage: 1
    label: process_attach
    description: A process opens a handle with full access to lsass.exe (or another target) to read its memory
    mapped_to: Q4
    source: attack:T1003.001.access_verification
  - stage: 2
    label: memory_dump
    description: The accessing process invokes a memory dump, writing dump output to disk
    mapped_to: Q5
    source: attack:T1003.001.tuning_parameter
  - stage: 3
    label: credential_use
    description: Captured credentials are subsequently used for lateral movement
    mapped_to: Q15
    source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
  - dimension: access_verification
    technique: T1003.001
    covered_by: Q4
    source: attack:T1003.001.access_verification
    coverage_type: direct
  - dimension: command_analysis
    technique: T1003.001
    covered_by: Q6
    source: attack:T1003.001.command_analysis
    coverage_type: direct
  - dimension: parent_process_analysis
    technique: T1003.001
    covered_by: Q7
    source: template:cue_process_tool.process_chain
    coverage_type: direct
  - dimension: time_correlation
    technique: T1003.001
    covered_by: Q5
    source: attack:T1003.001.tuning_parameter
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1003.001
    covered_by: Q5
    source: attack:T1003.001.tuning_parameter
    coverage_type: direct
  mutable_dispositions:
  - element: AccessMask
    dimension: access_verification
    specific_values: '0x1F0FFF'
    disposition: new_question
    target_question: Q4
    notes: Authored process_access question on TargetImage lsass.exe; GrantedAccess surfaced in fields so the analyst reads
      the actual mask (0x1F0FFF and its scoped-down variants) from the handle event.
  - element: TimeWindow
    dimension: time_correlation
    specific_values: 5 minutes between access and dump
    disposition: context
    target_question: Q5
    notes: Encoded as the +30m dump-file window after the alert and the +/-15m process_access window; threshold is not
      directly queryable so it is expressed as range scoping.
  - element: ParentProcessName
    dimension: parent_process_analysis
    specific_values: rationale only (allowlist AV/EDR)
    disposition: context
    target_question: Q7
    notes: Allowlist-known-tools rationale carried into the parent-chain question context; no fixed value list to inject.
  - element: DumpFilePath
    dimension: tuning_parameter
    specific_values: '%TEMP%, C:\Windows\Temp'
    disposition: new_question
    target_question: Q5
    notes: Authored file_event question filtering .dmp/.dump writes on the host; paths named in context as where to look.
  - element: CommandLinePattern
    dimension: command_analysis
    specific_values: rundll32, procdump, comsvcs.dll, Invoke-Mimikatz
    disposition: query_injection
    target_question: Q6
    notes: Injected comsvcs.dll, MiniDump, procdump, lsass, Invoke-Mimikatz, sekurlsa into the command_analysis selection_cli
      CommandLine|contains block.
  dedup_decisions:
  - dropped: cue_process_tool.mechanism_decode
    reason: 'replaced_by: credential_access.credential_target_identification — technique-set Phase 2 is more specific for a
      credential-access rule'
    kept: credential_access.credential_target_identification
  - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair
    reason: user_actor pair — non-deduplicable; emitted both per service-principal handling
    kept: both
  - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair
    reason: user_actor pair — non-deduplicable; emitted both per service-principal handling
    kept: both
  skipped_templates:
  - template: cue_process_tool.clustering
    reason: cue_host_context.clustering and cue_user_actor.clustering already cover concurrent-host activity; the
      command_analysis question (Q6) covers the dumping-tool variant of around-the-same-time host context more precisely
  - template: cue_host_context.clustering
    reason: superseded by cue_host_context.precursor (prior-alert context) plus the command_analysis sweep (Q6) for a
      credential-access rule; a flat host-wide process aggregation adds little over those for this disposition
  - template: cue_process_tool.scope
    reason: cross-host fleet count of the rule pattern adds little for a debugger that legitimately runs only on isolated
      dev/build hosts; cross-host coordination is already covered by the user_actor clustering_svc question (Q14)
  - template: universal.endpoint_protection_health
    reason: dropped under the 18-question cap; cue_host_context.precursor (Q17) already surfaces prior tamper/defense-evasion
      alerts on the host, and the credential-access investigation is centered on the dump primitive rather than a tamper
      precursor sweep
  reflection_overrides: []
  hint_dispositions:
  - hint: 'logsource ''registry_set'' (recommended by techniques: T1003.001) is not covered — pull in cue_registry_key.*
      or author a custom question with `category: registry_set`'
    disposition: declined
    reason: >-
      The registry_set analytic for T1003.001 targets the staged-dump variant where credential output is written to or
      flagged in the registry; adplus dumps to a file via its -o output directory, not the registry, so a registry_set
      question would not surface this binary's dump path. The file_event question (Q5) covers the on-disk dump artifact
      that is the actual output of an adplus capture.
  - hint: 'logsource ''security'' (recommended by techniques: T1003.001) is not covered and no template family exists —
      author a custom question with `category: security`'
    disposition: declined
    reason: >-
      The security analytic relies on Event 4673 (privileged service called) for SeDebugPrivilege use on lsass; the
      process_access question (Q4) covers the same access primitive at the handle level (TargetImage lsass.exe with
      GrantedAccess) from Sysmon EventCode 10, which is more directly tied to adplus opening the handle than the
      coarser 4673 privilege-use event. No compensating category:security question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - credential_access
  generation_stats:
    total_questions: 17
    source_counts:
      template: 14
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      process_access: 1
      file_event: 1
      sigma_alert: 2
