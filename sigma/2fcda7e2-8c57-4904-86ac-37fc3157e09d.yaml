name: Sensitive File Dump Via Print.EXE
id: e4f87b9b-8474-4af3-8308-a63210e83cad
description: |
  Detects abuse of the signed Windows utility print.exe to copy sensitive credential
  stores -- the SAM, SECURITY, or SYSTEM registry hives from the config directory, or
  the domain controller's ntds.dit -- to an attacker-chosen destination for offline
  hash extraction. The rule fires when print.exe (by image name or original filename)
  runs with the /D switch and a command line referencing one of those sensitive paths.
  Legitimate use of print.exe to send these specific files to a printer is implausible,
  so a true positive points at hands-on credential theft; the investigation should
  determine which store was targeted, where the copy landed, which accounts are exposed,
  and whether lateral movement followed.
type: detection
detection_id: 2fcda7e2-8c57-4904-86ac-37fc3157e09d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show print.exe copying a sensitive credential store with the /D switch?
  context: Confirm the alert is a true positive by reviewing the exact command line and process
    details. The rule fires only when print.exe (or a binary whose original filename is Print.EXE)
    runs with /D and a path referencing \config\SAM, \config\SECURITY, \config\SYSTEM, or
    \windows\ntds\ntds.dit. Note which target appears -- a registry hive points at local SAM-based
    theft, ntds.dit points at domain-wide credential extraction.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to copy registry hives or ntds.dit?
  context: Copying the SAM/SYSTEM/SECURITY hives or ntds.dit is not a routine task for ordinary
    users or workstations. On a domain controller a backup process may touch ntds.dit, but never
    through print.exe. Knowing whether the account and host function fit any legitimate workflow
    is the fastest way to separate a true positive from an anomaly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: Which credential store was targeted, and was the copy operation completed?
  context: The path in the command line identifies the store -- the SAM hive alone yields local
    account hashes, but SAM plus SYSTEM (and ideally SECURITY) is what enables offline hash
    extraction, while ntds.dit yields every domain account. Read the full command to confirm the
    source path and capture the destination the copy was written to.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.002.tuning_parameter
  question: Where did the copied credential store land on disk?
  context: The /D destination in the command line names where the hive or ntds.dit copy was
    written. Pull file-creation events on this host for that destination path to confirm the file
    landed and to see whether it was subsequently staged, archived, or moved. Dump files commonly
    appear under user-writable locations such as the public profile or a temp directory.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - file.path
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned print.exe and is that parent chain expected?
  context: The parent reveals how print.exe was invoked. A console shell, a script host (wscript,
    cscript, mshta), a remote-execution service, or an exploited application server spawning
    print.exe with these arguments points at hands-on operator activity rather than any legitimate
    workflow. Walk the parent's own parent if the immediate parent is itself a shell or service host.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1003.002.command_analysis
  question: What other credential-dumping commands ran on this host around the same time?
  context: A print.exe hive copy is rarely a standalone action. Operators commonly pair it with
    reg save of the SAM and SYSTEM hives, shadow-copy creation for ntds.dit access, ntdsutil,
    reconnaissance commands, or archiving of the dumped files. Surfacing nearby command lines that
    reference credential stores or staging shows whether this is one step in a broader collection.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_cred:
        CommandLine|contains:
          - 'reg save'
          - 'reg.exe save'
          - '\config\SAM'
          - '\config\SECURITY'
          - '\config\SYSTEM'
          - 'ntds.dit'
          - 'ntdsutil'
          - 'vssadmin'
          - 'shadowcopy'
          - 'HarddiskVolumeShadowCopy'
      condition: selection_host and selection_cred
    fields:
      - CommandLine
      - Image
      - User
- source: template:cue_process_tool.follow_on
  question: What child processes did print.exe spawn afterward?
  context: print.exe performing a file copy should not spawn children. Any subsequent process under
    it -- a compressor, an exfiltration tool, a shell, or a hash-extraction utility -- indicates the
    copy was part of a larger operation and identifies the next stage to investigate.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be compromised, and do any have elevated privileges?
  context: The severity depends on what the targeted store holds. A SAM copy exposes local accounts
    on this host; a SECURITY hive copy can expose cached domain credentials and LSA secrets; ntds.dit
    exposes every domain account including domain admins and the krbtgt account. Cross-reference the
    exposed accounts against directory group memberships to scope escalation and remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously matched this print.exe credential-dump pattern, what parents and users drove it, and does the current alert fit?
  context: The query mirrors the rule's selection scoped to this host, so the resulting parent/user
    distribution reflects how this specific firing pattern has run here before -- which for a
    credential-dump rule is almost always zero. A new parent, an unexpected account, or any prior
    history at all is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \print.exe
      - OriginalFileName: Print.EXE
      selection_cli:
        CommandLine|contains|windash: /D
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \windows\ntds\ntds.dit
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \print.exe
      - OriginalFileName: Print.EXE
      selection_cli:
        CommandLine|contains|windash: /D
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \windows\ntds\ntds.dit
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \print.exe
      - OriginalFileName: Print.EXE
      selection_cli:
        CommandLine|contains|windash: /D
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \windows\ntds\ntds.dit
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging on
    multiple endpoints. If the same account performed credential access or copied files on more than
    one host in this window, the scope of compromise is larger than the alerting host alone.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \print.exe
      - OriginalFileName: Print.EXE
      selection_cli:
        CommandLine|contains|windash: /D
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \windows\ntds\ntds.dit
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Credential dumping rarely happens in isolation. Earlier detections on the same host --
    exploitation of an application server, defense tampering, reconnaissance, or privilege escalation
    -- may reveal the stages that preceded the copy and help reconstruct how the operator reached the
    point of harvesting credential stores.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.003
      name: NTDS
      role: primary
    - id: T1003.002
      name: Security Account Manager
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: sensitive_store_access
      description: "print.exe invoked with /D to copy a SAM/SECURITY/SYSTEM hive or ntds.dit"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: credential_target_identification
      description: "Identify which credential store was targeted and whether the copy completed"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 3
      label: dump_file_creation
      description: "The copied hive/ntds.dit lands on disk at the /D destination for offline extraction"
      mapped_to: Q4
      source: attack:T1003.002.tuning_parameter
    - stage: 4
      label: offline_extraction_and_movement
      description: "Stolen credentials used for lateral movement / follow-on access"
      mapped_to: Q9
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: access_verification
      technique: T1003
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: indirect
    - dimension: parent_process_analysis
      technique: T1003.003
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.003
      covered_by: Q6
      source: attack:T1003.002.command_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1003.003
      covered_by: Q4
      source: attack:T1003.002.tuning_parameter
      coverage_type: direct
    - dimension: user_context
      technique: T1003.003
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1003.002
      covered_by: Q6
      source: attack:T1003.002.command_analysis
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: indirect
  mutable_dispositions:
    - element: TargetFilePath
      dimension: tuning_parameter
      specific_values: "\\config\\SAM, \\config\\SECURITY, \\config\\SYSTEM, \\windows\\ntds\\ntds.dit"
      disposition: new_question
      target_question: Q4
      notes: "Source paths are enforced by the rule selection and re-surfaced in Q1/Q3; Q4 pivots to the /D destination via a file_event query on %file_dirs%."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "reg save, reg.exe save, ntdsutil, vssadmin, shadowcopy"
      disposition: query_injection
      target_question: Q6
      notes: "Injected into the host-scoped credential-dump CommandLine|contains sweep."
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF"
      disposition: context
      target_question: Q3
      notes: "T1003 LSASS access-mask is not applicable; this rule detects direct file copy, not process_access. Not queryable here."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Parent-chain legitimacy covered by process_chain question."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q1
      notes: "print.exe is the signed proxy binary; named in description and Q1 verification."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism identity)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism identity)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification"
    - template: cue_process_tool.baseline
      reason: "rule-pattern baseline (cue_process_tool.baseline_deviation, reflected host_pattern) supersedes the generic per-binary baseline for a credential-dump rule where any history is anomalous"
    - template: cue_process_tool.clustering
      reason: "generic host-wide tool clustering replaced by the credential-dump-scoped command sweep (Q6) and host precursor question, which carry the relevant context without per-event noise"
    - template: cue_process_tool.scope
      reason: "reflected fleet_pattern cross-host count of a credential-dump pattern is near-certain to be a single host; cross-host coordination is instead covered by the parent+binary clustering_svc question (Q15) at the alert window"
    - template: cue_host_context.clustering
      reason: "host-wide Image+ParentImage aggregation overlaps the credential-dump command sweep (Q6) and precursor question; dropped to stay within the 14-18 cap while keeping the more specific views"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        T1218's image_load analytic targets DLL-loading proxy binaries (rundll32,
        regsvr32, msiexec) executing payloads. This rule's print.exe abuse is a file
        copy of credential stores, not module loading; print.exe loads no attacker DLL
        and the credential-theft chain leaves no image_load artifact that scopes the
        investigation. The dumped-file artifact is covered via file_event in Q4 instead.
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: declined
      reason: >-
        T1218's network_connection analytic covers signed binaries retrieving remote
        payloads. print.exe here copies local credential stores to a local destination
        and makes no outbound connection on the copy itself; the relevant network step
        is post-theft lateral movement, which is covered by the alert-correlation
        lateral-movement question (Q9) rather than a print.exe network_connection query.
    - hint: "logsource 'registry_set' (recommended by techniques: T1003.002) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        registry_set covers reg save / direct hive-key access dumping of SAM. This rule
        detects file-level copy of the hive files from \config, not registry-key writes,
        so a registry_set query on this event returns nothing tied to the firing. The
        adjacent reg save variant of SAM dumping is instead caught at process_creation
        level by the credential-dump command sweep (Q6).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      file_event: 1
      sigma_alert: 2
