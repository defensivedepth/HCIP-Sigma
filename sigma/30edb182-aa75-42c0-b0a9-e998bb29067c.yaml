name: Potential AMSI Bypass Via .NET Reflection
id: b21e630a-d03d-4efe-9f7f-86398e1d848a
description: |
  Detects command lines that use .NET reflection to reach the in-process AmsiUtils
  type and flip the amsiInitFailed field, which forces the Antimalware Scan Interface
  to return "not detected" for everything scanned afterward in that process. The rule
  fires when a command line contains the System.Management.Automation.AmsiUtils and
  amsiInitFailed strings together, or the reflection sequence [Ref].Assembly.GetType
  with SetValue($null,$true) and the NonPublic,Static binding flags. This is almost
  never a legitimate administrative action; the investigation should confirm the
  reflection pattern fired, decode the surrounding command to see what the bypass was
  meant to enable, and check whether other defenses were weakened or whether
  execution followed in the same window.
type: detection
detection_id: 30edb182-aa75-42c0-b0a9-e998bb29067c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain the .NET reflection AMSI-bypass pattern this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive
    by reading the full command line. The rule fires on either of two string sets in
    the same command — the AmsiUtils type name together with amsiInitFailed, or the
    reflection call [Ref].Assembly.GetType combined with SetValue($null,$true) and the
    NonPublic,Static binding flags. Note which set is present and whether the strings
    appear in plain form or are reconstructed from concatenation, encoding, or hex.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run .NET reflection code that disables AMSI?
  context: Legitimate administrative activity rarely reaches into private AmsiUtils
    fields via reflection. Knowing whether the account is a developer or security
    tester working in a sanctioned context, and whether the host is a workstation
    where such tooling is plausible, is often the fastest way to separate red-team or
    research activity from an actual intrusion.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Decode the full command line — what was the AMSI bypass embedded in, and what did it set up to run?
  context: The reflection one-liner that flips amsiInitFailed is usually a prefix to a
    larger payload that AMSI would otherwise have flagged. Read the full command to
    identify what follows the bypass — an encoded blob, a download cradle, an
    in-memory assembly load, or a credential-access script. Trigger strings are
    frequently reconstructed from concatenation, Base64, or hex to evade the
    signature, so the visible form may differ from the canonical text.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this command and is that parent chain expected?
  context: The parent process reveals how the bypass was invoked. A bypass launched
    from an interactive shell under explorer.exe looks different from one spawned by
    an Office application, a script host such as wscript.exe or mshta.exe, or a remote
    execution service — the latter chains point to exploitation or remote operator
    activity rather than local testing.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is AMSI scanning currently effective on this host, or does it remain bypassed?
  context: The amsiInitFailed flag is set per-process and dies with the process, but a
    bypass that ran inside a long-lived or repeatedly spawned host process can leave a
    window where scanning is ineffective. Verify current protection status through the
    endpoint console or direct host inspection, and confirm whether the process that
    ran the bypass is still alive before treating scanning as restored.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the AMSI bypass ran?
  context: The bypass exists to let something run that AMSI would otherwise block.
    Examining process creation immediately after the bypass reveals what it was meant
    to enable — script execution, credential dumping, or download of a follow-on
    stage. Correlate this window with network connections and file writes to see what
    the unscanned execution actually did.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host in the surrounding window?
  context: An operator who disables AMSI commonly disables several controls — stopping
    Defender, clearing event logs, adding scan exclusions, or truncating the
    script-block-logging signature set. Finding multiple tampering actions from the
    same user or parent process in the same window indicates a deliberate effort to
    blind the host rather than an isolated action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
          - 'cachedGroupPolicySettings'
          - 'ScriptBlockLogging'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1562.001.registry_tamper
  question: Were Defender or script-logging registry keys modified on this host around the alert?
  context: Beyond the in-memory AMSI flag, the same operator may persist defense
    evasion by editing registry values that disable real-time monitoring, add
    exclusions, or turn off PowerShell script-block logging. Registry modification
    events surface tampering that survives the process and reboots, which the
    command-line bypass alone does not.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_keys:
        TargetObject|contains:
          - '\Windows Defender\'
          - '\Windows Defender Security Center\'
          - '\PowerShell\ScriptBlockLogging'
          - '\PowerShell\ModuleLogging'
          - '\PowerShell\Transcription'
      condition: selection_host and selection_keys
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this AMSI-bypass pattern, what parents and users drove it, and does this alert fit?
  context: Even research and red-team activity follows a pattern on a given host. The
    query mirrors the rule's selection scoped to this host, so the resulting
    parent/user distribution reflects how this specific bypass pattern — not generic
    command activity — has run here before. A new parent process, an unexpected
    account, or a first-ever appearance is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains|all:
        - System.Management.Automation.AmsiUtils
        - amsiInitFailed
      - CommandLine|contains|all:
        - '[Ref].Assembly.GetType'
        - SetValue($null,$true)
        - NonPublic,Static
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this AMSI-bypass pattern in the past week, and does the spread look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a
    fleet-wide count of this specific bypass pattern rather than generic command
    usage. Many hosts firing the same pattern can point to a sanctioned security
    tool or scripted assessment; a single host or a small unrelated set points to
    manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains|all:
        - System.Management.Automation.AmsiUtils
        - amsiInitFailed
      - CommandLine|contains|all:
        - '[Ref].Assembly.GetType'
        - SetValue($null,$true)
        - NonPublic,Static
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains|all:
        - System.Management.Automation.AmsiUtils
        - amsiInitFailed
      - CommandLine|contains|all:
        - '[Ref].Assembly.GetType'
        - SetValue($null,$true)
        - NonPublic,Static
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains|all:
        - System.Management.Automation.AmsiUtils
        - amsiInitFailed
      - CommandLine|contains|all:
        - '[Ref].Assembly.GetType'
        - SetValue($null,$true)
        - NonPublic,Static
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the account's activity across all hosts in the alert window
    reveals lateral movement or parallel activity on multiple endpoints. If the same
    user ran commands on several hosts close in time, the scope of the operation may
    extend beyond the alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains|all:
        - System.Management.Automation.AmsiUtils
        - amsiInitFailed
      - CommandLine|contains|all:
        - '[Ref].Assembly.GetType'
        - SetValue($null,$true)
        - NonPublic,Static
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert
    reveals the immediate context: what led to this event, what happened after, and
    whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, or persistence installation surrounding the bypass.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, credential access, or
    other defense-evasion alerts — may reveal the attack stages that preceded the
    bypass and help reconstruct the full timeline. Defense tampering frequently sits
    in the middle of a chain rather than at its start.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tamper_invocation
      description: "Adversary invokes the .NET reflection sequence that flips amsiInitFailed, disabling AMSI scanning in-process"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: tamper_decode
      description: "Decode the full command to identify the targeted control and the payload the bypass enables"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 3
      label: telemetry_absence_followon
      description: "Defender observes abnormal absence of expected scanning and process/registry tamper; correlate post-tamper execution"
      mapped_to: Q6
      source: template:defense_tampering.activity_after_tampering
    - stage: 4
      label: registry_modification
      description: "Registry modification disabling security tools / script logging (EventCode 13,14)"
      mapped_to: Q8
      source: attack:T1562.001.registry_tamper
    - stage: 5
      label: service_process_termination
      description: "Process/service termination or other tamper of AV/EDR controls observed (EventCode 7045, 4688, 5)"
      mapped_to: Q7
      source: template:defense_tampering.other_tampering_activity
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q6
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.other_tampering_activity
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only (list of expected admin tools)"
      disposition: context
      target_question: Q7
      notes: "No concrete values in brief; tuning-rationale element. other_tampering_activity already excludes known patterns by design; the analyst calibrates against expected admin tools per environment."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window)"
      disposition: context
      target_question: Q6
      notes: "Tuning rationale, not a queryable value. Post-tamper window realized as +1h forward-looking range on activity_after_tampering and +/-1h on tamper-correlation questions."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only (security service names per deployment)"
      disposition: context
      target_question: Q7
      notes: "No concrete value list provided in the brief mutable_element; the security-service name list is embedded in the other_tampering_activity tamper selection. Per-deployment service names calibrated by analyst."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only (authorized admin tools)"
      disposition: context
      target_question: Q4
      notes: "Tuning rationale for T1562 secondary; process_chain identifies the launching process so the analyst can assess whether it is an authorized administrative tool."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Duplicate of ServiceNames rationale from secondary technique; covered by the same tamper-pattern question."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by defense_tampering.what_was_disabled — technique-set Phase 2 question is more specific"
      kept: defense_tampering.what_was_disabled
    - dropped: cue_process_tool.baseline
      reason: "Generic host+Image baseline; rule is CommandLine-only with no Image field, so %Image% scoping does not hydrate. baseline_deviation (REFLECTED, CommandLine-pattern scoped to host) supersedes it with a filter-correct historical view."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "Uses ParentImage|endswith %Image%; rule has no Image field so the scope does not hydrate. defense_tampering.activity_after_tampering covers post-event host activity and is more specific to the tamper-then-execute pattern."
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_process_tool.clustering
      reason: "host.name-only clustering duplicates cue_host_context.clustering; kept the host_context version (richer Image+ParentImage projection)."
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.baseline / baseline_svc pairing note
      reason: "user_actor pair — non-deduplicable. Both user-pivot baseline and the service-principal substitute emitted (Q11/Q12); they pivot on subject vs. mechanism."
      kept: both
    - dropped: cue_user_actor.clustering / clustering_svc pairing note
      reason: "user_actor pair — non-deduplicable. Both user-pivot clustering and the service-principal substitute emitted (Q13/Q14)."
      kept: both
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED-tier universal question. Safe-to-drop condition (a) applies: the defense_tampering technique set is CONFIRMED for this rule, and its what_was_disabled (Q3), activity_after_tampering (Q6), and other_tampering_activity (Q7) questions provide more specific, rule-tailored tamper coverage than the generic host-tamper sweep. The other_tampering_activity question explicitly surfaces OTHER tamper actions (Defender disable, log clearing, script-logging tamper) on the host in the same window, which is the precise signal endpoint_protection_health would have provided. Not a silent drop — recorded here."
    - template: persistence_scope.process_based_persistence
      reason: "RECOMMENDED-tier; cascade from defense_tampering activated persistence_scope, but this rule detects an in-memory AMSI bypass via .NET reflection — a defense-evasion action, not a persistence-installation action. No persistence mechanism is implied by the detection. Dropped to keep question count within the 14-18 target while prioritizing tamper-specific and identity coverage. Recorded here, not silently dropped."
    - template: persistence_scope.file_based_persistence
      reason: "RECOMMENDED-tier; same rationale as process_based_persistence — the rule fires on an in-memory reflection bypass, not a file-drop persistence behavior. Dropped to stay within question budget; recorded here."
  reflection_overrides: []
  hint_dispositions:
    # All five brief hints were ACTED ON; none silently declined. Acted-on hints
    # do not require an entry here, but the dispositions are summarized for audit:
    #   1. "cmd.exe too common for Image-based scoping" — ACTED ON: no query scopes
    #      on Image. mechanism_decode is ProcessGuid-scoped via its replacement
    #      (Q3 what_was_disabled). Host/fleet/user historical queries (Q9 scope,
    #      Q10 baseline_deviation, Q11/Q12 user baselines, Q14 clustering_svc) scope
    #      on the rule's CommandLine|contains|all AMSI pattern.
    #   2. "what_was_disabled replaces mechanism_decode" — ACTED ON: see
    #      dedup_decisions (mechanism_decode dropped, what_was_disabled = Q3).
    #   3. "defense_tampering -> persistence_scope cascade" — ACTED ON / evaluated:
    #      cascade made the persistence_scope set available; both its questions were
    #      deliberately not emitted (in-memory bypass, no persistence behavior) and
    #      the decision is recorded in skipped_templates.
    #   4. "CommandLine-only detection, no Image field" — ACTED ON: every scoping
    #      query uses host.name / User / ProcessGuid / CommandLine patterns; no
    #      %Image% placeholder appears anywhere in the playbook.
    #   5. "logsource registry_set recommended by T1562.001 not covered" — ACTED ON:
    #      Q8 (attack:T1562.001.registry_tamper) adds a category: registry_set query
    #      checking Defender / script-logging key modifications.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      registry_set: 1
      sigma_alert: 1
