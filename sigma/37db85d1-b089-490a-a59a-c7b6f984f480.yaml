name: Sysmon Discovery Via Default Driver Altitude Using Findstr.EXE
id: eda4fe95-5bb4-4b21-802f-e96a758989c5
description: |
  Detects findstr.exe (or find.exe) invoked with the argument 385201, the
  default driver altitude assigned to the Sysmon monitoring driver. Searching
  driver listings for this altitude value identifies an installed Sysmon
  service even when its service and driver names have been renamed, so the
  pattern is a security-software discovery step that commonly precedes defense
  evasion. Legitimate use is rare; confirm the command line, establish whether
  the user and host routinely perform this kind of enumeration, and determine
  what process drove the search and what ran around it.
type: detection
detection_id: 37db85d1-b089-490a-a59a-c7b6f984f480
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects, with find/findstr searching for the altitude value 385201?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when find.exe or findstr.exe (by Image or OriginalFileName) runs with 385201 in its arguments, so verify that string is present and that the match is not an incidental substring.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to enumerate installed security tooling from the command line?
  context: Legitimate administrative activity can trigger this detection, but searching driver altitudes for the Sysmon value is not routine troubleshooting. Knowing whether the user role and host function include security tooling inventory or detection-engineering work is often the fastest path to distinguishing a true positive from sanctioned activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did the full find/findstr command search for beyond the 385201 altitude, and what input was it reading?
  context: The full command line reveals the specific search target. Determine what file, pipe, or piped command output the search was reading and whether other altitude values or driver names were also queried, which indicates broader enumeration of monitoring drivers rather than a single check.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains: ' 385201'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this find/findstr command and is that parent chain expected?
  context: The parent process reveals how the search was invoked. Discovery commands run interactively typically descend from explorer.exe via cmd.exe, while parents such as script interpreters, Office applications, or remote-execution hosts point to automated post-compromise enumeration. The immediate parent is in the alert; trace what spawned it.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the discovery command, and is this a normal pattern?
  context: Legitimate discovery commands are typically run from cmd.exe spawned by explorer.exe or from automation scripts. Discovery commands spawned by unusual parents such as Word macros, script interpreters, or WMI and WinRM provider hosts indicate automated post-exploitation enumeration rather than an operator at a console.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user expected to run administrative discovery commands?
  context: Detection engineers, administrators, and IT staff occasionally inventory installed monitoring tooling, but standard users rarely search driver altitudes. The user's role and behavior baseline determine whether searching for the Sysmon altitude is routine work or anomalous enumeration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: attack:T1518.001.image_load
  question: Did the Sysmon driver load on this host, and was its altitude or driver name altered from the defaults?
  context: The 385201 search assumes Sysmon is present under the default driver altitude even if the service and driver were renamed. Driver-load events show whether the monitoring driver is actually loaded, under what image name, and whether it sits at the default altitude. A renamed driver still loading at altitude 385201 is exactly what this search is built to find. If driver-load events are not collected in this environment, treat zero results as a visibility gap rather than evidence the driver is absent.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ImageLoaded|endswith:
          - '\SysmonDrv.sys'
          - '.sys'
      condition: selection
    fields:
      - ImageLoaded
      - Image
      - Signature
- source: template:cue_process_tool.follow_on
  question: What child processes did this command spawn afterward?
  context: A discovery step that resolves the Sysmon altitude often precedes an attempt to tamper with or unload the monitoring driver. Processes launched after the search reveal whether the operator moved on to service control, driver unloading, or other defense-evasion actions.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single discovery command may be legitimate troubleshooting, but multiple reconnaissance commands in rapid succession is a signature pattern of post-compromise enumeration. Three or more distinct discovery binaries within the window alongside the altitude search points to automated host enumeration before the next attack stage.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the altitude-search pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even a benign-looking utility can be abused. The query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has run here before. A new parent process, an unexpected user account, or a first-ever occurrence is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \find.exe
        - \findstr.exe
      - OriginalFileName:
        - FIND.EXE
        - FINDSTR.EXE
      selection_cli:
        CommandLine|contains: ' 385201'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run find or findstr to inventory installed tooling?
  context: Compare this user's current action against their historical behavior. A user who has never run find or findstr against driver listings is more suspicious than one who does so regularly as part of a defined role. The user's typical toolset should align with security-tooling enumeration if this is to be considered routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned find or findstr, and whether that pair is historically common. A new or rare parent for this binary is the deviation worth chasing; a pair that already accounts for many prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar enumeration on other hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and binary combination fire on other hosts in the same window, indicating a coordinated automation rollout versus isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise because every host runs system-owned agents constantly. The meaningful question is whether the same launch mechanism — the parent process and spawned binary — fired on other hosts in the alert window. Multiple hosts points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or defense tampering.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the attack stages that preceded this enumeration and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1518.001
    name: Security Software Discovery
    role: primary
  - id: T1518
    name: Software Discovery
    role: secondary
  attack_chain_coverage:
  - stage: 1
    label: enumeration_execution
    description: Adversary executes a built-in tool to enumerate installed security software, here find/findstr searching for the Sysmon default driver altitude.
    mapped_to: Q3
    source: template:cue_process_tool.mechanism_decode
  - stage: 2
    label: driver_presence_confirmation
    description: The search confirms whether the monitoring driver is loaded under the default altitude even if renamed.
    mapped_to: Q7
    source: attack:T1518.001.image_load
  - stage: 3
    label: follow_on_action
    description: Discovery of installed monitoring tooling often precedes tampering or further enumeration.
    mapped_to: Q8
    source: template:cue_process_tool.follow_on
  dimension_coverage:
  - dimension: parent_process_analysis
    technique: T1518.001
    covered_by: Q4
    source: template:cue_process_tool.process_chain
    coverage_type: direct
  - dimension: time_correlation
    technique: T1518.001
    covered_by: Q9
    source: template:discovery_recon.discovery_burst_detection
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1518.001
    covered_by: Q3
    source: template:cue_process_tool.mechanism_decode
    coverage_type: direct
  - dimension: network_indicators
    technique: T1518
    covered_by: Q14
    source: template:cue_user_actor.clustering
    coverage_type: partial
  - dimension: parent_process_analysis
    technique: T1518
    covered_by: Q5
    source: template:discovery_recon.discovery_parent_chain
    coverage_type: direct
  - dimension: time_correlation
    technique: T1518
    covered_by: Q15
    source: template:cue_host_context.clustering
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1518
    covered_by: Q3
    source: template:cue_process_tool.mechanism_decode
    coverage_type: direct
  mutable_dispositions:
  - element: ParentProcess
    dimension: parent_process_analysis
    specific_values: rationale only
    disposition: new_question
    target_question: Q4
    notes: Parent-chain analysis covered by process_chain and discovery_parent_chain questions; no concrete parent values supplied by the analytic.
  - element: ImagePathContains
    dimension: tuning_parameter
    specific_values: ' 385201'
    disposition: query_injection
    target_question: Q3
    notes: Rule's distinguishing CommandLine|contains ' 385201' injected into mechanism_decode selection to keep the host+binary scope tied to the firing pattern.
  - element: TimeWindow
    dimension: time_correlation
    specific_values: rationale only
    disposition: new_question
    target_question: Q9
    notes: Burst-detection question scopes multiple recon commands within +/-15m; threshold is rationale only, not a queryable constant.
  dedup_decisions:
  - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
    reason: user_actor pair — non-deduplicable; emit both per AGENTS.md service-principal handling
    kept: both
  - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
    reason: user_actor pair — non-deduplicable; emit both per AGENTS.md service-principal handling
    kept: both
  skipped_templates:
  - template: template:cue_process_tool.baseline
    reason: Subsumed by cue_user_actor.baseline (user+binary 90d) and cue_process_tool.baseline_deviation (host pattern 14d); a third generic host+binary frequency query adds no distinct dimension.
  - template: template:cue_process_tool.clustering
    reason: Replaced by cue_host_context.clustering, which covers the same around-the-alert host activity with a richer Image+ParentImage projection.
  - template: template:cue_process_tool.scope
    reason: Fleet-wide firing-pattern count overlaps cue_user_actor.clustering_svc cross-host coverage; dropped to stay within the 14-18 cap while keeping the more discriminating parent+binary cross-host question.
  - template: template:universal.endpoint_protection_health
    reason: Recommended (not must_include) generic tamper sweep; this rule is discovery, not execution or credential access, and cue_host_context.precursor already surfaces prior tamper alerts on the host, so the generic sweep does not earn a slot.
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - discovery_recon
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      image_load: 1
      sigma_alert: 1
