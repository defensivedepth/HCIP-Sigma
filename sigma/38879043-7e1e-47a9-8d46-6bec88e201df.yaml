name: Potential Persistence Attempt Via Existing Service Tampering
id: e497c64d-d3a7-4390-9bd4-47f79a068871
description: |
  Detects modification of an existing Windows service so it executes an attacker-chosen
  payload when the service starts or fails, a persistence and privilege-escalation method
  that hijacks a service already present rather than installing a new one. The rule fires
  when a command line matches EITHER an sc.exe service reconfiguration (config with binpath=,
  or failure with command=) OR a reg.exe modification of a service registry value
  (FailureCommand or ImagePath) carrying an executable extension. Legitimate administrators
  and deployment tooling do reconfigure services, so the investigation should determine which
  branch fired, what service and binary path were involved, whether the payload path is a
  standard service-binary location or a user-writable directory, and whether the change was
  authorized.
type: detection
detection_id: 38879043-7e1e-47a9-8d46-6bec88e201df
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects — an sc.exe service reconfiguration
    or a reg.exe modification of a service registry value?
  context: Confirm the alert is a true positive before investing time. The rule fires when the
    command line matches EITHER an sc.exe selection (sc config with binpath=, or sc failure with
    command=) OR a reg add against a service registry value (FailureCommand or ImagePath) that
    carries an executable extension. Read the exact command line and note which branch fired —
    an sc.exe reconfiguration and a reg.exe registry edit reach the same persistence outcome
    through different surfaces, and the branch determines whether downstream service-event or
    registry questions carry the evidence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to reconfigure existing Windows services or edit
    service registry values?
  context: Reconfiguring a service binary path or failure command, or editing a service's
    ImagePath/FailureCommand registry value, requires Administrator or SYSTEM privileges and is
    normally done by a small set of administrators or by deployment tooling. Knowing whether this
    account and host routinely manage services is often the fastest way to separate routine
    maintenance from operator-driven persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for this
    service reconfiguration?
  context: Many organizations require change records for service modifications. A corresponding
    ticket or maintenance window suggests authorized administrative work; absence of one when
    policy requires it shifts the disposition toward unauthorized persistence. This check can
    quickly close a false positive or escalate a true positive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:service_persistence.service_configuration
  question: What service was created or modified, and what binary path does it now execute?
  context: System service-control events provide structured service fields instead of parsed
    command-line arguments. The image path is the persistence payload — attackers point a service
    at a binary in a user-writable directory such as C:\Users\Public or C:\Windows\Temp, or
    masquerade the path as a legitimate service binary. Low-use services such as Fax, W32Time,
    AppMgmt, and RemoteRegistry are common hijack targets because reconfiguring them rarely
    disrupts operations. Check the image path, start type, and the account the service runs under;
    LocalSystem grants full privileges and auto-start persists across reboots.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 7045
      condition: selection
    fields:
      - ServiceName
      - ImagePath
      - StartType
      - AccountName
- source: attack:T1574.011.registry_modification
  question: Were service registry values under HKLM\System\CurrentControlSet\Services modified
    on this host around the alert?
  context: When the reg.exe branch fires, the persistence is written directly to a service's
    registry value rather than through the service-control manager. Modifications to ImagePath,
    FailureCommand, ServiceDll, or FailureActions under
    HKLM\System\CurrentControlSet\Services redirect what the service executes. Registry-set
    evidence captures these edits even when the value is changed by tooling other than reg.exe,
    and reveals the new value pointing at the payload path. Watch for image paths in uncommon
    directories such as C:\Users or C:\Temp, and for .sys driver paths, which indicate
    driver-based execution rather than a normal service binary.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\System\CurrentControlSet\Services\'
      selection_values:
        TargetObject|endswith:
          - '\ImagePath'
          - '\FailureCommand'
          - '\ServiceDll'
          - '\FailureActions'
      condition: selection and selection_values
    fields:
      - TargetObject
      - Details
      - Image
- source: template:service_persistence.service_current_status
  question: Is the modified service currently running, and what account is it using?
  context: A reconfigured service may not have started yet (demand start) or may have been
    stopped after the change. Checking the current status reveals whether the persistence is
    active right now. A service running as LocalSystem has full system privileges; a service
    running under a specific account indicates the access level the change runs with. The
    analyst should run sc query and sc qc against the affected service name from the alert.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this command, and is that parent chain expected for service
    management?
  context: The parent process reveals how the service-tampering command was invoked. Service
    reconfiguration driven by deployment tooling or an interactive administrator session has
    predictable parents; a command interpreter spawned by a scripting host, an Office
    application, or a freshly written binary points toward a non-administrative origin. Walk up
    the tree to see what spawned the parent and what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users
    normally drive that activity, and does the current alert fit?
  context: Even routine service maintenance can resemble tampering. This query mirrors the
    rule's selection structure scoped to this host, so the resulting parent and user
    distribution reflects how this specific firing pattern has historically run here — not
    generic command usage. A new parent process, an unexpected account, or an unusual
    command-line structure against that baseline is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - CommandLine|contains|all:
        - 'sc '
        - 'config '
        - binpath=
      - CommandLine|contains|all:
        - 'sc '
        - failure
        - command=
      selection_reg_img:
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - FailureCommand
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - ImagePath
      selection_reg_ext:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin$
        - .bat
        - .cmd
        - .js
        - .msh$
        - .reg$
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection_sc or all of selection_reg_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host
    distribution look like consistent automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the result
    is a fleet-wide count of the tampering pattern rather than generic command usage. Many hosts
    firing the same pattern points to scripted deployment or vendor automation reconfiguring
    services at scale; one host or a small unrelated set points to manual operator activity. Use
    the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - CommandLine|contains|all:
        - 'sc '
        - 'config '
        - binpath=
      - CommandLine|contains|all:
        - 'sc '
        - failure
        - command=
      selection_reg_img:
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - FailureCommand
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - ImagePath
      selection_reg_ext:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin$
        - .bat
        - .cmd
        - .js
        - .msh$
        - .reg$
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
      condition: selection_sc or all of selection_reg_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - CommandLine|contains|all:
        - 'sc '
        - 'config '
        - binpath=
      - CommandLine|contains|all:
        - 'sc '
        - failure
        - command=
      selection_reg_img:
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - FailureCommand
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - ImagePath
      selection_reg_ext:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin$
        - .bat
        - .cmd
        - .js
        - .msh$
        - .reg$
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
      _pb_scope:
        User|expand: '%User%'
      condition: (selection_sc or all of selection_reg_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - CommandLine|contains|all:
        - 'sc '
        - 'config '
        - binpath=
      - CommandLine|contains|all:
        - 'sc '
        - failure
        - command=
      selection_reg_img:
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - FailureCommand
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - ImagePath
      selection_reg_ext:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin$
        - .bat
        - .cmd
        - .js
        - .msh$
        - .reg$
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_sc or all of selection_reg_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the account's activity across all hosts reveals lateral movement or parallel
    activity on multiple endpoints. If the same user triggered service changes or other
    hands-on activity on several hosts in the window, the scope is larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - CommandLine|contains|all:
        - 'sc '
        - 'config '
        - binpath=
      - CommandLine|contains|all:
        - 'sc '
        - failure
        - command=
      selection_reg_img:
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - FailureCommand
      - CommandLine|contains|all:
        - 'reg '
        - 'add '
        - ImagePath
      selection_reg_ext:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin$
        - .bat
        - .cmd
        - .js
        - .msh$
        - .reg$
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection_sc or all of selection_reg_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what
    led to the service change and what followed — reconnaissance, privilege escalation, payload
    drops, or additional persistence. Novel parent-child pairs stand out against the repetitive
    agent telemetry that dominates a busy host.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, or
    credential access — may reveal the stages that preceded the service change. Service
    tampering for persistence typically follows initial access and privilege escalation, so a
    preceding alert timeline often reconstructs how the operator reached the point of modifying a
    service.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After hijacking a service,
    they commonly install backup footholds using different techniques — scheduled tasks, other
    service changes, registry Run keys, BITS jobs, or WMI subscriptions. Several persistence
    installations within the same window indicate intentional stacking rather than an isolated
    administrative action, and the correlated timing distinguishes a campaign from routine work.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to persistence locations such as the Startup folder or Run key
    paths on this host?
  context: The persistence payload binary must be written to disk before the service is pointed
    at it, and operators often establish parallel file-based persistence that bypasses
    command-line detection entirely. Dropping an executable into a Startup folder or a path
    referenced by a registry Run key achieves persistence without running sc or reg. File event
    monitoring captures these drops even when standard persistence tools are avoided.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1543.003
      name: Windows Service
      role: primary
    - id: T1574.011
      name: Services Registry Permissions Weakness
      role: secondary
    - id: T1543
      name: Create or Modify System Process
      role: secondary
    - id: T1574
      name: Hijack Execution Flow
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: service_reconfiguration_command
      description: "Actor runs sc.exe to set a service binPath= or failure command=, or reg add to modify a service ImagePath/FailureCommand registry value"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: service_registration_or_registry_change
      description: "The reconfiguration registers a new binary path / failure command — visible as a service-control event (7045) or a registry-value modification under CurrentControlSet\\Services"
      mapped_to: Q4
      source: template:service_persistence.service_configuration
    - stage: 3
      label: registry_value_modification
      description: "ImagePath/FailureCommand/ServiceDll/FailureActions value set to point at the payload path (reg branch and SCM-driven changes alike)"
      mapped_to: Q5
      source: attack:T1574.011.registry_modification
    - stage: 4
      label: service_execution_under_privilege
      description: "Service starts (or fails) and executes the payload under the service account, persisting across reboots when start=auto"
      mapped_to: Q6
      source: template:service_persistence.service_current_status
  dimension_coverage:
    - dimension: tuning_parameter
      technique: T1543.003
      covered_by: Q4
      source: template:service_persistence.service_configuration
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1543
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1574.011
      covered_by: Q16
      source: template:persistence_scope.process_based_persistence
      coverage_type: direct
    - dimension: file_path_analysis
      technique: T1574
      covered_by: Q4
      source: template:service_persistence.service_configuration
      coverage_type: direct
  mutable_dispositions:
    - element: ServiceNamePattern
      dimension: tuning_parameter
      specific_values: "rationale only (regex examples: svhostx, winhelp)"
      disposition: context
      target_question: Q4
      notes: "Low-use / uncommon service names (Fax, W32Time, AppMgmt, RemoteRegistry) named in Q4 context as common hijack targets; no fixed value to inject."
    - element: ImagePathFilter
      dimension: tuning_parameter
      specific_values: "C:\\Users\\, C:\\Temp\\"
      disposition: context
      target_question: Q4
      notes: "User-writable image-path locations (C:\\Users\\Public, C:\\Windows\\Temp) called out in Q4 and Q5 context as the suspicious payload-path signal."
    - element: DriverExtensionList
      dimension: tuning_parameter
      specific_values: ".sys"
      disposition: context
      target_question: Q5
      notes: ".sys driver-path indicator recorded in Q5 registry context as a driver-based execution signal."
    - element: StartupTypeChangeWindow
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Start type / auto-start persistence across reboots described in Q4 context (StartType field projected)."
    - element: UnsignedBinaryAlert
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "ImagePath surfaced in Q4 for signing/reputation review; no directly queryable signing field in the structured service event."
    - element: ParentProcessFilter
      dimension: parent_process_analysis
      specific_values: "rationale only (non-administrative processes starting service tools)"
      disposition: context
      target_question: Q7
      notes: "Q7 (process_chain) surfaces ParentImage so the analyst can judge whether the parent is a non-administrative origin."
    - element: RegistryPathList
      dimension: tuning_parameter
      specific_values: "HKLM\\System\\CurrentControlSet\\Services"
      disposition: query_injection
      target_question: Q5
      notes: "Injected as TargetObject|contains '\\System\\CurrentControlSet\\Services\\' in the registry_set question."
    - element: MonitoredServiceKeys
      dimension: tuning_parameter
      specific_values: "ImagePath, ServiceDll, FailureCommand, FailureActions"
      disposition: query_injection
      target_question: Q5
      notes: "Injected as TargetObject|endswith value list in the registry_set question."
    - element: BaselineServiceConfig
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Comparison-to-known-good framing carried in Q4/Q5 context; no fixed baseline value to inject."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (registry/service-mod to execution interval)"
      disposition: context
      target_question: Q16
      notes: "Correlation window realized as the +/-4h range on the process-based-persistence question and +/-15m host clustering."
    - element: ServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Expected service keys/paths comparison framing in Q5 context."
    - element: AllowedDllPaths
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "ServiceDll abnormal-load-path framing carried in Q5 context (T1574 image_load not queried — see hint_dispositions)."
    - element: PrivilegedAccounts
      dimension: tuning_parameter
      specific_values: "rationale only (accounts permitted to modify service config)"
      disposition: context
      target_question: Q2
      notes: "Privilege/role expectation handled by host_user_context (Q2) and the user baseline (Q10)."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Overlaps cue_host_context.clustering (Q14) — both aggregate process_creation around the alert window; host_context version (Image+ParentImage) is retained as the broader-context view."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "Plain host/binary baseline subsumed by cue_process_tool.baseline_deviation (Q8), which scopes to the rule's actual firing pattern instead of generic binary usage."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_user_actor.baseline (user-pivot) paired with cue_user_actor.baseline_svc; cue_user_actor.clustering paired with cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both identity-pivot and parent+binary substitute emitted per AGENTS.md."
      kept: both variants (Q10+Q11, Q12+Q13)
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: service_persistence.service_configuration (acted on hint 2). Service-creation/modification evidence is better read from the structured System service event (Q4) than from re-decoding the cmd.exe command line."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED tier, dropped under notes_to_llm condition (b): cue_host_context.precursor (Q15) is present AND this is a persistence rule, not an execution/credential-access rule, so prior-alert context already carries any tamper-as-precursor signal. defense_tampering technique set is not confirmed for this rule. Dropped to stay within the 18-question cap."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1543.003) is not covered and no template family exists — author a custom question with category: security"
      disposition: declined
      reason: >-
        The security-log evidence this hint targets is service-installation /
        service-modification events (EventCode 4697/7045). Q4
        (service_persistence.service_configuration) already retrieves that
        structured service-install evidence via the Windows System service
        event (EventID 7045, the SCM-side record of a service binary path /
        account change), and Q5 (attack:T1574.011.registry_modification) covers
        the registry-side modification. A separate category: security question
        would duplicate Q4's service-install coverage without adding a distinct
        analytic angle, so no custom security question was authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - service_persistence
    - organizational_context
    - persistence_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 13
      registry_set: 1
      system_service: 1
      sigma_alert: 1
      file_event: 1
