name: Process Creation Using Sysnative Folder
id: fdc9e99f-d160-4c92-afc4-06f7c4ec85cb
description: |
  Detects a process whose Image path or command line references the Sysnative
  folder (:\Windows\Sysnative\), excluding known-legitimate ngen install and a
  specific XAMPP startup pattern. Sysnative is the file-system redirector alias
  that lets a 32-bit process reach the native 64-bit System32 directory, and the
  64-bit dllhost.exe under this path is a default spawn target used by injection
  frameworks to host injected code. Legitimate uses exist (some installers and
  WOW64 tooling reach native binaries through Sysnative), so the investigation
  should confirm the exact path and command, identify the parent that launched
  it, and pivot to the surrounding process-access and image-load evidence that
  would confirm or rule out process injection into the spawned host.
type: detection
detection_id: 3c1b5fb0-c72f-45ba-abd1-4d4c353144ab
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the Image path or command line reference the Sysnative folder as this rule detects?
  context: Confirm the alert is a true positive by reviewing the exact Image path and
    command line against the rule's criteria. The rule fires when either the Image
    path or the command line contains ':\Windows\Sysnative\', and it excludes ngen
    install activity from the Microsoft.NET framework directories and a specific XAMPP
    catalina startup. Note which field carried the Sysnative reference and the full
    path, because a 64-bit dllhost.exe under Sysnative is a common injection spawn
    target while a 32-bit tool reaching a native binary may be benign.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run processes that reach the Sysnative path?
  context: Legitimate administrative tooling and some installers reference native
    binaries through Sysnative, so knowing whether the user role and host function
    fit this behavior is often the fastest way to separate routine operations from
    abuse. A workstation user with no admin or build responsibilities reaching the
    native System32 path through a 32-bit parent is a stronger anomaly than the same
    activity on a build or management server.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the Sysnative process do? Decode its full command line and working directory.
  context: The full command line reveals the specific action taken through the
    Sysnative path. Decode any flags, encoded content, file paths, or child-command
    references, and identify whether the spawned binary is dllhost.exe, cmd.exe,
    rundll32.exe, or another host process. The native dllhost.exe under Sysnative is
    a default spawn target for injection; an empty or trivial command line on such a
    host process is itself notable because injected code runs in memory rather than
    via arguments.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: ':\Windows\Sysnative\'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this Sysnative process, and is that parent chain expected?
  context: The parent process reveals how the Sysnative reference was invoked.
    Legitimate usage often has predictable parents (installers, explorer.exe for
    interactive actions). Injection frameworks frequently spawn the native
    dllhost.exe from an already-compromised process such as rundll32.exe or a
    scripting host; an unexpected parent is a strong pivot point. The immediate
    parent is visible in the alert, so extend the investigation to what spawned the
    parent and what else the parent launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1055.access_verification
  question: Did any process on this host open a handle to another process with injection-class access rights around the alert time?
  context: Process injection requires acquiring a handle to the target process with
    rights that permit writing memory and creating threads, such as PROCESS_VM_WRITE,
    PROCESS_VM_OPERATION, and PROCESS_CREATE_THREAD (granted-access masks like
    0x1F0FFF or 0x1FFFFF cover the full set). Correlating a process-access event with
    these rights against the Sysnative spawn confirms whether code was injected into
    the hosted process rather than merely launched. Zero results may mean process-access
    telemetry is not collected in this environment rather than that no injection occurred.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      filter_rights:
        GrantedAccess:
          - '0x1F0FFF'
          - '0x1FFFFF'
          - '0x1F3FFF'
          - '0x143A'
          - '0x1438'
          - '0x0800'
          - '0x0820'
      condition: selection and filter_rights
    fields:
      - SourceImage
      - TargetImage
      - GrantedAccess
      - CallTrace
- source: attack:T1055.image_load
  question: What modules loaded into the Sysnative-hosted process, and were any unusual or unsigned DLLs loaded into it?
  context: Injection frequently manifests as an unexpected DLL loaded into the target
    process's address space, or as the hosted process loading modules inconsistent
    with its normal behavior. The native dllhost.exe normally loads a stable set of
    COM and system modules; an unsigned module, a DLL from a user-writable directory,
    or a load that does not match the host process's baseline points toward injected
    or sideloaded code. If image-load telemetry is not collected in this environment,
    treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|contains: ':\Windows\Sysnative\'
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_process_tool.follow_on
  question: What child processes did the Sysnative process spawn afterward?
  context: The processes spawned by the alerting process reveal its actual impact.
    A host process under Sysnative that goes on to launch cmd.exe, powershell.exe,
    rundll32.exe, or reconnaissance binaries suggests hands-on-keyboard activity or
    post-injection execution. A process that spawns nothing further may be a benign
    one-shot reference to a native binary.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|contains|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run processes that reference the Sysnative path?
  context: A path reference that has never been seen on a host is more suspicious than
    one that recurs. Zero or very low historical counts over the trailing period
    indicate first-time or rare use of the Sysnative redirector here, which raises the
    priority of confirming the injection evidence.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: ':\Windows\Sysnative\'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's Sysnative pattern, what parents and users normally drive that activity, and does the current alert fit?
  context: Even legitimate Sysnative use recurs with a stable signature. The query
    mirrors the rule's selection structure scoped to this host, so the resulting
    parent and user distribution reflects how the rule's specific firing pattern has
    historically run here. A new parent process, an unexpected user account, or an
    unusual command-line shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains: :\Windows\Sysnative\
      - Image|contains: :\Windows\Sysnative\
      filter_main_ngen:
        Image|contains:
        - C:\Windows\Microsoft.NET\Framework64\v
        - C:\Windows\Microsoft.NET\Framework\v
        - C:\Windows\Microsoft.NET\FrameworkArm\v
        - C:\Windows\Microsoft.NET\FrameworkArm64\v
        Image|endswith: \ngen.exe
        CommandLine|contains: install
      filter_optional_xampp:
        CommandLine|contains|all:
        - '"C:\Windows\sysnative\cmd.exe"'
        - \xampp\
        - \catalina_start.bat
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run processes that reach the Sysnative path?
  context: Compare this user's current action against their historical behavior. A
    user who has never invoked the Sysnative redirector is more suspicious than one
    who does so as part of a routine build or administrative workflow. User role and
    typical toolset should align with reaching native System32 binaries.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: ':\Windows\Sysnative\'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not
    attribute behavior; the meaningful pivot is the launch mechanism. A new or rare
    parent for the Sysnative-spawned binary on this host (or environment-wide) is the
    deviation worth chasing, while a pair that already accounts for many prior events
    is more likely vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|contains|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or
    activity spanning multiple endpoints. If the same user triggered similar activity
    on more than one host, the scope of the event may be larger than a single
    detection.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+binary combination fire on other hosts in the same window?
  context: Under a service principal, asking what else the user did across the network
    returns agent-telemetry noise because every host runs system-owned agents
    constantly. The meaningful question is whether the same launch mechanism — parent
    process plus spawned binary — fired on other hosts in the alert window. Multiple
    hosts in the same window points to scheduled or pushed automation; a single host
    points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|contains|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.clustering
  question: What other tools ran on this host around the same time, within the injection correlation window?
  context: Injection detection depends on correlating the spawn with nearby memory
    and thread activity, so activity in a tight window around the alert provides the
    context. Reconnaissance, credential access, or staging tools running alongside the
    Sysnative process broaden the picture and help confirm whether this is a single
    benign reference or one stage of a multi-step operation. A short correlation window
    of roughly fifteen minutes keeps unrelated background activity out of the result.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host —
    defense tampering, reconnaissance, or credential access — may reveal the stages
    that preceded this event and help reconstruct the timeline. Injection is often a
    mid-chain action, so a preceding execution or download alert helps explain how the
    Sysnative spawn target came to be used.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's Sysnative pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the
    result is a fleet-wide count of the rule's firing pattern rather than generic use
    of any binary. Many hosts firing the same pattern points to scripted deployment or
    vendor automation; one host or a small unrelated set points to manual operator
    activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains: :\Windows\Sysnative\
      - Image|contains: :\Windows\Sysnative\
      filter_main_ngen:
        Image|contains:
        - C:\Windows\Microsoft.NET\Framework64\v
        - C:\Windows\Microsoft.NET\Framework\v
        - C:\Windows\Microsoft.NET\FrameworkArm\v
        - C:\Windows\Microsoft.NET\FrameworkArm64\v
        Image|endswith: \ngen.exe
        CommandLine|contains: install
      filter_optional_xampp:
        CommandLine|contains|all:
        - '"C:\Windows\sysnative\cmd.exe"'
        - \xampp\
        - \catalina_start.bat
      condition: selection and not 1 of filter_main_* and not 1 of filter_optional_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1055
      name: Process Injection
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: handle_acquisition
      description: "Actor opens a handle to the target process with injection-class access rights (e.g., PROCESS_VM_WRITE, PROCESS_CREATE_THREAD)"
      mapped_to: Q5
      source: attack:T1055.access_verification
    - stage: 2
      label: memory_write_and_module_load
      description: "Memory is written and/or an unusual DLL is loaded into the target process's address space (CreateRemoteThread / unexpected image load)"
      mapped_to: Q6
      source: attack:T1055.image_load
    - stage: 3
      label: time_correlation
      description: "The handle acquisition, memory write/thread creation, and module load occur within a tight time window around the spawn"
      mapped_to: Q14
      source: template:cue_process_tool.clustering
  dimension_coverage:
    - dimension: access_verification
      technique: T1055
      covered_by: Q5
      source: attack:T1055.access_verification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1055
      covered_by: Q14
      source: template:cue_process_tool.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1055
      covered_by: Q6
      source: attack:T1055.image_load
      coverage_type: direct
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "PROCESS_VM_WRITE / PROCESS_VM_OPERATION / PROCESS_CREATE_THREAD masks; 0x1F0FFF, 0x1FFFFF, 0x1F3FFF, 0x143A, 0x1438, 0x0800, 0x0820"
      disposition: new_question
      target_question: Q5
      notes: "Injected as GrantedAccess filter list in the attack:T1055.access_verification process_access query; full set 0x1F0FFF/0x1FFFFF and VM-write/create-thread masks recorded in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window, no fixed value in analytic)"
      disposition: context
      target_question: Q14
      notes: "Threshold not directly queryable; +/-15m correlation window applied to clustering and process_access/image_load queries and stated in context."
    - element: InjectedProcessList
      dimension: tuning_parameter
      specific_values: "lsass.exe, explorer.exe (high-value injection targets)"
      disposition: context
      target_question: Q6
      notes: "Sysnative dllhost.exe is the rule's spawn target; the image_load question inspects modules loaded into the Sysnative-hosted process and the access_verification question's TargetImage surfaces high-value targets. Specific target names recorded in context rather than hard-filtered, since the rule's anchor is the Sysnative host process not a fixed target list."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "kept — parent cue is NOT subsumed; has_parent_in_detection is false, so no parent_process technique set is active and process_chain is the only parent-walk question."
      kept: cue_process_tool.process_chain
    - dropped: none
      reason: "user_actor pair (cue_user_actor.baseline + baseline_svc, cue_user_actor.clustering + clustering_svc) — non-deduplicable; both variants emitted per service-principal rule."
      kept: cue_user_actor.baseline_svc / cue_user_actor.clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "Dropped (recommended-tier). This rule is a process-injection proxy detection, not itself an execution/credential-access rule whose disposition turns on whether protection was weakened first; the generic host-tamper sweep does not earn its slot here. The cue_host_context.precursor question already surfaces prior tamper/defense-evasion alerts on the host in the -6h window, which carries the precursor-tamper signal for this non-execution-centric rule. Recorded here per the universal-question no-silent-drop rule."
  reflection_overrides: []
  hint_dispositions:
    - hint: "dimension 'access_verification' (techniques: T1055) is uncovered — author a custom attack:T1055.access_verification question"
      disposition: acted_on_noted
      reason: >-
        Acted on: authored Q5 (attack:T1055.access_verification) — a process_access
        query scoped to the host with a GrantedAccess filter for injection-class
        masks. Listed here for traceability; the action is visible in Q5, not a
        decline.
    - hint: "logsource 'image_load' (recommended by techniques: T1055) is not covered — author a custom question with category: image_load"
      disposition: acted_on_noted
      reason: >-
        Acted on: authored Q6 (attack:T1055.image_load) with category: image_load,
        inspecting modules loaded into the Sysnative-hosted process. Listed here for
        traceability; the action is visible in Q6, not a decline.
    - hint: "logsource 'process_access' (recommended by techniques: T1055) is not covered and no template family exists — author a custom question with category: process_access"
      disposition: acted_on_noted
      reason: >-
        Acted on: Q5 (attack:T1055.access_verification) uses category: process_access
        with SourceImage/TargetImage/GrantedAccess/CallTrace fields. Listed here for
        traceability; the action is visible in Q5, not a decline.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      process_access: 1
      image_load: 1
      sigma_alert: 1
