name: Suspicious Csi.exe Usage
id: 79c432b5-48a4-4c92-8d23-ec5e3a78cfa8
description: |
  Detects execution of csi.exe or rcsi.exe, the Microsoft-signed Roslyn C# interactive and
  script-runner binaries that ship with Visual Studio. The rule fires when the Image ends in
  \csi.exe or \rcsi.exe (or the OriginalFileName is csi.exe/rcsi.exe) AND the Company is
  Microsoft Corporation. Legitimate use is local C# development by software engineers. Because
  these binaries are Microsoft-signed and compile-and-run arbitrary C# from a .csx/.cs file at
  runtime, they are abused as a living-off-the-land path to execute unsigned code and bypass
  application allowlisting and code-integrity controls. Investigation should determine what
  source file or C# was run, what parent invoked the binary, whether the user and host are
  expected to perform C# development, and what executed before and after.
type: detection
detection_id: 40b95d31-1afc-469e-8d34-9a3a667d058e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the process match this rule — a Microsoft-signed csi.exe or rcsi.exe execution?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing
    the Image, OriginalFileName, and Company against the rule's criteria. The rule fires only
    when the binary resolves to csi.exe/rcsi.exe AND the Company is Microsoft Corporation, so a
    non-Microsoft signer or a renamed binary would not match.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Company
- source: template:universal.host_user_context
  question: Is this user and host expected to run C# interactive or script-runner tooling?
  context: Legitimate csi.exe/rcsi.exe use is software development work. Knowing whether the user
    role is a developer and whether the host is a development workstation is often the fastest path
    to distinguishing routine activity from a living-off-the-land bypass on a machine that has no
    business compiling C# at runtime.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What source file or C# did this binary run? Decode the full command line.
  context: The full command line reveals what was executed. csi.exe runs C# interactively or from
    a file argument; rcsi.exe runs a .csx script file. Decode the file path passed as an argument,
    any inline C#, and any URLs or script paths — these indicate whether arbitrary unsigned code
    was compiled and run, and where the source came from. A .csx or .cs path in a user-writable
    or temporary directory is a stronger signal than a project file under a developer's workspace.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1218.assembly_load
  question: What modules and assemblies did this binary load, and were any from user-writable or unsigned locations?
  context: csi.exe and rcsi.exe compile and run C# in-process, loading the Roslyn compiler and any
    assemblies the script references. A documented abuse pattern transports the Roslyn runner plus
    its dependency DLLs to a protected host (bring-your-own-LOLBin), so modules loaded from
    user-writable, temporary, or otherwise non-standard directories — rather than the Visual Studio
    install path — point to relocated or attacker-supplied components rather than a normal install.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1072.deployment_tool_context
  question: Do the deployment or management logs show a software-deployment job that pushed or launched this binary?
  context: Software deployment and remote-management tooling can legitimately push and run binaries
    from system context, and the same channel is abused to fan execution out across hosts. Checking
    application-channel deployment logs around the alert time shows whether a deployment job,
    scheduled push, or management console initiated this csi.exe/rcsi.exe execution, or whether it
    ran interactively without any deployment record behind it.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: application
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Provider
      - EventID
      - Message
- source: template:cue_process_tool.process_chain
  question: What process spawned this binary and is that parent chain expected?
  context: The parent process reveals how the binary was invoked. A developer-driven run usually
    descends from a shell, IDE, or build process under explorer.exe. Unusual parents such as
    mshta.exe, wscript.exe, Office applications, or a remote-management or deployment service
    suggest the signed binary is being used as a proxy-execution stage rather than for development.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this binary spawn afterward?
  context: The processes spawned by the C# runner reveal its actual impact. Development use rarely
    spawns interactive children; a run that launches cmd.exe, powershell.exe, certutil.exe, or
    other tooling indicates the compiled C# is being used as a launcher for post-exploitation
    rather than for legitimate compilation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run csi.exe or rcsi.exe?
  context: A binary that has never been seen on a host is more notable than one that runs regularly.
    Zero or very low historical counts over the past 90 days indicate first-time or rare execution
    of the C# runner on this host, which is harder to explain as routine development.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even legitimate developer tooling has a stable shape on a given host. The query mirrors
    the rule's selection scoped to this host, so the resulting parent and user distribution reflects
    how the rule's firing pattern — Microsoft-signed csi.exe/rcsi.exe — has historically run here. A
    new parent process, an unexpected user account, or an unusual command-line shape is the anomaly
    worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \csi.exe
        - \rcsi.exe
      - OriginalFileName:
        - csi.exe
        - rcsi.exe
      selection_cli:
        Company: Microsoft Corporation
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of
    the rule's firing pattern rather than generic binary usage. Many hosts firing the same pattern
    points to scripted deployment or vendor automation; one host or a small unrelated set points to
    manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \csi.exe
        - \rcsi.exe
      - OriginalFileName:
        - csi.exe
        - rcsi.exe
      selection_cli:
        Company: Microsoft Corporation
      condition: all of selection*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run C# interactive or script-runner tooling?
  context: Compare this user's current action against their historical behavior over the past 90
    days. A user who has never run csi.exe/rcsi.exe is more notable than a developer who does so
    regularly. The user's role and typical toolset should align with runtime C# compilation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism — which parent process spawned the C# runner, and
    whether that pair is historically common. A new or rare parent for this binary is the deviation
    worth chasing; a pair that already accounts for thousands of prior events is vendor or operating
    system automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals movement or activity across
    multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope of
    the activity may be larger than a single workstation.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and binary combination fire on other hosts in the same window?
  context: Under a service principal, asking what else the user did across the network returns
    agent-telemetry noise, since every host runs system-owned agents constantly. The meaningful
    question is whether the same launch mechanism — parent process plus spawned binary — fired on
    other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity in the window around the alert
    reveals the immediate context — what led to this execution and what happened after — and whether
    it is part of a larger sequence. Unusual timing, such as a deployment outside standard
    maintenance hours, is itself a signal worth noting.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense
    tampering, reconnaissance, or credential access — may reveal the attack stages that preceded
    this execution and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: A signed binary that compiles and runs arbitrary C# is a plausible payload stage, and
    operators often weaken protection before running one. The query surfaces direct attempts to
    disable AV or EDR services, flip Defender flags, add broad exclusions, clear logs, or patch
    AMSI/ETW. Zero rows is a useful "protection looks healthy" signal — kernel-level driver tampering
    and in-memory patches will not appear here. Populated rows shift the disposition toward operator
    preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:lateral_movement.target_activity_after_access
  question: What processes were spawned on this host shortly after the binary executed?
  context: T1072 abuse moves execution onto a target host as a means to an end — to run commands,
    deploy tools, or access data. Examining processes spawned shortly after the C# runner executed
    reveals what the activity was actually for on this host, independent of whether the launch came
    from a deployment channel or an interactive session.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - Image
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
    - id: T1072
      name: Software Deployment Tools
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: trusted_signed_binary_execution
      description: "Execution of a trusted Microsoft-signed binary (csi.exe/rcsi.exe) — confirmed as the rule's trigger"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: unsigned_payload_via_cli_or_assembly
      description: "The signed binary compiles and runs externally-hosted/unsigned C# via command-line file argument or loaded assemblies"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2b
      label: assembly_load
      description: "Roslyn compiler and referenced/dependency assemblies loaded in-process (BYO-DLL pattern)"
      mapped_to: Q4
      source: attack:T1218.assembly_load
    - stage: 3
      label: deployment_tool_initiation
      description: "T1072: deployment/management tooling pushes or launches scripts/binaries onto the host"
      mapped_to: Q5
      source: attack:T1072.deployment_tool_context
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q10
      source: template:cue_process_tool.scope
      coverage_type: indirect
    - dimension: time_correlation
      technique: T1072
      covered_by: Q15
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: user_context
      technique: T1072
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (e.g. regsvr32.exe not launched by explorer.exe)"
      disposition: context
      target_question: Q6
      notes: "process_chain question evaluates parent legitimacy for csi.exe/rcsi.exe; no fixed parent-allowlist values in brief"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (msiexec.exe, regsvr32.exe — not the rule's binary)"
      disposition: context
      target_question: Q10
      notes: "Allowlist concept; the rule already constrains to csi.exe/rcsi.exe. scope question gives fleet distribution of the rule's pattern"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (URLs, script paths, .csx/.cs entrypoints)"
      disposition: context
      target_question: Q3
      notes: "mechanism_decode (KEEP-intent structural) directs analyst to decode file path / inline C# / URLs; commandline_values empty in brief so no concrete |contains values to inject"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Suppression filter for enterprise domains; surfaced as context in deployment-tool question, no queryable values"
    - element: ParentImageList
      dimension: tuning_parameter
      specific_values: "rationale only (e.g. CCMExec.exe)"
      disposition: context
      target_question: Q5
      notes: "SCCM-spawner allowlist concept; deployment-tool question asks whether a deployment job launched the binary"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (scheduled system accounts)"
      disposition: context
      target_question: Q2
      notes: "Covered by host_user_context (developer expectation) and the service-principal baseline/clustering pair"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (outside maintenance hours)"
      disposition: context
      target_question: Q15
      notes: "host_context.clustering surfaces surrounding activity timing; threshold is not directly queryable"
  dedup_decisions:
    - dropped: lateral_movement.source_host_identification
      reason: "Rule has no remote-connection detection — it fires on local process_creation of a signed C# runner. A source-host / inbound-port question on SMB/RDP/WinRM ports queries a network_connection signal this rule does not produce; the relevant scope is covered topically by cue_user_actor.clustering (user network-wide) and cue_user_actor.clustering_svc (parent+binary across hosts)."
      kept: cue_user_actor.clustering_svc
    - dropped: lateral_movement.authentication_method
      reason: "Authentication-method analysis (pass-the-hash / Kerberos) presumes a remote logon event tied to the execution. This rule detects local LOLBin execution with no associated authentication artifact in scope; no 4624/4648 signal is implied by the detection. Host/user expectation is covered by host_user_context and the user_actor baseline pair."
      kept: cue_user_actor.baseline
    - dropped: lateral_movement.lateral_movement_scope
      reason: "Filters to psexec/mstsc/ssh/plink/wmic lateral tools, none of which is this rule's binary; the rule fires on csi.exe/rcsi.exe. User-wide and parent+binary fleet scope are covered by cue_user_actor.clustering and cue_user_actor.clustering_svc, and fleet pattern distribution by cue_process_tool.scope."
      kept: cue_process_tool.scope
    - dropped: network_scope.same_action_other_hosts
      reason: "Cross-host count of the same action is provided by cue_process_tool.scope (REFLECTED fleet_pattern), which enforces the rule's full selection (Image/OriginalFileName + Company) rather than Image-only — strictly more precise than this Image|endswith template for this rule."
      kept: cue_process_tool.scope
    - dropped: network_scope.actor_activity_network_wide
      reason: "User network-wide activity is covered by cue_user_actor.clustering (always_include, same +/-1h window, same User pivot); this network_scope template is a near-duplicate of it."
      kept: cue_user_actor.clustering
    - dropped: network_scope.actor_authentication_trail
      reason: "Queries Security-log 4624/4625 logon trail; this rule detects local LOLBin execution with no associated authentication event in scope. Same out-of-scope rationale as lateral_movement.authentication_method — no logon artifact is implied by the detection."
      kept: cue_user_actor.clustering
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - lateral_movement
    - network_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      image_load: 1
      application: 1
      sigma_alert: 1
