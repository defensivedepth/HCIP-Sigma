name: Harvesting Of Wifi Credentials Via Netsh.EXE
id: 11b54ef3-fbc1-431f-adb7-22877c8cb33d
description: |
  Detects netsh.exe (by Image name or OriginalFileName) invoked with a command
  line that contains all of wlan, the show-profile switches, and =clear - the
  pattern produced by "netsh wlan show profile NAME key=clear", which prints the
  stored wireless network key in plaintext. The rule fires only when every one of
  these tokens is present together, so it targets the credential-disclosure form
  of the command rather than a benign profile listing. Reading stored WiFi keys is
  occasionally done by IT staff recovering a network password, but it is also a
  routine credential-harvest step in commodity infostealers, which enumerate every
  saved profile and dump each plaintext key. The investigation should determine
  which binary ran the command, which profiles and keys were exposed, what parent
  launched it, whether the same account or host shows other discovery or
  credential-access activity, and whether the pattern recurs across the fleet.
type: detection
detection_id: 42b1a5b8-353f-4f10-b256-39de4467faff
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line run netsh with wlan show profile and key=clear,
    matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires only when the process is netsh (by Image or OriginalFileName) and
    its command line contains all of wlan, the show-profile switches, and =clear
    together - the form that prints a stored wireless key in plaintext. Read the exact
    command line and confirm the =clear token is present, since that is what turns a
    benign profile listing into a credential disclosure.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user and host expected to read stored wireless network keys?
  context: IT and help-desk staff occasionally recover a saved WiFi password for a
    user, so the command is not unique to attackers. Standard users and most endpoints
    never run netsh wlan show profile key=clear. Whether the acting account and the
    host function involve network support or laptop provisioning is often the fastest
    way to separate routine recovery from automated credential harvesting.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in
    the hour before the alert?
  context: Credential harvesting is frequently preceded by attempts to weaken
    monitoring. The query surfaces direct attempts to disable AV/EDR services, flip
    Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is
    a useful protection-looks-healthy signal - kernel-level driver tampering and
    in-memory patches will not appear here. Populated rows shift the disposition toward
    operator preparation; review the parent process and signing chain to identify the
    writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:credential_access.credential_target_identification
  question: Which wireless profiles and keys were exposed by this command?
  context: The credential disclosure depends on what was enumerated. The plaintext form
    of the command targets a named wireless profile and prints its stored key, so read
    the full command line to identify which profile or profiles were named and whether
    a preceding listing enumerated every saved profile first. Each exposed key is a
    network credential that may need to be rotated.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.credential_exposure_assessment
  question: Which wireless networks may now be compromised, and does any provide access
    to sensitive segments?
  context: The severity of a stored-key disclosure depends on what the network reaches.
    A guest or home SSID is lower impact than a corporate wireless network that bridges
    to internal infrastructure. Cross-reference the exposed profile names against the
    environment's network inventory to decide which keys must be rotated and whether
    the disclosure warrants escalation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned this netsh command, and is that parent expected for
    wireless key retrieval?
  context: The parent reveals how the key-retrieval command was initiated. Interactive
    recovery usually shows cmd.exe or powershell.exe under explorer.exe. A parent such
    as a script host, an Office application, or an unknown binary points to automated
    harvesting, where an infostealer spawns netsh to enumerate saved keys before
    exfiltration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:lolbin.renamed_binary_check
  question: Was netsh.exe executed from its expected System32 path?
  context: netsh.exe normally runs from System32 or SysWOW64. The rule also matches on
    OriginalFileName, so a copied or renamed netsh running from a user-writable
    directory still fires. A netsh executing from a temp or profile directory is more
    consistent with a relocated binary used to evade path-based allowlists than with a
    routine support action.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:credential_access.post_access_lateral_movement
  question: Were any new logon sessions, remote connections, or follow-on detections
    seen on this host after the key was read?
  context: Harvested wireless keys are typically staged for exfiltration or reused to
    reach other networks. Follow-on alerts on the same host - outbound connections, new
    authentication, or further credential access - indicate the disclosure is being
    acted on rather than a one-off recovery. The window after the read is where staging
    and exfiltration appear.
  range: +4h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance or system-information commands run on this host
    around the same time?
  context: A single key-retrieval command may be benign support, but multiple discovery
    commands in rapid succession - whoami, ipconfig, systeminfo, net user, nslookup -
    is a signature of post-compromise enumeration. Commodity stealers commonly collect
    host and user information alongside saved credentials before assembling them for
    exfiltration.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\cmdkey.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:lolbin.technique_class_lateral
  question: Were other built-in credential or download utilities used on this host
    recently?
  context: An actor harvesting saved credentials often pairs native tools - vaultcmd
    for the Credential Manager, cmdkey for stored logons, or certutil and bitsadmin to
    stage and move the collected data. Finding several of these running close together
    strengthens the case that the wireless key read is part of a credential-collection
    sweep rather than isolated support.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\cmdkey.exe'
          - '\vaultcmd.exe'
          - '\certutil.exe'
          - '\bitsadmin.exe'
          - '\curl.exe'
          - '\rundll32.exe'
          - '\reg.exe'
          - '\esentutl.exe'
          - '\makecab.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally read stored wireless keys with netsh?
  context: Compare the acting account against its own history. A network support
    technician may run netsh wlan show profile key=clear as part of provisioning; an
    account that has never done so suddenly dumping a stored key is more notable. The
    user role and typical toolset should align with wireless or laptop support.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
          - wlan
          - ' s'
          - ' p'
          - ' k'
          - =clear
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent process and wireless-key-read command-line pattern
    historically routine on this host or across the environment, or is the parent
    unusual for it?
  context: When the alert fires under a service principal, user identity does not
    attribute the behavior; the meaningful pivot is which parent process launched a
    child that read a stored wireless key, and whether that pairing is historically
    common. A new or rare parent for this pattern is the deviation worth chasing, while
    a parent that already accounts for many prior events is more likely a management
    tool or scripted provisioning.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
          - wlan
          - ' s'
          - ' p'
          - ' k'
          - =clear
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether the
    wireless key read was a single event or part of activity spanning multiple
    endpoints. If the same user triggered similar credential or discovery activity on
    other hosts, the scope of the incident is larger than one machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent process and wireless-key-read command-line pattern
    fire on other hosts in the same window, indicating a coordinated rollout rather
    than isolated activity?
  context: Under a service principal, asking what else the user did across the network
    returns agent-telemetry noise, since every host runs system-owned agents
    constantly. The meaningful question is whether the same launch mechanism - a parent
    process spawning a child that reads a stored wireless key - fired on other hosts in
    the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
          - wlan
          - ' s'
          - ' p'
          - ' k'
          - =clear
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    wireless key read reveals what led to it and what followed - the parent that staged
    the process, reconnaissance, or follow-on collection. Novel parent-child pairs at
    low counts are what to scan for against the high-count noise of routine system
    processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - reconnaissance, defense tampering, or
    other credential access - may reveal the attack stages that preceded the wireless
    key read and help reconstruct the timeline. Credential harvesting is often one step
    in a sequence, so prior alerts give context for why the key was dumped.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's wireless-key-read pattern,
    what parents and users normally drive it, and does the current alert fit?
  context: Even legitimate support can read a stored wireless key. This query mirrors
    the rule's selection structure scoped to this host, so the resulting parent and
    user distribution reflects how this specific firing pattern has historically run
    here, not generic netsh activity. A new parent process, an unexpected account, or a
    first-ever occurrence is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \netsh.exe
      - OriginalFileName: netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - wlan
        - ' s'
        - ' p'
        - ' k'
        - =clear
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's wireless-key-read pattern in the past
    week, and does the cross-host distribution look like consistent automation or
    one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the
    result is a fleet-wide count of the wireless-key-read pattern rather than generic
    netsh activity. Many hosts firing the same pattern points to a provisioning script
    or management tool that reads keys by design; one host or a small unrelated set
    points to manual or malicious activity. Use the host distribution to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \netsh.exe
      - OriginalFileName: netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - wlan
        - ' s'
        - ' p'
        - ' k'
        - =clear
      condition: all of selection_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1040
      name: Network Sniffing
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: harvest_invocation
      description: "netsh is launched with the wlan show-profile key=clear pattern, the credential-disclosure act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: keys_exposed
      description: "Identify which wireless profiles were named and which stored keys were printed in plaintext"
      mapped_to: Q4
      source: template:credential_access.credential_target_identification
    - stage: 3
      label: invocation_origin
      description: "Identify the parent that spawned netsh to determine interactive recovery vs automated harvesting"
      mapped_to: Q6
      source: template:discovery_recon.discovery_parent_chain
    - stage: 4
      label: followon_use
      description: "Correlate post-read activity (exfil staging, new logons, follow-on alerts) with reuse of the harvested keys"
      mapped_to: Q8
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: time_correlation
      technique: T1040
      covered_by: Q8
      source: template:credential_access.post_access_lateral_movement
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1040
      covered_by: Q9
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: indirect
    - dimension: user_context
      technique: T1040
      covered_by: Q2
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ToolNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q10
      notes: >-
        The T1040 analytic's ToolNames element lists packet-capture tools (Wireshark,
        tshark, Message Analyzer) and is a tuning rationale with no concrete values that
        fit this rule. This rule detects WiFi-profile-key harvesting via netsh, not
        packet capture, so the canonical sniffing tool list does not apply. The
        analogous tuning surface here is the set of native credential/collection
        utilities an operator pairs with the key read, realized as the binary list in
        lolbin.technique_class_lateral (cmdkey, vaultcmd, certutil, bitsadmin, etc.).
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: >-
        Tuning rationale (time of day / frequency of capture sessions); no concrete
        values in the analytic. Realized as the post-read +4h correlation window in
        post_access_lateral_movement and the +/-15m burst window in
        discovery_burst_detection, which bound the temporal correlation for this rule.
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair - non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). Image-pivot tightened to the rule's CommandLine|contains|all wlan/key=clear pattern per the LOLBin/CommandLine-driven detection so the baseline counts the rule's firing pattern, not all netsh usage."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair - non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). _svc variant pivots on parent+CommandLine pattern."
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "Overlaps discovery_parent_chain (both identify the parent of the alerting process). Technique-set version preferred per within-phase dedup rule (more specific to the discovery/credential context); discovery_parent_chain projects ParentImage+ParentCommandLine of the alerting event."
      kept: template:discovery_recon.discovery_parent_chain
    - dropped: universal.host_user_context
      reason: "Overlaps discovery_recon.discovery_user_context; per discovery_user_context notes_to_llm, prefer the discovery version (more specific to the discovery/credential context) and drop universal.host_user_context."
      kept: template:discovery_recon.discovery_user_context
    - dropped: lolbin.historical_lolbin_baseline
      reason: "REFLECTED host-pattern query near-identical to cue_process_tool.baseline_deviation (same rule detection block, same host scope, same -14d/-90d historical intent). Kept baseline_deviation, which projects ParentImage+User to surface parent/account anomalies; the CommandLine shape baseline would add is already covered by verify_trigger_match and credential_target_identification on the alerting event."
      kept: template:cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.baseline
      reason: "Generic Image|endswith \\netsh.exe host baseline counts all netsh usage (netsh is heavily used for benign network config), which floods the signal. Subsumed by the REFLECTED baseline_deviation, which scopes to the rule's actual wlan/key=clear firing pattern."
      kept: template:cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "Image-only host aggregation subsumed by cue_host_context.clustering, which projects Image+ParentImage and surfaces novel parent-child pairs."
      kept: template:cue_host_context.clustering
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (hint 1). Phase 2 decode of what the command harvested is provided by credential_target_identification, which directs the analyst to identify which wireless profiles and keys were exposed."
    - template: cue_process_tool.follow_on
      reason: "follow_on scopes children by ParentImage|endswith %Image% (netsh). netsh wlan show profile spawns no meaningful children; the credential-harvest follow-on is exfil/lateral, covered by post_access_lateral_movement (+4h alert correlation) and cue_host_context.clustering. Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions:
    - hint: "LOLBin rule - check for renamed binary and other LOLBins achieving same objective"
      disposition: acted_on
      reason: >-
        Acted on: lolbin.renamed_binary_check is included (Q7, verifies netsh ran from
        its expected System32 path and notes the OriginalFileName match), and
        lolbin.technique_class_lateral is included (Q10, adapted to native
        credential/collection utilities - cmdkey, vaultcmd, certutil, bitsadmin -
        rather than the generic download/proxy-execute list, matching this rule's
        credential-harvest objective). Recorded here for audit; not a decline.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - discovery_recon
    - lolbin
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 18
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      sigma_alert: 2
