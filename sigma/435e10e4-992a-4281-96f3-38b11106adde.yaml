name: Computer Discovery And Export Via Get-ADComputer Cmdlet
id: f0475228-3529-4515-88e8-417c4dc264a0
description: |
  Detects PowerShell (powershell.exe or pwsh.exe) running the Get-ADComputer
  cmdlet with an unrestricted -Filter * to enumerate every computer object in
  Active Directory, where the result is captured to a file or pipeline via a
  redirect (>), Out-File, Set-Content, Add-Content, or Select. The rule fires
  only when both the image/OriginalFileName selection AND the command-line
  selection match, so the alert always represents a domain-wide computer
  enumeration whose output was staged rather than read interactively. Pulling
  the full computer inventory is routine for administrators building reports or
  scripts, but the same one-line export is a common early step in hands-on
  intrusions to map the domain before lateral movement. The investigation should
  decode the full command and its output destination, establish whether the
  acting user and host normally perform this enumeration, and look for other
  reconnaissance or follow-on activity around the same window.
type: detection
detection_id: 435e10e4-992a-4281-96f3-38b11106adde
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did PowerShell run Get-ADComputer with an unrestricted -Filter * and
    redirect the output to a file or pipeline, matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires only when both conditions hold together - the image is
    powershell.exe or pwsh.exe AND the command line contains Get-ADComputer with
    -Filter * AND an output cmdlet or redirect (>, Out-File, Set-Content,
    Add-Content, or Select). Read the exact command line and note the -Filter
    value, any -Properties requested, and where the output was written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user and host expected to enumerate the full Active Directory
    computer inventory?
  context: Domain and IT administrators legitimately run Get-ADComputer to build
    reports, inventory scripts, and scheduled exports. Standard users and
    non-administrative hosts rarely pull the entire computer list. The acting
    account's role and the host's function are often the fastest way to separate
    routine administration from post-compromise domain mapping - an unrestricted
    -Filter * export from an account or endpoint that has no reason to inventory
    the domain is the case worth pursuing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did the full Get-ADComputer command enumerate, and where was the
    output written?
  context: The full command line reveals the scope of the enumeration and its
    staging location. Note the -Filter expression (whether it is the broad * or a
    narrower query), any -Properties requested (a full -Properties * pull harvests
    operating system, last-logon, and addressing data useful for targeting), and
    the output destination from the redirect or Out-File/Set-Content/Add-Content
    target. The query scopes to Get-ADComputer activity on this host within a few
    minutes rather than the single triggering event, so a multi-step export or a
    sequence of related enumeration commands appears together. powershell.exe is
    too common to scope by image alone, so the command-line pattern bounds the
    result.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Get-ADComputer '
      condition: selection
    fields:
      - CommandLine
      - User
      - CurrentDirectory
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full enumeration script
    content?
  context: Script block logging records the deobfuscated script content PowerShell
    actually executed, including the complete Get-ADComputer pipeline, the
    properties requested, and any further processing of the result before it was
    written out. This is ground truth for what the enumeration collected, even if
    the process command line was truncated or wrapped. If PowerShell operational
    logs are not ingested in this environment, treat zero results as a visibility
    gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the PowerShell that ran the enumeration, and is
    that parent chain expected?
  context: The parent process shows how the enumeration was launched and helps
    distinguish interactive administration from automated or non-interactive
    execution. A console shell spawned from explorer.exe or a known management
    console fits routine work; a script host (wscript.exe, cscript.exe,
    mshta.exe), an Office application, wmiprvse.exe, or wsmprovhost.exe points to
    macro-driven or remote execution. Enumeration originating from a
    non-interactive shell or a service context is more consistent with automated
    post-compromise discovery than with an administrator at a keyboard.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single domain enumeration may be legitimate troubleshooting, but
    several reconnaissance commands clustered together - whoami, net user, nltest,
    dsquery, systeminfo, or additional AD cmdlets such as Get-ADUser, Get-ADGroup,
    or Get-DomainComputer - is a signature of post-compromise enumeration before
    the next stage. Tightly grouped discovery from one host or user in a short
    window is the temporal pattern that turns a lone Get-ADComputer export into
    likely hands-on activity.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host
    before the enumeration ran?
  context: Enumeration during an intrusion is frequently preceded by steps that
    weaken PowerShell defenses so the operator's later payloads run unimpeded -
    execution policy bypass (-ExecutionPolicy Bypass, -ep bypass), AMSI patching,
    a downgrade to PowerShell v2 which lacks modern logging, or real-time
    monitoring being disabled. Finding these precursors in the hour before the
    Get-ADComputer export strengthens the case that the discovery is part of
    hands-on operator activity rather than routine administration.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    enumeration reveals what led to it and what followed - the parent that staged
    PowerShell, other discovery, credential access, or lateral movement attempts.
    Aggregating by image and parent image collapses routine system noise into
    high-count rows while novel parent-child pairs stand out at low counts, which
    is what to scan for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains|all:
        - 'Get-ADComputer '
        - ' -Filter \*'
        CommandLine|contains:
        - ' > '
        - ' | Select '
        - Out-File
        - Set-Content
        - Add-Content
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains|all:
        - 'Get-ADComputer '
        - ' -Filter \*'
        CommandLine|contains:
        - ' > '
        - ' | Select '
        - Out-File
        - Set-Content
        - Add-Content
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether
    the enumeration was a single event or part of activity spanning multiple
    endpoints. If the same user triggered similar discovery or other actions on
    other hosts in the same window, the scope of the incident is larger than one
    machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains|all:
        - 'Get-ADComputer '
        - ' -Filter \*'
        CommandLine|contains:
        - ' > '
        - ' | Select '
        - Out-File
        - Set-Content
        - Add-Content
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's Get-ADComputer export
    pattern, what parents and users normally drive it, and does the current alert
    fit?
  context: Even legitimate administration can run this enumeration. This query
    mirrors the rule's selection structure scoped to this host, so the resulting
    parent and user distribution reflects how this specific firing pattern has
    historically run here, not generic PowerShell activity. A new parent process,
    an unexpected account, or a first-ever occurrence of the pattern is the anomaly
    worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains|all:
        - 'Get-ADComputer '
        - ' -Filter \*'
        CommandLine|contains:
        - ' > '
        - ' | Select '
        - Out-File
        - Set-Content
        - Add-Content
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's Get-ADComputer export pattern in the
    past week, and does the cross-host distribution look like consistent automation
    or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so
    the result is a fleet-wide count of the Get-ADComputer export pattern rather
    than generic PowerShell activity. Many hosts firing the same pattern points to
    a reporting script or inventory job that runs the export by design; one host or
    a small unrelated set points to manual operator activity. Use the host
    distribution and the consistency of the parents to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains|all:
        - 'Get-ADComputer '
        - ' -Filter \*'
        CommandLine|contains:
        - ' > '
        - ' | Select '
        - Out-File
        - Set-Content
        - Add-Content
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - credential access, defense
    tampering, or other reconnaissance - may reveal the attack stages that preceded
    the domain enumeration and help reconstruct the timeline. Computer discovery is
    often a mid-sequence step taken after initial access to map the domain before
    lateral movement, so prior alerts give the context for why the inventory was
    pulled.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1033
      name: System Owner/User Discovery
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: enumeration_launch
      description: "PowerShell runs Get-ADComputer with -Filter * to enumerate every computer object in the domain, the act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: output_staged
      description: "The enumeration result is captured to a file or pipeline via redirect/Out-File/Set-Content/Add-Content/Select; decode the full command and its output destination"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: correlate_followon
      description: "Correlate the enumeration with other reconnaissance and follow-on activity in the same window (enumeration + lateral movement grouping)"
      mapped_to: Q6
      source: template:discovery_recon.discovery_burst_detection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1033
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1033
      covered_by: Q6
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1033
      covered_by: Q5
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1033
      covered_by: Q2
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessContext
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Analytic gives a rationale (identify whether enumeration originates from a non-interactive shell or system service), not concrete values. Realized in discovery_parent_chain, which surfaces ParentImage/ParentCommandLine so the analyst can judge interactive vs non-interactive/service-launched execution."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Analytic gives a tuning rationale (temporal grouping of enumeration + lateral movement) with no fixed threshold. Realized in discovery_burst_detection (+/-15m grouping) and reinforced by cue_host_context.clustering."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q2
      notes: "Analytic gives a rationale (flag unexpected users, e.g., service accounts, issuing enumeration). Realized in discovery_user_context (Q2) and the user_actor baseline pair (Q9 user-pivot, Q10 service-principal parent-pivot)."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). Image-pivot tightened with CommandLine|contains 'Get-ADComputer ' per the powershell.exe Image-scoping hint."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). clustering_svc tightened with CommandLine|contains 'Get-ADComputer ' per the powershell.exe Image-scoping hint."
      kept: both
    - dropped: universal.host_user_context
      reason: "discovery_recon.discovery_user_context is more specific to the discovery context and the template's own notes direct preferring it over universal.host_user_context. Kept the discovery version (Q2)."
      kept: discovery_recon.discovery_user_context
    - dropped: cue_process_tool.process_chain
      reason: "parent-chain coverage provided by the more specific discovery_recon.discovery_parent_chain (Q5), which projects ParentImage+ParentCommandLine and is tuned for discovery-command parents."
      kept: discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.baseline
      reason: "generic Image|endswith powershell.exe host baseline is noise (powershell.exe is too common to scope by image — see hint); subsumed by the REFLECTED host-pattern question (baseline_deviation, Q13) which scopes to the rule's actual Get-ADComputer export firing pattern."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "Image-only host aggregation subsumed by cue_host_context.clustering (Q8), which projects Image+ParentImage and surfaces novel parent-child pairs."
      kept: cue_host_context.clustering
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "follow_on scopes children by ParentImage|endswith %Image% (powershell.exe), which on a CommandLine-scoped discovery rule returns broad unrelated PowerShell child activity rather than enumeration-specific follow-on. Post-enumeration activity is covered more directly by discovery_burst_detection (Q6) and cue_host_context.clustering (Q8). Trimmed to stay within the 14-18 target."
    - template: powershell.encoded_command_decode
      reason: "Rule detects a plain-text, already-readable command (Get-ADComputer -Filter * piped to a file). Per the template's notes_to_llm, the encoding-decode question is skipped for plain-text commands; the readable enumeration is decoded directly in cue_process_tool.mechanism_decode (Q3) and via script_block_logging (Q4). This is the survivor of hint 2 (powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode); the replaced template was never instantiated and the survivor is not load-bearing for a plain-text command."
    - template: universal.endpoint_protection_health
      reason: "Recommended-tier generic tamper sweep. Per the template's own drop-condition (b): cue_host_context.precursor (Q15) is present AND this is a discovery rule, not an execution/credential-access rule whose disposition turns on whether protection was weakened first. Prior-alert context (Q15) carries any tamper-as-precursor signal, and powershell_security_bypass (Q7) already surfaces command-line defense weakening in the hour before the enumeration. Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: declined
      reason: >-
        The replacement survivor (powershell.encoded_command_decode) is itself not
        instantiated: the rule fires on a plain-text Get-ADComputer pipeline, not an
        encoded command, so per that template's notes_to_llm the encoding-decode
        question is skipped for readable commands. The readable enumeration is decoded
        in cue_process_tool.mechanism_decode (Q3) and script_block_logging (Q4).
        Recorded in skipped_templates. (The 'powershell.exe too common for Image-based
        scoping' hint was acted on — mechanism_decode (Q3) and the user_actor
        baseline/clustering pivots (Q9, Q10, Q12) scope by CommandLine|contains
        'Get-ADComputer ' rather than Image.)
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' in the references is incidental — the rule detects domain computer enumeration (discovery), not impact/encryption behavior. No ransomware-specific question would map to the rule's detection logic."
      - id: network_scope
        confidence: low
        reason: "Keyword match on 'ransomware' in the references is incidental — the rule is a process_creation discovery rule with no network indicators. Cross-host scope is already covered by cue_process_tool.scope (Q14) and the user_actor clustering pair (Q11, Q12)."
  technique_sets_applied:
    - universal
    - powershell
    - discovery_recon
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 15
    source_counts:
      template: 15
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 12
    logsource_diversity:
      process_creation: 13
      powershell: 1
      sigma_alert: 1
