name: Potential Obfuscated Ordinal Call Via Rundll32
id: 64009764-2ec7-4a15-bcf2-aa3be9552cd8
description: |
  Detects rundll32 invoked with command-line tokens that call an exported function by ordinal
  using an obfuscated syntax (#+, #-, #0, or padded ordinal forms such as #655 / #656) instead of
  naming the export directly. Calling a DLL export by ordinal hides which function is being run and
  defeats string-based detection and casual review of the command line. Legitimate software almost
  always references DLL exports by name, so an ordinal call combined with obfuscation tokens is
  unusual; the investigation should decode which DLL and ordinal are being invoked, identify what
  that export does, and determine whether the parent, user, and host make this a deliberate evasion
  attempt or a rare but benign invocation.
type: detection
detection_id: 43fa5350-db63-4b8f-9a01-789a427074e1
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the rundll32 command line carry an obfuscated ordinal call token, and which DLL and ordinal does it target?
  context: |
    Confirm the alert is a true positive by reading the full command line. The rule fires when
    the process is rundll32 (by Image, OriginalFileName, or a rundll32 token) AND the command line
    contains an ordinal-call token such as #+, #-, #0, #655, or #656. Verify the token is present and
    note which DLL path and which ordinal are being called, since that pair drives the rest of the
    investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run rundll32 with an ordinal-based export call?
  context: Legitimate administrative or vendor software occasionally calls DLL exports by ordinal, but
    it is rare on most endpoints. Knowing whether this user role and host function routinely run
    rundll32 in this way is often the fastest way to separate a true positive from an unusual but
    sanctioned operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly is this rundll32 invocation doing? Resolve the DLL and ordinal to the underlying export.
  context: |
    The command line names a DLL and an export by ordinal rather than by name. Map the ordinal
    back to the actual exported function (via the DLL's export table) to learn what operation is being
    invoked, and decode any additional arguments, paths, or encoded values passed after the ordinal.
    The obfuscation tokens (#+, #-, #0, padded ordinal forms) are syntactic tricks that resolve to the
    same export call, so normalize them before interpreting.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - '#+'
          - '#-'
          - '#0'
          - '#655'
          - '#656'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1027.010.obfuscation_check
  question: Beyond the ordinal token, does the command line show additional obfuscation such as escape characters, encoding, or concatenation?
  context: Ordinal calls are one obfuscation layer; operators frequently stack more on top. Look for
    excessive escape or caret characters, base64 or hex blobs, character concatenation, environment
    variable substitution, and abnormally long or high-entropy argument strings. Multiple layers of
    obfuscation in a single command line raise the likelihood the invocation is deliberately hiding its
    purpose rather than a quirk of legitimate tooling.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was rundll32 executed from its expected System32 or SysWOW64 path?
  context: rundll32 run from outside System32 or SysWOW64 may have been copied or renamed to evade
    path-based allowlists. A rundll32 image in a user temp directory, a profile folder, or another
    user-writable location is more suspicious than one in its standard system path. Compare the Image
    path against the OriginalFileName to spot a renamed copy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:cue_process_tool.process_chain
  question: What process spawned this rundll32 and is that parent chain expected?
  context: The parent process reveals how rundll32 was launched. Predictable parents (explorer.exe for
    user actions, services.exe for service-hosted activity) fit routine usage. Parents such as
    mshta.exe, wscript.exe, winword.exe, or an unfamiliar binary in a user-writable path point toward
    script-driven or macro-driven execution and warrant tracing one level further up the tree.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this rundll32 spawn afterward?
  context: The export invoked by ordinal may load or execute follow-on code. Child processes spawned by
    rundll32 reveal its actual impact -- a spawned cmd.exe, powershell.exe, or a network utility points
    toward post-exploitation, while no children is consistent with an in-memory-only operation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1027.010.time_correlation
  question: What file writes, network connections, or follow-on commands occurred on this host within minutes of the ordinal call?
  context: An obfuscated export call is often the pivot between an initial foothold and the next stage.
    Examining what happened in the short window around the invocation -- a dropped file, an outbound
    connection, or a follow-up command -- shows whether the call advanced an operation or stood alone.
    A tight time correlation between the ordinal call and downstream artifacts strengthens the case for
    deliberate evasion.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this ordinal-call pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even routine tools can be abused. The query mirrors the rule's selection structure scoped to
    this host, so the resulting parent and user distribution reflects how this specific firing pattern
    has historically run here -- not generic rundll32 usage. A new parent process, an unexpected user
    account, or an unusual command-line structure relative to that history is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli:
        CommandLine|contains:
        - '#+'
        - '#-'
        - '#0'
        - '#655'
        - '#656'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the rundll32 ordinal-call pattern over the past 90 days, and what command-line structures are typical?
  context: A pattern with no prior history on a host is more suspicious than one used regularly under
    stable parents. The query mirrors the rule's selection structure scoped to this host, so the count
    reflects this specific firing pattern rather than all rundll32 usage. The command-line projection
    lets the analyst compare typical invocation patterns against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli:
        CommandLine|contains:
        - '#+'
        - '#-'
        - '#0'
        - '#655'
        - '#656'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli:
        CommandLine|contains:
        - '#+'
        - '#-'
        - '#0'
        - '#655'
        - '#656'
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli:
        CommandLine|contains:
        - '#+'
        - '#-'
        - '#0'
        - '#655'
        - '#656'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar activity on other hosts, the scope of the
    investigation may be larger than the single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli:
        CommandLine|contains:
        - '#+'
        - '#-'
        - '#0'
        - '#655'
        - '#656'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:lolbin.technique_class_lateral
  question: Were any other proxy-execution LOLBins used on this host recently?
  context: Operators who use one proxy-execution LOLBin often use several. If rundll32 was used to run
    code by ordinal, check whether regsvr32, mshta, msbuild, installutil, or similar proxy-execution
    binaries were also used nearby. Finding multiple LOLBins driving the same objective strengthens the
    case for deliberate evasion.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\msiexec.exe'
          - '\installutil.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\control.exe'
          - '\infdefaultinstall.exe'
          - '\msdt.exe'
          - '\mavinject.exe'
          - '\wmic.exe'
          - '\xwizard.exe'
          - '\ie4uinit.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering,
    reconnaissance, credential access -- may reveal the stages that preceded this event and help
    reconstruct the timeline that led to an obfuscated execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1027.010
    name: Command Obfuscation
    role: primary
  - id: T1027
    name: Obfuscated Files or Information
    role: secondary
  attack_chain_coverage:
  - stage: 1
    label: obfuscated_invocation
    description: rundll32 is launched with an obfuscated ordinal-call token in place of a named export
    mapped_to: Q1
    source: template:universal.verify_trigger_match
  - stage: 2
    label: mechanism_decode
    description: Resolve the DLL and ordinal to the underlying export and decode the full command line
    mapped_to: Q3
    source: template:cue_process_tool.mechanism_decode
  - stage: 3
    label: additional_obfuscation
    description: Inspect for stacked obfuscation (escapes, encoding, concatenation, entropy) beyond the ordinal token
    mapped_to: Q4
    source: attack:T1027.010.obfuscation_check
  - stage: 4
    label: follow_up_behavior
    description: Correlate downstream child processes, file writes, or connections within the time window
    mapped_to: Q8
    source: attack:T1027.010.time_correlation
  dimension_coverage:
  - dimension: obfuscation_check
    technique: T1027.010
    covered_by: Q4
    source: attack:T1027.010.obfuscation_check
    coverage_type: direct
  - dimension: time_correlation
    technique: T1027.010
    covered_by: Q8
    source: attack:T1027.010.time_correlation
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1027.010
    covered_by: Q9
    source: template:cue_process_tool.baseline_deviation
    coverage_type: indirect
  - dimension: obfuscation_check
    technique: T1027
    covered_by: Q3
    source: template:cue_process_tool.mechanism_decode
    coverage_type: direct
  - dimension: time_correlation
    technique: T1027
    covered_by: Q8
    source: attack:T1027.010.time_correlation
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1027
    covered_by: Q15
    source: template:lolbin.technique_class_lateral
    coverage_type: indirect
  - dimension: user_context
    technique: T1027
    covered_by: Q2
    source: template:universal.host_user_context
    coverage_type: direct
  mutable_dispositions:
  - element: CommandLineEntropyThreshold
    dimension: obfuscation_check
    specific_values: rationale only
    disposition: context
    target_question: Q4
    notes: No concrete threshold value supplied; framed as look for high-entropy and encoded argument strings in Q4 context.
  - element: SuspiciousCharacterCount
    dimension: tuning_parameter
    specific_values: rationale only
    disposition: context
    target_question: Q4
    notes: No concrete count supplied; framed as excessive escape/caret characters in Q4 context.
  - element: TimeWindow
    dimension: time_correlation
    specific_values: rationale only
    disposition: context
    target_question: Q8
    notes: No concrete window supplied; Q8 uses +/-15m to correlate the call with follow-on artifacts.
  - element: PayloadEntropyThreshold
    dimension: obfuscation_check
    specific_values: rationale only
    disposition: context
    target_question: Q4
    notes: T1027 entropy tuning; same obfuscation-inspection question carries the rationale.
  - element: SuspiciousParentProcessList
    dimension: tuning_parameter
    specific_values: rationale only
    disposition: context
    target_question: Q6
    notes: Parent-legitimacy framing carried by the process-chain question; names mshta/wscript/Office as suspect parents.
  dedup_decisions:
  - dropped: cue_process_tool.baseline
    reason: cue_user_actor.baseline (Image+User over 90d) and the reflected lolbin.historical_lolbin_baseline both cover historical frequency more specifically; the host-only binary baseline is subsumed.
    kept: template:lolbin.historical_lolbin_baseline
  - dropped: cue_process_tool.clustering
    reason: cue_host_context same-window host clustering is duplicated by the time_correlation question's +/-15m host aggregation; kept the attack-sourced version.
    kept: attack:T1027.010.time_correlation
  - dropped: cue_host_context.clustering
    reason: same-window host activity already provided by the attack:T1027.010.time_correlation +/-15m host aggregation over Image/ParentImage.
    kept: attack:T1027.010.time_correlation
  - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair
    reason: user_actor pair -- non-deduplicable; both emitted (user-pivot and parent+binary substitute).
    kept: both
  - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair
    reason: user_actor pair -- non-deduplicable; both emitted (user-pivot and parent+binary substitute).
    kept: both
  skipped_templates:
  - template: universal.evidence_preservation
    reason: Rule fires on the in-memory ordinal-call syntax in the command line, not on a dropped file artifact; the DLL referenced is typically a resident system module, so a file_event %file_paths% lookup has no rule-derived path to scope to.
  - template: universal.endpoint_protection_health
    reason: Rule detects an obfuscation/defense-evasion technique, not payload execution or credential access where tamper-as-precursor is the live question; cue_host_context.precursor already surfaces any prior tamper alerts on the host.
  reflection_overrides: []
  hint_dispositions:
  - hint: File paths derived from CommandLine -- use %file_paths% placeholder
    disposition: declined
    reason: >-
      The rule's detection is the ordinal-call token syntax in the rundll32 command line, not a file
      path; commandline_values are obfuscation tokens and the literal rundll32, none of which yield a
      file path to populate %file_paths%. The referenced DLL is normally a resident system module
      rather than a dropped artifact, so universal.evidence_preservation was skipped rather than
      authored against an empty path scope.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - obfuscation
  - lolbin
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 15
      sigma_alert: 1
