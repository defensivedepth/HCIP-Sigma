name: Suspicious Windows Defender Folder Exclusion Added Via Reg.EXE
id: 3e52c934-4c9d-4719-99ac-7c7b94f657d8
description: |
  Detects reg.exe adding a folder exclusion under the Windows Defender or
  legacy Microsoft Antimalware Exclusions\Paths registry key. The rule fires
  when a single reg.exe command line contains one of those Exclusions\Paths
  subkeys AND the full set of ADD-a-REG_DWORD switches (ADD, /t, REG_DWORD,
  /v, /d, 0) - the shape that registers a path Defender will no longer scan.
  Excluding a path from scanning is occasionally done by administrators and
  software installers, but the same command is a common precursor to running
  a payload from the now-excluded directory, so the investigation should
  determine which path was excluded, which process and user issued the
  command, what ran in that path afterward, and whether the host shows other
  tampering or a history of this pattern.
type: detection
detection_id: 48917adc-a28e-4f5d-b729-11e75da8941f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the reg.exe command line add a Defender exclusion path with a
    REG_DWORD value of 0, matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires when a reg.exe command line both references an Exclusions\Paths
    subkey (Windows Defender or the legacy Microsoft Antimalware path) and carries
    the full ADD a REG_DWORD switch set (ADD, /t REG_DWORD, /v, /d, 0). Read the
    exact command line and note the path passed to /v - that directory is the one
    being removed from Defender scanning.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to add Windows Defender folder exclusions?
  context: Adding Defender exclusions is a privileged administrative action that a
    narrow set of accounts and endpoints perform legitimately - server admins,
    endpoint-management tooling, or software installers that register their own
    install directory. Most users and most hosts never touch Exclusions\Paths.
    Knowing whether the acting account and host function involve security
    configuration is often the fastest path to separating a sanctioned change
    from an attacker clearing the way for a payload.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:registry.registry_key_context
  question: Which exact path was excluded, and under which Defender exclusions key
    was it registered?
  context: The value name passed to /v is the directory being excluded from
    scanning, and the parent key tells you whether modern Defender
    (SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths) or the legacy
    Microsoft Antimalware path was targeted. A path under a normal software
    install directory is a different finding than a user-writable location such
    as a profile AppData or ProgramData subfolder, which is where dropped
    payloads commonly live. Read the structured registry write to confirm the
    target object and the value written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:defense_tampering.what_was_disabled
  question: What Defender scanning coverage was removed, and does the REG_DWORD
    value of 0 confirm the path was excluded rather than re-enabled?
  context: An Exclusions\Paths entry tells Defender to skip real-time and scheduled
    scanning for everything under the named directory. The rule keys on a /d 0
    REG_DWORD write, which is the value Defender records for an active path
    exclusion. Read the full command line to confirm which directory lost
    coverage and that the value disables scanning, since that gap is what
    subsequent execution from the path exploits.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is the folder exclusion still in place, or has it been removed?
  context: A path exclusion persists in the registry until something deletes it, so
    unlike a one-shot command its effect is ongoing - every file written under
    the excluded directory stays unscanned. Check whether the exclusion still
    exists in the host's Defender configuration; if it is still present it should
    be removed to restore scanning coverage for that path before the
    investigation continues.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this reg.exe, and is that parent chain expected for
    adding a Defender exclusion?
  context: The parent process reveals how the exclusion command was invoked. The
    REG_DWORD write is set by whatever launched reg.exe, so the parent is where
    the decision was made. An interactive administrative session usually shows a
    console shell or a management agent; an unexpected parent such as a script
    host, mshta.exe, regsvr32.exe, or an Office application points to automated
    or malicious invocation that excludes a path to evade scanning.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was this reg.exe executed from its expected System32 or SysWOW64 path?
  context: reg.exe normally runs from C:\Windows\System32 or C:\Windows\SysWOW64. A
    copy executed from a user-writable or temporary directory, or a differently
    named binary whose OriginalFileName resolves to reg.exe, suggests the binary
    was relocated or renamed to evade path-based allowlisting. Compare the Image
    path and OriginalFileName against the expected system location.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:defense_tampering.activity_after_tampering
  question: What ran on this host after the exclusion was added, especially from the
    excluded path?
  context: A path exclusion is usually a preparatory step that lets a payload run
    from the now-unscanned directory without Defender interference. Examining
    process creation in the window after the exclusion reveals what the gap was
    meant to enable - execution from the excluded folder, follow-on download, or
    further configuration changes. A burst of process creation right after the
    exclusion, particularly from the excluded directory, points to the activity
    the tampering allowed.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host around the
    same time?
  context: An actor who excludes a path from Defender often pairs it with other
    evasion steps - stopping security services, clearing event logs, deleting
    shadow copies, disabling real-time monitoring, or weakening firewall rules.
    Finding multiple tampering events from the same user or parent process within
    a short window indicates a coordinated effort to blind the host rather than an
    isolated administrative exclusion. This also surfaces other LOLBins used for
    the same defense-evasion objective.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session?
  context: An actor who edits one registry key often edits several - adding a
    Defender exclusion AND setting a Run key for persistence AND modifying
    firewall or policy values. Finding clusters of reg.exe write operations from
    the same host within a short window reveals the full scope of the
    configuration changes and whether multiple objectives were pursued alongside
    the exclusion.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's Defender-exclusion pattern,
    what parents and users normally drive it, and does the current alert fit?
  context: Even legitimate software installers can add Defender exclusions. This
    query mirrors the rule's selection structure scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing
    pattern has historically run here, not generic reg.exe activity. A new parent
    process, an unexpected account, or a first-ever occurrence is the anomaly
    worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
        CommandLine|contains|all:
        - 'ADD '
        - '/t '
        - 'REG_DWORD '
        - '/v '
        - '/d '
        - '0'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the rule's Defender-exclusion pattern over
    the past 90 days, and what command-line shapes are typical?
  context: A Defender-exclusion pattern with no prior history on a host is more
    suspicious than one added regularly under a stable parent. This query mirrors
    the rule's selection structure scoped to this host, so the count reflects the
    rule's specific firing pattern rather than generic reg.exe usage. The
    command-line projection lets the analyst compare the directories excluded
    historically against the path in the current alert.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
        CommandLine|contains|all:
        - 'ADD '
        - '/t '
        - 'REG_DWORD '
        - '/v '
        - '/d '
        - '0'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's Defender-exclusion pattern in the past
    week, and does the cross-host distribution look like consistent automation or
    one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so
    the result is a fleet-wide count of the Defender-exclusion pattern rather than
    generic reg.exe activity. Many hosts adding the same exclusion points to a
    software deployment or management policy that excludes a path by design; one
    host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
        CommandLine|contains|all:
        - 'ADD '
        - '/t '
        - 'REG_DWORD '
        - '/v '
        - '/d '
        - '0'
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally add Windows Defender folder exclusions?
  context: Compare the acting account against its own history. A server or endpoint
    administrator may add exclusions routinely as part of deployment work; an
    account that has never registered an Exclusions\Paths entry doing so now is
    more notable. The user role and typical responsibilities should align with
    making Defender configuration changes.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent process and Defender-exclusion command-line pattern
    historically routine on this host or across the environment, or is the parent
    unusual for it?
  context: When the alert fires under a service principal, user identity does not
    attribute the behavior; the meaningful pivot is which parent process launched
    a reg.exe carrying the Defender-exclusion pattern and whether that pairing is
    historically common. A new or rare parent for this pattern on this host or
    environment-wide is the deviation worth chasing, while a parent that already
    accounts for many prior events is more likely an installer or management
    agent.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether
    the Defender exclusion was a single event or part of activity spanning
    multiple endpoints. If the same user triggered similar activity on other
    hosts, the scope of the incident is larger than one machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent process and Defender-exclusion command-line pattern
    fire on other hosts in the same window, indicating a coordinated rollout
    rather than isolated activity?
  context: Under a service principal, asking what else the user did across the
    network returns agent-telemetry noise, since every host runs system-owned
    agents constantly. The meaningful question is whether the same launch
    mechanism - a parent process spawning a reg.exe that carries the
    Defender-exclusion pattern - fired on other hosts in the alert window.
    Multiple hosts in the same window points to scheduled or pushed automation; a
    single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith: \reg.exe
        CommandLine|contains:
        - SOFTWARE\Microsoft\Windows Defender\Exclusions\Paths
        - SOFTWARE\Microsoft\Microsoft Antimalware\Exclusions\Paths
      condition: selection
    fields:
      - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Impair Defenses - Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: exclusion_added
      description: "reg.exe adds a path under the Defender or Microsoft Antimalware Exclusions\\Paths key with a REG_DWORD value of 0 - the act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: exclusion_target_identified
      description: "The specific excluded directory and which Defender exclusions key it was registered under are identified from the registry write and command line"
      mapped_to: Q3
      source: template:registry.registry_key_context
    - stage: 3
      label: followon_enabled
      description: "Correlate process creation after the exclusion, especially from the excluded path, with the activity the unscanned directory was meant to enable"
      mapped_to: Q8
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: process_identification
      technique: T1562
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q8
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q9
      source: template:defense_tampering.other_tampering_activity
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Tuning rationale for excluding authorized admin tools; no concrete values in the analytic. Realized as the parent-chain legitimacy judgment in cue_process_tool.process_chain, which asks whether the parent that launched reg.exe is expected for adding a Defender exclusion."
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q2
      notes: "Expected administrative tools/processes to suppress false positives; no concrete values. Realized as the user/host expectation judgment in universal.host_user_context (is this account/host expected to add exclusions)."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Customizable list of security service names; no concrete values in the analytic. Realized as the tamper-class binary and command list in defense_tampering.other_tampering_activity, which surfaces stop/disable actions against security services and tools."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Refines which services are security-critical; no concrete values. Same realization as ServiceNames - the tamper-class sweep in defense_tampering.other_tampering_activity."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Correlation window linking the exclusion to follow-on activity; no fixed value in the analytic. Realized as the +1h forward window in defense_tampering.activity_after_tampering and the +/-1h window in other_tampering_activity."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair - non-deduplicable; both emitted (subject-pivot Q14 and launch-mechanism-pivot Q15). Image-pivot adapted to add the rule's Exclusions\\Paths CommandLine constraint per the Q-vs-query alignment rule."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair - non-deduplicable; both emitted (subject-pivot Q16 and launch-mechanism-pivot Q17)."
      kept: both
    - dropped: cue_process_tool.baseline
      reason: "generic Image|endswith reg.exe host baseline counts all reg.exe usage (extremely high-volume system binary) and is subsumed by the REFLECTED host-pattern questions (cue_process_tool.baseline_deviation Q11 and lolbin.historical_lolbin_baseline Q12) which scope to the rule's actual Defender-exclusion pattern"
      kept: cue_process_tool.baseline_deviation + lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "Image-only host aggregation of all process activity is subsumed by defense_tampering.activity_after_tampering (Q8, post-exclusion host activity) and other_tampering_activity (Q9, tamper-class host activity)"
      kept: defense_tampering.activity_after_tampering + defense_tampering.other_tampering_activity
    - dropped: cue_host_context.clustering
      reason: "generic host-activity aggregation subsumed by defense_tampering.activity_after_tampering (Q8) which projects the post-exclusion host activity that matters for this tamper rule; trimmed to stay within the 14-18 target"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_host_context.precursor
      reason: "prior-alert timeline context is desirable but lower-tier (tier 3); trimmed to stay within the 14-18 target while keeping all must_include technique-set questions and the non-deduplicable user_actor pair"
      kept: none
    - dropped: lolbin.technique_class_lateral
      reason: "the rule's objective is defense tampering (Defender exclusion), not download/proxy-execution; the lolbin download/proxy binary list does not match this objective. Other LOLBins achieving the same defense-evasion objective are covered by defense_tampering.other_tampering_activity (Q9), whose Image|endswith list includes reg/sc/wevtutil/netsh/bcdedit/vssadmin/powershell tamper binaries"
      kept: defense_tampering.other_tampering_activity
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context (hint 2) and defense_tampering.what_was_disabled (hint 1). Phase 2 decode is provided by registry_key_context (Q3, structured registry path/value) and what_was_disabled (Q4, command-line confirmation that scanning was removed)."
    - template: cue_process_tool.follow_on
      reason: "follow_on scopes children by ParentImage|endswith %Image% = reg.exe; reg.exe rarely spawns meaningful children (it writes the registry and exits), so the post-exclusion question that matters is host-wide activity, covered by defense_tampering.activity_after_tampering (Q8). Trimmed to stay within the 14-18 target."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED universal sweep; its own notes_to_llm say drop when defense_tampering is confirmed. defense_tampering is confirmed for this rule and its other_tampering_activity (Q9) plus what_was_disabled (Q4) are more specific. The generic host-tamper sweep would duplicate Q9."
    - template: persistence_scope.process_based_persistence
      reason: "recommended priority; cascaded from binary reg.exe. applicability.is_persistence_rule is false - this rule detects a Defender exclusion (defense evasion), not persistence installation. Trimmed to stay within the 14-18 target."
    - template: persistence_scope.file_based_persistence
      reason: "recommended priority; cascaded from binary reg.exe. File-drop persistence is orthogonal to adding a registry exclusion. Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - registry
    - lolbin
    - cue_process_tool
    - cue_user_actor
  generation_stats:
    total_questions: 17
    source_counts:
      template: 17
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 16
      registry_set: 1
