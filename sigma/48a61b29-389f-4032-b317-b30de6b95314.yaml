name: Suspicious Plink Port Forwarding
id: 112c1ab6-1a5b-4101-9c79-97d308f1ac97
description: |
  Detects a command-line SSH client (Plink, the non-interactive PuTTY variant)
  invoked with a remote-forward switch (' -R '), which establishes a reverse
  tunnel that exposes a host-local service to a remote SSH endpoint. The rule
  fires when a process whose file description is "Command-line SSH, Telnet, and
  Rlogin client" runs with ' -R ' on its command line. Reverse port forwarding is
  used to relay otherwise firewalled internal services - including RDP and SMB -
  outbound through an attacker-controlled SSH server, but administrators also use
  it for legitimate remote maintenance. The investigation should decode the full
  tunnel specification (forwarded ports, the SSH destination, and credential
  flags), determine which internal service is being exposed, confirm whether the
  outbound SSH connection and destination are expected, and establish whether the
  same pattern appears on other hosts.
type: detection
detection_id: 48a61b29-389f-4032-b317-b30de6b95314
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain a remote-forward switch (' -R ') on a
    command-line SSH client, matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires when a process described as a command-line SSH/Telnet/Rlogin
    client runs with ' -R ' on its command line. Read the exact command line and
    confirm the remote-forward switch is present rather than an unrelated argument
    that happens to contain the characters. Note the full forward specification
    that follows -R, since it names the local service being exposed and the remote
    endpoint it is relayed to.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Description
- source: template:universal.host_user_context
  question: Is this user and host expected to run an SSH client with reverse port
    forwarding?
  context: Outbound SSH tunneling with reverse forwarding is normal for a narrow set
    of administrators and developers who reach internal services through a jump
    host. Most users and most endpoints never run a command-line SSH client this
    way. Knowing whether the acting account and the host function involve remote
    administration or tunnel-based maintenance is often the fastest path to
    separating sanctioned remote access from an attacker relaying a host-local
    service outbound.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:remote_access_tool.tool_authorization
  question: Is this SSH tunneling client authorized for use on this host, and what
    business purpose does the tunnel serve?
  context: Organizations typically maintain a list of approved remote access and
    tunneling tools. A command-line SSH client establishing a reverse forward - even
    a well-known utility like Plink - may represent shadow IT or attacker-deployed
    relay infrastructure if it is not sanctioned on this host. Unauthorized
    tunneling tools should be checked regardless of the tool's reputation, since the
    same binary serves both administrative and adversarial port forwarding.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is this SSH session part of a documented administrative procedure or an
    open support ticket?
  context: Legitimate tunneling usually correlates with an active support ticket, a
    scheduled maintenance window, or a documented administrative procedure. If no
    such context exists for this session, the reverse tunnel may have been
    established by an attacker to relay an internal service outbound or by a user
    circumventing network restrictions. Match the acting account and host against
    change and support records for the alert window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What does the full Plink command line specify - which local service is
    forwarded, to which SSH destination, and were credential flags supplied inline?
  context: The remote-forward argument follows the pattern
    -R <bind>:<rport>:127.0.0.1:<lport>, which exposes the host-local service on
    <lport> at the remote SSH endpoint. Decode the full specification to identify
    the internal service being relayed (for example 3389 for RDP, 445 for SMB, or
    an application port) and the SSH destination it is relayed to. Note whether -pw
    supplies a password inline, whether -ssh forces the protocol, and whether -N
    suppresses the remote shell so the process exists only to maintain the tunnel.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1572.obfuscation_check
  question: Does the command line carry credential or protocol-forcing flags (-pw,
    -ssh, -N, -batch) that mark this as a non-interactive tunnel rather than an
    interactive session?
  context: Reverse-forwarding tunnels used to relay traffic outbound are typically
    launched non-interactively with the credential supplied inline. -pw embeds the
    SSH password directly on the command line, -ssh forces the SSH protocol, -N
    requests no remote command (tunnel only), and -batch disables interactive
    prompts. The presence of these flags alongside -R indicates an automated tunnel
    set up to encapsulate another protocol, whereas an interactive admin session
    would normally prompt for credentials rather than embed them.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
        CommandLine|contains:
          - ' -R '
      selection_noninteractive:
        CommandLine|contains:
          - ' -pw '
          - ' -ssh '
          - ' -N '
          - ' -batch '
      condition: selection and selection_noninteractive
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this SSH client, and is that parent chain expected?
  context: The parent process reveals how the tunnel was launched. A reverse forward
    is configured by whatever invoked the client, so the parent is where the tunnel
    decision was made. Interactive administration usually shows a console shell or a
    terminal application; an unexpected parent such as a script host, mshta.exe, an
    Office application, or a remote-execution service points to automated or
    attacker-driven invocation that set up the tunnel programmatically.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:remote_access_tool.connection_destination
  question: What external SSH endpoint did this client connect to, and is that
    destination expected infrastructure?
  context: A reverse-forwarding SSH client establishes an outbound session to the
    SSH server that the tunnel relays through. The destination reveals whether the
    client is reaching a sanctioned jump host or bastion versus an unknown or
    external address that would relay an internal service to an attacker. Correlate
    the network connection from this host and binary with the SSH destination named
    in the command line, and note the destination port, which is commonly 22 but may
    be moved to evade egress filtering.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:lateral_movement.target_activity_after_access
  question: What processes ran on this host shortly after the tunnel was
    established, and do any align with a relayed RDP or SMB session?
  context: A reverse tunnel that exposes RDP (3389) or SMB (445) is a means to an end
    - the relayed session is used to log in and act on the host. Examining process
    creation in the window right after the tunnel launch reveals interactive logon
    artifacts, file access, or follow-on tooling that the tunnel enabled. Suspicious
    post-tunnel processes include cmd.exe, powershell.exe, and certutil.exe, which
    indicate hands-on activity arriving through the relayed connection.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - Image
- source: attack:T1021.001.remote_logon_correlation
  question: Were any remote interactive or network logons recorded on this host
    around the time the tunnel was established?
  context: When a reverse tunnel forwards RDP or SMB, the relayed session produces a
    logon on the target host. Correlating successful and attempted logons within a
    few minutes of the tunnel launch shows whether the forwarded port was actually
    used to authenticate. Network logons (type 3) and RDP logons (type 10) close to
    the alert time, particularly from a loopback or unexpected source, indicate the
    tunnel carried an interactive session rather than sitting idle.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID:
          - 4624
          - 4625
          - 4648
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TargetUserName
      - LogonType
      - IpAddress
      - EventID
- source: template:lateral_movement.lateral_movement_scope
  question: Has this user account run other tunneling or remote-execution tools
    across the environment?
  context: Attackers rarely stop at one tunnel. If the acting account is launching
    SSH forwarders or other remote-execution utilities on multiple hosts, the scope
    of activity is larger than this one endpoint. Finding the same user driving
    tunneling tools across several hosts in a short window indicates active pivoting
    rather than a one-off administrative session.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      selection_lateral:
        Image|endswith:
          - '\plink.exe'
          - '\plink32.exe'
          - '\ssh.exe'
          - '\psexec.exe'
          - '\psexec64.exe'
          - '\mstsc.exe'
          - '\wmic.exe'
      condition: selection and selection_lateral
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.baseline
  question: Does this user normally launch an SSH client with reverse port
    forwarding?
  context: Compare the acting account against its own history. An administrator or
    developer may run reverse-forwarding tunnels routinely as part of remote
    maintenance; an account that has never done so launching one is more notable.
    The user role and typical toolset should align with tunnel-based remote access.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains:
          - ' -R '
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent process and reverse-forward command-line pattern
    historically routine on this host or across the environment, or is the parent
    unusual for it?
  context: When the alert fires under a service principal, user identity does not
    attribute the behavior; the meaningful pivot is which parent process launched a
    child carrying the reverse-forward pattern and whether that pairing is
    historically common. A new or rare parent for this pattern on this host or
    environment-wide is the deviation worth chasing, while a parent that already
    accounts for many prior events is more likely automation or a maintenance
    pipeline.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains:
          - ' -R '
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether
    the tunnel was a single event or part of activity spanning multiple endpoints.
    If the same user triggered similar activity on other hosts, the scope of the
    incident is larger than one machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent process and reverse-forward command-line pattern
    fire on other hosts in the same window, indicating a coordinated rollout rather
    than isolated activity?
  context: Under a service principal, asking what else the user did across the
    network returns agent-telemetry noise, since every host runs system-owned agents
    constantly. The meaningful question is whether the same launch mechanism - a
    parent process spawning a child that carries the reverse-forward pattern - fired
    on other hosts in the alert window. Multiple hosts in the same window points to
    scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains:
          - ' -R '
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    tunnel launch reveals what led to it and what followed - the parent that staged
    the process, reconnaissance, or follow-on execution the tunnel was meant to
    carry. Novel parent-child pairs at low counts are what to scan for against the
    high-count noise of routine system processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - reconnaissance, credential access,
    or defense tampering - may reveal the attack stages that preceded the tunnel and
    help reconstruct the full timeline. Outbound tunneling is often a late step in a
    sequence, set up to relay access the operator already established, so prior
    alerts give the context for why the tunnel was created.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's reverse-forward pattern in the past
    week, and does the cross-host distribution look like consistent automation or
    one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so
    the result is a fleet-wide count of the reverse-forward firing pattern rather
    than generic process activity. Many hosts firing the same pattern points to a
    maintenance pipeline or scripted deployment that sets up tunnels by design; one
    host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Description: Command-line SSH, Telnet, and Rlogin client
        CommandLine|contains: ' -R '
      condition: selection
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1572
      name: Protocol Tunneling
      role: primary
    - id: T1021.001
      name: Remote Desktop Protocol
      role: secondary
    - id: T1021
      name: Remote Services
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tunnel_invocation
      description: "A command-line SSH client is launched with a remote-forward switch (' -R '), the act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: tunnel_specification
      description: "Decode the forward specification (local service exposed, SSH destination, credential and protocol flags) to understand what is being relayed and where"
      mapped_to: Q5
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: outbound_session
      description: "Correlate the process launch with the outbound SSH network connection to the destination the tunnel relays through"
      mapped_to: Q8
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: relayed_access
      description: "Post-tunnel activity (relayed RDP/SMB logon and follow-on process execution) that the tunnel was set up to carry"
      mapped_to: Q9
      source: template:lateral_movement.target_activity_after_access
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1572
      covered_by: Q6
      source: attack:T1572.obfuscation_check
      coverage_type: direct
    - dimension: time_correlation
      technique: T1572
      covered_by: Q8
      source: template:remote_access_tool.connection_destination
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1572
      covered_by: Q3
      source: template:remote_access_tool.tool_authorization
      coverage_type: direct
    - dimension: time_correlation
      technique: T1021.001
      covered_by: Q10
      source: attack:T1021.001.remote_logon_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1021.001
      covered_by: Q9
      source: template:lateral_movement.target_activity_after_access
      coverage_type: direct
    - dimension: user_context
      technique: T1021.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1021
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1021
      covered_by: Q10
      source: attack:T1021.001.remote_logon_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1021
      covered_by: Q11
      source: template:lateral_movement.lateral_movement_scope
      coverage_type: direct
  mutable_dispositions:
    - element: AllowedTools
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Whitelist of legitimate tunneling tools; no concrete values in the analytic. Realized as the tool-authorization judgment in remote_access_tool.tool_authorization, which asks whether this SSH tunneling client is sanctioned on this host."
    - element: DataAsymmetryThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Sent/received byte ratio that flags tunneling; no concrete threshold in the analytic and not directly queryable from process_creation. Surfaced as context in remote_access_tool.connection_destination, which directs the analyst to the outbound SSH session and destination."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (analytic suggests ~N seconds / 5 minutes)"
      disposition: query_injection
      target_question: Q10
      notes: "Process-to-network and login-to-activity correlation window. Realized as the +/-15m range on the remote-logon correlation query (Q10) and the +30m post-tunnel activity window (Q9)."
    - element: ProcessList
      dimension: tuning_parameter
      specific_values: "cmd.exe, powershell.exe, certutil.exe"
      disposition: context
      target_question: Q9
      notes: "Suspicious post-login processes. Values named in the context of lateral_movement.target_activity_after_access; query intentionally left broad (host-scoped aggregation by User+Image) so the analyst sees all post-tunnel execution rather than only the named binaries, then filters."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q2
      notes: "Tune for non-admin users / service accounts expected to use RDP. Realized as the host_user_context judgment and the user-actor baseline pair."
    - element: HostAccessPatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: "Scope to uncommon or first-time source/destination host pairs. Realized as the cross-host lateral-movement scope question."
    - element: LogonUser
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q12
      notes: "Limit to service accounts or privileged users. Realized via the user-actor baseline (subject pivot) and the service-principal baseline substitute (launch-mechanism pivot)."
    - element: RemoteHostList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Allowlist known admin jumpboxes. Realized as the destination-expectation judgment in remote_access_tool.connection_destination."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). Image-pivot adapted to the CommandLine ' -R ' reverse-forward pattern per the CommandLine-only hint, since the rule has no Image field."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). Image-pivot adapted to the CommandLine ' -R ' reverse-forward pattern per the CommandLine-only hint."
      kept: both
    - dropped: cue_process_tool.baseline
      reason: "generic Image|endswith host baseline depends on a rule Image that does not exist (CommandLine-only rule). Host-history of the firing pattern is subsumed by the user-actor baseline_svc (parent + ' -R ' pattern scoped) and the cross-host scope question; a bare host+binary baseline would key on the wrong field."
      kept: cue_user_actor.baseline_svc + cue_process_tool.scope
    - dropped: cue_process_tool.clustering
      reason: "Image-only host aggregation subsumed by cue_host_context.clustering, which projects Image+ParentImage and surfaces novel parent-child pairs"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline_deviation
      reason: "REFLECTED host-pattern question overlaps the fleet-wide cue_process_tool.scope (also REFLECTED) and the user-actor baseline_svc; the per-host parent/user distribution it would add is already carried by baseline_svc (parent + ' -R ') and cue_host_context.clustering. Trimmed to stay within the 14-18 target while keeping the fleet-wide scope view."
      kept: cue_process_tool.scope
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "follow_on scopes children by ParentImage|endswith %Image%, which depends on a constraining rule Image that does not exist here (CommandLine-only). Post-launch host activity is covered by lateral_movement.target_activity_after_access (+30m host scope) and cue_host_context.clustering. Trimmed to stay within the 14-18 target."
    - template: lateral_movement.source_host_identification
      reason: "must_include lateral_movement template, but it queries INBOUND connections to the target on SMB/RDP/WinRM/SSH ports. This rule fires on an OUTBOUND reverse tunnel established from this host; the originating-host question is inverted here. The outbound SSH destination is covered by remote_access_tool.connection_destination instead. Trimmed to stay within the 14-18 target."
    - template: lateral_movement.authentication_method
      reason: "must_include lateral_movement template; its pass-the-hash/Kerberos framing applies to a direct lateral-movement logon, not to an outbound port-forward process. The relayed-logon angle is covered more precisely by attack:T1021.001.remote_logon_correlation (Security 4624/4625/4648 on this host). Trimmed to stay within the 14-18 target."
    - template: network_scope.same_action_other_hosts
      reason: "recommended; same_action keys on Image|endswith %Image% which does not exist (CommandLine-only rule). Cross-host spread of the actual firing pattern is covered by cue_process_tool.scope (REFLECTED fleet-wide ' -R ' count) and cue_user_actor.clustering_svc. Trimmed to stay within the 14-18 target."
    - template: network_scope.actor_activity_network_wide
      reason: "recommended; duplicates cue_user_actor.clustering (User|expand, host+Image aggregation, +/-1h). Kept the cue version which is paired with its service-principal substitute. Trimmed to stay within the 14-18 target."
    - template: network_scope.actor_authentication_trail
      reason: "recommended; its 4624/4625 TargetUserName trail across all hosts overlaps attack:T1021.001.remote_logon_correlation (host-scoped 4624/4625/4648 around the tunnel) for the relayed-logon angle. Network-wide auth trail trimmed to stay within the 14-18 target; the host-scoped logon correlation is the load-bearing one here."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
    - lateral_movement
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      security_log: 1
      sigma_alert: 1
