name: Suspicious MsiExec Embedding Parent
id: 7eef10b0-57af-4c0e-8065-c489e3e8cb56
description: |
  Detects powershell.exe, pwsh.exe, or cmd.exe spawned by a parent whose command line contains both MsiExec.exe and the -Embedding flag. msiexec.exe running with -Embedding is the COM out-of-process server mode that Windows Installer uses during package installation; a script interpreter launched from that context is a known proxy-execution pattern where an MSI package carries an embedded payload that shells out to the command line. Legitimate installers occasionally invoke command interpreters this way (the rule already excludes the DismFoDInstall and Splunk forwarder cases), so the investigation should determine what the parent MSI was, what the interpreter actually ran, and whether the package was retrieved from a remote or untrusted source.
type: detection
detection_id: 4a2a2c3e-209f-4d01-b513-4155a540b469
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the parent command line contain both MsiExec.exe and the -Embedding flag, with a script interpreter child?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when the child Image ends in powershell.exe, pwsh.exe, or cmd.exe AND the parent command line contains both MsiExec.exe and -Embedding, and none of the installer exclusions match. Read the full event and verify both parent tokens are present and the child is the expected interpreter.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentCommandLine
      - Image
- source: template:universal.host_user_context
  question: Is this user and host expected to install MSI packages that shell out to command interpreters?
  context: Legitimate software installation through Windows Installer can spawn command interpreters, especially during custom action steps. Knowing whether this host routinely receives MSI-packaged software and whether the user is performing an expected install is often the fastest path to distinguishing a routine deployment from proxy execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: What is the full parent MsiExec.exe command line, and what started msiexec?
  context: The parent-child relationship is the core signal for this detection. The -Embedding flag means msiexec is running as a COM server during an installation; tracing the parent's own command line and its grandparent reveals whether this was a user-initiated install (double-clicked MSI, software deployment agent) or an unexpected chain where msiexec was driven by another process to proxy execution.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did this same MsiExec.exe parent spawn?
  context: An installer that legitimately runs one custom action often runs several. If the msiexec parent spawned only a single script interpreter that performed download or execution behavior, that is more suspicious than a parent that spawned a coherent set of install-related helpers. Multiple suspicious children from the same parent points to an MSI carrying a payload rather than a benign package.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.mechanism_decode
  question: What MSI package was msiexec installing, and what triggered it to run in -Embedding COM-server mode?
  context: The -Embedding flag indicates msiexec was launched as a COM out-of-process server by another process or by the installer service. Identifying the MSI package path or product, and what process drove msiexec into this mode, reveals whether the interpreter child came from a legitimate install custom action or from a package staged to proxy execution.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: template:powershell.encoded_command_decode
  question: What did the interpreter child actually run? Decode any encoded or obfuscated command line.
  context: The interpreter's command line reveals what the MSI's custom action executed. Decode any -EncodedCommand or -enc base64, deobfuscate backtick or concatenation tricks, and identify download cradles, reverse shells, or in-memory .NET assembly loads. A package that legitimately needs an interpreter usually runs short, readable setup commands; encoded or staged payloads are the deviation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1218.csv_command_analysis
  question: Does the interpreter command line reference remote MSI packages, UNC paths, HTTP/HTTPS URLs, or DLL execution flags?
  context: Proxy execution through a trusted signed binary typically carries the payload reference on the command line. Look for UNC paths, http or https URLs, raw IP destinations, .dll entrypoints, or remote .msi references in the interpreter's arguments. These argument patterns distinguish a package retrieving and executing external content from a self-contained local install step.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - ParentCommandLine
- source: attack:T1218.007.image_load
  question: What modules or DLLs did the interpreter and msiexec load shortly after this execution?
  context: Msiexec proxy execution frequently completes by loading an unsigned or attacker-supplied DLL, or by the interpreter side-loading a payload module. Reviewing image-load events for these processes around the alert surfaces module loads from user-writable or temporary directories and unsigned modules that indicate malicious installation rather than a benign package. If image-load telemetry is not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.007.network_connection
  question: Did the interpreter or msiexec make network connections to previously unseen or external destinations around this time?
  context: The msiexec abuse chain links process creation with network retrieval of a payload to previously unseen destinations. Correlating outbound connections from these processes in the alert window with the time of execution shows whether external content was fetched. Connections to external domains or raw IP addresses, especially ones the host has not contacted before, point toward remote payload retrieval rather than a local install.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script content?
  context: Script block logging records the deobfuscated script content PowerShell actually executed, even when the command line was encoded or obfuscated by the MSI custom action. This is ground truth about what ran. If PowerShell operational logs are not ingested in this environment, treat the absence of results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened before this execution?
  context: A package that proxies execution often weakens controls first. Look for AMSI bypass, execution policy changes, or a PowerShell v2 downgrade in the hour before the interpreter ran. These precursor events strengthen the case that the MSI carried a payload rather than performing a benign install step.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has this msiexec parent normally spawned this interpreter on this host before?
  context: Some installers routinely shell out to command interpreters during custom actions, while others never do. Historical frequency of an msiexec-in-embedding parent spawning this specific interpreter on this host distinguishes a recurring deployment pattern from a first-time, novel spawning relationship worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run command interpreters launched from MSI installs?
  context: Compare this user's current action against their historical pattern. A user who routinely deploys MSI-packaged software may legitimately trigger interpreter spawns during installation, while a user who has never done so is a stronger deviation. User role and typical toolset should align with installing software that shells out to the command line.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this msiexec parent and interpreter child combination historically routine on this host or across the environment?
  context: When the alert fires under a service or installer account, user identity does not attribute behavior. The meaningful pivot is the launch mechanism, namely whether this msiexec parent spawning this interpreter is historically common. A new or rare pairing on this host or environment-wide is the deviation; a pairing that already accounts for many prior events is software-deployment automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar msiexec-driven execution on multiple hosts, the scope may be a wider software-push or a multi-host operation rather than an isolated install.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same msiexec parent and interpreter child fire on other hosts in the same window?
  context: Under a service or installer account, asking what else the user did returns agent-telemetry noise. The meaningful question is whether the same launch mechanism fired on other hosts in the alert window. Multiple hosts in the same window points to a scheduled or pushed software deployment; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host, such as defense tampering, reconnaissance, or credential access, may reveal the attack stages that preceded this execution and help reconstruct the timeline around the msiexec proxy event.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1218.007
    name: Msiexec
    role: primary
  - id: T1218
    name: System Binary Proxy Execution
    role: secondary
  attack_chain_coverage:
  - stage: 1
    label: process_creation_with_suspicious_parameters
    description: msiexec.exe (in -Embedding COM-server mode) spawns a script interpreter; identify the parent package and the interpreter command line
    mapped_to: Q5
    source: template:cue_parent_process.mechanism_decode
  - stage: 2
    label: network_retrieval
    description: Network activity to retrieve the payload from a remote or previously unseen destination
    mapped_to: Q9
    source: attack:T1218.007.network_connection
  - stage: 3
    label: module_load
    description: Module loads indicative of malicious installation or DLL execution following the proxy execution
    mapped_to: Q8
    source: attack:T1218.007.image_load
  dimension_coverage:
  - dimension: time_correlation
    technique: T1218.007
    covered_by: Q9
    source: attack:T1218.007.network_connection
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1218.007
    covered_by: Q7
    source: attack:T1218.csv_command_analysis
    coverage_type: direct
  - dimension: command_analysis
    technique: T1218
    covered_by: Q6
    source: template:powershell.encoded_command_decode
    coverage_type: direct
  - dimension: parent_process_analysis
    technique: T1218
    covered_by: Q3
    source: template:parent_child.parent_process_legitimacy
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1218
    covered_by: Q7
    source: attack:T1218.csv_command_analysis
    coverage_type: direct
  mutable_dispositions:
  - element: SuspiciousCommandlinePatterns
    dimension: tuning_parameter
    specific_values: 'UNC paths, external domains, DLL execution flags, HTTP/HTTPS URLs, remote MSI references'
    disposition: new_question
    target_question: Q7
    notes: Drove attack:T1218.csv_command_analysis — interpreter argument patterns for remote/DLL references named in question and context with exact pattern classes.
  - element: SuspiciousDestinationList
    dimension: tuning_parameter
    specific_values: 'rationale only — external domains / IP ranges, no concrete list'
    disposition: context
    target_question: Q9
    notes: No concrete values in brief; framed in network_connection question context as external domains or raw IPs not previously contacted by the host.
  - element: TimeWindow
    dimension: time_correlation
    specific_values: 'rationale only — correlation window in seconds/minutes'
    disposition: context
    target_question: Q9
    notes: Realized as the +/-10m correlation range on the network_connection and image_load questions linking process creation to network/module activity.
  - element: LegitimateMSIHashes
    dimension: tuning_parameter
    specific_values: 'rationale only — known-good MSI hash allowlist'
    disposition: context
    target_question: Q3
    notes: No concrete hashes; addressed in the parent-process question context as identifying the MSI package to assess whether it is a known-good deployment.
  - element: ParentProcessName
    dimension: parent_process_analysis
    specific_values: 'rationale only — unexpected parent-child profiling'
    disposition: query_injection
    target_question: Q3
    notes: Parent profiling realized via ParentProcessGuid lookup and parent baseline questions.
  - element: SignedBinaryList
    dimension: tuning_parameter
    specific_values: 'rationale only — msiexec.exe is the in-scope signed binary'
    disposition: context
    target_question: Q5
    notes: msiexec is the trusted signed binary being proxied; named in description and mechanism question context.
  - element: CommandLineRegex
    dimension: command_analysis
    specific_values: 'rationale only — URL/script/DLL entrypoint matching'
    disposition: new_question
    target_question: Q7
    notes: Realized as the interpreter argument-pattern question (URLs, UNC, DLL entrypoints).
  - element: RemoteDomainAllowlist
    dimension: tuning_parameter
    specific_values: 'rationale only — enterprise-domain suppression filter'
    disposition: context
    target_question: Q9
    notes: Framed as previously-unseen vs known destinations in the network_connection question context.
  dedup_decisions:
  - dropped: cue_process_tool.mechanism_decode
    reason: powershell.exe too common for Image+host scoping; Phase 2 decode is served by powershell.encoded_command_decode (ProcessGuid-scoped) which is more specific for this interpreter set.
    kept: powershell.encoded_command_decode
  - dropped: cue_parent_process.follow_on
    reason: parent_child.sibling_processes (ParentProcessGuid-scoped) is more precise than the ParentName-scoped follow_on and covers the same other-children-of-this-parent intent.
    kept: parent_child.sibling_processes
  - dropped: cue_parent_process.clustering
    reason: same-time parent activity is covered by sibling_processes (children of the exact parent instance) and host_context.precursor (host timeline); ParentName-scoped clustering would duplicate without adding precision.
    kept: parent_child.sibling_processes
  - dropped: cue_parent_process.scope
    reason: cross-host spread of the parent+child pair is covered by cue_user_actor.clustering_svc and baseline_svc, which pivot on the same parent+binary pair env-wide.
    kept: cue_user_actor.clustering_svc
  - dropped: cue_process_tool.baseline
    reason: host-level binary baseline subsumed by cue_parent_process.baseline (parent+child pair on host) which is more specific to the detected relationship.
    kept: cue_parent_process.baseline
  - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair
    reason: user_actor pair — non-deduplicable; both emitted (user-pivot and parent+binary substitute).
    kept: both
  - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair
    reason: user_actor pair — non-deduplicable; both emitted (user-pivot and parent+binary substitute).
    kept: both
  skipped_templates:
  - template: cue_process_tool.process_chain
    reason: 'parent_process is primary cue (brief hint) — parent_child.parent_process_legitimacy and cue_parent_process.mechanism_decode cover the parent chain more specifically.'
  - template: obfuscation.encoded_payload_decode
    reason: 'replaced_by powershell.encoded_command_decode (brief hint) — the powershell technique-set decode supersedes the generic obfuscation decode.'
  - template: universal.endpoint_protection_health
    reason: Generic host-tamper sweep; powershell.powershell_security_bypass already surfaces AMSI/policy/downgrade precursors and cue_host_context.precursor carries prior-alert tamper context. This is a proxy-execution rule, not a hands-on-keyboard impact rule, so the dedicated bypass question is sufficient.
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - powershell
  - parent_child
  generation_stats:
    total_questions: 17
    source_counts:
      template: 14
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 13
      powershell: 1
      image_load: 1
      network_connection: 1
      sigma_alert: 1
