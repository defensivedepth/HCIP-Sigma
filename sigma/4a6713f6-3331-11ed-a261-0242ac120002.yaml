name: Taskkill Symantec Endpoint Protection
id: 883f2870-197b-4234-96bb-5745e8cc2e7c
description: |
  Detects a taskkill command line that force-terminates the Symantec Endpoint Protection
  service host process ccSvcHst.exe — the command line contains taskkill together with the
  /F and /IM flags and the process name ccSvcHst.exe. Forcibly killing the AV service host
  disables endpoint protection without uninstalling it, which is a common precursor to
  payload execution, credential access, or ransomware deployment; it can also appear during
  legitimate SEP uninstall or troubleshooting performed by IT. The investigation should
  determine whether protection was actually torn down, who or what issued the command, and
  what activity followed once the service was stopped.
type: detection
detection_id: 4a6713f6-3331-11ed-a261-0242ac120002
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects — taskkill with /F and /IM targeting ccSvcHst.exe?
  context: Confirm the alert is a true positive before investing further time. Review the exact command line of the triggering event and verify it contains taskkill, the /F (force) and /IM (image name) flags, and the target process ccSvcHst.exe, which is the Symantec Endpoint Protection service host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to stop the Symantec Endpoint Protection service from the command line?
  context: Legitimate SEP uninstall, repair, or troubleshooting can issue this exact command, typically from IT staff or a deployment account on managed endpoints. Knowing whether the user role and host function are consistent with manual AV service management is often the fastest way to separate routine maintenance from tampering.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Which Symantec process was targeted, and was this part of a sequence of taskkill commands against AV components?
  context: Force-killing ccSvcHst.exe stops the core SEP service host and leaves the endpoint without active protection. Read the full command line to confirm the exact target process and the flags used, and note whether the same operation also targeted related Symantec components such as Smc.exe, SmcGui.exe, or PAUI.exe, since teardown sequences often kill several processes in turn.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is Symantec Endpoint Protection currently running again on this host, or is it still stopped?
  context: Killing the service host with /F is a runtime action — the service may auto-restart, or it may stay down until manually restarted or the host reboots. Confirm the current protection state; if ccSvcHst.exe is not running, the host remains unprotected and the service should be restored before further investigation continues.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned the taskkill command, and is that parent chain expected?
  context: The parent process reveals how the kill command was invoked. A parent of explorer.exe or an interactive cmd.exe points to hands-on activity; parents such as a deployment agent or scripting host (wscript.exe, powershell.exe) point to automation; unusual parents like mshta.exe or an Office application suggest the command was launched as part of an intrusion.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this taskkill-against-ccSvcHst pattern, what parents and users drove it, and does the current alert fit?
  context: Even routine SEP maintenance leaves a historical signature. The query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has run here before. A new parent process, an unexpected user account, or a one-off where prior activity was scripted is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - taskkill
        - ' /F '
        - ' /IM '
        - ccSvcHst.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the Symantec service was killed?
  context: Defenses are disabled to enable something — payload execution, credential dumping, or other actions that protection would normally block or log. Examine process activity in the window immediately after the service host was stopped; new execution clustered right after the kill points to what the tampering was meant to enable.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host around the same time?
  context: An operator who kills one AV component often disables several controls — stopping additional services, clearing event logs, deleting shadow copies, or disabling Defender — to maximize coverage. Finding multiple tampering events from the same user or parent process indicates a deliberate teardown rather than an isolated administrative action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1562.001.registry_set
  question: Were security-product service registry keys disabled or deleted on this host around the alert?
  context: Killing the service host stops protection at runtime, but durable teardown often also edits the service registry — setting the Start value to disabled or removing the service key so it does not come back after reboot. This surfaces modifications to AV/EDR service registry paths, which are not visible from command-line monitoring alone. Populated rows alongside the kill command point to a deliberate, persistent disable rather than a one-time stop.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_service_keys:
        TargetObject|contains:
          - '\Services\SepMasterService\'
          - '\Services\SmcService\'
          - '\Services\ccSvcHst\'
          - '\Services\SAVService\'
          - '\Services\SAVAdminService\'
          - '\Services\WinDefend\'
          - '\Services\Sense\'
          - '\Services\SecurityHealthService\'
      condition: selection_host and selection_service_keys
    fields:
      - TargetObject
      - Image
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally stop Symantec services or run AV teardown commands?
  context: Compare this user's current action against their historical behavior. The query counts how often this user has issued the taskkill-against-ccSvcHst pattern across all hosts; a user who has never run it is more suspicious than a deployment or IT account that does so as part of routine SEP maintenance. User role and typical toolset should align.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
        - taskkill
        - ' /F '
        - ' /IM '
        - ccSvcHst.exe
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is the kill of ccSvcHst.exe historically routine across the environment, or is this firing pattern rare?
  context: When the alert fires under a service principal, user identity does not attribute the behavior; the meaningful pivot is whether this exact command pattern is part of established automation. The query counts the rule's firing pattern environment-wide, grouped by host. A pattern that already accounts for many prior events across managed hosts is deployment or maintenance automation; a rare or first-time pattern is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - taskkill
        - ' /F '
        - ' /IM '
        - ccSvcHst.exe
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated action on multiple endpoints. If the same user disabled protection or ran other commands on several hosts in this window, the scope of the activity is larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same kill-ccSvcHst command pattern fire on other hosts in this window — a coordinated rollout, or isolated to this host?
  context: Under a service principal, asking what the user did network-wide returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether this same command pattern fired on other hosts in the alert window. Multiple hosts at once points to scripted or pushed teardown ahead of a wider operation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - taskkill
        - ' /F '
        - ' /IM '
        - ccSvcHst.exe
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the broader set of processes around the kill command reveals what led up to it and what followed — reconnaissance, privilege escalation, or persistence installation that frames the disabling of protection within a larger sequence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, credential access, or other defense tampering — may reveal the attack stages that preceded the service kill and help reconstruct the timeline. Disabling protection is frequently a mid-chain step rather than the first action.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this kill-ccSvcHst pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of this firing pattern rather than generic taskkill usage. Many hosts running the same pattern points to scripted deployment or SEP-removal automation; one host or a small unrelated set points to manual operator activity. Use the host spread to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - taskkill
        - ' /F '
        - ' /IM '
        - ccSvcHst.exe
      condition: selection
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were process-based persistence mechanisms installed on this host around the alert?
  context: Disabling protection is often paired with establishing or refreshing persistence so access survives a reboot. After tearing down AV, operators commonly install scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions. Persistence tooling running in the same timeframe as the service kill indicates deliberate footholding rather than isolated maintenance.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tool_termination
      description: "Adversary force-terminates the AV/EDR service process (taskkill /F /IM ccSvcHst.exe) to disable endpoint protection"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: protection_state
      description: "Defenders observe abnormal absence of expected telemetry / stopped protection service; confirm current state"
      mapped_to: Q4
      source: template:defense_tampering.protection_status
    - stage: 3
      label: registry_or_service_disable
      description: "Durable disable via service registry modification (Start value / key deletion) so protection does not return after reboot"
      mapped_to: Q9
      source: attack:T1562.001.registry_set
    - stage: 4
      label: post_tamper_activity
      description: "Activity the tampering was meant to enable runs once protection is down"
      mapped_to: Q7
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q6
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q10
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562
      covered_by: Q8
      source: template:defense_tampering.other_tampering_activity
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562
      covered_by: Q17
      source: template:cue_process_tool.scope
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "No concrete values in brief — described as a list of expected admin tools to suppress false positives. The host-scoped baseline_deviation question covers the same intent: identify which parents/users are the expected drivers so anomalies stand out."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "No concrete threshold in brief — defines the correlation window linking termination, registry edits, and service stop. Realized as the +1h post-tamper window and the +/-1h other-tampering window."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: new_question
      target_question: Q9
      notes: "No concrete list in brief (customizable per deployment). Concrete SEP/AV service key names were authored into the registry_set question (SepMasterService, SmcService, ccSvcHst, SAVService, WinDefend, Sense, etc.) to give the abstract element real values."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "No concrete values — authorized admin tools that legitimately stop services. Covered as the legitimate-context framing in what_was_disabled and the user/host expectation question."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "No concrete values in brief; realized as the explicit AV/EDR service-key list in the registry_set question."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Identical host-scoped same-window clustering intent as cue_host_context.clustering; host_context version kept (groups by Image+ParentImage which is the richer projection)."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by defense_tampering.what_was_disabled (more specific Phase 2 question for a tampering rule)"
      kept: defense_tampering.what_was_disabled
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable: Q10 pivots on user identity, Q11 on the firing-pattern (launch mechanism). Both emitted per service-principal degeneration handling."
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable: Q12 pivots on user identity network-wide, Q13 on the firing-pattern across hosts. Both emitted."
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled"
    - template: cue_process_tool.baseline
      reason: "Image|endswith|expand on %Image% — rule is CommandLine-only with primary_binary: null, so the binary baseline degenerates to an empty filter. The CommandLine-pattern baseline (baseline_deviation, host-scoped) and the user/fleet pattern baselines cover historical frequency with a filter that actually matches."
    - template: cue_process_tool.follow_on
      reason: "ParentImage|endswith|expand on %Image% — null Image (CommandLine-only rule) makes the children-of-this-binary query degenerate. Post-event execution is covered by activity_after_tampering (host-scoped +1h), which is the load-bearing post-kill question here."
    - template: persistence_scope.file_based_persistence
      reason: "is_persistence_rule: false and persistence_scope cascaded only as a recommended set. process_based_persistence is kept as the persistence representative; file-drop persistence is a secondary angle not specific to AV teardown, dropped to stay within the 18-question cap."
    - template: universal.endpoint_protection_health
      reason: "Recommended generic host-tamper sweep. defense_tampering technique set is confirmed for this rule and its other_tampering_activity question (Q8) is the more specific equivalent; per the template's own drop guidance, the confirmed defense_tampering set supersedes this generic sweep. Declining avoids a near-duplicate of Q8."
  reflection_overrides: []
  hint_dispositions:
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted_on
      reason: >-
        Drove the skip of cue_process_tool.baseline and cue_process_tool.follow_on
        (both pivot on null %Image%) and the adaptation of cue_user_actor.baseline
        and both _svc variants to filter on the rule's CommandLine|contains|all
        pattern (taskkill / /F / /IM / ccSvcHst.exe) instead of Image. Recorded
        here for audit since it manifested as query rewrites and template skips.
    - hint: "logsource 'registry_set' (recommended by techniques: T1562.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Authored Q9 (attack:T1562.001.registry_set) with category: registry_set,
        querying AV/EDR service registry keys (SepMasterService, SmcService,
        ccSvcHst, SAVService, WinDefend, Sense, SecurityHealthService) on this
        host. This is the cross-logsource pivot the technique calls for — a runtime
        process kill plus a service-key Start/disable or deletion is the durable
        teardown that the CommandLine-only rule cannot see on its own.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - persistence_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      registry_set: 1
      sigma_alert: 1
