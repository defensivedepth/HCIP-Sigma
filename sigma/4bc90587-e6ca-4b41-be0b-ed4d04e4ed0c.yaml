name: Suspicious Velociraptor Child Process
id: 13907ee2-7f9d-401d-a141-0624ca012c32
description: |
  Detects Velociraptor.exe spawning a child process that matches one of three patterns: a Visual Studio Code tunnel
  launch (code.exe with tunnel and --accept-server-license-terms), an msiexec install pulling an MSI over HTTP
  (msiexec /i http...), or a PowerShell download cradle (Invoke-WebRequest, IWR, .DownloadFile, .DownloadString).
  The rule fires when the Velociraptor parent is present AND any one of these child branches matches, so the analyst
  should first note which branch fired. Velociraptor is a legitimate open-source DFIR/collection tool, so the parent
  alone is not malicious; the suspicious signal is a legitimate response agent being used to fetch additional payloads,
  install software, or stand up a remote-access tunnel. Determine whether the activity maps to a sanctioned IR engagement
  or deployment, or whether the tool is being repurposed for foothold and remote execution.
type: detection
detection_id: 4bc90587-e6ca-4b41-be0b-ed4d04e4ed0c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does this Velociraptor child process match one of the rule's three child patterns?
  context: >-
    The rule fires when Velociraptor.exe is the parent AND at least one child branch matches: a VS Code tunnel launch
    (code.exe with 'tunnel' and '--accept-server-license-terms'), an msiexec HTTP install ('msiexec' with '/i' and
    'http'), or a PowerShell download cradle (powershell/powershell_ise/pwsh running 'Invoke-WebRequest', 'IWR',
    '.DownloadFile', or '.DownloadString'). Confirm the parent is Velociraptor.exe and note which of the three branches
    fired, because the branch determines which downstream questions carry the most weight.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to be running Velociraptor and to spawn installers, tunnels, or download cradles from it?
  context: >-
    Legitimate Velociraptor deployments run as a managed collection agent under a known service account or during a
    sanctioned IR engagement on a defined set of hosts. If this host is not part of a known Velociraptor deployment, or
    the account is not one that normally drives DFIR collection, the parent-child chain is harder to explain as routine.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Is Velociraptor expected to spawn this child, and what started Velociraptor itself?
  context: >-
    The Velociraptor-to-child relationship is the core signal. Velociraptor normally executes collection artifacts and
    its own helper binaries, not msiexec installers, VS Code tunnels, or PowerShell download cradles. Trace the parent's
    own origin to see whether Velociraptor was deployed through a sanctioned mechanism (managed agent install, IR
    engagement) or dropped and run by an unexpected grandparent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the spawned child command do? Decode its full command line, flags, URLs, and target paths.
  context: >-
    Parse the full child command line. For the msiexec branch, identify the HTTP source of the MSI and any properties
    passed. For the VS Code branch, confirm the tunnel flags and any service-install or output-redirection arguments. For
    the PowerShell branch, extract the source URL and the local path the payload is saved or executed from. The download
    cradle and tunnel arguments reveal the operational intent of the chain.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%ParentImage%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: If the PowerShell branch fired, was the command encoded or obfuscated, and what does it decode to?
  context: >-
    PowerShell child processes in this chain frequently arrive as -EncodedCommand (-enc) base64 payloads or use download
    cradles such as IWR/Invoke-WebRequest piped to execution, or Net.WebClient .DownloadFile/.DownloadString. Decode any
    base64 and resolve the cradle to identify the source URL being fetched and what gets executed or written to disk.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script content?
  context: >-
    Script block logging (Event ID 4104) records the deobfuscated script PowerShell actually executed, even when the
    command line is encoded. This is ground truth for what the download cradle did. If PowerShell operational logs are
    not ingested in this environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before the child ran?
  context: >-
    Operators often weaken PowerShell controls before running a download cradle: AMSI patching, execution policy bypass
    (-ExecutionPolicy Bypass / -ep bypass), or a v2 downgrade that lacks logging. Finding these precursor events on the
    host strengthens the case that the Velociraptor-spawned PowerShell was used for payload delivery rather than routine
    collection.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_artifact.artifact_verification
  question: For any file fetched or installed by the child, what is its hash and does it have known threat intel detections?
  context: >-
    The msiexec and PowerShell branches pull files (an MSI installer, a VS Code package, or a download-cradle payload)
    onto disk. Recover the dropped file paths from the command line, then look up the hash to determine whether the file
    is known malicious, known benign, or previously unseen. This answers whether the staged file is recognized, distinct
    from reading what is inside it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:parent_child.sibling_processes
  question: What other child processes did this same Velociraptor instance spawn?
  context: >-
    If this Velociraptor instance was repurposed, it likely spawned more than the one process that triggered the alert.
    Reviewing siblings of the same parent reveals the full scope of what was launched through this agent — additional
    installers, tunnels, shells, or reconnaissance tools point to hands-on operation rather than an isolated collection
    artifact.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has this Velociraptor parent spawned this child binary on this host before?
  context: >-
    A Velociraptor instance that has never previously spawned msiexec, code.exe, or a PowerShell interpreter on this host
    is a novel relationship. Zero or very low historical counts indicate a first-time spawning pattern, which is the
    deviation worth chasing; a high count suggests an established collection or deployment routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:remote_access_tool.tool_authorization
  question: Is Velociraptor on this organization's approved tooling list for this host?
  context: >-
    Velociraptor is a legitimate DFIR and endpoint-collection tool, but it is also a full remote-execution capability. If
    it is not part of a sanctioned deployment or an active IR engagement covering this host, its presence may represent
    shadow tooling or an attacker repurposing a known-good binary for remote access and payload staging.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there an active IR engagement, helpdesk ticket, or maintenance window explaining this Velociraptor activity?
  context: >-
    Sanctioned Velociraptor use typically maps to a documented IR engagement, a managed deployment, or an approved
    collection effort with a defined scope and timeframe. If no such context exists for this host and time window, the
    agent spawning installers, tunnels, or download cradles is harder to attribute to authorized work.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:remote_access_tool.connection_destination
  question: What external endpoints did the child process and any installed tunnel connect to?
  context: >-
    The chain's endpoints reveal intent: an MSI or payload pulled from an unfamiliar host, or a VS Code tunnel exposing
    the machine through external tunneling infrastructure, both establish outbound or remote-access channels. Compare the
    destinations against expected update, vendor, and management infrastructure; connections to unfamiliar staging hosts
    or tunnel relays are the load-bearing signal for this remote-access chain.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: attack:T1219.process_identification
  question: Was a service or autorun entry installed on this host that would relaunch the staged tool after reboot?
  context: >-
    Remote-access chains commonly persist the staged tool by registering it as a Windows service or writing an autorun
    registry value, so the tunnel or agent survives reboot. Look for service-install or Run/RunOnce registry writes around
    the alert time, and tie the target image back to the file the child fetched. A persistence write that points at a
    recently staged binary is a strong escalation signal.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        TargetObject|contains:
          - '\Services\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_user_actor.clustering
  question: What else did this account do across the environment around the time of the alert?
  context: >-
    Tracking the account's activity across all hosts in the alert window reveals whether the Velociraptor abuse is
    isolated or part of broader hands-on activity. Similar staging, installation, or tunnel activity under the same
    account on other hosts widens the scope of the engagement or compromise.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \Velociraptor.exe
      selection_child_vscode_tunnel:
        CommandLine|contains|all:
        - code.exe
        - tunnel
        - --accept-server-license-terms
      selection_child_msiexec:
        CommandLine|contains|all:
        - msiexec
        - /i
        - http
      selection_child_powershell:
        Image|endswith:
        - \powershell.exe
        - \powershell_ise.exe
        - \pwsh.exe
        CommandLine|contains:
        - 'Invoke-WebRequest '
        - 'IWR '
        - .DownloadFile
        - .DownloadString
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection_parent and 1 of selection_child_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or the
    initial delivery that installed Velociraptor — may reveal the stages that preceded this event and help reconstruct
    the timeline leading up to the remote-access activity.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219
      name: Remote Access Tools
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: initial_execution
      description: "Initial execution of remote-control/collection agent (Velociraptor) under user/service context"
      mapped_to: Q3
      source: template:parent_child.parent_process_legitimacy
    - stage: 2
      label: persistence
      description: "Persistence via service or autorun for the staged tool (e.g., code.exe installed as a Windows service)"
      mapped_to: Q14
      source: attack:T1219.process_identification
    - stage: 3
      label: outbound_tunnel
      description: "Long-lived outbound connection/tunnel to external infrastructure (VS Code tunnel, MSI/payload fetch)"
      mapped_to: Q13
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: interactive_control
      description: "Interactive control signals: shell/installer/tunnel child processes spawned by the RAT parent"
      mapped_to: Q9
      source: template:parent_child.sibling_processes
  dimension_coverage:
    - dimension: process_identification
      technique: T1219
      covered_by: Q14
      source: attack:T1219.process_identification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1219
      covered_by: Q9
      source: template:parent_child.sibling_processes
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219
      covered_by: Q13
      source: template:remote_access_tool.connection_destination
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (default 15m, adjust per environment)"
      disposition: context
      target_question: Q9
      notes: "No specific queryable value. Sibling-process window (+/-15m) and clustering window (+/-1h) bind the start->persistence->egress->child correlation period."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (help-desk/jump/admin vs standard endpoints)"
      disposition: context
      target_question: Q2
      notes: "Help-desk/jump/admin distinction is judgment, not a field value; surfaced in host_user_context."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only (known-good remote support tools)"
      disposition: context
      target_question: Q11
      notes: "Allowlist membership is an organizational fact; tool_authorization question asks whether Velociraptor is sanctioned for this host."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "rationale only (alert when services/agents run from user-writable/temp paths)"
      disposition: context
      target_question: Q14
      notes: "No concrete regex provided. Persistence question surfaces service/autorun writes and ties the target image back to the staged file's path for the analyst to judge writability."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (vendor support clouds/CDNs)"
      disposition: context
      target_question: Q13
      notes: "No concrete IP/host list provided. connection_destination surfaces destinations for the analyst to compare against expected vendor/management infrastructure."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only (acceptable vs suspicious child shells from GUI parents)"
      disposition: context
      target_question: Q10
      notes: "No concrete regex provided. Parent baseline asks whether this Velociraptor parent has spawned this child binary before."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only (session duration, conn counts, bytes_out/in thresholds)"
      disposition: context
      target_question: Q13
      notes: "Byte/duration thresholds are not available as process_creation/network_connection selection values here; destination review carries the egress angle."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue — parent_child.parent_process_legitimacy (Q3) subsumes the process_chain question (both look up ParentProcessGuid); per hint."
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.mechanism_decode
      reason: "Parent's own event and 'what started the parent' are covered by parent_child.parent_process_legitimacy (Q3); avoids a second ParentProcessGuid lookup."
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_user_actor.baseline (and its baseline_svc pair)
      reason: "user_actor baseline pair declined to stay within the 18-question cap; the user-vs-host expectation is covered by host_user_context (Q2) and the launch-mechanism baseline by cue_parent_process.baseline (Q10). The clustering user_actor pair (Q15/Q16) is emitted in full and is the non-deduplicable pair retained."
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; BOTH emitted as Q15 (user pivot) and Q16 (parent+binary substitute) per AGENTS.md."
      kept: both
  skipped_templates:
    - template: cue_parent_process.follow_on
      reason: "Overlaps parent_child.sibling_processes (Q9) which covers other children of the same parent instance via ParentProcessGuid; follow_on uses the looser parent.name scope."
    - template: cue_parent_process.clustering
      reason: "Around-the-time parent activity is covered by sibling_processes (Q9, +/-15m) and host_context.precursor (Q17); a parent.name-scoped same-time question adds no distinct angle."
    - template: cue_parent_process.scope
      reason: "Cross-host parent+child distribution is covered by cue_user_actor.clustering_svc (Q16), which asks whether the same parent+binary launch fired on other hosts in the window."
    - template: cue_process_tool.process_chain
      reason: "replaced by parent_child.parent_process_legitimacy (recorded in dedup_decisions); parent_process primary per hint."
    - template: cue_process_tool.baseline
      reason: "Host-runs-this-tool baseline overlaps cue_parent_process.baseline (Q10), which is more specific to the alerting parent+child pair than binary-only frequency."
    - template: cue_artifact.mechanism_decode
      reason: "Content-of-file analysis overlaps cue_process_tool.mechanism_decode (Q4), which decodes the child command including the fetched URLs/paths; artifact hash/reputation is retained as artifact_verification (Q8)."
    - template: cue_artifact.baseline
      reason: "File-seen-before baseline overlaps cue_parent_process.baseline (Q10) and artifact_verification (Q8); the staged file is expected to be new by construction (download cradle), so a 90d file baseline adds little."
    - template: universal.evidence_preservation
      reason: "Recovering/hashing the fetched file is covered by cue_artifact.artifact_verification (Q8), which scopes to %file_paths% on file_event and returns Hashes."
    - template: universal.endpoint_protection_health
      reason: "Tamper-as-precursor is carried by powershell.powershell_security_bypass (Q7, which includes Set-MpPreference/AMSI bypass) plus cue_host_context.precursor (Q17). This is a remote-access/staging rule, not an execution/credential rule whose disposition turns on prior tamper; generic host-tamper sweep would not earn its slot under the 17-question budget."
    - template: organizational_context.change_ticket
      reason: "Authorization/ticket context for this remote-access tool is covered by remote_access_tool.active_session_context (Q12) and tool_authorization (Q11); a separate change-ticket question would duplicate."
    - template: organizational_context.human_recall
      reason: "Direct human confirmation overlaps remote_access_tool.active_session_context (Q12, IR-engagement/ticket recall); a separate human_recall slot is redundant for this rule."
    - template: organizational_context.timing_alignment
      reason: "Timing-vs-work-hours overlaps active_session_context (Q12, maintenance-window/engagement check) and is lower-yield than the remote-access-specific questions for this rule."
    - template: organizational_context.procedural_consistency
      reason: "Standard-procedure check overlaps tool_authorization (Q11); the discriminating question for a DFIR tool is whether it is sanctioned at all, not which deployment channel was used."
    - template: persistence_scope.process_based_persistence
      reason: "Process-tool persistence (schtasks/sc/reg/bitsadmin) overlaps the authored registry_set persistence question (Q14), which targets the service/autorun write the reference describes (code.exe installed as a service); registry_set captures the autorun mechanism directly."
    - template: persistence_scope.file_based_persistence
      reason: "Startup-folder/Run-path file drops overlap the registry_set persistence question (Q14) for autorun coverage; the reference describes service-install persistence, which Q14 targets via \\Services\\."
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: acted
      reason: >-
        Acted on. The child mechanism_decode (Q4) is scoped by parent (ParentImage|endswith %ParentImage% = Velociraptor)
        rather than Image|endswith powershell.exe, so the noisy-binary scoping is avoided by construction — the parent is
        the rare anchor. Encoded/cradle decoding is handled separately by powershell.encoded_command_decode (Q5), which is
        ProcessGuid-scoped to the single alerting event.
    - hint: "cue_artifact: derived: \\bDownloadFile\\b in CommandLine"
      disposition: acted
      reason: >-
        Acted on. cue_artifact.artifact_verification (Q8) uses %file_paths% derived from the CommandLine download cradle to
        recover and hash the fetched file.
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted
      reason: "Acted on. powershell.encoded_command_decode is included as Q5; obfuscation.encoded_payload_decode was not emitted."
    - hint: "parent_process is primary — drop cue_process_tool:process_chain"
      disposition: acted
      reason: "Acted on. cue_process_tool.process_chain dropped (see dedup_decisions/skipped_templates); parent_child.parent_process_legitimacy (Q3) carries the parent-chain question."
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted
      reason: "Acted on. artifact_verification (Q8) uses TargetFilename|endswith|expand %file_paths% rather than %TargetFilename%."
    - hint: "logsource 'registry_set' (recommended by techniques: T1219) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: acted
      reason: >-
        Acted on. Authored a custom registry_set question (Q14, source attack:T1219.process_identification) targeting
        service-install and Run/RunOnce autorun writes — the persistence stage of the T1219 chain (the reference describes
        code.exe being installed as a Windows service). This is genuinely cross-logsource: the rule fires on
        process_creation but the persistence write lands in registry_set.
    - hint: "Condition uses OR among selection groups: (selection_child_vscode_tunnel OR selection_child_msiexec OR selection_child_powershell). Q1 and the playbook description must frame this as 'EITHER ... OR ...', not 'X AND Y AND Z'."
      disposition: acted
      reason: >-
        Acted on. Q1 (verify_trigger_match) and the description use disjunctive framing: Velociraptor parent AND at least
        one of three child branches; Q1 context directs the analyst to note which branch fired so downstream questions can
        be weighted accordingly.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - remote_access_tool
    - parent_child
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
    phase_distribution:
      triage: 2
      analysis: 3
      context: 12
    logsource_diversity:
      process_creation: 12
      network_connection: 1
      file_event: 1
      registry_set: 1
      powershell: 1
      sigma_alert: 1
