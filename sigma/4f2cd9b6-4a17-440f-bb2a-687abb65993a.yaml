name: Uncommon AddinUtil.EXE CommandLine Execution
id: 3d19a80b-ba71-4ba9-b427-9a72620a6d8f
description: |
  Detects execution of the .NET Add-In deployment cache utility AddinUtil.exe with an -AddInRoot: or -PipelineRoot:
  parameter that points somewhere other than the standard VSTA shared-component path. AddinUtil.exe is a legitimate,
  Microsoft-signed binary, but it can be abused for proxy execution: pointing -AddInRoot: at a user-controlled folder
  that contains a crafted Addins.store file causes the add-in cache read to deserialize that file with BinaryFormatter,
  which executes attacker-supplied code under a trusted binary. The rule fires when the binary (by Image name or
  OriginalFileName) runs with one of those parameters AND the parameter does not resolve to the legitimate VSTA path.
  Investigation should confirm where the -AddInRoot:/-PipelineRoot: path resolves, whether a deserialization payload or
  child process followed, and whether the parent, user, and host are consistent with expected add-in maintenance.
type: detection
detection_id: 4f2cd9b6-4a17-440f-bb2a-687abb65993a
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: 'Did AddinUtil.exe run with an -AddInRoot: or -PipelineRoot: parameter that does not resolve to the standard VSTA path?'
  context: 'Confirm the alert is a true positive. The rule fires when the binary identifies as AddinUtil.exe (by Image name
    or OriginalFileName) AND the command line carries -AddInRoot: or -PipelineRoot: AND that parameter does not point at
    the legitimate path C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA. Read the exact command line and verify all
    three conditions hold before investing further time.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run AddinUtil.exe with a custom add-in root path?
  context: 'Legitimate AddinUtil.exe activity is normally driven by Office add-in maintenance and resolves to the standard
    VSTA shared path. A custom -AddInRoot:/-PipelineRoot: path is unusual for routine use. Knowing whether this user role
    and host function involve add-in development or deployment is often the fastest way to separate a benign developer
    workflow from proxy execution.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Proxy execution through a trusted binary is often a delivery step in a larger sequence, and operators frequently
    weaken AV/EDR before running a payload. This query surfaces direct attempts to disable protection services, flip Defender
    flags, add exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal; populated
    rows shift the disposition toward operator preparation, so review the parent process and signing chain of any matches.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_process_tool.mechanism_decode
  question: 'Where does the -AddInRoot:/-PipelineRoot: path resolve, and is it a user-writable location?'
  context: 'The parameter value is the heart of this technique. Decode the full -AddInRoot:/-PipelineRoot: argument and
    resolve it against the current working directory if it is relative (for example -AddInRoot:. or the quoted dot form). A path
    under Temp, Desktop, Downloads, Users\Public, or Windows\Temp is user-writable and consistent with a planted Addins.store
    payload; the standard, benign location is C:\Windows\Microsoft.NET\Framework or Framework64 or WinSxS. Note whether an
    Addins.store file exists at that path.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - '-AddInRoot:'
          - '-PipelineRoot:'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned AddinUtil.exe and is that parent chain expected?
  context: The parent process reveals how the binary was invoked. Routine add-in maintenance is typically launched from
    Office or installer/servicing processes. An unexpected parent such as a shell, script host, Office application running a
    macro, or an Electron/IDE process suggests the invocation was driven by an operator or a delivered document rather than
    normal servicing.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did AddinUtil.exe spawn afterward?
  context: A successful deserialization payload runs code under AddinUtil.exe, which most visibly manifests as an unexpected
    child process. Legitimate children are limited to conhost.exe and werfault.exe; anything else — a shell, script host,
    certutil, rundll32, or an arbitrary executable — points to code execution through the crafted add-in cache and is the
    strongest confirmation of impact.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.image_load
  question: What modules did AddinUtil.exe load, and were any loaded from a non-system or unsigned location?
  context: AddinUtil.exe is a .NET host that normally loads framework and add-in assemblies from system directories. Modules
    loaded from a user-writable path, or unsigned modules tied to this execution, indicate the process was steered to load
    attacker-supplied code rather than serviced add-ins. If image-load telemetry is not collected in this environment, treat
    zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.network_connection
  question: Did AddinUtil.exe initiate any network connections, and to where?
  context: AddinUtil.exe performing add-in cache maintenance has no routine reason to make network connections. An outbound
    connection from this process suggests the deserialized payload reached out to retrieve a stage or establish command and
    control. Review the destination against expected enterprise endpoints; a connection to an external or unfamiliar host is
    high-value confirmation of malicious use.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run AddinUtil.exe at all?
  context: AddinUtil.exe runs infrequently outside of add-in development and Office servicing. A host that has never or rarely
    executed this binary over the past 90 days makes the current execution stand out; a host with regular history suggests an
    established workflow worth confirming against the parameter path.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even a binary that appears occasionally can be abused. This query mirrors the rule's exact selection (AddinUtil.exe
    with -AddInRoot:/-PipelineRoot:, excluding the legitimate VSTA path) scoped to this host, so the resulting parent/user
    distribution reflects how this specific firing pattern has run here before. A new parent, an unexpected user account, or
    a first-ever occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_cli:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      filter_main_addinroot:
        CommandLine|contains:
        - -AddInRoot:"C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -AddInRoot:C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -PipelineRoot:"C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -PipelineRoot:C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection with no host scope, giving a fleet-wide count of this specific firing
    pattern rather than generic AddinUtil.exe usage. Many hosts firing the same pattern points to a scripted deployment or
    vendor automation worth allowlisting; one host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_cli:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      filter_main_addinroot:
        CommandLine|contains:
        - -AddInRoot:"C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -AddInRoot:C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -PipelineRoot:"C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
        - -PipelineRoot:C:\Program Files (x86)\Common Files\Microsoft Shared\VSTA
      condition: all of selection_* and not 1 of filter_main_*
    fields:
    - host.name
- source: template:cue_process_tool.clustering
  question: What other tools ran on this host around the same time?
  context: Proxy execution rarely stands alone. Activity surrounding the alert may reveal the document or script that
    delivered the payload, follow-on reconnaissance, credential access, or staging that frames the broader operation.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
- source: template:cue_user_actor.baseline
  question: Does this user normally run AddinUtil.exe across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never run AddinUtil.exe is
    more suspicious than one who does so as part of a known add-in development or packaging role. User role and typical
    toolset should align with custom add-in cache operations.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+AddinUtil.exe combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is
    the launch mechanism: which parent process spawned AddinUtil.exe, and is that pair historically common. A new or rare
    parent for this binary is the deviation worth chasing; a pair that already accounts for many prior events is more likely
    servicing or vendor automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If
    the same user drove similar activity on several hosts, the scope of the operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+AddinUtil.exe combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism — parent process plus spawned binary — fired on other hosts in
    the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, delivery of a document,
    reconnaissance, or credential access — may reveal the stages that preceded this proxy execution and help reconstruct the
    full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: trusted_binary_invocation
      description: "AddinUtil.exe (a trusted Microsoft-signed binary) is invoked with -AddInRoot:/-PipelineRoot: pointing at a non-standard, user-controlled path"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_parameter_analysis
      description: "The -AddInRoot:/-PipelineRoot: parameter is decoded and resolved to determine whether it points at a user-writable location holding a crafted Addins.store payload"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_execution
      description: "Deserialization of the crafted add-in cache executes attacker code under the trusted binary, surfacing as unexpected child processes, module loads, or network connections"
      mapped_to: Q6
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q9
      source: template:cue_process_tool.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profile unexpected parent-child relationships)"
      disposition: context
      target_question: Q5
      notes: "process_chain question profiles the parent of AddinUtil.exe; parent+binary baseline (Q14) and same-window parent+binary clustering (Q16) extend the parent-process analysis. No specific parent value to inject."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (allowlist of signed binaries; here the rule's own filter encodes the legitimate VSTA path)"
      disposition: context
      target_question: Q10
      notes: "The rule's filter_main_addinroot already encodes the legitimate VSTA path allowlist; baseline_deviation (Q10) and scope (Q11) reflect that filter verbatim. Legitimate AddinUtil paths described in Q4 context."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "-AddInRoot:, -PipelineRoot: (rule commandline_values); user-writable path patterns Temp/Desktop/Downloads/Public/Windows\\Temp"
      disposition: query_injection
      target_question: Q4
      notes: "mechanism_decode query (ADAPT_STRUCTURALLY) injects CommandLine|contains -AddInRoot:/-PipelineRoot:; context enumerates user-writable suspicious path patterns vs legitimate Framework/Framework64/WinSxS paths."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q8
      notes: "network_connection question (Q8) asks where AddinUtil.exe connected; context directs analyst to review destination against expected enterprise endpoints (the allowlist concept). No fixed domain values in the rule to inject."
  dedup_decisions:
    - dropped: null
      reason: "user_actor pair — non-deduplicable (baseline): Q13 pivots on user identity, Q14 on launch mechanism (parent+AddinUtil.exe). Both emitted per AGENTS.md."
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: null
      reason: "user_actor pair — non-deduplicable (clustering): Q15 pivots on user identity across the network, Q16 on same-window parent+binary across hosts. Both emitted per AGENTS.md."
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_host_context.clustering
      reason: "Same-host process_creation around the alert is already covered by cue_process_tool.clustering (Q12, +/-15m, host-scoped Image aggregation). Dropping the near-identical host_context.clustering avoids a duplicate query and keeps the playbook within the 18-question cap; precursor (alert-level prior detections) is retained as the distinct host-context contribution."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted-on
      reason: >-
        Authored a custom image_load question (Q7, source attack:T1218.image_load)
        scoped to host.name + Image ending in addinutil.exe, projecting
        ImageLoaded/Signed/Signature. This covers the T1218 image_load analytic —
        a trusted .NET binary loading modules from non-system or unsigned locations
        when steered to execute attacker-supplied add-in code.
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted-on
      reason: >-
        Authored a custom network_connection question (Q8, source
        attack:T1218.network_connection) scoped to host.name + Image ending in
        addinutil.exe, projecting DestinationIp/DestinationPort/DestinationHostname.
        This covers the T1218 network_connection analytic and the documented
        behavior of AddinUtil.exe initiating outbound connections after a successful
        deserialization payload.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      image_load: 1
      network_connection: 1
      sigma_alert: 1
