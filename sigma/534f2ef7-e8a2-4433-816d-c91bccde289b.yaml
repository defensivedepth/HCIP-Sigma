name: Explorer NOUACCHECK Flag
id: 72089c23-2a73-4cf9-a656-98b9d9af6afc
description: |
  Detects explorer.exe launched with the /NOUACCHECK flag, which tells Explorer to run every child process it spawns
  without User Account Control consent prompts. The rule fires when an Image ending in \explorer.exe runs with /NOUACCHECK
  on its command line, excluding the benign Domain Controller logon case where Explorer is started by the scheduled-task
  svchost service (svchost.exe -k netsvcs -p -s Schedule). Setting /NOUACCHECK requires an already-elevated context, so on a
  workstation it is a means of laundering an existing high-integrity token into an interactive shell whose children inherit
  elevation silently. Investigation should confirm the flag is present, identify the elevated process that launched Explorer,
  and determine what ran at high integrity afterward.
type: detection
detection_id: 534f2ef7-e8a2-4433-816d-c91bccde289b
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting event show explorer.exe started with the /NOUACCHECK flag on its command line?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when both conditions hold together, an Image ending in \explorer.exe AND a command line containing /NOUACCHECK, with the Domain Controller scheduled-task logon parent excluded. Review the exact Image and command line in the triggering event and confirm the flag is literally present rather than a substring of an unrelated argument.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to relaunch Explorer with elevation-bypass flags?
  context: Setting /NOUACCHECK requires an already-elevated context, so a routine interactive user session does not produce this pattern on its own. Some imaging, deployment, or administrative tooling restarts the shell during privileged setup. Knowing whether the user role and host function involve privileged shell relaunches is often the fastest path to separating a deployment artifact from an operator laundering an elevated token.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What elevated context launched this explorer.exe, and what command line and integrity level did the alerting Explorer carry?
  context: The /NOUACCHECK launch only matters because Explorer was already running elevated when it was relaunched. Read the full command line of the alerting Explorer and the integrity level it ran at, then look at every explorer.exe started on this host in the surrounding window to see whether a normal interactive shell was killed and replaced with an elevated one. A medium-integrity user shell relaunching as a high-integrity /NOUACCHECK shell is the token-laundering step.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - IntegrityLevel
      - ParentImage
      - ParentCommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this explorer.exe, and was that parent already running with elevated privileges?
  context: The /NOUACCHECK relaunch must come from an already-elevated process, so the parent identity is the pivot. A parent like cmd.exe, powershell.exe, an installer, or an unsigned binary running at high integrity points to an operator who first obtained elevation by another means and is now converting it into a persistent elevated shell. Walk up the tree to see what spawned the parent and how that process became elevated.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:uac_bypass.elevated_action_after_bypass
  question: What ran at high or system integrity on this host in the half hour after the Explorer relaunch?
  context: 'The /NOUACCHECK flag exists so that children of Explorer inherit elevation without consent prompts, so the payoff appears as high-integrity child processes shortly after the relaunch. Examining processes that ran at High or System integrity in the window after the alert reveals the objective: persistence installation, credential dumping, security-tool tampering, or access to protected resources. A relaunch with no elevated follow-on activity is weaker than one immediately followed by privileged action.'
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        IntegrityLevel:
          - 'High'
          - 'System'
          - 'S-1-16-16384'
          - 'S-1-16-12288'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: attack:T1548.002.elevation_token
  question: Did the elevated parent or its successors receive special privileges consistent with a high-integrity logon?
  context: An elevated process backing the /NOUACCHECK relaunch is granted sensitive privileges (SeDebugPrivilege, SeTcbPrivilege, SeBackupPrivilege, and similar) when its token is created. Special-privilege assignment events on this host around the alert time corroborate that a genuinely elevated token was in play rather than a benign substring match on the command line. If special-privilege logging is not collected in this environment, treat zero results as a visibility gap rather than evidence nothing elevated.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4672
      condition: selection
    fields:
      - SubjectUserName
      - PrivilegeList
      - ProcessName
- source: template:uac_bypass.bypass_technique_registry_artifacts
  question: Were registry keys written by command line to set up a separate auto-elevated UAC bypass on this host?
  context: The /NOUACCHECK flag is one elevation path, but an operator who is escalating often also primes the classic auto-elevated-binary bypasses that write a command into a registry key an elevated process reads. Command-line writes touching ms-settings, mscfile, exefile, Folder shell-open, Environment, or DelegateExecute keys point to a second bypass attempt and reveal the exact payload that would run elevated. Finding these alongside the Explorer relaunch indicates deliberate, repeated escalation rather than a single artifact.
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\reg.exe'
          - '\powershell.exe'
          - '\cmd.exe'
        CommandLine|contains:
          - 'ms-settings'
          - 'mscfile'
          - 'Classes\exefile'
          - 'Classes\Folder'
          - 'Environment'
          - 'Shell\Open\command'
          - 'DelegateExecute'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: attack:T1548.002.registry_bypass_setup
  question: Were UAC-related registry values modified on this host shortly before the Explorer relaunch?
  context: 'Command-line evidence misses registry writes performed in-memory by a loaded module or a tool that does not shell out to reg.exe. Registry modification events targeting known auto-elevation keys, the shell-open command verbs under user-writable Classes hives, or the consent-prompt policy values under the system policies key, capture the bypass setup directly at the registry layer. A write to one of these keys in the minutes before the relaunch ties the elevation to a specific mechanism and payload. If registry modification events are not collected in this environment, treat zero results as a visibility gap.'
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - 'ms-settings\Shell\Open\command'
          - 'mscfile\shell\open\command'
          - 'Classes\exefile'
          - 'Classes\Folder\shell'
          - '\DelegateExecute'
          - 'Policies\System\EnableLUA'
          - 'Policies\System\ConsentPromptBehaviorAdmin'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: template:uac_bypass.other_escalation_attempts
  question: Did other privilege-escalation or UAC-bypass detections fire on this host around the same time?
  context: Operators who attempt one elevation path often try several until one succeeds, especially if the host configuration blocks a given technique. Other privilege-escalation or bypass alerts on this host in the surrounding window confirm deliberate escalation activity rather than an isolated false positive, and the sequence of alerts helps reconstruct how elevation was first obtained before the Explorer relaunch.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:cue_process_tool.baseline
  question: Does this host normally run explorer.exe with the /NOUACCHECK flag?
  context: A relaunch of Explorer with elevation-bypass flags is rare on most hosts, so historical frequency is a strong triage signal. Zero or very low counts over the prior 90 days indicate a first-time or unusual occurrence and raise the priority of the elevated follow-on activity; a host that produces this pattern routinely is more likely running deployment or imaging automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even on hosts where the pattern appears, the parent and user distribution tells you whether this instance is routine. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user breakdown reflects how this specific /NOUACCHECK firing pattern has historically run here, not generic Explorer usage. A new parent process, an unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      filter_dc_logon:
      - ParentCommandLine: C:\Windows\system32\svchost.exe -k netsvcs -p -s Schedule
      - ParentImage: C:\Windows\System32\svchost.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not 1 of filter_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally relaunch Explorer with elevation-bypass flags across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never produced this pattern is more suspicious than one who does so regularly as part of an administrative or imaging role. User role and typical toolset should align with deliberately restarting the shell at high integrity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and explorer.exe combination historically routine on this host or across the environment, or is the parent unusual for this launch?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned the /NOUACCHECK Explorer, and is that pair historically common. A new or rare parent for this launch on this host or environment-wide is the deviation worth chasing; a pair that already accounts for many prior events is deployment or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or privileged action across multiple endpoints. If the same user triggered similar elevation activity on other hosts, the scope of the operation may be larger than this single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and Explorer relaunch fire on other hosts in the same window, indicating a coordinated rollout rather than isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, the parent process plus the /NOUACCHECK Explorer, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local operator action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of the /NOUACCHECK firing pattern, not generic Explorer usage. Many hosts firing the same pattern points to scripted deployment or imaging automation; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \explorer.exe
        CommandLine|contains: /NOUACCHECK
      filter_dc_logon:
      - ParentCommandLine: C:\Windows\system32\svchost.exe -k netsvcs -p -s Schedule
      - ParentImage: C:\Windows\System32\svchost.exe
      condition: selection and not 1 of filter_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded the elevation and help reconstruct the timeline. Look for patterns: how the parent process first obtained elevation, or tampering and reconnaissance that preceded the relaunch.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1548.002
      name: Bypass User Account Control
      role: primary
    - id: T1548
      name: Abuse Elevation Control Mechanism
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: elevated_context_present
      description: An already-elevated process exists on the host and serves as the launching parent for the relaunch.
      mapped_to: Q4
      source: template:cue_process_tool.process_chain
    - stage: 2
      label: nouaccheck_relaunch
      description: explorer.exe is relaunched with /NOUACCHECK so its children inherit elevation without UAC consent prompts.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: token_laundering
      description: The elevated Explorer's command line and integrity level are decoded against the prior interactive shell to confirm token laundering.
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 4
      label: elevated_followon
      description: High or System integrity child processes perform the operator's objective after the bypass.
      mapped_to: Q5
      source: template:uac_bypass.elevated_action_after_bypass
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1548.002
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1548.002
      covered_by: Q5
      source: template:uac_bypass.elevated_action_after_bypass
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1548.002
      covered_by: Q9
      source: template:uac_bypass.other_escalation_attempts
      coverage_type: direct
    - dimension: user_context
      technique: T1548
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (e.g., 30 second correlation window between elevation and action)"
      disposition: context
      target_question: Q5
      notes: "No fixed numeric threshold; the +30m post-bypass high-integrity window plus the -15m registry-setup window carry the temporal correlation. Tightening to 30s would miss operator pauses between relaunch and action."
    - element: ElevatedProcessNameList
      dimension: tuning_parameter
      specific_values: "sdclt.exe, eventvwr.exe, computerdefaults.exe (examples — these are OTHER bypass binaries, not the NOUACCHECK mechanism)"
      disposition: context
      target_question: Q9
      notes: "These auto-elevated binaries describe registry-key bypass variants distinct from the NOUACCHECK flag this rule fires on; surfaced as the other-escalation-attempts alert sweep and the registry-bypass questions rather than hard-coded into the Explorer query."
    - element: ParentProcessAnomalyThreshold
      dimension: parent_process_analysis
      specific_values: "rationale only (non-elevated process spawning auto-elevated one)"
      disposition: context
      target_question: Q4
      notes: "No queryable threshold; the process_chain question retrieves the parent and its integrity level so the analyst judges the elevation lineage directly."
    - element: ElevatedProcessPath
      dimension: tuning_parameter
      specific_values: "rationale only (paths of unsigned/unexpected elevated binaries)"
      disposition: context
      target_question: Q5
      notes: "No fixed path list; the post-bypass high-integrity child query returns Image paths for the analyst to assess signing/location."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (parent-child chains suspicious in the local environment)"
      disposition: context
      target_question: Q4
      notes: "Folded into the process_chain parent-lineage question."
    - element: ElevationPrivilege
      dimension: parent_process_analysis
      specific_values: "special privileges / 4672 special-privilege token (rationale)"
      disposition: new_question
      target_question: Q6
      notes: "Drove attack:T1548.002.elevation_token security-logsource question correlating special-privilege assignment, satisfying the 'security' logsource hint."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.follow_on
      reason: "follow-on child-process angle is more specifically covered by uac_bypass.elevated_action_after_bypass, which filters to High/System integrity children — the relevant post-bypass population for this rule"
      kept: template:uac_bypass.elevated_action_after_bypass
    - dropped: cue_process_tool.clustering
      reason: "generic host activity clustering subsumed by cue_host_context.precursor (prior-alert timeline) and uac_bypass.other_escalation_attempts (concurrent escalation alerts); a flat +/-15m Image aggregation adds no escalation-specific signal here"
      kept: template:cue_host_context.precursor
  skipped_templates:
    - template: cue_host_context.clustering
      reason: "Concurrent-activity context is carried by uac_bypass.other_escalation_attempts (+/-1h sigma alerts on host) and the precursor question; a generic Image+ParentImage aggregation would duplicate that coverage without an escalation-specific lens. Slot reallocated to the security/registry logsource questions the hints call for."
    - template: universal.endpoint_protection_health
      reason: "defense_tampering is not confirmed for this rule, but other_escalation_attempts (+/-1h) and the precursor question (-6h) already surface prior tamper alerts on the host. The disposition here turns on the elevation lineage and high-integrity follow-on, not on whether AV was disabled first, so the generic 1h tamper-command sweep does not earn a slot under the 18-question cap."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1548.002) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        image_load coverage targets DLL/COM-hijack UAC bypass variants where an
        auto-elevated process side-loads an attacker module. This rule's
        mechanism is the /NOUACCHECK command-line flag on an already-elevated
        explorer.exe, which involves no module load to bypass UAC. The
        elevation evidence for this rule lives in process lineage and
        integrity level (Q4 process_chain, Q3 mechanism_decode) and in the
        4672 special-privilege correlation (Q6 attack:T1548.002.elevation_token),
        not in image_load events. No compensating image_load question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (Sysmon 10 / handle-open) coverage targets token-theft
        UAC bypass variants (potato-style cross-process handle duplication).
        The NOUACCHECK flag launders an existing elevated token through Explorer
        relaunch and opens no handle into another process to do so. The elevated
        parent is identified by lineage and integrity level (Q4 process_chain)
        and corroborated by special-privilege assignment (Q6), which cover the
        analyst-side question without a process_access query.
    - hint: "logsource 'registry_set' (recommended by techniques: T1548.002) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Acted on — authored Q8 attack:T1548.002.registry_bypass_setup with
        `category: registry_set` to capture registry-layer UAC bypass setup
        (auto-elevation shell-open keys, consent-prompt policy values) that the
        command-line reg.exe question (Q7) misses when writes are performed
        in-memory.
    - hint: "logsource 'security' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: acted_on
      reason: >-
        Acted on — authored Q6 attack:T1548.002.elevation_token with
        `service: security` querying EventID 4672 special-privilege assignment
        to corroborate that a genuinely elevated token backed the relaunch.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - uac_bypass
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      registry_set: 1
      security: 1
      sigma_alert: 2
