name: Potential Excel.EXE DCOM Lateral Movement Via ActivateMicrosoftApp
id: 6a0bb1e5-d57e-43cb-a014-4454c40254bd
description: |
  Detects excel.exe spawning one of the end-of-life Microsoft binaries foxprow.exe,
  schdplus.exe, or winproj.exe. The rule fires when the parent image ends in
  \excel.exe AND the child is one of those three binaries, matched either by its
  OriginalFileName or by an Image path ending in the binary name. Excel's
  ActivateMicrosoftApp DCOM method takes an integer that maps to a legacy app
  (5 = FoxPro/foxprow.exe, 6 = Project/winproj.exe, 7 = SchedulePlus/schdplus.exe)
  and launches it by searching the system PATH, which can be driven remotely over
  RPC. An attacker with local admin and write access to a PATH directory (for
  example a user WindowsApps folder) plants a malicious binary under one of those
  legacy names and invokes the Excel COM object from another host to execute it,
  producing a child of excel.exe on the target. These legacy binaries are rarely
  run interactively, so the investigation should confirm what excel.exe launched,
  whether a remote DCOM/RPC source drove it, what the spawned binary did, and
  whether the same pattern reached other hosts.
type: detection
detection_id: 551d9c1f-816c-445b-a7a6-7a3864720d60
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did excel.exe spawn foxprow.exe, schdplus.exe, or winproj.exe, matching
    what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires when the parent image ends in \excel.exe AND the child is one of
    foxprow.exe, schdplus.exe, or winproj.exe, identified either by its
    OriginalFileName or by an Image path ending in that name. Read the ParentImage,
    Image, and OriginalFileName from the event and confirm both conditions hold.
    Note which of the three legacy binaries was launched and whether its Image path
    is a normal Office install location or a user-writable directory, since a binary
    under a path such as a user WindowsApps folder is the planted-payload case rather
    than a genuine legacy app.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to run these legacy Microsoft binaries
    under excel.exe?
  context: The binaries foxprow.exe, schdplus.exe, and winproj.exe correspond to
    end-of-life Microsoft applications that are almost never launched on modern
    endpoints, and never as children of Excel in normal use. Knowing whether the
    acting account and host function involve any legacy-app workflow helps separate
    a rare-but-benign invocation from a remotely driven execution. On a standard
    workstation there is no legitimate reason for Excel to spawn these binaries.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1021.003.process_identification
  question: Which legacy binary ran, where does its Image live, and is it the genuine
    Microsoft binary or a planted file under that name?
  context: The detection matches the child by OriginalFileName or by an Image path
    ending in foxprow.exe, schdplus.exe, or winproj.exe, so a file planted under one
    of those names will fire the rule regardless of what it actually is. Identify the
    full Image path and compare the OriginalFileName and signing details against the
    path. A signed Microsoft binary in an Office install directory is a different
    finding than an unsigned or mismatched file running from a user-writable PATH
    location such as a user WindowsApps folder, which is the location an operator
    plants the payload so Excel's PATH search executes it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:cue_parent_process.mechanism_decode
  question: How was excel.exe itself started, and was it launched interactively or
    instantiated as a COM object?
  context: The ActivateMicrosoftApp method is reached through Excel's COM
    automation interface, which a remote caller drives by creating an Excel
    Application object over DCOM rather than a user opening a workbook. Look at how
    this excel.exe instance began. An interactive launch from explorer.exe with a
    document on the command line points to ordinary use, while an excel.exe with no
    document and a service or DCOM-related ancestry points to remote COM activation
    that exists only to call ActivateMicrosoftApp.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1021.003.remote_dcom_source
  question: Did an inbound RPC or DCOM connection reach this host around the time
    excel.exe spawned the legacy binary, and from which source?
  context: Remote ActivateMicrosoftApp abuse drives Excel over DCOM, which begins
    with an inbound RPC connection to the endpoint mapper on port 135 followed by
    activity on a dynamically negotiated high port. Correlating an inbound RPC
    connection in the minutes around the spawn identifies the source host that drove
    the execution and distinguishes remote lateral movement from a purely local
    invocation. A known administrative jump host is a different finding than an
    unexpected workstation reaching this machine over RPC.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationPort:
          - 135
        Initiated: 'false'
      condition: selection
    fields:
      - SourceIp
      - SourcePort
      - Image
- source: attack:T1021.003.dcom_authentication
  question: What account authenticated to this host over the network just before
    excel.exe spawned the legacy binary?
  context: Remote DCOM activation requires the caller to authenticate to the target,
    producing a network logon (Event 4624 type 3, or 4648 for explicit credentials)
    shortly before the child process appears. Identifying the logon account and its
    source address attributes the remote invocation and shows whether it used a
    privileged or service account, which is the user-context signal the analytic
    relies on to flag rare or first-time DCOM callers. If Security logon events are
    not collected in this environment, treat the absence as a visibility gap and pivot
    to the inbound RPC source instead.
  range: -10m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID:
          - 4624
          - 4648
        LogonType: 3
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TargetUserName
      - IpAddress
      - LogonType
- source: template:parent_child.parent_process_legitimacy
  question: Is excel.exe expected to spawn this child, and what started excel.exe?
  context: An Office application spawning these legacy binaries is the core signal
    for this detection. Tracing how excel.exe was itself created reveals whether the
    chain is interactive (a user opening a workbook) or driven by remote COM
    activation. An excel.exe with no parent document, launched under dcom or service
    infrastructure, that immediately spawns foxprow.exe, schdplus.exe, or winproj.exe
    points to ActivateMicrosoftApp abuse rather than legitimate legacy-app use.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did this same excel.exe instance spawn?
  context: If a remotely activated Excel instance spawned the legacy binary, it may
    have spawned other processes in the same window. Examining the siblings of the
    detected child reveals the full scope of what the COM activation drove. Multiple
    unusual children from one excel.exe, or a follow-on script host or shell, points
    to hands-on execution rather than an isolated legacy-app launch.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1021.003.followon_module_load
  question: What did the spawned legacy binary do after launch, and what modules did
    it load?
  context: Because the rule matches on filename rather than file identity, the binary
    Excel launched may be an attacker payload using a legacy name. What it does next
    reveals its true purpose. Reviewing the loaded modules and any child processes of
    the spawned binary shows whether it behaves like the inert legacy application or
    loads networking, scripting, or injection libraries consistent with a planted
    payload. If image-load telemetry is not collected in this environment, fall back
    to the spawned binary's own child processes and network connections to characterize
    its behavior.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \foxprow.exe
          - \schdplus.exe
          - \winproj.exe
      condition: selection
    fields:
      - ImageLoaded
      - Signed
- source: template:lateral_movement.target_activity_after_access
  question: What processes were spawned on this host in the half hour after the
    legacy binary launched?
  context: DCOM lateral movement is a means to an end. After the remote caller drives
    Excel to spawn a binary, the follow-on activity on the host reveals the operator's
    objective, such as deploying tools, accessing data, or further execution. A burst
    of process creation under the spawned binary or the same account shortly after the
    launch points to active hands-on activity rather than a single stray invocation.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \excel.exe
      selection_child:
      - OriginalFileName:
        - foxprow.exe
        - schdplus.exe
        - winproj.exe
      - Image|endswith:
        - \foxprow.exe
        - \schdplus.exe
        - \winproj.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \excel.exe
      selection_child:
      - OriginalFileName:
        - foxprow.exe
        - schdplus.exe
        - winproj.exe
      - Image|endswith:
        - \foxprow.exe
        - \schdplus.exe
        - \winproj.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether the
    Excel DCOM execution was a single event or part of activity spanning multiple
    endpoints. If the same user triggered similar activity on other hosts, the scope
    of the lateral movement is larger than one machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith: \excel.exe
      selection_child:
      - OriginalFileName:
        - foxprow.exe
        - schdplus.exe
        - winproj.exe
      - Image|endswith:
        - \foxprow.exe
        - \schdplus.exe
        - \winproj.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    Excel-spawned launch reveals what led to it and what followed, including the COM
    activation, reconnaissance, or follow-on execution. Novel parent-child pairs at
    low counts are what to scan for against the high-count noise of routine system
    processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host, such as credential access,
    reconnaissance, or remote-service activity, may reveal the stages that preceded
    the Excel DCOM execution and help reconstruct the timeline. Lateral movement is
    usually one step in a sequence, so prior alerts give the context for how the
    operator reached this host.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1021.003
      name: Distributed Component Object Model
      role: primary
    - id: T1021
      name: Remote Services
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: remote_dcom_invocation
      description: "A remote DCOM/RPC invocation by an account reaches the target host over port 135, driving Excel COM activation"
      mapped_to: Q5
      source: attack:T1021.003.remote_dcom_source
    - stage: 2
      label: abnormal_process_instantiation
      description: "excel.exe spawns one of the legacy binaries (foxprow.exe / schdplus.exe / winproj.exe), the act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: followon_code_execution
      description: "Module loading and follow-on process activity by the spawned binary indicating remote code execution on the target"
      mapped_to: Q9
      source: attack:T1021.003.followon_module_load
  dimension_coverage:
    - dimension: process_identification
      technique: T1021.003
      covered_by: Q3
      source: attack:T1021.003.process_identification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1021.003
      covered_by: Q5
      source: attack:T1021.003.remote_dcom_source
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1021.003
      covered_by: Q14
      source: template:cue_user_actor.clustering_svc
      coverage_type: direct
    - dimension: user_context
      technique: T1021.003
      covered_by: Q6
      source: attack:T1021.003.dcom_authentication
      coverage_type: direct
    - dimension: command_analysis
      technique: T1021
      covered_by: Q3
      source: attack:T1021.003.process_identification
      coverage_type: indirect
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "300s correlation window (rationale)"
      disposition: context
      target_question: Q5
      notes: "Realized as the +/-5m correlation window in the remote_dcom_source RPC question and the -10m window in the dcom_authentication question, correlating inbound RPC/logon with the spawn."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — rare/first-time DCOM callers"
      disposition: new_question
      target_question: Q6
      notes: "Realized as attack:T1021.003.dcom_authentication, which pulls the network-logon account (4624 type 3 / 4648) that drove the remote activation, plus the user baseline questions."
    - element: ProcessName
      dimension: process_identification
      specific_values: "excel.exe (parent), wmiprvse.exe (example DCOM hosts)"
      disposition: query_injection
      target_question: Q1
      notes: "Rule already constrains parent to excel.exe and child to the three legacy binaries; the concrete child binary list (foxprow.exe/schdplus.exe/winproj.exe) is injected into the baseline, clustering_svc, and followon_module_load detection blocks. wmiprvse.exe is a generic DCOM-host example not matched by this Excel-specific rule, so it is not injected."
    - element: RemoteHostList
      dimension: tuning_parameter
      specific_values: "rationale only — known/allowed DCOM source systems"
      disposition: context
      target_question: Q5
      notes: "Realized as the SourceIp projection in the remote_dcom_source question and the cross-host distribution in clustering_svc, letting the analyst compare the source against expected administrative origins."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). Image-pivot adapted to the rule's legacy-binary list."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot)."
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (hint 1) — parent_child.parent_process_legitimacy and cue_parent_process.mechanism_decode cover the parent chain more specifically"
      kept: parent_child.parent_process_legitimacy
  skipped_templates:
    - template: cue_parent_process.baseline
      reason: "Subsumed by cue_user_actor.baseline_svc, which counts the excel.exe -> legacy-binary parent-child pairing environment-wide with the concrete binary list injected. A generic ParentName+Image baseline would duplicate it."
    - template: cue_parent_process.follow_on
      reason: "Children of this excel.exe instance are covered precisely by parent_child.sibling_processes (ParentProcessGuid-scoped); a ParentName-scoped follow_on would be broader and noisier. Trimmed to stay within the 14-18 target."
    - template: cue_parent_process.clustering
      reason: "Same-parent host activity covered by cue_host_context.clustering and parent_child.sibling_processes. Trimmed to stay within the 14-18 target."
    - template: cue_parent_process.scope
      reason: "Cross-host parent-child distribution covered by cue_user_actor.clustering_svc (same window) and cue_user_actor.baseline_svc (historical). Trimmed to stay within the 14-18 target."
    - template: cue_process_tool.mechanism_decode
      reason: "The legacy binaries take no meaningful command line; mechanism is the file identity, not arguments. Covered by attack:T1021.003.process_identification (Image/OriginalFileName/signing) and followon_module_load (behavior). Trimmed to stay within the 14-18 target."
    - template: cue_process_tool.baseline
      reason: "Host-level baseline of the legacy binary subsumed by cue_user_actor.baseline (user-scoped) and baseline_svc (parent-child-scoped) with the binary list injected. Trimmed to stay within the 14-18 target."
    - template: lateral_movement.source_host_identification
      reason: "Inbound lateral-movement port question replaced by the more specific attack:T1021.003.remote_dcom_source, which scopes to the DCOM RPC port (135) and inbound direction rather than the generic SMB/RDP/WinRM port list."
    - template: lateral_movement.authentication_method
      reason: "Authentication question replaced by the more specific attack:T1021.003.dcom_authentication, which queries the actual Security logon events (4624/4648 type 3) for the remote DCOM activation rather than a ProcessGuid lookup of the local user."
    - template: lateral_movement.lateral_movement_scope
      reason: "Image|endswith psexec/mstsc/etc lateral-tool list does not match DCOM-driven Excel movement; cross-host scope covered by cue_user_actor.clustering/clustering_svc. Trimmed to stay within the 14-18 target."
    - template: initial_access.delivery_vector
      reason: "initial_access set confirmed only because parent binary is excel.exe, but this rule detects the lateral-movement destination (remote DCOM activation), not phishing delivery. No email/document delivery vector applies. See contextual_sets."
    - template: initial_access.campaign_scope
      reason: "Same-rule cross-host fire covered by cue_user_actor.clustering_svc (same-window parent-child rollout). initial_access framing (phishing campaign) does not fit a DCOM lateral-movement rule. See contextual_sets."
    - template: initial_access.application_patch_status
      reason: "ActivateMicrosoftApp is a documented DCOM method, not a patchable CVE; patch status is not the relevant pivot. initial_access set does not fit this rule. See contextual_sets."
    - template: remote_access_tool.tool_authorization
      reason: "remote_access_tool set confirmed on a stale 'title contains atera' heuristic; this rule has no RMM tool. No remote-access-tool questions apply. See contextual_sets."
    - template: remote_access_tool.active_session_context
      reason: "No remote access tool in this rule; helpdesk-session framing does not fit DCOM lateral movement. See contextual_sets."
    - template: remote_access_tool.connection_destination
      reason: "No remote access tool; outbound RAT-relay framing does not fit. Inbound DCOM source is covered by attack:T1021.003.remote_dcom_source. See contextual_sets."
    - template: network_scope.same_action_other_hosts
      reason: "Recommended; cross-host same-binary spread covered by cue_user_actor.clustering_svc and baseline_svc with the parent-child pairing. Trimmed to stay within the 14-18 target."
    - template: network_scope.actor_activity_network_wide
      reason: "Recommended; duplicates cue_user_actor.clustering (User|expand network-wide activity). Trimmed to stay within the 14-18 target."
    - template: network_scope.actor_authentication_trail
      reason: "Recommended; user authentication trail overlaps attack:T1021.003.dcom_authentication (4624/4648). The DCOM-specific version is host-scoped to the inbound activation and is the load-bearing auth question here. Trimmed to stay within the 14-18 target."
    - template: universal.endpoint_protection_health
      reason: "Recommended generic host-tamper sweep. This rule fires on a lateral-movement destination event with no command-line payload; tamper-as-precursor is covered by cue_host_context.precursor (prior alerts on the host). Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: initial_access
        confidence: low
        reason: "Confirmed only because the parent binary is excel.exe, but this rule detects the lateral-movement destination (remote DCOM activation on the target), not a phishing/exploitation delivery. delivery_vector, campaign_scope, and application_patch_status do not fit and are skipped."
      - id: remote_access_tool
        confidence: low
        reason: "Confirmed on a stale 'title contains atera' heuristic that does not match this rule's title; there is no RMM/remote-access tool involved. All three remote_access_tool templates skipped."
  technique_sets_applied:
    - universal
    - lateral_movement
    - parent_child
    - cue_parent_process
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 16
    source_counts:
      template: 12
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 12
      network_connection: 1
      image_load: 1
      security: 1
      sigma_alert: 1
