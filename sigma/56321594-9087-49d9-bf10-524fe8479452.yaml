name: Potential Persistence Via Netsh Helper DLL
id: 6102b9c4-d3ea-4e79-8b37-035cf8541a6e
description: |
  Detects netsh.exe run with both the "add" and "helper" tokens on its command
  line, which registers a NetShell helper DLL. The rule fires when the process is
  netsh.exe (by image name or original filename) AND the command line contains
  both "add" and "helper". Registering a helper DLL writes a value under
  HKLM\SOFTWARE\Microsoft\Netsh, after which every subsequent run of netsh.exe
  loads that DLL and executes its InitHelperDll entry point - a persistence and
  code-execution proxy. The helper DLL can live anywhere readable by the running
  context, including user-writable directories, and netsh is not started at boot,
  so an actor typically pairs this registration with a separate trigger. Some
  software and VPN clients register helpers legitimately, so the investigation
  should determine which DLL was registered, where it came from, what parent
  launched netsh, whether the registry value and DLL load are visible, and whether
  the same pattern appears elsewhere on the host or fleet.
type: detection
detection_id: 56321594-9087-49d9-bf10-524fe8479452
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Was netsh.exe run with both the add and helper tokens on its command line,
    matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires when the process is netsh.exe (by image name or original filename)
    and the command line contains both "add" and "helper". Read the full command line
    and identify the DLL path passed to "add helper" - that path is the helper DLL
    being registered for persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to register netsh helper DLLs?
  context: Registering a NetShell helper is rare administrative activity, performed
    by a small set of networking software, VPN clients, and IT tooling. Most users and
    most endpoints never run "netsh add helper". Knowing whether the acting account and
    the host function involve network-stack configuration is often the fastest path to
    separating a legitimate deployment from an actor establishing a persistence proxy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: Which DLL path did this netsh add helper command register, and where does
    that DLL live?
  context: The command line carries the full registration. Decode the path passed to
    "add helper" and note where the DLL resides. A helper DLL under C:\Windows\System32
    alongside the OS-provided helpers is a different finding than one under a
    user-writable directory such as %APPDATA% or a temp path, which a non-privileged
    context can write to and which legitimate helpers do not use. The path identifies
    the file to pull for signing and reputation analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\netsh.exe'
        CommandLine|contains|all:
          - add
          - helper
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:parent_child.parent_process_legitimacy
  question: What process spawned netsh.exe, and what started that parent?
  context: Whatever launched netsh.exe is where the helper-registration decision was
    made. Interactive administration usually shows a console shell or a management
    tool; an unexpected parent such as a script host, mshta.exe, an Office application,
    or an unknown binary from a user-writable path points to automated or malicious
    invocation. Tracing the parent's own origin reveals whether the chain is a routine
    deployment or an actor staging persistence.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent spawn around this time?
  context: If the parent that launched netsh.exe is suspicious, it likely spawned other
    processes too. Examining siblings reveals the full scope of activity from that
    parent - a helper-DLL registration alongside reconnaissance, a download, or a
    scheduled-task creation from the same parent indicates orchestrated activity rather
    than an isolated administrative action.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1546.007.registry_set
  question: Was a value written under HKLM\SOFTWARE\Microsoft\Netsh on this host around
    the time of the alert?
  context: The persistent artifact of a helper-DLL registration is a value under
    HKLM\SOFTWARE\Microsoft\Netsh; the command line is the action, but this registry
    value is what survives and causes netsh to load the DLL on every later run. If
    registry-set telemetry is collected, a new value here corroborates the
    registration and names the helper. If it is not collected, treat the absence of
    rows as a visibility gap rather than evidence nothing was written, and confirm the
    registration from the command line directly.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\SOFTWARE\Microsoft\Netsh'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
      - User
- source: attack:T1546.007.image_load
  question: Did netsh.exe load a helper DLL from a non-System32 path on this host?
  context: A registered helper DLL is loaded by netsh.exe on every subsequent run.
    Legitimate helpers live under C:\Windows\System32; a DLL loaded by netsh.exe from a
    user-writable or otherwise unusual directory is the execution half of this
    persistence primitive and indicates a planted helper. If module-load telemetry is
    collected, this surfaces the actual loaded file for signing and reputation review;
    if it is not collected, the command-line path identifies the DLL to inspect on disk.
  range: +/-24h
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\netsh.exe'
      filter_system32:
        ImageLoaded|contains:
          - '\Windows\System32\'
          - '\Windows\SysWOW64\'
      condition: selection and not filter_system32
    fields:
      - ImageLoaded
      - Image
      - User
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for
    registering this netsh helper DLL?
  context: Persistence-relevant changes such as registering a NetShell helper are
    typically gated by change control in managed environments. A corresponding ticket
    or a known software deployment that registers a helper suggests authorized activity;
    the absence of one when policy requires it shifts the disposition toward
    unauthorized persistence and warrants confirming what the registered DLL is.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was netsh.exe executed from its expected System32 path?
  context: netsh.exe run from a non-standard path (outside System32 or SysWOW64) may
    indicate the binary was copied or renamed to evade path-based allowlists. Compare
    the image path and original filename in the alert - a netsh.exe running from a
    user's temp or profile directory is more suspicious than one in System32, and a
    mismatch between the image name and the original filename indicates a renamed copy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were other persistence or defense-tampering LOLBins used on this host recently?
  context: An actor who registers a netsh helper for persistence often pairs it with
    other persistence or tampering primitives. Finding schtasks, sc, reg, at, wevtutil,
    bcdedit, vssadmin, or similar built-in tools used on the same host within the same
    window strengthens the case for deliberate persistence and reveals backup
    mechanisms or trigger installation that the helper registration relies on.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\wmic.exe'
          - '\bitsadmin.exe'
          - '\wevtutil.exe'
          - '\bcdedit.exe'
          - '\vssadmin.exe'
          - '\diskshadow.exe'
          - '\dnscmd.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the rule's netsh add helper pattern over the
    past 90 days, and what command-line structures are typical?
  context: A netsh helper-registration pattern with no prior history on a host is more
    suspicious than one that recurs under stable automation. This query mirrors the
    rule's selection structure scoped to this host, so the resulting count reflects the
    rule's specific firing pattern rather than generic netsh usage. The command-line
    projection lets the analyst compare the helper paths seen historically against the
    alerting one - a first-ever occurrence or a new DLL path is the anomaly worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - OriginalFileName: netsh.exe
      - Image|endswith: \netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - add
        - helper
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's netsh add helper pattern in the past week,
    and does the cross-host distribution look like consistent automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the
    result is a fleet-wide count of the netsh helper-registration pattern rather than
    generic netsh usage. Many hosts firing the same pattern points to a software
    deployment or vendor automation that registers a helper by design; one host or a
    small unrelated set points to manual operator activity. Use the host distribution
    to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - OriginalFileName: netsh.exe
      - Image|endswith: \netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - add
        - helper
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - OriginalFileName: netsh.exe
      - Image|endswith: \netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - add
        - helper
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - OriginalFileName: netsh.exe
      - Image|endswith: \netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - add
        - helper
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether the
    helper registration was a single event or part of activity spanning multiple
    endpoints. If the same user registered helpers or ran related tooling on other
    hosts, the scope of the incident is larger than one machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - OriginalFileName: netsh.exe
      - Image|endswith: \netsh.exe
      selection_cli:
        CommandLine|contains|all:
        - add
        - helper
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    helper registration reveals what led to it and what followed - the parent that
    staged netsh, reconnaissance, a dropped DLL, or trigger installation that arranges
    for netsh to run after reboot. Novel parent-child pairs at low counts are what to
    scan for against the high-count noise of routine system processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - reconnaissance, credential access, or
    defense tampering - may reveal the attack stages that preceded the helper
    registration and help reconstruct the timeline. Persistence is usually installed
    after an actor already has a foothold, so prior alerts give the context for why a
    netsh helper was registered.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1546.007
      name: Netsh Helper DLL
      role: primary
    - id: T1546
      name: Event Triggered Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: helper_registration
      description: "netsh.exe is run with add helper on its command line to register a helper DLL, the act the rule detects"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: registry_persistence
      description: "The registration writes a value under HKLM\\SOFTWARE\\Microsoft\\Netsh that survives reboot and causes the DLL to load on every later netsh run"
      mapped_to: Q6
      source: attack:T1546.007.registry_set
    - stage: 3
      label: helper_dll_execution
      description: "On subsequent netsh.exe runs the registered helper DLL is loaded and its InitHelperDll entry point executes"
      mapped_to: Q7
      source: attack:T1546.007.image_load
  dimension_coverage:
    - dimension: time_correlation
      technique: T1546.007
      covered_by: Q6
      source: attack:T1546.007.registry_set
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1546.007
      covered_by: Q7
      source: attack:T1546.007.image_load
      coverage_type: direct
    - dimension: user_context
      technique: T1546
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (e.g., within 10 minutes)"
      disposition: context
      target_question: Q6
      notes: "Tuning rationale, no concrete value to query. Realized as the +/-15m correlation window on the registry_set question that ties the netsh command line to the registry write, and the surrounding host-clustering window."
    - element: NetshChildProcessWhitelist
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Whitelist of expected netsh children; no concrete values in the analytic. Realized as the sibling-process and host-clustering questions, which surface anomalous children/parent-child pairs for the analyst to judge against expected tooling."
    - element: DLLLoadPath
      dimension: tuning_parameter
      specific_values: "C:\\Windows\\System32 canonical; user-writable alt e.g. %APPDATA% (from references)"
      disposition: query_injection
      target_question: Q7
      notes: "Path heuristic distinguishing benign from malicious helper DLLs. Injected as a not-filter excluding \\Windows\\System32\\ and \\Windows\\SysWOW64\\ in the image_load question, surfacing helper DLLs loaded from non-standard paths. Also reinforced in mechanism_decode (Q3) context, which directs the analyst to note whether the registered DLL path is System32 or user-writable."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (SYSTEM / LOCAL SERVICE vs user-initiated)"
      disposition: context
      target_question: Q2
      notes: "Filters SYSTEM vs user-initiated triggers; no concrete values. Realized in host_user_context (Q2) and the user_actor baseline pair, and the service-principal variants explicitly handle the SYSTEM/service-account firing case."
    - element: PathAnomalyThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Path-deviation scoring for execution anomalies; no concrete threshold. Realized as renamed_binary_check (Q9), which compares netsh.exe's image path and original filename against the expected System32 location."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_child technique set active — parent_process_legitimacy (Q4) subsumes process_chain and also walks the grandparent, per the parent_process-active dedup rule"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_process_tool.baseline_deviation
      reason: "REFLECTED host_pattern query is byte-for-byte identical in detection block to lolbin.historical_lolbin_baseline (same netsh add+helper selection, same host scope); differs only in projected fields/range. Near-identical query — kept the lolbin must_include version (projects CommandLine shapes); baseline_deviation projected ParentImage/User, which parent_process_legitimacy and the user_actor baseline pair already cover."
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "Image-only host aggregation subsumed by cue_host_context.clustering (Q17), which projects Image+ParentImage and surfaces novel parent-child pairs"
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot Q13 and launch-mechanism-pivot Q14). Image-pivot adapted to the CommandLine add+helper pattern since the rule is image-name-loose (OriginalFileName OR Image)."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot Q15 and launch-mechanism-pivot Q16)."
      kept: both
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "follow_on scopes children by ParentImage|endswith %Image%; the load-bearing follow-on for this rule is the helper DLL being loaded by netsh on later runs, covered directly by attack:T1546.007.image_load (Q7). Process-children of netsh are also covered by sibling_processes (Q5) under the parent. Trimmed to stay within the 14-18 target."
    - template: persistence_scope.process_based_persistence
      reason: "recommended priority. Process-tool persistence (schtasks/sc/reg/at/bitsadmin/wmic) on this host is already surfaced by lolbin.technique_class_lateral (Q10), whose list covers the same persistence binaries plus the trigger-installation tools this technique depends on. Avoided a near-duplicate aggregation question."
    - template: persistence_scope.file_based_persistence
      reason: "recommended priority. The persistent artifact for netsh helper persistence is a registry value under HKLM\\SOFTWARE\\Microsoft\\Netsh plus a DLL on disk, covered by attack:T1546.007.registry_set (Q6) and attack:T1546.007.image_load (Q7). Startup-folder / Run-key file drops are an orthogonal mechanism not implied by this rule. Trimmed to stay within the 14-18 target."
    - template: universal.endpoint_protection_health
      reason: "recommended, not must_include. This rule detects a persistence registration, not execution or credential access, so tamper-as-immediate-precursor is not the live question; cue_host_context.precursor (Q18) already carries prior defense-tampering alerts on the host. Dropped to keep within the 18-question cap with the load-bearing persistence/chain coverage prioritized."
    - template: organizational_context.human_recall
      reason: "recommended. change_ticket (Q8) is the load-bearing authorization check for this persistence registration; human_recall would largely restate the same authorization question. Trimmed to stay within the 14-18 target."
    - template: organizational_context.timing_alignment
      reason: "recommended. Timing-vs-maintenance-window judgment is folded into change_ticket (Q8, maintenance window) and cue_host_context.precursor; a standalone timing question added little for this rule. Trimmed to stay within the 14-18 target."
    - template: organizational_context.procedural_consistency
      reason: "recommended. Whether helper registration follows normal deployment tooling is captured by change_ticket (Q8) and the user/host expectation question (Q2). Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions:
    - hint: "LOLBin rule — check for renamed binary and other LOLBins achieving same objective"
      disposition: acted_on
      reason: >-
        Acted on, not declined. lolbin set applied: renamed_binary_check (Q9) checks
        netsh.exe path/original-filename against System32, technique_class_lateral (Q10)
        sweeps other persistence/tamper LOLBins on the host, and historical_lolbin_baseline
        (Q11) baselines the rule's netsh add+helper pattern. Recorded here for the audit
        trail since the hint list also surfaces acted-on items.
    - hint: "logsource 'image_load' (recommended by techniques: T1546.007) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted_on
      reason: >-
        Acted on, not declined. Authored attack:T1546.007.image_load (Q7) querying DLLs
        loaded by netsh.exe from non-System32/SysWOW64 paths — the execution half of the
        netsh helper persistence chain (every later netsh run loads the registered helper).
        This crosses the process_creation -> image_load boundary the technique implies.
    - hint: "logsource 'registry_set' (recommended by techniques: T1546.007) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Acted on, not declined. Authored attack:T1546.007.registry_set (Q6) querying writes
        under HKLM\SOFTWARE\Microsoft\Netsh — the persistent artifact of the registration
        that survives reboot, captured even if the registration bypasses the netsh
        command-line path.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - organizational_context
    - parent_child
    - lolbin
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      image_load: 1
      sigma_alert: 1
