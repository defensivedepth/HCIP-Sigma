name: HackTool - Default PowerSploit/Empire Scheduled Task Creation
id: 26df4f09-3e2b-4210-b22e-3620a03faaa9
description: |
  Detects powershell.exe or pwsh.exe spawning schtasks.exe to create a scheduled task using the default
  configuration emitted by the PowerSploit and Empire persistence modules. The rule fires only when all of the
  signature arguments are present together: /Create, a task runner of powershell.exe -NonI, the task name and
  runner flags /TN Updater /TR, and one of the recurring or trigger schedules (/SC ONLOGON, /SC DAILY /ST,
  /SC ONIDLE, or /SC HOURLY). This is a userland persistence mechanism that re-launches a non-interactive
  PowerShell payload on the chosen trigger. Legitimate administrators rarely create a task literally named
  Updater that runs powershell -NonI through these exact flags, so the investigation should decode the task
  runner payload and the parent PowerShell command line, confirm whether an authorized engagement explains the
  tool, and determine whether the task is still registered and has already executed.
type: detection
detection_id: 56c217c3-2de2-479b-990f-5c109ba8458f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects, with powershell.exe or pwsh.exe spawning schtasks.exe to create a task named Updater that runs powershell -NonI on a recurring or trigger schedule?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process lineage. The rule fires only when all of these hold together, a PowerShell parent (powershell.exe or pwsh.exe) AND an schtasks.exe child AND a command line containing /Create, powershell.exe -NonI, and /TN Updater /TR, AND one of the schedules /SC ONLOGON, /SC DAILY /ST, /SC ONIDLE, or /SC HOURLY. Confirm every clause is present in the actual event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to create scheduled-task persistence through command-line schtasks driven by PowerShell?
  context: Legitimate administrative activity can create scheduled tasks, but doing so by having PowerShell shell out to schtasks.exe with a task that re-launches non-interactive PowerShell is uncommon outside of attacker tooling. Knowing whether the user role and host function involve PowerShell-driven task management is often the fastest path to distinguishing routine administration from a planted persistence mechanism.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this scheduled-task creation part of an authorized penetration test or red team engagement?
  context: PowerSploit and Empire persistence modules are used in authorized security assessments. Before investing investigation time, check whether a pentest or red team exercise is active on this host or network segment. An authorized engagement with matching tool usage and timing resolves the alert without further analysis; absence of one when this exact default signature fires raises the priority.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:parent_child.parent_process_legitimacy
  question: Is this PowerShell parent expected to spawn schtasks.exe, and what started the PowerShell process itself?
  context: The PowerShell-to-schtasks relationship is the core signal for this detection. Tracing the parent PowerShell process back to its own origin reveals whether the chain is a legitimate administrative script or an attacker-controlled session. A PowerShell parent launched by an Office application, a script host, an archive tool, or an unexpected service points to staged execution rather than interactive administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:scheduled_task.task_configuration
  question: What scheduled task was created, and what command does it execute? Identify the task name, runner, and schedule from the schtasks arguments.
  context: The schtasks command line carries the task name (/TN), the command the task runs (/TR), and the trigger schedule (/SC). This signature names the task Updater and sets the runner to a non-interactive PowerShell invocation. Read the full /TR value to see the exact PowerShell payload the task will execute on its trigger, and note which schedule fired (ONLOGON, DAILY, ONIDLE, or HOURLY) because that determines when the persistence activates.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.encoded_command_decode
  question: What did the parent PowerShell process run, and was its command line encoded or obfuscated?
  context: The PowerShell process that launched schtasks is the actor that staged the persistence, and its own command line shows what module or script created the task. Look for -EncodedCommand or -enc base64 blobs, string concatenation, backtick escaping, and variable substitution. Decoding the parent command line reveals the operator framework that drove the task creation and any download cradle or in-memory loader staged alongside it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full script that created this task?
  context: Script block logging (Event ID 4104) records the deobfuscated script content PowerShell actually executed, even when the command line is encoded. For this detection it can surface the persistence module that shelled out to schtasks, providing ground truth about what ran. If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:hacking_tool.tool_capability_used
  question: What specific persistence configuration did this tool invoke?
  context: The persistence modules in these frameworks expose options for the trigger schedule, the task name, and the staged payload. The /SC schedule, the /TN task name, and the /TR runner together reveal exactly how the operator configured the foothold, which determines how often the payload re-executes and what it does when it runs. Compare the observed configuration against the default signature to see whether the operator customized it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:scheduled_task.task_trigger_and_timing
  question: When is the task scheduled to run, and has it already executed?
  context: The trigger schedule determines when the persistence activates. A task set to run at logon fires on the next sign-in, while DAILY, HOURLY, or ONIDLE schedules may have already executed multiple times since creation. Checking for processes spawned by the Task Scheduler service after the task was registered reveals whether the staged PowerShell payload has already run.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\svchost.exe'
        ParentCommandLine|contains: 'Schedule'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:scheduled_task.task_still_present
  question: Is the scheduled task still registered on the system?
  context: Persistence is only effective while the task exists. Some operators delete the task after execution to reduce forensic artifacts, while others leave it for repeated re-execution. Confirming whether the Updater task is still present (via schtasks /query or the endpoint management console) determines whether the persistence mechanism is active and requires remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1053.005.task_creation_event
  question: Does a task-creation event on this host confirm the scheduled task, and what task name and author does it record?
  context: Task creation is recorded as Event ID 4698 in the Security log independently of the schtasks process. This event captures the registered task name, the author account, and the action XML, which confirms the task exists and attributes who created it even if the schtasks process detail is incomplete. Compare the recorded task name and author against the alerting command line to confirm the persistence and identify the responsible account.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4698
      condition: selection
    fields:
      - TaskName
      - SubjectUserName
      - TaskContent
- source: template:parent_child.sibling_processes
  question: What other child processes did the same PowerShell parent spawn?
  context: If the PowerShell session that created this task is operator-controlled, it likely spawned other processes too. Examining the siblings of the schtasks child reveals the full scope of activity from that parent. Multiple tools launched from the same PowerShell instance, such as reconnaissance, credential access, or additional persistence, point to hands-on-keyboard orchestration rather than a single isolated action.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \schtasks.exe
        CommandLine|contains|all:
        - /Create
        - powershell.exe -NonI
        - /TN Updater /TR
        CommandLine|contains:
        - /SC ONLOGON
        - /SC DAILY /ST
        - /SC ONIDLE
        - /SC HOURLY
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \schtasks.exe
        CommandLine|contains|all:
        - /Create
        - powershell.exe -NonI
        - /TN Updater /TR
        CommandLine|contains:
        - /SC ONLOGON
        - /SC DAILY /ST
        - /SC ONIDLE
        - /SC HOURLY
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user staged similar persistence on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
        ParentImage|endswith:
        - \powershell.exe
        - \pwsh.exe
        Image|endswith: \schtasks.exe
        CommandLine|contains|all:
        - /Create
        - powershell.exe -NonI
        - /TN Updater /TR
        CommandLine|contains:
        - /SC ONLOGON
        - /SC DAILY /ST
        - /SC ONIDLE
        - /SC HOURLY
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, credential access, defense tampering, or additional persistence installation in the same window.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded this persistence and help reconstruct the timeline. Look for patterns: tampering followed by execution, or reconnaissance followed by persistence installation.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1053.005
      name: Scheduled Task
      role: primary
    - id: T1059.001
      name: PowerShell
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: parent_powershell_invocation
      description: A PowerShell parent (powershell.exe/pwsh.exe) drives the persistence module that shells out to schtasks.
      mapped_to: Q4
      source: template:parent_child.parent_process_legitimacy
    - stage: 2
      label: task_creation
      description: schtasks.exe /Create registers the Updater task with a powershell -NonI runner and a trigger schedule.
      mapped_to: Q5
      source: template:scheduled_task.task_configuration
    - stage: 3
      label: task_creation_event_confirmation
      description: The task registration is recorded as a Security 4698 event with task name and author.
      mapped_to: Q11
      source: attack:T1053.005.task_creation_event
    - stage: 4
      label: task_execution
      description: The Task Scheduler service launches the staged PowerShell payload on the configured trigger.
      mapped_to: Q9
      source: template:scheduled_task.task_trigger_and_timing
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1053.005
      covered_by: Q6
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1053.005
      covered_by: Q9
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1053.005
      covered_by: Q5
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: user_context
      technique: T1053.005
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q5
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: execution_context
      technique: T1053
      covered_by: Q5
      source: template:scheduled_task.task_configuration
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (task creation to execution proximity)"
      disposition: context
      target_question: Q9
      notes: "No numeric threshold supplied; task_trigger_and_timing carries the creation-to-execution temporal correlation."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (SYSTEM / non-standard accounts)"
      disposition: context
      target_question: Q2
      notes: "No fixed account values; user-expectation question plus user-actor baseline carry this. SubjectUserName surfaced via Q11 (4698)."
    - element: TaskNamePattern
      dimension: tuning_parameter
      specific_values: "/TN Updater (fixed by rule), /TR powershell.exe -NonI"
      disposition: query_injection
      target_question: Q1
      notes: "Task name Updater and runner powershell.exe -NonI are part of the rule's CommandLine|contains|all; verify_trigger_match is ProcessGuid-scoped so the alerting event already enforces them. task_configuration (Q5) reads the /TN, /TR, /SC values out of the command line."
    - element: CommandLineEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (encoded/obfuscated PowerShell payload)"
      disposition: context
      target_question: Q6
      notes: "No numeric threshold; encoded_command_decode and script_block_logging cover obfuscation analysis of the parent PowerShell command line qualitatively."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop, encoded/obfuscated PowerShell arguments"
      disposition: context
      target_question: Q6
      notes: "encoded_command_decode (KEEP_EXACT, ParentProcessGuid-scoped) reads the parent PowerShell command line; the alerting event is entity-scoped so no query-side CommandLine filter is required."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "abnormal parents of PowerShell (e.g., Office, mshta)"
      disposition: context
      target_question: Q4
      notes: "parent_process_legitimacy walks the PowerShell parent's own origin; example abnormal grandparents named in prose, not hard-coded into a query selection."
    - element: TaskAuthor
      dimension: tuning_parameter
      specific_values: "rationale only (unexpected initiating account)"
      disposition: new_question
      target_question: Q11
      notes: "Drove the attack:T1053.005.task_creation_event Security 4698 question; SubjectUserName field surfaces the recorded task author."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (suspicious binaries/scripts tied to the task)"
      disposition: context
      target_question: Q5
      notes: "task_configuration reads the /TR runner and full command line; no fixed regex values supplied."
    - element: ExecutionWindow
      dimension: tuning_parameter
      specific_values: "rationale only (lookback to correlate execution after registration)"
      disposition: context
      target_question: Q9
      notes: "task_trigger_and_timing +4h window correlates post-registration execution; no fixed lookback value supplied."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only (rare .NET assemblies)"
      disposition: context
      target_question: Q7
      notes: "No fixed module values and image_load logsource is out of scope for a schtasks-creation rule (see hint_dispositions); tool_capability_used covers the framework-configuration angle qualitatively."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (4104 length filter)"
      disposition: context
      target_question: Q7
      notes: "No queryable threshold; script_block_logging (Q7) surfaces 4104 content for the parent PowerShell without a length filter."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (hint) — parent_child.parent_process_legitimacy (Q4) subsumes the process-chain question with a more specific parent-lineage framing"
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.mechanism_decode
      reason: "Phase 2 parent decode is covered by powershell.encoded_command_decode (Q6), which is ParentProcessGuid-scoped to the same PowerShell parent and more specific to the PowerShell payload"
      kept: template:powershell.encoded_command_decode
    - dropped: cue_parent_process.follow_on
      reason: "siblings/follow-on angle covered by parent_child.sibling_processes (Q12, ParentProcessGuid-scoped) — more precise than the parent-binary-name follow_on query"
      kept: template:parent_child.sibling_processes
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: scheduled_task.task_configuration (hint) — task_configuration (Q5) is the Phase 2 decode for the schtasks command line"
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by: powershell.encoded_command_decode (hint) — parent is powershell.exe/pwsh.exe, so the PowerShell-specific decode supersedes the generic obfuscation question"
    - template: cue_parent_process.baseline
      reason: "parent-binary baseline subsumed by cue_user_actor.baseline_svc (Q14), which aggregates the parent+binary launch mechanism over 90d"
    - template: cue_parent_process.clustering
      reason: "host-window clustering covered by cue_host_context.clustering (Q17, same +/-15m host-scoped aggregation)"
    - template: cue_parent_process.scope
      reason: "cross-host scope of the parent+child pair covered by cue_user_actor.clustering_svc (Q16); 18-question cap reached"
    - template: cue_process_tool.baseline
      reason: "binary baseline subsumed by cue_user_actor.baseline_svc (Q14) parent+binary aggregation; schtasks.exe alone is too common to baseline meaningfully"
    - template: powershell.powershell_security_bypass
      reason: "the rule fires on task creation, not on a PowerShell bypass; bypass-before-execution coverage deprioritized against the 18-question cap in favor of task-persistence and user/host scoping questions"
    - template: persistence_scope.process_based_persistence
      reason: "other-persistence-tools sweep deprioritized against the 18-question cap; cue_host_context.clustering (Q17) surfaces additional persistence-tool executions in the same window"
    - template: persistence_scope.file_based_persistence
      reason: "file-based Startup/Run-key persistence is a parallel mechanism not implicated by this schtasks-creation rule; deprioritized against the 18-question cap"
    - template: organizational_context.change_ticket
      reason: "skip for hktl-prefixed detections per template notes — change tickets are irrelevant for default offensive-tool signatures"
    - template: organizational_context.human_recall
      reason: "user-recall coverage folded into host_user_context (Q2) and authorized_engagement (Q3); 18-question cap reached"
    - template: organizational_context.timing_alignment
      reason: "timing-vs-work-hours coverage folded into task_trigger_and_timing (Q9) and host_context.clustering (Q17); 18-question cap reached"
    - template: organizational_context.procedural_consistency
      reason: "procedural-consistency for schtasks folded into host_user_context (Q2, expected to create task persistence this way); 18-question cap reached"
    - template: universal.endpoint_protection_health
      reason: "the defense_tampering technique set is not confirmed and cue_host_context.precursor (Q18) already surfaces prior tamper alerts on the host; the rule fires on persistence creation, not payload execution, so the generic tamper sweep is dropped under the 18-question cap"
    - template: hacking_tool.tool_specific_artifacts
      reason: "file-artifact sweep for offensive tools deprioritized against the 18-question cap; the persistence artifact is the registered task itself, covered by task_still_present (Q10) and the 4698 event (Q11)"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1059.001) is not covered — pull in cue_image_load.* or author a custom question with category: image_load"
      disposition: declined
      reason: >-
        This rule fires on schtasks.exe creating a scheduled task from a
        PowerShell parent; the persistence artifact is the registered task and
        the parent PowerShell command line, not a .NET assembly loaded into a
        PowerShell host. image_load on powershell.exe returns volume without
        precision for this task-creation behavior. The PowerShell payload's
        in-memory execution (if any) happens later, when the task fires, and is
        covered by task_trigger_and_timing (Q9) and script_block_logging (Q7).
        No compensating attack:T1059.001.image_load question authored.
    - hint: "logsource 'registry_set' (recommended by techniques: T1053.005) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: declined
      reason: >-
        The TaskCache registry writes a scheduled task produces are the same
        artifact the rule already detects at the process_creation/schtasks.exe
        layer, and the authoritative task-registration evidence is the Security
        4698 event, which is covered by attack:T1053.005.task_creation_event
        (Q11) and task_still_present (Q10). A registry_set question on TaskCache
        keys would duplicate that confirmation without adding an independent
        investigative angle for this specific schtasks-driven creation.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - scheduled_task
    - powershell
    - hacking_tool
    - parent_child
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 15
      powershell_script_block: 1
      security: 1
      sigma_alert: 1
