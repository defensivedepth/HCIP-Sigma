name: Suspicious Invoke-WebRequest Execution
id: c4608586-7376-4446-a4f9-357fd14f4c96
description: |
  Detects a PowerShell process (powershell.exe, powershell_ise.exe, or pwsh.exe) invoking a
  download cmdlet (Invoke-WebRequest, iwr, curl, or wget) together with an output flag (-uri/-url
  or -outfile/-o) where the command line also references a user-writable or staging location such
  as AppData, Desktop, Temp, Users\Public, or C:\Windows. This is the download-cradle phase of
  ingress tool transfer: a signed interpreter pulls a remote file and writes it to a low-friction
  drop path, where a follow-on signed binary may side-load a weaponized DLL. The rule fires only
  when all four conditions match simultaneously, so the investigation should confirm the URL and
  destination, verify whether the file actually landed, and determine whether the download was
  expected for this user, host, and parent process.
type: detection
detection_id: 5e3cc4d8-3e68-43db-8656-eaaeefdec9cc
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show a PowerShell download cmdlet writing to a user-writable or staging location?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires only when
    all four conditions are present at once - a PowerShell binary (powershell.exe, powershell_ise.exe, or pwsh.exe),
    a download cmdlet (Invoke-WebRequest, iwr, curl, or wget), an output flag (-uri/-url or -outfile/-o), and a
    suspicious destination (AppData, Desktop, Temp, Users\Public, or C:\Windows). Verify all four are genuinely
    present before investing further.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to download files with command-line PowerShell into staging directories?
  context: Legitimate administration and software installs occasionally fetch files via command-line tools, but a
    download cradle that writes into AppData, Temp, or Users\Public is unusual for most user roles. Knowing whether
    this host and account routinely perform scripted downloads is often the fastest way to separate routine activity
    from operator-driven staging.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.evidence_preservation
  question: Is the downloaded file still on disk at the destination path for analysis?
  context: The command line names an output path under a user-writable or staging location. If that file still exists,
    it can be hashed, submitted for reputation lookup, or reviewed - especially relevant when the dropped file is a
    DLL or log file staged next to a signed binary for side-loading.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: What is the full Invoke-WebRequest command - which URL is fetched and where is the output written?
  context: Decode the complete command line to extract the source URL (the -uri/-url value) and the output destination
    (the -outfile/-o value). The download itself may be benign; what matters is the remote host being contacted and
    whether the destination path stages a payload for later execution. Scoping to this binary on this host within a
    few minutes also surfaces sequential download commands that together form one staging operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - 'curl '
          - 'Invoke-WebRequest'
          - 'iwr '
          - 'wget '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Is the download command wrapped in encoding or obfuscation, and what does it resolve to?
  context: The download cmdlet may be readable, but the URL, output path, or surrounding logic can be hidden with
    base64 (-EncodedCommand/-enc), string concatenation, backtick escaping, or variable substitution. Decode any
    such wrapping to recover the true source URL and destination. A fetched file that is immediately executed or
    loaded into memory indicates the transfer was a delivery step rather than a benign download.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated download script?
  context: Script block logging (Event ID 4104) records the script content PowerShell actually executed, even when
    the command line is encoded or obfuscated, providing ground truth about the URL contacted and the file written.
    If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility gap
    rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: The parent reveals how the download was invoked. A legitimate updater or interactive session (explorer.exe,
    a console host) is routine; parents such as Office applications, mshta.exe, wscript.exe, web-server worker
    processes, or a web shell point to exploitation-driven execution. Known-good updater parents (for example
    software-update services) can be treated as expected once confirmed.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1105.network_connection
  question: What outbound connections did this PowerShell instance make, and are the destinations external and uncommon?
  context: Ingress tool transfer pairs a process initiating an outbound connection with a file write. Pivot from the
    alerting process to its network connections to identify the remote host the cradle contacted. External destinations
    that are uncommon for this host - especially raw IP addresses or hosts on non-standard ports - are higher concern
    than connections to known internal or vendor infrastructure; filter out routine internal ranges when calibrating.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
- source: template:download_tool.download_success_verification
  question: Did the file actually land at the destination path?
  context: A download command does not guarantee the transfer succeeded - network errors, blocked URLs, or controls
    may have stopped it. Confirming a file-creation event at the named output path under the user-writable or staging
    directory establishes whether the operation progressed past delivery.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: If the downloaded file was executed, what is its hash and reputation?
  context: The hash for a reputation lookup is recorded when the payload runs, not when Invoke-WebRequest writes it to
    disk — file-creation events carry no hash. If PowerShell then runs it, the payload is a child or sibling of the
    alerting process, so this query returns those processes with their executable hashes. If instead a weaponized DLL
    was staged beside a signed executable to be side-loaded, it won't appear here as a process — check its load event
    (dll.hash) and review the drop directory. If the file was run later by an unrelated process, retrieve it from disk
    and hash it manually.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_children:
        ParentProcessGuid|expand: '%ProcessGuid%'
      selection_siblings:
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: 1 of selection_*
    fields:
      - Image
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the same drop directory?
  context: Staging operations often write multiple files to one directory - a signed binary, a weaponized DLL, and an
    encrypted payload, or several sequential downloads. Examining directory-level file writes in user-writable or
    staging paths reveals the broader scope of the delivery beyond the single file named in the alert.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened before this download ran?
  context: Operators frequently disable or evade PowerShell controls before fetching a payload. Common precursors include
    AMSI bypass (patching amsi.dll in memory), execution-policy changes (Set-ExecutionPolicy or -ep bypass), and version
    downgrade to PowerShell v2 to evade logging. Finding these on the host shortly before the download strengthens the
    case that the transfer was a delivery step.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this download-cradle pattern, what parents and users drove it, and does the current alert fit?
  context: Even common tools can be abused. The query mirrors the rule's selection structure scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing pattern - not generic PowerShell usage - has
    run here. A new parent process, an unexpected account, or an unusual command structure relative to that history is the
    anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-WebRequest
        - 'iwr '
        - 'wget '
      selection_flags:
        CommandLine|contains:
        - ' -ur'
        - ' -o'
      selection_susp_locations:
        CommandLine|contains:
        - \AppData\
        - \Desktop\
        - \Temp\
        - \Users\Public\
        - '%AppData%'
        - '%Public%'
        - '%Temp%'
        - '%tmp%'
        - :\Windows\
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-WebRequest
        - 'iwr '
        - 'wget '
      selection_flags:
        CommandLine|contains:
        - ' -ur'
        - ' -o'
      selection_susp_locations:
        CommandLine|contains:
        - \AppData\
        - \Desktop\
        - \Temp\
        - \Users\Public\
        - '%AppData%'
        - '%Public%'
        - '%Temp%'
        - '%tmp%'
        - :\Windows\
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-WebRequest
        - 'iwr '
        - 'wget '
      selection_flags:
        CommandLine|contains:
        - ' -ur'
        - ' -o'
      selection_susp_locations:
        CommandLine|contains:
        - \AppData\
        - \Desktop\
        - \Temp\
        - \Users\Public\
        - '%AppData%'
        - '%Public%'
        - '%Temp%'
        - '%tmp%'
        - :\Windows\
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of this alert?
  context: Tracking the account's activity across all hosts in the alert window reveals whether the download is isolated or
    part of broader hands-on activity such as lateral movement or multi-host staging. The same user driving similar activity
    on several endpoints widens the apparent scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - powershell_ise.EXE
        - PowerShell.EXE
        - pwsh.dll
      selection_commands:
        CommandLine|contains:
        - 'curl '
        - Invoke-WebRequest
        - 'iwr '
        - 'wget '
      selection_flags:
        CommandLine|contains:
        - ' -ur'
        - ' -o'
      selection_susp_locations:
        CommandLine|contains:
        - \AppData\
        - \Desktop\
        - \Temp\
        - \Users\Public\
        - '%AppData%'
        - '%Public%'
        - '%Temp%'
        - '%tmp%'
        - :\Windows\
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1105
      name: Ingress Tool Transfer
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: process_initiates_transfer
      description: "An uncommon process initiates an outbound network connection to an external destination (the download cmdlet contacting the source URL)"
      mapped_to: Q8
      source: attack:T1105.network_connection
    - stage: 2
      label: file_creation
      description: "The transfer results in file creation at a drop path (tool downloaded to disk)"
      mapped_to: Q9
      source: template:download_tool.download_success_verification
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1105
      covered_by: Q9
      source: template:download_tool.download_success_verification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1105
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1105
      covered_by: Q8
      source: attack:T1105.network_connection
      coverage_type: direct
  mutable_dispositions:
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: "C:\\Users\\Public\\ (and rule's staging tokens: \\AppData\\, \\Desktop\\, \\Temp\\, \\Users\\Public\\, :\\Windows\\)"
      disposition: query_injection
      target_question: Q9
      notes: "Drop-path focus realized via %file_paths% / %file_dirs% placeholders in evidence_preservation (Q3), download_success_verification (Q9), payload_analysis (Q10), and directory_activity (Q11); the rule's own susp-location tokens are embedded verbatim in the reflected baseline_deviation query (Q13)."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (known-good updaters, e.g. ChromeUpdate, OneDrive)"
      disposition: context
      target_question: Q7
      notes: "No concrete value list in the mutable element - it is a tuning rationale for excluding known-good updaters. Folded into process_chain (Q7) context as guidance to treat confirmed software-update parents as expected; also the pivot field for the parent+binary substitute pair (Q15, Q17)."
    - element: DestinationIPCategory
      dimension: tuning_parameter
      specific_values: "rationale only (internal vs external IP blocks)"
      disposition: context
      target_question: Q8
      notes: "No concrete IP/port list - tuning rationale to filter internal vs external destinations. Realized as the network_connection question (Q8) returning DestinationIp/DestinationPort/DestinationHostname, with context directing the analyst to weight external/uncommon destinations and filter routine internal ranges."
  dedup_decisions:
    - dropped: cue_user_actor.baseline_svc paired with cue_user_actor.baseline
      reason: "user_actor pair - non-deduplicable (subject identity vs launch-mechanism identity; degenerate under different conditions)"
      kept: both (Q14 user-pivot baseline, Q15 parent+binary substitute)
    - dropped: cue_user_actor.clustering_svc paired with cue_user_actor.clustering
      reason: "user_actor pair - non-deduplicable (subject identity vs launch-mechanism identity; degenerate under different conditions)"
      kept: both (Q16 user-pivot clustering, Q17 parent+binary substitute)
    - dropped: cue_process_tool.baseline
      reason: "host-scoped historical frequency over generic Image is subsumed by the rule-pattern-scoped baseline_deviation (Q13), which counts the actual firing pattern on this host rather than generic PowerShell usage"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_host_context.clustering
      reason: "host-wide around-the-alert activity is covered by cue_user_actor.clustering (Q16) for actor scope and the network/file follow-on questions; dropping it keeps the playbook within the 18-question cap while preserving the load-bearing T1105 chain coverage"
      kept: cue_user_actor.clustering
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "child-process follow-on is lower-value for a download-cradle rule whose impact manifests as file writes and network connections rather than spawned children; file/network follow-on (Q8-Q11) covers the actual progression. Dropped to stay within the 18-question cap."
    - template: cue_process_tool.clustering
      reason: "broad host-wide tool co-occurrence overlaps cue_user_actor.clustering (Q16); dropped to stay within the 18-question cap without losing chain coverage."
    - template: cue_process_tool.scope
      reason: "fleet-wide rule-pattern count (REFLECTED) is informational for this rule; host-scoped baseline_deviation (Q13) plus the parent+binary cross-host substitute (Q17) already supply the cross-host calibration the analyst needs. Dropped to stay within the 18-question cap."
    - template: cue_host_context.precursor
      reason: "prior-alert timeline is partially served by powershell_security_bypass (Q12, prior tamper/bypass on the host); a download-cradle rule's disposition turns more on the URL, destination file, and parent than on a full prior-alert sweep. Dropped to stay within the 18-question cap."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. powershell_security_bypass (Q12) already surfaces prior AMSI/Defender/execution-policy tampering on the host in the hour before the alert, which is the tamper-as-precursor signal for this PowerShell download rule; the generic host-tamper sweep would duplicate that coverage. defense_tampering is not a confirmed technique set for this rule."
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted_on
      reason: "powershell.encoded_command_decode is included as Q5; the superseded obfuscation.encoded_payload_decode template was not emitted."
    - hint: "File paths derived from CommandLine - use %file_paths% placeholder"
      disposition: acted_on
      reason: "All file-artifact questions (evidence_preservation Q3, download_success_verification Q9, payload_analysis Q10, directory_activity Q11) scope on %file_paths% / %file_dirs% rather than a literal path, since the destination is parsed from the CommandLine at hydration time."
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' comes from the reference write-up, not the rule's detection logic. The rule fires on a generic PowerShell download cradle (T1105 ingress tool transfer), not on encryption/impact behavior; ransomware-impact questions would inject incident-specific framing the rule does not detect."
      - id: network_scope
        confidence: low
        reason: "Same incidental 'ransomware' keyword origin. Network scope is already addressed by the T1105 network_connection question (Q8) and the cross-host substitute clustering (Q17); a separate network_scope set would not add a dimension the rule's detection supports."
  technique_sets_applied:
    - universal
    - powershell
    - download_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 11
      file_event: 4
      network_connection: 1
      powershell: 1
