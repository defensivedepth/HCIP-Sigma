name: Suspicious Parent Double Extension File Execution
id: ba99f87a-e084-4be2-bfbb-aa9d26559316
description: |
  Detects a process whose parent was launched from a file using a double extension — a benign-looking
  first extension (.doc, .docx, .xls, .xlsx, .ppt, .pptx, .rtf, .pdf, .txt) immediately followed by an
  executable second extension (.lnk or .js). The rule fires when the parent process's image path ends in
  one of these double extensions OR the parent command line contains one. Double-extension naming masquerades
  an executable launcher as a harmless document so a user double-clicks it expecting to open a file; the
  spawned child is then the first process of the delivery chain. Investigation should confirm the parent file
  name, identify how it arrived and what it spawned, and determine whether the user and host context fit a
  benign explanation or an initial-access execution.
type: detection
detection_id: 5e6a80c8-2d45-4633-9ef4-fa2671a39c5c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the parent image or parent command line end in a benign-then-executable double extension?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when the
    parent's image path ends in a document-then-executable double extension (such as .doc.lnk or .pdf.js) or
    when the parent command line contains one. Read the full ParentImage and ParentCommandLine and confirm the
    visible "document" extension is actually followed by .lnk or .js.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to open document files that launch executables?
  context: Legitimate document workflows open in their associated application, not by spawning a child process
    from a .lnk or .js launcher. Knowing whether the user routinely receives and opens attachments, and whether
    the host function fits that behavior, is often the fastest path to distinguishing a phishing-delivered
    launcher from a benign file. Standard-user sessions opening such files point toward user-initiated execution;
    activity under an elevated or service context shifts the interpretation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What is the double-extension parent file, and what spawned the parent itself?
  context: The parent double-extension file is the launcher. Retrieving the parent's own process_creation event
    shows the parent's full command line and the grandparent that started it — for example an archive utility,
    a browser download handler, or explorer.exe acting on a user double-click. The grandparent reveals the
    delivery vector that placed and triggered the launcher.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: template:masquerading.true_identity
  question: What is the actual identity of the spawned child process based on its metadata and hash?
  context: The child launched by the double-extension parent is the first executing payload of the chain.
    Comparing the Image path against OriginalFileName reveals whether the child is a renamed tool, and the
    signing details indicate whether it is a Microsoft-signed system binary, a third-party signed tool, or
    unsigned. A mismatch between OriginalFileName and the Image name, or execution from a non-standard
    directory, is a second masquerading layer. The file hash enables a reputation lookup to determine whether
    this is a known-good tool, known malware, or a novel binary.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: attack:T1036.007.benign_extension_artifact
  question: When and how was the double-extension parent file created on this host, and where does it live?
  context: Double-extension launchers do not appear spontaneously — they are downloaded, extracted from an
    archive, or dropped by another process. Tracing the file_event that created a file ending in a
    document-then-executable extension reveals the delivery mechanism and the directory it landed in.
    Files appearing in user-writable locations (Downloads, %TEMP%, Desktop, AppData) are consistent with
    a downloaded or extracted attachment; the creating process shown in Image is the dropper, archive tool,
    or download handler.
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - .doc.lnk
          - .docx.lnk
          - .xls.lnk
          - .xlsx.lnk
          - .ppt.lnk
          - .pptx.lnk
          - .rtf.lnk
          - .pdf.lnk
          - .txt.lnk
          - .doc.js
          - .docx.js
          - .xls.js
          - .xlsx.js
          - .ppt.js
          - .pptx.js
          - .rtf.js
          - .pdf.js
          - .txt.js
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1036.007.time_correlation
  question: How much time elapsed between creation of the double-extension file and its execution?
  context: Correlating the file_event that wrote the double-extension launcher with the process_creation that
    ran it bounds the activity. A short interval between file creation and execution — seconds to minutes —
    is consistent with a user opening a freshly delivered attachment, while a launcher that sat dormant for
    days before firing points to delayed or staged execution. Compare the creation timestamp from the file
    event against the alert time to establish the window.
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - .doc.lnk
          - .docx.lnk
          - .xls.lnk
          - .xlsx.lnk
          - .ppt.lnk
          - .pptx.lnk
          - .rtf.lnk
          - .pdf.lnk
          - .txt.lnk
          - .doc.js
          - .docx.js
          - .xls.js
          - .xlsx.js
          - .ppt.js
          - .pptx.js
          - .rtf.js
          - .pdf.js
          - .txt.js
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the spawned child process do? Decode its command-line arguments.
  context: The child's full command line reveals the action the launcher initiated. Decode any flags, encoded
    content, file paths, or URLs. Double-extension launchers commonly chain into a shell or script host that
    iterates directories, re-extracts an archive, or fetches a follow-on payload, so the arguments often name
    the next stage and its location.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_parent_process.follow_on
  question: What other child processes did the double-extension parent spawn?
  context: A delivery launcher often spawns several processes in sequence — a decoy document viewer to satisfy
    the victim, plus a background chain that extracts, decodes, or downloads the actual payload. Seeing every
    child of the same parent reveals the full scope of activity initiated through this launcher rather than only
    the one process that tripped the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.clustering
  question: What file and process activity occurred around the double-extension parent at the same time?
  context: Beyond direct children, the launcher chain may write files (extracted archives, dropped payloads,
    decoy documents) or stage binaries in temp directories. Examining process activity tied to the same parent
    in a tight window around the alert helps reconstruct what the launcher set in motion and where it placed
    follow-on files.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has this parent process name spawned this child binary on this host before?
  context: A parent-child pairing that has never been seen on the host is more suspicious than a recurring one.
    Because the parent here is a double-extension file, almost any historical match is itself notable; a zero or
    very low count confirms this is a novel spawning relationship rather than an established workflow.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run this binary?
  context: Compare this user's current action against their historical behavior. A user who has never run this
    binary, or never opened files that launch executables, is more suspicious than one who does so routinely.
    User role and typical toolset should align with the activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-name and child-binary combination historically routine on this host or environment-wide?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism — which parent spawned this binary, and whether that pair is
    historically common. A new or rare parent for this binary is the deviation worth chasing; a pair that
    already accounts for many prior events is more likely established automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether the launcher execution was an isolated
    event or part of broader hands-on activity. If the same user triggered similar activity on multiple
    endpoints in the same window, the scope of the incident may be larger than one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-name and child-binary combination fire on other hosts in the same window?
  context: Under a service principal, asking what else the user did returns agent-telemetry noise. The meaningful
    question is whether the same launch mechanism — parent process plus spawned binary — fired on other hosts in
    the alert window. Multiple hosts in the same window points to a coordinated rollout or phishing wave; a single
    host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_parent_process.scope
  question: Is the same double-extension parent spawning this binary on other hosts?
  context: The same launcher-and-child pairing appearing across multiple hosts may indicate a phishing wave or
    distribution campaign, while a single occurrence is more likely targeted. An environment-wide comparison
    helps calibrate severity and identify the scope of delivery.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context —
    what led to this execution and what happened after. Aggregating by Image and ParentImage collapses repetitive
    agent telemetry while novel parent-child pairs stand out at low counts, which is what distinguishes the
    delivery chain from background noise.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, defense tampering, or other execution events —
    may reveal the stages that preceded this launcher and help reconstruct the timeline. For an initial-access
    execution like a double-extension file, the absence of prior alerts is consistent with this being the entry
    point of the chain.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1036.007
    name: Double File Extension
    role: primary
  - id: T1036
    name: Masquerading
    role: secondary
  attack_chain_coverage:
  - stage: 1
    label: file_creation_misleading_naming
    description: A file with a benign-looking first extension and a dangerous executable second extension is
      written to disk (downloaded, extracted, or dropped).
    mapped_to: Q5
    source: attack:T1036.007.benign_extension_artifact
  - stage: 2
    label: user_or_system_execution
    description: The disguised double-extension file is executed and spawns a child process — the alerting event.
    mapped_to: Q1
    source: template:universal.verify_trigger_match
  - stage: 3
    label: follow_on_payload_activity
    description: The launcher chain spawns additional processes (decoy document, extractor, downloader) that
      carry out the operation.
    mapped_to: Q8
    source: template:cue_parent_process.follow_on
  dimension_coverage:
  - dimension: time_correlation
    technique: T1036.007
    covered_by: Q6
    source: attack:T1036.007.time_correlation
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1036.007
    covered_by: Q5
    source: attack:T1036.007.benign_extension_artifact
    coverage_type: direct
  - dimension: user_context
    technique: T1036.007
    covered_by: Q2
    source: template:universal.host_user_context
    coverage_type: direct
  - dimension: execution_context
    technique: T1036
    covered_by: Q4
    source: template:masquerading.true_identity
    coverage_type: direct
  - dimension: time_correlation
    technique: T1036
    covered_by: Q6
    source: attack:T1036.007.time_correlation
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1036
    covered_by: Q5
    source: attack:T1036.007.benign_extension_artifact
    coverage_type: direct
  mutable_dispositions:
  - element: benign_extensions
    dimension: tuning_parameter
    specific_values: ".doc, .docx, .xls, .xlsx, .ppt, .pptx, .rtf, .pdf, .txt (first extension of the rule's parent suffix list)"
    disposition: query_injection
    target_question: Q5
    notes: Injected as the document-extension prefixes of the TargetFilename|contains list in the file-creation
      question (and the time-correlation question Q6), mirroring the rule's parent suffix list.
  - element: dangerous_extensions
    dimension: tuning_parameter
    specific_values: ".lnk, .js (second extension of the rule's parent suffix list)"
    disposition: query_injection
    target_question: Q5
    notes: Injected as the executable-extension suffixes of the TargetFilename|contains list in Q5 and Q6.
      The rule itself only fires on .lnk and .js second extensions, so .exe/.scr/.hta from the analytic's
      example list are intentionally not added — adding them would broaden beyond what this rule detects.
  - element: monitored_paths
    dimension: tuning_parameter
    specific_values: "Downloads, %TEMP%, Desktop, AppData"
    disposition: context
    target_question: Q5
    notes: Tuning rationale, not a rule field value. Surfaced in Q5 context to direct the analyst's attention
      to user-writable landing directories; the file_event query returns TargetFilename so the directory is
      visible without hard-filtering paths (which would drop legitimately staged variants).
  - element: TimeWindow
    dimension: time_correlation
    specific_values: rationale only
    disposition: new_question
    target_question: Q6
    notes: Drove the dedicated time-correlation question Q6 comparing file-creation timestamp to execution time.
  - element: UserContext
    dimension: user_context
    specific_values: rationale only
    disposition: context
    target_question: Q2
    notes: Standard-user vs. elevated/service context distinction surfaced in Q2 context.
  - element: OriginalFilenameMismatch
    dimension: tuning_parameter
    specific_values: rationale only
    disposition: query_injection
    target_question: Q4
    notes: true_identity query returns OriginalFileName alongside Image for the spawned child so the analyst
      can detect a metadata/name mismatch.
  - element: KnownSystemUtilityPaths
    dimension: tuning_parameter
    specific_values: rationale only
    disposition: context
    target_question: Q4
    notes: Expected-install-directory rationale surfaced in Q4 context (execution from a non-standard
      directory as a second masquerading layer).
  - element: TimeWindow
    dimension: time_correlation
    specific_values: rationale only
    disposition: new_question
    target_question: Q6
    notes: T1036's correlation-window element shares Q6 with the T1036.007 TimeWindow element.
  dedup_decisions:
  - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
    reason: user_actor pair — non-deduplicable
    kept: both emitted (Q11 user-pivot, Q12 service-principal substitute)
  - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
    reason: user_actor pair — non-deduplicable
    kept: both emitted (Q13 user-pivot, Q14 service-principal substitute)
  - dropped: masquerading.binary_origin
    reason: >-
      Confirmed-set template targets the child binary basename (%file_name%) via file_event. For this rule the
      masquerading object is the PARENT double-extension file, not the spawned child; delivery/origin of that
      file is covered more precisely by Q5 (attack:T1036.007.benign_extension_artifact), which scopes the
      file_event to the rule's own double-extension suffix list. Child-binary origin would investigate the
      wrong artifact.
    kept: attack:T1036.007.benign_extension_artifact
  - dropped: masquerading.renamed_binary_activity
    reason: >-
      Confirmed-set template asks what the renamed binary did and whether it appears on other hosts, keyed on
      ParentImage|endswith %Image% (the spawned child as parent). Here the load-bearing follow-on activity is
      the children of the double-extension launcher, covered by Q8 (cue_parent_process.follow_on), and cross-host
      scope of the launcher pattern is covered by Q15 (cue_parent_process.scope). Re-running the child-as-parent
      query would duplicate Q8's intent on the wrong entity.
    kept: cue_parent_process.follow_on (Q8), cue_parent_process.scope (Q15)
  skipped_templates:
  - template: cue_process_tool.process_chain
    reason: "parent_process is primary cue (per hint) — drop cue_process_tool:process_chain"
  - template: cue_process_tool.baseline
    reason: >-
      Child-binary host baseline is subsumed by the parent-child baseline Q10 (cue_parent_process.baseline),
      which is the discriminating pivot for a parent-keyed rule. A bare child-binary baseline would return
      volume for common interpreters (cmd.exe, wscript.exe) without isolating the double-extension launch.
  - template: universal.endpoint_protection_health
    reason: >-
      RECOMMENDED, not must_include. This rule fires on initial-access execution (a double-extension launcher),
      not on credential access, privilege escalation, or impact where tamper-as-precursor is the live question.
      The defense_tampering technique set is not confirmed, and prior-alert context is already provided by Q17
      (cue_host_context.precursor). A generic AV/EDR tamper sweep does not earn its slot for an entry-point
      execution alert.
  reflection_overrides: []
  hint_dispositions:
  - hint: "parent_process is primary — drop cue_process_tool:process_chain"
    disposition: acted_on
    reason: >-
      Acted on. cue_process_tool.process_chain was dropped (see skipped_templates); the parent chain is
      investigated by Q3 (cue_parent_process.mechanism_decode, which walks up via ParentProcessGuid) and Q8
      (cue_parent_process.follow_on). Recorded here for audit.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - masquerading
  - cue_parent_process
  - cue_process_tool
  - cue_user_actor
  - cue_host_context
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 14
      file_event: 2
      sigma_alert: 1
