name: Root Certificate Installed From Susp Locations
id: 2ee706c4-89f8-4f6c-9635-f588466d8304
description: |
  Detects a PowerShell Import-Certificate command that installs a certificate into the
  machine-wide root store (Cert:\LocalMachine\Root) using a -FilePath that points to a
  user-writable or transient directory — AppData\Local\Temp, Windows\TEMP, Desktop,
  Downloads, Perflogs, or Users\Public. The rule fires when the command line contains
  all three of Import-Certificate, ' -FilePath ', and Cert:\LocalMachine\Root AND
  references one of those suspicious source directories. Adding a certificate to the
  machine Root store grants it system-wide trust and requires administrative rights;
  legitimate enterprise CAs are deployed from managed locations via Group Policy, not
  staged in a temp or Downloads folder. The investigation should confirm the exact
  command and source file, identify the certificate being installed, and determine
  whether the install was an administrative action or an operator subverting trust to
  enable TLS interception or signed-payload acceptance.
type: detection
detection_id: 5f6a601c-2ecb-498b-9c33-660362323afa
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match the rule — an Import-Certificate -FilePath into Cert:\LocalMachine\Root sourced from a temp, Downloads, Desktop, Public, or Perflogs directory?
  context: Before investing investigation time, confirm the alert is a true positive. The
    rule requires all three of Import-Certificate, the -FilePath switch, and the
    Cert:\LocalMachine\Root store target on the same command line, plus a source path under
    a user-writable or transient directory. Read the full command line and verify each part
    is present and that the source file genuinely lives in one of those locations rather
    than a managed deployment path.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to install root certificates?
  context: Installing a certificate into the machine root store is an administrative action
    normally performed by IT through managed deployment, not by individual users staging a
    file in a temp or Downloads folder. Knowing whether the account is a local admin and
    whether the host's function involves certificate management is often the fastest way to
    separate a sanctioned install from a trust-subversion attempt.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What certificate is being installed, and what is the full source path and store target on the command line?
  context: The full command line names the certificate file and the store it is written to.
    Decode the -FilePath argument to recover the source file (.cer, .p7b, or .sst) and its
    directory, and confirm the store is Cert:\LocalMachine\Root rather than a per-user or
    My store. Note that Import-Certificate adds the certificate without the warning prompt
    the certificate snap-in shows, so a successful install may have left no interactive
    trace. Multiple Import-Certificate invocations on this host in the window may install a
    chain rather than a single cert.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Import-Certificate'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_artifact.mechanism_decode
  question: What is in the certificate file being imported — issuer, subject, validity, and whether it spoofs a known CA?
  context: Read the actual certificate referenced by -FilePath. Inspect the issuer and
    subject names, key usage, validity dates, and thumbprint. A root certificate whose
    issuer impersonates a well-known CA or vendor, has an unusually long validity, or
    chains to an unknown self-signed authority is the artifact of trust subversion. A
    certificate installed into the machine root store can be used to forge trusted TLS
    certificates, accept man-in-the-middle traffic, or make signed payloads appear valid.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_artifact.artifact_verification
  question: What is the hash of the certificate file, and does it have known threat-intel detections?
  context: The certificate file's hash provides a unique identifier for reputation lookup.
    Submit it to threat-intel sources to determine whether the file is known malicious,
    known benign, or previously unseen. A first-seen certificate file staged in a temp or
    Downloads directory immediately before a root-store install is worth verifying against
    any internal record of approved enterprise certificates.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Hashes
- source: template:universal.evidence_preservation
  question: Is the referenced certificate file still on disk for analysis?
  context: The rule keys on a -FilePath pointing to a source certificate in a user-writable
    or transient directory. Checking whether that file still exists enables deeper analysis
    — hash lookup, inspection of the certificate's issuer and subject, and confirmation of
    what was added to the trust store. Files staged in Temp or Downloads are often deleted
    after use, so capture them quickly.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: attack:T1553.004.registry_set
  question: Was a new certificate written into the machine root certificate store in the registry around the time of the alert?
  context: Installing a certificate into Cert:\LocalMachine\Root persists it under the
    SystemCertificates\Root\Certificates registry path, keyed by the certificate
    thumbprint. A registry-set event records the store write directly, independent of which
    process performed it, and catches installs done through the .NET X509Store API or
    certutil rather than the PowerShell cmdlet. A new entry under the machine Root or
    TrustedPublisher store — the two stores that grant trust system-wide — confirms the
    certificate took effect and gives the thumbprint to cross-reference.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - '\SystemCertificates\Root\Certificates\'
          - '\SystemCertificates\TrustedPublisher\Certificates\'
          - '\Microsoft\SystemCertificates\Root\'
      condition: selection
    fields:
      - TargetObject
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned the PowerShell that ran Import-Certificate, and is that parent chain expected?
  context: The parent process reveals how the certificate install was invoked. An
    interactive administrator running PowerShell from explorer.exe or a console is different
    from PowerShell spawned by a script host, an Office application, a remote-execution
    service, or a downloaded loader. An unexpected parent points to the install being part
    of an automated or hands-on-keyboard operation rather than routine administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit?
  context: Even legitimate certificate management leaves a footprint. The query mirrors the
    rule's exact selection — Import-Certificate into Cert:\LocalMachine\Root from a
    suspicious source directory — scoped to this host, so the parent/user distribution
    reflects how this specific firing pattern has run here before. A new parent process, an
    unexpected user account, or a first-ever occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - Import-Certificate
        - ' -FilePath '
        - Cert:\LocalMachine\Root
        CommandLine|contains:
        - \AppData\Local\Temp\
        - :\Windows\TEMP\
        - \Desktop\
        - \Downloads\
        - \Perflogs\
        - :\Users\Public\
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a
    fleet-wide count of this exact firing pattern rather than generic PowerShell usage. Many
    hosts installing root certificates from suspicious source directories points to a
    scripted deployment or a configuration-management push that should be confirmed with the
    owning team; one host or a small unrelated set points to manual operator activity. Use
    the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - Import-Certificate
        - ' -FilePath '
        - Cert:\LocalMachine\Root
        CommandLine|contains:
        - \AppData\Local\Temp\
        - :\Windows\TEMP\
        - \Desktop\
        - \Downloads\
        - \Perflogs\
        - :\Users\Public\
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - Import-Certificate
        - ' -FilePath '
        - Cert:\LocalMachine\Root
        CommandLine|contains:
        - \AppData\Local\Temp\
        - :\Windows\TEMP\
        - \Desktop\
        - \Downloads\
        - \Perflogs\
        - :\Users\Public\
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - Import-Certificate
        - ' -FilePath '
        - Cert:\LocalMachine\Root
        CommandLine|contains:
        - \AppData\Local\Temp\
        - :\Windows\TEMP\
        - \Desktop\
        - \Downloads\
        - \Perflogs\
        - :\Users\Public\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the account's activity across all hosts in the surrounding window reveals
    lateral movement or coordinated activity on multiple endpoints. If the same user
    installed certificates or ran related commands elsewhere, the scope is larger than this
    one host. Trust subversion on multiple endpoints points to a deliberate effort to
    establish broad interception or signed-payload acceptance.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - Import-Certificate
        - ' -FilePath '
        - Cert:\LocalMachine\Root
        CommandLine|contains:
        - \AppData\Local\Temp\
        - :\Windows\TEMP\
        - \Desktop\
        - \Downloads\
        - \Perflogs\
        - :\Users\Public\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Surveying activity around the alert reveals what
    led to the certificate install and what followed — reconnaissance, credential access, or
    further configuration changes. Aggregating by process and parent collapses repetitive
    agent telemetry into high-count rows while novel parent-child pairs stand out at low
    counts, which is what the analyst is scanning for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense
    tampering, reconnaissance, or credential access — may reveal the attack stages that
    preceded the certificate install and help reconstruct the timeline. Trust subversion is
    typically a setup step for later command-and-control or payload acceptance, so prior
    alerts give the broader picture.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1553.004
      name: Install Root Certificate
      role: primary
    - id: T1553
      name: Subvert Trust Controls
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: source_file_staging
      description: "Certificate file is staged in a user-writable or transient directory (Temp, Downloads, Desktop, Public, Perflogs)"
      mapped_to: Q6
      source: template:universal.evidence_preservation
    - stage: 2
      label: certificate_install_command
      description: "Import-Certificate -FilePath installs the cert into Cert:\\LocalMachine\\Root (the detection point)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: store_modification
      description: "Certificate is written into the machine root/TrustedPublisher certificate store, granting system-wide trust"
      mapped_to: Q7
      source: attack:T1553.004.registry_set
  dimension_coverage:
    - dimension: time_correlation
      technique: T1553.004
      covered_by: Q7
      source: attack:T1553.004.registry_set
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1553.004
      covered_by: Q4
      source: template:cue_artifact.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1553
      covered_by: Q17
      source: template:cue_host_context.precursor
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1553
      covered_by: Q11
      source: template:cue_process_tool.scope
      coverage_type: partial
  mutable_dispositions:
    - element: TrustedRootHashList
      dimension: tuning_parameter
      specific_values: "rationale only (baseline list of trusted root cert hashes)"
      disposition: context
      target_question: Q5
      notes: "No fixed values to inject. artifact_verification (Q5) recovers the cert hash and directs the analyst to compare it against the org's record of approved certificates — the baseline this element describes. registry_set (Q7) surfaces the thumbprint as written to the store."
    - element: MonitoredProcesses
      dimension: tuning_parameter
      specific_values: "rationale only (cert-management processes flagged in unusual contexts)"
      disposition: context
      target_question: Q13
      notes: "Rule already constrains the process to PowerShell running Import-Certificate. cue_user_actor.baseline (Q12) and baseline_svc (Q13) test whether this user / parent-binary combination is a routine certificate-management context, which is the non-admin/unusual-context check this element describes."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window for registry + cert install + process creation)"
      disposition: context
      target_question: Q7
      notes: "registry_set (Q7) uses +/-15m to bracket the store write against the process-creation install; host clustering (Q16) and precursor (Q17) provide the surrounding correlation windows. No numeric threshold given to inject."
    - element: TrustedPublisherList
      dimension: tuning_parameter
      specific_values: "Cert:\\LocalMachine\\Root, Cert:\\LocalMachine\\TrustedPublisher (system-wide trust stores)"
      disposition: query_injection
      target_question: Q7
      notes: "T1553 element. The TrustedPublisher store path is injected alongside Root into the registry_set (Q7) TargetObject|contains selection so the store-write query catches both system-wide trust stores."
    - element: FilePathAllowList
      dimension: tuning_parameter
      specific_values: "rationale only (exclusions for enterprise-signed binaries in unusual dirs)"
      disposition: context
      target_question: Q11
      notes: "T1553 element. cue_process_tool.scope (Q11) frames the cross-host distribution as 'consistent automation vs one-off' so the analyst can recognize a sanctioned managed deployment from suspicious source directories — the allow-list distinction this element describes."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "near-duplicate of cue_user_actor.baseline_svc / cue_process_tool.baseline_deviation host-history coverage; the reflected baseline_deviation (Q10) gives the rule-pattern-specific host history which is more precise than a generic Image-based count, and the rule has no Image field so a bare Image baseline is weaker"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "Import-Certificate is a single-shot cmdlet that does not spawn meaningful children; child-process enumeration of powershell.exe returns generic noise. Post-install context is covered by host clustering"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.clustering
      reason: "near-identical query and intent to cue_host_context.clustering (host-scoped process aggregation around the alert window)"
      kept: cue_host_context.clustering
    - dropped: cue_artifact.baseline
      reason: "near-duplicate of evidence_preservation / artifact baseline on %file_paths%; first-appearance of the cert file is implicit in artifact_verification (hash + reputation) and the staging-directory scoping already flags transient locations"
      kept: cue_artifact.artifact_verification
    - dropped: cue_user_actor.baseline_pair
      reason: "user_actor pair — non-deduplicable; cue_user_actor.baseline (Q12) and cue_user_actor.baseline_svc (Q13) both emitted as separate questions"
      kept: both
    - dropped: cue_user_actor.clustering_pair
      reason: "user_actor pair — non-deduplicable; cue_user_actor.clustering (Q14) and cue_user_actor.clustering_svc (Q15) both emitted as separate questions"
      kept: both
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "cue_artifact: derived: [A-Za-z]:\\ in CommandLine"
      disposition: acted_on
      reason: >-
        The artifact cue drove two distinct questions on the certificate file referenced by
        -FilePath: cue_artifact.mechanism_decode (Q4, inspect issuer/subject/validity) and
        cue_artifact.artifact_verification (Q5, hash + reputation). Both use %file_paths%
        derived from the CommandLine, and evidence_preservation (Q6) confirms the file is
        still on disk.
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted_on
      reason: >-
        mechanism_decode (Q3) was adapted to scope by CommandLine|contains 'Import-Certificate'
        instead of the template's default Image|endswith, since the rule carries no Image
        field. cue_user_actor.baseline (Q12) likewise scopes by CommandLine|contains
        'Import-Certificate'. The reflected baseline_deviation (Q10) and scope (Q11) queries
        already enforce the rule's CommandLine pattern by construction.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted_on
      reason: >-
        evidence_preservation (Q6), cue_artifact.mechanism_decode (Q4), and
        cue_artifact.artifact_verification (Q5) all scope TargetFilename|endswith|expand on
        %file_paths% extracted from the CommandLine, rather than %TargetFilename%.
    - hint: "logsource 'registry_set' (recommended by techniques: T1553.004) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Authored attack:T1553.004.registry_set (Q7) with category: registry_set, keying on
        the SystemCertificates\\Root and TrustedPublisher\\Certificates registry paths where
        a machine-root install persists. This catches store writes done via the .NET
        X509Store API or certutil that the process_creation rule would miss.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "The 'ransomware' keyword match comes from a reference profile, not the rule's behavior. The rule detects root-certificate trust subversion (defense evasion), not encryption-for-impact. No ransomware-specific question would investigate this rule's actual firing pattern."
      - id: network_scope
        confidence: low
        reason: "Keyword match on 'ransomware' in references is incidental. The rule fires on a local process_creation certificate install with no network indicator; a network-scope question set would not advance the trust-subversion investigation."
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 12
    logsource_diversity:
      process_creation: 12
      file_event: 3
      registry_set: 1
      sigma_alert: 1
