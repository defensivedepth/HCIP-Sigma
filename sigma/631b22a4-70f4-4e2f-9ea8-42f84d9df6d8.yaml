name: Suspicious AddinUtil.EXE CommandLine Execution
id: fe501c28-3aab-47c9-b057-8fbc4b050a0d
description: |
  Detects the Microsoft-signed Add-In deployment cache utility addinutil.exe running with
  -AddInRoot: or -PipelineRoot: parameters that point at user-writable directories, or with a
  working directory under those same locations. The rule fires when the binary matches AND EITHER
  the suspicious flags appear together with a user-writable path in the command line, OR
  self-referential -AddInRoot:/-PipelineRoot: flags appear alongside a user-writable current
  directory. Legitimately, addinutil.exe runs from .NET framework directories to refresh Office
  add-in caches; abused, it is a proxy-execution LOLBAS that deserializes a crafted AddIns.store
  file via BinaryFormatter, executing an embedded payload under a trusted signed process.
  Investigation should confirm where the addin root was pointed, what payload that directory held,
  and what the binary did next.
type: detection
detection_id: 631b22a4-70f4-4e2f-9ea8-42f84d9df6d8
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did addinutil.exe run with a suspicious AddInRoot/PipelineRoot flag plus a user-writable path, or with a self-referential root flag and a user-writable working directory?
  context: >-
    The rule fires when the process is addinutil.exe AND EITHER the command line contains a
    -AddInRoot:/-PipelineRoot: flag together with a user-writable path (Temp, Desktop, Downloads,
    Public, Windows\Temp), OR a self-referential -AddInRoot:./-PipelineRoot:. flag runs while the
    current directory is one of those user-writable locations. Confirm the alert is a true positive
    by reading the exact Image, OriginalFileName, CommandLine, and CurrentDirectory, and note which
    branch fired: a path-in-command-line fire and a working-directory fire point the analyst at
    different evidence locations for the addin root.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
      - CurrentDirectory
- source: template:universal.host_user_context
  question: Is this user and host expected to run the .NET add-in deployment utility?
  context: >-
    addinutil.exe is a .NET framework utility for refreshing Office add-in caches and is rarely
    invoked interactively. Knowing whether this user role and host function legitimately involve
    add-in development or deployment is often the fastest way to separate a benign run from
    proxy-execution abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What add-in root or pipeline root did addinutil.exe load, and what file lived at that path?
  context: >-
    The full command line and working directory reveal where the utility was pointed. Read the
    -AddInRoot:/-PipelineRoot: value (or the self-referential "." form resolved against the current
    directory) to identify the folder the cache was read from, then check that folder for an
    AddIns.store file. The vulnerable read path deserializes that file with BinaryFormatter, so the
    contents of the addin root directory are the payload.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned addinutil.exe and is that parent chain expected?
  context: >-
    The parent process reveals how the utility was invoked. A legitimate cache refresh is typically
    driven by Office or installer tooling; an interactive shell, script host, or Office macro host
    spawning addinutil.exe with a user-writable add-in root points toward operator-driven proxy
    execution. Walk up the parent chain to see what set up the run.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run addinutil.exe at all?
  context: >-
    addinutil.exe is uncommon on most endpoints. A host with zero or very low historical execution
    of the binary makes a first-time run more notable; a host that runs it regularly may have a
    legitimate add-in deployment workflow. The aggregation shows historical frequency grouped by
    user.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: >-
    Even an occasionally legitimate utility can be abused. The query mirrors the rule's selection
    structure scoped to this host, so the resulting parent/user distribution reflects how this
    specific firing pattern has historically run here rather than generic addinutil.exe usage. A new
    parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth
    chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_susp_1_flags:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      selection_susp_1_paths:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      selection_susp_2:
        CommandLine|contains:
        - -AddInRoot:.
        - -AddInRoot:"."
        - -PipelineRoot:.
        - -PipelineRoot:"."
        CurrentDirectory|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection_img and (all of selection_susp_1_* or selection_susp_2)) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did addinutil.exe spawn afterward?
  context: >-
    The processes spawned by addinutil.exe reveal the payload's actual effect. Benign children are
    limited to conhost.exe and werfault.exe; any other child — a command interpreter, script host,
    or a freshly dropped binary — indicates the deserialized add-in cache executed code and is the
    strongest line of the investigation to follow.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_connection
  question: Did addinutil.exe make any outbound network connections after it ran?
  context: >-
    A proxy-execution payload often retrieves a second stage or beacons out under the trusted
    process. addinutil.exe has no legitimate reason to initiate network traffic, so any outbound
    connection from this binary is a meaningful escalation signal. Correlate destination addresses
    with the add-in root contents to see whether the deserialized payload reached out.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1218.image_load
  question: What modules did addinutil.exe load, and did anything beyond the expected .NET framework assemblies appear?
  context: >-
    The abuse path runs through System.AddIn.Hosting reading a crafted cache and invoking
    BinaryFormatter deserialization, which can pull in serialization gadget assemblies that a routine
    cache refresh would never load. Unsigned modules, assemblies loaded from a user-writable add-in
    root, or unusual serialization libraries loaded by this binary point to gadget-driven code
    execution rather than a benign cache update. If image_load events are not collected in this
    environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of this specific firing pattern rather than generic addinutil.exe usage. Many
    hosts firing the same pattern points to scripted deployment or vendor automation; one host or a
    small unrelated set points to manual operator activity. Use the host distribution to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_susp_1_flags:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      selection_susp_1_paths:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      selection_susp_2:
        CommandLine|contains:
        - -AddInRoot:.
        - -AddInRoot:"."
        - -PipelineRoot:.
        - -PipelineRoot:"."
        CurrentDirectory|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      condition: selection_img and (all of selection_susp_1_* or selection_susp_2)
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_susp_1_flags:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      selection_susp_1_paths:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      selection_susp_2:
        CommandLine|contains:
        - -AddInRoot:.
        - -AddInRoot:"."
        - -PipelineRoot:.
        - -PipelineRoot:"."
        CurrentDirectory|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      _pb_scope:
        User|expand: '%User%'
      condition: (selection_img and (all of selection_susp_1_* or selection_susp_2)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_susp_1_flags:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      selection_susp_1_paths:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      selection_susp_2:
        CommandLine|contains:
        - -AddInRoot:.
        - -AddInRoot:"."
        - -PipelineRoot:.
        - -PipelineRoot:"."
        CurrentDirectory|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection_img and (all of selection_susp_1_* or selection_susp_2)) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or coordinated activity
    across multiple endpoints. If the same user triggered similar runs on other hosts, the scope of
    the activity may be larger than this one alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \addinutil.exe
      - OriginalFileName: AddInUtil.exe
      selection_susp_1_flags:
        CommandLine|contains:
        - '-AddInRoot:'
        - '-PipelineRoot:'
      selection_susp_1_paths:
        CommandLine|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      selection_susp_2:
        CommandLine|contains:
        - -AddInRoot:.
        - -AddInRoot:"."
        - -PipelineRoot:.
        - -PipelineRoot:"."
        CurrentDirectory|contains:
        - \AppData\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - \Windows\Temp\
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection_img and (all of selection_susp_1_* or selection_susp_2)) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining the process activity surrounding this event reveals
    what led to it and what happened after — reconnaissance, privilege escalation, persistence
    installation, or staging that provides broader context. Aggregating by Image and ParentImage
    collapses repetitive agent noise while novel parent-child pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host — defense tampering, reconnaissance, credential access — may
    reveal the attack stages that preceded this proxy-execution event and help reconstruct the full
    timeline. The sequence of alerts on a host often tells the operational story.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: trusted_binary_invocation
      description: "Microsoft-signed addinutil.exe is invoked with a command-line parameter pointing the add-in/pipeline root at a user-writable directory (or self-referential root with a user-writable working directory)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_load_and_decode
      description: "The add-in root directory holds a crafted AddIns.store that is read and deserialized via BinaryFormatter, loading serialization gadget assemblies"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: gadget_assembly_load
      description: "Deserialization pulls in unexpected modules / serialization gadget assemblies under the trusted process"
      mapped_to: Q10
      source: attack:T1218.image_load
    - stage: 4
      label: payload_execution
      description: "The embedded payload executes, spawning child processes and/or retrieving externally hosted content via network"
      mapped_to: Q8
      source: template:cue_process_tool.follow_on
    - stage: 5
      label: network_retrieval
      description: "Payload retrieves a second stage or beacons out under the trusted binary"
      mapped_to: Q9
      source: attack:T1218.network_connection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q7
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "-AddInRoot:, -PipelineRoot:, -AddInRoot:., -AddInRoot:\".\", -PipelineRoot:., -PipelineRoot:\".\", \\AppData\\Local\\Temp\\, \\Desktop\\, \\Downloads\\, \\Users\\Public\\, \\Windows\\Temp\\"
      disposition: query_injection
      target_question: Q7
      notes: "Rule's full selection (flags + user-writable paths + self-referential roots + current-directory branch) is embedded verbatim in the REFLECTED baseline_deviation (Q7) and scope (Q11) detection blocks. Verify_trigger_match (Q1) displays these via ProcessGuid; mechanism_decode (Q4) directs the analyst to read them."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profiling unexpected parent-child relationships)"
      disposition: new_question
      target_question: Q5
      notes: "No fixed value list in the analytic; covered by process_chain (Q5) plus the parent+binary baseline (Q13) and clustering (Q15) substitutes."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (allowlist of signed binaries — addinutil.exe is the in-scope binary)"
      disposition: context
      target_question: Q1
      notes: "addinutil.exe is itself the trusted signed binary being proxied; the rule already constrains Image/OriginalFileName. No separate allowlist query needed."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q9
      notes: "Surfaced in the network_connection question (Q9) context — addinutil.exe has no legitimate network behavior, so destination triage drives the allowlist decision per environment."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Near-identical query to cue_host_context.clustering (host.name-scoped, +/-15m, aggregated). host_context version adds ParentImage to the projection and is kept; process_tool clustering by Image alone is subsumed."
      kept: cue_host_context.clustering
    - dropped: null
      reason: "user_actor pair (Q12/Q13 baseline + svc-substitute, Q14/Q15 clustering + svc-substitute) emitted as BOTH variants — non-deduplicable per service-principal rule."
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc + cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted_on
      reason: >-
        Acted on — authored attack:T1218.network_connection (Q9) as a network_connection
        question scoped to addinutil.exe on the host. The blog research notes "AddinUtil.exe
        initiating network connections" as a detection signal; the binary has no legitimate
        network behavior, so this is a direct escalation pivot.
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted_on
      reason: >-
        Acted on — authored attack:T1218.image_load (Q10) as an image_load question scoped to
        addinutil.exe. The abuse path runs through BinaryFormatter deserialization of a crafted
        AddIns.store, which loads serialization gadget assemblies; image_load directly evidences
        gadget-driven code execution distinct from a benign cache refresh.
    - hint: "Condition uses OR among selection groups: (selection_susp_1_flags OR selection_susp_1_paths OR selection_susp_2). Q1 (verify_trigger_match) and the playbook description must frame this as 'EITHER ... OR ...', not 'X AND Y AND Z'."
      disposition: acted_on
      reason: >-
        Acted on — Q1 and the description use disjunctive framing per condition_topology.prose:
        Image match AND EITHER (suspicious flag + user-writable path in CommandLine) OR
        (self-referential root flag + user-writable CurrentDirectory). Q1 context instructs the
        analyst to note which branch fired.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      network_connection: 1
      image_load: 1
      sigma_alert: 1
