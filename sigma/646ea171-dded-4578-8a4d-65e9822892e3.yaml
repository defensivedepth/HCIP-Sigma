name: Process Memory Dump Via Comsvcs.DLL
id: 125b8140-690c-41fc-b7e9-00d9fb32b490
description: |
  Detects a process memory dump driven through comsvcs.dll, the COM+ Services library that exports a MiniDumpWriteDump wrapper callable from rundll32. The rule fires when either a rundll32 invocation carries comsvcs MiniDump markers (the ordinal #24, #-, #+, #65560, or the literal MiniDump string together with "full"), or a more generic obfuscated form combines "24", "comsvcs", and "full" with spacing or comma variants used to evade naive string matching. This primitive is the canonical hands-off way to dump LSASS memory for offline credential extraction, but the same call can target any readable process. Investigation should confirm which process was dumped, where the dump landed, whether the caller held the privilege needed to read LSASS, and whether credential theft was followed by lateral movement.
type: detection
detection_id: 646ea171-dded-4578-8a4d-65e9822892e3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match either comsvcs MiniDump branch this rule detects?
  context: >-
    This rule fires on either of two branches, so confirm which one matched before investing time. The first branch is a
    rundll32 process whose command line pairs "comsvcs" and "full" with a dump marker (the ordinal #24, the padded forms #-,
    #+, #65560, or the literal MiniDump). The second branch is a more obfuscated form combining "24", "comsvcs", and "full"
    with spacing or comma tricks (" #", ",#", ", #", or a quoted #) that hide the ordinal from simple matching. Read the exact
    command line and note which branch fired and whether a target PID and an output file path (typically lsass and a .dmp or
    .bin filename) are present.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to dump process memory with rundll32 and comsvcs.dll?
  context: >-
    Legitimate use of the comsvcs MiniDump primitive is rare. Few administrative or support workflows call rundll32 against
    comsvcs.dll to dump a process, and almost none target LSASS. Knowing whether this host is a developer or support box where
    crash-dump tooling runs, and whether the acting account routinely performs memory captures, is often the fastest way to
    separate a sanctioned diagnostic from credential theft.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:masquerading.true_identity
  question: Is the executing binary genuinely rundll32, and does the command line use ordinal or comma obfuscation to hide the
    comsvcs call?
  context: >-
    Two masquerading angles apply. First, confirm the binary is the real Windows rundll32: compare the Image path against the
    OriginalFileName metadata and check that it runs from System32 or SysWOW64 rather than a copied or relocated path. Second,
    the rule's generic branch exists because operators disguise the comsvcs ordinal with padded forms and comma or spacing
    tricks (#24, #-, #+, #65560, ",#", " #") so the call reads as benign at a glance. A hash lookup of the binary and a careful
    read of the obfuscated argument together establish what actually ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:credential_access.credential_target_identification
  question: Which process was targeted for the memory dump, and what dump method and output were specified?
  context: >-
    Decode the full command line to identify the target. The comsvcs MiniDump primitive takes a target PID, an output file
    path, and the literal "full"; lsass.exe is the canonical victim, but any readable process can be dumped. Determine the
    target PID or process name, the output file path and extension (commonly .dmp or .bin), and confirm whether the marker
    is the MiniDump string or one of the ordinal forms. Other dumping syntaxes (procdump, direct NtReadVirtualMemory, Invoke-Mimikatz)
    point to a broader credential-access toolkit if present nearby. A successful dump of LSASS means every credential cached
    in memory may be exposed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.001.access_verification
  question: Did a process open a full-access handle to lsass.exe on this host around the time of the alert?
  context: >-
    Dumping LSASS memory requires opening a handle with read access to the process. A handle requested with full access (mask
    0x1F0FFF, or the read-and-query combination 0x1010) to lsass.exe from a non-security tool is the access half of the dump
    sequence and corroborates that the command-line dump actually reached its target. Allowlist known AV and endpoint-protection
    tooling that legitimately reads LSASS. If process-access events are not collected in this environment, treat zero results
    as a visibility gap rather than evidence the dump failed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetImage|endswith: \lsass.exe
      selection_access:
        GrantedAccess|contains:
          - '0x1F0FFF'
          - '0x1010'
          - '0x1410'
      condition: selection and selection_access
    fields:
      - SourceImage
      - GrantedAccess
      - CallTrace
- source: attack:T1003.001.dump_file_artifact
  question: Was a memory dump file written to disk on this host, and where did it land?
  context: >-
    A successful MiniDump call writes the captured memory to the output path named in the command line. Common landing spots
    are user-writable or staging directories such as the user TEMP folder, C:\Windows\Temp, C:\Users\Public, or the path the
    operator chose. Locating the dump file confirms the capture completed and gives the exact artifact to preserve; the writing
    process and timestamp tie the file back to the alerting command. The standard victim pattern is lsass.exe written to a
    .dmp or .bin file, sometimes with a reversed or innocuous filename to blend in.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_ext:
        TargetFilename|endswith:
          - '.dmp'
          - '.bin'
          - '.pmd'
      condition: selection and selection_ext
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned this rundll32, and is that parent chain expected for a memory-dump command?
  context: >-
    The parent process reveals how the dump was launched. A comsvcs MiniDump invoked from an interactive cmd.exe or powershell.exe,
    from a remote-execution service, or from an unexpected scripting host (wscript, mshta, an Office application) points to
    hands-on-keyboard or scripted credential theft. Allowlisting known legitimate parents that read LSASS (AV or EDR tooling)
    helps separate sanctioned activity. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: >-
    Operators frequently weaken protection before dumping credentials so the dump tool and its output are not flagged or quarantined.
    The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or
    patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal; kernel-level driver tampering and in-memory patches
    will not appear here. Populated rows shift the disposition toward operator preparation, so review the parent process and
    signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be exposed by this dump, and do any have elevated privileges?
  context: >-
    The severity of an LSASS dump depends on which credentials were resident in memory on this host. Interactive logons, cached
    domain credentials, and service-account tokens present at dump time may all be exposed. Cross-reference the accounts that
    have logged on to this host with directory group memberships: domain admin or other privileged credentials require incident
    escalation and credential resets, while a standalone workstation with only a local user may need narrower remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were any new logon sessions or remote connections initiated from this host after the dump?
  context: >-
    Credential theft is typically followed by use of the stolen credentials to reach other systems. Alerts on this host in
    the hours after the dump that indicate RDP sessions, SMB connections, WinRM usage, remote-execution tools, or new authentication
    events suggest the captured credentials are already being exercised. Their absence does not clear the dump, since offline
    cracking and later reuse are common, but their presence raises the urgency.
  range: +4h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this comsvcs MiniDump pattern, what parents and users drove it, and does the
    current alert fit?
  context: >-
    Even on hosts where crash-dump tooling runs, the comsvcs MiniDump pattern should have a stable parent and user profile.
    The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects
    how this specific firing pattern has historically run here rather than generic rundll32 usage. A new parent process, an
    unexpected account, or a first-ever occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli_1:
        CommandLine|contains|all:
        - comsvcs
        - full
        CommandLine|contains:
        - '#-'
        - '#+'
        - '#24'
        - '24 '
        - MiniDump
        - '#65560'
      selection_generic:
        CommandLine|contains|all:
        - '24'
        - comsvcs
        - full
        CommandLine|contains:
        - ' #'
        - ',#'
        - ', #'
        - '"#'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: ((selection_img and 1 of selection_cli_*) or selection_generic) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    A credential dump rarely stands alone. Reconnaissance, privilege escalation, additional credential-access tooling, or staging
    activity in the same window provides the immediate context for the dump and shows whether it sits inside a larger sequence.
    Aggregating by Image and ParentImage collapses repetitive agent telemetry into high-count rows while novel parent-child
    pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_user_actor.baseline
  question: Does this user normally run rundll32 with comsvcs.dll on any host?
  context: >-
    Compare the acting account's current action against its historical behavior. A user who has never invoked rundll32 against
    comsvcs.dll is more suspicious than one who does so as part of a known diagnostic workflow. User role and typical toolset
    should align; a standard user account driving a memory-dump primitive rarely does.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-and-rundll32 launch combination historically routine on this host or environment-wide, or is the
    parent unusual for rundll32?
  context: >-
    When the alert fires under a service principal such as SYSTEM, LOCAL SERVICE, or a computer account, the user identity does
    not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned rundll32, and whether
    that pair is historically common. A new or rare parent for rundll32 on this host or environment-wide is the deviation worth
    chasing; a pair that already accounts for thousands of prior events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the acting account's activity across all hosts reveals lateral movement or parallel activity on multiple endpoints.
    If the same user drove similar dump or credential-access activity on more than one host in this window, the scope of compromise
    may be larger than the single alerting endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-and-rundll32 combination fire on other hosts in the same window, indicating a coordinated rollout
    rather than isolated activity?
  context: >-
    Under a service principal, asking what else the user did across the network returns agent-telemetry noise, because every
    host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, the parent process
    spawning rundll32, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this comsvcs MiniDump pattern in the past week, and does the cross-host distribution look
    like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this specific
    firing pattern rather than generic rundll32 usage. Many hosts firing the same pattern points to scripted deployment or
    sanctioned diagnostic automation; one host or a small unrelated set points to manual operator activity. Use the host distribution
    to calibrate severity, since credential-dump tooling spreading across many endpoints is materially different from a single
    isolated dump.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli_1:
        CommandLine|contains|all:
        - comsvcs
        - full
        CommandLine|contains:
        - '#-'
        - '#+'
        - '#24'
        - '24 '
        - MiniDump
        - '#65560'
      selection_generic:
        CommandLine|contains|all:
        - '24'
        - comsvcs
        - full
        CommandLine|contains:
        - ' #'
        - ',#'
        - ', #'
        - '"#'
      condition: (selection_img and 1 of selection_cli_*) or selection_generic
    fields:
    - host.name
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the comsvcs MiniDump pattern over the past 90 days, and what command-line shapes are
    typical?
  context: >-
    A LOLBin pattern with no prior history on a host is more suspicious than one used regularly under stable parents. The query
    mirrors the rule's selection structure scoped to this host, so the resulting count reflects this specific firing pattern
    rather than generic rundll32 usage. The CommandLine projection lets the analyst compare typical invocation shapes against
    the alerting one and spot whether the ordinal or obfuscation form has changed.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \rundll32.exe
      - OriginalFileName: RUNDLL32.EXE
      - CommandLine|contains: rundll32
      selection_cli_1:
        CommandLine|contains|all:
        - comsvcs
        - full
        CommandLine|contains:
        - '#-'
        - '#+'
        - '#24'
        - '24 '
        - MiniDump
        - '#65560'
      selection_generic:
        CommandLine|contains|all:
        - '24'
        - comsvcs
        - full
        CommandLine|contains:
        - ' #'
        - ',#'
        - ', #'
        - '"#'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: ((selection_img and 1 of selection_cli_*) or selection_generic) and _pb_scope
    fields:
    - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.001
      name: LSASS Memory
      role: primary
    - id: T1036
      name: Masquerading
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: handle_to_lsass
      description: A process opens a full-access handle to lsass.exe (mask 0x1F0FFF) preparatory to reading its memory.
      mapped_to: Q5
      source: attack:T1003.001.access_verification
    - stage: 2
      label: dump_invocation
      description: rundll32 invokes comsvcs.dll MiniDump with target PID, output path, and "full" to capture process memory.
      mapped_to: Q4
      source: template:credential_access.credential_target_identification
    - stage: 3
      label: dump_file_written
      description: The captured memory is written to a dump file on disk (commonly .dmp/.bin in a temp or staging path).
      mapped_to: Q6
      source: attack:T1003.001.dump_file_artifact
    - stage: 4
      label: credential_reuse
      description: Stolen credentials are used for lateral movement or new logon sessions after the dump.
      mapped_to: Q10
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: access_verification
      technique: T1003.001
      covered_by: Q5
      source: attack:T1003.001.access_verification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1003.001
      covered_by: Q4
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1003.001
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.001
      covered_by: Q12
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.dump_file_artifact
      coverage_type: direct
    - dimension: execution_context
      technique: T1036
      covered_by: Q3
      source: template:masquerading.true_identity
      coverage_type: direct
    - dimension: time_correlation
      technique: T1036
      covered_by: Q12
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1036
      covered_by: Q3
      source: template:masquerading.true_identity
      coverage_type: partial
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF (also 0x1010, 0x1410 read+query combos)"
      disposition: query_injection
      target_question: Q5
      notes: "Injected into the process_access GrantedAccess|contains selection."
    - element: DumpFilePath
      dimension: tuning_parameter
      specific_values: "%TEMP%, C:\\Windows\\Temp, C:\\Users\\Public; .dmp/.bin/.pmd extensions"
      disposition: query_injection
      target_question: Q6
      notes: "Dump-file extensions injected into the file_event TargetFilename|endswith selection; landing paths named in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "~5 minutes between LSASS access and dump-file creation"
      disposition: context
      target_question: Q6
      notes: "Access (Q5) and dump-file (Q6) both run +/-5m; the timing relationship is described in context, not directly queryable."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only — allowlist known AV/EDR parents reading LSASS"
      disposition: context
      target_question: Q7
      notes: "Allowlisting guidance carried in process_chain context; no fixed value list to inject."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rundll32, procdump, comsvcs.dll, Invoke-Mimikatz"
      disposition: context
      target_question: Q4
      notes: "credential_target_identification is KEEP_EXACT (ProcessGuid-scoped); dumping-syntax values named in context."
    - element: OriginalFilenameMismatch
      dimension: tuning_parameter
      specific_values: "rationale only — compare Image basename vs OriginalFilename"
      disposition: context
      target_question: Q3
      notes: "true_identity projects Image + OriginalFileName + Hashes; mismatch check is the question's core ask."
    - element: KnownSystemUtilityPaths
      dimension: tuning_parameter
      specific_values: "rationale only — expected System32/SysWOW64 install dirs"
      disposition: context
      target_question: Q3
      notes: "Path-anomaly guidance carried in true_identity context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — file creation to execution correlation window (T1036)"
      disposition: context
      target_question: Q6
      notes: "Subsumed by the T1003.001 access-to-dump timing handled around Q5/Q6."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification (brief hint) — the credential-store/dump-method question is the more specific Phase 2."
      kept: credential_access.credential_target_identification
    - dropped: cue_process_tool.baseline
      reason: "Generic Image|endswith host-frequency count is subsumed by lolbin.historical_lolbin_baseline, which counts the rule's specific comsvcs pattern on this host (more precise)."
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "Host-wide concurrent-activity intent is covered by cue_host_context.clustering (same +/-15m host aggregation)."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.follow_on
      reason: "Child-process-of-rundll32 thread does not add signal — comsvcs MiniDump writes a file rather than spawning children; downstream activity is covered by post_access_lateral_movement and host clustering."
      kept: credential_access.post_access_lateral_movement
    - dropped: masquerading.renamed_binary_activity
      reason: "rundll32 is the legitimate signed system binary, not a renamed/dropped file; the cross-host scope thread is covered by cue_process_tool.scope (fleet pattern)."
      kept: cue_process_tool.scope
    - dropped: cue_host_context.precursor
      reason: "Prior-alert-on-host context is carried by endpoint_protection_health (pre-dump tamper sweep) and the analysis already runs at the 18-question cap; precursor's generic prior-alert sweep added least marginal signal."
      kept: template:universal.endpoint_protection_health
    - dropped: lolbin.technique_class_lateral
      reason: "The LOLBin list is download/proxy-execute binaries, not credential-dump tooling; other dump methods (procdump, mimikatz) are surfaced in credential_target_identification context instead."
      kept: credential_access.credential_target_identification
    - dropped: lolbin.renamed_binary_check
      reason: "Path/identity check (Image from non-standard dir) is already covered by masquerading.true_identity, which projects Image + OriginalFileName for the same anomaly."
      kept: template:masquerading.true_identity
  skipped_templates:
    - template: masquerading.binary_origin
      reason: "File-delivery thread targets a dropped/renamed binary; the executing binary here is the legitimate system rundll32.exe, so there is no foreign file_event to trace. The comsvcs.dll dump artifact (a created file) is instead covered by attack:T1003.001.dump_file_artifact."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1003.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        The comsvcs MiniDump primitive captures LSASS memory directly to a dump file via MiniDumpWriteDump; it does not write
        a registry value. registry_set coverage in the T1003.001 analytic targets other LSASS-dump variants (e.g. the
        MiniDumpAuxiliaryDll or WDigest UseLogonCredential registry routes), which this rule's command-line pattern does not
        exercise. The access-and-dump sequence for comsvcs is fully covered by Q5 (process_access handle to lsass) and Q6
        (dump-file creation). No compensating registry_set question authored.
    - hint: "logsource 'security' (recommended by techniques: T1003.001) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The Windows Security analytic for T1003.001 relies on EID 4673/4663 SACL-audit events on lsass.exe, which require
        object-access auditing and a SACL on the LSASS process to be configured — rarely enabled in practice. The same
        access dimension is covered with higher fidelity by Q5 (attack:T1003.001.access_verification) using Sysmon
        process_access (EID 10) GrantedAccess, which does not depend on audit-policy tuning. No compensating security
        question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - masquerading
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      process_access: 1
      file_event: 1
      sigma_alert: 1
