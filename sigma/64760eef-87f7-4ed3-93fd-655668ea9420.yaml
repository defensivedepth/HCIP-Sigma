name: Use of Scriptrunner.exe
id: 4b48d202-56ec-4ba6-82d8-09699fca0d51
description: |
  Detects ScriptRunner.exe (identified by Image or OriginalFileName) invoked with the ' -appvscript ' command-line switch.
  ScriptRunner.exe is a Microsoft-signed App-V helper that can be abused to proxy execution of an arbitrary binary or a
  remote .cmd payload through a trusted signer, bypassing application allowlisting and obscuring the real process lineage.
  The rule fires only when both conditions hold together: the binary is ScriptRunner.exe AND the command line contains
  ' -appvscript '. Legitimate firing is expected where App-V is actually deployed, so the investigation should decode the
  proxied target, confirm the parent and user are consistent with App-V use, and determine whether the target binary is a
  signed managed application or an unsigned/remote payload staged for execution.
type: detection
detection_id: 64760eef-87f7-4ed3-93fd-655668ea9420
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the event match what this rule detects, with ScriptRunner.exe running and the command line containing the -appvscript switch?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when both halves match together, the binary is ScriptRunner.exe (by Image or OriginalFileName) AND the command line contains ' -appvscript '. Confirm both are present, and read the value passed to -appvscript, which names the binary or remote .cmd being proxied.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run App-V virtualized applications via ScriptRunner.exe?
  context: ScriptRunner.exe is part of the App-V client; legitimate firing is expected on hosts where App-V is deployed and the user runs virtualized applications. Knowing whether App-V is actually in use on this host and whether this user's role involves virtualized apps is often the fastest path to distinguishing routine App-V activity from proxy-execution abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What target binary or remote payload did ScriptRunner.exe proxy via -appvscript, and what does that target do?
  context: 'The value passed to -appvscript names what ScriptRunner.exe is proxying: a local executable, or a remote .cmd retrieved over SMB or HTTP. Read the full argument and resolve the target. A signed managed application installed by App-V is routine; an unsigned binary, a path in a user-writable or temp directory, or a remote share path is the operator pattern this technique exploits. Note any embedded paths, share names, URLs, or IP addresses for follow-on pivots.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains: ' -appvscript '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1218.command_analysis
  question: Does the -appvscript argument reference a remote location, a user-writable path, or an unsigned binary rather than an installed App-V application?
  context: The proxy-execution abuse hinges on what is being executed, not just that ScriptRunner.exe ran. The technique substitutes an arbitrary target for the expected managed application, so the argument may point to a remote SMB/UNC path, an HTTP-fetched .cmd, a temp or profile directory, or an unsigned executable. Resolve the target path, check its signing status and on-disk location, and compare against the App-V package layout to decide whether the proxied content is legitimate.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned ScriptRunner.exe, and is that parent chain expected for App-V?
  context: The parent process reveals how ScriptRunner.exe was invoked. App-V usage typically descends from the App-V client service chain or an explorer-launched virtualized shortcut. Parents like cmd.exe, powershell.exe, mshta.exe, wscript.exe, an Office application, or an archive tool indicate scripted or staged invocation rather than the App-V runtime, which points toward proxy-execution abuse. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.parent_process_analysis
  question: Is the parent that launched ScriptRunner.exe historically routine for this binary on this host, or a new and unusual launcher?
  context: App-V activity has a predictable parent process; proxy-execution abuse usually arrives through a different launcher (a shell, a script host, or an Office application). Comparing the current parent against the historical parents that have driven ScriptRunner.exe on this host separates established App-V automation from a one-off invocation by an unexpected process. A parent that already accounts for prior ScriptRunner.exe events is most likely the App-V runtime; a first-seen parent is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ParentImage
- source: template:cue_process_tool.follow_on
  question: What child processes did ScriptRunner.exe spawn afterward?
  context: The processes spawned through the proxy reveal its actual impact. Legitimate App-V virtualized apps have predictable children; proxy-execution abuse commonly spawns cmd.exe, powershell.exe, certutil.exe, a script host, or the proxied target binary itself for follow-on staging. Review the children and their command lines.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_retrieval
  question: Did ScriptRunner.exe or its child processes make outbound network connections around the time of the alert?
  context: This technique can proxy a remote payload, and the LOLBAS pattern retrieves a .cmd from an external server or SMB share. Outbound connections from ScriptRunner.exe or its immediate children, especially to external IPs or non-domain SMB endpoints, indicate retrieval of remote content rather than execution of a locally installed App-V package. Correlate any destination here with paths or share names seen in the proxied argument.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \ScriptRunner.exe
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run ScriptRunner.exe at all?
  context: ScriptRunner.exe should only run where App-V is deployed; on a host without App-V it should never appear. Zero or very low historical counts indicate first-time or rare execution, which on a non-App-V endpoint raises the priority of the proxied-target analysis. A steady historical baseline is consistent with genuine App-V use.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even on App-V hosts the -appvscript pattern has a normal shape. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific ScriptRunner.exe firing pattern has historically run here, not generic binary usage. A new parent process, an unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \ScriptRunner.exe
      - OriginalFileName: ScriptRunner.exe
      selection_cli:
        CommandLine|contains: ' -appvscript '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run ScriptRunner.exe across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never invoked ScriptRunner.exe is more suspicious than one who runs it regularly as part of App-V-delivered applications. User role and typical toolset should align with launching virtualized applications.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and ScriptRunner.exe combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned ScriptRunner.exe, and is that pair historically common. A new or rare parent for this binary on this host or environment-wide is the deviation worth chasing; a pair that already accounts for thousands of prior events is App-V or vendor automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and ScriptRunner.exe combination fire on other hosts in the same window, indicating a coordinated automation rollout rather than isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, parent process plus spawned binary, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or data staging.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this ScriptRunner.exe -appvscript firing pattern, not generic binary usage. Many hosts firing the same pattern points to a scripted App-V deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \ScriptRunner.exe
      - OriginalFileName: ScriptRunner.exe
      selection_cli:
        CommandLine|contains: ' -appvscript '
      condition: all of selection*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded this event and help reconstruct the timeline. Look for patterns: tampering followed by execution, or reconnaissance followed by exploitation.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: trusted_binary_invocation
      description: A trusted Microsoft-signed binary (ScriptRunner.exe) is invoked with the -appvscript proxy switch.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: proxied_target_decode
      description: ScriptRunner.exe proxies execution of an arbitrary local or remote target named by the -appvscript argument.
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_classification
      description: The proxied target is classified as a managed App-V app versus an unsigned/remote/user-writable payload.
      mapped_to: Q4
      source: attack:T1218.command_analysis
    - stage: 4
      label: network_retrieval
      description: A remote .cmd or binary may be retrieved over SMB/HTTP before proxy execution.
      mapped_to: Q8
      source: attack:T1218.network_retrieval
    - stage: 5
      label: follow_on_execution
      description: The proxied target spawns child processes for follow-on staging.
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q4
      source: attack:T1218.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q6
      source: attack:T1218.parent_process_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q9
      source: template:cue_process_tool.baseline
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "unexpected parents (e.g., regsvr32.exe not launched by explorer.exe) — examples, no fixed list"
      disposition: new_question
      target_question: Q6
      notes: "Drove attack:T1218.parent_process_analysis (parent-baseline anomaly); also surfaced in process_chain (Q5) prose. No fixed parent list to inject; aggregation by ParentImage exposes the unusual launcher."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "list of allowed signed binaries (e.g., msiexec.exe, regsvr32.exe) — rationale only for this rule"
      disposition: context
      target_question: Q9
      notes: "Rule fixes the signed binary to ScriptRunner.exe; the allowlist is an environment-tuning rationale. Baseline (Q9) carries the is-this-binary-expected-here intent; signing of the proxied TARGET is asked in Q4 context."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "regex for suspicious args (URLs, script paths, DLL entrypoints) — rationale only"
      disposition: context
      target_question: Q4
      notes: "No literal regex supplied. The rule's queryable token (' -appvscript ') is injected into mechanism_decode (Q3); the command_analysis question (Q4) asks the analyst to classify the proxied path as remote/user-writable/unsigned."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "suppress activity to legitimate enterprise domains — rationale only"
      disposition: context
      target_question: Q8
      notes: "No fixed domain values; network-retrieval question (Q8) surfaces outbound destinations from ScriptRunner.exe for the analyst to compare against known-good enterprise endpoints."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.clustering
      reason: "host-clustering coverage provided by cue_host_context.clustering (same +/-15m host-scoped Image aggregation); process_tool variant redundant"
      kept: cue_host_context.clustering
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule's abuse is proxy execution of a SEPARATE target binary or
        remote .cmd through ScriptRunner.exe (-appvscript), not DLL side-loading
        INTO ScriptRunner.exe. The proxied payload runs as its own process and
        is surfaced by the follow_on child-process question (Q7) and the
        command_analysis target-classification question (Q4) via process_creation
        evidence. image_load on ScriptRunner.exe would return its routine App-V
        client DLL set without precision for the proxy-execution this rule fires
        on. No compensating attack:T1218.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 14
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      sigma_alert: 1
