name: Visual Studio NodejsTools PressAnyKey Renamed Execution
id: 66968735-45a8-42c3-a779-61dd264c88b3
description: |
  Detects a process whose embedded PE metadata (OriginalFileName) is Microsoft.NodejsTools.PressAnyKey.exe but whose
  on-disk Image path does not end in that filename, indicating the signed Visual Studio NodejsTools helper has been renamed.
  The rule fires when the OriginalFileName matches AND the legitimately-named-and-located instance is NOT present. This helper
  is a proxy-execution LOLBIN: given at least three arguments, it writes the second argument as a PID file and passes the
  third argument directly to Process.Start, so any local or file-share binary named on the command line runs as a child of
  the helper. Renaming it lets an operator launch an arbitrary payload under the cover of a Microsoft-signed parent.
  Investigation should decode the command line to recover the proxied binary path, confirm the user and host are expected to
  run Visual Studio tooling, and determine whether the child process is benign or stages further execution.
type: detection
detection_id: 65c3ca2c-525f-4ced-968e-246a713d164f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does this event match the rule, with OriginalFileName Microsoft.NodejsTools.PressAnyKey.exe while the Image path does not end in that same filename?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when the embedded OriginalFileName is Microsoft.NodejsTools.PressAnyKey.exe AND the on-disk Image path does NOT end in \Microsoft.NodejsTools.PressAnyKey.exe. Confirm both halves hold in the actual event, the metadata identifies the NodejsTools helper while the file on disk carries a different name.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to run Visual Studio NodejsTools helper binaries?
  context: The legitimate Microsoft.NodejsTools.PressAnyKey.exe ships inside a Visual Studio Node.js Tools installation and runs from the IDE Extensions directory. A renamed copy on a developer workstation with the toolchain installed is far more plausible than one on a server or general-purpose endpoint. Knowing whether the user role and host function involve Visual Studio development is often the fastest path to distinguishing routine tooling from proxy-execution abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:masquerading.true_identity
  question: What is the actual identity of this binary based on its metadata, signature, and hash?
  context: The file on disk is named something other than Microsoft.NodejsTools.PressAnyKey.exe, but the embedded OriginalFileName and the code-signing chain still reflect the original Visual Studio helper. Compare the Image path against OriginalFileName to confirm the rename, then check the signature, a genuine copy is Microsoft-signed, while a tampered or trojanized copy may be unsigned or signed by an unrelated party. The file hash enables a reputation lookup to confirm whether this is the known-good helper or a modified binary.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:masquerading.binary_origin
  question: How did this renamed helper binary arrive on the host, and what process wrote it?
  context: 'A renamed copy of the NodejsTools helper does not appear spontaneously, it was copied from the Visual Studio install tree, extracted from an archive, downloaded, or dropped by a parent process. Tracing the file-creation event for the binary by basename over a long window reveals the delivery mechanism: a copy operation, a download tool, an installer, or a malware dropper. A user-writable destination such as Desktop, Downloads, Temp, or AppData points to manual relocation or staging rather than a normal install.'
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_name%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: Decode the command line. Which binary path is being passed as the third argument for proxied execution, and what is in the PID file argument?
  context: 'The helper proxies execution by deterministic argument positions: the first argument is a mode token, the second is a path it writes the launched process PID into, and the third is the path passed straight to Process.Start and executed. Recover all three from the command line. The third argument is the actual payload, a local executable or one on a file share, and is the most important field to identify. The second argument shows where a PID-tracking file was dropped. Anything after the third argument is forwarded to the launched binary.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this renamed helper, and is that parent chain expected?
  context: The parent process reveals how the helper was invoked. Legitimate Visual Studio tooling descends from the IDE or its build processes; interactive abuse typically descends from explorer.exe or a shell, while staged execution may descend from an Office application, a script host such as wscript.exe or mshta.exe, or a remote-administration tool. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.proxied_execution
  question: What child processes did this renamed helper spawn, and do those children match the binary path named on its command line?
  context: The whole point of this LOLBIN is to launch the binary named in its third argument, which then runs as a direct child of the renamed helper. Enumerate the children and compare their Image paths against the proxied path recovered from the command line. A child that is a shell, a script host, certutil.exe, rundll32.exe, or a binary from a user-writable or file-share location is the proxied payload and the core of the investigation. No children at all may indicate a failed or test invocation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_connection
  question: Did the renamed helper or its child process make outbound network connections, and were they to an internal file share or an external endpoint?
  context: This helper can launch a binary located on a file share, and the proxied payload itself may retrieve further code or beacon outbound. Network connections initiated by the helper or its children show whether the proxied binary was pulled from a remote location or is communicating after launch. Connections to internal SMB file shares fit the file-share execution pattern; connections to external addresses point to download or command-and-control activity. Correlate by the helper and child ProcessGuid through the same host window.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run the NodejsTools helper, renamed or not?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. On a host without Visual Studio Node.js tooling installed, any execution of this helper is unexpected. Zero or very low historical counts indicate first-time or rare execution and raise the priority of the decode result.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even a legitimately installed helper can be renamed and abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific renamed-helper firing pattern has historically run here, not generic execution of the binary. A new parent process, an unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: Microsoft.NodejsTools.PressAnyKey.exe
      filter_main_legit_name:
        Image|endswith: \Microsoft.NodejsTools.PressAnyKey.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run the NodejsTools helper across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never run this helper is more suspicious than a developer who runs Visual Studio tooling regularly. The user role and typical toolset should align with running a Node.js Tools build helper.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and helper combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned this helper, and is that pair historically common. A new or rare parent for this binary on this host or environment-wide is the deviation worth chasing; a pair that already accounts for thousands of prior events is vendor or build-tooling automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and helper combination fire on other hosts in the same window, indicating a coordinated automation rollout rather than isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, parent process plus spawned helper, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or data staging surrounding the proxied execution.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this renamed-helper firing pattern, not generic execution of the binary. Many hosts firing the same pattern points to scripted deployment or build automation that legitimately relocates the helper; one host or a small unrelated set points to manual operator activity. Use host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: Microsoft.NodejsTools.PressAnyKey.exe
      filter_main_legit_name:
        Image|endswith: \Microsoft.NodejsTools.PressAnyKey.exe
      condition: selection and not 1 of filter_main_*
    fields:
    - host.name
- source: attack:T1218.distribution_scope
  question: Is the same renamed helper, identified by OriginalFileName, present on other hosts in the environment?
  context: The purpose of renaming a signed helper is to proxy execution while evading name-based detection. The same renamed binary, matched by its embedded OriginalFileName rather than the on-disk name, appearing on multiple hosts suggests deliberate distribution rather than an isolated developer artifact. Pair the cross-host spread with the parent and child consistency to judge whether this is an automation rollout or operator activity.
  range: +4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: Microsoft.NodejsTools.PressAnyKey.exe
      filter_main_legit_name:
        Image|endswith: \Microsoft.NodejsTools.PressAnyKey.exe
      condition: selection and not 1 of filter_main_*
    fields:
      - host.name
      - Image
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded this event and help reconstruct the timeline. Look for patterns: tampering followed by execution, or reconnaissance followed by exploitation.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: renamed_helper_execution
      description: A renamed, Microsoft-signed Microsoft.NodejsTools.PressAnyKey.exe executes (OriginalFileName matches, Image path does not).
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: argument_decode
      description: The helper's command-line arguments name a PID file (arg 2) and the binary path passed to Process.Start (arg 3).
      mapped_to: Q5
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: proxied_child_execution
      description: The third argument's binary runs as a direct child of the renamed helper (local or file-share executable).
      mapped_to: Q7
      source: attack:T1218.proxied_execution
    - stage: 4
      label: proxied_network_activity
      description: The helper or its proxied child retrieves code from a file share / external endpoint or beacons after launch.
      mapped_to: Q8
      source: attack:T1218.network_connection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q3
      source: template:masquerading.true_identity
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "regsvr32.exe not launched by explorer.exe (example, no fixed list)"
      disposition: context
      target_question: Q6
      notes: "Parent-child legitimacy framed in process_chain context; ProcessGuid-scoped parent lookup, example parents named in prose not hard-coded into a selection."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "msiexec.exe, regsvr32.exe (signed-LOLBIN allowlist examples)"
      disposition: context
      target_question: Q3
      notes: "Allowlist is a tuning rationale; true_identity checks this specific helper's signature/OriginalFileName/hash rather than a hard-coded list. Rule has no commandline_values to inject."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (URLs, script paths, DLL entrypoints — no fixed regex supplied)"
      disposition: context
      target_question: Q5
      notes: "mechanism_decode recovers the proxied binary path (arg 3) and PID-file path (arg 2) qualitatively; rule's commandline_values is empty so no CommandLine|contains injection is available without inventing values."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: new_question
      target_question: Q8
      notes: "Drove the attack:T1218.network_connection question covering file-share vs. external destinations from the helper and its child."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.follow_on
      reason: "follow-on child-process angle reauthored as attack:T1218.proxied_execution (proxied child = arg-3 binary); same host+ParentImage query, technique-sourced and tied to the LOLBIN's proxy mechanism"
      kept: attack:T1218.proxied_execution
    - dropped: cue_process_tool.clustering
      reason: "host-clustering coverage provided by cue_host_context.clustering (same +/-15m host-scoped Image aggregation); process_tool variant redundant"
      kept: cue_host_context.clustering
    - dropped: masquerading.renamed_binary_activity
      reason: "template's local-activity query (host + ParentImage|endswith %Image%) duplicates attack:T1218.proxied_execution; its scope thread (same renamed binary on other hosts) reauthored as attack:T1218.distribution_scope with a cross-host OriginalFileName-matched aggregation that the template's fixed [Image, CommandLine] fields cannot express"
      kept: attack:T1218.distribution_scope
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. Rule fires on the rename/proxy mechanism, not a hands-on-keyboard payload, and the disposition turns on identifying the proxied binary (Q5/Q7/Q8), not on whether AV/EDR was weakened first. cue_host_context.precursor already surfaces prior tamper alerts on the host. Dropped to stay within the 18-question cap while keeping the proxy-chain and network coverage."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule's abuse path is process-launch proxy execution: the renamed
        helper passes its third argument to Process.Start and the payload runs
        as a child process (visible in process_creation), not as a DLL/module
        loaded into the helper. The helper is not a DLL host. image_load on the
        renamed helper would return its own normal .NET runtime modules without
        precision for the proxied execution. The proxied-child question
        (attack:T1218.proxied_execution) covers the execution stage via
        process_creation evidence sufficiently for triage. No compensating
        attack:T1218.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - masquerading
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      file_event: 1
      network_connection: 1
      sigma_alert: 1
