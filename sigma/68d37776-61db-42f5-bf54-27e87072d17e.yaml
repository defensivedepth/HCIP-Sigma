name: PUA - NPS Tunneling Tool Execution
id: 79954b5c-55ae-4832-9ab0-6947da894f35
description: |
  Detects execution of the NPS intranet penetration / reverse-proxy client (npc.exe), a Go-based
  tunneling tool that brokers TCP, UDP, HTTP(S), and SOCKS5 traffic between an internal host and an
  external server, typically over a control connection on a high port. The rule fires when EITHER the
  process image ends in npc.exe, OR the command line carries the provisioned client argument set
  (-server= and -vkey= and -password=), OR the command line contains -config=npc, OR the file hash
  matches a known NPS client build. Legitimate use exists for NAT traversal and remote administration,
  but the same client is routinely repurposed to tunnel RDP, SOCKS5, and other internal services back
  to attacker-controlled infrastructure. Investigation should confirm which branch fired, decode the
  server and tunnel parameters, identify the outbound destination and port, and determine whether the
  tool is authorized and explained by an active support or automation context.
type: detection
detection_id: 68d37776-61db-42f5-bf54-27e87072d17e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did this execution match the rule by binary name, by the -server/-vkey/-password argument set, by -config=npc, or by file hash?
  context: >-
    The rule fires when at least one branch matches: the image ends in npc.exe, the command line
    contains all of -server=, -vkey=, and -password=, the command line contains -config=npc, or the
    file hash matches a known NPS client build. Confirm which branch fired before investing further
    time and note it, because the branch shapes the rest of the investigation. A command-line branch
    match gives you the server, key, and tunnel parameters directly; an image-only or hash-only match
    means the tunnel parameters live in a config file rather than on the command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - Hashes
- source: template:universal.host_user_context
  question: Is this user and host expected to run an intranet tunneling or reverse-proxy client?
  context: >-
    Legitimate use of a tunneling client is usually limited to specific administrators, network
    engineers, or hosts with a documented NAT-traversal need. A tunneling client running under a
    general user account, a service account, or a host with no such role is a stronger signal than the
    same activity on a known jump host. Knowing whether the user role and host function fit this kind
    of tool is often the fastest path to separating a true positive from sanctioned use.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:remote_access_tool.tool_authorization
  question: Is this tunneling client on the organization's approved software list?
  context: >-
    Organizations typically sanction a small set of remote-access and tunneling tools for IT and
    network operations. A tunneling client that is not on that list may represent shadow IT or
    attacker-deployed access, regardless of the tool's reputation. NPS in particular supports
    registering itself as a system service, so an unapproved instance can also be a persistence
    foothold.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there an approved use case, change ticket, or support session explaining this tunnel?
  context: >-
    Legitimate tunneling usually correlates with an active support ticket, a scheduled maintenance
    window, or a documented development or remote-access need. If no such context exists for this
    execution, the tool may have been deployed by an attacker for persistent access or by a user
    bypassing network controls. Check change-management and helpdesk records for an entry that names
    this host or this purpose.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What server, key, and tunnel parameters did this client invocation specify? Decode the full command line.
  context: >-
    The provisioned client startup command takes the form npc -server=<host>:<port> -vkey=<key>
    -type=<mode>, where the server and port identify the external control endpoint and the key
    authenticates the client to it. A -config=npc invocation instead loads parameters from a config
    file, so the command line alone will not reveal the destination. Decode every argument to identify
    the control server, the port (the tool's default control bridge is 8024 but any port can be set),
    and any tunnel mode. If parameters are absent because the image-only or hash branch fired, locate
    and read the referenced config file.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this tunneling client and is that parent chain expected?
  context: >-
    The parent process reveals how the client was launched. A launch from explorer.exe or a service
    manager is consistent with interactive or service-installed use; a launch from a script host,
    Office application, mshta, or an unexpected staging directory points to delivery through another
    stage. NPS can register itself as a Windows service, so a services.exe parent may reflect installed
    persistence rather than a one-off run. Walk up the chain to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1090.network_destination
  question: What external endpoint and port did this client connect to after launch?
  context: >-
    The outbound connection is where the tunnel becomes visible from inside the network. The control
    connection runs to the external server on a single high or non-standard port (the tool's default is
    8024, but operators commonly choose other ports such as 1080, 8080, or 4444). A destination that is
    a public IP on a high port, rather than a known internal relay, indicates traffic is being bridged
    to attacker-reachable infrastructure. Capture the destination IP, hostname, and port and compare
    them against expected egress.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: attack:T1090.tuning_parameter
  question: Did this host make outbound connections to common proxy or high-numbered ports around the alert time?
  context: >-
    Tunneling and proxy traffic concentrates on a small set of operator-chosen ports. Connections to
    well-known proxy ports such as 1080, 8080, or 4444, or to other high-numbered non-standard ports,
    around the alert time help confirm that a bridge was actually established rather than just a client
    being launched. Correlate the destinations with the control endpoint identified from the command
    line to distinguish the control channel from any forwarded tunnel traffic.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationPort:
          - 1080
          - 8080
          - 4444
          - 8024
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
- source: template:cue_process_tool.follow_on
  question: What child processes did this client spawn afterward?
  context: >-
    The processes a tunneling client spawns reveal whether it did more than open a connection. A proxy
    client that launches command interpreters, credential-access tooling, or reconnaissance binaries
    suggests the tunnel is being used for hands-on activity rather than passive forwarding. Unexpected
    children in the window after launch are worth tracing into their own command lines.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run this tunneling client?
  context: >-
    A client that has never run on this host before is more suspicious than one that appears regularly
    as part of a sanctioned tunnel. Zero or very low historical counts over the baseline window indicate
    first-time or rare execution and raise the priority of confirming authorization and destination.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: >-
    Even an authorized tunnel has a normal shape. This view mirrors the rule's selection scoped to this
    host, so the resulting parent and user distribution reflects how this specific firing pattern has
    historically run here rather than generic binary usage. A new parent process, an unexpected user
    account, or an unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
        Image|endswith: \npc.exe
      selection_cli_1:
        CommandLine|contains|all:
        - ' -server='
        - ' -vkey='
        - ' -password='
      selection_cli_2:
        CommandLine|contains: ' -config=npc'
      selection_hashes:
        Hashes|contains:
        - MD5=AE8ACF66BFE3A44148964048B826D005
        - SHA1=CEA49E9B9B67F3A13AD0BE1C2655293EA3C18181
        - SHA256=5A456283392FFCEEEACA3D3426C306EB470304637520D72FED1CC1FEBBBD6856
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run intranet tunneling or proxy tooling?
  context: >-
    Compare this user's current action against their historical behavior across hosts. A user who has
    never run this kind of tool is more suspicious than one who does so as part of a documented network
    or administration role. The user's role and typical toolset should align with running a tunneling
    client.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and binary combination historically routine on this host or across the environment, or is the parent unusual for this client?
  context: >-
    When the alert fires under a service account, user identity does not attribute the behavior; the
    meaningful pivot is the launch mechanism. Look at which parent process spawned this client and
    whether that pairing is historically common. A new or rare parent for this binary is the deviation
    worth chasing, while a pairing that already accounts for many prior events is more likely an
    installed service or automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts in the surrounding window reveals lateral movement or
    parallel activity on other endpoints. If the same user established tunnels or ran related tooling on
    multiple hosts, the scope of the activity may be larger than the alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and binary combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: >-
    Under a service account, asking what else the user did returns broad agent-telemetry noise. The
    meaningful question is whether the same launch mechanism — the same parent process spawning this
    client — fired on other hosts in the alert window. Multiple hosts in the same window points to
    scheduled or pushed deployment of the tunnel; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    A tunnel rarely stands alone. Earlier detections on the same host — reconnaissance, credential
    access, or defense tampering — may reveal the stages that preceded the proxy setup and help
    reconstruct the timeline. Sequencing prior alerts against this one shows whether the tunnel is an
    entry point or a later stage of an ongoing operation.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1090
      name: Proxy
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: proxy_client_invocation
      description: "Tunneling client process is launched with server/key/tunnel parameters (or a config file reference)"
      mapped_to: Q5
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: network_bridge_established
      description: "Client opens an outbound connection to an external endpoint on a high or non-standard port, bridging traffic"
      mapped_to: Q7
      source: attack:T1090.network_destination
  dimension_coverage:
    - dimension: command_analysis
      technique: T1090
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: network_destination
      technique: T1090
      covered_by: Q7
      source: attack:T1090.network_destination
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1090
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1090
      covered_by: Q8
      source: attack:T1090.tuning_parameter
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1090
      covered_by: Q8
      source: attack:T1090.tuning_parameter
      coverage_type: direct
    - dimension: user_context
      technique: T1090
      covered_by: Q12
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (rundll32, svchost as examples of rare network-capable spawners)"
      disposition: context
      target_question: Q6
      notes: "process_chain question asks whether the parent chain is expected; example spawners are rationale, not a queryable value set for this rule."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "1080, 8080, 4444 (plus tool default 8024)"
      disposition: query_injection
      target_question: Q8
      notes: "Ports injected into a network_connection DestinationPort selection; added 8024 as the NPS control-bridge default from db_refs."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (spikes in outbound connections over a short period)"
      disposition: context
      target_question: Q8
      notes: "Threshold has no concrete value; expressed via +/-15m window and context calling for clustering around the alert time."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "host-scoped 'what other tools ran' overlaps cue_host_context activity context; precursor (sigma-alert) chosen for context slot and the host-tamper sweep dropped, keeping question count within cap while preserving network-pivot coverage that is more specific to a proxy rule."
      kept: cue_host_context.precursor
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline + baseline_svc and clustering + clustering_svc emitted as separate questions per service_principal handling."
      kept: cue_user_actor.baseline / baseline_svc / clustering / clustering_svc
  skipped_templates:
    - template: cue_host_context.clustering
      reason: "Surrounding-host-activity context overlaps the network-destination and tuning_parameter questions that are more specific to a tunneling rule; precursor retained for the cross-alert timeline. Dropped to stay within the 14-18 cap."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This rule fires on C2/proxy setup, not payload execution or credential access; tamper-as-precursor is not the live question. cue_host_context.precursor already surfaces any prior tamper alerts on the host. defense_tampering technique set is not confirmed for this rule."
  reflection_overrides: []
  hint_dispositions:
    - hint: "Condition uses OR among selection groups: (selection_img OR selection_cli_1 OR selection_cli_2 OR selection_hashes). Q1 (verify_trigger_match) and the playbook description must frame this as 'EITHER ... OR ...', not 'X AND Y AND Z'."
      disposition: acted-on
      reason: >-
        Q1 question text and the playbook description both use disjunctive framing
        enumerating the four branches with EITHER/OR; Q1 context directs the analyst to
        note which branch fired and explains the downstream impact (command-line branch
        exposes server/key directly; image/hash branch means parameters live in a config file).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      network_connection: 2
      sigma_alert: 1
