name: Uninstall Sysinternals Sysmon
id: 719fcdc5-92e9-4289-b8d1-9fbcc72bd5c2
description: |
  Detects an execution of the Sysmon binary (Sysmon64.exe or Sysmon.exe, identified by Image or by the file Description
  "System activity monitor") with a command line containing the -u uninstall flag. Both the binary selection AND the -u
  command-line selection must match for the rule to fire. The -u switch removes the Sysmon driver and service, which
  silences the host's process, network, and registry telemetry feed. Legitimate administrators occasionally remove Sysmon
  during troubleshooting or version upgrades, so the investigation should confirm who ran the uninstall, whether it was a
  sanctioned change, what telemetry gap it created, and whether any activity exploited that gap once Sysmon stopped reporting.
type: detection
detection_id: 6a5f68d1-c4b5-46b9-94ee-5324892ea939
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects, with a Sysmon binary run together with the -u uninstall flag?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when both halves match together, a Sysmon binary (Sysmon64.exe or Sysmon.exe by Image, or the file Description "System activity monitor") AND a command line carrying the -u uninstall switch. Confirm both are present in the actual event, since -u with a Sysmon binary removes the driver and service rather than reconfiguring them.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - Description
- source: template:universal.host_user_context
  question: Is this user and host expected to uninstall or reconfigure host telemetry tooling?
  context: Removing Sysmon is a sanctioned action for a small set of administrators during upgrades or troubleshooting, and unusual for everyone else. Knowing whether the user role and host function involve managing endpoint telemetry is often the fastest path to separating a planned maintenance change from an operator quietly disabling visibility before further action.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Confirm from the command line that this is a full Sysmon uninstall, and note whether the -u removes the driver and service or targets a specific configuration.
  context: The -u switch on a Sysmon binary removes the Sysmon driver and service, which stops process, network, file, and registry event collection for that host. Read the full command line to confirm the action is an uninstall and not a related management call, and capture any path or service-name argument. Identifying exactly what was removed determines the size of the telemetry gap and the remediation needed to restore monitoring.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this Sysmon uninstall, and is that parent chain expected?
  context: The parent process reveals how the uninstall was invoked. A sanctioned removal usually descends from an interactive admin shell, a deployment agent, or services.exe. Parents like mshta.exe, wscript.exe, an Office application, or an unexpected one-off script point to operator-driven or staged tampering rather than routine maintenance. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is Sysmon currently running on this host, or does it remain uninstalled?
  context: The uninstall may be temporary (part of a version upgrade that reinstalls Sysmon moments later) or persistent. Checking whether the Sysmon service and driver are present and reporting again reveals whether the host still has a telemetry blind spot. If Sysmon is still absent, monitoring should be restored before the investigation continues, since events generated in the interim were never recorded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after Sysmon was uninstalled?
  context: Tooling is removed for a reason, to run actions that would normally be recorded. Examining activity in the window immediately after the uninstall reveals what the removal was meant to hide or enable. Because Sysmon-sourced events stop once the service is gone, a sudden drop in process telemetry followed by activity from other log sources points to operations deliberately run under reduced visibility.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host around the uninstall?
  context: An operator who removes one telemetry source often disables several, stopping Sysmon AND clearing event logs AND turning off Defender to maximize coverage of their activity. Finding additional tampering events from the same user or parent process in the same window indicates a deliberate visibility-reduction operation rather than an isolated administrative uninstall.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1562.001.registry_tamper
  question: Were the Sysmon configuration or driver registry keys modified or deleted on this host around the alert?
  context: Beyond a clean -u uninstall, Sysmon can be neutralized by editing or deleting its registry state, the SysmonDrv driver keys and the configuration values under the Sysmon service, which alters or silences collection without a tracked process. This file and registry view catches tampering that the process command line alone does not show, and pairs with the uninstall to reveal whether the operator also touched the underlying configuration.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - '\SysmonDrv'
          - '\Services\Sysmon'
          - '\Services\Sysmon64'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_process_tool.baseline
  question: Does this host normally run the Sysmon binary?
  context: A Sysmon execution that has never been seen on a host is more notable than one that recurs on a predictable maintenance cadence. Zero or very low historical counts indicate first-time or rare invocation, which on a managed endpoint raises the priority of confirming this uninstall was a planned change.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even a routine tool can be abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific Sysmon-uninstall firing pattern has historically run here, not generic Sysmon usage. A new parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pe:
      - Image|endswith:
        - \Sysmon64.exe
        - \Sysmon.exe
      - Description: System activity monitor
      selection_cli:
        CommandLine|contains|windash: -u
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pe:
      - Image|endswith:
        - \Sysmon64.exe
        - \Sysmon.exe
      - Description: System activity monitor
      selection_cli:
        CommandLine|contains|windash: -u
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pe:
      - Image|endswith:
        - \Sysmon64.exe
        - \Sysmon.exe
      - Description: System activity monitor
      selection_cli:
        CommandLine|contains|windash: -u
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user removed telemetry tooling or ran other actions on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pe:
      - Image|endswith:
        - \Sysmon64.exe
        - \Sysmon.exe
      - Description: System activity monitor
      selection_cli:
        CommandLine|contains|windash: -u
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or data staging surrounding the telemetry removal.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this Sysmon-uninstall firing pattern, not generic Sysmon usage. Many hosts firing the same pattern points to a scripted deployment or sanctioned upgrade wave; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_pe:
      - Image|endswith:
        - \Sysmon64.exe
        - \Sysmon.exe
      - Description: System activity monitor
      selection_cli:
        CommandLine|contains|windash: -u
      condition: all of selection_*
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were process-based persistence mechanisms installed on this host around the uninstall?
  context: Removing telemetry is often paired with establishing or refreshing a foothold while the host is not reporting. Scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions created in the same timeframe indicate intentional persistence stacking rather than an isolated maintenance uninstall. Reviewing these tools in the window around the alert reveals whether the operator used the visibility gap to install backup access.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, reconnaissance, credential access, or other tampering, may reveal the stages that preceded the telemetry removal and help reconstruct the timeline. Look for patterns: reconnaissance followed by tampering, or credential access followed by visibility reduction.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: telemetry_tool_removal
      description: Sysmon binary is invoked with -u to uninstall the driver and service, killing host telemetry.
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: telemetry_gap_status
      description: Confirm whether the Sysmon service/driver remains absent (the abnormal absence of expected telemetry).
      mapped_to: Q5
      source: template:defense_tampering.protection_status
    - stage: 3
      label: registry_modification
      description: Sysmon driver/service registry keys are altered or deleted to silence collection without a clean uninstall.
      mapped_to: Q8
      source: attack:T1562.001.registry_tamper
    - stage: 4
      label: activity_under_reduced_visibility
      description: Follow-on activity runs on the host after collection stops.
      mapped_to: Q6
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q6
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.other_tampering_activity
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562
      covered_by: Q15
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562
      covered_by: Q8
      source: attack:T1562.001.registry_tamper
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only (expected admin tools to suppress false positives)"
      disposition: context
      target_question: Q2
      notes: "No fixed exclusion list in brief; user/host expectation question carries the legitimate-admin distinction qualitatively."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window linking termination, registry edits, service stop)"
      disposition: context
      target_question: Q6
      notes: "No queryable threshold; activity_after_tampering (+1h) and host-clustering windows carry the temporal-correlation intent."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "Sysmon, Sysmon64, SysmonDrv (service/driver names for this rule's target)"
      disposition: query_injection
      target_question: Q8
      notes: "Sysmon service and driver key names injected into the attack:T1562.001.registry_tamper TargetObject|contains selection."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only (authorized admin tools that stop services during maintenance)"
      disposition: context
      target_question: Q4
      notes: "No fixed allowlist; parent-chain question distinguishes sanctioned deployment parents from operator-driven invocation."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only (which services count as security-critical)"
      disposition: context
      target_question: Q7
      notes: "No queryable value beyond the generic tamper service list already embedded in other_tampering_activity."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.follow_on
      reason: "post-uninstall activity covered by defense_tampering.activity_after_tampering (host-scoped +1h), which is more specific to the tampering-then-act pattern"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_process_tool.clustering
      reason: "host-clustering coverage provided by cue_host_context.clustering (same +/-15m host-scoped Image aggregation); process_tool variant redundant"
      kept: cue_host_context.clustering
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled (hint-confirmed) — the technique-set decode question is more specific to identifying the tampered control"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed for this rule; what_was_disabled / other_tampering_activity are more specific than this generic host-tamper sweep, and other_tampering_activity already surfaces prior tamper activity on the host. Per the template's own drop guidance (case a + b)."
    - template: persistence_scope.file_based_persistence
      reason: "recommended-only; rule is a process_creation telemetry-removal action with no file-drop angle, and process_based_persistence already covers the persistence-stacking question. Dropped to stay within the 18-question cap."
  reflection_overrides: []
  hint_dispositions:
    - hint: "defense_tampering.what_was_disabled replaces cue_process_tool.mechanism_decode"
      disposition: acted_on
      reason: "mechanism_decode skipped (skipped_templates); defense_tampering.what_was_disabled authored as Q3 in its place."
    - hint: "defense_tampering → persistence_scope cascade activated"
      disposition: acted_on
      reason: "persistence_scope.process_based_persistence authored as Q17; cascade honored."
    - hint: "logsource 'registry_set' (recommended by techniques: T1562.001) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: acted_on
      reason: "Authored attack:T1562.001.registry_tamper (Q8) with category: registry_set, scoping TargetObject to the SysmonDrv driver and Sysmon/Sysmon64 service keys. Covers the registry_set tampering angle the technique's analytics call for, distinct from the -u process_creation uninstall."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - persistence_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      sigma_alert: 1
      file_event: 0
