name: Allow Service Access Using Security Descriptor Tampering Via Sc.EXE
id: 0d42b76c-37ac-465d-9fd9-c76b1f313893
description: |
  Detects sc.exe invoked with the sdset verb to write a service security descriptor that adds an Allow ACE
  (A;) granting access to a trustee SID such as Everyone (WD), Authenticated Users, Interactive (IU), Service
  (SU), Built-in Administrators (BA), or Local System (SY). Loosening a service DACL lets a low-privilege
  principal start, stop, or reconfigure a SYSTEM-context service, which is a fileless and persistent route to
  privilege escalation and persistence. Legitimate use is rare and usually tied to deliberate administrative
  hardening or vendor agent configuration; the investigation should determine which service was modified, what
  rights were granted to which principal, whether the change was authorized, and whether the service was then
  started or reconfigured to execute attacker-controlled code.
type: detection
detection_id: 6c8fbee5-dee8-49bc-851d-c3142d02aa47
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show sc.exe running sdset with an Allow ACE granting access to a trustee SID?
  context: Confirm the alert is a true positive by reviewing the triggering event. The rule fires only when the
    image resolves to sc.exe AND the command line contains both sdset and an Allow ACE marker (A;) AND it grants
    a recognized trustee alias (;IU, ;SU, ;BA, ;SY, or ;WD). Read the full SDDL string to confirm all three
    conditions are present rather than a benign sdshow or query.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to modify service security descriptors from the command line?
  context: Editing a service DACL with sc.exe sdset is an uncommon administrative action normally limited to a
    small set of platform engineers or a vendor agent. If the account and host are not associated with service
    hardening work, the activity is harder to explain as routine and warrants closer review.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:service_persistence.service_configuration
  question: Which service had its security descriptor modified, and what binary and account does that service execute?
  context: The sdset command names the target service, but the service installation record reveals the binary
    path, start type, and service account behind it. A service that runs under LocalSystem, points at a binary
    in a world-writable directory (C:\Users\Public, C:\Windows\Temp), or masquerades as a legitimate name is the
    persistence payload the loosened DACL is meant to protect. Match the service named in the sdset command line
    against recent service installation records to recover its configuration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 7045
      condition: selection
    fields:
      - ServiceName
      - ImagePath
      - StartType
      - AccountName
- source: attack:T1543.003.security_event
  question: Is there a service installation record in the Security log for the service whose descriptor was modified?
  context: The Security channel records service installation under event 4697, which captures the service name,
    image path, service type, and account as a separate corroborating record from the process telemetry that
    fired this alert. Cross-referencing the
    descriptor change against 4697 confirms whether the service was newly created around the same time or already
    existed and was only reconfigured. If Security audit logging for service installation is not enabled in this
    environment, treat zero results as a visibility gap rather than evidence nothing was installed.
  range: -24h
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4697
      condition: selection
    fields:
      - ServiceName
      - ServiceFileName
      - ServiceAccount
      - ServiceType
- source: attack:T1543.003.tuning_parameter
  question: Were registry values under the Services hive modified around the time of the descriptor change?
  context: Service configuration ultimately lives under HKLM\System\CurrentControlSet\Services. A descriptor
    change is often paired with edits to a service subkey such as ImagePath, Start, or the Security value to
    point the service at a new binary or change its start type. Registry value-set events on Services subkeys in
    the same window show whether the DACL change was part of a broader reconfiguration of the service.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\System\CurrentControlSet\Services\'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: template:service_persistence.service_current_status
  question: Is the modified service currently running, and what account is it using?
  context: A loosened DACL is only useful if the service can be started by the granted principal. Checking the
    current state shows whether the persistence is active now, was already stopped, or is set to start on demand.
    A service running as LocalSystem grants full system privileges to whoever can start it; a service running as
    a specific account indicates the level of access the granted trustee gains. Run sc query or sc qc against the
    service named in the sdset command to read its live status.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned sc.exe, and is that parent chain expected for service administration?
  context: The parent process shows how sc.exe was invoked. Interactive service hardening typically descends from
    explorer.exe, a console shell, or a known management agent. A parent such as a script host, an Office
    application, a browser child, or a freshly written binary points to invocation by a script or implant rather
    than a human administrator.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was sc.exe executed from its expected System32 or SysWOW64 path?
  context: sc.exe running from outside System32 or SysWOW64 may have been copied or renamed to evade path-based
    allowlists or to carry an altered binary. Compare the Image path and OriginalFileName from the triggering
    event against the expected location for the service control utility.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:organizational_context.change_ticket
  question: Was there a change ticket or documented approval to modify this service's access control?
  context: Service DACL hardening is occasionally performed deliberately, but as a controlled change it normally
    has an associated ticket or maintenance record. A corresponding approval suggests authorized hardening;
    absence of one when policy requires it pushes the activity toward unauthorized persistence setup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_sdset:
        CommandLine|contains|all:
        - sdset
        - A;
      selection_trustee:
        CommandLine|contains:
        - ;IU
        - ;SU
        - ;BA
        - ;SY
        - ;WD
      filter_optional_hexnode:
        ParentImage: C:\Hexnode\Hexnode Agent\Current\HexnodeAgent.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_sdset:
        CommandLine|contains|all:
        - sdset
        - A;
      selection_trustee:
        CommandLine|contains:
        - ;IU
        - ;SU
        - ;BA
        - ;SY
        - ;WD
      filter_optional_hexnode:
        ParentImage: C:\Hexnode\Hexnode Agent\Current\HexnodeAgent.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of the descriptor change?
  context: Tracking the account's activity across all hosts reveals lateral movement or parallel actions on other
    endpoints. If the same user drove similar service or persistence activity on multiple hosts, the scope of the
    operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_sdset:
        CommandLine|contains|all:
        - sdset
        - A;
      selection_trustee:
        CommandLine|contains:
        - ;IU
        - ;SU
        - ;BA
        - ;SY
        - ;WD
      filter_optional_hexnode:
        ParentImage: C:\Hexnode\Hexnode Agent\Current\HexnodeAgent.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this sdset pattern, what parents and users drove it, and does the current alert fit?
  context: Even legitimate tools get abused. The query mirrors the rule's selection structure scoped to this
    host, so the resulting parent and user distribution reflects how this specific sdset-with-Allow-ACE pattern
    has historically run here rather than generic sc.exe usage. A new parent process, an unexpected account, or
    an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_sdset:
        CommandLine|contains|all:
        - sdset
        - A;
      selection_trustee:
        CommandLine|contains:
        - ;IU
        - ;SU
        - ;BA
        - ;SY
        - ;WD
      filter_optional_hexnode:
        ParentImage: C:\Hexnode\Hexnode Agent\Current\HexnodeAgent.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.technique_class_lateral
  question: Were other persistence or tamper LOLBins used on this host recently?
  context: Operators who tamper with one service's access control often stage additional persistence or defense
    changes with related living-off-the-land binaries. Finding schtasks, reg, at, netsh, wevtutil, bcdedit, or
    vssadmin executing on the same host around this time strengthens the case that the descriptor change is part
    of deliberate persistence setup rather than an isolated administrative action.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\schtasks.exe'
          - '\reg.exe'
          - '\at.exe'
          - '\netsh.exe'
          - '\wevtutil.exe'
          - '\bcdedit.exe'
          - '\vssadmin.exe'
          - '\diskshadow.exe'
          - '\dnscmd.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this sdset pattern over the past 90 days, and what command-line structures are typical?
  context: A descriptor-tampering pattern with no prior history on a host is more suspicious than one seen
    regularly under stable parents. The query mirrors the rule's selection structure scoped to this host, so the
    count reflects this specific sdset-with-Allow-ACE pattern rather than generic sc.exe usage. The CommandLine
    projection lets the analyst compare typical SDDL patterns and granted trustees against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sc:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_sdset:
        CommandLine|contains|all:
        - sdset
        - A;
      selection_trustee:
        CommandLine|contains:
        - ;IU
        - ;SU
        - ;BA
        - ;SY
        - ;WD
      filter_optional_hexnode:
        ParentImage: C:\Hexnode\Hexnode Agent\Current\HexnodeAgent.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Service-descriptor tampering is usually a late step in a hands-on-keyboard sequence. Earlier
    detections on the same host — privilege escalation, credential access, defense tampering, or reconnaissance —
    may reveal how the operator reached the point of editing service access control and help reconstruct the
    timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1543.003
      name: Windows Service
      role: primary
    - id: T1543
      name: Create or Modify System Process
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: descriptor_modification
      description: "sc.exe sdset writes a service security descriptor with an Allow ACE granting a trustee SID"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: service_configuration
      description: "Identify the target service, its binary path, start type, and service account"
      mapped_to: Q3
      source: template:service_persistence.service_configuration
    - stage: 3
      label: registry_service_edit
      description: "Service configuration changes land under HKLM\\System\\CurrentControlSet\\Services"
      mapped_to: Q5
      source: attack:T1543.003.tuning_parameter
    - stage: 4
      label: service_execution
      description: "The granted principal can start the SYSTEM-context service to gain execution"
      mapped_to: Q6
      source: template:service_persistence.service_current_status
  dimension_coverage:
    - dimension: tuning_parameter
      technique: T1543.003
      covered_by: Q5
      source: attack:T1543.003.tuning_parameter
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1543
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ServiceNamePattern
      dimension: tuning_parameter
      specific_values: "rationale only (e.g. svhostx, winhelp)"
      disposition: context
      target_question: Q3
      notes: "Service name is recovered from the sdset command and matched against installation records; no fixed name list to inject."
    - element: ImagePathFilter
      dimension: tuning_parameter
      specific_values: "C:\\Users\\, C:\\Temp\\"
      disposition: context
      target_question: Q3
      notes: "World-writable image-path locations called out in service_configuration context as the payload signal."
    - element: DriverExtensionList
      dimension: tuning_parameter
      specific_values: "rationale only (.sys)"
      disposition: context
      target_question: Q5
      notes: "Driver/service registry changes surfaced by the Services-hive registry_set question; no .sys-specific injection added since this rule fires on DACL edit not driver load."
    - element: StartupTypeChangeWindow
      dimension: tuning_parameter
      specific_values: "rationale only (temporal window)"
      disposition: context
      target_question: Q5
      notes: "Start key change correlation covered by the +/-15m registry_set window on Services subkeys."
    - element: UnsignedBinaryAlert
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Binary path/account surfaced via 7045 fields for the analyst to assess signing; no signing field queryable here."
    - element: ParentProcessFilter
      dimension: parent_process_analysis
      specific_values: "rationale only (non-admin parents)"
      disposition: context
      target_question: Q7
      notes: "Parent legitimacy assessed by process_chain question; baseline_deviation adds historical parent distribution."
    - element: RegistryPathList
      dimension: tuning_parameter
      specific_values: "HKLM\\System\\CurrentControlSet\\Services"
      disposition: query_injection
      target_question: Q5
      notes: "Injected as TargetObject|contains '\\System\\CurrentControlSet\\Services\\' in the registry_set question."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "Rule-specific reflected baselines (cue_process_tool.baseline_deviation, lolbin.historical_lolbin_baseline) and cue_user_actor.baseline cover historical frequency with pattern precision; generic Image-only baseline adds no distinct signal."
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "Host-time clustering covered by cue_user_actor.clustering (user pivot) and the precursor alert sweep; generic same-host tool clustering would duplicate."
      kept: cue_user_actor.clustering
    - dropped: cue_host_context.clustering
      reason: "Same-host activity context provided by cue_user_actor.clustering and the precursor question; the aggregated host-wide process sweep adds noise without a distinct angle for this single-event config change."
      kept: cue_host_context.precursor
    - dropped: cue_process_tool.follow_on
      reason: "Child-process follow-on subsumed by service_current_status (did the service start/execute) plus the persistence/tamper LOLBin sweep; sc.exe sdset itself does not spawn the payload — the service start does."
      kept: template:service_persistence.service_current_status
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: service_persistence.service_configuration (structured service fields supersede raw command-line decode)"
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED-only generic tamper sweep; defense_tampering not confirmed and this rule is a config-change not an execution/credential rule. Prior-host tamper context already carried by cue_host_context.precursor."
    - template: persistence_scope.process_based_persistence
      reason: "RECOMMENDED; persistence-tool scan covered by lolbin.technique_class_lateral adapted to the persist/tamper LOLBin set (schtasks, reg, sc, at, netsh, bcdedit, vssadmin)."
    - template: persistence_scope.file_based_persistence
      reason: "RECOMMENDED; this rule fires on service-DACL tampering, not a file drop. Startup-folder/Run-key file monitoring is a different mechanism and out of scope for a sdset config change."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1543.003) is not covered and no template family exists — author a custom question with category: security"
      disposition: acted-on
      reason: >-
        Authored Q4 (attack:T1543.003.security_event) querying Security event 4697 (service installation)
        for the modified service, complementing the System-log 7045 service_configuration question.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - service_persistence
    - organizational_context
    - lolbin
    - cue_user_actor
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      service_system: 1
      security: 1
      registry_set: 1
      sigma_alert: 1
