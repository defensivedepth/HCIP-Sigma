name: Potentially Suspicious JWT Token Search Via CLI
id: 5b4d8739-3631-4080-b692-17a1750a2510
description: |
  Detects command-line searches for JSON Web Token (JWT) strings, identified when a search utility (find, find.exe,
  findstr, select-string, strings) runs AND its command line contains a JWT prefix such as eyJ0eX or eyJhbG. Both
  conditions must be present for the rule to fire. JWTs are bearer access tokens for cloud and SaaS platforms; an
  operator who can dump a process or read a token file often greps for these prefixes to harvest live tokens for
  reuse from another device. Legitimate triggers include developers or support staff inspecting their own tokens and
  scripted token-validation tooling. The investigation should confirm what the command actually searched, where the
  token material came from, who ran it, and whether stolen tokens were subsequently used.
type: detection
detection_id: 6d3a3952-6530-44a3-8554-cf17c116c615
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line both run a search utility and contain a JWT prefix?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires only when a search
    tool (find, find.exe, findstr, select-string, strings) is invoked AND the command line contains a JWT prefix
    (eyJ0eXAiOi, eyJhbGciOi, eyJ0eX, or eyJhbG). Verify both elements are present and read the full argument string to
    see what file or stream was searched.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to search for JWT or access-token strings on the command line?
  context: Legitimate inspection of one's own access tokens by developers, support staff, or automation can trigger this
    pattern. Knowing whether the user's role and the host's function involve cloud-token handling is often the fastest
    way to separate routine work from token harvesting.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: What token material did this command search, and where did it come from?
  context: Decode the full command line to identify the search target. JWT prefixes are commonly grepped out of process
    memory dumps of authenticated desktop applications, token cache files, or saved credential files. Identify whether
    the search ran against a memory dump, a file path, piped output, or a directory, since the source determines which
    accounts and which token audiences may be exposed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.evidence_preservation
  question: Are any files referenced by the command (memory dumps, token caches, output files) still on disk?
  context: The search frequently targets a file the operator created or read — a process memory dump, a token cache, or
    an output file capturing matched tokens. If such a file still exists on disk, it enables hash lookup, content review,
    and a determination of which tokens were exposed. File paths are derived from the alerting command line.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this search command, and is that parent chain expected?
  context: The parent process reveals how the search was invoked. Interactive use typically descends from a shell or
    terminal launched by explorer.exe. Parents such as Office applications, script hosts (wscript.exe, mshta.exe), or a
    remote-execution service suggest the search was driven by a payload or hands-on-keyboard operator rather than the
    console user.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this JWT-search pattern, what parents and users normally drive it, and
    does the current alert fit that pattern?
  context: Even routine token validation can match this rule. The query mirrors the rule's full selection — a search
    tool combined with a JWT prefix — scoped to this host, so the resulting parent and user distribution reflects how
    this specific pattern has historically run here. A new parent process, an unexpected user account, or an unusual
    command structure is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_tools:
        CommandLine|contains:
        - 'find '
        - find.exe
        - findstr
        - 'select-string '
        - strings
      selection_jwt_string:
        CommandLine|contains:
        - eyJ0eXAiOi
        - eyJhbGciOi
        - ' eyJ0eX'
        - ' "eyJ0eX"'
        - ' ''eyJ0eX'''
        - ' eyJhbG'
        - ' "eyJhbG"'
        - ' ''eyJhbG'''
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did the search command spawn afterward?
  context: A token search that is part of a larger operation may launch follow-on processes — a tool to decode or replay
    the harvested token, an archiver to stage it, or a network client to send it outbound. The children of the alerting
    process show whether the search led directly to exfiltration or token reuse.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired this JWT-search pattern in the past week, and does the cross-host distribution look
    like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of
    this firing pattern rather than generic search-tool usage. Many hosts running the same pattern points to scripted
    deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_tools:
        CommandLine|contains:
        - 'find '
        - find.exe
        - findstr
        - 'select-string '
        - strings
      selection_jwt_string:
        CommandLine|contains:
        - eyJ0eXAiOi
        - eyJhbGciOi
        - ' eyJ0eX'
        - ' "eyJ0eX"'
        - ' ''eyJ0eX'''
        - ' eyJhbG'
        - ' "eyJhbG"'
        - ' ''eyJhbG'''
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_tools:
        CommandLine|contains:
        - 'find '
        - find.exe
        - findstr
        - 'select-string '
        - strings
      selection_jwt_string:
        CommandLine|contains:
        - eyJ0eXAiOi
        - eyJhbGciOi
        - ' eyJ0eX'
        - ' "eyJ0eX"'
        - ' ''eyJ0eX'''
        - ' eyJhbG'
        - ' "eyJhbG"'
        - ' ''eyJhbG'''
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_tools:
        CommandLine|contains:
        - 'find '
        - find.exe
        - findstr
        - 'select-string '
        - strings
      selection_jwt_string:
        CommandLine|contains:
        - eyJ0eXAiOi
        - eyJhbGciOi
        - ' eyJ0eX'
        - ' "eyJ0eX"'
        - ' ''eyJ0eX'''
        - ' eyJhbG'
        - ' "eyJhbG"'
        - ' ''eyJhbG'''
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the user's activity across all hosts reveals whether the token search was isolated or part of a
    broader sequence — credential access on one host followed by reuse on another. If the same user triggered related
    activity on multiple endpoints, the scope of exposure may be larger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_tools:
        CommandLine|contains:
        - 'find '
        - find.exe
        - findstr
        - 'select-string '
        - strings
      selection_jwt_string:
        CommandLine|contains:
        - eyJ0eXAiOi
        - eyJhbGciOi
        - ' eyJ0eX'
        - ' "eyJ0eX"'
        - ' ''eyJ0eX'''
        - ' eyJhbG'
        - ' "eyJhbG"'
        - ' ''eyJhbG'''
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context — what led
    to the search, what happened after, and whether it is part of a larger sequence. Look for process-dumping utilities,
    reconnaissance, or staging that surrounds the token search.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, process memory dumping, or reconnaissance — may reveal
    the attack stages that preceded the token search and help reconstruct the timeline that led to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts and token audiences may be exposed, and do any carry elevated privileges?
  context: The severity depends on what the harvested tokens grant. A JWT carries an audience and scope that determine
    which resources it can reach; a token for a high-privilege identity or a broadly scoped application requires
    escalation, while a low-privilege user token may only need monitoring. Cross-reference the running user and any
    decoded token claims with directory group memberships.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1552.001.security_authentication
  question: Were new authentication or logon events recorded on this host shortly after the search?
  context: If the harvested token or credential was used locally, it can surface as a new logon in Windows Security
    authentication events. Correlating logon activity on this host immediately after the search helps confirm whether the
    captured material was actually exercised on the endpoint, distinguishing reconnaissance from active reuse. If Security
    authentication events are not collected in this environment, treat zero results as a visibility gap rather than
    evidence nothing happened.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: security
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - LogonType
      - IpAddress
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1552.001
      name: Credentials In Files
      role: primary
    - id: T1528
      name: Steal Application Access Token
      role: secondary
    - id: T1552
      name: Unsecured Credentials
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: search_invocation
      description: A search utility is invoked with a JWT prefix to locate access tokens in files or process memory
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: token_source_identification
      description: Identify the source the search targeted (memory dump, token cache, credential file) and the token material exposed
      mapped_to: Q3
      source: credential_access.credential_target_identification
    - stage: 3
      label: credential_use
      description: Use of the retrieved token or credential — new authentication, logon, or remote/cloud reuse following the search
      mapped_to: Q18
      source: attack:T1552.001.security_authentication
  dimension_coverage:
    - dimension: time_correlation
      technique: T1552.001
      covered_by: Q16
      source: credential_access.post_access_lateral_movement
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1552.001
      covered_by: Q1
      source: template:universal.verify_trigger_match
      coverage_type: direct
    - dimension: user_context
      technique: T1552.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: network_indicators
      technique: T1528
      covered_by: Q16
      source: credential_access.post_access_lateral_movement
      coverage_type: indirect
    - dimension: command_analysis
      technique: T1552
      covered_by: Q3
      source: credential_access.credential_target_identification
      coverage_type: direct
    - dimension: process_identification
      technique: T1552
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: FileNamePattern
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q4
      notes: >-
        T1552.001 calls out *.env / *credential* style patterns; this rule's concrete tunable
        values are the JWT prefixes and search tools, already enforced in Q1/Q7/Q9. The file-target
        angle is carried as context in the evidence-preservation question.
    - element: ProcessAccessScope
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q6
      notes: >-
        Untrusted-parent scoping is a tuning rationale without fixed values for this rule; covered by
        the parent-chain question's analysis of how the search was invoked.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: rationale only
      disposition: context
      target_question: Q16
      notes: >-
        Time delta between credential access and reuse is a threshold, not a queryable value;
        carried by the follow-on authentication / lateral-movement windows.
    - element: SuspiciousProcessList
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q1
      notes: >-
        T1552 lists reg.exe/cmd.exe/powershell.exe as monitorable processes; this rule's own
        selection_tools (find/findstr/strings/select-string) is the analog and is enforced in the
        trigger-match and baseline-deviation queries.
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification — more specific Phase 2 question for credential-access rules"
      kept: credential_access.credential_target_identification
    - dropped: cue_process_tool.clustering
      reason: "near-identical host-scoped aggregation to cue_host_context.clustering (other activity on host around alert time)"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "generic single-binary baseline is muddy for a CommandLine-only multi-tool rule; baseline_deviation covers historical firing of the rule's actual pattern on this host more precisely"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_user_actor.baseline_svc and cue_user_actor.clustering_svc dedup
      reason: "NOT dropped — user_actor pair is non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: both variants
  skipped_templates:
    - template: cue_user_actor.baseline (paired)
      reason: "kept — listed here only to note the service-principal pair was preserved per service_principals.yaml emit_both default"
  hint_dispositions:
    - hint: "credential_access.credential_target_identification replaces cue_process_tool.mechanism_decode"
      disposition: acted-on
      reason: >-
        cue_process_tool.mechanism_decode skipped; credential_access.credential_target_identification
        authored as the Phase 2 analysis question (Q3). Recorded in dedup_decisions.
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted-on
      reason: >-
        Rule-pattern scoping queries (baseline_deviation Q7, scope Q9, user baseline Q10) use the
        rule's CommandLine|contains JWT pattern, not Image. follow_on (Q8) reframed to ParentProcessGuid
        child lookup. Only the user_actor service-principal substitutes (Q11/Q13) retain Image|endswith
        — that field hydrates from the alert event's actual executable, not from a rule-constrained
        binary, which is the intended mechanism pivot.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted-on
      reason: "evidence_preservation question (Q4) uses TargetFilename|endswith|expand '%file_paths%'."
    - hint: "logsource 'security' (recommended by techniques: T1552.001) is not covered and no template family exists — author a custom question with category: security"
      disposition: acted-on
      reason: >-
        Authored Q18 (attack:T1552.001.security_authentication) with logsource category: security to
        cover authentication/logon events after the search — the analyst-side pivot into credential
        reuse that crosses the process_creation → security logsource boundary.
  contextual_sets:
    included: []
    excluded:
      - id: lateral_movement
        confidence: medium
        reason: >-
          Reference material mentions lateral movement only as a downstream consequence of token
          theft, not as behavior this rule detects. The rule fires on a local CLI search; lateral
          follow-on is already covered by credential_access.post_access_lateral_movement (Q16)
          without pulling in a full lateral_movement technique set.
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      file_event: 1
      security: 1
      sigma_alert: 2
