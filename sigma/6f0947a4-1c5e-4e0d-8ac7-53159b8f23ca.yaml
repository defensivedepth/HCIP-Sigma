name: Potentially Suspicious Child Process Of Regsvr32
id: 9b212d7e-4176-49ba-9095-21d23685484c
description: |
  Detects regsvr32.exe spawning a child process drawn from a list of interpreters, recon, and persistence tools that it should almost never launch — calc.exe, cscript.exe, explorer.exe, mshta.exe, net.exe, net1.exe, nltest.exe, notepad.exe, powershell.exe, pwsh.exe, reg.exe, schtasks.exe, werfault.exe, or wscript.exe — excluding the benign WerFault crash-reporting case (werfault.exe with -u -p). regsvr32 is the signed COM registration utility; a common abuse loads a local or remote scriptlet or DLL (for example regsvr32 /s /i:<url> scrobj.dll), runs attacker code through the trusted binary, and that code then spawns one of these children. Because only one child fires per alert, the investigation pivots on which child it is: confirm it, decode regsvr32's own command line to recover the scriptlet or DLL it loaded and from where, decode what the child did, and look at the network fetch, the module loaded, and the sibling and follow-on activity.
type: detection
detection_id: 6f0947a4-1c5e-4e0d-8ac7-53159b8f23ca
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-03
questions:
- source: template:universal.verify_trigger_match
  question: Does this match the rule — regsvr32.exe spawning a suspicious child process?
  context: Confirm the alert before investing time. The rule fires when the parent is regsvr32.exe and the child is one of calc.exe, cscript.exe, explorer.exe, mshta.exe, net.exe, net1.exe, nltest.exe, notepad.exe, powershell.exe, pwsh.exe, reg.exe, schtasks.exe, werfault.exe, or wscript.exe — with the benign WerFault case (werfault.exe -u -p) excluded. Note which child fired, because it determines the rest of the investigation — a script host or PowerShell points to script execution, net/nltest/reg point to discovery or persistence, and an interpreter points to a follow-on stage.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
- source: template:parent_child.parent_process_legitimacy
  question: What did regsvr32 load, and what launched regsvr32 itself?
  context: regsvr32's own command line is the proxy mechanism — recover whether it registered a local DLL, loaded a scriptlet (the /i switch, often with an http(s) URL and scrobj.dll), or ran silently (/s). The grandparent shows how regsvr32 was started — an interactive admin or installer is routine, while an Office application, a script host, or a download cradle spawning regsvr32 that then spawns one of these children is the proxy-execution chain. This parent context is the core signal for the alert.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: attack:T1218.010.child_command
  question: What did the spawned child process actually do? Decode its command line.
  context: Decode the child's full command line and match the analysis to which child fired. For powershell.exe or pwsh.exe, look for an encoded or obfuscated command and a download or in-memory load. For reg.exe, identify the key and value being written. For schtasks.exe, identify the task name, trigger, and action. For net/net1/nltest, identify the enumeration target. For wscript/cscript/mshta, identify the script or URL. A child launched by regsvr32 that performs any of these is running through a proxy and should be treated as attacker-controlled until shown otherwise.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:universal.host_user_context
  question: Is regsvr32 expected to spawn interpreters or admin tools on this host?
  context: regsvr32 legitimately registers COM components, usually as a child of an installer or setup routine, and it rarely spawns command interpreters, recon utilities, or persistence tools. Knowing whether this user's role and this host's function involve software that drives regsvr32 this way helps separate an unusual installer from proxied execution. A developer or packaging host differs from an end-user workstation where regsvr32 spawning powershell is anomalous.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1218.010.remote_scriptlet
  question: Did regsvr32 make outbound network connections to fetch a remote scriptlet or DLL?
  context: regsvr32 can load a scriptlet or DLL straight from a URL, so a network connection from the regsvr32 parent is a strong sign the code it proxied was remotely hosted rather than a local COM registration. Correlating the regsvr32 process to its connections recovers the destination host, IP, and port; compare them against known software-distribution endpoints. An external or newly seen address is the signal to chase, while no connection points to a local scriptlet or DLL instead.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
- source: attack:T1218.010.proxy_module
  question: Did regsvr32 load a DLL or scriptlet module from a user-writable or temp location?
  context: Beyond registering a system COM DLL, regsvr32 is abused to load attacker code — a payload DLL or the scriptlet runtime backing a remote .sct. A module loaded by the regsvr32 parent from a temp, AppData, ProgramData, or public path, rather than from a signed system directory, indicates proxied execution of attacker-supplied code. Matching such a load to the regsvr32 instance corroborates the chain; loads only from standard locations are consistent with a normal registration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
        ImageLoaded|contains:
          - '\AppData\Local\Temp\'
          - '\AppData\Roaming\'
          - '\Windows\Temp\'
          - '\Users\Public\'
          - '\ProgramData\'
      condition: selection
    fields:
      - ImageLoaded
- source: template:parent_child.sibling_processes
  question: What other child processes did this regsvr32 instance spawn?
  context: If regsvr32 proxied execution, the script or DLL it loaded may have spawned more than one child. Listing the siblings of the alerting child — all children of this exact regsvr32 instance — reveals the full scope of what the proxied code did. Multiple suspicious children from one regsvr32 strongly suggests orchestration rather than a one-off, and a recon-then-persistence pattern across the siblings tells the operational story.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:powershell.encoded_command_decode
  question: If the child was PowerShell, was the command encoded or obfuscated, and what does it decode to?
  context: When the child is powershell.exe or pwsh.exe, attackers frequently hide intent with -EncodedCommand (-enc), string concatenation, or backticks. Base64-encoded commands are the most common form. Decoding reveals the actual payload — a download cradle, a reverse shell, or in-memory execution of a .NET assembly — which is the real action proxied through regsvr32. If the child was not PowerShell, this question does not apply.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: Several of the children this rule fires on (net, net1, nltest, reg) are discovery tools, and a burst of reconnaissance in a tight window — whoami, ipconfig, net user, systeminfo, nltest — is the signature of post-compromise enumeration. If the regsvr32-spawned child sits inside such a burst, the proxied execution is more likely one step in hands-on activity than an isolated event.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:persistence_scope.process_based_persistence
  question: Were process-based persistence mechanisms installed on this host around this time?
  context: Several children this rule fires on (reg, schtasks) directly establish persistence, and proxied execution is frequently used to install a foothold. This surfaces persistence tool executions — schtasks, sc, reg, at, bitsadmin, wmic — clustered around the alert. Multiple persistence installations in the same window indicate intentional stacking rather than an isolated action, and identify footholds to remediate.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Does regsvr32 normally spawn this child on this host?
  context: Some parent-child pairings are routine and others are inherently suspicious. This counts how often regsvr32 has spawned this specific child binary on this host historically — zero or very low counts mean a novel and suspicious spawning relationship, while a recurring count may reflect a particular installer or application that drives regsvr32 this way. regsvr32 spawning an interpreter or recon tool with no prior history is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is this regsvr32-spawning-child pattern occurring on other hosts?
  context: The same regsvr32-to-child pairing appearing on multiple hosts may indicate a deployed package that uses regsvr32 or a campaign proxying execution the same way; a single occurrence is more likely targeted. Comparing the pairing enterprise-wide helps calibrate severity and identify the scope of any compromise.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \regsvr32.exe
        Image|endswith:
        - \calc.exe
        - \cscript.exe
        - \explorer.exe
        - \mshta.exe
        - \net.exe
        - \net1.exe
        - \nltest.exe
        - \notepad.exe
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
        - \schtasks.exe
        - \werfault.exe
        - \wscript.exe
      filter_main_werfault:
        Image|endswith: \werfault.exe
        CommandLine|contains: ' -u -p '
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \regsvr32.exe
        Image|endswith:
        - \calc.exe
        - \cscript.exe
        - \explorer.exe
        - \mshta.exe
        - \net.exe
        - \net1.exe
        - \nltest.exe
        - \notepad.exe
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
        - \schtasks.exe
        - \werfault.exe
        - \wscript.exe
      filter_main_werfault:
        Image|endswith: \werfault.exe
        CommandLine|contains: ' -u -p '
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts in a tight window reveals lateral movement or activity spanning multiple endpoints. If the same user drove proxied execution or tooling on other hosts in the same window, the scope is larger than a single machine. This pivot is meaningful only when a real user account fired the alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \regsvr32.exe
        Image|endswith:
        - \calc.exe
        - \cscript.exe
        - \explorer.exe
        - \mshta.exe
        - \net.exe
        - \net1.exe
        - \nltest.exe
        - \notepad.exe
        - \powershell.exe
        - \pwsh.exe
        - \reg.exe
        - \schtasks.exe
        - \werfault.exe
        - \wscript.exe
      filter_main_werfault:
        Image|endswith: \werfault.exe
        CommandLine|contains: ' -u -p '
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — a download cradle, a macro-spawned shell, defense tampering, or reconnaissance — may reveal how regsvr32 came to proxy execution and help reconstruct the timeline. A high-severity proxy-execution event is usually a stage in a larger chain, so what came before and after is what tells the story.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.010
      name: Regsvr32
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: proxy_invocation
      description: "regsvr32 is invoked to register/load a DLL or scriptlet (recovered from its command line)"
      mapped_to: Q2
      source: template:parent_child.parent_process_legitimacy
    - stage: 2
      label: payload_retrieval_and_load
      description: "regsvr32 fetches a remote scriptlet/DLL and/or loads a payload module"
      mapped_to: Q5
      source: attack:T1218.010.remote_scriptlet
    - stage: 3
      label: child_execution
      description: "The proxied code spawns a suspicious child (the rule trigger); decode what it did"
      mapped_to: Q3
      source: attack:T1218.010.child_command
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: attack:T1218.010.child_command
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q2
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1218.010
      covered_by: Q5
      source: attack:T1218.010.remote_scriptlet
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.010
      covered_by: Q6
      source: attack:T1218.010.proxy_module
      coverage_type: direct
  mutable_dispositions:
    - element: AllowedDLLPaths
      dimension: tuning_parameter
      specific_values: "scrobj.dll / allowed DLL load paths vs DLLs from user-writable locations"
      disposition: query_injection
      target_question: Q6
      notes: "proxy_module (Q6) filters ImageLoaded to user-writable/temp paths to surface a payload DLL loaded by regsvr32 outside the allowed system locations."
    - element: ScriptletExtensions
      dimension: tuning_parameter
      specific_values: ".sct scriptlet via /i:"
      disposition: context
      target_question: Q2
      notes: "parent_process_legitimacy (Q2) directs the analyst to regsvr32's /i: scriptlet argument and any URL."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "window between regsvr32 execution, network fetch, and child spawn"
      disposition: context
      target_question: Q5
      notes: "Q5 (network) and Q6 (image_load) both use a +/-5m window, realising the process/network/module correlation the analytic calls for."
    - element: ParentProcessWhitelist
      dimension: tuning_parameter
      specific_values: "allowed parents of regsvr32 (installers/setup)"
      disposition: context
      target_question: Q2
      notes: "Q2 recovers what launched regsvr32 (the grandparent); an installer/setup is allowed, an Office app or script host is not."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "regsvr32.exe as parent of an interpreter/tool"
      disposition: context
      target_question: Q2
      notes: "The regsvr32-as-parent relationship is the rule's core signal, analysed in Q2 and baselined in Q12/Q13."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "regsvr32.exe is the trusted signed binary"
      disposition: context
      target_question: Q2
      notes: "regsvr32 is the signed proxy; the abuse is what it loads and spawns, covered by Q2/Q3/Q6."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, scriptlet paths, DLL entrypoints"
      disposition: context
      target_question: Q3
      notes: "The child command decode (Q3) and regsvr32 command (Q2) carry the URL/scriptlet/entrypoint analysis."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "legitimate enterprise domains to suppress"
      disposition: context
      target_question: Q5
      notes: "Q5 frames the destination comparison against known software-distribution endpoints."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "Brief hint 'parent_process is primary — drop cue_process_tool:process_chain'. The parent (regsvr32) analysis is covered by parent_process_legitimacy (Q2)."
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_process_tool.mechanism_decode
      reason: "Has replaced_by per the brief; its decode role is split — regsvr32's command is decoded in Q2 and the child's command in Q3."
      kept: attack:T1218.010.child_command
    - dropped: cue_parent_process.mechanism_decode
      reason: "Overlaps parent_process_legitimacy (Q2), the more specific must_include parent-analysis question that also recovers the grandparent."
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "Pivots on the parent name (regsvr32) host-wide; the precise 'what this regsvr32 spawned' is covered by sibling_processes (Q7, ParentProcessGuid-scoped)."
      kept: template:parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "Parent-name host-wide activity overlaps sibling_processes (Q7) and the precursor alert timeline (Q18)."
      kept: template:parent_child.sibling_processes
    - dropped: cue_process_tool.baseline
      reason: "Child-binary host frequency is less informative than the parent-child pairing baseline (Q12), which is the load-bearing signal for a 'regsvr32 should not spawn this' rule."
      kept: template:cue_parent_process.baseline
    - dropped: cue_host_context.clustering
      reason: "Surrounding host activity is reached via sibling_processes (Q7), the recon burst (Q10), the persistence sweep (Q11), and precursor alerts (Q17)."
      kept: cue_host_context.precursor
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "brief replaced_by scheduled_task.task_configuration; the decode role is consolidated into Q2 (regsvr32 command) and Q3 (child command). See the multi-child consolidation note below."
    - template: obfuscation.encoded_payload_decode
      reason: "brief hint 'powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode' — obfuscation set not active; powershell.encoded_command_decode is included as Q8 for the PowerShell-child case."
    - template: powershell.script_block_logging
      reason: "Multi-child consolidation: applies only when the child is powershell/pwsh. The encoded-command decode (Q8) covers the PowerShell-child case; adding the 4104 question as a permanent slot would be dead weight for the 12 non-PowerShell children. Analysts pull 4104 for that child from Q8's guidance."
    - template: powershell.powershell_security_bypass
      reason: "Multi-child consolidation: PowerShell-child-only. endpoint_protection_health (Q9) covers AMSI/Defender tampering host-wide; a PowerShell-specific bypass question would be irrelevant to most firings of this multi-child rule."
    - template: discovery_recon.discovery_parent_chain
      reason: "Multi-child consolidation: discovery-child-only and duplicates parent_process_legitimacy (Q2), which already returns the parent/grandparent for the alerting child regardless of type."
    - template: discovery_recon.discovery_user_context
      reason: "Multi-child consolidation: discovery-child-only; overlaps host_user_context (Q4), which covers the user-expectation judgement for any child type."
    - template: registry.registry_key_context
      reason: "Multi-child consolidation: applies only when the child is reg.exe. The child command decode (Q3) explicitly directs the analyst to the registry key/value for a reg.exe child; a permanent registry-only slot would be irrelevant to the other 13 children."
    - template: registry.registry_related_modifications
      reason: "Multi-child consolidation: reg.exe-child-only. Persistence-relevant registry changes are reachable via the persistence sweep (Q11) and Q3's reg.exe guidance."
    - template: scheduled_task.task_configuration
      reason: "Multi-child consolidation: schtasks.exe-child-only. Q3 directs the analyst to the task name/trigger/action for a schtasks child; a permanent scheduled-task slot is dead weight for the other children."
    - template: scheduled_task.task_trigger_and_timing
      reason: "Multi-child consolidation: schtasks.exe-child-only; folded into Q3's schtasks guidance and the persistence sweep (Q11)."
    - template: scheduled_task.task_still_present
      reason: "Multi-child consolidation: schtasks.exe-child-only; remediation-check that applies only to a task-creating firing."
    - template: persistence_scope.file_based_persistence
      reason: "Recommended; process_based_persistence (Q11) covers the more relevant persistence-tool angle for the reg/schtasks children. Startup/Run-key file drops are a secondary path for this proxy-execution rule."
  reflection_overrides: []
  hint_dispositions:
    - hint: "registry.registry_key_context replaces cue_process_tool.mechanism_decode"
      disposition: acted_on
      reason: >-
        cue_process_tool.mechanism_decode is skipped (recorded under
        skipped_templates). registry.registry_key_context applies only to a reg.exe
        child; in this multi-child rule its intent is folded into the child command
        decode (Q3), which explicitly directs the analyst to the registry key/value
        when the child is reg.exe. See the multi-child consolidation note.
    - hint: "scheduled_task.task_configuration replaces cue_process_tool.mechanism_decode"
      disposition: acted_on
      reason: >-
        Same consolidation — scheduled_task.task_configuration applies only to a
        schtasks child; Q3 directs the analyst to the task name/trigger/action for
        that child. cue_process_tool.mechanism_decode is recorded as skipped.
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted_on
      reason: >-
        powershell.encoded_command_decode is included as Q8 (framed conditionally
        for a PowerShell child); obfuscation.encoded_payload_decode is recorded as
        skipped.
    - hint: "parent_process is primary — drop cue_process_tool:process_chain"
      disposition: acted_on
      reason: >-
        cue_process_tool.process_chain is dropped (recorded under dedup_decisions);
        the parent-analysis slot is parent_child.parent_process_legitimacy (Q2).
    - hint: >-
        logsource 'image_load' (recommended by techniques: T1218.010) is not covered — pull in cue_image_load.* or author a custom
        question with `category: image_load`
      disposition: acted_on
      reason: >-
        Authored Q6 (attack:T1218.010.proxy_module, category: image_load) scoped to
        the regsvr32 parent's ProcessGuid and filtered to modules loaded from
        user-writable/temp paths — the proxied payload-DLL / scriptlet-runtime load.
    - hint: >-
        logsource 'network_connection' (recommended by techniques: T1218.010) is not covered — pull in cue_network_endpoint.*
        or author a custom question with `category: network_connection`
      disposition: acted_on
      reason: >-
        Authored Q5 (attack:T1218.010.remote_scriptlet, category: network_connection)
        scoped to the regsvr32 parent's ProcessGuid — regsvr32 fetching a remote
        scriptlet/DLL via /i:<url> is the remote-retrieval stage, so the destination
        is a load-bearing pivot.
  contextual_sets:
    included: []
    excluded: []
  multi_child_consolidation: >-
    This rule fires on regsvr32 spawning any one of 14 children, and the brief
    auto-confirmed the powershell, discovery_recon, registry, scheduled_task, and
    persistence_scope technique sets because their binaries appear in that child
    list. Only one child fires per alert, so including every set's must_include
    questions would yield 25+ questions, most irrelevant to any single firing.
    Instead the child-specific decodes are consolidated into Q3
    (attack:T1218.010.child_command), which names the right analytical task per
    child type (encoded PowerShell, registry key, scheduled-task config, recon
    flags, script/URL), with powershell.encoded_command_decode (Q8) kept as the
    one high-value child-specific decode, discovery_recon.discovery_burst_detection
    (Q10) for the recon children, and persistence_scope.process_based_persistence
    (Q11) for the reg/schtasks children. The dropped per-set must_includes are
    listed under skipped_templates with reasons; nothing was silently truncated.
  technique_sets_applied:
    - universal
    - parent_child
    - powershell
    - discovery_recon
    - persistence_scope
  user_actor_pairs:
    - reason: "user_actor pair — non-deduplicable (baseline): Q14 pivots on user identity, Q15 on launch mechanism (regsvr32+child)"
    - reason: "user_actor pair — non-deduplicable (clustering): Q16 pivots on user identity, Q17 on launch mechanism (regsvr32+child)"
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      image_load: 1
      sigma_alert: 1
