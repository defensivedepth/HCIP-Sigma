name: HackTool - CrackMapExec PowerShell Obfuscation
id: 9a6d15d0-121d-483f-81d4-3edda574034b
description: |
  Detects PowerShell (powershell.exe or pwsh.exe, identified by Image or OriginalFileName) invoked with command-line
  fragments matching a known offensive-framework obfuscation routine that builds strings from indexed environment and
  automatic variables (for example join/split chains, $ShellId and $PSHome character indexing, and $env:ComSpec slicing).
  The rule fires only when both halves match together, a PowerShell interpreter AND one of the obfuscation static strings,
  so neither condition alone triggers it. This string-building obfuscation hides the real payload from casual inspection
  and from naive command-line signatures, and is characteristic of tooling that wraps lateral-movement and post-exploitation
  PowerShell. Legitimate activity rarely produces these exact concatenation patterns, so the investigation should reconstruct
  what the obfuscated command actually does, confirm whether the user and host are part of an authorized assessment, and
  determine whether follow-on execution or defense tampering accompanied it.
type: detection
detection_id: 6f8b3439-a203-45dc-a88b-abf57ea15ccf
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects, with PowerShell running one of the framework obfuscation string-building fragments?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when both conditions hold together, a PowerShell interpreter (powershell.exe or pwsh.exe by Image or OriginalFileName) AND a command line containing one of the obfuscation fragments such as a join/split chain, $ShellId or $PSHome character indexing, $env:Public indexing, or $env:ComSpec slicing. Confirm both halves are present in the actual event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run PowerShell that builds commands from indexed environment and automatic variables?
  context: Legitimate administrative and automation scripts occasionally use string manipulation, but the specific pattern of indexing $ShellId, $PSHome, $env:Public, and $env:ComSpec to reassemble characters is an obfuscation technique rather than ordinary scripting. Knowing whether the user role and host function involve PowerShell development or red-team tooling is often the fastest path to distinguishing an authorized assessment from a live intrusion.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this PowerShell obfuscation part of an authorized penetration test or red team engagement?
  context: The obfuscation routine this rule matches is emitted by an offensive pentesting framework used in authorized security assessments. Before investing investigation time, check whether a pentest or red team exercise is currently active on this host or network segment. An authorized engagement with matching tooling and timing resolves the alert without further analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: Reconstruct the obfuscated PowerShell command. What does the string-building routine resolve to, and what operation does the deobfuscated command perform?
  context: The full command line is the operator's payload hidden behind character indexing and concatenation. Resolve the $ShellId, $PSHome, $env:Public, and $env:ComSpec index expressions and the join/split chains to recover the literal string they assemble, then read the resulting command. The deobfuscated content reveals the actual intent, whether a download cradle, an in-memory loader, credential access, or lateral-movement staging. The query scopes to PowerShell executions on this host that carry these obfuscation fragments, so multi-stage invocations that together form one logical operation appear alongside the triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - 'join*split'
          - '( $ShellId[1]+$ShellId[13]'
          - '( $PSHome['
          - '( $env:Public[13]+$env:Public[5]'
          - '( $env:ComSpec[4,'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Beyond the obfuscation fragments the rule matched, does the command carry encoded or further-obfuscated content, and what does the fully resolved payload do?
  context: Attackers frequently layer obfuscation, so the indexed-variable string building this rule matched may itself reassemble an -EncodedCommand value, a base64 blob, or a download cradle. Resolve every layer until the script is human-readable, then identify the operation, a download cradle (IEX over Invoke-WebRequest or Net.WebClient), an in-memory .NET assembly load via [Reflection.Assembly]::Load, a reverse shell, or credential theft. Note any embedded URLs, IP addresses, file paths, or child binary names the decoded content references.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated script content?
  context: Script block logging records the deobfuscated script content that PowerShell actually executes, even when the command line is built from indexed variables or otherwise obfuscated. This provides ground truth about what ran, bypassing the string-building obfuscation entirely. If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility gap rather than evidence nothing ran, and fall back to manual reconstruction of the obfuscated command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before this execution?
  context: Operators frequently disable or bypass PowerShell security controls before running an obfuscated payload. Common precursors include AMSI bypass (patching amsi.dll in memory), execution-policy changes (Set-ExecutionPolicy or the -ep bypass flag), constrained-language-mode escapes, and PowerShell v2 downgrade attacks that evade script block logging. Finding these precursor events on this host strengthens the case for malicious activity.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:hacking_tool.tool_specific_artifacts
  question: Are file artifacts characteristic of this offensive framework present on the host?
  context: The framework that emits this obfuscation routine leaves characteristic artifacts on hosts it operates from or against, including a local results database and per-run log files written under the operator profile, plus staged scripts and output files in temporary directories. Checking for these artifacts confirms tool execution and reveals the scope of activity even if the PowerShell process has already exited. File creation events from the PowerShell process narrow the search to artifacts this execution wrote.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1059.001.image_load
  question: Did this PowerShell process load .NET or automation assemblies consistent with in-memory or indirect execution?
  context: PowerShell abuse is not limited to direct powershell.exe command lines; the same capability can be driven through .NET automation by loading the System.Management.Automation assembly into another host process. Image-load events on the alerting host around the alert time reveal whether the obfuscated execution loaded the automation runtime or other rarely-seen assemblies, which points to an in-memory or .NET-hosted cradle rather than a simple script invocation. If image-load telemetry is not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_module:
        ImageLoaded|contains:
          - 'System.Management.Automation'
          - 'amsi.dll'
          - 'clr.dll'
          - 'System.Reflection'
      condition: selection and selection_module
    fields:
      - Image
      - ImageLoaded
      - Signed
- source: attack:T1027.005.application
  question: Did host antivirus or endpoint protection raise its own detection for this activity on this host around the alert time?
  context: Indicator-removal tradecraft often shows up as an antivirus or endpoint-protection detection that fires just before or alongside a modified, re-run payload. Correlating native AV application events on this host within the alert window surfaces whether protection already flagged the same tooling, and whether the operator then dropped or re-ran a modified variant to evade it. A vendor detection paired with continued execution is a stronger signal than the obfuscation match alone.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: application
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Channel: Application
      condition: selection
    fields:
      - Provider_Name
      - EventID
      - Message
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: The parent process reveals how PowerShell was invoked. User-driven administration typically descends from explorer.exe or a shell; service automation from services.exe. Parents like winword.exe, mshta.exe, wscript.exe, an archive tool, or a remote-execution service host indicate macro-driven, staged, or lateral-movement execution rather than interactive use. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even common interpreters can be abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific obfuscation firing pattern has historically run here, not generic PowerShell usage. A new parent process, an unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains:
        - join*split
        - ( $ShellId[1]+$ShellId[13]+'x')
        - ( $PSHome[*]+$PSHOME[*]+
        - ( $env:Public[13]+$env:Public[5]+'x')
        - ( $env:ComSpec[4,*,25]-Join'')
        - '[1,3]+''x''-Join'''')'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this obfuscation firing pattern, not generic PowerShell usage. Many hosts firing the same pattern points to scripted deployment or sanctioned security tooling; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \powershell.exe
        - \pwsh.exe
      - OriginalFileName:
        - PowerShell.EXE
        - pwsh.dll
      selection_cli:
        CommandLine|contains:
        - join*split
        - ( $ShellId[1]+$ShellId[13]+'x')
        - ( $PSHome[*]+$PSHOME[*]+
        - ( $env:Public[13]+$env:Public[5]+'x')
        - ( $env:ComSpec[4,*,25]-Join'')
        - '[1,3]+''x''-Join'''')'
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run PowerShell across the environment at all?
  context: Compare this user's current action against their historical behavior. A user who has never run PowerShell is more suspicious than one who does so regularly as part of their role; the aggregation counts every PowerShell execution by this user, not just the obfuscated pattern, so a low total is itself meaningful. User role and typical toolset should align with running an interpreter that reassembles commands from indexed variables.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and PowerShell combination historically routine on this host or across the environment, or is the parent unusual for the interpreter?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned PowerShell, and is that pair historically common. A new or rare parent for PowerShell on this host or environment-wide is the deviation worth chasing; a pair that already accounts for thousands of prior events is vendor or scheduled automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user triggered similar activity on multiple hosts in the same window, the scope of the operation may be larger than a single endpoint. The same-window view is the temporal-correlation pivot, tying this execution to surrounding activity.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and PowerShell combination fire on other hosts in the same window, indicating a coordinated automation rollout rather than isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, parent process plus spawned PowerShell, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation or coordinated lateral movement; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded this execution and help reconstruct the timeline. Look for patterns: tampering followed by execution, or reconnaissance followed by lateral movement.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1027.005
      name: Indicator Removal from Tools
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
    - id: T1027
      name: Obfuscated Files or Information
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: powershell_invocation
      description: PowerShell (powershell.exe/pwsh.exe) is launched and the obfuscation string-building fragments are present in the command line.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: obfuscation_decode
      description: The indexed-variable string-building routine is resolved to recover the literal command and its operation.
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: encoded_layer_unwrap
      description: Any further-layered encoding (EncodedCommand, base64, download cradle) is decoded to readable payload.
      mapped_to: Q5
      source: template:powershell.encoded_command_decode
    - stage: 4
      label: ground_truth_capture
      description: Script block logging captures the deobfuscated script content PowerShell actually executed.
      mapped_to: Q6
      source: template:powershell.script_block_logging
    - stage: 5
      label: assembly_or_module_load
      description: PowerShell or a host process loads .NET automation or AMSI-related assemblies, indicating in-memory/indirect execution.
      mapped_to: Q9
      source: attack:T1059.001.image_load
    - stage: 6
      label: av_evasion_correlation
      description: Native AV/endpoint-protection detection correlates with a modified, re-run payload (indicator removal from tools).
      mapped_to: Q10
      source: attack:T1027.005.application
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q11
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: template:cue_user_actor.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q9
      source: attack:T1059.001.image_load
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q5
      source: template:powershell.encoded_command_decode
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "join*split, ( $ShellId[1]+$ShellId[13]+'x'), ( $PSHome[*]+$PSHOME[*]+, ( $env:Public[13]+$env:Public[5]+'x'), ( $env:ComSpec[4,*,25]-Join''), [1,3]+'x'-Join'')"
      disposition: query_injection
      target_question: Q4
      notes: "mechanism_decode (ADAPT_STRUCTURALLY) tightened with the rule's obfuscation CommandLine|contains fragments for Q-vs-query alignment; the reflected baseline_deviation (Q12) and scope (Q13) queries carry the full verbatim selection."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "winword.exe, mshta.exe, wscript.exe (examples, no fixed list)"
      disposition: context
      target_question: Q11
      notes: "Abnormal-parent rationale framed in process_chain and baseline_deviation context; example parents named in prose, not hard-coded into a query selection."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (off-hours / lateral-movement / non-maintenance windows; 5-30 min AV-to-rerun gap)"
      disposition: context
      target_question: Q16
      notes: "No queryable threshold; user clustering +/-1h and precursor -6h windows carry the temporal-correlation intent."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "System.Management.Automation, amsi.dll, clr.dll, System.Reflection"
      disposition: query_injection
      target_question: Q9
      notes: "Rare/never-before-seen .NET assemblies tied to PowerShell abuse injected into the attack:T1059.001.image_load ImageLoaded|contains selection."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (4104 script-block length filter)"
      disposition: context
      target_question: Q6
      notes: "No numeric threshold supplied; script_block_logging question captures the deobfuscated content qualitatively."
    - element: AVAlertMessage
      dimension: tuning_parameter
      specific_values: "rationale only (vendor-specific signature/detection message)"
      disposition: context
      target_question: Q10
      notes: "No fixed vendor string in brief; attack:T1027.005.application surfaces native AV application events for the analyst to read the Message field."
    - element: FilenameSimilarityThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (string/hash similarity between original and modified binary)"
      disposition: context
      target_question: Q10
      notes: "No queryable similarity value; the AV-correlation question carries the modified-rerun rationale."
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (entropy threshold for encoded/compressed payloads)"
      disposition: context
      target_question: Q5
      notes: "No numeric threshold; encoded_command_decode question covers obfuscation analysis qualitatively."
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "rationale only (LOLBins / admin tools misused for obfuscation)"
      disposition: context
      target_question: Q11
      notes: "Folded into the process_chain abnormal-parent rationale; no fixed list hard-coded."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: hacking_tool.tool_capability_used
      reason: "for an obfuscation-string rule the invoked capability IS the obfuscated PowerShell payload; fully covered by mechanism_decode (Q4) and encoded_command_decode (Q5). Dropping under 18-question cap to make room for the two logsource-coverage attack questions."
      kept: template:cue_process_tool.mechanism_decode
    - dropped: cue_host_context.clustering
      reason: "host-local clustering redundant with cue_user_actor.clustering same-window cross-host view and cue_host_context.precursor; time_correlation covered by Q16. Dropped under 18-question cap."
      kept: template:cue_user_actor.clustering
  skipped_templates:
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by: powershell.encoded_command_decode — parent is powershell.exe/pwsh.exe so the PowerShell-specific decode question is more specific."
    - template: universal.evidence_preservation
      reason: "rule detection is purely in-memory PowerShell string obfuscation with no file path in the detection block (has_file_reference is a generic logsource flag, not a real path in this rule). tool_specific_artifacts (Q8) covers file-event artifacts the process wrote."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED generic host-tamper sweep; powershell.powershell_security_bypass (Q7) covers AV/AMSI/execution-policy precursors specific to this PowerShell execution and cue_host_context.precursor (Q18) carries prior-alert tamper context. Generic sweep would duplicate Q7."
    - template: cue_process_tool.baseline
      reason: "host-baseline of generic powershell.exe usage is too noisy to be useful (powershell.exe runs constantly); the reflected baseline_deviation (Q12) gives the rule-pattern-specific host history instead."
    - template: cue_process_tool.follow_on
      reason: "follow-on child-process angle is in-memory for this obfuscation rule; the image_load assembly question (Q9) and encoded_command_decode (Q5) cover in-memory/indirect execution, and host clustering covers surrounding processes."
  reflection_overrides: []
  hint_dispositions:
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: declined
      reason: >-
        This rule's detection is in-memory PowerShell string obfuscation with no
        file path in the selection block; there is no command-line-derived file
        path to scope a %file_paths% query against. The file-event angle is
        instead carried by tool_specific_artifacts (Q8), which scopes file
        creation to the alerting PowerShell process via Image|endswith. No
        evidence_preservation question authored (see skipped_templates).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - hacking_tool
    - obfuscation
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 13
      powershell_script_block: 1
      file_event: 1
      image_load: 1
      application: 1
      sigma_alert: 1
