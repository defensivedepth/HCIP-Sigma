name: Console CodePage Lookup Via CHCP
id: b9c7fae1-6c95-4151-9814-c2f3b512a5b0
description: |
  Detects chcp.com spawned by cmd.exe (with an inline command switch such as -c, -r, or -k) where the
  command line ends in a bare chcp invocation. Run without a code-page argument, chcp prints the active
  console code page, which reveals the system's language and locale — a quick host-fingerprinting step.
  Legitimately, installers and applications (for example package managers or chat clients) call chcp to
  set up console encoding, so the same pattern appears in benign automation. The investigation should
  determine what spawned the parent shell, whether the locale lookup sits inside a burst of other
  discovery commands, and whether this user and host normally run this sequence.
type: detection
detection_id: 7090adee-82e2-4269-bd59-80691e7c6338
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did cmd.exe spawn chcp.com with a bare chcp command line and an inline command switch?
  context: Confirm the alert is a true positive by reviewing the exact event against the rule's criteria —
    the parent is cmd.exe with an inline switch (one of -c, -r, or -k), the child Image ends in chcp.com,
    and the child command line ends in chcp with no code-page number. A bare chcp reads the active code
    page rather than setting one, which is the locale-fingerprinting behavior this rule targets.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
      - Image
      - CommandLine
- source: template:parent_child.parent_process_legitimacy
  question: Is cmd.exe expected to spawn chcp.com here, and what started cmd.exe?
  context: The parent-child relationship is the core signal for this detection. Tracing the parent shell's
    own origin reveals whether the chain is legitimate (a user or script opened a command prompt) or
    suspicious (a macro, loader DLL, or download cradle spawned cmd.exe, which then ran the locale lookup
    as its first discovery step).
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same cmd.exe parent spawn?
  context: If a loader or script used this shell to fingerprint the locale, it likely used the same shell
    to run additional discovery or staging. Examining the other children of this exact parent instance
    reveals the full scope of activity orchestrated through it; several discovery binaries from one parent
    points to automated enumeration.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What did every chcp.com execution on this host do, and how was the parent command packaged?
  context: Decode the chcp invocations on this host around the alert and read them against the alert's
    parent command line. A bare chcp (no number) reads the active code page and fingerprints the locale; a
    chcp followed by a code-page number sets the console encoding, the common benign use by installers and
    applications. Note which inline switch (-c, -r, or -k) carried the call and whether chcp was chained
    with other commands (for example redirected with >&2 or followed by more recon) — a switch plus a
    multi-command string is more consistent with scripted discovery. Launch frequency and the time window
    that separate automation from one-off use are not directly queryable; judge them from the command shape.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single locale lookup may be benign encoding setup, but several discovery commands in quick
    succession — whoami, ipconfig, systeminfo, net, nltest — is a signature of post-compromise enumeration.
    Locale fingerprinting frequently opens such a burst, used to decide whether to continue based on the
    host's region or language.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: attack:T1614.001.registry_language_key
  question: Did processes on this host read the system language and locale registry keys around this time?
  context: Beyond running chcp, the same locale-discovery technique can be carried out by reading the
    language registry keys directly (for example HKLM\SYSTEM\CurrentControlSet\Control\Nls\Language and the
    related locale values). Surfacing registry sets against those keys around the alert shows whether the
    locale fingerprint was a one-off command-line lookup or part of broader programmatic enumeration. If
    registry-set events for these keys are not ingested in this environment, treat zero results as a
    visibility gap rather than evidence nothing read them.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - '\Control\Nls\Language'
          - '\Control\Nls\Locale'
          - '\International\LocaleName'
          - '\International\sLanguage'
      condition: selection
    fields:
      - Image
      - TargetObject
      - User
- source: template:discovery_recon.discovery_user_context
  question: Is this user expected to run administrative discovery commands?
  context: Administrators and IT staff legitimately query host configuration for troubleshooting, and
    application installers run locale lookups under whatever account launched them. Standard users rarely
    invoke chcp from a shell directly. The user's role and typical behavior determine whether this locale
    lookup is routine or anomalous.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:cue_parent_process.baseline
  question: Does cmd.exe normally spawn chcp.com on this host?
  context: Some parent-child pairings are routine while others are novel. Historical frequency of this
    specific cmd.exe-to-chcp.com pairing on this host distinguishes recurring automation (an installer or
    application that always sets up console encoding) from a first-time spawn worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run chcp.com?
  context: A binary that has rarely or never run on a host is more notable than one that runs regularly.
    Zero or very low historical counts indicate first-time or rare execution of the locale lookup; high
    counts driven by a recurring parent suggest routine application or installer behavior.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run chcp.com or similar discovery commands?
  context: Compare this user's current action against their historical pattern across hosts. A user who has
    never run this tool is more notable than one who does so regularly, and the user's role should align
    with running console or discovery utilities at all.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this cmd.exe-to-chcp.com combination historically routine on this host or across the
    environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism: which parent spawned chcp.com, and whether that pair is
    historically common. A new or rare parent for this binary is the deviation worth chasing; a pair that
    already accounts for thousands of prior events is application or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether the same actor triggered similar
    locale or discovery activity on multiple endpoints, which would widen the apparent scope. A single host
    is more consistent with localized benign automation.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same cmd.exe-to-chcp.com combination fire on other hosts in the same window, or is the
    activity isolated to this host?
  context: Under a service principal, asking what else the user did network-wide returns agent-telemetry
    noise because every host runs SYSTEM-owned agents constantly. The meaningful question is whether the
    same launch mechanism — parent process plus spawned binary — fired on other hosts in the alert window.
    Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the locale lookup reveals the
    immediate context: what led to it, what happened after, and whether it sits inside a larger sequence.
    Look for reconnaissance, credential access, or persistence in the surrounding window.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Locale fingerprinting often opens a post-compromise sequence. Earlier detections on the same
    host — execution, reconnaissance, or credential access — may reveal the stages that preceded this
    lookup and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1614.001
      name: System Language Discovery
      role: primary
    - id: T1614
      name: System Location Discovery
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: locale_fingerprint_execution
      description: A process invokes a locale/code-page lookup (chcp) to read the system language as a
        host-fingerprinting step.
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: locale_registry_or_api_read
      description: The same technique can read language/locale registry keys directly instead of (or in
        addition to) running chcp.
      mapped_to: Q6
      source: attack:T1614.001.registry_language_key
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1614.001
      covered_by: Q2
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1614.001
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: partial
    - dimension: user_context
      technique: T1614.001
      covered_by: Q7
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
    - dimension: time_correlation
      technique: T1614
      covered_by: Q5
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessAllowList
      dimension: parent_process_analysis
      specific_values: rationale only
      disposition: context
      target_question: Q2
      notes: No enumerated allow-list values in the brief; parent legitimacy is investigated by tracing
        cmd.exe's origin and baselining the parent-child pair.
    - element: QueryThreshold
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q4
      notes: Frequency threshold is a tuning rationale with no concrete value; surfaced as command-shape
        and frequency judgment in mechanism_decode context.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: rationale only
      disposition: context
      target_question: Q5
      notes: Correlation-window rationale (T1614 example 2m); realized as the +/-15m discovery-burst window
        rather than a queryable field.
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: parent_process is primary cue — parent-chain analysis is carried by
        parent_child.parent_process_legitimacy; hint instructs dropping this template.
      kept: parent_child.parent_process_legitimacy
    - dropped: discovery_recon.discovery_parent_chain
      reason: ProcessGuid-scoped parent lookup duplicates parent_child.parent_process_legitimacy, which
        uses ParentProcessGuid and traces the grandparent as well.
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: parent-name-scoped child enumeration is subsumed by parent_child.sibling_processes, which
        scopes to the exact ParentProcessGuid instance.
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.mechanism_decode
      reason: ParentProcessGuid lookup of the parent shell duplicates parent_child.parent_process_legitimacy;
        the parent-command decode angle (which inline switch, chaining) is folded into the
        cue_process_tool.mechanism_decode question's context instead.
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.clustering
      reason: parent-name-scoped same-window activity overlaps cue_host_context.clustering (host-wide) and
        parent_child.sibling_processes (exact parent instance); both already cover the surrounding window.
      kept: cue_host_context.clustering
    - dropped: cue_parent_process.scope
      reason: enterprise-wide parent-child spread is covered by cue_user_actor.clustering_svc, which
        searches the same parent-name + binary pair across hosts in the alert window.
      kept: cue_user_actor.clustering_svc
    - dropped: universal.host_user_context
      reason: discovery_recon.discovery_user_context is more specific to the discovery context and the
        template notes instruct preferring it over the generic version.
      kept: discovery_recon.discovery_user_context
    - dropped: user_actor_pair
      reason: user_actor pair — non-deduplicable (cue_user_actor.baseline + baseline_svc, and clustering +
        clustering_svc emitted as both variants per service-principal handling).
      kept: cue_user_actor.baseline/baseline_svc/clustering/clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: RECOMMENDED (not must_include). This is a discovery rule, not an execution / credential-access
        / impact rule, so tamper-as-precursor is not the live disposition question. cue_host_context.precursor
        is present and carries prior-alert tamper context for this host. Dropped per the template's own
        drop-when-precursor-present-and-not-execution-centric guidance.
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1614.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: Acted on, not declined — authored Q7 (attack:T1614.001.registry_language_key) with
        category registry_set, targeting the Nls\Language / Locale / LocaleName / sLanguage keys the
        T1614.001 analytic calls out. Recorded here for the audit trail.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: Keyword match on 'ransomware' comes from a reference incident, not the rule's behavior.
          The rule detects a single discovery primitive (locale lookup); no encryption, shadow-copy, or
          impact behavior is in scope.
      - id: network_scope
        confidence: low
        reason: Same incidental 'ransomware' keyword origin. The rule has no network indicators; lateral
          movement and network scoping are not part of what this detection fires on.
  technique_sets_applied:
    - universal
    - discovery_recon
    - parent_child
  generation_stats:
    total_questions: 15
    source_counts:
      template: 14
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      registry_set: 1
      sigma_alert: 1
