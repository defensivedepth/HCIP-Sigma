name: Lolbin Unregmp2.exe Use As Proxy
id: 1de50764-de4b-4bf1-a2bc-08297c7da71a
description: |
  Detects the signed Windows Media Player setup utility unregmp2.exe (matched by Image or OriginalFileName) invoked
  with the ' /HideWMP' argument. Both the binary identity and the command-line flag must match together for the rule to
  fire. This invocation is the trigger for a known proxy-execution pattern: an operator copies a payload to a controlled
  directory named like a Media Player path, sets the ProgramW6432 environment variable to that directory, then runs
  unregmp2.exe /HideWMP, which spawns wmpnscfg.exe from the hijacked path rather than the system location. The result is
  attacker code running under the lineage of a trusted signed binary. Legitimate Media Player setup occasionally runs this
  utility, so the investigation should confirm the flag and binary identity, decode the surrounding command context,
  identify the child process and the path it launched from, and determine whether the spawned binary is the expected
  system wmpnscfg.exe or a relocated payload.
type: detection
detection_id: 727454c0-d851-48b0-8b89-385611ab0704
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting event match this rule, with unregmp2.exe (by Image or OriginalFileName) run together with the ' /HideWMP' argument?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when both halves match together, the binary is unregmp2.exe identified by Image ending in \unregmp2.exe or by OriginalFileName, AND the command line contains ' /HideWMP'. Confirm both are present in the actual event and note the full command line for later decoding.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run the Windows Media Player setup utility?
  context: Legitimate Media Player setup and servicing occasionally runs unregmp2.exe, but the /HideWMP proxy pattern is uncommon on general-purpose endpoints and servers. Knowing whether the user role and host function involve Media Player configuration is often the fastest path to distinguishing routine servicing from a proxy-execution attempt.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: Decode the full unregmp2.exe command line and surrounding context. What path is it running from, and is there evidence of a hijacked ProgramW6432 environment variable?
  context: The /HideWMP invocation causes unregmp2.exe to spawn wmpnscfg.exe from the directory named by the ProgramW6432 environment variable. An operator abusing this copies a payload into a controlled 'Windows Media Player' directory and points ProgramW6432 there before launching. Read the working directory and surrounding command context to see whether the launch path is a system location or a user-writable staging directory.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains|windash: ' /HideWMP'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned unregmp2.exe, and is that parent chain expected?
  context: The parent process reveals how the utility was invoked. Legitimate Media Player servicing usually descends from explorer.exe, a setup host, or services.exe. A parent such as cmd.exe with a /V /C ProgramW6432 assignment, a script host, or an Office application points to scripted proxy execution rather than routine servicing. Walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.proxy_child_spawn
  question: What child processes did unregmp2.exe spawn, and did wmpnscfg.exe launch from a system path or a user-writable staging directory?
  context: The proxy mechanism completes when unregmp2.exe spawns a child from the path named by ProgramW6432. The expected system child is %SystemRoot%\System32\wmpnscfg.exe or its SysWOW64 counterpart. A child named wmpnscfg.exe launching from a temp, profile, or other user-writable directory is the hijacked payload, and any other spawned child binary is itself the proxied code. Review each child Image, its launch path, and its command line for follow-on staging.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_retrieval
  question: Did the alerting host make outbound network connections from the spawned child binary or from unregmp2.exe around the time of the alert?
  context: Proxy execution under a trusted signed binary is often used to retrieve or beacon to externally hosted content while the parent lineage looks benign. Outbound connections from wmpnscfg.exe, from unregmp2.exe, or from a child binary launched at the hijacked path point to download or command-and-control activity rather than local Media Player servicing. Excluding well-known system endpoints, review the destinations and whether they resolve to enterprise infrastructure.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      filter_system:
        Image|endswith:
          - \svchost.exe
          - \System
      condition: selection and not filter_system
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run unregmp2.exe?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. Zero or very low historical counts indicate first-time or rare execution, which on a host that does not routinely service Media Player raises the priority of the proxy-execution finding.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit that pattern?
  context: Even a signed system utility can be abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific unregmp2.exe /HideWMP firing pattern has historically run here, not generic Media Player activity. A new parent process, an unexpected user account, or an unusual command structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \unregmp2.exe
      - OriginalFileName: unregmp2.exe
      selection_cmd:
        CommandLine|contains|windash: ' /HideWMP'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \unregmp2.exe
      - OriginalFileName: unregmp2.exe
      selection_cmd:
        CommandLine|contains|windash: ' /HideWMP'
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \unregmp2.exe
      - OriginalFileName: unregmp2.exe
      selection_cmd:
        CommandLine|contains|windash: ' /HideWMP'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \unregmp2.exe
      - OriginalFileName: unregmp2.exe
      selection_cmd:
        CommandLine|contains|windash: ' /HideWMP'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, file staging into a controlled directory, environment-variable manipulation, or persistence installation.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this unregmp2.exe /HideWMP firing pattern, not generic Media Player usage. Many hosts firing the same pattern points to scripted deployment or vendor servicing automation, the kind of activity an allowlist of expected parents and destinations would normally suppress; one host or a small unrelated set points to manual operator activity. Use host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \unregmp2.exe
      - OriginalFileName: unregmp2.exe
      selection_cmd:
        CommandLine|contains|windash: ' /HideWMP'
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, defense tampering, reconnaissance, or credential access, may reveal the stages that preceded this proxy-execution event and help reconstruct the timeline. Look for patterns: file staging or environment manipulation followed by execution, or tampering followed by payload launch.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution, and proxy execution under a trusted binary is one way operators run that payload while evading scrutiny. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful protection-looks-healthy signal; kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: trusted_binary_invocation
      description: The signed utility unregmp2.exe is invoked with the ' /HideWMP' proxy argument.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: proxy_mechanism_decode
      description: unregmp2.exe /HideWMP resolves ProgramW6432 and prepares to spawn wmpnscfg.exe from that path; decode the launch path and environment context.
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: proxied_child_execution
      description: A child (wmpnscfg.exe or other binary) is spawned from the hijacked path, running attacker code under the trusted lineage.
      mapped_to: Q5
      source: attack:T1218.proxy_child_spawn
    - stage: 4
      label: payload_network_activity
      description: The proxied binary retrieves externally hosted content or beacons outbound.
      mapped_to: Q6
      source: attack:T1218.network_retrieval
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q14
      source: template:cue_process_tool.scope
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profile unexpected parent-child relationships)"
      disposition: context
      target_question: Q4
      notes: "No fixed parent list in brief; process_chain question names the cmd.exe /V /C ProgramW6432 launch shape in prose and the baseline_svc question profiles parent rarity."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (allowlist of expected signed binaries)"
      disposition: context
      target_question: Q14
      notes: "Tuning allowlist with no queryable value; the rule already constrains the signed binary to unregmp2.exe, so the fleet-scope question frames the expected-automation allowlist angle in prose."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "/HideWMP (the rule's command-line trigger)"
      disposition: query_injection
      target_question: Q3
      notes: "mechanism_decode (ADAPT_STRUCTURALLY) tightened with CommandLine|contains|windash ' /HideWMP' for Q-vs-query alignment; same flag the rule fires on."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q6
      notes: "No queryable allowlist value; network_retrieval question excludes known-good system processes and asks the analyst to judge whether destinations resolve to enterprise infrastructure."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.follow_on
      reason: "follow-on child-process angle reauthored as attack:T1218.proxy_child_spawn (the proxied wmpnscfg.exe child is the core mechanism) — same host+ParentImage query, technique-sourced"
      kept: attack:T1218.proxy_child_spawn
    - dropped: cue_process_tool.clustering
      reason: "host-clustering coverage provided by cue_host_context.clustering (same +/-15m host-scoped Image aggregation); process_tool variant redundant"
      kept: cue_host_context.clustering
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The unregmp2.exe /HideWMP abuse documented for this rule is a binary
        proxy-spawn, not DLL proxying: unregmp2.exe resolves the ProgramW6432
        environment variable and spawns wmpnscfg.exe from the hijacked path. No
        attacker DLL is loaded into the signed process, so an image_load query on
        unregmp2.exe returns ordinary module-load volume without surfacing the
        proxy mechanism. Q5 (attack:T1218.proxy_child_spawn) covers the actual
        proxy via the spawned child's Image and launch path in process_creation,
        which is where this technique's evidence lives. No compensating
        attack:T1218.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      network_connection: 1
      sigma_alert: 1
