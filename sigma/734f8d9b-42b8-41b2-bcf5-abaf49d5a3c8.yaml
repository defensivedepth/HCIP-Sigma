name: Remote PowerShell Session Host Process (WinRM)
id: 689f121a-031b-4c63-9d80-df5b08f4994d
description: |
  Detects an active PowerShell remoting session over WinRM by flagging the host process wsmprovhost.exe when it appears
  either as the process that started OR as the parent of another process. When wsmprovhost.exe is the process itself, the
  WinRM service has initialized a remote PowerShell session on this host; when it is the parent, that remote session is
  executing commands as child processes. Remote PowerShell is routine for management tooling (Exchange, configuration
  management, jump-box administration), so the investigation should determine the originating host and account, whether
  that source is an approved administrative path, and what commands ran inside the session.
type: detection
detection_id: 734f8d9b-42b8-41b2-bcf5-abaf49d5a3c8
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does this event show wsmprovhost.exe as either the running process or the parent of a child process?
  context: >-
    The rule fires on either of two roles for wsmprovhost.exe (the WinRM remote-session host): the process itself starting,
    or wsmprovhost.exe spawning a child. Confirm which role fired here. If wsmprovhost.exe is the process, the WinRM service
    initialized a remote PowerShell session on this host; if it is the parent, the remote session is running a command as a
    child process. Which branch fired determines where the rest of the investigation looks: the parent-role fire makes the
    child command line and the spawned binary the load-bearing evidence, while the process-role fire points to the source
    host and account that opened the session.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
- source: template:universal.host_user_context
  question: Is this user and host expected to be a source or target of remote PowerShell management?
  context: >-
    Remote PowerShell over WinRM is a standard administration channel. Knowing whether this host is a managed endpoint that
    legitimately receives remote sessions, and whether the account is an administrator or service identity authorized to use
    WinRM, is often the fastest way to separate routine management from unexpected remote access.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: What spawned wsmprovhost.exe, or what did wsmprovhost.exe spawn, and is that chain expected?
  context: >-
    The wsmprovhost.exe host is normally started by the WinRM service (svchost.exe hosting the WinRM service). If
    wsmprovhost.exe was started by an unexpected parent, the chain is suspicious. When wsmprovhost.exe is the parent, the
    child it launched is the command executed inside the remote session; an abnormal child (command interpreters, script
    hosts, or reconnaissance binaries) reveals what the remote operator ran.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:powershell.encoded_command_decode
  question: Was any command run in this session encoded or obfuscated, and what does it decode to?
  context: >-
    Commands executed inside a remote PowerShell session can carry the -EncodedCommand (-enc) parameter, string
    concatenation, backtick escaping, or variable substitution to hide intent. Base64-encoded commands are the most common
    form. Decoding reveals the actual payload such as download cradles, reverse shells, or in-memory execution of .NET
    assemblies. Inspect the alerting event and its child commands for these patterns.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full content of what ran in this remote session?
  context: >-
    Windows Event ID 4104 (script block logging) records the deobfuscated script content PowerShell actually executes, even
    when the command line is encoded or obfuscated. For a remote session this is the ground truth of what the operator ran
    on this host. If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility gap
    rather than evidence nothing executed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before the session ran?
  context: >-
    Operators often disable or bypass PowerShell security controls before running a payload. Common techniques include AMSI
    bypass (patching amsi.dll in memory), execution policy changes (Set-ExecutionPolicy or -ep bypass), constrained language
    mode escape, and downgrade attacks (powershell.exe v2, which lacks logging). Finding these precursor events on the host
    strengthens the case that the remote session activity is malicious.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did this wsmprovhost.exe session spawn?
  context: >-
    A remote PowerShell session that runs more than one command will spawn several children from the same wsmprovhost.exe
    instance. Examining all children of that exact parent reveals the full set of actions the remote operator took on this
    host. Multiple distinct tools launched from one session points to hands-on activity rather than a single routine task.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Does this host normally see wsmprovhost.exe in this parent-child relationship?
  context: >-
    A host that regularly receives remote PowerShell management will show recurring wsmprovhost.exe activity in this
    spawning pattern; a host that has never hosted a remote session before makes this a novel event. Zero or very low
    historical counts indicate first-time remote-session activity worth scrutiny.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1021.006.remote_logon
  question: What account authenticated over WinRM to open this session, and from which source host?
  context: >-
    A WinRM remote session is preceded by a network logon. Windows Security events 4624 (logon) and 4648 (explicit
    credential logon) recorded around the alert reveal the account and source address that opened the session. Network and
    remote-interactive logon types from an unexpected source, or a service or non-administrator account, indicate the remote
    access may not be sanctioned. If Security logs are not ingested in this environment, treat zero results as a visibility
    gap.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID:
          - 4624
          - 4648
      condition: selection
    fields:
      - TargetUserName
      - IpAddress
      - LogonType
      - WorkstationName
- source: template:lateral_movement.source_host_identification
  question: What inbound connections reached the WinRM listener on this host around the alert?
  context: >-
    WinRM listens on 5985 (HTTP) and 5986 (HTTPS) by default. Inbound connections to these ports identify the originating
    host of the remote session. Comparing that source against the known administrative jump boxes determines whether the
    session came from an approved management path or from a compromised or unexpected workstation acting as a pivot.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationPort:
          - 5985
          - 5986
      condition: selection
    fields:
      - SourceIp
      - DestinationPort
- source: template:cue_user_actor.baseline
  question: Does this user normally use remote PowerShell management?
  context: >-
    Compare this account's current activity against its historical pattern. An account that has never opened or hosted a
    remote PowerShell session before is more notable than one that does so routinely as part of an administrative role. User
    role and typical toolset should align with WinRM remote management.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this wsmprovhost.exe parent-binary combination historically routine on this host or across the environment?
  context: >-
    When the alert fires under a service or computer account, user identity does not attribute the behavior. The meaningful
    pivot is the launch mechanism: which parent spawned this binary, and whether that pairing is historically common. A new
    or rare parent for this binary on this host or environment-wide is the deviation worth chasing; a pairing that already
    accounts for thousands of prior events is established automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the account's activity across all hosts reveals whether the remote session is isolated or part of movement
    across multiple endpoints. The same account triggering remote PowerShell or related activity on several hosts in a short
    window suggests the scope of access is larger than this one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-binary combination fire on other hosts in the same window?
  context: >-
    Under a service or computer account, network-wide user activity returns agent-telemetry noise. The meaningful question
    is whether the same launch mechanism (parent process plus spawned binary) appeared on other hosts in the alert window.
    Multiple hosts in the same window point to scheduled or pushed automation, while a single host points to a host-local
    trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:lateral_movement.lateral_movement_scope
  question: Has this user account run lateral movement tooling against other hosts recently?
  context: >-
    Operators rarely stop at one target. If the same account is running lateral movement utilities across multiple systems,
    the scope of compromise is larger. Finding remote-execution and remote-desktop tools used by this account against many
    hosts in a short window indicates active movement across the environment rather than a single management action.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      selection_lateral:
        Image|endswith:
          - '\psexec.exe'
          - '\psexec64.exe'
          - '\mstsc.exe'
          - '\ssh.exe'
          - '\plink.exe'
          - '\wmic.exe'
      condition: selection and selection_lateral
    fields:
      - host.name
      - Image
- source: attack:T1059.001.image_load
  question: What .NET and scripting assemblies were loaded into wsmprovhost.exe during this session?
  context: >-
    A remote PowerShell session host loads the .NET and PowerShell automation assemblies it runs. In-memory tradecraft
    surfaces here as rare or never-before-seen modules loaded into wsmprovhost.exe, which process command-line evidence
    alone does not show. Reviewing the loaded-module list distinguishes a baseline session from one reflectively loading
    additional assemblies. If image-load telemetry is not ingested in this environment, treat zero results as a visibility
    gap.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\wsmprovhost.exe'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host such as defense tampering, reconnaissance, or
    credential access may reveal the stages that preceded the remote session and help reconstruct the timeline. The sequence
    of alerts on the host tells the broader story of the activity.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_parent_process.scope
  question: Is this wsmprovhost.exe spawning pattern occurring on other hosts?
  context: >-
    The same remote-session spawning relationship appearing on multiple hosts may indicate an operator moving laterally
    across the environment, while a single occurrence is more likely a targeted or routine management action. Environment-wide
    comparison helps calibrate the scope of the activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1021.006
      name: Windows Remote Management
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: remote_session_init
      description: WinRM service initializes a remote PowerShell session host (wsmprovhost.exe) on the target
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: remote_logon
      description: Account authenticates over WinRM from a source host to open the session
      mapped_to: Q9
      source: attack:T1021.006.remote_logon
    - stage: 3
      label: session_command_execution
      description: The remote session executes commands, often encoded PowerShell, as child processes of wsmprovhost.exe
      mapped_to: Q4
      source: template:powershell.encoded_command_decode
    - stage: 4
      label: child_process_spawning
      description: wsmprovhost.exe spawns child processes carrying out the operator's actions on the host
      mapped_to: Q7
      source: template:parent_child.sibling_processes
    - stage: 5
      label: lateral_scope
      description: Source host or account moves to additional hosts in the environment
      mapped_to: Q15
      source: template:lateral_movement.lateral_movement_scope
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1021.006
      covered_by: Q17
      source: template:cue_host_context.precursor
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q16
      source: attack:T1059.001.image_load
      coverage_type: direct
    - dimension: user_context
      technique: T1021.006
      covered_by: Q9
      source: attack:T1021.006.remote_logon
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop, -ExecutionPolicy Bypass, -ep bypass, AmsiUtils, -Version 2"
      disposition: query_injection
      target_question: Q6
      notes: "Encoded/bypass patterns injected into powershell_security_bypass CommandLine|contains; decode covered by Q4"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (abnormal parents like Excel, WinWord, mshta)"
      disposition: context
      target_question: Q3
      notes: "wsmprovhost is normally spawned by the WinRM svchost; abnormal-parent rationale carried in Q3 context"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (off-hours / lateral-movement window; ~60s shell-to-child)"
      disposition: context
      target_question: Q17
      notes: "Temporal correlation carried by precursor (-6h) and session clustering windows"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only (rare/never-before-seen .NET assemblies)"
      disposition: new_question
      target_question: Q16
      notes: "Drove the custom image_load question on wsmprovhost.exe"
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (Event 4104 script block length)"
      disposition: context
      target_question: Q5
      notes: "4104 ground-truth capture covered by script_block_logging; threshold is a tuning rationale, not queryable"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (non-admin / service-account remote logons)"
      disposition: context
      target_question: Q9
      notes: "Remote-logon account scrutiny carried in Q9 (4624/4648) and user baseline Q11"
    - element: CommandLineAnomalyScore
      dimension: tuning_parameter
      specific_values: "rationale only (suspicious command scoring via WinRM)"
      disposition: context
      target_question: Q4
      notes: "Anomalous command analysis covered by encoded_command_decode"
    - element: KnownAdminHosts
      dimension: tuning_parameter
      specific_values: "rationale only (trusted WinRM source allowlist)"
      disposition: context
      target_question: Q10
      notes: "Source-host vetting against known admin jump boxes carried in source_host_identification context"
  dedup_decisions:
    - dropped: cue_parent_process.mechanism_decode
      reason: "parent_child technique set active — parent_process_legitimacy is more specific"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "within-phase duplicate of parent_child.sibling_processes (children of same parent); technique-set version preferred and ParentProcessGuid-scoped"
      kept: parent_child.sibling_processes
    - dropped: cue_process_tool.mechanism_decode
      reason: "wsmprovhost.exe command line is minimal; command decode covered by encoded_command_decode and the child commands surfaced by parent_process_legitimacy/sibling_processes"
      kept: powershell.encoded_command_decode
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; emitted both (user-pivot Q11 + service-principal substitute Q12)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; emitted both (user-pivot Q13 + service-principal substitute Q14)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain (parent cue subsumes it)"
    - template: cue_process_tool.baseline
      reason: "host baseline of wsmprovhost.exe covered by cue_parent_process.baseline (Q8) in the rule-relevant parent-child framing"
    - template: cue_parent_process.clustering
      reason: "around-the-alert host activity and session children covered by sibling_processes (Q7) and precursor (Q17); slot budget"
    - template: cue_host_context.clustering
      reason: "around-the-alert host activity covered by sibling_processes (Q7) and precursor (Q17); slot budget"
    - template: universal.endpoint_protection_health
      reason: "tamper-as-precursor covered by powershell_security_bypass (Q6), which surfaces AV/AMSI/policy weakening on this host within -1h; generic host-tamper sweep would duplicate that signal"
    - template: lateral_movement.authentication_method
      reason: "authentication detail covered by the custom 4624/4648 remote-logon question (Q9), which queries the Security log the template only referenced in prose"
    - template: lateral_movement.target_activity_after_access
      reason: "post-access child activity on the target covered by sibling_processes (Q7, children of the wsmprovhost session)"
    - template: network_scope.same_action_other_hosts
      reason: "fleet-wide wsmprovhost spawning scope covered by cue_parent_process.scope (Q18)"
    - template: network_scope.actor_activity_network_wide
      reason: "network-wide user activity covered by cue_user_actor.clustering (Q13)"
    - template: network_scope.actor_authentication_trail
      reason: "user authentication trail covered by the custom 4624/4648 remote-logon question (Q9)"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - lateral_movement
    - parent_child
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 14
      powershell: 1
      security: 1
      network_connection: 1
      image_load: 1
