name: PUA - Nimgrab Execution
id: 0787f557-633d-4fc2-8a1b-f804b163ac5d
description: |
  Detects execution of nimgrab.exe, a file-download utility bundled with the Nim programming framework, identified
  either by an Image path ending in nimgrab.exe OR by a process whose Hashes match the known MD5, SHA256, or IMPHASH
  values for the tool. The rule fires when at least one of those two branches matches, so a renamed or relocated copy
  is still caught by the hash branch even when the file name differs. Nimgrab pulls files from a remote URL over
  HTTP(S), which makes it a convenient ingress-transfer tool for retrieving additional payloads onto a host; legitimate
  use is limited to Nim developers building or testing software. Investigation should confirm what was downloaded and
  from where, whether the file landed and ran, and whether the user, host, and parent chain are consistent with
  legitimate Nim development rather than operator-driven tool staging.
type: detection
detection_id: 74a12f18-505c-4114-8d0b-8448dd5485c6
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did this process fire on the nimgrab.exe file name, on the known nimgrab hash set, or both?
  context: The rule fires when at least one branch matches, either an Image path ending in nimgrab.exe OR a process whose MD5, SHA256, or IMPHASH equals the known nimgrab values. Confirm which branch fired by reading the Image path and the Hashes field. If only the hash branch matched, the binary has been renamed or relocated and the file name will not say nimgrab, which makes the signature and origin questions load-bearing. If only the name matched, treat an unexpected hash as a sign the file was replaced.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - Hashes
- source: template:universal.host_user_context
  question: Is this user and host expected to run a Nim file-download utility?
  context: Legitimate nimgrab use is limited to developers building or testing Nim software. Knowing whether the user role and host function involve Nim development is often the fastest path to separating a routine developer action from operator-driven tool staging on a general-purpose endpoint or server.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did nimgrab download? Read the full command line for the source URL and the destination path.
  context: The full command line carries the download target and where the file was written. Nimgrab takes a remote URL and a local destination, so reading the arguments reveals what was fetched and where it landed. A URL pointing at unfamiliar infrastructure, a raw IP, or a destination in a user-writable staging directory shifts the disposition toward tool delivery rather than development.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned nimgrab, and is that parent chain expected?
  context: The parent process reveals how nimgrab was invoked. Developer use typically descends from explorer.exe, a shell, or a build tool such as nim.exe or a terminal. Parents like mshta.exe, wscript.exe, powershell.exe, or an Office application point to scripted or macro-driven staging rather than interactive development. Tune out known-good updaters, then walk up the tree to see what spawned the parent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:remote_access_tool.connection_destination
  question: What external endpoint did nimgrab connect to, and is the destination internal or external infrastructure?
  context: Nimgrab establishes an outbound HTTP(S) connection to retrieve the requested file. The destination reveals whether the transfer targeted a recognized internal mirror or update host versus an external, unfamiliar, or raw-IP endpoint. Filter by internal against external address blocks to separate routine internal fetches from external tool retrieval, which is the higher-priority case for ingress transfer.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: attack:T1105.file_path_analysis
  question: Where did the downloaded file land on disk, and is the drop path an uncommon staging location?
  context: Ingress transfer tools commonly write into user-writable staging directories rather than managed install paths. A file dropped into a world-writable or per-user profile location, for example a public profile directory or a temp folder, is a stronger signal of tool staging than a write into a managed application directory. Identify the destination written by nimgrab and judge whether the path is a routine location for this host.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.download_success_verification
  question: Was the download successful? Check for file creation at the destination path.
  context: A nimgrab invocation does not guarantee the file was retrieved. Network errors, blocked URLs, or security controls may have stopped the transfer. Confirming file creation at the target path establishes whether the activity progressed past the delivery stage into a payload sitting on disk.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded payload from a safe environment?
  context: If the downloaded file is still on disk, submitting its hash to threat intelligence services or detonating it in a sandbox reveals whether it is a known malicious payload, a legitimate tool, or something novel that requires deeper analysis. The file path and hash from the creation event drive the lookup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:universal.evidence_preservation
  question: Is the file referenced by the nimgrab command line still on disk for analysis?
  context: If the nimgrab command line names a download destination, checking whether that file still exists on disk enables deeper analysis, hash lookups, sandbox submission, or content review. The file path here is derived from the command line itself.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the download target directory?
  context: Operators often download multiple files to the same staging directory, or the first file triggers further writes through extraction, decompression, or chained downloads. Examining directory-level file activity reveals the broader scope of the delivery rather than the single fetched file.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_dirs%'
      filter_self:
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection and not filter_self
    fields:
      - TargetFilename
      - Image
- source: template:remote_access_tool.tool_authorization
  question: Is nimgrab on the organization's approved software list for this host?
  context: Many environments scope developer toolchains such as Nim to specific build machines or developer workstations. Nimgrab running outside that approved footprint may represent shadow IT or operator-deployed tooling, regardless of the binary's benign reputation. An unauthorized presence on a general-purpose host should be checked even when the file is the genuine Nim utility.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a change ticket, build job, or developer activity that explains this nimgrab run?
  context: Legitimate nimgrab usage usually correlates with a documented build, a developer working on a Nim project, or a scheduled software task. If no such context exists for this execution, the tool may have been staged by an operator to pull additional payloads or by a user circumventing software controls.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run nimgrab?
  context: A download utility that has never been seen on a host is more suspicious than one that runs regularly. Zero or very low historical counts indicate first-time or rare execution, which on a non-developer endpoint raises the priority of the download destination and payload.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive that activity, and does the current alert fit?
  context: Even a legitimate developer tool can be abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific nimgrab firing pattern, by name or by hash, has historically run here rather than generic binary usage. A new parent process, an unexpected user account, or an unusual command shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_name:
        Image|endswith: \nimgrab.exe
      selection_hashes:
        Hashes|contains:
        - MD5=2DD44C3C29D667F5C0EF5F9D7C7FFB8B
        - SHA256=F266609E91985F0FE3E31C5E8FAEEEC4FFA5E0322D8B6F15FE69F4C5165B9559
        - IMPHASH=C07FDDD21D123EA9B3A08EEF44AAAC45
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run nimgrab across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never run nimgrab is more suspicious than one who does so regularly as part of Nim development work. The user's role and typical toolset should align with running a command-line file-download utility.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and nimgrab combination historically routine on this host or across the environment, or is the parent unusual for the binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned nimgrab, and is that pair historically common. A new or rare parent for nimgrab on this host or environment-wide is the deviation worth chasing; a pair that already accounts for thousands of prior events is vendor or scheduled automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity across multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and nimgrab combination fire on other hosts in the same window, indicating a coordinated automation rollout rather than isolated activity?
  context: Under a service principal, asking what else this user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism, parent process plus spawned nimgrab, fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1105
      name: Ingress Tool Transfer
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: tool_invocation
      description: An uncommon download utility (nimgrab) is invoked on the host.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: network_transfer
      description: The tool initiates an outbound network connection to an external destination to fetch a file.
      mapped_to: Q5
      source: template:remote_access_tool.connection_destination
    - stage: 3
      label: file_creation
      description: The downloaded tool/file is written to disk, often in an uncommon staging path.
      mapped_to: Q6
      source: attack:T1105.file_path_analysis
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1105
      covered_by: Q6
      source: attack:T1105.file_path_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1105
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1105
      covered_by: Q5
      source: template:remote_access_tool.connection_destination
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "known-good updaters (e.g., ChromeUpdate, OneDrive) — rationale only, no fixed list"
      disposition: context
      target_question: Q4
      notes: "Parent-lineage tuning framed in process_chain context (tune out known-good updaters); no fixed parent list to inject into a query selection."
    - element: DestinationIPCategory
      dimension: tuning_parameter
      specific_values: "internal vs external IP blocks — rationale only, no fixed values"
      disposition: context
      target_question: Q5
      notes: "Internal/external filtering framed in connection_destination context; DestinationIp/DestinationHostname fields surface the category for the analyst to judge. No fixed CIDR values supplied."
    - element: FilePathRegex
      dimension: file_path_analysis
      specific_values: "uncommon drop paths (e.g., C:\\Users\\Public\\) — example, no fixed regex"
      disposition: new_question
      target_question: Q6
      notes: "Drove attack:T1105.file_path_analysis; example staging paths named in context. No fixed regex in brief, so path judgment is analyst-driven over the file-creation events by nimgrab."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_process_tool.clustering
      reason: "+/-15m host-scoped Image aggregation overlaps the broader host-context clustering angle; cross-host user clustering (cue_user_actor.clustering) and fleet scope already provide lateral/temporal context. process_tool clustering dropped to stay within the 18-question target."
      kept: cue_user_actor.clustering
    - dropped: cue_process_tool.follow_on
      reason: "T1105 chain is network-transfer to file-creation, not child-process spawning. download_success_verification + payload_analysis + directory_activity cover the post-download file artifacts; a child-process question would add little for a pure download utility."
      kept: download_tool.download_success_verification
  skipped_templates:
    - template: cue_process_tool.scope
      reason: "Cross-host firing-pattern count overlaps cue_user_actor.clustering_svc (parent+binary cross-host in-window) and the host baseline; dropped to keep the playbook at 18 questions. Fleet prevalence is reachable via the user-actor cross-host pivots already present."
    - template: cue_host_context.clustering
      reason: "Host-around-the-time activity overlaps cue_user_actor.clustering; dropped to stay within the 18-question target while preserving the user-pivot lateral view."
    - template: cue_host_context.precursor
      reason: "Prior-alert reconstruction is a generic context question; for a download-utility PUA the disposition turns on the download target and payload, which the mechanism/network/file questions carry. Dropped to stay within the 18-question target."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This rule fires on a benign-by-default download utility, not on payload execution, credential access, or impact; tamper-as-precursor is not the live disposition question here and no other selected question is displaced by omitting it. Per the template's own drop guidance, recorded here rather than included."
  reflection_overrides: []
  hints_acted_on:
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      where: >-
        %file_paths% and %file_dirs% placeholders drive the file-event questions
        (evidence_preservation Q9, download_success_verification Q7, payload_analysis Q8,
        directory_activity Q10), which resolve the download destination from the nimgrab
        command line.
    - hint: "Condition uses OR among selection groups: (selection_name OR selection_hashes). Q1 (verify_trigger_match) and the playbook description must frame this as 'EITHER ... OR ...', not 'X AND Y AND Z'."
      where: >-
        Q1 (verify_trigger_match) and the playbook description both use disjunctive
        name-branch OR hash-branch framing; Q1's context directs the analyst to note which
        branch fired because the hash-only branch makes the signature and origin questions
        load-bearing.
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - download_tool
    - remote_access_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 11
      file_event: 4
      network_connection: 3
