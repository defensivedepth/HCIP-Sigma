name: Write Protect For Storage Disabled
id: 47b95edd-460d-4b4c-8010-faf65613d1dc
description: |
  Detects a command line that modifies the StorageDevicePolicies\WriteProtect value under
  HKLM\System\CurrentControlSet\Control to re-enable writes to removable storage. Setting this
  flag to 0 removes a machine-wide write-block on removable media; IT administrators clear it
  legitimately when imaging or deploying systems, but adversaries clear it to stage or exfiltrate
  data to USB devices. Determine which process issued the change, whether the user and host are
  expected to manage removable-media policy, and whether the modification is accompanied by other
  defense-tampering or data-staging activity.
type: detection
detection_id: 75f7a0e2-7154-4c4d-9eae-5cdb4e0a5c13
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing
    the exact command line against the rule's criteria. The rule fires when a single command line
    contains all of the StorageDevicePolicies path under \System\CurrentControlSet\Control, the
    "Write Protection" string, the value 0, and the word storage — i.e. a command setting the
    removable-storage WriteProtect flag to 0 (writes allowed).
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manage removable-storage write-protection policy?
  context: Legitimate administrative activity can trigger this detection — imaging, deployment, and
    USB-policy management all touch StorageDevicePolicies. Knowing whether the user role and host
    function are consistent with changing removable-media policy is often the fastest path to
    distinguishing a routine operation from operator tampering.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: What exact value was written to the storage write-protect setting, and what control did
    it weaken?
  context: Decode the full command line to confirm the WriteProtect DWORD under
    StorageDevicePolicies was set to 0 (writes allowed) rather than 1 (write-protected), and note
    whether the key was created or an existing value overwritten. Setting it to 0 removes a
    machine-wide block on writing to removable media, which is a precondition for staging or
    exfiltrating data to USB devices.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:registry.registry_key_context
  question: What registry key and value were modified, and is StorageDevicePolicies a known
    evasion location?
  context: The registry path reveals intent. HKLM\System\CurrentControlSet\Control\StorageDevicePolicies
    with the WriteProtect DWORD governs whether removable media can be written machine-wide; a value
    of 0 enables writes. If a corresponding registry write event exists, the TargetObject and Details
    fields give the exact key path and data without parsing them from the command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:cue_process_tool.process_chain
  question: What process spawned the tool that made this change, and is that parent chain expected?
  context: The parent process reveals how the registry modification was invoked. A change driven by
    explorer.exe during interactive administration looks different from one spawned by a script host,
    Office application, or remote-execution utility. An unusual parent points to automated or
    operator-driven tampering rather than routine maintenance.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1562.process_identification
  question: Which binary issued the storage write-protect change, and is it an expected
    administrative tool for this operation?
  context: The rule keys on the command line, not on a specific binary, so identify the executable
    that carried the modification — reg.exe, powershell.exe, cmd.exe, or another interpreter — and
    confirm it is a tool an administrator would legitimately use here. Authorized maintenance
    utilities that touch StorageDevicePolicies can be excluded from suspicion; an unexpected or
    renamed binary issuing the same change is the deviation worth chasing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: selection and _pb_scope
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this storage write-protect pattern, what parents and
    users normally drive it, and does the current alert fit that pattern?
  context: Even routine policy changes leave a baseline. The query mirrors the rule's selection
    scoped to this host, so the parent/user distribution reflects how this specific write-protect
    change has historically run here rather than generic tool usage. A new parent process, an
    unexpected user account, or a first-time appearance of the pattern is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:defense_tampering.protection_status
  question: Is removable-storage write protection currently restored, or is the host still left
    writable?
  context: The change may be temporary (for a specific maintenance action) or persistent. Confirm
    the current StorageDevicePolicies\WriteProtect state — whether write protection has been
    re-enabled or the host still allows unrestricted writes to removable media. If the host remains
    writable without a justified business reason, the policy should be restored before further
    investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after removable-storage writes were re-enabled?
  context: Re-enabling writes to removable media is typically a precursor to using that capability.
    Examining process activity immediately after the change reveals what it was meant to enable —
    archiving utilities, file-copy operations to removable volumes, or staging tools. A burst of
    file or copy activity in this window points to data staging for exfiltration over physical media.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense-tampering actions performed on this host around the same time?
  context: An actor who weakens one control often weakens several — clearing logs, stopping
    security services, deleting shadow copies, or changing firewall rules alongside the storage
    policy change. Finding multiple tampering events from the same user or parent process within a
    short window indicates a coordinated effort to remove controls rather than an isolated
    administrative action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session?
  context: Actors who modify one registry value often modify several — pairing a removable-storage
    policy change with security-tool, persistence, or firewall registry edits. Clusters of registry
    write operations from the same user within a short window reveal the full scope of configuration
    changes and whether multiple objectives were pursued. The triggering StorageDevicePolicies change
    itself can be set aside to surface the additional edits.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally modify removable-storage or other machine policy on hosts?
  context: Compare this user's current action against their historical behavior. A user who has
    never changed storage write-protection or similar machine-wide policy is more suspicious than
    one who does so regularly as part of an imaging or deployment role. The user's role and typical
    toolset should align with managing removable-media policy.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and command-line combination historically routine on this host or across
    the environment?
  context: When the alert fires under a service principal, user identity does not attribute behavior;
    the meaningful pivot is the launch mechanism — which parent process issued this storage policy
    change. A new or rare parent for this pattern on this host or environment-wide is the deviation
    worth chasing; a parent that already accounts for many prior events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated
    action across multiple endpoints. If the same user triggered similar policy changes or staging
    activity on multiple hosts, the scope of the operation may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and command-line combination fire on other hosts in the same window?
  context: Under a service principal, asking what else the user did across the network returns
    agent-telemetry noise. The meaningful question is whether the same launch mechanism — the parent
    process issuing this storage write-protect change — fired on other hosts in the alert window.
    Multiple hosts in the same window points to scheduled or pushed automation; a single host points
    to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the
    immediate context — what led to the storage policy change, what happened after, and whether it
    is part of a larger sequence. Look for reconnaissance, additional defense tampering, or data
    staging that provides broader context.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, credential access, or other defense
    tampering — may reveal the attack stages that preceded re-enabling removable-storage writes and
    help reconstruct the full timeline. The sequence of alerts often tells the story leading up to
    data staging.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this storage write-protect pattern in the past week, and does the
    cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide
    count of this specific write-protect change rather than generic tool usage. Many hosts firing the
    same pattern points to scripted deployment or imaging automation; one host or a small unrelated
    set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
        CommandLine|contains|all:
        - \System\CurrentControlSet\Control
        - Write Protection
        - '0'
        - storage
      condition: selection
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562
      name: Impair Defenses
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: registry_modification_disabling_control
      description: "Process modifies StorageDevicePolicies\\WriteProtect to 0, re-enabling writes to removable media"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: process_identification
      description: "Identify the binary and parent that issued the policy change and correlate with process creation"
      mapped_to: Q6
      source: attack:T1562.process_identification
    - stage: 3
      label: post_tamper_activity
      description: "Activity enabled by the weakened control — staging/exfil to removable media after the change"
      mapped_to: Q9
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: process_identification
      technique: T1562
      covered_by: Q6
      source: attack:T1562.process_identification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562
      covered_by: Q9
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562
      covered_by: Q7
      source: template:cue_process_tool.baseline_deviation
      coverage_type: indirect
  mutable_dispositions:
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only (exclude authorized administrative tools)"
      disposition: context
      target_question: Q6
      notes: "process_identification question asks the analyst to confirm the issuing binary is an expected administrative tool, letting authorized tools be excluded; no fixed allowlist values to inject"
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only (refine which services are security-critical)"
      disposition: context
      target_question: Q10
      notes: "rule does not stop a named service; other_tampering_activity carries the security-service name/command patterns for correlating concurrent control tampering"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. mechanism identity pivot)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. mechanism identity pivot)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context; also superseded by defense_tampering.what_was_disabled (both more specific for this tampering rule)"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed for this rule; its what_was_disabled and other_tampering_activity questions are more specific and supersede this generic host-tamper sweep (per template notes_to_llm drop condition (a))"
    - template: cue_process_tool.baseline
      reason: "Image-based baseline (Image|endswith %Image%) is empty for this CommandLine-only rule; cue_process_tool.baseline_deviation (reflected, CommandLine-scoped) and cue_user_actor.baseline (CommandLine-scoped) cover historical frequency with filter-correct queries"
    - template: cue_process_tool.follow_on
      reason: "Image-based child-process scope (ParentImage|endswith %Image%) is empty for this CommandLine-only rule; defense_tampering.activity_after_tampering covers post-change host activity"
    - template: cue_process_tool.clustering
      reason: "host-wide tool clustering duplicates cue_host_context.clustering coverage already present; dropped to stay within the 18-question cap"
    - template: persistence_scope.process_based_persistence
      reason: "rule re-enables removable-media writes (staging/exfil precursor), not a process-persistence foothold; backup-persistence stacking via schtasks/sc/bitsadmin is not the live question for this storage-policy change"
    - template: persistence_scope.file_based_persistence
      reason: "rule re-enables removable-media writes, not Startup/Run-key file persistence; file-based persistence drops are out of scope for a StorageDevicePolicies modification"
  reflection_overrides: []
  hint_dispositions:
    - hint: "persistence_scope.process_based_persistence / file_based_persistence (defense_tampering -> persistence_scope cascade)"
      disposition: declined
      reason: >-
        Cascade activated the persistence_scope set, but this rule re-enables
        writes to removable media (a data-staging / exfiltration precursor),
        not a persistence foothold. Backup-persistence stacking (schtasks, sc,
        bitsadmin, Startup-folder drops) is tangential; the disposition turns on
        post-change staging activity, covered by
        defense_tampering.activity_after_tampering (Q9). No persistence_scope
        question authored.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: medium
        reason: "description mentions ransomware as one possible follow-on, but the rule detects a removable-media write-enable, not encryption/shadow-copy/recovery tampering; ransomware_impact keyword match is incidental to what the rule fires on"
      - id: network_scope
        confidence: medium
        reason: "the unlocked capability is exfiltration over physical/USB media, not network egress; network_scope (DestinationIp/Port pivots) does not fit a registry-policy command line"
  technique_sets_applied:
    - universal
    - defense_tampering
    - registry
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 16
      registry_set: 1
      sigma_alert: 1
