name: Password Set to Never Expire via WMI
id: e41e1397-e8af-4a46-a914-6233eda89911
description: |
  Detects wmic.exe modifying a user account so its password never expires, via a command line that contains all of useraccount, set, passwordexpires, and false. Disabling password expiration on an account keeps a credential valid indefinitely and is a common way to durably preserve access to a compromised or attacker-controlled account, including built-in accounts that were re-enabled. Legitimate administrators do occasionally set this attribute, so the investigation should establish which account was changed, who and what process drove the change, and whether surrounding account-manipulation or re-enablement activity occurred on the host.
type: detection
detection_id: 7864a175-3654-4824-9f0d-f0da18ab27c0
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match a wmic useraccount set passwordexpires=false invocation?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when wmic.exe (or a binary with OriginalFileName wmic.exe) runs with a command line containing all of useraccount, set, passwordexpires, and false, so verify those tokens are present and read which account name the change targeted.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to modify account password-expiration settings from the command line?
  context: Legitimate administrative activity can trigger this detection. Knowing whether the acting user role and host function include account administration is often the fastest path to distinguishing a routine change from an attacker preserving access. Account-management hosts (domain controllers, admin jump boxes) and identity-admin users make this routine; an arbitrary workstation and a non-admin user do not.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: Which account did this wmic command target, and what exactly was changed?
  context: The full command line reveals which account was modified and how the WMI query was scoped. Read the where clause to identify the target account name (built-in accounts such as Guest or other re-enabled default accounts are a notable case), and confirm the set operation only disabled password expiration rather than chaining additional attribute changes. The query scopes to wmic executions carrying the useraccount/set/passwordexpires/false pattern on this host so multiple sequential account-change invocations that form one operation are visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains|all:
          - useraccount
          - ' set '
          - passwordexpires
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this wmic execution and is that parent chain expected?
  context: The parent process reveals how wmic was invoked. Account changes driven from an interactive cmd.exe or powershell.exe launched by a remote-access tool, or from an unusual scripting host, point to operator activity, while management tooling or a documented automation parent points to routine administration. Walk the parent up further if the immediate parent is itself a shell.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1098.time_correlation
  question: What other account-manipulation commands ran on this host in the surrounding window?
  context: Disabling password expiration rarely stands alone. The same operator commonly re-enables or creates an account, sets a password, and adds it to a privileged local group around the same time. Surfacing net.exe and net1.exe activity (user creation, /active:yes re-enablement, localgroup membership changes) in the surrounding window shows whether this password-expiration change is one step in an account-takeover sequence or an isolated administrative tweak.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\net.exe'
          - '\net1.exe'
        CommandLine|contains:
          - 'user '
          - 'localgroup '
      condition: selection
    fields:
      - CommandLine
- source: attack:T1098.security
  question: Do account-management security events on this host confirm which account was changed, enabled, or added to a privileged group?
  context: Process command lines show the tool that was run, but the authoritative record of the resulting account change lives in account-management audit events. Correlate the wmic change with user-account-changed, user-account-enabled, and member-added-to-security-group events for the same target account to confirm the modification took effect and to catch companion changes (a re-enabled built-in account, or a new membership in Administrators or Remote Desktop Users) that the process logs alone may not reveal. If account-management auditing is not collected in this environment, treat the absence of these events as a visibility gap rather than evidence nothing changed.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: security
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        winlog.event_id:
          - 4722
          - 4738
          - 4732
          - 4728
      condition: selection
    fields:
      - winlog.event_id
      - winlog.event_data.TargetUserName
      - winlog.event_data.SubjectUserName
- source: attack:T1047.network_connection
  question: Did wmic on this host make a remote connection consistent with a remote WMI operation rather than a local account change?
  context: WMI can target remote hosts as well as the local machine. A local password-expiration change produces no outbound WMI connection, so an outbound DCOM/WMI connection (typically TCP 135 plus a high ephemeral port) originating from wmic around the alert time indicates the operation reached across the network and may have changed an account on another host. Correlate connection timing with the wmic process to separate local-only changes from remote account manipulation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\wmic.exe'
        Initiated: true
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - CommandLine
- source: attack:T1047.wmi
  question: Do WMI provider operational events on this host corroborate a user-account modification through the WMI provider?
  context: The WMI service emits operational events when a provider executes a method or query, independent of the process_creation record for wmic. Reviewing WMI activity events around the alert time corroborates that the account change went through the WMI provider and can reveal additional WMI-driven operations (subscriptions, remote method calls) that the command line alone does not show. If WMI operational logging is not ingested in this environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: wmi
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - winlog.event_id
      - User
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run wmic to administer accounts?
  context: Compare the acting user's current action against their historical behavior. A user who has never run wmic for account administration is more notable than one who does so regularly as part of their role. Aggregating by host over 90 days shows where, if anywhere, this user routinely performs this action.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and wmic combination historically routine on this host or across the environment, or is the parent unusual for wmic?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism. A parent process that has spawned wmic many times before is automation or management tooling, while a new or rare parent for wmic is the deviation worth chasing. Aggregating by host highlights whether the pairing concentrates on the alerting host.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting user's activity across all hosts in the surrounding window reveals whether the account change is part of broader hands-on activity or lateral movement. The same user driving similar account or tool activity on multiple hosts widens the apparent scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and wmic combination fire on other hosts in the same window, indicating a coordinated automation rollout rather than isolated activity?
  context: Under a service principal, user-pivoted clustering returns agent-telemetry noise because every host runs system-owned agents constantly. The meaningful question is whether the same launch mechanism (parent process plus wmic) fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:lolbin.renamed_binary_check
  question: Was wmic.exe executed from its expected System32 or SysWOW64 path?
  context: wmic.exe executed from a non-standard path (outside System32 or SysWOW64) may indicate the binary was copied or renamed to evade path-based allowlists. A wmic.exe running from a user temp or download directory is more notable than one in System32, and the OriginalFileName field confirms whether a renamed binary triggered the rule.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the wmic password-expiration pattern over the past 90 days, and what command-line shapes are typical?
  context: A pattern with no prior history on a host is more notable than one used regularly under stable parents. The query mirrors the rule's selection structure scoped to this host, so the count reflects the rule's specific firing pattern rather than generic wmic usage. The command-line projection lets the analyst compare typical invocation shapes against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wmic.exe
      - OriginalFileName: wmic.exe
      selection_cli:
        CommandLine|contains|all:
        - useraccount
        - ' set '
        - passwordexpires
        - 'false'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:lolbin.technique_class_lateral
  question: Were any other proxy-execution or persistence LOLBins used on this host recently?
  context: Operators who use one living-off-the-land binary often use several. After an account-manipulation step via wmic, check whether other proxy-execution or persistence binaries (rundll32, mshta, schtasks, reg, sc, bitsadmin) ran on the host in the same day. Multiple LOLBins working toward the same objective strengthen the case for malicious activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\installutil.exe'
          - '\schtasks.exe'
          - '\reg.exe'
          - '\sc.exe'
          - '\at.exe'
          - '\bitsadmin.exe'
          - '\certutil.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After keeping a credential valid by disabling password expiration, they commonly install backup mechanisms using scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions. Multiple persistence installations from the same timeframe indicate intentional stacking rather than an isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host (defense tampering, account re-enablement, reconnaissance, credential access) may reveal the attack stages that preceded this account change and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1047
      name: Windows Management Instrumentation
      role: primary
    - id: T1098
      name: Account Manipulation
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: wmi_process_creation
      description: "T1047 — wmic process is created to invoke a WMI operation"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: wmi_command_execution
      description: "T1047 — the WMI command executes (account attribute modification); decode the operation"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: wmi_remote_connection
      description: "T1047 — corresponding network connection if the WMI operation is remote"
      mapped_to: Q7
      source: attack:T1047.network_connection
    - stage: 4
      label: account_attribute_change
      description: "T1098 — account attribute change (password never-expire) correlated with surrounding account-manipulation commands and audit events"
      mapped_to: Q5
      source: attack:T1098.time_correlation
  dimension_coverage:
    - dimension: time_correlation
      technique: T1047
      covered_by: Q7
      source: attack:T1047.network_connection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1047
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1047
      covered_by: Q9
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: time_correlation
      technique: T1098
      covered_by: Q5
      source: attack:T1098.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1098
      covered_by: Q6
      source: attack:T1098.security
      coverage_type: direct
  mutable_dispositions:
    - element: WMIQueryScope
      dimension: tuning_parameter
      specific_values: "rationale only (\\root\\cimv2, \\root\\subscription namespaces)"
      disposition: context
      target_question: Q3
      notes: "Namespace scoping is a tuning rationale, not a queryable value in process_creation; mechanism_decode context directs the analyst to read the WMI query scope from the command line."
    - element: SuspiciousCommandPatterns
      dimension: tuning_parameter
      specific_values: "wmic process call, powershell Invoke-WmiMethod, useraccount set passwordexpires"
      disposition: query_injection
      target_question: Q3
      notes: "Rule's useraccount/set/passwordexpires pattern injected into mechanism_decode CommandLine|contains|all to align the decode query with the rule's firing pattern."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (~5m correlation window)"
      disposition: context
      target_question: Q5
      notes: "Correlation window applied as the +/-1h range on the account-manipulation correlation question; exact threshold is environment-tunable, surfaced as context."
    - element: RemoteDestinationThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (unique remote hosts within window)"
      disposition: context
      target_question: Q7
      notes: "Remote-host count is a tuning rationale; the network_connection question surfaces outbound wmic connections for the analyst to assess remote scope."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (interactive vs system/low-priv)"
      disposition: context
      target_question: Q9
      notes: "Interactive-vs-service execution distinction carried by the user-actor baseline plus the service-principal substitute pairing."
    - element: HighPrivilegeGroupList
      dimension: tuning_parameter
      specific_values: "Administrators, Remote Desktop Users, Domain Admins, Enterprise Admins"
      disposition: query_injection
      target_question: Q5
      notes: "net localgroup membership changes captured via CommandLine|contains 'localgroup ' in the account-manipulation correlation query; privileged group names called out in Q5/Q6 context."
    - element: SubjectTargetMismatch
      dimension: tuning_parameter
      specific_values: "modifier account != modified account (potential hijack)"
      disposition: query_injection
      target_question: Q6
      notes: "Security-event question projects TargetUserName and SubjectUserName so the analyst can flag subject/target mismatch on the account change."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted per service_principal pairing rules"
      kept: both (Q9 user-pivot + Q10 parent+binary substitute)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted per service_principal pairing rules"
      kept: both (Q11 user-pivot + Q12 parent+binary substitute)
    - dropped: cue_process_tool.baseline
      reason: "host-baseline coverage subsumed by lolbin.historical_lolbin_baseline (Q14), which is rule-pattern-scoped and more specific than generic wmic host frequency"
      kept: template:lolbin.historical_lolbin_baseline
  skipped_templates:
    - template: cue_process_tool.baseline_deviation
      reason: "Per-host rule-pattern parent/user distribution overlaps lolbin.historical_lolbin_baseline (Q14, same host-scoped rule-pattern selection); kept the lolbin must_include version to avoid two near-identical reflected host-pattern queries."
    - template: cue_process_tool.scope
      reason: "Fleet-wide rule-pattern distribution; dropped to hold the 14-18 cap. Cross-host coordination is covered for the relevant launch-mechanism angle by cue_user_actor.clustering_svc (Q12)."
    - template: cue_process_tool.follow_on
      reason: "wmic useraccount set is a terminal account-attribute change that does not spawn post-exploitation children; child-process enumeration adds no signal for this rule. Surrounding tool activity is covered by Q5 (account-manipulation correlation) and Q11 (user clustering)."
    - template: cue_process_tool.clustering
      reason: "Generic same-window tool activity on the host overlaps Q5 (account-manipulation correlation) and Q11 (user network-wide clustering); dropped to hold the cap."
    - template: cue_host_context.clustering
      reason: "Generic host-wide same-window aggregation overlaps Q5 (account-manipulation correlation) on this host; precursor (Q17) carries the prior-activity timeline angle."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED generic tamper sweep. This rule detects an account-persistence step, not payload execution or credential access; tamper-as-precursor is not the disposition pivot. Prior-alert tamper context is carried by cue_host_context.precursor (Q17)."
    - template: organizational_context.change_ticket
      reason: "Dropped to hold the 14-18 cap; authorization/legitimacy is carried by host_user_context (Q2) which asks whether this user/host is expected to make this account change."
    - template: organizational_context.human_recall
      reason: "Dropped to hold the cap; legitimacy confirmation overlaps host_user_context (Q2)."
    - template: organizational_context.timing_alignment
      reason: "Dropped to hold the cap; off-hours assessment overlaps the timing reading available from verify_trigger_match (Q1) event context."
    - template: organizational_context.procedural_consistency
      reason: "Dropped to hold the cap; tooling-consistency angle overlaps host_user_context (Q2) and process_chain (Q4)."
    - template: persistence_scope.file_based_persistence
      reason: "Dropped to hold the cap; the rule's persistence is credential-based (never-expire on an account), not file-drop based, so Startup/Run-key file drops are a lower-yield parallel mechanism. Process-based persistence stacking is covered by Q16."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - organizational_context
    - persistence_scope
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 13
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      security: 1
      network_connection: 1
      wmi: 1
      sigma_alert: 1
