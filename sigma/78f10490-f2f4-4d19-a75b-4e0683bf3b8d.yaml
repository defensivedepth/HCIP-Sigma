name: Suspicious Speech Runtime Binary Child Process
id: 7b2e8b99-caae-4985-8e4d-af0989c8697c
description: |
  Detects any child process spawned by SpeechRuntime.exe (ParentImage ending in \SpeechRuntime.exe).
  SpeechRuntime.exe is instantiated by a Speech-related COM class that, when configured as INTERACTIVE
  USER, runs in the session of the currently logged-on user; if that COM class is also vulnerable to
  COM hijacking, an actor can plant a registry hijack and a dropped DLL, trigger loading via DCOM over
  RPC (port 135) from a remote host, and execute code in the victim user's session without taking over
  the machine. SpeechRuntime.exe spawning child processes is the observable tail of that chain, so the
  investigation should confirm what child was spawned, identify the remote DCOM source and authenticating
  account, check whether SpeechRuntime.exe loaded a newly dropped or unsigned DLL, and determine whether
  the child activity is benign speech-feature behavior or operator follow-on execution.
type: detection
detection_id: 78f10490-f2f4-4d19-a75b-4e0683bf3b8d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alert show a child process whose parent image ends in \SpeechRuntime.exe?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the triggering event. The rule fires on a single condition, a process whose ParentImage ends in \SpeechRuntime.exe. Confirm SpeechRuntime.exe is the direct parent and note the child Image and command line, since the child identity drives the rest of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to produce speech-related processes that spawn child programs?
  context: SpeechRuntime.exe normally runs as part of Windows speech and dictation features and rarely spawns arbitrary child processes. Knowing whether the logged-on user actually uses speech features, and whether this host has a role consistent with that, is often the fastest way to separate routine behavior from a hijacked COM class executing in the user's session.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1021.003.process_identification
  question: What is the child binary, and is it a known living-off-the-land or remote-execution program rather than an expected speech component?
  context: The child process is the payload of the chain. Speech-feature behavior produces few or no child processes; a hijacked COM class commonly spawns command interpreters, script hosts, or signed binaries used for proxy execution such as cmd.exe, powershell.exe, rundll32.exe, regsvr32.exe, mshta.exe, or wmic.exe. Identify the child binary and its full path so its name, location, and signing can be assessed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the child process do? Decode its full command line.
  context: The full command line of the spawned child reveals the operator's intent. Decode any flags, encoded blobs, file paths, URLs, or DLL entry points. A download cradle, an in-memory loader, a reverse shell, or a regsvr32/rundll32 invocation pointing at a dropped DLL all signal follow-on execution; benign speech behavior carries no such arguments. Scoping to all executions of this child binary on the host within the window captures multi-command sequences that form one logical operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1021.003.image_load
  question: Did SpeechRuntime.exe load a newly dropped or unsigned DLL shortly before spawning this child?
  context: COM hijacking of the Speech class works by pointing a hijacked CLSID at an attacker DLL that SpeechRuntime.exe then loads from the hijack location, often dropped over SMB just before execution. A module load into SpeechRuntime.exe from a user-writable or temp path, or an unsigned or recently created DLL, is the loader step that precedes the child process. If image_load events are not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: -10m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \SpeechRuntime.exe
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1021.003.dcom_remote_source
  question: What remote host initiated DCOM activity to this host over RPC around the alert time, and is it an expected administrative source?
  context: 'DCOM-triggered execution requires a remote caller reaching this host over the RPC endpoint mapper (port 135) and dynamic high ports, typically preceded by a network logon. Identifying the source address shows whether the trigger came from a known admin jump box or an unexpected workstation acting as a pivot. If network connection telemetry is not collected, this step depends on the authentication record instead.'
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationPort: 135
      condition: selection
    fields:
      - SourceIp
      - DestinationPort
      - Initiated
- source: attack:T1021.003.authentication_trail
  question: What account authenticated to this host around the alert time, and was it a network logon consistent with a remote DCOM trigger?
  context: 'DCOM-based remote execution authenticates to the target before instantiating the COM object. Network logons (type 3) and explicit-credential logons from a remote address, especially under a privileged or rarely seen account, point to a remote operator rather than the interactive session owner. Correlate the authenticating account and source address with the user the child process ran as.'
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID:
          - 4624
          - 4648
        Computer|expand: '%hostname%'
      condition: selection
    fields:
      - TargetUserName
      - IpAddress
      - LogonType
      - EventID
- source: template:cue_parent_process.baseline
  question: Has SpeechRuntime.exe ever spawned this child binary on this host before?
  context: SpeechRuntime.exe spawning child processes is unusual on its own, so the historical frequency of this specific parent-child pairing is a strong discriminator. Zero or very low counts indicate a novel spawning relationship consistent with COM-class abuse rather than a routine speech-feature side effect that has run many times before.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did SpeechRuntime.exe spawn on this host?
  context: An operator using the hijacked COM class as an execution foothold often spawns several tools in sequence through the same parent, such as reconnaissance, credential access, then lateral movement. Listing every child SpeechRuntime.exe spawned reveals the full scope of activity initiated through this chain beyond the single child that tripped the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentName|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \SpeechRuntime.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \SpeechRuntime.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar activity on other hosts in the window, the scope of the operation may be larger than this single endpoint, which is the expected pattern when DCOM is used to reach multiple victims.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \SpeechRuntime.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:lateral_movement.lateral_movement_scope
  question: Has this user account driven lateral-movement tooling against other hosts in the environment?
  context: Actors rarely stop at one target. If the account tied to this activity is also running remote-execution and lateral-movement tools across other systems, the scope of compromise is larger. Finding such tool usage by the same account on many hosts within a short window indicates active lateral movement rather than a contained event.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      selection_lateral:
        Image|endswith:
          - '\psexec.exe'
          - '\psexec64.exe'
          - '\mstsc.exe'
          - '\ssh.exe'
          - '\plink.exe'
          - '\wmic.exe'
      condition: selection and selection_lateral
    fields:
      - host.name
      - Image
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or further remote-execution attempts close in time to the SpeechRuntime.exe child spawn.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:network_scope.same_action_other_hosts
  question: Is SpeechRuntime.exe spawning child processes on other hosts in the environment?
  context: A DCOM-driven COM-hijack campaign reaches many victims by triggering the same COM class on each. Finding SpeechRuntime.exe spawning child processes on other hosts in a short window shows the activity is not isolated and helps scope containment. A single host is more consistent with a targeted action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \SpeechRuntime.exe
      filter_self:
        host.name|expand: '%hostname%'
      condition: selection and not filter_self
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: 'Alerts rarely happen in isolation. Earlier detections on the same host, such as remote registry modification, SMB file drops, or reconnaissance, may reveal the COM-hijack setup steps that preceded execution and help reconstruct the timeline. Look for patterns: a remote file drop or registry change followed by this execution.'
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1021.003
      name: Distributed Component Object Model
      role: primary
    - id: T1021
      name: Remote Services
      role: secondary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: remote_dcom_trigger
      description: Remote DCOM invocation by an account over RPC (port 135), preceded by a network logon, reaches the target host.
      mapped_to: Q6
      source: attack:T1021.003.dcom_remote_source
    - stage: 2
      label: module_load_on_target
      description: SpeechRuntime.exe loads a newly dropped or unsigned DLL from the COM-hijack location on the remote system.
      mapped_to: Q5
      source: attack:T1021.003.image_load
    - stage: 3
      label: child_process_execution
      description: SpeechRuntime.exe spawns a child process in the logged-on user's session (the rule's trigger).
      mapped_to: Q1
      source: template:universal.verify_trigger_match
  dimension_coverage:
    - dimension: process_identification
      technique: T1021.003
      covered_by: Q3
      source: attack:T1021.003.process_identification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1021.003
      covered_by: Q6
      source: attack:T1021.003.dcom_remote_source
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1021.003
      covered_by: Q6
      source: attack:T1021.003.dcom_remote_source
      coverage_type: direct
    - dimension: user_context
      technique: T1021.003
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1021
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q9
      source: template:cue_parent_process.follow_on
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "300s (example correlation window)"
      disposition: context
      target_question: Q6
      notes: "Correlation window between RPC trigger and process creation framed in DCOM-source context; no fixed numeric value is queryable, the +/-15m range carries the correlation intent."
    - element: UserContext
      dimension: user_context
      specific_values: "rare or first-time DCOM invocations by specific accounts (rationale only)"
      disposition: context
      target_question: Q2
      notes: "No fixed account list; host_user_context plus user-actor baseline carry user-expectation and rare-account analysis."
    - element: ProcessName
      dimension: process_identification
      specific_values: "excel.exe, wmiprvse.exe (examples of DCOM-abused executables)"
      disposition: new_question
      target_question: Q3
      notes: "Drove attack:T1021.003.process_identification; example LOLBins named in context, child Image projected for identification."
    - element: RemoteHostList
      dimension: tuning_parameter
      specific_values: "known systems that should/should not invoke DCOM (rationale only)"
      disposition: context
      target_question: Q6
      notes: "No fixed allowlist; expected-admin-source judgment folded into the DCOM remote-source question."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (unexpected parent-child profiling)"
      disposition: context
      target_question: Q9
      notes: "SpeechRuntime.exe is fixed as the parent by the rule; parent-child profiling expressed via follow_on and parent-process baseline."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "msiexec.exe, regsvr32.exe (examples of signed proxy binaries)"
      disposition: context
      target_question: Q3
      notes: "Signed proxy-execution binaries named in process-identification context; OriginalFileName projected so signing/identity can be assessed."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, script paths, DLL entry points (rationale only, no fixed regex)"
      disposition: context
      target_question: Q4
      notes: "No fixed regex; mechanism_decode question directs decoding of flags, URLs, paths, and DLL entry points in the child command line."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "legitimate enterprise domains (rationale only)"
      disposition: context
      target_question: Q6
      notes: "No fixed allowlist; expected-source judgment carried by the DCOM remote-source question."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_parent_process.clustering
      reason: "+/-15m host-scoped activity sweep duplicated by cue_host_context.clustering (same Image+ParentImage aggregation on host)"
      kept: cue_host_context.clustering
    - dropped: cue_parent_process.scope
      reason: "enterprise-wide parent-child scope duplicated by network_scope.same_action_other_hosts, which queries the same ParentImage \\SpeechRuntime.exe pattern across hosts"
      kept: network_scope.same_action_other_hosts
    - dropped: cue_process_tool.baseline
      reason: "host baseline of the spawned binary duplicated by cue_parent_process.baseline (parent-child pairing on host is the more specific discriminator for a parent-only rule)"
      kept: cue_parent_process.baseline
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — process_chain dropped (parent cue subsumes it; ParentImage is already the detection field)"
    - template: cue_parent_process.mechanism_decode
      reason: "parent process is fixed as SpeechRuntime.exe by the rule and is a benign OS binary; its own process_creation event carries no operator command line to decode, and the COM/DCOM trigger does not appear on the parent's command line. Child-process decode (Q4) and DCOM-source/authentication questions carry the mechanism analysis instead."
    - template: cue_parent_process.clustering
      reason: "duplicated by cue_host_context.clustering — see dedup_decisions"
    - template: cue_parent_process.scope
      reason: "duplicated by network_scope.same_action_other_hosts — see dedup_decisions"
    - template: cue_process_tool.baseline
      reason: "duplicated by cue_parent_process.baseline — see dedup_decisions"
    - template: universal.endpoint_protection_health
      reason: "recommended-only generic tamper sweep; this is a lateral-movement/DCOM-trigger rule, not an execution or credential-access rule whose disposition turns on prior AV/EDR tampering. cue_host_context.precursor already surfaces any prior tamper alerts on the host, and the slot is spent on DCOM-source, authentication, and image_load coverage that is specific to this chain."
    - template: lateral_movement.source_host_identification
      reason: "remote-source identification reauthored as attack:T1021.003.dcom_remote_source (Q6), which narrows the lateral_movement port template to the DCOM-specific port 135 + Initiated direction for this trigger."
    - template: lateral_movement.authentication_method
      reason: "authentication analysis reauthored as attack:T1021.003.authentication_trail (Q7), which queries the actual Security log 4624/4648 events on the target rather than the ProcessGuid boilerplate the lateral_movement template carries — context promised Security-log analysis, so the query must target it."
    - template: lateral_movement.target_activity_after_access
      reason: "post-access process activity on the target duplicated by cue_parent_process.follow_on (Q9) and cue_host_context.clustering (Q15); the SpeechRuntime.exe child spawn IS the post-access activity, already enumerated."
    - template: network_scope.actor_activity_network_wide
      reason: "user network-wide activity duplicated by cue_user_actor.clustering (Q12), same User-scoped host+Image aggregation over the same window."
    - template: network_scope.actor_authentication_trail
      reason: "user authentication trail across hosts duplicated by attack:T1021.003.authentication_trail (Q7), which already queries 4624/4648 for the account; a second network-wide auth query adds no distinct pivot for this rule."
  reflection_overrides: []
  hint_dispositions: []
  hints_acted_on:
    - hint: "parent_process is primary — drop cue_process_tool:process_chain"
      action: "process_chain skipped (see skipped_templates); ParentImage is already the detection field"
    - hint: "dimension 'process_identification' (T1021.003) uncovered — author attack:T1021.003.process_identification"
      action: "authored Q3 attack:T1021.003.process_identification (child binary / path / OriginalFileName / LOLBin identification)"
    - hint: "logsource 'image_load' (T1021.003,T1218) not covered — author category: image_load question"
      action: "authored Q5 attack:T1021.003.image_load (newly dropped / unsigned DLL loaded into SpeechRuntime.exe — the COM-hijack loader step)"
    - hint: "logsource 'security' (T1021.003) not covered — author category: security question"
      action: "authored Q7 attack:T1021.003.authentication_trail (4624/4648 network logon for the DCOM trigger on the target)"
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - lateral_movement
    - network_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 13
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      image_load: 1
      network_connection: 1
      security: 1
      sigma_alert: 1
