name: UAC Bypass Abusing Winsat Path Parsing - Process
id: 88beabc6-b4e5-4704-a7ba-ce2d95f73789
description: |
  Detects the child-process signature of a UAC bypass that abuses a path-parsing flaw in winsat.exe. The rule
  fires when a process is spawned at High or System integrity by a parent winsat.exe located under
  \AppData\Local\Temp\system32\winsat.exe whose own command line references the real C:\Windows \system32\winsat.exe
  path (note the space). The technique plants a copy of winsat.exe in a user-writable directory whose layout
  mimics the trusted system path; the auto-elevated winsat then loads an attacker-controlled DLL and spawns a
  child at elevated integrity without a consent prompt. Legitimately, winsat.exe runs from System32 under SYSTEM
  during assessments and does not live under a user's Temp directory. Investigation should confirm the parent
  path and command line, identify the elevated child and what it did, find the planted winsat.exe and the DLL it
  loaded, and determine whether the elevated session was used to install persistence, dump credentials, or tamper
  with security tooling.
type: detection
detection_id: 7a01183d-71a2-46ad-ad5c-acd989ac1793
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Was this process spawned at high or system integrity by a winsat.exe running from a user Temp directory that referenced the real system path?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when ALL
    of these hold for the same event - the integrity level is High or System (or the equivalent SIDs S-1-16-16384
    / S-1-16-12288), the parent image ends in \AppData\Local\Temp\system32\winsat.exe, and the parent command
    line references C:\Windows \system32\winsat.exe (the trailing space after Windows is the path-parsing trick).
    Read the parent image and parent command line and verify all three are present rather than an unrelated winsat
    invocation from System32.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
      - IntegrityLevel
- source: template:universal.host_user_context
  question: Is this user and host expected to run winsat.exe from a user-writable directory and elevate from it?
  context: Legitimate winsat.exe assessments run from System32 under SYSTEM, not from a user's AppData\Local\Temp
    tree. Knowing whether this user role and host function ever legitimately stage and run binaries from a Temp
    directory is often the fastest way to separate a true positive from an unusual but benign action. A standard
    workstation account elevating through a relocated system binary is rarely expected.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What is the planted winsat.exe parent, and what spawned it onto this host?
  context: The parent winsat.exe running from \AppData\Local\Temp\system32\ is the elevated proxy. Retrieving the
    parent's own process_creation event shows how it was launched, its full command line, and its grandparent -
    the process that staged and ran the relocated winsat (often a loader, script host, or interactive shell). The
    range is intentionally wide because the parent may have started well before the child fired.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1548.002.elevated_child_action
  question: What exactly did the elevated child process do? Decode its full command line and arguments.
  context: The bypass exists to run something at high integrity. The default child for this method is
    cmd.exe, but operators substitute their own payload. Decode every argument of the elevated child - encoded
    commands, file paths, downloaded URLs, or chained subcommands - to identify the operational purpose of the
    elevation. The query returns the child binaries spawned by the planted winsat parent on this host so chained
    or repeated invocations appear together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%ParentImage%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: attack:T1548.002.planted_binary_origin
  question: Where did the relocated winsat.exe under the Temp directory come from, and was a DLL dropped alongside it?
  context: This bypass plants a copy of winsat.exe in a user-writable directory laid out to mimic the trusted
    system path, then relies on winsat loading an attacker-supplied DLL from that same directory. Look for file
    writes that created the winsat.exe copy and any DLL written next to it in the AppData\Local\Temp\system32 tree;
    the writing process identifies who staged the bypass.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - \AppData\Local\Temp\system32\
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: attack:T1548.002.loaded_dll
  question: What DLL did the planted winsat.exe load, and was it signed and from the expected location?
  context: The path-parsing abuse causes the auto-elevated winsat.exe to load a DLL from the user-writable
    directory rather than from System32. Reviewing image loads by the planted winsat process reveals the
    attacker-controlled module - an unsigned or unexpectedly-located DLL loaded out of the Temp tree is the
    elevation mechanism. If image_load events are not collected in this environment, treat the absence as a
    visibility gap and pivot to the file-write evidence for the dropped module.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%ParentImage%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_parent_process.baseline
  question: Has winsat.exe ever previously run from this user Temp path on this host?
  context: Winsat.exe is a legitimate system binary, but it runs from System32, not from a user's
    AppData\Local\Temp\system32 directory. A historical count of executions where the parent image matches this
    relocated path distinguishes a first-time planted-binary event from a recurring pattern. Zero or very low
    counts indicate a novel execution from a path winsat should never run from.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%ParentImage%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did the planted winsat.exe spawn on this host?
  context: An elevated proxy is often used to launch several tools in sequence - reconnaissance, credential
    access, persistence. Seeing every child of the planted winsat.exe parent reveals the full scope of activity
    run at elevated integrity through this bypass, not just the single child that triggered the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%ParentImage%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is this planted-winsat spawning pattern occurring on other hosts?
  context: The same elevated winsat-from-Temp parent appearing on multiple hosts points to a scripted deployment
    or a tool being run across the fleet rather than a one-off targeted action. A single occurrence is more likely
    a focused intrusion. Enterprise-wide comparison helps calibrate severity and identify the scope of the
    activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \AppData\Local\Temp\system32\winsat.exe
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        IntegrityLevel:
        - High
        - System
        - S-1-16-16384
        - S-1-16-12288
        ParentImage|endswith: \AppData\Local\Temp\system32\winsat.exe
        ParentCommandLine|contains: C:\Windows \system32\winsat.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        IntegrityLevel:
        - High
        - System
        - S-1-16-16384
        - S-1-16-12288
        ParentImage|endswith: \AppData\Local\Temp\system32\winsat.exe
        ParentCommandLine|contains: C:\Windows \system32\winsat.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging on multiple
    endpoints. If the same user triggered similar activity on more than one host, the scope of the operation may
    be larger than a single bypass attempt.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        IntegrityLevel:
        - High
        - System
        - S-1-16-16384
        - S-1-16-12288
        ParentImage|endswith: \AppData\Local\Temp\system32\winsat.exe
        ParentCommandLine|contains: C:\Windows \system32\winsat.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context:
    what led to the elevation, what happened after, and whether it is part of a larger sequence. Look for
    reconnaissance, persistence mechanisms, or additional elevation attempts in the surrounding window.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host - defense tampering,
    reconnaissance, credential access - may reveal the attack stages that preceded this elevation and help
    reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:uac_bypass.elevated_action_after_bypass
  question: What action was performed with elevated privileges after the UAC bypass?
  context: 'UAC bypass is a means to an end - attackers elevate to perform actions that require high integrity:
    installing persistence, dumping credentials, tampering with security tools, or accessing protected resources.
    Examining processes spawned at high or system integrity on this host shortly after the bypass reveals the
    objective. Correlate timing tightly - elevation and the follow-on action typically occur within seconds to a
    few minutes.'
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        IntegrityLevel:
          - 'High'
          - 'System'
          - 'S-1-16-16384'
          - 'S-1-16-12288'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:uac_bypass.other_escalation_attempts
  question: Were other privilege escalation techniques attempted on this host?
  context: Attackers who attempt one UAC bypass often try multiple methods until one succeeds, especially if the
    OS version or configuration blocks certain techniques. Finding multiple bypass or escalation detections from
    the same user or host confirms deliberate privilege escalation rather than a false positive.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1548.002
      name: Abuse Elevation Control Mechanism - Bypass User Account Control
      role: primary
    - id: T1548
      name: Abuse Elevation Control Mechanism
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: planted_binary_staging
      description: A copy of winsat.exe and an attacker DLL are written into a user-writable directory mimicking the trusted system path
      mapped_to: Q6
      source: attack:T1548.002.planted_binary_origin
    - stage: 2
      label: auto_elevated_dll_load
      description: The auto-elevated winsat.exe loads the attacker-controlled DLL from the planted directory
      mapped_to: Q7
      source: attack:T1548.002.loaded_dll
    - stage: 3
      label: elevated_child_execution
      description: winsat.exe spawns a child process at High/System integrity without a consent prompt
      mapped_to: Q5
      source: attack:T1548.002.elevated_child_action
    - stage: 4
      label: elevated_objective
      description: The elevated session is used to perform the actual objective (persistence, credential access, tampering)
      mapped_to: Q17
      source: template:uac_bypass.elevated_action_after_bypass
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1548.002
      covered_by: Q4
      source: template:cue_parent_process.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1548.002
      covered_by: Q17
      source: template:uac_bypass.elevated_action_after_bypass
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1548.002
      covered_by: Q5
      source: attack:T1548.002.elevated_child_action
      coverage_type: direct
    - dimension: user_context
      technique: T1548
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "e.g. 30 seconds between registry/elevation events"
      disposition: context
      target_question: Q17
      notes: >-
        Tunable correlation window between bypass and elevated action — a threshold, not a queryable value. The
        elevated_action_after_bypass question uses a forward +30m range and its context tells the analyst to
        correlate elevation and follow-on action within seconds to minutes.
    - element: ElevatedProcessNameList / ElevatedProcessPath
      dimension: tuning_parameter
      specific_values: "sdclt.exe, eventvwr.exe, computerdefaults.exe (auto-elevated proxy list)"
      disposition: context
      target_question: Q5
      notes: >-
        This rule's auto-elevated proxy is fixed to winsat.exe by the detection's ParentImage; the tunable list of
        other proxies does not apply. The elevated-child question instead decodes whatever child the winsat proxy
        spawned, which is the variable element for this method.
    - element: ParentProcessAnomalyThreshold / ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only — parent-child mismatch logic"
      disposition: context
      target_question: Q4
      notes: >-
        Parent-child anomaly is the core of this rule (relocated winsat parent). mechanism_decode retrieves the
        parent event and its grandparent; baseline question quantifies how rare this parent path is on the host.
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: >-
        cue_parent_process is the primary cue; the parent-process mechanism_decode (Q4) retrieves the elevated
        winsat parent and its grandparent, and the attack:elevated_child_action question (Q5) decodes the child's
        command line. The generic process_tool mechanism_decode would scope to %Image% of the child, which is not
        a rule-fixed binary (default cmd.exe but operator-variable) and would duplicate the elevated-child decode.
    - dropped: cue_process_tool.baseline
      reason: >-
        cue_parent_process.baseline (Q8) provides the more specific historical count scoped to the relocated winsat
        parent path. A child-binary frequency baseline has no rule-fixed child binary to scope to.
    - dropped: cue_user_actor.clustering_svc / cue_user_actor.baseline_svc pairing
      reason: >-
        user_actor pair — non-deduplicable. Both user-pivot and parent+child substitute variants emitted for
        baseline and for clustering per AGENTS.md service-principal handling.
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: >-
        Hint 'parent_process is primary — drop cue_process_tool:process_chain'. parent_process cue is primary and
        its mechanism_decode (Q4) already walks the parent chain upward via ParentProcessGuid.
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'process_access' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with category: process_access"
      disposition: declined
      reason: >-
        process_access (Sysmon EID 10) in the T1548.002 strategy targets token-manipulation / handle-duplication
        bypasses (potato-style). This rule's bypass is a path-parsing DLL-load abuse of winsat.exe with no
        cross-process handle access in its mechanism; a process_access sweep would return unrelated handle-open
        noise. The DLL-load mechanism is instead covered by the image_load question (loaded_dll) and the
        elevated-action sweep (elevated_action_after_bypass) covers post-elevation behavior.
    - hint: "logsource 'registry_set' (recommended by techniques: T1548.002) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: declined
      reason: >-
        registry_set covers the HKCU shell-handler hijack family of UAC bypasses (fodhelper/sdclt/computerdefaults).
        This winsat path-parsing method does not use a registry handler hijack — its mechanism is a planted binary +
        DLL load, covered by the file_event question (planted_binary_origin, category: file_event) and the
        image_load question (loaded_dll). No registry artifact is part of this rule's detection or this method's
        documented chain.
    - hint: "logsource 'security' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with category: security"
      disposition: declined
      reason: >-
        The 'security' logsource maps to WinEventLog:Security EID 4672 (special privileges assigned), used as a
        generic elevation signal. The rule already fires on the IntegrityLevel=High/System child via
        process_creation, and elevated_action_after_bypass (Q17) sweeps High/System integrity processes on the host
        through process_creation, which is the same elevation signal with command-line context. No template family
        exists for category: security and a bare 4672 sweep would add elevation volume without the process context
        this method needs.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - uac_bypass
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      file_event: 1
      image_load: 1
      sigma_alert: 2
