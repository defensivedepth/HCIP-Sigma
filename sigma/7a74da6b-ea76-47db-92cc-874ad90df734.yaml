name: Suspicious MSDT Parent Process
id: 22fa4ecb-a9c6-4a91-bb8e-b214558c1543
description: |
  Detects msdt.exe (the Microsoft Support Diagnostic Tool) spawned by a command interpreter, script host, or proxy-execution
  binary (cmd.exe, cscript.exe, mshta.exe, powershell.exe, pwsh.exe, regsvr32.exe, rundll32.exe, schtasks.exe, wmic.exe,
  wscript.exe, or wsl.exe). The rule fires when both the parent is one of these binaries AND the child is msdt.exe (matched by
  Image name or OriginalFileName). msdt.exe launched this way is the proxy-execution pattern behind the ms-msdt URI handler abuse,
  where a malicious document or script invokes msdt.exe to run attacker-supplied code under a trusted, signed binary. Legitimate
  msdt.exe runs are normally started by the Windows troubleshooting framework or by a user opening a diagnostic, not by a scripting
  engine. Determine what the parent was doing, what msdt.exe was instructed to execute, and whether code ran outside the diagnostic
  workflow.
type: detection
detection_id: 7a74da6b-ea76-47db-92cc-874ad90df734
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Is the parent one of the flagged interpreters or proxy binaries, and is the child msdt.exe?
  context: Confirm the alert is a true positive by reviewing the exact parent and child of the triggering event. The rule fires
    only when ParentImage ends in one of cmd.exe, cscript.exe, mshta.exe, powershell.exe, pwsh.exe, regsvr32.exe, rundll32.exe,
    schtasks.exe, wmic.exe, wscript.exe, or wsl.exe AND the child resolves to msdt.exe by Image name or OriginalFileName metadata.
    Note which parent fired -- a script host (wscript, cscript, mshta) points toward document or scriptlet delivery, while a
    shell (cmd, powershell) points toward hands-on or staged execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - OriginalFileName
- source: template:universal.host_user_context
  question: Is this user and host expected to launch the diagnostic tool from a scripting or proxy-execution parent?
  context: Legitimate msdt.exe activity is normally initiated by the Windows troubleshooting framework or a user opening a
    diagnostic, not by a command interpreter or script host. Knowing whether this user role and host function routinely involve
    scripted diagnostics or support automation is often the fastest way to separate a true positive from a benign administrative
    or vendor workflow.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did msdt.exe do? Decode its full command line and any referenced answer file or payload.
  context: The full command line reveals what msdt.exe was instructed to execute. Look for an answer file (/af) or PCWDiagnostic
    package reference, embedded or encoded strings, IT_BrowseForFile or IT_RebrowseForFile parameters, and any path or URL the
    invocation pulls in. The ms-msdt proxy-execution pattern smuggles a command into these parameters so code runs under the
    signed binary; decode every argument to identify what would actually run.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:parent_child.parent_process_legitimacy
  question: Is this parent expected to spawn msdt.exe, and what started the parent itself?
  context: The parent-child relationship is the core signal for this detection. Tracing the parent's own origin (the grandparent
    process) shows whether the chain is legitimate -- a user opening a diagnostic -- or whether the interpreter was itself spawned
    by a document, a macro, or a remote-access tool. A scripting engine or shell launched by an Office application, a browser,
    or an email client and then spawning msdt.exe is the delivery pattern this rule targets.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:cue_parent_process.mechanism_decode
  question: What did the parent interpreter or script host execute that led it to spawn msdt.exe?
  context: The parent's own command line shows how the activity was initiated. For a script host (wscript, cscript, mshta) the
    command line names the script or scriptlet that ran; for a shell (cmd, powershell) it shows the command or encoded payload.
    For regsvr32.exe or rundll32.exe it shows the DLL or scriptlet loaded. This is what triggered the chain that reached msdt.exe.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: template:masquerading.true_identity
  question: Does the child's metadata and hash confirm it is the genuine Microsoft-signed msdt.exe?
  context: The rule matches both the Image name msdt.exe and the OriginalFileName metadata, so a renamed copy of msdt.exe is
    also in scope. Comparing the Image path against OriginalFileName, and checking the signing party and hash, confirms whether
    this is the genuine Microsoft-signed system binary or a relocated or replaced copy. Submit the hash for reputation lookup
    to distinguish the legitimate tool from a tampered or planted one.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:lolbin.renamed_binary_check
  question: Was msdt.exe executed from its expected system path?
  context: msdt.exe normally runs from System32 or SysWOW64. A copy running from a user-writable directory (Desktop, Downloads,
    Temp, AppData) or any non-standard path suggests it was copied or staged to evade path-based controls, which raises the
    suspicion beyond the parent-child signal alone.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent instance spawn?
  context: If the interpreter or script host spawned msdt.exe as part of an attack, it likely spawned other processes too.
    Examining the siblings of the alerting process from the same parent instance reveals the full scope of what that parent
    launched -- additional LOLBins, reconnaissance commands, or payload stages point to deliberate orchestration rather than
    a one-off diagnostic.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.command_analysis
  question: Did msdt.exe make any network connections after it launched?
  context: Proxy execution through a signed binary often pulls in a remotely hosted payload or beacons out after the smuggled
    command runs. Network connections initiated by msdt.exe -- which has no routine need to reach external hosts -- point to
    download or command-and-control activity rather than a local diagnostic. Pivot on the alerting process to its outbound
    connections and review the destinations against known-good enterprise endpoints.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
- source: attack:T1218.command_analysis
  question: What modules did msdt.exe load, and were any unsigned or loaded from a user-writable path?
  context: When a signed binary is used to proxy-execute code, the payload frequently arrives as a DLL or scriptlet loaded into
    the process. Reviewing the modules msdt.exe loaded -- specifically any unsigned DLL or one loaded from a temp, AppData, or
    other user-writable directory rather than System32 -- helps distinguish a genuine diagnostic run from in-process code execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_parent_process.baseline
  question: Has this parent binary spawned msdt.exe on this host before?
  context: Some parent-child pairings are routine while others are inherently suspicious. A command interpreter or script host
    spawning msdt.exe is rare in normal operation; a zero or very low historical count for this specific pairing on this host
    marks a novel spawning relationship worth chasing, while a high count may reflect an established support or vendor workflow.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith:
        - \cmd.exe
        - \cscript.exe
        - \mshta.exe
        - \powershell.exe
        - \pwsh.exe
        - \regsvr32.exe
        - \rundll32.exe
        - \schtasks.exe
        - \wmic.exe
        - \wscript.exe
        - \wsl.exe
      selection_msdt:
      - Image|endswith: \msdt.exe
      - OriginalFileName: msdt.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith:
        - \cmd.exe
        - \cscript.exe
        - \mshta.exe
        - \powershell.exe
        - \pwsh.exe
        - \regsvr32.exe
        - \rundll32.exe
        - \schtasks.exe
        - \wmic.exe
        - \wscript.exe
        - \wsl.exe
      selection_msdt:
      - Image|endswith: \msdt.exe
      - OriginalFileName: msdt.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this msdt.exe parent pattern over the past 90 days, and what command-line structures are
    typical?
  context: A LOLBin pattern with no prior history on a host is more suspicious than one used regularly under stable parents.
    This query mirrors the rule's full selection structure scoped to this host, so the count reflects the rule's specific firing
    pattern rather than generic msdt.exe usage. The CommandLine projection lets the analyst compare typical invocation patterns
    against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_parent:
        ParentImage|endswith:
        - \cmd.exe
        - \cscript.exe
        - \mshta.exe
        - \powershell.exe
        - \pwsh.exe
        - \regsvr32.exe
        - \rundll32.exe
        - \schtasks.exe
        - \wmic.exe
        - \wscript.exe
        - \wsl.exe
      selection_msdt:
      - Image|endswith: \msdt.exe
      - OriginalFileName: msdt.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:lolbin.technique_class_lateral
  question: Were any other proxy-execution LOLBins used on this host recently?
  context: Operators who abuse one signed binary for proxy execution often use several. If msdt.exe was used to run smuggled
    code, check whether rundll32, regsvr32, mshta, msbuild, cmstp, installutil, or similar binaries were also used to execute
    code in the same window. Multiple proxy-execution LOLBins on one host strengthens the case for deliberate activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\installutil.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\control.exe'
          - '\infdefaultinstall.exe'
          - '\mavinject.exe'
          - '\wmic.exe'
          - '\xwizard.exe'
          - '\ie4uinit.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- a malicious document opening, defense
    tampering, reconnaissance, or credential access -- may reveal the stages that preceded the msdt.exe execution and help
    reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
    - id: T1036
      name: Masquerading
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: parent_delivery
      description: A command interpreter or script host (the parent) is driven by a document, scriptlet, or operator to invoke msdt.exe
      mapped_to: Q4
      source: template:parent_child.parent_process_legitimacy
    - stage: 2
      label: proxy_invocation
      description: msdt.exe is launched with parameters that smuggle attacker-supplied code through the signed binary
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_execution
      description: The proxied payload runs - loading a module and/or reaching out over the network
      mapped_to: Q10
      source: attack:T1218.command_analysis
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q7
      source: template:lolbin.renamed_binary_check
      coverage_type: direct
    - dimension: execution_context
      technique: T1036
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: time_correlation
      technique: T1036
      covered_by: Q8
      source: template:parent_child.sibling_processes
      coverage_type: direct
  mutable_dispositions:
    - element: OriginalFilenameMismatch
      dimension: tuning_parameter
      specific_values: "Compare Image name vs OriginalFilename (rule already matches OriginalFileName: msdt.exe)"
      disposition: query_injection
      target_question: Q6
      notes: "OriginalFileName projected in true_identity query; mismatch surfaces a renamed msdt.exe copy"
    - element: KnownSystemUtilityPaths
      dimension: tuning_parameter
      specific_values: "System32 / SysWOW64 expected install dirs"
      disposition: context
      target_question: Q7
      notes: "Expected-path framing in renamed_binary_check context; no fixed path value to inject into entity-scoped query"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only - correlation window between events"
      disposition: context
      target_question: Q8
      notes: "Sibling-process window (+/-15m) embodies the time-correlation tuning"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "11 suspicious parents already enumerated in the rule selection_parent block"
      disposition: query_injection
      target_question: Q14
      notes: "Parent enumeration carried verbatim in reflected historical baseline; parent pivot also in Q4/Q11/Q13"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "msiexec.exe, regsvr32.exe, rundll32.exe, etc. (signed proxy binaries)"
      disposition: query_injection
      target_question: Q15
      notes: "Proxy-execution LOLBin list in technique_class_lateral selection_img"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, script paths, DLL entrypoints (rationale - no fixed regex)"
      disposition: context
      target_question: Q3
      notes: "Rule carries no CommandLine values; mechanism_decode context directs decode of /af, answer files, encoded args, URLs"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only - suppress legit enterprise domains"
      disposition: context
      target_question: Q9
      notes: "Network-connection question context directs comparison of destinations against known-good enterprise endpoints"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (per hint) - parent_child.parent_process_legitimacy and cue_parent_process.mechanism_decode subsume the process-chain pivot"
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.follow_on
      reason: "parent_child.sibling_processes (exact ParentProcessGuid instance) is more precise than the parent-binary-name follow_on pivot"
      kept: template:parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "overlaps sibling_processes (same-parent activity in window); host_context.precursor and follow-on network question cover surrounding-activity dimension"
      kept: template:parent_child.sibling_processes
    - dropped: cue_user_actor.clustering
      reason: "user-network-wide clustering and its svc substitute add two near-duplicate aggregations; baseline pair already covers user vs launch-mechanism scoping, kept question budget for image_load/network_connection logsource coverage"
      kept: template:cue_user_actor.baseline
    - dropped: cue_user_actor.clustering_svc
      reason: "dropped alongside its clustering pair (clustering not included); baseline_svc retained for the service-principal launch-mechanism pivot"
      kept: template:cue_user_actor.baseline_svc
    - dropped: cue_process_tool.baseline
      reason: "host-runs-this-tool baseline subsumed by reflected historical_lolbin_baseline (rule-specific pattern count on host) and cue_parent_process.baseline (parent-child pairing)"
      kept: template:lolbin.historical_lolbin_baseline
    - dropped: universal.endpoint_protection_health
      reason: "cue_host_context.precursor present and rule is proxy-execution-centric without a credential/impact disposition hinging on prior tamper; generic host-tamper sweep does not earn a slot here"
      kept: template:cue_host_context.precursor
    - dropped: masquerading.binary_origin
      reason: "file_event basename-arrival question targets dropped renamed tools; msdt.exe is a stock OS binary, so a 30d basename file_event returns OS-install noise rather than a delivery signal. Renamed-copy detection covered by true_identity (OriginalFileName/hash) and renamed_binary_check (path)"
      kept: template:masquerading.true_identity
    - dropped: masquerading.renamed_binary_activity
      reason: "child-of-renamed-binary thread overlaps sibling_processes and the msdt network/module questions; scope-across-hosts thread overlaps the reflected and technique_class_lateral baselines"
      kept: template:parent_child.sibling_processes
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair - non-deduplicable; both emitted (Q12 user-pivot, Q13 launch-mechanism substitute)"
      kept: both
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "parent_process is primary — drop cue_process_tool:process_chain"
      disposition: acted-on
      reason: >-
        cue_process_tool.process_chain dropped (see dedup_decisions); parent pivot
        carried by Q4 (parent_process_legitimacy) and Q5 (parent mechanism_decode).
    - hint: "LOLBin rule — check for renamed binary and other LOLBins achieving same objective"
      disposition: acted-on
      reason: >-
        Renamed-copy check in Q6 (true_identity OriginalFileName/hash) and Q7
        (renamed_binary_check path); other proxy-execution LOLBins in Q15
        (technique_class_lateral).
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted-on
      reason: >-
        Authored Q10 (attack:T1218.command_analysis) with category: image_load,
        scoped to the alerting msdt.exe ProcessGuid, to surface unsigned or
        user-writable-path modules loaded into the proxy-execution binary.
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted-on
      reason: >-
        Authored Q9 (attack:T1218.command_analysis) with category: network_connection,
        scoped to the alerting msdt.exe ProcessGuid, to surface payload-download / C2
        connections from the proxy-execution binary.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - masquerading
    - parent_child
    - lolbin
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      network_connection: 1
      image_load: 1
      sigma_alert: 1
