name: Visual Basic Command Line Compiler Usage
id: 0d1e2f3a-4b5c-6d7e-8f90-1a2b3c4d5e6f
description: |
  Detects the Visual Basic command-line compiler (vbc.exe) spawning the Windows
  Resource to Object Converter (cvtres.exe). The rule fires when a process whose
  parent image ends in \vbc.exe creates a child whose image ends in \cvtres.exe,
  which is the on-host signal that vbc.exe successfully compiled Visual Basic
  source into an object file. Compilation on an endpoint is normal on developer
  workstations and build servers but unusual elsewhere; it is also a known
  defense-evasion primitive in which an actor delivers source code as text,
  compiles it locally to evade signature and reputation checks, and then runs the
  freshly built binary. The investigation should determine what source file was
  compiled, where the compiler output was written, what spawned vbc.exe, whether
  the host and user perform compilation routinely, and whether a newly written
  binary executed or made network connections shortly afterward.
type: detection
detection_id: 7b10f171-7f04-47c7-9fa2-5be43c76e535
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did a process whose parent is vbc.exe spawn cvtres.exe on this host?
  context: Before investing investigation time, confirm the alert is a true positive.
    The rule fires when the parent image ends in \vbc.exe and the child image ends in
    \cvtres.exe - the signal that the Visual Basic compiler ran the resource-to-object
    converter during a successful compilation. Confirm both ends of the pair and note
    the path vbc.exe ran from; the legitimate compiler lives under
    C:\Windows\Microsoft.NET\Framework, so a vbc.exe running from a user-writable or
    unusual path is itself notable.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
- source: template:parent_child.parent_process_legitimacy
  question: What spawned vbc.exe, and is that parent chain expected for compilation on
    this host?
  context: The vbc.exe parent is the core signal for this detection. Legitimate
    compilation is typically driven by a developer's IDE, a build process, or an
    installer that compiles components at setup time. Tracing vbc.exe's own origin
    reveals whether the chain is a normal build workflow or whether a script host, an
    Office application, or an unexpected process drove the compile - the latter is the
    compile-after-delivery pattern where source is dropped and built locally to evade
    detection.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to compile code with the Visual Basic
    compiler?
  context: On-host compilation is routine on developer workstations and build servers
    and for accounts that do software development, but most endpoints and most user
    accounts never run vbc.exe. Knowing whether the acting account and the host
    function involve building software is often the fastest path to separating normal
    development activity from source dropped and compiled locally to evade signature
    and reputation checks.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1027.004.source_file
  question: What Visual Basic source file did vbc.exe compile, and what output binary
    did it produce?
  context: The compiler reads source from the path passed on the vbc.exe command line
    (the .vb file) and writes an executable or library to its output path. Read
    vbc.exe's full command line to recover the source path, any /target and /out
    options, and referenced assemblies. Source compiled from a temporary, public, or
    user-profile directory rather than a project tree points to delivered-and-compiled
    code; the output path is where the freshly built binary will be found for hashing
    and analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '\vbc.exe'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - ParentImage
- source: template:obfuscation.encoded_payload_decode
  question: Is the compiled source or its command line encoded or obfuscated, and what
    operation does the decoded content perform?
  context: Compile-after-delivery is itself an obfuscation step - source delivered as
    text evades binary signatures until it is built locally. The source file or the
    compiler command line may also carry encoded or concatenated content. Read the
    vbc.exe command line for inline source, base64 or hex blobs, character
    concatenation, or referenced files, and review the .vb source on disk. Decoding
    reveals the built binary's intent - download cradle, loader, reverse shell, or
    credential theft.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.evidence_preservation
  question: Is the compiler output binary still on disk for analysis?
  context: A successful vbc.exe/cvtres.exe compilation writes an object file and an
    output binary to the path given on the compiler command line. If that binary still
    exists on disk it can be hashed, reputation-checked, and submitted for analysis.
    Scope the search to the output directory recovered from the compiler command line
    rather than all file events on the host.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: attack:T1027.004.followon_execution
  question: Did a newly written binary execute on this host shortly after the
    compilation?
  context: The point of compile-after-delivery is to run the freshly built binary. In
    the window right after vbc.exe/cvtres.exe runs, look for execution of a new
    executable - especially one launched from a temporary, public, or user-profile
    directory or from the compiler's output path. Execution of a binary that did not
    exist before the compile links the compile step to its payload and is the
    transition from building code to running it.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: attack:T1027.004.followon_network
  question: Did this host make outbound network connections shortly after the
    compilation?
  context: A binary compiled on delivery commonly reaches out for command and control,
    additional payloads, or exfiltration once it runs. Outbound connections in the
    window after the compile - particularly from a process running out of a temporary
    or user-writable directory or from the compiler output path - tie the freshly
    built binary to network activity. Exclude known-good system processes when reading
    the results so operator-driven connections stand out.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
      filter_system:
        Image|endswith:
          - '\svchost.exe'
          - '\System'
      condition: selection and not filter_system
    fields:
      - Image
      - DestinationIp
      - DestinationPort
- source: template:parent_child.sibling_processes
  question: What other child processes did the same vbc.exe instance spawn?
  context: If the vbc.exe invocation was driven by a malicious or scripted parent, that
    compiler instance and its parent may have spawned other processes too. Examining
    the children of the exact vbc.exe instance reveals whether the compile was a single
    step or one of several actions - additional compiles, the execution of the built
    binary, or staging tools launched alongside it point to orchestration rather than
    an isolated build.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \vbc.exe
        Image|endswith: \cvtres.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \vbc.exe
        Image|endswith: \cvtres.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the acting account's activity across all hosts reveals whether the
    compilation was a single event or part of activity spanning multiple endpoints. If
    the same user triggered similar activity on other hosts, the scope of the incident
    is larger than one machine and may indicate a coordinated rollout of
    delivered-and-compiled code.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \vbc.exe
        Image|endswith: \cvtres.exe
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the activity surrounding the
    compilation reveals what led to it and what followed - the parent that staged the
    source, reconnaissance, or the execution of the built binary. Novel parent-child
    pairs at low counts are what to scan for against the high-count noise of routine
    system processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - reconnaissance, delivery of a script
    or archive, or defense tampering - may reveal the attack stages that preceded the
    compilation and help reconstruct the full timeline. Compiling delivered source is
    often one step in a sequence, so prior alerts give the context for why code was
    built on this host.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_parent_process.scope
  question: Have other hosts run the vbc.exe-to-cvtres.exe compilation pattern in the
    past week, and does the cross-host distribution look like consistent build
    automation or one-off activity?
  context: This query counts the vbc.exe-to-cvtres.exe pair fleet-wide with no host
    scope. Many hosts running the same pattern points to a software-deployment or
    build workflow that compiles components by design; one host or a small unrelated
    set points to manual or targeted activity. Use the host distribution to calibrate
    severity and decide whether this fits a build baseline or stands out as isolated
    on-host compilation.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentName|expand: '%ParentName%'
        Image|endswith|expand: '\cvtres.exe'
      condition: selection
    fields:
      - host.name
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1027.004
      name: Compile After Delivery
      role: primary
    - id: T1027
      name: Obfuscated Files or Information
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: compile_invocation
      description: "vbc.exe spawns cvtres.exe - the resource-to-object conversion that signals a successful local compilation of Visual Basic source (the act the rule detects)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: source_to_output
      description: "Identify the source file vbc.exe compiled and the output binary it produced, recovered from the compiler command line"
      mapped_to: Q4
      source: attack:T1027.004.source_file
    - stage: 3
      label: followon_execution_or_c2
      description: "The freshly built binary executes and/or makes outbound network connections in the window after the compile"
      mapped_to: Q7
      source: attack:T1027.004.followon_execution
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1027.004
      covered_by: Q2
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1027.004
      covered_by: Q7
      source: attack:T1027.004.followon_execution
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1027.004
      covered_by: Q4
      source: attack:T1027.004.source_file
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q5
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1027
      covered_by: Q3
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (filter for unexpected, non-dev parents launching compilers)"
      disposition: context
      target_question: Q2
      notes: "No concrete values in the analytic. Realized as the parent-legitimacy judgment in parent_process_legitimacy, which traces what spawned vbc.exe and whether a dev/build workflow or an unexpected parent (script host, Office, mail client) drove the compile."
    - element: OutputDirectoryPath
      dimension: tuning_parameter
      specific_values: "C:\\Users\\Public\\, %TEMP%, Desktop"
      disposition: context
      target_question: Q4
      notes: "Sensitive write-zone paths named in the analytic. The compiler output path is read from the vbc.exe command line, not from a fixed selection, so the values inform the context (temporary/public/profile directories flag delivered-and-compiled code) on the source_file question and the evidence_preservation %file_paths% scope rather than a hardcoded path selection."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window between compile and subsequent execution or C2)"
      disposition: context
      target_question: Q7
      notes: "No concrete value in the analytic. Realized as the +1h forward scope on the followon_execution and followon_network questions linking the compile to subsequent execution and outbound connections."
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (entropy threshold to distinguish obfuscation from legitimate compression)"
      disposition: context
      target_question: Q5
      notes: "T1027 secondary; no concrete value in the analytic and entropy of the built binary is not directly queryable from process_creation. Realized as the decode/obfuscation judgment in encoded_payload_decode, which asks what the compiled source and command line decode to."
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "rationale only (LOLBins or admin tools misused for obfuscation)"
      disposition: context
      target_question: Q2
      notes: "T1027 secondary; no concrete values in the analytic. Realized as the parent-legitimacy judgment in parent_process_legitimacy (what spawned vbc.exe) and the launch-mechanism baseline questions."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). The rule has no commandline_values, so the discriminating signal is the cvtres.exe child under a vbc.exe parent; the baseline queries scope on Image \\cvtres.exe (and ParentName for the svc variant)."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject-pivot and launch-mechanism-pivot). The svc variant scopes on the vbc.exe parent spawning cvtres.exe."
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (hint 1) — parent_child.parent_process_legitimacy subsumes it with a more specific ParentProcessGuid lookup and grandparent (what-spawned-vbc.exe) context."
      kept: template:parent_child.parent_process_legitimacy
    - dropped: cue_parent_process.mechanism_decode + cue_process_tool.mechanism_decode
      reason: "Phase 2 mechanism decode for this rule is the compiler command line (what source was compiled, what output was produced). That is covered by attack:T1027.004.source_file (vbc.exe command line, host+Image scope) and obfuscation.encoded_payload_decode, which are more specific to the compile-after-delivery operation than a generic binary-decode question."
      kept: attack:T1027.004.source_file + template:obfuscation.encoded_payload_decode
    - dropped: cue_parent_process.follow_on + cue_parent_process.clustering
      reason: "Parent-scoped host activity is covered by parent_child.sibling_processes (exact ParentProcessGuid instance) and cue_host_context.clustering (Image+ParentImage host aggregation); the parent-binary-name versions would duplicate that coverage at coarser scope."
      kept: template:parent_child.sibling_processes + template:cue_host_context.clustering
    - dropped: cue_parent_process.baseline + cue_process_tool.baseline
      reason: "Parent-child and host baselines are subsumed by cue_user_actor.baseline_svc (launch-mechanism baseline, vbc.exe parent spawning cvtres.exe, -90d) and cue_parent_process.scope (fleet baseline of the same pair); both constrain to the rule's actual parent-child pair rather than a single generic Image."
      kept: template:cue_user_actor.baseline_svc + template:cue_parent_process.scope
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "recommended priority. This rule fires on on-host compilation (defense evasion via compile-after-delivery), not on an execution or credential-access action whose disposition turns on whether AV/EDR was disabled first. cue_host_context.precursor already carries prior-alert (including tamper) context for this host, and no commandline-tamper signal is part of this detection. Trimmed to stay within the 14-18 target."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - obfuscation
    - parent_child
    - cue_parent_process
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 16
    source_counts:
      template: 13
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      file_event: 1
      network_connection: 1
      sigma_alert: 1
