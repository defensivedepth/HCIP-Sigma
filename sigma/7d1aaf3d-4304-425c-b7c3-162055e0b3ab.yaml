name: Potential Data Exfiltration Activity Via CommandLine Tools
id: 60fe87e5-ae9e-4917-821a-0e8e45815471
description: |
  Detects command-line web utilities being used to push data outbound. The rule fires when a
  recon/staging payload pattern is present AND at least one of several transfer shapes matches:
  powershell.exe / cmd.exe issuing an HTTP POST with URL, method, and body flags; curl.exe with a
  URL and data flags; or wget.exe with --post-data / --post-file. The payload half looks for
  reconnaissance output or file content being read for the body (whoami, ipconfig, systeminfo,
  ToBase64String, type C:\...). Legitimate context exists -- automation, monitoring agents, and
  developers do POST data to APIs from the command line -- so the investigation determines whether
  the outbound request carries host or domain reconnaissance, what destination received it, and
  whether the launch context (parent, user, host) is consistent with sanctioned automation.
type: detection
detection_id: 7d1aaf3d-4304-425c-b7c3-162055e0b3ab
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match the outbound-transfer-plus-recon pattern this rule detects?
  context: >-
    Confirm the alert is a true positive before investing time. The rule fires when a recon or
    file-content payload is present AND at least one transfer shape matches -- EITHER powershell/cmd
    issuing an HTTP POST (URL + method + body flags together), OR curl.exe with a URL and data flags,
    OR wget.exe with --post-data/--post-file. Note which branch fired and which payload tokens
    appear (whoami, ipconfig, systeminfo, ToBase64String, type C:\...): the transfer branch tells you
    which binary did the sending, and the payload tokens tell you what was being staged for the body.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to push data outbound via command-line web tools?
  context: >-
    Legitimate automation, monitoring agents, and developers do POST data to APIs from curl,
    powershell, or wget. Knowing whether this user role and host function normally make scripted
    outbound web requests is often the fastest way to separate sanctioned automation from operator
    activity. A workstation with no automation role POSTing reconnaissance output is far more
    concerning than a known integration host calling a known API.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.evidence_preservation
  question: Are the files referenced in the command line still on disk for analysis?
  context: >-
    The payload half of this rule reads file content into the request body (type C:\... > file
    patterns, Get-Content). If the command references a source file or an intermediate output file,
    checking whether it still exists enables hash lookup, content review, and a determination of
    exactly what data was selected for the body. File paths here are derived from the command line,
    so the query scopes to those extracted paths rather than all file activity on the host.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this command do? Decode the full request -- URL, method, and body.
  context: >-
    The full command line reveals the destination and what was sent. Decode the URL (where did the
    data go -- internal API, known SaaS endpoint, or a raw external IP?), the method (POST signals an
    upload rather than a fetch), and the body expression. Body content built from whoami, ipconfig,
    systeminfo, nltest, or ToBase64String means host or domain reconnaissance was staged for
    transfer. This host+binary scope captures sequential invocations that together form one logical
    exfil operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Was the request body encoded, and what does it decode to?
  context: >-
    A common shape here wraps reconnaissance output in
    [System.Convert]::ToBase64String([System.Text.Encoding]::ASCII.GetBytes((<recon>))) and POSTs the
    result, so the body on the wire is base64 even though the command itself is readable. Decode the
    body expression to recover what was actually sent -- a whoami result, domain trust enumeration, a
    file's contents -- and identify the destination URL or IP it was sent to. Distinguishing
    benign API payloads from staged host reconnaissance turns on what the decoded body contains.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:data_staging_exfil.data_scope
  question: What files or data sources were selected to populate the request body?
  context: >-
    The command-line arguments reveal what was targeted for the body: a specific file read with type
    or Get-Content, output of reconnaissance commands (systeminfo, tasklist, netstat, nltest), or a
    file path under C:\. Broad or sensitive selections (credential stores, business document
    directories, full enumeration output) raise severity and determine which data owners need to be
    notified. The query surfaces files the sending binary touched beyond what is visible in the
    command line itself.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
- source: template:data_staging_exfil.archive_destination
  question: Where did the data go, and was anything written to disk along the way?
  context: >-
    Outbound transfer typically has a destination encoded in the request and may stage an
    intermediate file before sending. Checking file events from the sending binary reveals any local
    staging (an output file from a type/redirect, a temporary archive), while the destination in the
    decoded URL or IP reveals the exfiltration target -- an internal endpoint, a known cloud service,
    or an unfamiliar external server. Pairing the local writes with the destination establishes the
    full transfer path.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:data_staging_exfil.encryption_password
  question: Was the body encoded or obfuscated to hide what was being sent?
  context: >-
    Encoding the body (ToBase64String, GetBytes wrapping) keeps the data opaque to inspection while
    in transit and is part of this rule's payload pattern. Identify how the body was transformed --
    base64, byte-array conversion, or no encoding at all -- and whether reconnaissance output was
    deliberately wrapped before sending. A request body that base64-encodes whoami or domain-trust
    output is a stronger indicator of staged exfiltration than an unencoded API call.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this tool, and is that parent chain expected?
  context: >-
    The parent reveals how the transfer was launched. A scheduled task, monitoring agent, or
    deployment tool spawning curl or powershell to POST telemetry is routine. An Office application,
    mshta, wscript, or a web-server process (w3wp, a shell dropped by a web shell) spawning the same
    command points to exploitation or hands-on-keyboard activity. Walk up from the immediate parent
    to see what spawned it and what else it launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened before this execution?
  context: >-
    Operators frequently disable or bypass PowerShell controls before running a payload: AMSI
    patching, execution-policy bypass flags, language-mode escape, or a v2 downgrade that strips
    logging. Finding these precursor events on the host strengthens the case that the outbound
    request is part of an operation rather than sanctioned automation. Tamper that immediately
    precedes a recon-and-POST sequence is a meaningful escalation signal.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script?
  context: >-
    Script block logging records the script content PowerShell actually executed, even when the
    body expression is built dynamically or base64-wrapped. It provides ground truth about what was
    assembled for the request body and where it was sent, bypassing command-line obfuscation. If
    PowerShell operational logs are not ingested in this environment, treat zero results as a
    visibility gap rather than evidence nothing ran, and fall back to decoding the body from the
    alerting command line directly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: attack:T1059.001.image_load
  question: Did this PowerShell process load .NET or web-stack assemblies consistent with in-memory transfer?
  context: >-
    PowerShell web requests run through .NET. Loaded modules such as System.Net.Http, System.Net, or
    System.Management.Automation tie the transfer to in-process .NET execution rather than a separate
    binary, and rare or never-before-seen assemblies loaded into the same process can indicate a
    fileless cradle assembling the request. This crosses from the command line into module-load
    evidence, which confirms how the request was constructed when the command line alone is ambiguous.
  range: +/-5m
  query: |
    aggregation: true
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    Even routine automation can match this pattern. The query mirrors the rule's full selection
    structure scoped to this host, so the resulting parent and user distribution reflects how this
    specific recon-and-POST pattern has historically run here -- not generic powershell or curl usage.
    A new parent process, an unexpected account, or a first-ever appearance of the pattern on this
    host is the anomaly worth chasing; a stable parent/user pair points to established automation.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_iwr:
        Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
        - \cmd.exe
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - 'irm '
        - 'iwr '
        - 'wget '
        CommandLine|contains|all:
        - ' -ur'
        - ' -me'
        - ' -b'
        - ' POST '
      selection_curl:
        Image|endswith: \curl.exe
        CommandLine|contains: --ur
      selection_curl_data:
        CommandLine|contains:
        - ' -d '
        - ' --data '
      selection_wget:
        Image|endswith: \wget.exe
        CommandLine|contains:
        - --post-data
        - --post-file
      payloads:
      - CommandLine|re:
        - net\s+view
        - sc\s+query
      - CommandLine|contains:
        - Get-Content
        - GetBytes
        - hostname
        - ifconfig
        - ipconfig
        - netstat
        - nltest
        - qprocess
        - systeminfo
        - tasklist
        - ToBase64String
        - whoami
      - CommandLine|contains|all:
        - 'type '
        - ' > '
        - ' C:\'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: ((selection_iwr or all of selection_curl* or selection_wget) and payloads) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run command-line web tools that push data outbound?
  context: >-
    Compare this user's current action against their historical behavior across all hosts. A user
    who has never invoked curl, wget, or powershell web requests is more suspicious than one who does
    so as part of a known role. Combine the historical count with what you know about the account --
    a service or automation account with a long history of outbound POSTs reads very differently from
    an interactive user account with none.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+binary combination historically routine on this host or across the environment?
  context: >-
    When the alert fires under SYSTEM, a service account, or a computer account, user identity does
    not attribute the behavior -- the meaningful pivot is the launch mechanism. Whether this parent
    process spawning this binary is a historically common pair tells you if the activity is
    established automation or a new mechanism. A parent/binary pair that already accounts for
    thousands of prior events is vendor or OS automation; a new or rare pair is the deviation worth
    chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts in the surrounding window reveals whether the
    outbound transfer is isolated or part of broader movement. The same account running recon,
    staging, and POSTs on multiple endpoints points to a wider scope of compromise; activity confined
    to one host narrows it. This is the immediate cross-host view, distinct from the long-term
    historical baseline.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+binary combination fire on other hosts in the same window?
  context: >-
    Under a service principal, asking what else the user did across the network returns agent
    telemetry noise. The meaningful question is whether the same launch mechanism -- this parent
    spawning this binary -- fired on other hosts in the alert window. Multiple hosts in the same
    window points to scheduled or pushed automation running the outbound transfer fleet-wide; a
    single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's full selection structure with no host scope, so the result is a
    fleet-wide count of this specific recon-and-POST pattern -- not generic usage of the binaries.
    Many hosts firing the same pattern points to scripted deployment or vendor automation that should
    be confirmed and tuned; one host or a small unrelated set points to manual operator activity.
    Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_iwr:
        Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
        - \cmd.exe
        CommandLine|contains:
        - 'curl '
        - Invoke-RestMethod
        - Invoke-WebRequest
        - 'irm '
        - 'iwr '
        - 'wget '
        CommandLine|contains|all:
        - ' -ur'
        - ' -me'
        - ' -b'
        - ' POST '
      selection_curl:
        Image|endswith: \curl.exe
        CommandLine|contains: --ur
      selection_curl_data:
        CommandLine|contains:
        - ' -d '
        - ' --data '
      selection_wget:
        Image|endswith: \wget.exe
        CommandLine|contains:
        - --post-data
        - --post-file
      payloads:
      - CommandLine|re:
        - net\s+view
        - sc\s+query
      - CommandLine|contains:
        - Get-Content
        - GetBytes
        - hostname
        - ifconfig
        - ipconfig
        - netstat
        - nltest
        - qprocess
        - systeminfo
        - tasklist
        - ToBase64String
        - whoami
      - CommandLine|contains|all:
        - 'type '
        - ' > '
        - ' C:\'
      condition: (selection_iwr or all of selection_curl* or selection_wget) and payloads
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: interpreter_invocation
      description: "Scripting/command interpreter (powershell, cmd, curl, wget) launched with transfer flags"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: "Encoded/obfuscated or dynamically-built request body assembled and decoded"
      mapped_to: Q5
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: module_load
      description: ".NET/web-stack assemblies loaded for in-process request construction"
      mapped_to: Q12
      source: attack:T1059.001.image_load
    - stage: 4
      label: network_transfer
      description: "Outbound POST of recon/file-content body to a destination"
      mapped_to: Q7
      source: template:data_staging_exfil.archive_destination
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: template:cue_user_actor.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q13
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rationale only (-enc/-nop tuning guidance; rule's own encoding tokens ToBase64String/GetBytes are in detection)"
      disposition: context
      target_question: Q5
      notes: "Decode question keys on the rule's ToBase64String/GetBytes body-wrap pattern; no extra CommandLine|contains injection needed (entity-scoped ProcessGuid query auto-satisfies)."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (Office/mshta abnormal-parent guidance)"
      disposition: context
      target_question: Q9
      notes: "process_chain context names Office/mshta/wscript/web-server parents as the abnormal cases."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (off-hours / non-maintenance scoping)"
      disposition: context
      target_question: Q16
      notes: "Cross-host clustering uses +/-1h window; off-hours framing in context."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "System.Net.Http, System.Net, System.Management.Automation (rare/never-before-seen .NET assemblies)"
      disposition: new_question
      target_question: Q12
      notes: "Drove the attack:T1059.001.image_load question; assembly names named in context."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (4104 length-threshold noise filter)"
      disposition: context
      target_question: Q11
      notes: "Script-block-logging question surfaces 4104 ground truth; threshold is a tuning rationale, not directly queryable."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair -- non-deduplicable; emitted BOTH (Q14 user-pivot, Q15 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair -- non-deduplicable; emitted BOTH (Q16 user-pivot, Q17 parent+binary substitute)"
      kept: both
    - dropped: cue_host_context.clustering
      reason: "host-wide same-window activity is covered by user_actor.clustering (Q16, net-wide same window) plus the fleet scope question (Q18); a host-only clustering view would duplicate the surrounding-activity angle without adding a distinct pivot"
      kept: cue_user_actor.clustering
    - dropped: cue_process_tool.clustering
      reason: "host-wide concurrent-tool view duplicates the surrounding-window activity already provided by user_actor.clustering (Q16)"
      kept: cue_user_actor.clustering
    - dropped: cue_process_tool.baseline
      reason: "generic host-binary frequency is subsumed by baseline_deviation (Q13), which counts the rule's specific firing pattern on this host rather than all powershell/curl usage"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "child-process spawning is not part of this rule's exfil chain (the transfer is terminal); slot reallocated to data-staging and user-actor coverage"
      kept: none
    - dropped: cue_host_context.precursor
      reason: "prior-alert timeline overlaps with powershell.powershell_security_bypass (Q10, prior tamper on host) and user_actor.clustering (Q16); dropped to hold at the 18-question cap"
      kept: template:powershell.powershell_security_bypass
    - dropped: cue_artifact.mechanism_decode / cue_artifact.artifact_verification / cue_artifact.baseline
      reason: "the cue_artifact derivation came from the 'type C:\\ > ' payload token; on-disk artifact content/hash/baseline is covered by evidence_preservation (Q3) and data_scope (Q6). The rule's 'file' is read INTO an outbound body, not a downloaded payload, so hash-reputation and 90d file-baseline questions add little"
      kept: template:universal.evidence_preservation
    - dropped: download_tool.download_success_verification / download_tool.payload_analysis / download_tool.directory_activity
      reason: "download_tool set was confirmed on binary curl.exe, but here curl/wget/powershell POST data OUTBOUND rather than download a payload to disk. Inbound-download verification, payload sandboxing, and staging-directory questions do not match the exfil direction; outbound destination + local staging is covered by archive_destination (Q7) and evidence_preservation (Q3)"
      kept: template:data_staging_exfil.archive_destination
  skipped_templates:
    - template: cue_process_tool.mechanism_decode (cue_artifact variant) -- see dedup_decisions
      reason: "cue_artifact derived cue declined; on-disk content covered by Q3/Q6"
  hint_dispositions:
    - hint: "cue_artifact: derived: [A-Za-z]:\\ in CommandLine"
      disposition: declined
      reason: >-
        The derivation fired on the 'type C:\ > ' payload token, but that file is read INTO the
        outbound request body, not dropped as a downloaded artifact. On-disk presence and content of
        any referenced source/output file is covered by evidence_preservation (Q3, %file_paths%) and
        the data-scope question (Q6, files the sending binary touched). A dedicated cue_artifact
        hash/reputation/90d-baseline triad would investigate a downloaded payload that this exfil rule
        does not produce.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' comes from the reference blog summary, not the rule's behavior. The rule detects outbound recon-and-POST exfil, not encryption/impact -- no shadow-copy-deletion, no ransom-note, no mass-file-modification logic to investigate. Impact-stage questions would not align with what fired."
      - id: network_scope
        confidence: low
        reason: "Same incidental 'ransomware' keyword origin. The rule fires on process_creation command-line patterns; it carries no DestinationIp/DestinationPort fields and no network_connection logsource, so a network-scope (C2-beacon-shape) question set has no in-detection anchor here. Outbound destination is investigated via the decoded URL in mechanism_decode (Q4) and archive_destination (Q7) instead."
  technique_sets_applied:
    - universal
    - powershell
    - data_staging_exfil
    - cue_process_tool
    - cue_user_actor
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 13
    logsource_diversity:
      process_creation: 13
      file_event: 3
      powershell: 1
      image_load: 1
