name: CMSTP Execution Process Creation
id: 47254f44-68be-4ce4-9b29-7fb4e309d135
description: |
  Detects a child process spawned by cmstp.exe, the Microsoft Connection Manager Profile Installer.
  cmstp.exe is a signed, on-disk system binary that can execute local or remote VBScript/JScript via a
  weaponized INF file, perform proxy execution of arbitrary commands, and auto-elevate from medium to high
  integrity through its COM interface with no user prompt — making it attractive for application-allowlist
  bypass and UAC bypass. Legitimate CMSTP use is rare in modern enterprises, so any process descending from
  cmstp.exe should be investigated to determine what the child binary is, what INF/SCT/DLL payload drove it,
  and whether network, registry, file, or COM-injection artifacts accompanied the execution.
type: detection
detection_id: 7d4cdc5a-0076-40ca-aac8-f7e714570e47
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Is the alerting process a genuine child of cmstp.exe, and what binary did it launch?
  context: Before investing investigation time, confirm the parent is cmstp.exe and review the child Image,
    its full command line, and integrity level. cmstp.exe spawning a child is the rule's sole trigger, so the
    parent attribution and the identity of the spawned binary are the first facts to establish.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to run cmstp.exe and install connection-manager profiles?
  context: Legitimate CMSTP use is rare in modern enterprises and usually tied to remote-access deployment
    on specific managed endpoints. Knowing whether this user role and host function involve connection-profile
    installation is often the fastest way to separate routine administration from abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What was cmstp.exe's own command line, and which INF or SCT did it process?
  context: The parent cmstp.exe event shows the INF/SCT path or switch it was invoked with — for example a
    /s flag pointing at a local or remote INF whose UnregisterOCXSection invokes scrobj.dll and a .SCT script,
    or a RunPreSetupCommandSection holding an arbitrary executable path. Decoding the parent command line
    identifies the payload the profile installer was driven to execute.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - IntegrityLevel
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the child process do? Decode its command-line arguments.
  context: The child's full command line reveals the action proxied through cmstp.exe. Decode any flags,
    script paths, URLs, or encoded content. A child such as cmd.exe, powershell.exe, wscript.exe, or mshta.exe
    running at high integrity points to UAC bypass or remote-script fetch-and-execute rather than benign
    profile installation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - IntegrityLevel
- source: template:cue_parent_process.baseline
  question: Has cmstp.exe spawned this child binary on this host before?
  context: A cmstp.exe-to-this-child relationship that has never been seen on the host is more suspicious than
    one that recurs. Connection-profile installation is uncommon, so zero or very low historical counts for
    this specific parent-child pairing indicate a novel and likely operator-driven execution.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did cmstp.exe spawn on this host around the alert?
  context: A profile installer driven by an attacker often launches multiple tools in sequence —
    reconnaissance, credential access, or additional payloads. Enumerating every child of cmstp.exe reveals
    the full scope of activity proxied through this signed binary beyond the one process that triggered the
    alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:cue_parent_process.clustering
  question: What other activity is associated with cmstp.exe on this host around the same time?
  context: Beyond child processes, cmstp.exe may have generated other process activity in the same window.
    Correlating that activity within minutes of the alert helps reconstruct the sequence — what configured
    the profile install, what it launched, and what ran immediately after.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is cmstp.exe spawning this child binary on other hosts in the environment?
  context: The same cmstp.exe parent-child pattern appearing across multiple hosts may indicate a coordinated
    rollout, a phishing wave, or lateral reuse of the same INF payload. A single occurrence is more likely
    targeted. Environment-wide comparison calibrates the scope of the activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: attack:T1218.003.network_connection
  question: Did cmstp.exe make outbound network connections around the time of the alert?
  context: A weaponized INF can point cmstp.exe at a remote .SCT or DLL, causing the signed binary itself to
    reach out to an external host to fetch and execute script. cmstp.exe initiating outbound connections is
    abnormal for legitimate local profile installation, so any external destination here is investigative
    signal worth resolving against threat intelligence.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \cmstp.exe
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
- source: attack:T1218.003.process_access
  question: Was the CMSTPLUA or CMLUAUTIL auto-elevating COM interface invoked on this host around the alert?
  context: The direct COM-interface UAC bypass drives the auto-elevated CMSTPLUA interface (CLSID
    3E5FC7F9-9A51-4367-9063-A120244FBEC7) via dllhost.exe, often without cmstp.exe appearing as the parent at
    all. The footprint is a process-access event where the CallTrace references cmlua.dll, or a high-integrity
    cmd.exe spawned by dllhost.exe carrying the CMSTPLUA CLSID in its parent command line. Inspect access
    source, target, granted access, and call trace to confirm or rule out the COM-elevation path.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CallTrace|contains: cmlua.dll
      condition: selection
    fields:
      - SourceImage
      - TargetImage
      - GrantedAccess
      - CallTrace
- source: attack:T1218.003.registry_set
  question: Were connection-manager profile registry values written on this host around the alert?
  context: The RunPreSetupCommandSection UAC bypass leaves a registry footprint where dllhost.exe creates a
    cmmgr32.exe object and sets ProfileInstallPath to C:\ProgramData\Microsoft\Network\Connections\Cm. Registry
    writes touching cmmgr32.exe or that ProfileInstallPath value, especially when not attributable to a known
    profile deployment, corroborate CMSTP-based profile installation as the elevation mechanism.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_target:
        TargetObject|contains:
          - cmmgr32.exe
          - \Network\Connections\Cm
      condition: selection_host and selection_target
    fields:
      - Image
      - TargetObject
      - Details
- source: attack:T1218.003.file_event
  question: Were INF, SCT, or DLL files written on this host shortly before cmstp.exe executed?
  context: Abuse usually requires dropping a weaponized INF, and often an accompanying .SCT script or DLL, to
    disk before invoking cmstp.exe. A newly written .inf, .sct, or .dll — particularly in a user-writable
    location such as a user profile, ProgramData, or a temp directory — staged just before the execution
    identifies the payload and its origin.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_ext:
        TargetFilename|endswith:
          - '.inf'
          - '.sct'
          - '.dll'
      condition: selection_host and selection_ext
    fields:
      - Image
      - TargetFilename
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run cmstp.exe or install connection-manager profiles?
  context: Compare this user's current action against their historical behavior. A user who has never driven
    cmstp.exe before is more suspicious than one with a recurring profile-deployment role. User role and
    typical toolset should align with connection-manager administration.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is the cmstp.exe parent historically routine for this child binary on this host or environment-wide?
  context: When the alert fires under a service principal, user identity does not attribute the behavior. The
    meaningful pivot is the launch mechanism — whether cmstp.exe spawning this child binary is historically
    common here. A new or rare cmstp.exe parent for this binary is the deviation worth chasing; a pairing that
    already accounts for many prior events points to vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining process activity around the alert reveals what led to
    the cmstp.exe execution and what followed it. Aggregating by Image and ParentImage collapses routine
    agent telemetry into high-count rows while novel parent-child pairs stand out at low counts — those are
    what to scan for reconnaissance, escalation, or persistence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, or credential access — may
    reveal the attack stages that preceded the CMSTP execution and help reconstruct the full timeline. UAC
    bypass and proxy execution are typically mid-chain steps rather than the initial foothold.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.003
      name: CMSTP
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: cmstp_execution_with_payload
      description: "cmstp.exe executes with arguments pointing at a local/remote INF/SCT/DLL payload and spawns a child"
      mapped_to: Q3
      source: template:cue_parent_process.mechanism_decode
    - stage: 2
      label: outbound_fetch
      description: "cmstp.exe makes outbound network connections to retrieve remote .SCT/DLL script"
      mapped_to: Q9
      source: attack:T1218.003.network_connection
    - stage: 3
      label: com_interface_injection
      description: "Auto-elevated COM interface (CMSTPLUA/CMLUAUTIL) invoked via dllhost.exe for UAC bypass"
      mapped_to: Q10
      source: attack:T1218.003.process_access
    - stage: 4
      label: registry_profile_registration
      description: "Registry writes registering the CMSTP profile (cmmgr32.exe / ProfileInstallPath)"
      mapped_to: Q11
      source: attack:T1218.003.registry_set
    - stage: 5
      label: payload_staging
      description: "Suspicious INF/SCT/DLL files written to disk prior to cmstp.exe execution"
      mapped_to: Q12
      source: attack:T1218.003.file_event
  dimension_coverage:
    - dimension: time_correlation
      technique: T1218.003
      covered_by: Q9
      source: attack:T1218.003.network_connection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.003
      covered_by: Q11
      source: attack:T1218.003.registry_set
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_parent_process.baseline
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q12
      source: attack:T1218.003.file_event
      coverage_type: indirect
  mutable_dispositions:
    - element: INFPathRegex
      dimension: tuning_parameter
      specific_values: ".inf / .sct / .dll payload extensions"
      disposition: query_injection
      target_question: Q12
      notes: "INF/SCT/DLL extensions injected into file_event TargetFilename|endswith selection"
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (allowlist tuning, no concrete IPs in brief)"
      disposition: context
      target_question: Q9
      notes: "Network question surfaces DestinationIp/Hostname for the analyst to compare against known-good; no concrete allowlist values to inject"
    - element: COMInterfaceGUIDs
      dimension: tuning_parameter
      specific_values: "CMSTPLUA CLSID 3E5FC7F9-9A51-4367-9063-A120244FBEC7; cmlua.dll (CMLUAUTIL)"
      disposition: query_injection
      target_question: Q10
      notes: "cmlua.dll injected into process_access CallTrace|contains; CMSTPLUA CLSID stated in context"
    - element: RegistryKeyAllowlist
      dimension: tuning_parameter
      specific_values: "cmmgr32.exe object, ProfileInstallPath C:\\ProgramData\\Microsoft\\Network\\Connections\\Cm"
      disposition: query_injection
      target_question: Q11
      notes: "cmmgr32.exe and \\Network\\Connections\\Cm injected into registry_set TargetObject|contains"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlate within N seconds)"
      disposition: context
      target_question: Q9
      notes: "Realized as +/-15m correlation windows on network/process_access/registry questions"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "cmstp.exe (parent), dllhost.exe (COM surrogate)"
      disposition: query_injection
      target_question: Q5
      notes: "Parent baseline pivots on cmstp.exe; dllhost.exe surrogate path noted in Q10 context"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (cmstp.exe is the in-scope signed binary)"
      disposition: context
      target_question: Q1
      notes: "Rule is already scoped to the signed binary cmstp.exe; no list to inject"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (suspicious args: URLs, script paths, DLL entrypoints)"
      disposition: context
      target_question: Q4
      notes: "Child mechanism_decode and cmstp parent decode direct the analyst to inspect URLs/script paths in the command line"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Network question context directs comparison of destinations against known-good enterprise domains"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject vs. mechanism pivot)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "Hint: parent_process is primary — parent cue subsumes process_chain. The immediate parent (cmstp.exe) is already a detection field and is covered by cue_parent_process.mechanism_decode/baseline/follow_on."
    - template: cue_process_tool.baseline
      reason: "Superseded by cue_parent_process.baseline (Q5), which is more specific — it baselines the cmstp.exe-to-child pairing rather than generic child-binary frequency."
    - template: cue_user_actor.clustering
      reason: "Dropped to hold the 16-question count; cross-network user activity for this rare-binary alert is lower-value than the ATT&CK chain stages (network/process_access/registry/file). Host-wide and environment-wide scope already covered by cue_host_context.clustering (Q15) and cue_parent_process.scope (Q8)."
    - template: cue_user_actor.clustering_svc
      reason: "Service-principal pair partner of cue_user_actor.clustering; dropped together with it for the same count reason. The baseline service-principal substitute (Q14) preserves the mechanism-pivot coverage for service-principal fires."
    - template: universal.endpoint_protection_health
      reason: "Recommended-not-must_include generic tamper sweep. cue_host_context.precursor (Q16) already surfaces prior tamper-as-precursor alerts on the host, and this rule fires on proxy execution rather than a hands-on-keyboard impact action that turns on whether AV was disabled first."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - cue_parent_process
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 16
    source_counts:
      template: 12
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 11
      network_connection: 1
      process_access: 1
      registry_set: 1
      file_event: 1
      sigma_alert: 1
