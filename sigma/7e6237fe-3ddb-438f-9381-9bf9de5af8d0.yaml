name: Windows Internet Hosted WebDav Share Mount Via Net.EXE
id: f666844f-7f7b-47b9-b38a-270b38740900
description: |
  Detects net.exe or net1.exe mounting an internet-hosted WebDAV share, identified by a command line that
  contains both ' use ' and an ' http' URL. Mapping a drive to an attacker-controlled WebDAV endpoint lets an
  operator stage and execute remote payloads as if they were local files, bypassing some download controls and
  blending file access into normal SMB-style drive usage. Legitimate uses exist (developers or IT mapping a
  WebDAV-backed document store), so the investigation should confirm the destination URL, who and what launched
  net.exe, whether the host has mounted such shares before, and what executed from or after the mount.
type: detection
detection_id: 7e6237fe-3ddb-438f-9381-9bf9de5af8d0
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did net.exe or net1.exe run with ' use ' and an ' http' URL on the command line?
  context: Confirm the alert is a true positive by reviewing the exact command line and process identity. The
    rule fires only when net.exe or net1.exe (by Image or OriginalFileName) carries both the ' use ' verb and an
    ' http' substring, which together indicate a drive mapping to an internet-hosted WebDAV endpoint rather than
    an internal SMB share.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to map internet-hosted WebDAV shares with net.exe?
  context: Legitimate administrative or developer activity can map WebDAV-backed drives, but most endpoints and
    users never do. Knowing whether the user role and host function are consistent with mounting an HTTP(S) share
    is often the fastest path to distinguishing routine operations from operator activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What is the full WebDAV URL and drive letter in the net use command?
  context: The full command line reveals the exact destination. Decode the ' http' URL (host, path, http vs.
    https, any embedded credentials or ports) and the target drive letter. The destination host is the pivot for
    reputation and network-trail checks; a freshly registered or unfamiliar domain mapped as a drive is the
    detail that drives disposition.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains|all:
          - ' use '
          - ' http'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned net.exe, and is that parent chain expected?
  context: The parent process reveals how the mount was invoked. A drive mapping launched by explorer.exe or a
    logon script differs from one launched by mshta.exe, wscript.exe, powershell.exe, or an Office application,
    which would point to scripted or macro-driven execution rather than a deliberate user action.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1021.002.security_logon
  question: What network logon or credential-mapping events accompanied this WebDAV mount?
  context: Mounting a WebDAV share authenticates to the remote endpoint, producing logon events (4624) and, when
    explicit credentials are passed, credential-mapping events (4648). Correlating those events with the mount
    identifies the account used and whether the access came with explicit alternate credentials, which separates
    routine session reuse from deliberate credential supply to a remote host. If Security event logs are not
    ingested in this environment, treat zero results as a visibility gap rather than evidence nothing authenticated.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID:
          - 4624
          - 4648
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TargetUserName
      - LogonType
      - IpAddress
- source: template:lateral_movement.target_activity_after_access
  question: What processes ran on this host shortly after the WebDAV share was mounted?
  context: Mounting a remote share is a means to an end. After the mount, look for processes that read or execute
    from the mapped drive letter or the WebDAV cache, especially script hosts, command interpreters, or unsigned
    binaries launched from the newly available path. This reveals whether the share was used to stage and run a
    payload.
  range: +30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - Image
- source: template:cue_process_tool.follow_on
  question: What child processes did net.exe spawn afterward?
  context: net.exe normally spawns net1.exe to carry out the requested operation and little else. Any other
    child process, or a chain that leads to a command interpreter or download tool, indicates the mount was a
    step in a larger sequence rather than a standalone drive mapping.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single net.exe invocation may be routine, but multiple reconnaissance commands in rapid succession
    -- whoami, nltest, net user, net view, ipconfig -- is a signature of post-compromise enumeration. WebDAV
    mounts that sit inside a recon burst are more likely to be operator-driven staging than administrative drive
    mapping.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \net.exe
        - \net1.exe
      - OriginalFileName:
        - net.exe
        - net1.exe
      selection_cli:
        CommandLine|contains|all:
        - ' use '
        - ' http'
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \net.exe
        - \net1.exe
      - OriginalFileName:
        - net.exe
        - net1.exe
      selection_cli:
        CommandLine|contains|all:
        - ' use '
        - ' http'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging across multiple
    endpoints. If the same user mounted WebDAV shares or ran tools on several hosts in the same window, the scope
    of activity is broader than this single mount.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \net.exe
        - \net1.exe
      - OriginalFileName:
        - net.exe
        - net1.exe
      selection_cli:
        CommandLine|contains|all:
        - ' use '
        - ' http'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously mounted WebDAV shares with net.exe, what parents and users drove it, and
    does this alert fit?
  context: Even routine tools can be abused. The query mirrors the rule's selection structure scoped to this
    host, so the resulting parent and user distribution reflects how the rule's specific firing pattern -- net.exe
    with ' use ' and ' http' -- has historically run here. A new parent process, an unexpected user account, or
    an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \net.exe
        - \net1.exe
      - OriginalFileName:
        - net.exe
        - net1.exe
      selection_cli:
        CommandLine|contains|all:
        - ' use '
        - ' http'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.renamed_binary_check
  question: Was net.exe executed from its expected System32 path?
  context: net.exe and net1.exe run from System32 or SysWOW64 under normal use. A copy running from a user temp
    directory, a download folder, or the mounted WebDAV path itself would indicate the binary was relocated to
    evade path-based allowlists or staged from the share.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were any other download or transfer LOLBins used on this host recently?
  context: Operators who use one living-off-the-land binary to fetch remote content often use several. A WebDAV
    mount alongside certutil, bitsadmin, curl, or PowerShell download cradles on the same host strengthens the
    case that this was a deliberate remote-staging step rather than a one-off drive mapping.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\certutil.exe'
          - '\bitsadmin.exe'
          - '\curl.exe'
          - '\esentutl.exe'
          - '\expand.exe'
          - '\extrac32.exe'
          - '\makecab.exe'
          - '\replace.exe'
          - '\hh.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts mounted WebDAV shares with net.exe in the past week, and does the spread look like
    automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, giving a fleet-wide count of the
    rule's firing pattern rather than generic net.exe usage. Many hosts firing the same pattern points to scripted
    deployment or vendor automation mapping a shared WebDAV store; one host or a small unrelated set points to
    manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \net.exe
        - \net1.exe
      - OriginalFileName:
        - net.exe
        - net1.exe
      selection_cli:
        CommandLine|contains|all:
        - ' use '
        - ' http'
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- reconnaissance, defense
    tampering, or credential access -- may reveal the stages that preceded the mount and help reconstruct whether
    the WebDAV share was part of a multi-step operation or an isolated event.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1021.002
      name: SMB/Windows Admin Shares
      role: primary
    - id: T1021
      name: Remote Services
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: remote_share_access
      description: "net.exe maps a drive to an internet-hosted WebDAV (HTTP/S) endpoint"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: authentication_to_target
      description: "Network logon / credential-mapping to the remote WebDAV host"
      mapped_to: Q5
      source: attack:T1021.002.security_logon
    - stage: 3
      label: post_access_execution
      description: "Lateral-movement actions / payload execution after the share is mounted"
      mapped_to: Q6
      source: template:lateral_movement.target_activity_after_access
  dimension_coverage:
    - dimension: time_correlation
      technique: T1021.002
      covered_by: Q6
      source: template:lateral_movement.target_activity_after_access
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1021.002
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1021.002
      covered_by: Q9
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1021
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ShareName
      dimension: tuning_parameter
      specific_values: "C$, ADMIN$, IPC$ (admin shares) — N/A; this rule targets HTTP WebDAV not admin shares"
      disposition: context
      target_question: Q3
      notes: "Rule fires on an ' http' WebDAV URL, not C$/ADMIN$/IPC$. The mechanism_decode question directs the analyst to decode the actual WebDAV URL and drive letter instead of admin-share names."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "5-10 minutes between remote access and remote execution"
      disposition: query_injection
      target_question: Q6
      notes: "Post-access activity captured with +30m forward window; recon burst uses +/-15m."
    - element: UserContext
      dimension: user_context
      specific_values: "expected remote admins vs. rare/first-time access"
      disposition: query_injection
      target_question: Q9
      notes: "user-pivot 90d baseline of net.exe per user; svc substitute Q10 covers service-principal fires."
    - element: ProcessList
      dimension: tuning_parameter
      specific_values: "cmd.exe, powershell.exe, runonce.exe and other post-copy tools"
      disposition: query_injection
      target_question: Q6
      notes: "target_activity_after_access surfaces processes spawned post-mount; follow_on (Q7) narrows to net.exe children."
    - element: LogonUser
      dimension: tuning_parameter
      specific_values: "service accounts / privileged users (T1021)"
      disposition: query_injection
      target_question: Q5
      notes: "security-logon question projects TargetUserName + LogonType to scope the authenticating account."
    - element: RemoteHostList
      dimension: tuning_parameter
      specific_values: "allowlist known admin jumpboxes / deployment tools (T1021)"
      disposition: context
      target_question: Q16
      notes: "fleet scope question lets analyst judge whether the cross-host spread is known automation; no static allowlist available at gen time."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "kept — parent_process cue not separately active (no parent in detection); process_chain provides the parent lookup"
      kept: cue_process_tool.process_chain
    - dropped: cue_user_actor.baseline / cue_user_actor.clustering vs their _svc substitutes
      reason: "user_actor pair — non-deduplicable; emitted BOTH user-pivot (Q9, Q11) and parent+binary substitute (Q10, Q12) per AGENTS.md service-principal rule"
      kept: all four
    - dropped: discovery_recon.discovery_user_context
      reason: "overlaps universal.host_user_context (Q2); host_user_context kept and adapted to the WebDAV-mount operation"
      kept: universal.host_user_context
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "cue_process_tool.baseline_deviation (Q13, host-pattern reflected) supersedes the generic per-host net.exe baseline — it counts the rule's specific firing pattern, not all net.exe"
    - template: cue_process_tool.clustering
      reason: "cue_host_context.clustering would duplicate the +/-15m host activity sweep already covered by discovery_recon.discovery_burst_detection (Q8) and the post-access window (Q6)"
    - template: cue_host_context.clustering
      reason: "redundant with discovery_recon.discovery_burst_detection (Q8) host-window sweep and lateral_movement.target_activity_after_access (Q6)"
    - template: universal.endpoint_protection_health
      reason: "rule fires on lateral-movement drive mapping, not payload execution or credential access; tamper-as-precursor is not the live disposition question. cue_host_context.precursor (Q17) already surfaces prior tamper alerts on the host"
    - template: lolbin.historical_lolbin_baseline
      reason: "duplicates cue_process_tool.baseline_deviation (Q13) — both are host-scoped reflected counts of the same rule pattern over a historical window; baseline_deviation kept for its Parent/User projection"
    - template: lateral_movement.source_host_identification
      reason: "queries inbound SMB/RDP/WinRM ports to this host; rule detects OUTBOUND WebDAV mount initiated FROM this host, so inbound port correlation does not match the direction of this activity"
    - template: lateral_movement.authentication_method
      reason: "covered more precisely by the authored attack:T1021.002.security_logon question (Q5), which queries the actual 4624/4648 events instead of redisplaying the ProcessGuid"
    - template: lateral_movement.lateral_movement_scope
      reason: "its psexec/mstsc/wmic Image list does not include net.exe WebDAV mounts; cue_user_actor.clustering (Q11) and scope (Q16) cover cross-host spread of THIS pattern"
    - template: discovery_recon.discovery_parent_chain
      reason: "duplicates cue_process_tool.process_chain (Q4) ProcessGuid parent lookup; process_chain kept"
    - template: network_scope.same_action_other_hosts
      reason: "duplicates cue_process_tool.scope (Q16) which is the reflected, pattern-accurate cross-host count; scope kept"
    - template: network_scope.actor_activity_network_wide
      reason: "duplicates cue_user_actor.clustering (Q11) user-wide activity sweep; clustering kept"
    - template: network_scope.actor_authentication_trail
      reason: "covered by attack:T1021.002.security_logon (Q5) which queries 4624/4648 for the host; authentication_trail's user-wide 4624/4625 sweep would overlap"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1021.002) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: acted_on
      reason: >-
        Authored attack:T1021.002.security_logon (Q5) with logsource product:windows service:security querying
        EventID 4624/4648 scoped to %hostname% — the WebDAV mount authenticates to the remote endpoint, so the
        security logon/credential-mapping trail is the cross-logsource pivot this hint calls for.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - lateral_movement
    - discovery_recon
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      security: 1
      sigma_alert: 1
