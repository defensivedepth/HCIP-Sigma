name: Suspicious DLL Loaded via CertOC.EXE
id: d9846bd9-bf9a-4f4d-864e-82323af4b877
description: |
  Detects certoc.exe invoked with the -LoadDLL flag against a DLL path that lives in a user-writable
  location (AppData\Local\Temp, Desktop, Downloads, Users\Public, Windows\Tasks, or Windows\Temp).
  CertOC.exe is a signed Microsoft binary whose -LoadDLL option will load and execute code from an
  arbitrary DLL, making it a proxy-execution path that bypasses application-control rules keyed on the
  loading process. Legitimate certificate-installation workflows do not load DLLs from temp or download
  directories, so the combination of the trusted binary, the -LoadDLL flag, and an untrusted path is the
  signal to investigate. Determine which DLL was loaded, where it came from, what process drove the
  invocation, and whether code execution or network activity followed.
type: detection
detection_id: 84232095-ecca-4015-b0d7-7726507ee793
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did certoc.exe run with the -LoadDLL flag against a DLL in a user-writable path?
  context: Confirm the alert is a true positive by reviewing the full command line. The rule fires only
    when all three conditions hold together — the image is certoc.exe (or OriginalFileName CertOC.exe),
    the command line contains the -LoadDLL flag, and the referenced path is under AppData\Local\Temp,
    Desktop, Downloads, Users\Public, Windows\Tasks, or Windows\Temp. Note the exact DLL path; it drives
    the downstream content and origin questions.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to install certificates or load DLLs with certoc.exe?
  context: Certificate-management tooling is typically run by a narrow set of administrators or by
    automation, and legitimate certoc.exe usage installs certificates rather than loading DLLs from
    temp or download folders. If the user role and host function do not involve certificate operations,
    the proxy-execution interpretation strengthens.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1218.command_analysis
  question: What exactly did certoc.exe load? Decode the full command line and identify the DLL path and any other flags.
  context: The command line carries the operational detail — the absolute path of the loaded DLL, the
    directory it was dropped into, and whether additional certoc flags (for example -GetCACAPS, which
    retrieves files from a remote URL) are present. Parse the path against the known user-writable
    locations and note the DLL file name for hash and content lookup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains|windash: ' -LoadDLL '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_artifact.mechanism_decode
  question: What is in the loaded DLL? Check signing status, strings, imports, and known malware indicators.
  context: The DLL referenced on the command line is the payload certoc.exe executed. Retrieve it from
    the host or EDR and examine its signing status, embedded strings, imports, and packer signatures.
    A DLL dropped into a temp or download directory and loaded through a signed proxy binary is rarely
    a legitimate certificate component.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_artifact.artifact_verification
  question: What is the hash of the loaded DLL and does it have known AV or threat-intel detections?
  context: A file hash gives a portable identifier for reputation lookup. Submit the loaded DLL's hash
    to threat-intel sources to determine whether it is known malicious, known benign, or previously
    unseen. A previously unseen DLL loaded from a user-writable path warrants deeper analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:universal.evidence_preservation
  question: Is the loaded DLL still on disk for analysis?
  context: If the DLL referenced on the command line still exists on disk, it can be hashed, submitted
    to a sandbox, and reviewed for content. Proxy-execution payloads are often deleted after running,
    so capture the artifact promptly if it is still present.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1218.parent_process_analysis
  question: What process spawned certoc.exe and is that parent chain expected?
  context: The parent process reveals how certoc.exe was invoked. Legitimate certificate operations are
    usually launched interactively (explorer.exe, an admin console) or by a management agent. Parents
    such as cmd.exe, powershell.exe, wscript.exe, mshta.exe, or an Office application point to scripted
    or operator-driven proxy execution rather than routine certificate maintenance.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.image_load
  question: What DLLs did certoc.exe load on this host, and are they unsigned or located in user-writable paths?
  context: certoc.exe -LoadDLL maps the target DLL into the certoc process. The image-load record
    confirms which module was actually loaded and its signing status — the load-side counterpart to the
    command-line evidence. An unsigned DLL, or one loaded from a temp or download directory, confirms the
    proxy-execution behavior the command line implied. If image-load events are not collected in this
    environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.network_retrieval
  question: Did certoc.exe make any outbound network connections around the time of the alert?
  context: Beyond -LoadDLL, certoc.exe can retrieve remote files (for example via -GetCACAPS), and a
    loaded DLL may itself initiate command-and-control. Outbound connections from certoc.exe indicate the
    binary was used to stage a payload or beacon, not just to load a local DLL. If network telemetry is
    not collected for this host, treat zero results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.follow_on
  question: What child processes did certoc.exe spawn afterward?
  context: The code inside a loaded DLL frequently launches follow-on processes. Children such as cmd.exe,
    powershell.exe, rundll32.exe, or certutil.exe spawned by certoc.exe indicate the loaded DLL executed
    post-exploitation actions rather than performing a benign certificate task.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run certoc.exe?
  context: certoc.exe is present on Windows but is rarely executed interactively outside certificate
    management. Zero or very low historical counts over 90 days indicate first-time or rare use, which
    raises the weight of the current alert.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: The query mirrors the rule's full selection structure scoped to this host, so the resulting
    parent and user distribution reflects how this specific certoc -LoadDLL pattern has run here before.
    A new parent process, an unexpected user account, or an unusual command-line shape is the deviation
    worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \certoc.exe
      - OriginalFileName: CertOC.exe
      selection_cli:
        CommandLine|contains|windash: ' -LoadDLL '
      selection_paths:
        CommandLine|contains:
        - \Appdata\Local\Temp\
        - \Desktop\
        - \Downloads\
        - \Users\Public\
        - C:\Windows\Tasks\
        - C:\Windows\Temp\
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run certoc.exe?
  context: Compare this user's current action against their historical behavior. A user who has never run
    certoc.exe is more suspicious than one who does so as part of a known certificate-administration role.
    The user's typical toolset should align with certificate operations.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and certoc.exe combination historically routine on this host or environment-wide, or is the parent unusual for certoc.exe?
  context: When the alert fires under a service principal, user identity does not attribute the behavior;
    the meaningful pivot is the launch mechanism. A new or rare parent for certoc.exe on this host or
    environment-wide is the deviation worth chasing, whereas a pair that already accounts for thousands
    of prior events is automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the
    operation may be larger than the single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and certoc.exe combination fire on other hosts in the same window — is this a coordinated rollout or isolated to this host?
  context: Under a service principal, asking what else the user did returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism — parent process plus certoc.exe — fired on
    other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering,
    reconnaissance, or credential access — may reveal the attack stages that preceded this proxy
    execution and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_binary_invocation
      description: "Trusted signed binary (certoc.exe) invoked with -LoadDLL to proxy-execute a DLL"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_line_payload_decode
      description: "Command-line parameters identify the loaded DLL / remote retrieval flag"
      mapped_to: Q3
      source: attack:T1218.command_analysis
    - stage: 3
      label: payload_load
      description: "DLL is mapped into the proxy process (image load)"
      mapped_to: Q8
      source: attack:T1218.image_load
    - stage: 4
      label: network_retrieval
      description: "Externally hosted payload retrieved / C2 from the proxy process"
      mapped_to: Q9
      source: attack:T1218.network_retrieval
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: attack:T1218.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q7
      source: attack:T1218.parent_process_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q8
      source: attack:T1218.image_load
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (e.g., regsvr32.exe not launched by explorer.exe)"
      disposition: new_question
      target_question: Q7
      notes: "Parent-chain expectation drives the parent_process_analysis question; rule has no parent in detection so profiled via ParentProcessGuid lookup."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (msiexec.exe, regsvr32.exe, etc.) — rule already constrains binary to certoc.exe"
      disposition: context
      target_question: Q8
      notes: "Rule fixes the proxy binary to certoc.exe; signed-binary allowlisting is the analyst's signing check on the loaded DLL, surfaced in the image_load Signed/Signature fields."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (URLs, script paths, DLL entrypoints)"
      disposition: query_injection
      target_question: Q3
      notes: "command_analysis question filters on CommandLine|contains|windash ' -LoadDLL ' and decodes the DLL path / -GetCACAPS retrieval flag."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (enterprise domains to suppress)"
      disposition: new_question
      target_question: Q9
      notes: "Remote-retrieval suppression is the network dimension; surfaced as the network_connection question on certoc.exe egress."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject vs. launch-mechanism pivot)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject vs. launch-mechanism pivot)"
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "subsumed by attack:T1218.parent_process_analysis (ParentProcessGuid lookup) — same parent-chain intent, more specific source"
      kept: attack:T1218.parent_process_analysis
    - dropped: cue_process_tool.mechanism_decode
      reason: "superseded by attack:T1218.command_analysis — same Phase-2 decode intent but adds CommandLine|contains|windash ' -LoadDLL ' filter for Q-vs-query alignment"
      kept: attack:T1218.command_analysis
  skipped_templates:
    - template: cue_process_tool.clustering
      reason: "cue_host_context.clustering intent and the user_actor clustering pair already cover around-the-time host/network context; an additional host-wide tool sweep would duplicate without unique fields"
    - template: cue_host_context.clustering
      reason: "around-the-time host context covered by the user_actor clustering pair and the precursor alert sweep; dropped to stay within the 18-question cap and avoid an Image+ParentImage aggregation duplicating the clustering pair"
    - template: cue_process_tool.scope
      reason: "fleet-pattern reflection dropped to stay within the 18-question cap; the baseline_deviation host-pattern reflection and the user_actor clustering_svc pair already provide cross-host distribution of the rule's firing pattern"
    - template: cue_artifact.baseline
      reason: "file first-seen baseline subsumed by evidence_preservation (is the DLL still on disk) plus artifact_verification (hash reputation); the DLL is a dropped payload, not a recurring host file worth a 90-day baseline"
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED generic tamper sweep dropped: this rule fires on proxy-execution, not credential access/impact; the cue_host_context.precursor alert sweep already surfaces any prior defense-tampering detections on the host"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted-on
      reason: >-
        Acted on — authored attack:T1218.image_load (Q8) with category: image_load
        to confirm which DLL certoc.exe actually mapped and its signing status, the
        load-side counterpart to the command-line evidence.
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: acted-on
      reason: >-
        Acted on — authored attack:T1218.network_retrieval (Q9) with category:
        network_connection to surface outbound connections from certoc.exe
        (-GetCACAPS retrieval / DLL-driven C2).
    - hint: "cue_artifact: derived: [A-Za-z]:\\ in CommandLine"
      disposition: acted-on
      reason: >-
        Acted on — cue_artifact patterns (mechanism_decode Q4, artifact_verification Q5)
        applied to the loaded DLL referenced in the command line.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: acted-on
      reason: >-
        Acted on — Q4, Q5, and Q6 use TargetFilename|endswith|expand '%file_paths%'
        rather than %TargetFilename% because the path is derived from CommandLine.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 13
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 12
    logsource_diversity:
      process_creation: 11
      file_event: 3
      image_load: 1
      network_connection: 1
      sigma_alert: 1
