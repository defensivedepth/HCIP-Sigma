name: Suspicious CustomShellHost Execution
id: 6b40ade9-a7b9-4b3a-bf93-fd1a29f6dfbe
description: |
  Detects CustomShellHost.exe spawning a child process other than C:\Windows\explorer.exe. CustomShellHost.exe is a signed Microsoft binary used by custom shells in kiosk mode; by design it launches explorer.exe with /NoShellRegistrationCheck, so any other child is unexpected and can be a signed-binary proxy execution (T1216) used to run untrusted code under a trusted parent. Investigation should identify the spawned child, confirm it is not explorer.exe, determine the child's signature and on-disk location, and reconstruct what the child did and how CustomShellHost.exe itself was launched.
type: detection
detection_id: 84b14121-9d14-416e-800b-f3b829c5a14d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did CustomShellHost.exe spawn a child process other than C:\Windows\explorer.exe?
  context: Confirm the alert is a true positive by reviewing the triggering event. The rule fires when a process whose ParentImage ends in \CustomShellHost.exe spawns any child whose Image is not exactly C:\Windows\explorer.exe. Verify the parent is genuinely CustomShellHost.exe and note the exact child Image and its path, since the child's identity drives the rest of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to be running custom shell / kiosk-mode configurations?
  context: CustomShellHost.exe is unlikely to run on a normal workstation; it is associated with kiosk or custom-shell deployments. A host or user with no kiosk-mode role spawning non-explorer children from this binary is more suspicious than a configured kiosk endpoint.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What was CustomShellHost.exe's own command line, and what launched it?
  context: The parent's own process_creation event shows how CustomShellHost.exe was invoked and what spawned it. Legitimate kiosk use launches it as part of shell setup; an unexpected launcher (a script interpreter, a download tool, or a user-writable binary) points to proxy abuse. The -7d range is intentional because the parent may have started long before the alert.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1216.signed_to_unsigned_transition
  question: Is the child process spawned by CustomShellHost.exe signed, and by whom?
  context: T1216 proxies execution of untrusted code through a signed Microsoft parent. The diagnostic transition is a Microsoft-signed parent launching an unsigned or third-party-signed child. Pull the child binary's signature and signer; an unsigned child, or one signed by an unexpected vendor, is the load-bearing signal that this is proxy execution rather than a misconfiguration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: attack:T1216.child_payload_decode
  question: What did the spawned child process do? Decode its full command line.
  context: The child's command line reveals the actual operational purpose proxied through CustomShellHost.exe. Decode any flags, encoded content, file paths, or URLs; download cradles, encoded PowerShell, or interpreter invocations indicate payload execution rather than benign shell setup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1216.child_file_artifacts
  question: What files did the spawned child process write to disk?
  context: Proxy execution frequently drops a follow-on payload, script, or configuration file. Reviewing files written by the child binary reveals staged tooling or persistence artifacts that the process_creation event alone does not show. Files written to user-writable or temporary locations are of particular interest.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did CustomShellHost.exe spawn on this host?
  context: A compromised or abused parent often launches multiple tools in sequence. Seeing every child of CustomShellHost.exe on this host reveals the full scope of activity proxied through it, beyond the single child that triggered the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.clustering
  question: What other activity is associated with CustomShellHost.exe around the same time?
  context: Beyond child processes, the parent may correlate with file writes or network activity in the same window. Reviewing process activity tied to CustomShellHost.exe around the alert helps place the spawn in a broader sequence of actions.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has CustomShellHost.exe spawned this child binary on this host before?
  context: Some parent-child pairings are routine on configured kiosk hosts; a never-before-seen child binary launched from CustomShellHost.exe is a novel relationship. Zero or very low historical counts indicate first-time execution worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run CustomShellHost.exe at all?
  context: CustomShellHost.exe is unlikely to run on standard workstations. A host with no prior history of this binary executing is more suspicious than a kiosk endpoint where it runs as part of shell setup. Zero or very low counts indicate first-time or rare execution.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \CustomShellHost.exe
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run CustomShellHost.exe or its spawned child?
  context: Compare this user's current action against their historical behavior. A user who has never been associated with custom-shell execution or with the spawned child binary is more suspicious than one who does so regularly. User role should align with kiosk or shell-management duties.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this CustomShellHost.exe-to-child launch mechanism historically routine on this host or across the environment?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism. A new or rare CustomShellHost.exe-to-child pairing on this host or environment-wide is the deviation; a pairing that already accounts for many prior events is OS or vendor automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar activity on multiple hosts, the scope may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same CustomShellHost.exe-to-child combination fire on other hosts in the same window?
  context: Under a service principal, user-pivoted activity returns agent-telemetry noise. The meaningful question is whether the same parent-and-child launch mechanism fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_parent_process.scope
  question: Is this CustomShellHost.exe-to-child spawning pattern occurring on other hosts?
  context: The same parent-child relationship appearing on multiple hosts may indicate a campaign or a pushed configuration; a single occurrence is more likely targeted. Environment-wide comparison helps calibrate scope and severity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the attack stages that preceded this event and help reconstruct the timeline leading up to the proxied execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1216
      name: System Script Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_parent_invocation
      description: "Microsoft-signed CustomShellHost.exe is launched (by design it spawns explorer.exe with /NoShellRegistrationCheck)"
      mapped_to: Q3
      source: template:cue_parent_process.mechanism_decode
    - stage: 2
      label: proxied_child_spawn
      description: "Signed parent spawns a child other than explorer.exe (the rule's trigger), proxying execution of the child"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: unsigned_payload_execution
      description: "The proxied child executes untrusted/unsigned payload; signature transition and command line reveal intent"
      mapped_to: Q4
      source: attack:T1216.signed_to_unsigned_transition
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1216
      covered_by: Q3
      source: template:cue_parent_process.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1216
      covered_by: Q8
      source: template:cue_parent_process.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1216
      covered_by: Q4
      source: attack:T1216.signed_to_unsigned_transition
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "wscript.exe, cscript.exe, pubprn.vbs, installutil.exe (rationale only — this rule's parent is fixed to CustomShellHost.exe)"
      disposition: context
      target_question: Q3
      notes: "Rule's parent is fixed (ParentImage|endswith \\CustomShellHost.exe). The interpreter examples are illustrative for T1216 broadly; parent identity is confirmed by the rule, so mechanism_decode focuses on how CustomShellHost.exe itself was launched."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — time delta between signed-script execution and child creation"
      disposition: context
      target_question: Q8
      notes: "No queryable value; covered by +/-15m clustering window around the parent and by follow_on +4h."
    - element: ChildCommandLineRegex
      dimension: tuning_parameter
      specific_values: "rationale only — download cradle / PowerShell decode patterns (no concrete regex supplied)"
      disposition: new_question
      target_question: Q5
      notes: "Authored attack:T1216.child_payload_decode to decode the child's full command line; brief supplies no concrete regex/values to inject."
    - element: SignedToUnsignedTransition
      dimension: tuning_parameter
      specific_values: "rationale only — Microsoft-signed parent, unsigned/unknown child"
      disposition: new_question
      target_question: Q4
      notes: "Authored attack:T1216.signed_to_unsigned_transition to pull the child's signature/signer; signature is read from the alert event, not a queryable selection value."
  dedup_decisions: []
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "parent_process is primary (hint) — cue_parent_process.mechanism_decode covers parent invocation and what launched the parent; process_chain is subsumed"
    - template: cue_process_tool.mechanism_decode
      reason: "child command-line decode is covered by attack:T1216.child_payload_decode (Q5) with identical host+Image scope; the attack question is more specific to the T1216 child payload"
    - template: universal.endpoint_protection_health
      reason: "recommended-only generic tamper sweep; this rule fires on signed-binary proxy execution, not payload/credential/impact action where tamper-as-precursor is the live question. cue_host_context.precursor (Q16) already surfaces prior tamper alerts on the host"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1216) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule's detection turns on the process-lineage transition
        (CustomShellHost.exe spawning a non-explorer child) and the child's
        signature/payload, not on a DLL-side-loading vector. Module-load
        coverage of CustomShellHost.exe or its child would return volume
        without precision for the proxy-execution behavior this rule fires on.
        The signature transition is covered by Q4
        (attack:T1216.signed_to_unsigned_transition) via process_creation
        evidence. No compensating attack:T1216.image_load question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1216) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (handle/memory access into a target process) addresses
        the credential-access and injection variants of T1216 analytics, not
        the signed-parent-spawns-child lineage this rule detects. The rule has
        no target-process or memory-access component. The proxied-child
        behavior is covered by the child decode (Q5) and child file-artifact
        (Q6) questions over process_creation/file_event. No compensating
        attack:T1216.process_access question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 13
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 11
    logsource_diversity:
      process_creation: 14
      file_event: 1
      sigma_alert: 1
