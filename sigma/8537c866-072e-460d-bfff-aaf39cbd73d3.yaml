name: Potentially Suspicious Inline JavaScript Execution via NodeJS Binary
id: 0aeba2c5-43a6-451a-9c53-53d5ba2943f2
description: |
  Detects node.exe (identified by Image name, OriginalFileName, or Product) running with a command line that contains all of
  the module/keyword markers http, execSync, spawn, fs, path, and zlib together. The rule fires only when the node.exe identity
  AND the full keyword set are present at once. Legitimate Node.js applications and developer tooling use these modules, so the
  combination alone is not malicious; the same inline-JavaScript pattern has also been used to run downloader and discovery code
  directly from the command line via node.exe -e while blending in with trusted Node.js usage. Investigation should confirm the
  exact inline code, identify what spawned node.exe, decode what the JavaScript actually does, and determine whether the user,
  host, and parent chain are consistent with sanctioned Node.js development or automation.
type: detection
detection_id: 8537c866-072e-460d-bfff-aaf39cbd73d3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting node.exe command line contain all of the markers http, execSync, spawn, fs, path, and zlib together,
    and is the binary identified as node.exe by name, original filename, or product?
  context: Before investing investigation time, confirm the alert is a true positive. The rule requires both the node.exe identity
    (Image ending in node.exe, OriginalFileName node.exe, or Product Node.js) and every one of the six keyword markers in the
    command line at once. Review the exact command line and binary identity against those two criteria.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
      - Product
- source: template:universal.host_user_context
  question: Is this user and host expected to run Node.js with inline or module-loading JavaScript?
  context: Legitimate Node.js development, build tooling, and packaged Node.js applications routinely use these modules. Knowing
    whether the user role (developer, automation account, end user) and host function (workstation, build server, general
    endpoint) line up with sanctioned Node.js usage is often the fastest way to separate routine activity from an inline-script
    payload running on a host with no Node.js footprint.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly does the node.exe command line execute? Decode the inline JavaScript and the modules and arguments it
    references.
  context: The full command line reveals the specific action taken. Look for an -e or --eval flag carrying inline JavaScript, a
    -r preload module, or a script path, then read what the code does with the referenced modules — http for outbound requests,
    execSync/spawn for launching commands, fs/path for file access, and zlib for compression or decompression of staged data.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:obfuscation.encoded_payload_decode
  question: Is the inline JavaScript encoded or obfuscated? What does it decode to, and what operation does the decoded content
    perform?
  context: Inline JavaScript passed to node.exe is frequently obfuscated to hide its purpose. Common encodings include base64
    string literals decoded at runtime, String.fromCharCode and char-code concatenation chains, hex escapes, and Buffer-based
    decoding. Reconstruct the readable script and identify the actual intent — outbound HTTP to a remote host, spawning of a
    secondary command interpreter, reading or staging local files, or compressing data before exfiltration. Iterate decoding
    until the payload is readable for multi-layer obfuscation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1059.007.image_load
  question: What library modules did this node.exe process load, and do any of them point to network, compression, or credential
    access beyond a normal application?
  context: Inline-JavaScript abuse pulls in supporting library modules at runtime, and the loaded-module list shows what the
    script is actually wired to do even when the command line is short or obfuscated. Network, compression, and cryptography or
    credential-store modules loaded by a node.exe instance with no legitimate application context indicate the script is set up
    for outbound communication, data staging, or browser-data access rather than ordinary development. If image-load events are
    not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ImageLoaded
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this node.exe instance, and is that parent chain expected for Node.js execution?
  context: The parent process reveals how node.exe was invoked. Sanctioned usage commonly descends from a developer shell,
    explorer.exe, a build or packaging tool, or a service wrapper. A parent such as powershell.exe, cmd.exe, mshta.exe, a
    browser, or an Office application launching node.exe with inline JavaScript is a stronger indicator that the runtime was
    dropped and driven by another stage rather than run by the user.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this node.exe instance spawn afterward?
  context: The processes spawned by the inline script reveal its actual impact. The execSync and spawn markers in the command
    line mean the script is set up to launch other commands. Look for follow-on cmd.exe, powershell.exe, certutil.exe, or
    network utilities that indicate downloading, discovery, persistence installation, or data staging after the initial
    execution.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run node.exe?
  context: A runtime that has never been seen on a host is more suspicious than one that runs regularly. Zero or very low
    historical counts of node.exe on this host indicate first-time or rare execution, which on a general-purpose endpoint with
    no development role points toward a dropped runtime rather than sanctioned tooling.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this node.exe inline-JavaScript pattern, what parents and users normally drove
    it, and does the current alert fit that pattern?
  context: Even legitimately installed runtimes can be abused. The query mirrors the rule's selection structure scoped to this
    host, so the resulting parent and user distribution reflects how this specific firing pattern — node.exe with the full
    module-marker set — has historically run here, not generic node.exe usage. A new parent process, an unexpected user account,
    or an unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \node.exe
      - OriginalFileName: node.exe
      - Product: Node.js
      selection_cmd:
        CommandLine|contains|all:
        - http
        - execSync
        - spawn
        - fs
        - path
        - zlib
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this node.exe inline-JavaScript pattern in the past week, and does the cross-host distribution
    look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    specific firing pattern rather than generic node.exe usage. Many hosts firing the same pattern points to scripted
    deployment or packaged Node.js application rollout; one host or a small unrelated set points to manual operator activity.
    Use the host distribution and parent and binary consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \node.exe
      - OriginalFileName: node.exe
      - Product: Node.js
      selection_cmd:
        CommandLine|contains|all:
        - http
        - execSync
        - spawn
        - fs
        - path
        - zlib
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run Node.js?
  context: Compare this user's current action against their historical behavior. A user who has never run node.exe is more
    suspicious than a developer or automation account that runs it regularly. The user's role and typical toolset should align
    with running a JavaScript runtime that loads network and file-system modules.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-node.exe launch combination historically routine on this host or across the environment, or is
    the parent unusual for node.exe?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is
    the launch mechanism — which parent process spawned node.exe, and whether that pair is historically common. A new or rare
    parent for node.exe on this host or environment-wide is the deviation worth chasing; a pair that already accounts for
    thousands of prior events is vendor or application automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If
    the same user triggered similar Node.js or follow-on execution on multiple hosts, the scope of the activity may be larger
    than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-node.exe combination fire on other hosts in the same window — is this a coordinated
    automation rollout or activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise, since
    every host runs service-owned processes constantly. The meaningful question is whether the same launch mechanism — parent
    process plus spawned node.exe — fired on other hosts in the alert window. Multiple hosts in the same window points to
    scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to this node.exe execution
    and what followed it, and whether it is part of a larger sequence. Look for reconnaissance, the runtime and its modules
    being downloaded, persistence installation, or data staging in the surrounding window.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, a
    download cradle, or scripted persistence — may reveal the attack stages that preceded the node.exe execution and help
    reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes payload execution, and inline-JavaScript delivery chains have been observed adding
    Defender exclusion paths and process exclusions before fetching and running the payload. The query surfaces direct attempts
    to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful
    protection-looks-healthy signal; populated rows shift the disposition toward operator preparation, so review the parent
    process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.007
      name: JavaScript
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: interpreter_invocation
      description: "node.exe is invoked with inline JavaScript or module preloads carrying the full marker set"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: "The inline JavaScript / arguments are decoded to reveal the operation performed"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: module_load
      description: "node.exe loads supporting library modules for network, compression, or credential access"
      mapped_to: Q5
      source: attack:T1059.007.image_load
    - stage: 4
      label: secondary_execution
      description: "Script spawns secondary commands or correlates with outbound activity / system modification"
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.007
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.007
      covered_by: Q15
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.007
      covered_by: Q4
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1059.007
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcess
      dimension: parent_process_analysis
      specific_values: "wscript.exe, cscript.exe, mshta.exe, Excel/Outlook (rationale; adapted to node.exe parents)"
      disposition: context
      target_question: Q6
      notes: "T1059.007 analytic names WSH/HTA hosts and Office parents; adapted to node.exe context — parent chain question calls out powershell.exe/cmd.exe/mshta.exe/browser/Office as the abnormal parents."
    - element: ScriptPath
      dimension: tuning_parameter
      specific_values: "%TEMP%, user download folder, UNC/web path"
      disposition: context
      target_question: Q3
      notes: "Decode question directs analyst to read -e inline / -r preload / script path and working directory."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "non-business / patch windows (rationale only)"
      disposition: context
      target_question: Q15
      notes: "Host-context clustering frames the surrounding-window activity; no specific queryable threshold."
    - element: UserContext
      dimension: user_context
      specific_values: "non-admin / accounts not authorized for scripting (rationale only)"
      disposition: context
      target_question: Q2
      notes: "host_user_context asks whether user/host are expected to run Node.js."
    - element: EntropyScore
      dimension: tuning_parameter
      specific_values: "high-entropy obfuscated JS (rationale only)"
      disposition: context
      target_question: Q4
      notes: "Obfuscation decode question covers base64/fromCharCode/hex reconstruction; entropy is not directly queryable from process_creation."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "encoded/uncommon script execution patterns (rationale only)"
      disposition: context
      target_question: Q3
      notes: "mechanism_decode reads full command line; the six marker keywords are enforced by the rule selection itself."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "varies across managed/unmanaged workstations (rationale only)"
      disposition: context
      target_question: Q6
      notes: "Covered by process_chain parent question; also surfaced via service-principal parent+binary baseline."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "Subsumed by cue_host_context.clustering — both ask what else ran on this host around the same time; host_context aggregates Image+ParentImage (richer parent-child view) over the same +/-15m window."
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.baseline (user-pivot) / cue_user_actor.baseline_svc (service-principal substitute)
      reason: "user_actor pair — non-deduplicable; emitted BOTH per AGENTS.md (subject vs. launch-mechanism pivots)."
      kept: both
    - dropped: cue_user_actor.clustering (user-pivot) / cue_user_actor.clustering_svc (service-principal substitute)
      reason: "user_actor pair — non-deduplicable; emitted BOTH per AGENTS.md (subject vs. launch-mechanism pivots)."
      kept: both
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - obfuscation
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      image_load: 1
      sigma_alert: 1
