name: PowerShell Download and Execution Cradles
id: 7ba535f3-9d1d-46dd-a8bd-955cc1fafbde
description: |
  Detects PowerShell command lines that combine a remote-download primitive (Net.WebClient
  DownloadString/DownloadFile, Invoke-WebRequest/iwr, Invoke-RestMethod/irm) with an in-place
  execution primitive (Invoke-Expression/IEX, including backtick-obfuscated variants) in the same
  invocation. The rule fires only when BOTH a download token AND an execution token are present,
  the canonical pattern of a fileless download-and-run cradle that fetches a script or payload and
  executes it in memory. Legitimate installers and packaging tooling occasionally pair these
  primitives, so the investigation determines what URL was fetched, what it executed, whether the
  payload touched disk, and whether the user, host, and parent process are consistent with expected
  scripting activity.
type: detection
detection_id: 85b0b087-eddf-4a2b-b033-d771fa2b9775
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line pair a download primitive with an in-memory execution primitive
    as this rule requires?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires
    only when the command line contains BOTH a download token (DownloadString, DownloadFile,
    Invoke-WebRequest/iwr, Invoke-RestMethod/irm) AND an execution token (IEX/Invoke-Expression,
    including backtick-broken forms like I`EX). Read the full command line and confirm both halves
    are present rather than a benign single primitive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to download and execute remote scripts from the command line?
  context: Legitimate administrative and packaging activity can pair a download primitive with
    in-memory execution. Knowing whether the user role and host function include running download
    cradles (build automation, software deployment, developer tooling) is often the fastest path to
    distinguishing a true positive from routine operations.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:powershell.encoded_command_decode
  question: What URL does the cradle fetch, and what does the downloaded content execute?
  context: The detection matches a plain-text cradle pattern (DownloadString/DownloadFile/iwr/irm piped
    into IEX), so the command line is already readable rather than base64-encoded. Extract the source
    URL or host being fetched, identify whether the result is piped straight into Invoke-Expression
    (in-memory execution) or saved to a path first, and check the URL reputation. Watch for backtick
    breaks (I`EX) and string concatenation used to evade simple matching.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: Decode the full PowerShell command line on this host — what is the complete download-and-run
    operation, including any staged second-stage commands?
  context: Cradles frequently arrive as multi-statement command lines or as a sequence of PowerShell
    invocations that together form one logical operation (fetch loader, then execute payload). Scoping
    to PowerShell command lines that carry the same download and execution tokens on this host within a
    short window surfaces the full operation rather than only the single triggering event. Decode any
    flags, concatenation, and the fetched resource.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_download:
        CommandLine|contains:
        - .DownloadString(
        - .DownloadFile(
        - 'Invoke-WebRequest '
        - 'iwr '
        - 'Invoke-RestMethod '
        - 'irm '
      selection_iex:
        CommandLine|contains:
        - ;iex $
        - '| IEX'
        - '|IEX '
        - I`E`X
        - I`EX
        - IE`X
        - 'iex '
        - IEX (
        - IEX(
        - Invoke-Expression
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1059.command_analysis
  question: Does the command line carry obfuscation or evasion markers beyond the literal download and
    execution tokens?
  context: Command-and-scripting-interpreter abuse is tunable around the exact pattern. Beyond the
    DownloadString/IEX tokens the rule keys on, cradles commonly add execution-policy bypass,
    window-hidden, non-interactive, or base64 (-enc) wrappers, and break keywords with backticks or
    string joins. Surfacing which of these markers are present calibrates how deliberately the command
    was crafted to evade detection.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_evasion:
        CommandLine|contains:
        - '-enc'
        - '-EncodedCommand'
        - '-w hidden'
        - '-WindowStyle Hidden'
        - '-nop'
        - '-NoProfile'
        - 'FromBase64String'
        - I`E`X
        - I`EX
        - IE`X
      condition: selection_host and selection_evasion
    fields:
      - CommandLine
      - User
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened before this execution?
  context: Attackers often disable or bypass PowerShell security controls before running a cradle.
    Common techniques include AMSI bypass (patching amsi.dll in memory), execution policy changes
    (Set-ExecutionPolicy or -ep bypass), constrained language mode escape, and downgrade attacks
    (powershell.exe v2, which lacks logging). Finding these precursor events strengthens the case for
    malicious activity.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script content?
  context: Script block logging records the deobfuscated script content PowerShell actually executes,
    even when the command line uses backtick breaks or concatenation. This provides ground truth about
    what the cradle fetched and ran, bypassing command-line obfuscation. If PowerShell operational logs
    are not ingested in this environment, treat zero results as a visibility gap rather than evidence
    nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: The parent process reveals how the cradle was invoked. User-driven scripting typically has
    predictable parents (explorer.exe, a console host, an IDE), while Office applications, mshta.exe,
    wscript.exe, or a server service (sqlservr.exe, w3wp.exe) spawning a download-and-run command line
    points to macro execution or exploitation of an internet-facing application.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this PowerShell instance spawn afterward?
  context: The processes a cradle spawns reveal its actual impact. In-memory execution may launch
    cmd.exe, additional powershell.exe instances, certutil.exe, rundll32.exe, or a dropped binary for
    post-exploitation. A cradle that fetches and reflectively loads a .NET assembly may show no child at
    all, so an empty result does not clear the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.evidence_preservation
  question: Is a downloaded or staged file referenced by the command line still on disk for analysis?
  context: If the cradle saved the fetched content to a path (DownloadFile to a local file, or output
    redirected to disk) rather than executing purely in memory, checking whether that file still exists
    enables hash lookup, sandbox submission, or content review. Many cradles run entirely in memory and
    leave no on-disk artifact.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded payload from a safe environment?
  context: If the fetched file is still on disk, submitting its hash to threat intelligence services or
    detonating it in a sandbox reveals whether it is a known malicious payload, a legitimate tool, or
    something novel that requires deeper analysis. Pair the file hash with the source URL identified
    from the command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the directory the cradle wrote to?
  context: Cradles often stage multiple files in the same directory, or the fetched payload triggers
    further writes (extraction, decompression, additional downloads). Examining directory-level file
    activity reveals the broader scope of the delivery operation beyond the single referenced file.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_download:
        CommandLine|contains:
        - .DownloadString(
        - .DownloadFile(
        - 'Invoke-WebRequest '
        - 'iwr '
        - 'Invoke-RestMethod '
        - 'irm '
      selection_iex:
        CommandLine|contains:
        - ;iex $
        - '| IEX'
        - '|IEX '
        - I`E`X
        - I`EX
        - IE`X
        - 'iex '
        - IEX (
        - IEX(
        - Invoke-Expression
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_download:
        CommandLine|contains:
        - .DownloadString(
        - .DownloadFile(
        - 'Invoke-WebRequest '
        - 'iwr '
        - 'Invoke-RestMethod '
        - 'irm '
      selection_iex:
        CommandLine|contains:
        - ;iex $
        - '| IEX'
        - '|IEX '
        - I`E`X
        - I`EX
        - IE`X
        - 'iex '
        - IEX (
        - IEX(
        - Invoke-Expression
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar download-and-execute activity on multiple
    hosts, the scope of the operation may be larger than a single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_download:
        CommandLine|contains:
        - .DownloadString(
        - .DownloadFile(
        - 'Invoke-WebRequest '
        - 'iwr '
        - 'Invoke-RestMethod '
        - 'irm '
      selection_iex:
        CommandLine|contains:
        - ;iex $
        - '| IEX'
        - '|IEX '
        - I`E`X
        - I`EX
        - IE`X
        - 'iex '
        - IEX (
        - IEX(
        - Invoke-Expression
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's download-and-execute pattern in the past week, and does
    the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of the cradle pattern (not generic PowerShell usage). Many hosts firing the same
    pattern points to scripted deployment or vendor automation; one host or a small unrelated set points
    to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_download:
        CommandLine|contains:
        - .DownloadString(
        - .DownloadFile(
        - 'Invoke-WebRequest '
        - 'iwr '
        - 'Invoke-RestMethod '
        - 'irm '
      selection_iex:
        CommandLine|contains:
        - ;iex $
        - '| IEX'
        - '|IEX '
        - I`E`X
        - I`EX
        - IE`X
        - 'iex '
        - IEX (
        - IEX(
        - Invoke-Expression
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering,
    reconnaissance, exploitation of an internet-facing service, or credential access — may reveal the
    attack stages that preceded the cradle and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059
      name: Command and Scripting Interpreter
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: interpreter_invocation
      description: "Scripting interpreter (powershell.exe) is invoked, often from an abnormal parent or user context"
      mapped_to: Q8
      source: template:cue_process_tool.process_chain
    - stage: 2
      label: encoded_or_obfuscated_args
      description: "Interpreter runs with download + execution arguments, frequently obfuscated"
      mapped_to: Q3
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: secondary_execution
      description: "Cradle fetches and executes a secondary payload (in-memory or on-disk follow-on)"
      mapped_to: Q9
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059
      covered_by: Q5
      source: attack:T1059.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059
      covered_by: Q8
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059
      covered_by: Q18
      source: template:cue_host_context.precursor
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1059
      covered_by: Q5
      source: attack:T1059.command_analysis
      coverage_type: partial
    - dimension: user_context
      technique: T1059
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rationale only (encoded/uncommon script execution patterns)"
      disposition: new_question
      target_question: Q5
      notes: "Authored attack:T1059.command_analysis with concrete evasion markers (-enc, -w hidden, -nop, FromBase64String, backtick-broken IEX) since the mechanism_decode question is reflection-scoped to the rule pattern"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (varies across managed/unmanaged/user-driven)"
      disposition: context
      target_question: Q8
      notes: "No concrete values; parent legitimacy handled by process_chain question and parent+binary svc-principal questions"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (work hours / admin maintenance windows)"
      disposition: context
      target_question: Q18
      notes: "No queryable threshold; precursor question carries the temporal/prior-alert dimension"
  dedup_decisions:
    - dropped: cue_artifact.mechanism_decode
      reason: "artifact is DERIVED from a CommandLine DownloadFile token, not a file_event; the cradle's content is read from the command line, which the encoded_command_decode and mechanism_decode questions already analyze. No real TargetFilename to scope a content-analysis query."
      kept: template:powershell.encoded_command_decode
    - dropped: cue_artifact.artifact_verification
      reason: "merged into download_tool.payload_analysis (hash + reputation of the fetched payload via %file_paths%); a separate file_event verification on a derived artifact would duplicate it"
      kept: template:download_tool.payload_analysis
    - dropped: cue_artifact.baseline
      reason: "no concrete file artifact to baseline (derived from CommandLine); file presence is handled by evidence_preservation and directory_activity"
      kept: template:universal.evidence_preservation
    - dropped: cue_user_actor.baseline pairing
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering pairing
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "Image|endswith baseline of generic binary usage is superseded by cue_user_actor.baseline (rule-pattern scoped, command-line filtered) and cue_process_tool.scope (rule-pattern fleet count); a bare powershell.exe 90d frequency count adds noise without the cradle filter on a CommandLine-only rule"
    - template: cue_process_tool.baseline_deviation
      reason: "host-scoped rule-pattern parent/user distribution overlaps cue_user_actor.baseline_svc (parent+pattern on host) and the command_analysis question; dropping avoids a third near-identical host-scoped rule-pattern aggregation"
    - template: cue_process_tool.clustering
      reason: "broad host process aggregation duplicates cue_host_context.precursor (prior alerts on host) for context; cradle follow-on activity is better served by the follow_on child-process question"
    - template: cue_host_context.clustering
      reason: "host-wide process aggregation overlaps the precursor prior-alert sweep and follow_on; on an 18-question budget the precursor question carries the on-host context dimension more precisely"
    - template: download_tool.download_success_verification
      reason: "merged into universal.evidence_preservation (file-on-disk at %file_paths%) — same file_event check for download completion; keeping both would be a near-identical query"
    - template: universal.endpoint_protection_health
      reason: "powershell.powershell_security_bypass (kept) is the more specific tamper/precursor sweep for a PowerShell execution rule and covers AMSI/Defender/exec-policy weakening; the generic host-tamper sweep would duplicate it"
  hint_dispositions:
    - hint: "cue_artifact: derived: \\bDownloadFile\\b in CommandLine"
      disposition: declined
      reason: >-
        The artifact is a payload path derived from a CommandLine DownloadFile token, not a real
        file_event artifact (TargetFilename is unknown at rule time). Content/hash analysis of the
        fetched file is covered by Q3 (encoded_command_decode — identifies the fetched URL/content from
        the command line) and Q11 (download_tool.payload_analysis — hash + reputation via %file_paths%);
        on-disk presence is covered by Q10 (evidence_preservation) and Q12 (directory_activity). The
        cue_artifact.mechanism_decode/artifact_verification/baseline templates are recorded as declined
        in dedup_decisions because they would scope file_event queries to a TargetFilename that does not
        exist for this rule.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - download_tool
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 13
      file_event: 3
      powershell: 1
      sigma_alert: 1
