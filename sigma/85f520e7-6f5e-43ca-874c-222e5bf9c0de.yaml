name: Devcon Execution Disabling VMware VMCI Device
id: ef4447a2-31bc-41c9-a5db-37b5a5f95379
description: |
  Detects execution of devcon.exe (the Windows Device Console tool) with a command line that disables the VMware Virtual Machine Communication Interface (VMCI) device. The rule fires only when all three conditions match together: the image is devcon.exe (or carries the OriginalFileName DevCon.exe), the command line contains ' disable ', and the command line references the VMCI device identifier (15AD&DEV_0740 or VMWVMCIHOSTDEV). devcon.exe can enable, disable, install, start, and stop arbitrary devices from the command line without Device Manager, so it can be scripted to turn off security or virtualization devices.
  Disabling the VMCI device can be legitimate during VMware Tools troubleshooting or driver-conflict resolution, but it is also used to hijack guest-to-host communication and has facilitated VM-escape exploitation against the ESXi hypervisor. Investigation should confirm the exact command, establish whether the action was authorized and consistent with normal administration on this host, and check for surrounding persistence or defense-tampering activity.
type: detection
detection_id: 85f520e7-6f5e-43ca-874c-222e5bf9c0de
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match the devcon disable of the VMware VMCI device that this rule detects?
  context: Confirm this is a true positive by reviewing the exact command line and image against the rule's three combined criteria — the process is devcon.exe (or has OriginalFileName DevCon.exe), the command line contains ' disable ', and it references the VMCI device identifier (15AD&DEV_0740 or VMWVMCIHOSTDEV). All three must be present together for the rule to fire.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manage device state with devcon.exe?
  context: devcon.exe ships with the Windows Driver Kit and is normally found only on developer or driver-engineering systems, not general workstations or servers. Knowing whether this host's function and the acting user's role involve VMware Tools maintenance or driver troubleshooting is often the fastest way to separate authorized administration from abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for disabling the VMCI device on this host?
  context: VMware Tools troubleshooting and driver-conflict resolution are normally planned activities. A corresponding ticket or maintenance window suggests authorized work; absence of one when policy requires it shifts the disposition toward unauthorized device tampering and warrants follow-up.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:organizational_context.timing_alignment
  question: Does the timing of this activity align with normal work hours and maintenance windows?
  context: Legitimate device or driver maintenance typically occurs during business hours or scheduled windows. Disabling a virtualization device outside those windows, especially on a server, is more suspicious than the same action during a documented maintenance window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.what_was_disabled
  question: Which device or component did the devcon command target, and was it the VMCI device specifically?
  context: The full command line reveals the exact device targeted. Confirm whether the hardware identifier (15AD&DEV_0740 or VMWVMCIHOSTDEV) points at the VMware VMCI device and whether the action was a disable, remove, or a broader device operation. Disabling the VMCI device interrupts guest-to-host communication and is a precondition for some VM-escape exploitation paths.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:service_persistence.service_configuration
  question: Was a Windows service created or modified on this host around the same time, and what binary does it execute?
  context: devcon.exe can install device drivers as kernel services, and driver installation is a common persistence and privilege-escalation path tied to this activity. System event 7045 records service installations with structured fields — the image path is the payload to analyze, and a path in a world-writable or unusual directory (C:\Users\, C:\Temp\) or an unsigned binary is a strong follow-up signal.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 7045
      condition: selection
    fields:
      - ServiceName
      - ImagePath
      - StartType
      - AccountName
- source: attack:T1543.003.registry_modification
  question: Were service or device registry keys created or modified on this host around the time of the alert?
  context: Disabling a device with devcon.exe and installing drivers as services both write to registry locations under HKLM\System\CurrentControlSet (Services and Enum/device state). Registry changes capture service creation, StartType edits, and driver registration that may not appear in command-line evidence, and correlating these with the alert window reveals whether the device disable was paired with a persistence or autostart change.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - '\CurrentControlSet\Services\'
          - '\CurrentControlSet\Enum\'
      condition: selection
    fields:
      - TargetObject
      - Image
      - User
- source: template:service_persistence.service_current_status
  question: Is the VMCI device or any newly created service currently disabled or running, and under what account?
  context: A device disabled by devcon.exe stays disabled until re-enabled, and a created service may be set to demand or auto start. Checking the current state of the VMCI device and any associated service reveals whether the host remains in a tampered state. A service running as LocalSystem has full privileges; the current device/service state should be verified directly on the host before closing the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned devcon.exe and is that parent chain expected?
  context: The parent process reveals how devcon.exe was invoked. Legitimate use is typically launched interactively (explorer.exe, a console) or by a known deployment tool. Unusual parents such as mshta.exe, wscript.exe, an Office application, or a script interpreter point toward scripted or exploited execution rather than hands-on administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the VMCI device was disabled?
  context: Disabling the VMCI device is often a precondition for follow-on actions — code execution against the hypervisor, payload deployment, or further tampering. Examining process activity in the window immediately after the disable reveals what the action was meant to enable. A gap in expected telemetry followed by new process creation is itself a signal worth chasing.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense or device tampering actions performed on this host?
  context: Operators who disable one device or control often disable several — stopping services, clearing logs, deleting shadow copies, or disabling additional devices to maximize coverage. Finding multiple tampering events from the same user or parent process indicates a coordinated operation rather than an isolated maintenance action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\devcon.exe'
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - ' disable '
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this devcon VMCI-disable pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even legitimate device maintenance leaves a baseline. This query mirrors the rule's selection structure scoped to this host, so the resulting parent/user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected user account, or a first-ever occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \devcon.exe
      - OriginalFileName: DevCon.exe
      selection_action:
        CommandLine|contains: ' disable '
      selection_vmci_pci:
        CommandLine|contains:
        - 15AD&DEV_0740
        - VMWVMCIHOSTDEV
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run devcon.exe to manage device state?
  context: Compare the acting user's current action against their historical behavior. A user who has never run devcon.exe is more suspicious than one who does so routinely as part of a driver or virtualization administration role. User role and typical toolset should align.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+devcon.exe launch combination historically routine on this host or across the environment, or is the parent unusual?
  context: When the alert fires under a service principal, user identity does not attribute behavior, so the meaningful pivot is the launch mechanism — which parent process spawned devcon.exe, and whether that pair is historically common. A new or rare parent for devcon.exe is the deviation worth chasing; a pair already accounting for many prior events is likely vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar device or persistence activity on multiple hosts, the scope of the operation may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+devcon.exe combination fire on other hosts in the same window, indicating a coordinated rollout rather than isolated activity?
  context: Under a service principal, cross-network user activity is dominated by agent telemetry. The meaningful question is whether the same launch mechanism — parent process plus devcon.exe — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — reconnaissance, credential access, or other defense tampering — may reveal the attack stages that preceded the device disable and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After establishing one foothold, they commonly install backup mechanisms using different techniques — scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions. Multiple persistence installations in the same timeframe indicate intentional stacking rather than isolated administration.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1543.003
      name: 'Create or Modify System Process: Windows Service'
      role: primary
    - id: T1562.001
      name: 'Impair Defenses: Disable or Modify Tools'
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: device_disable_action
      description: "devcon.exe disables the VMCI device via command line (image + ' disable ' + VMCI identifier)"
      mapped_to: Q5
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: service_driver_creation
      description: "Driver/service installation or modification accompanying the device action (4697/7045, registry Services keys)"
      mapped_to: Q6
      source: template:service_persistence.service_configuration
    - stage: 3
      label: registry_service_state
      description: "Registry changes under CurrentControlSet\\Services / Enum tied to service or device state"
      mapped_to: Q7
      source: attack:T1543.003.registry_modification
    - stage: 4
      label: post_tamper_activity
      description: "Actions enabled by the device disable (execution, follow-on tampering)"
      mapped_to: Q10
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: tuning_parameter
      technique: T1543.003
      covered_by: Q6
      source: template:service_persistence.service_configuration
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1543
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q10
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q13
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q5
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
  mutable_dispositions:
    - element: ServiceNamePattern
      dimension: tuning_parameter
      specific_values: "rationale only (regex for uncommon service names)"
      disposition: context
      target_question: Q6
      notes: "No concrete values; service-name anomaly framed in service_configuration context"
    - element: ImagePathFilter
      dimension: tuning_parameter
      specific_values: "C:\\Users\\, C:\\Temp\\"
      disposition: context
      target_question: Q6
      notes: "World-writable image-path heuristic stated in service_configuration context"
    - element: DriverExtensionList
      dimension: tuning_parameter
      specific_values: ".sys via sc / Registry / ZwLoadDriver"
      disposition: context
      target_question: Q7
      notes: "Driver registration framed via CurrentControlSet\\Services registry query context"
    - element: RegistryPathList
      dimension: tuning_parameter
      specific_values: "HKLM\\System\\CurrentControlSet\\Services"
      disposition: query_injection
      target_question: Q7
      notes: "Injected as TargetObject|contains CurrentControlSet\\Services and \\Enum\\ in registry_set query"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window)"
      disposition: context
      target_question: Q10
      notes: "Realized as the +1h forward range on activity_after_tampering"
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only (security service name list)"
      disposition: context
      target_question: Q11
      notes: "Generic security-service tamper list; other_tampering_activity sweep covers stop/delete of services"
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only (authorized admin tools)"
      disposition: context
      target_question: Q2
      notes: "Expected-administration judgement framed in host_user_context"
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only (expected admin tools)"
      disposition: context
      target_question: Q2
      notes: "Same expected-admin framing"
    - element: UnsignedBinaryAlert
      dimension: tuning_parameter
      specific_values: "rationale only (unsigned service binaries)"
      disposition: context
      target_question: Q6
      notes: "Unsigned/unusual image-path note in service_configuration context"
    - element: StartupTypeChangeWindow
      dimension: tuning_parameter
      specific_values: "rationale only (Start key change window)"
      disposition: context
      target_question: Q7
      notes: "StartType correlation framed in registry_modification context"
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context coverage and cue_user_actor.clustering already provide around-the-time host/network context; host-wide tool clustering would duplicate activity_after_tampering + precursor without unique fields"
      kept: template:defense_tampering.activity_after_tampering
    - dropped: cue_user_actor.clustering (user-pivot) + cue_user_actor.clustering_svc (substitute)
      reason: "user_actor pair — non-deduplicable; both emitted (Q15 user-pivot, Q16 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.baseline (user-pivot) + cue_user_actor.baseline_svc (substitute)
      reason: "user_actor pair — non-deduplicable; both emitted (Q13 user-pivot, Q14 parent+binary substitute)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by service_persistence.service_configuration and defense_tampering.what_was_disabled (both present); per hints"
    - template: cue_process_tool.baseline
      reason: "host-binary frequency subsumed by cue_process_tool.baseline_deviation (rule-pattern-scoped host baseline, more specific) — avoids two near-identical host+devcon aggregations"
    - template: cue_process_tool.follow_on
      reason: "child-of-devcon question subsumed by defense_tampering.activity_after_tampering (host-wide post-action window); devcon disabling a device rarely spawns children, so a parent-scoped follow_on adds little unique signal"
    - template: cue_process_tool.scope
      reason: "fleet-wide rule-pattern count dropped to keep within 18-question cap; cross-host coordination is covered by cue_user_actor.clustering_svc (parent+binary across hosts in the alert window), which carries the same coordinated-rollout signal"
    - template: cue_host_context.clustering
      reason: "host-wide around-the-time activity is covered by defense_tampering.activity_after_tampering (+1h) and cue_host_context.precursor (prior alerts); a third host-wide aggregation would duplicate without unique fields"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled and other_tampering_activity questions are more specific than the generic host-tamper sweep, and this rule is not an execution/credential-access rule whose disposition turns on prior AV/EDR weakening"
    - template: organizational_context.human_recall
      reason: "change_ticket and timing_alignment already cover the authorization/legitimacy axis; human_recall would add a third organizational question without unique investigative fields, and slot budget is constrained at 18"
    - template: organizational_context.procedural_consistency
      reason: "device-disable-via-devcon is a developer/admin niche action; procedural-norm framing overlaps change_ticket, and dropped to stay within the 18-question cap"
    - template: persistence_scope.file_based_persistence
      reason: "rule's persistence vector is driver/service-based (devcon device disable + service install), not file-drop based; process_based_persistence covers the relevant service/registry mechanisms, and file_event Startup-folder drops are out of the immediate behavioral chain"
  hint_dispositions:
    - hint: "defense_tampering.what_was_disabled replaces cue_process_tool.mechanism_decode"
      disposition: acted-on
      reason: >-
        cue_process_tool.mechanism_decode skipped (see skipped_templates);
        defense_tampering.what_was_disabled authored as Q5.
    - hint: "service_persistence.service_configuration replaces cue_process_tool.mechanism_decode"
      disposition: acted-on
      reason: >-
        cue_process_tool.mechanism_decode skipped (see skipped_templates);
        service_persistence.service_configuration authored as Q6.
    - hint: "logsource 'registry_set' (recommended by techniques: T1543.003,T1562.001) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: acted-on
      reason: >-
        Authored Q7 (attack:T1543.003.registry_modification) with
        category: registry_set, scanning CurrentControlSet\Services and \Enum\
        for the service/device registry changes that accompany devcon-driven
        device disable and driver-as-service installation.
    - hint: "logsource 'security' (recommended by techniques: T1543.003) is not covered and no template family exists — author a custom question with category: security"
      disposition: declined
      reason: >-
        Q6 (service_persistence.service_configuration) already covers the
        security-channel intent — service creation/modification — via the
        Windows System channel event 7045, which records the same
        ServiceName/ImagePath/StartType/AccountName fields as Security event
        4697 in this environment's collection. A separate category: security
        4697 question would surface the identical service-install fact with no
        unique fields, so a second cross-channel query was not authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - service_persistence
    - organizational_context
    - persistence_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 2
      context: 15
    logsource_diversity:
      process_creation: 15
      system: 1
      registry_set: 1
      sigma_alert: 1
