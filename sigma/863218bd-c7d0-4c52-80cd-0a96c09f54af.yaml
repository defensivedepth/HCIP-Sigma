name: Arbitrary File Download Via IMEWDBLD.EXE
id: 1ebb361e-14cc-4714-aabd-a7ea816ed47f
description: |
  Detects IMEWDBLD.exe (a Microsoft IME dictionary download utility located at
  C:\Windows\System32\IME\SHARED\IMEWDBLD.exe) being invoked with an HTTP or
  HTTPS URL on its command line. Given a URL argument, the binary downloads the
  remote file and saves it to the INetCache directory, making it a signed,
  Microsoft-trusted proxy for arbitrary file retrieval. The rule fires when the
  process Image ends in \IMEWDBLD.exe (or OriginalFileName is imewdbld.exe) AND the
  command line contains a URL. Legitimate use is rare outside of IME dictionary
  updates; investigate whether the URL, the parent process, and the downloaded
  payload indicate operator-driven ingress tool transfer.
type: detection
detection_id: 863218bd-c7d0-4c52-80cd-0a96c09f54af
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting event show IMEWDBLD.exe running with an HTTP or HTTPS URL on its command line?
  context: Confirm the alert is a true positive before investing time. The rule requires both that the process is IMEWDBLD.exe (Image ends in \IMEWDBLD.exe or OriginalFileName is imewdbld.exe) and that the command line contains a URL. Read the exact command line and note the full URL that was passed as the download argument.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run IMEWDBLD.exe to fetch files over HTTP(S)?
  context: IMEWDBLD.exe is an IME dictionary download helper; outside of IME dictionary updates it is rarely run interactively, and almost never with an arbitrary external URL. Knowing whether the user role and host function involve IME configuration is often the fastest way to separate a routine action from operator-driven download abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What full URL and arguments did IMEWDBLD.exe receive, and what file would it have fetched?
  context: The command line carries the operational intent. Decode the full URL, note the scheme (http vs https), the host, and any path or file extension. IMEWDBLD.exe saves whatever it retrieves to the INetCache directory under the invoking user; the URL is the only indicator of what was requested. Multiple sequential invocations on the host may represent staged retrieval of several files.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_network_endpoint.mechanism_decode
  question: What is the download destination in the URL, and what do DNS, WHOIS, and threat intel reveal about it?
  context: Investigate the host in the URL passed to IMEWDBLD.exe — DNS resolution history, registration age, TLS certificate details, and known threat intelligence hits. Recently registered domains, raw IP addresses, dynamic-DNS hosts, or known malware-staging infrastructure point to attacker-controlled endpoints; an internal or well-known vendor host points to benign activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationIp|expand: '%dst_ip%'
      condition: selection
    fields:
      - DestinationHostname
      - DestinationPort
- source: template:cue_process_tool.process_chain
  question: What process spawned IMEWDBLD.exe, and is that parent chain expected?
  context: The parent process reveals how IMEWDBLD.exe was launched. Legitimate IME dictionary updates are typically driven by the input-method framework or user shell. A parent such as cmd.exe, powershell.exe, wscript.exe, mshta.exe, or an Office application suggests scripted or operator-driven invocation of the binary as a download proxy rather than genuine IME use.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.download_success_verification
  question: Did IMEWDBLD.exe write a file to disk after the download, and where?
  context: A download command does not guarantee the file was retrieved — the URL may have been blocked, returned an error, or been a probe. IMEWDBLD.exe saves successful retrievals to the INetCache directory under the invoking user. Confirming a file write by IMEWDBLD.exe establishes whether the attack progressed past delivery; the destination path is not in the command line, so scope file events to the binary itself.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1218.payload_analysis
  question: Can you retrieve and analyze the downloaded file from a safe environment?
  context: If the retrieved file is still on disk in INetCache, submitting its hash to threat intelligence or detonating it in an isolated environment reveals whether it is a known malicious payload, a benign file, or something novel. The hash and path of the file written by IMEWDBLD.exe are the pivot for reputation lookup.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: attack:T1218.follow_on
  question: What child processes did IMEWDBLD.exe spawn afterward, and were any downloaded files executed?
  context: A signed download proxy is typically the delivery stage; the impact comes from what runs next. Look for execution of the retrieved payload or for IMEWDBLD.exe spawning cmd.exe, powershell.exe, or other interpreters. Genuine IME dictionary use spawns no follow-on processes, so any child process here is investigation-worthy.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Operators often weaken protection before staging an ingress download. This query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW in the hour preceding the alert. Zero rows is a useful "protection looks healthy" signal; populated rows shift the disposition toward operator preparation, so review the writing process and its signer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_process_tool.baseline
  question: Does this host normally run IMEWDBLD.exe at all?
  context: IMEWDBLD.exe is an on-demand IME helper; on most hosts it runs rarely or never. A zero or very low historical count over 90 days indicates first-time or rare execution and raises the weight of the URL-download invocation. A host that runs it routinely (e.g., one configured for an East Asian input method) sets a different baseline.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even an IME helper can be abused. The query mirrors the rule's selection (IMEWDBLD.exe with a URL on the command line) scoped to this host, so the resulting parent/user distribution reflects how the rule's specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual URL shape relative to that history is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \IMEWDBLD.exe
      - OriginalFileName: imewdbld.exe
      selection_cli:
        CommandLine|contains:
        - http://
        - https://
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_network_endpoint.baseline
  question: Does this host normally communicate with this download destination?
  context: First-time connections to the URL's host are more suspicious than routine traffic. Establishing whether this host has reached the destination before helps distinguish new attacker infrastructure from a known internal or vendor endpoint. Zero or very low historical counts indicate a new destination for this host.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationIp|expand: '%dst_ip%'
      condition: selection
    fields:
      - DestinationPort
      - Image
- source: template:cue_user_actor.baseline
  question: Does this user normally run IMEWDBLD.exe across the environment?
  context: Compare this user's current action against their historical behavior. A user who has never run IMEWDBLD.exe is more notable than one who does so regularly, and the user's role should align with IME configuration work. Zero or very low counts indicate the user does not normally use this binary.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+IMEWDBLD.exe combination historically routine on this host or environment-wide, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism — which parent spawned IMEWDBLD.exe, and whether that pair is historically common. A new or rare parent for this binary is the deviation worth chasing; a pair accounting for thousands of prior events points to OS or vendor automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar download or execution activity on other hosts, the scope of the operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+IMEWDBLD.exe combination fire on other hosts in the same window — a coordinated rollout, or isolated to this host?
  context: Under a service principal, "what else did this user do" returns agent-telemetry noise. The meaningful question is whether the same launch mechanism — parent process plus IMEWDBLD.exe — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection (IMEWDBLD.exe with a URL) with no host scope, giving a fleet-wide count of the firing pattern rather than generic binary usage. Many hosts firing the same pattern points to scripted deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \IMEWDBLD.exe
      - OriginalFileName: imewdbld.exe
      selection_cli:
        CommandLine|contains:
        - http://
        - https://
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or credential access — may reveal the stages that preceded this download and help reconstruct the full timeline leading to the proxy execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: proxy_binary_invocation
      description: "Trusted, Microsoft-signed binary (IMEWDBLD.exe) is invoked with a URL argument on the command line"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_line_intent
      description: "The URL/argument is decoded to determine what remote file was requested"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: network_retrieval
      description: "Binary contacts the remote endpoint to retrieve the externally hosted payload"
      mapped_to: Q4
      source: template:cue_network_endpoint.mechanism_decode
    - stage: 4
      label: payload_landed
      description: "Retrieved file is written to disk (INetCache) — delivery succeeded"
      mapped_to: Q6
      source: attack:T1218.download_success_verification
    - stage: 5
      label: payload_execution
      description: "Downloaded payload is executed or the proxy binary spawns follow-on processes"
      mapped_to: Q8
      source: attack:T1218.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q12
      source: template:cue_network_endpoint.baseline
      coverage_type: partial
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profile unexpected parent-child relationships)"
      disposition: context
      target_question: Q5
      notes: "Parent-chain analysis question surfaces unexpected parents (cmd/powershell/wscript/mshta/Office); baseline_deviation and baseline_svc also pivot on parent."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (allowlist of signed binaries — IMEWDBLD.exe is the single binary this rule scopes to)"
      disposition: context
      target_question: Q1
      notes: "Rule already constrains to one signed binary (IMEWDBLD.exe); no multi-binary list to inject. verify_trigger_match documents the binary identity."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "http://, https:// (URL match on command line)"
      disposition: query_injection
      target_question: Q11
      notes: "URL pattern is the rule's selection_cli; preserved verbatim in the REFLECTED baseline_deviation and scope queries which embed the rule's http://,https:// CommandLine|contains."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q4
      notes: "Destination reputation question guides analyst to distinguish enterprise/vendor hosts from attacker infrastructure; allowlist is environment-specific tuning, not directly queryable here."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context.clustering and cue_user_actor.clustering already provide same-window activity context; a third generic host-clustering question would duplicate without adding a distinct pivot."
      kept: cue_user_actor.clustering
    - dropped: cue_host_context.clustering
      reason: "user_actor.clustering (user-pivot, network-wide) plus the +/-1h same-window coverage make the generic host Image+ParentImage clustering redundant for this single-binary download rule."
      kept: cue_user_actor.clustering
    - dropped: download_tool.directory_activity
      reason: "Rule has no file_reference (has_file_reference: false) and IMEWDBLD writes to INetCache with no CLI-specified destination, so %file_dirs% does not resolve; directory-scoped file query would be unanchored. download_success_verification (Image-scoped) covers payload landing instead."
      kept: attack:T1218.download_success_verification
    - dropped: cue_user_actor.baseline / cue_user_actor.clustering service-principal pairs
      reason: "user_actor pair — non-deduplicable; both user-pivot and parent+binary substitute variants emitted per AGENTS.md (baseline + baseline_svc, clustering + clustering_svc)."
      kept: cue_user_actor.baseline_svc / cue_user_actor.clustering_svc
  skipped_templates:
    - template: download_tool.directory_activity
      reason: "has_file_reference false; %file_dirs% unresolved for INetCache destination — see dedup_decisions."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with category: image_load"
      disposition: declined
      reason: >-
        The T1218 image_load analytic targets the DLL-proxy variant of system
        binary proxy execution (rundll32/regsvr32/msiexec loading an unsigned or
        externally hosted DLL via EventCode=7). This rule's specific detection is
        a network-retrieval variant: IMEWDBLD.exe takes an http/https URL as a
        command-line argument and downloads a file to INetCache — there is no
        DLL-load step in the abuse path. The network-retrieval branch of the same
        analytic is covered by the destination-reputation question
        (cue_network_endpoint.mechanism_decode) and the network baseline question
        (cue_network_endpoint.baseline). An image_load query on IMEWDBLD.exe would
        return its own benign module loads with no precision for the URL-download
        behavior the rule fires on. No compensating attack:T1218.image_load
        question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - download_tool
    - cue_process_tool
    - cue_network_endpoint
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 13
      network_connection: 2
      file_event: 2
      sigma_alert: 1
