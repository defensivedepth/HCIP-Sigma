name: Change PowerShell Policies to an Insecure Level
id: 5ea079ff-9007-4674-b363-a27bcfb1ad8d
description: |
  Detects PowerShell (powershell.exe, powershell_ise.exe, or pwsh.exe, matched by image name or original filename) launched with an execution-policy switch (-ExecutionPolicy, -ep, or -exec) AND a permissive level of Bypass or Unrestricted, excluding the known-good msiexec-launched PowerShell 7 installer and the Avast updater patterns. The PowerShell execution policy is not a security boundary, but flipping it to Bypass or Unrestricted is a common preparatory step so that unsigned or downloaded scripts run without prompts — frequently as the leading switch of a one-liner that also carries -File, -Command, or -EncodedCommand. Administrators set permissive policies legitimately during deployment, so the investigation should decode what the session actually ran under the relaxed policy, confirm the parent chain that launched it, and determine whether this user and host normally invoke PowerShell this way.
type: detection
detection_id: 87e3c4e8-a6a8-4ad9-bb4f-46e7ff99a180
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-03
questions:
- source: template:universal.verify_trigger_match
  question: Does this match the rule — PowerShell launched with an execution-policy switch set to Bypass or Unrestricted?
  context: Confirm the alert before investing time. The rule requires all three conditions — the image (or original filename) is powershell.exe, powershell_ise.exe, or pwsh.exe; the command line carries an execution-policy switch (-executionpolicy, -ep, or -exec); and the level is Bypass or Unrestricted. The msiexec-launched PowerShell 7 installer and the Avast updater are filtered out. Read the full command line and confirm the policy switch and level, and note what else the line carries (a -File path, -Command, or -EncodedCommand), since that payload is the real subject of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to set a permissive PowerShell execution policy?
  context: Deployment tooling, packaging scripts, and some admin workflows legitimately pass -ExecutionPolicy Bypass to run an unsigned script. Knowing whether this account and host have a reason to do so — a build server or an IT admin versus a standard user workstation — is often the fastest way to separate routine automation from a hands-on bypass.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Weakening the execution policy is often one step in a broader preparation that also disables protection before a payload runs. This surfaces direct attempts to disable AV/EDR services, flip Defender settings, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal — kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the writing process and its signing chain.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_process_tool.mechanism_decode
  question: What did PowerShell run under the relaxed policy — decode the rest of the command line.
  context: The execution-policy switch is rarely the whole command. Recover what accompanied it — a -File path, an inline -Command, or an -EncodedCommand blob — and the scope (Process, CurrentUser, or LocalMachine). A bypass paired with a script in a user-writable or temp path, a download cradle, or a hidden window is the pattern that matters; a bypass paired with a signed deployment script under Program Files is far more likely routine. Scoping to the policy-bypass invocations on this host in the window captures a multi-step sequence where one call sets policy and a later one runs the payload.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains:
          - '-executionpolicy '
          - ' -ep '
          - ' -exec '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Was the bypassed command encoded or obfuscated, and what does it decode to?
  context: A permissive execution policy is often set precisely so an obfuscated payload can run. If the command line carries -EncodedCommand (-enc) or uses string concatenation, backticks, or variable substitution, decode it to recover the real script — a download cradle, a reverse shell, or in-memory execution of a .NET assembly. If the command is plain text, identify the cradle pattern directly and check any source URL's reputation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture what ran under the relaxed policy?
  context: Windows Event ID 4104 records the deobfuscated script PowerShell actually executed, even when the command line is encoded or built from concatenated strings — the ground truth for what the bypassed session did. This is the natural follow-on to a policy-change alert, because the policy switch only enables execution; the 4104 content shows the result. If PowerShell operational logging is not collected in this environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell, and is that parent chain expected?
  context: A bypass invocation launched from an interactive admin session or a deployment service is routine; the same invocation spawned by winword.exe or excel.exe (a macro), mshta.exe, wscript.exe, or another script host points to a delivery chain that handed off to PowerShell. The parent's own command line shows whether a loader or document drove the policy change, and walking up from there reconstructs how it started.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What did this PowerShell process spawn afterward?
  context: The point of relaxing the policy is to run something. Children of this PowerShell process reveal what the unsigned script did — a new cmd.exe or another powershell.exe, csc.exe compiling code in memory, rundll32 or regsvr32 running a payload, or a dropped executable. A quiet bypass that spawns nothing reads very differently from one that immediately launches tooling.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:powershell.powershell_security_bypass
  question: Beyond the execution policy, were other PowerShell protections weakened on this host around this time?
  context: The rule already caught the execution-policy change, so this looks for the other controls an operator commonly weakens alongside it — an AMSI patch, a downgrade to PowerShell v2 (which lacks script block logging), or known obfuscation-framework markers. Finding these clustered near the policy change strengthens the case that the bypass is preparation for malicious execution rather than a routine deployment switch.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously set a permissive PowerShell policy, what parents and users drove it, and does the current alert fit?
  context: Even a policy bypass has a normal shape on a given host. This mirrors the rule's pattern scoped to this host, so the parent/user distribution reflects how policy-bypass invocations have historically run here — a deployment service under a known account, an admin in an interactive session. A new parent process, an unexpected user, or an unusual command shape against that baseline is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - OriginalFileName:
        - powershell_ise.exe
        - PowerShell.EXE
        - pwsh.dll
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      selection_option:
        CommandLine|contains:
        - '-executionpolicy '
        - ' -ep '
        - ' -exec '
      selection_level:
        CommandLine|contains:
        - Bypass
        - Unrestricted
      filter_main_powershell_core:
        ParentImage:
        - C:\Windows\SysWOW64\msiexec.exe
        - C:\Windows\System32\msiexec.exe
        CommandLine|contains:
        - -NoProfile -ExecutionPolicy Bypass -File "C:\Program Files\PowerShell\7\
        - -NoProfile -ExecutionPolicy Bypass -File "C:\Program Files (x86)\PowerShell\7\
      filter_optional_avast:
        ParentImage|contains:
        - C:\Program Files\Avast Software\Avast\
        - C:\Program Files (x86)\Avast Software\Avast\
        - \instup.exe
        CommandLine|contains:
        - -ExecutionPolicy ByPass -File "C:\Program Files\Avast Software\Avast
        - -ExecutionPolicy ByPass -File "C:\Program Files (x86)\Avast Software\Avast\
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run PowerShell with a permissive execution policy across the environment?
  context: Compare this user's current action against their historical pattern. A user who has never set -ExecutionPolicy Bypass is more suspicious than a deployment or admin account that does so routinely. This pivot is meaningful only when a real user account fired the alert; the launch-mechanism baseline alongside it carries the answer when the actor is a service or computer account.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-and-PowerShell combination historically routine on this host or across the environment?
  context: When the alert fires under a service or computer account, user identity does not attribute behavior, so the meaningful baseline is the launch mechanism — which parent process spawned PowerShell, and whether that pairing is historically common. A new or rare parent (an Office application or a script host where none was seen before) is the deviation worth chasing; a pairing that already accounts for thousands of prior events is deployment or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts in a tight window reveals lateral movement or activity spanning multiple endpoints. If the same user ran PowerShell or other tooling on other hosts in the same window, the scope is larger than a single machine. This pivot is meaningful only when a real user account fired the alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-and-PowerShell combination fire on other hosts in the same window?
  context: Under a service or computer account, asking what else the user did returns agent-telemetry noise. The meaningful question is whether the same launch mechanism — this parent spawning PowerShell with a policy bypass — fired on other hosts in the alert window. Multiple hosts at once points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts set a permissive PowerShell policy this way in the past week, and does the distribution look like automation or one-off activity?
  context: This mirrors the rule's pattern with no host scope, giving a fleet-wide count of where the policy-bypass invocation fired. Many hosts at once points to a pushed deployment or packaging routine; one host or a small unrelated set points to manual operator activity. Use the distribution to judge how unusual this single alert is.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - OriginalFileName:
        - powershell_ise.exe
        - PowerShell.EXE
        - pwsh.dll
      - Image|endswith:
        - \powershell_ise.exe
        - \powershell.exe
        - \pwsh.exe
      selection_option:
        CommandLine|contains:
        - '-executionpolicy '
        - ' -ep '
        - ' -exec '
      selection_level:
        CommandLine|contains:
        - Bypass
        - Unrestricted
      filter_main_powershell_core:
        ParentImage:
        - C:\Windows\SysWOW64\msiexec.exe
        - C:\Windows\System32\msiexec.exe
        CommandLine|contains:
        - -NoProfile -ExecutionPolicy Bypass -File "C:\Program Files\PowerShell\7\
        - -NoProfile -ExecutionPolicy Bypass -File "C:\Program Files (x86)\PowerShell\7\
      filter_optional_avast:
        ParentImage|contains:
        - C:\Program Files\Avast Software\Avast\
        - C:\Program Files (x86)\Avast Software\Avast\
        - \instup.exe
        CommandLine|contains:
        - -ExecutionPolicy ByPass -File "C:\Program Files\Avast Software\Avast
        - -ExecutionPolicy ByPass -File "C:\Program Files (x86)\Avast Software\Avast\
      condition: all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — a macro-spawned shell, a download cradle, defense tampering, or reconnaissance — may reveal the stages that preceded the policy change and help reconstruct the timeline. Setting a permissive policy is frequently an early preparatory step, so what came just before and just after is what tells the story.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: policy_change
      description: "PowerShell is launched with a permissive execution policy (the rule trigger)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_decode
      description: "Recover what ran under the relaxed policy (-File / -Command / -EncodedCommand)"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_execution
      description: "The unsigned script executes — surfaced by script block logging and spawned children"
      mapped_to: Q6
      source: template:powershell.script_block_logging
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: template:cue_host_context.precursor
      coverage_type: indirect
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q6
      source: template:powershell.script_block_logging
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "execution-policy switches (-executionpolicy, -ep, -exec) plus the accompanying -File / -Command / -EncodedCommand"
      disposition: query_injection
      target_question: Q4
      notes: "mechanism_decode (Q4) is scoped with the rule's execution-policy switches via CommandLine|contains so the host-scoped decode returns only policy-bypass invocations; the encoded form is decoded in Q5."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "winword.exe, excel.exe, mshta.exe, wscript.exe (suspicious) vs deployment service / interactive admin (benign)"
      disposition: context
      target_question: Q7
      notes: "process_chain is parent-entity scoped; abnormal parents are named in context rather than filtered."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q16
      notes: "No discrete value; off-hours/maintenance-window framing carried by the precursor timeline."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rare .NET assemblies"
      disposition: context
      target_question: Q6
      notes: "image_load was declined for this rule (see hint_dispositions); the executed payload is surfaced through script block logging (Q6) and spawned children (Q8) instead, which is the precise cross-logsource pivot for an execution event."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Threshold is not directly queryable; script_block_logging surfaces the 4104 content for manual review."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "powershell.exe runs constantly, so a generic host+Image baseline is uninformative. baseline_deviation (Q10) reflects the rule's actual policy-bypass pattern scoped to the host and is the load-bearing 'is this normal here' signal."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "Host-wide tool clustering overlaps the precursor alert timeline (Q16) and user network-wide clustering (Q13); a flat Image-only host listing would add noise without a new angle."
      kept: cue_host_context.precursor
    - dropped: cue_host_context.clustering
      reason: "Surrounding host activity is reached via the precursor alert question (Q16) and follow_on (Q8); avoided a near-duplicate broad host-scope aggregation."
      kept: cue_host_context.precursor
  skipped_templates:
    - template: obfuscation.encoded_payload_decode
      reason: "brief hint 'powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode' — the obfuscation set is not active; powershell.encoded_command_decode (Q5) provides the decode coverage."
  reflection_overrides: []
  hint_dispositions:
    - hint: "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode"
      disposition: acted_on
      reason: >-
        powershell.encoded_command_decode is included as Q5; the superseded
        obfuscation.encoded_payload_decode is recorded under skipped_templates.
    - hint: >-
        logsource 'image_load' (recommended by techniques: T1059.001) is not covered — pull in cue_image_load.* or author a custom
        question with `category: image_load`
      disposition: declined
      reason: >-
        The rule fires on powershell.exe / powershell_ise.exe / pwsh.exe directly
        setting a permissive execution policy — a PowerShell execution event whose
        natural cross-logsource pivot is the script-block content (Event 4104),
        which Q6 covers. image_load of powershell surfaces the standard automation
        assemblies it always loads and offers no precision for a policy-change rule,
        and no specific rare-assembly filter is cleanly expressible here. This is
        the AGENTS.md justified-decline case: a rule firing on script-block-relevant
        execution does not need image_load coverage of the launching process.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
  user_actor_pairs:
    - reason: "user_actor pair — non-deduplicable (baseline): Q11 pivots on user identity, Q12 on launch mechanism (parent+PowerShell)"
    - reason: "user_actor pair — non-deduplicable (clustering): Q13 pivots on user identity, Q14 on launch mechanism (parent+PowerShell)"
  generation_stats:
    total_questions: 16
    source_counts:
      template: 16
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 14
      powershell: 1
      sigma_alert: 1
