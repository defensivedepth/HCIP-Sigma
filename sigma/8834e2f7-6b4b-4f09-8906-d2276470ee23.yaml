name: PsExec/PAExec Escalation to LOCAL SYSTEM
id: ef4fcb77-0dea-4f53-8a55-b195a4211e5c
description: |
  Detects command-line flags used by PsExec and PAExec to launch a command interpreter
  (cmd, powershell, or pwsh) under the LOCAL SYSTEM account. The rule fires when a command
  line contains a system-escalation flag combination (-s, or -s with -i in either order)
  immediately preceding a shell name AND also contains one of the tool markers (psexec,
  paexec, or accepteula). Both conditions must be present together. Administrators
  occasionally escalate to SYSTEM for maintenance or while following vendor troubleshooting
  steps, so the investigation should establish whether the user, host, and parent chain are
  consistent with sanctioned administration, or whether this is an operator obtaining an
  interactive SYSTEM shell as a step toward credential access, lateral movement, or impact.
type: detection
detection_id: 8834e2f7-6b4b-4f09-8906-d2276470ee23
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain a PsExec/PAExec marker together with a flag combination that escalates a shell to LOCAL SYSTEM?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when both a tool marker (psexec, paexec, or accepteula) AND a system-escalation flag combination preceding a shell name (for example -s cmd, -s -i powershell, or -i -s pwsh) are present in the same command line. Read the exact command line and confirm both halves are present rather than an incidental substring.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to escalate a shell to LOCAL SYSTEM using PsExec or PAExec?
  context: Legitimate administrative activity can trigger this detection. A workstation admin or maintenance account running this on a host they manage, or someone following documented vendor troubleshooting steps, is far more routine than the same pattern under a standard user or service account. Knowing whether the user role and host function fit SYSTEM-shell administration is often the fastest path to a disposition.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What is the full command line, and which shell and flag combination was used to obtain the SYSTEM context?
  context: The full command line reveals exactly which interpreter (cmd, powershell, or pwsh) was launched and whether the session is interactive (-i present) or detached. Note any target host specification, copy flags (-c), custom service name (-r or -noname), or credential flags (-u/-p), since these distinguish a local SYSTEM shell from a remote push that could stage a payload across hosts.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this command, and is that parent chain expected?
  context: The parent process reveals how the escalation was invoked. An interactive admin session (explorer.exe, an RDP-hosted shell, or a remote-management console) is consistent with sanctioned use, while parents such as script hosts, Office applications, or a freshly dropped binary point toward operator activity. Walk up the tree to see what spawned the parent and what else it launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: How often has this same SYSTEM-escalation pattern run on this host before, and which users drove it?
  context: A command interpreter alone runs constantly, so a generic binary baseline is meaningless here. This query counts only command lines that match the rule's escalation pattern on this host over 90 days, grouped by user. Zero or very low counts indicate a first-time or rare event; a steady history under known maintenance accounts points toward sanctioned administration.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      selection_host:
        host.name|expand: '%hostname%'
      condition: all of selection_*
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, which parents and users normally drive it, and does the current alert fit?
  context: Even legitimate SYSTEM escalation tends to come from a consistent set of parents and accounts on a given host. The query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected account, or an unusual command structure is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did this escalated shell spawn afterward?
  context: A SYSTEM shell is a means to an end. The processes launched from it reveal the actual objective, whether that is credential-access tooling, reconnaissance, registry or hive access (for example regedit reading SAM/SECURITY), service or task creation, or payload execution. Scoping to the alerting process instance keeps the result precise rather than returning every child of an interpreter.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired this same SYSTEM-escalation pattern in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of this specific escalation pattern rather than generic interpreter usage. Many hosts firing the same pattern points to scripted deployment or vendor automation; a single host or a small unrelated set points to manual operator activity. Use the host spread and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether this escalation is isolated or part of broader movement. If the same user triggered activity on multiple endpoints in the same window, the scope of the operation may extend past this host. This view is most meaningful when the alert fires under a named interactive account rather than a service principal.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_sys:
        CommandLine|contains|windash:
        - ' -s cmd'
        - ' -s -i cmd'
        - ' -i -s cmd'
        - ' -s pwsh'
        - ' -s -i pwsh'
        - ' -i -s pwsh'
        - ' -s powershell'
        - ' -s -i powershell'
        - ' -i -s powershell'
      selection_other:
        CommandLine|contains:
        - psexec
        - paexec
        - accepteula
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining what ran on this host shortly before and after the escalation reveals the immediate context, such as preceding reconnaissance, the tool being transferred in, or follow-on persistence and impact. Aggregating by parent and child collapses repetitive agent telemetry while novel process pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host, such as defense tampering, reconnaissance, or credential access, may reveal the attack stages that preceded this escalation and help reconstruct the timeline. The sequence of alerts often tells the story leading up to an operator obtaining a SYSTEM shell.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering with protection often precedes hands-on-keyboard escalation. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful healthy-protection signal, though kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: false
  techniques:
  - id: T1587.001
    name: Malware
    role: primary
  - id: T1587
    name: Develop Capabilities
    role: secondary
  attack_chain_coverage: []
  dimension_coverage: []
  mutable_dispositions: []
  dedup_decisions:
  - dropped: cue_process_tool.clustering
    reason: "Host-wide process_creation aggregated by Image around the alert duplicates cue_host_context.clustering, which is broader (Image + ParentImage). Kept the host_context version."
    kept: cue_host_context.clustering
  - dropped: none
    reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline (subject identity) and cue_user_actor.baseline_svc (launch mechanism) emitted as both."
    kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
  - dropped: none
    reason: "user_actor pair — non-deduplicable: cue_user_actor.clustering (subject identity) and cue_user_actor.clustering_svc (launch mechanism) emitted as both."
    kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
  - hint: 'cmd.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode'
    disposition: acted_on
    reason: >-
      mechanism_decode (Q3) scopes by ProcessGuid (the alerting instance) rather than
      Image|endswith on cmd.exe. baseline (Q5) was rewritten from Image|endswith on cmd.exe
      to the rule's CommandLine escalation pattern scoped to host. follow_on (Q7) scopes by
      ParentProcessGuid rather than ParentImage on cmd.exe.
  - hint: CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries
    disposition: acted_on
    reason: >-
      The rule has no Image field, so every host/fleet/user historical query uses the rule's
      CommandLine|contains(|windash) escalation pattern for scoping instead of %Image%:
      baseline (Q5), baseline_deviation (Q6), scope (Q8), user_actor.baseline (Q9),
      baseline_svc (Q10), clustering_svc (Q12) all carry the CommandLine selection blocks.
      follow_on (Q7) and process_chain (Q4) pivot on ProcessGuid/ParentProcessGuid, which is
      instance-precise and does not rely on Image.
  contextual_sets:
    included: []
    excluded:
    - id: lateral_movement
      confidence: medium
      reason: >-
        The rule's detection requires a local SYSTEM-shell flag combination plus a tool
        marker; it does not require the remote-target (\\host, @file, -u/-p) flags that
        constitute the lateral-movement behavior. The brief emitted no lateral_movement
        templates. Remote-push framing is surfaced in mechanism_decode (Q3) context instead
        of adding speculative lateral-movement questions.
    - id: ransomware_impact
      confidence: low
      reason: >-
        Keyword match on 'ransomware' in references is incidental — the rule detects SYSTEM
        escalation, not encryption or recovery-inhibition behavior. No ransomware_impact
        templates were emitted by the brief. follow_on (Q7) and host_context.clustering (Q14)
        already surface any impact-stage children without an impact-specific question.
    - id: network_scope
      confidence: low
      reason: >-
        Keyword match on 'ransomware' is incidental and the rule has no network_connection
        logsource or destination fields. No network_scope templates were emitted; adding a
        network query would not map to the rule's process_creation detection.
  technique_sets_applied:
  - universal
  generation_stats:
    total_questions: 15
    source_counts:
      template: 15
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 12
    logsource_diversity:
      process_creation: 14
      sigma_alert: 1
