name: UAC Bypass WSReset
id: 333741e5-243b-42cc-b93c-5db2b6f83711
description: |
  Detects wsreset.exe running at a High or System integrity level, the signature of a UAC bypass that abuses this auto-elevated Store-reset binary. wsreset.exe reads a command from an HKCU shell-handler key during startup and runs it as a high-integrity process without a consent prompt, so an elevated wsreset.exe is rarely a normal user action. Determine whether the elevation was operator-driven by examining the parent chain, any HKCU handler-key writes that preceded it, and what high-integrity activity followed.
type: detection
detection_id: 89a9a0e0-f61a-42e5-8957-b1479565a658
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did wsreset.exe run at High or System integrity, matching what this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the process image and integrity level against the rule's detection criteria. The rule fires only when wsreset.exe runs at High, System, or the equivalent SID-form integrity levels, so an elevated wsreset.exe with no consent prompt is the pattern to verify.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - IntegrityLevel
- source: template:universal.host_user_context
  question: Is this user and host expected to run wsreset.exe at an elevated integrity level?
  context: Legitimate Store troubleshooting runs wsreset.exe at the user's own integrity level, not High or System. Knowing whether the user role and host function involve administrative elevation is often the fastest path to distinguishing a true positive from a routine Store reset.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What was the full wsreset.exe command line, and did it carry any non-default arguments?
  context: Normal Store-reset invocations run wsreset.exe with no arguments. Decode the full command line and working directory to confirm whether this is a bare auto-elevated launch or carries unexpected parameters. The bypass mechanism lives in a registry handler key rather than the command line, so a blank command line here does not clear the alert.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this elevated wsreset.exe, and is that parent chain expected?
  context: The parent reveals how wsreset.exe was launched. A user-driven Store reset is typically spawned from explorer.exe at medium integrity. A parent such as a shell, script host, or Office application spawning an already-elevated wsreset.exe points to a scripted bypass rather than a manual Store action.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1548.002.bypass_registry_setup
  question: Was the WSReset HKCU shell-handler key written or modified before this alert?
  context: The WSReset bypass works by writing an attacker command to the HKCU Store-app shell open-command handler that wsreset.exe reads at startup. Finding a registry write to that handler key shortly before the elevated launch confirms the bypass mechanism and reveals the exact payload that ran with high integrity. Absence of a write may mean the registry channel is not collected in this environment.
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - 'AppX82a6gwre4fdg3bt635tn5ctqjf8msdd2\Shell\open\command'
          - 'Classes\ms-windows-store'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: template:uac_bypass.bypass_technique_registry_artifacts
  question: Were registry-handler writes made through reg.exe, powershell.exe, or cmd.exe to set up this UAC bypass?
  context: Many UAC bypasses write a command to an HKCU shell-handler key that an auto-elevated process reads and executes. Common targets include the ms-windows-store handler abused by WSReset, plus ms-settings, mscfile, and exefile handlers used by sibling bypasses. Finding the write through a command-line tool confirms the mechanism and reveals the payload that ran elevated.
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\reg.exe'
          - '\powershell.exe'
          - '\cmd.exe'
        CommandLine|contains:
          - 'ms-windows-store'
          - 'ms-settings'
          - 'mscfile'
          - 'Classes\exefile'
          - 'Classes\Folder'
          - 'Environment'
          - 'Shell\Open\command'
          - 'DelegateExecute'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:uac_bypass.elevated_action_after_bypass
  question: What action was performed with elevated privileges after the UAC bypass?
  context: UAC bypass is a means to an end -- the elevation exists to perform actions that require high integrity, such as installing persistence, dumping credentials, tampering with security tools, or accessing protected resources. Examining processes that ran at High or System integrity on this host shortly after the bypass reveals the objective.
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        IntegrityLevel:
          - 'High'
          - 'System'
          - 'S-1-16-16384'
          - 'S-1-16-12288'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:cue_process_tool.follow_on
  question: What child processes did this elevated wsreset.exe spawn?
  context: A normal Store reset spawns at most mmc.exe or no child at all. A child such as cmd.exe, powershell.exe, or another interpreter spawned by an elevated wsreset.exe is the payload the bypass was set up to launch, and reveals the actual impact of the elevation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:uac_bypass.other_escalation_attempts
  question: Were other privilege escalation or UAC bypass detections seen on this host around the alert?
  context: Attackers who attempt one UAC bypass often try multiple methods until one succeeds, especially when the OS version or configuration blocks certain techniques. Multiple bypass or escalation alerts in a short window confirm deliberate privilege escalation activity rather than a benign elevated Store reset.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:cue_process_tool.baseline
  question: Does this host normally run wsreset.exe?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. wsreset.exe is invoked only when a user troubleshoots the Store, so it should be rare on most hosts. Zero or very low historical counts indicate first-time or rare execution worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this elevated-wsreset pattern, what parents and users drove it, and does the current alert fit?
  context: Even a known binary can be abused. The query mirrors the rule's selection -- wsreset.exe at High or System integrity -- scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected user, or an unusual launch shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \wsreset.exe
        IntegrityLevel:
        - High
        - System
        - S-1-16-16384
        - S-1-16-12288
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run wsreset.exe at an elevated integrity level?
  context: Compare this user's current action against their historical behavior. A user who has never run wsreset.exe -- let alone at High or System integrity -- is more suspicious than one who does so regularly. The user's role and typical toolset should align with administrative elevation for this to read as routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-wsreset.exe combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism -- which parent process spawned wsreset.exe, and whether that pair is historically common. A new or rare parent for this binary is the deviation worth chasing; a pair that already accounts for many prior events is OS or vendor automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar elevation activity on other hosts, the scope of the operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-wsreset.exe combination fire on other hosts in the same window, indicating a coordinated rollout rather than host-local activity?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise, since every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism -- parent process plus spawned binary -- fired on other hosts in the alert window. Multiple hosts point to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to the elevation, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, additional escalation attempts, or persistence installation.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired this elevated-wsreset pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection -- wsreset.exe at High or System integrity -- with no host scope, so the result is a fleet-wide count of this firing pattern rather than generic wsreset.exe usage. Many hosts firing the same pattern points to scripted deployment or a shared image quirk; one host or a small unrelated set points to manual operator activity. Use the distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \wsreset.exe
        IntegrityLevel:
        - High
        - System
        - S-1-16-16384
        - S-1-16-12288
      condition: selection
    fields:
    - host.name
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering typically precedes hands-on-keyboard action that an elevated context enables. The query surfaces direct attempts to disable protection services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal; kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation -- review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1548.002
      name: Bypass User Account Control
      role: primary
    - id: T1548
      name: Abuse Elevation Control Mechanism
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: bypass_registry_setup
      description: "Actor writes an attacker command to an HKCU shell-handler key (the WSReset ms-windows-store handler) that the auto-elevated binary reads at startup"
      mapped_to: Q5
      source: attack:T1548.002.bypass_registry_setup
    - stage: 2
      label: auto_elevated_execution
      description: "Auto-elevated wsreset.exe runs at High/System integrity without a consent prompt and executes the planted command"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: elevated_action
      description: "High-integrity child/process performs the privileged objective (persistence, credential access, tampering)"
      mapped_to: Q7
      source: template:uac_bypass.elevated_action_after_bypass
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1548.002
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1548.002
      covered_by: Q5
      source: attack:T1548.002.bypass_registry_setup
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1548.002
      covered_by: Q7
      source: template:uac_bypass.elevated_action_after_bypass
      coverage_type: direct
    - dimension: user_context
      technique: T1548
      covered_by: Q12
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (e.g., 30 seconds correlation window)"
      disposition: context
      target_question: Q5
      notes: "Registry-setup question uses a -15m backward range to correlate the handler-key write with the elevated launch; exact threshold is environment-tunable, stated as rationale not a queryable value."
    - element: ElevatedProcessNameList
      dimension: tuning_parameter
      specific_values: "sdclt.exe, eventvwr.exe, computerdefaults.exe, wsreset.exe (auto-elevated bypass binaries)"
      disposition: context
      target_question: Q7
      notes: "Rule already constrains to wsreset.exe; the broader auto-elevated binary list is post-bypass elevated-action context. Q7 surfaces any High/System integrity process after the bypass rather than a fixed name list."
    - element: ParentProcessAnomalyThreshold
      dimension: parent_process_analysis
      specific_values: "rationale only (non-elevated process spawning auto-elevated one)"
      disposition: new_question
      target_question: Q4
      notes: "Parent-child lineage is the anomaly signal; covered by the process_chain question and reinforced by the parent+binary baseline/clustering pivots."
    - element: ElevatedProcessPath
      dimension: tuning_parameter
      specific_values: "rationale only (paths for unsigned/unexpected elevated binaries)"
      disposition: context
      target_question: Q7
      notes: "T1548 secondary element; folded into the elevated-action question which lists Image for any High/System integrity process."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "host-wide Image aggregation duplicates cue_host_context.clustering, which adds ParentImage for better novel-pair surfacing"
      kept: cue_host_context.clustering
    - dropped: cue_host_context.precursor
      reason: "sigma-alert-on-host query duplicates uac_bypass.other_escalation_attempts (more specific to escalation framing); kept the technique-set version"
      kept: uac_bypass.other_escalation_attempts
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.baseline (Q12) and baseline_svc (Q13) emitted as distinct subject-vs-mechanism pivots"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: null
      reason: "user_actor pair — non-deduplicable: cue_user_actor.clustering (Q14) and clustering_svc (Q15) emitted as distinct subject-vs-mechanism pivots"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1548.002) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule keys solely on Image=wsreset.exe and IntegrityLevel; it does not
        involve a DLL-hijack or COM-surrogate bypass family where module loads
        sharpen detection. WSReset abuse is a registry-handler hijack, so the
        load-bearing artifacts are the HKCU handler write (covered by Q5,
        attack:T1548.002.bypass_registry_setup) and the elevated process lineage
        (Q4). An image_load question on wsreset.exe would return baseline module
        loads without precision for this bypass. No compensating
        attack:T1548.002.image_load question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (cross-process handle/memory access) is relevant to token-
        theft and COM-surrogate UAC bypass variants, not the WSReset registry-
        handler hijack this rule detects. The bypass leaves no inter-process access
        artifact; the elevation is achieved by the OS auto-elevating wsreset.exe.
        Post-bypass credential-access tooling that would generate process_access
        events is surfaced behaviorally by the elevated-action question (Q7) and
        the escalation-alert sweep (Q9). No compensating process_access question
        authored.
    - hint: "logsource 'security' (recommended by techniques: T1548.002) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The security-channel analytic for this technique correlates 4688/4672
        elevated-token events, which restates the same High/System integrity
        process-creation signal this rule already fires on. Q1 verifies the
        integrity level on the triggering event and Q7 enumerates subsequent
        High/System integrity processes from process_creation, covering the
        elevated-token dimension without a separate security-channel question.
        No compensating attack:T1548.002.security question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - uac_bypass
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      registry_set: 1
      sigma_alert: 1
