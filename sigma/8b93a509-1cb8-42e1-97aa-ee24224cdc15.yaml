name: Sensitive File Dump Via Wbadmin.EXE
id: dd550b23-eabf-4547-a53b-6a05a015b95f
description: |
  Detects wbadmin.exe (the Windows Server Backup command-line utility) being run with a start/backup command line that
  names a sensitive credential-bearing file, specifically a registry hive under \config\ (SAM, SECURITY, or SYSTEM) or the
  Active Directory database at \Windows\NTDS\NTDS.dit. The rule fires when all three conditions hold at once, namely that
  the image is wbadmin.exe (or its original filename is WBADMIN.EXE), the command line contains both start and backup, and
  the command
  line references one of those sensitive paths. wbadmin is a legitimate administrative tool, so authorized backup jobs can
  match; however, backing up SAM/SYSTEM/SECURITY hives or NTDS.dit into a backup image is a documented offline credential
  dumping primitive, because the resulting image can be carried off-host and parsed to extract local or domain account
  hashes. Investigation should confirm exactly which sensitive store was targeted, where the backup was written, whether the
  user and host legitimately run backups of those files, and whether credential-theft follow-on or lateral movement appears
  afterward.
type: detection
detection_id: 8b93a509-1cb8-42e1-97aa-ee24224cdc15
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line run wbadmin start backup against a sensitive hive or NTDS.dit as this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line and
    process details. The rule fires only when all three conditions hold together, namely that the image is wbadmin.exe (or
    its original filename is WBADMIN.EXE), the command line contains both start and backup, and the command line references
    a sensitive path (\config\SAM, \config\SECURITY, \config\SYSTEM, or \Windows\NTDS\NTDS.dit). Confirm which sensitive
    file was named and read the full command line before continuing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to back up registry hives or the Active Directory database?
  context: Legitimate wbadmin backups of SAM/SYSTEM/SECURITY hives or NTDS.dit are normally performed by backup operators or
    scheduled backup automation on domain controllers and a small set of managed servers. The same command run from a
    workstation, by a standard user, or by an account with no backup role is a stronger indicator of credential dumping than
    the same command on a known backup host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: Which credential store was targeted, and where was the backup written?
  context: The sensitive path in the command line identifies the store at risk. SAM (with SYSTEM) yields local account
    hashes; SECURITY holds LSA secrets and cached domain credentials; NTDS.dit (with the SYSTEM hive) yields every domain
    account hash including krbtgt. Read the command line to confirm which files were named and to recover the
    -backupTarget destination. A UNC share or removable/alternate volume as the target means the credential-bearing image
    can be carried off-host for offline extraction, which raises the severity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.003.tuning_parameter
  question: What backup target and item set did this wbadmin invocation use, and do they point off-host?
  context: wbadmin start backup takes a -backupTarget destination (drive letter, \\?\Volume{GUID} path, or UNC share) and
    -include/-systemState selectors. A -systemState or -allCritical capture on a domain controller pulls NTDS.dit and the
    registry into a single image even without an explicit NTDS path. A UNC or removable backupTarget stages the image where
    it can be retrieved off the host. Recover both the target and the included items to judge whether this is a routine
    backup to an approved repository or a dump staged for exfiltration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned wbadmin, and is that parent chain expected for a backup operation?
  context: The parent process reveals how the backup was invoked. Scheduled or operator-driven backups typically descend
    from the task scheduler, a service host, or an interactive administrative shell. An unusual parent such as a script host,
    an Office application, a remote-execution service, or a recently dropped binary points to hands-on-keyboard credential
    dumping rather than routine maintenance.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did wbadmin spawn afterward on this host?
  context: A backup that is part of a credential-dumping operation is often followed by tooling that retrieves and parses
    the resulting image. Processes spawned by wbadmin, or close-in-time execution of archive utilities, copy commands, or
    hash-extraction tools, indicate the backup was a means to harvest credentials rather than an end in itself.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this backup-of-sensitive-files pattern, what parents and users drove it, and
    does the current alert fit?
  context: Even on a legitimate backup server, the specific pattern of backing up a hive or NTDS.dit has a normal driver.
    The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution
    reflects how this exact firing pattern has historically run here. A new parent process, an unexpected account, or a
    first-time appearance of this pattern is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wbadmin.exe
      - OriginalFileName: WBADMIN.EXE
      selection_backup:
        CommandLine|contains:
        - start
        - backup
      selection_path:
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \Windows\NTDS\NTDS.dit
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.baseline
  question: Does this host normally run wbadmin at all?
  context: A backup utility that has never been seen on a host, or runs only rarely, is more suspicious than one that runs
    on a regular schedule. Zero or very low historical counts on this host indicate first-time or unusual use of wbadmin,
    independent of whether the sensitive-file pattern matched.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wbadmin.exe
      - OriginalFileName: WBADMIN.EXE
      selection_backup:
        CommandLine|contains:
        - start
        - backup
      selection_path:
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \Windows\NTDS\NTDS.dit
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wbadmin.exe
      - OriginalFileName: WBADMIN.EXE
      selection_backup:
        CommandLine|contains:
        - start
        - backup
      selection_path:
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \Windows\NTDS\NTDS.dit
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same account triggered backups or other administrative activity on several hosts in the same window, the scope of
    a possible credential-dumping operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wbadmin.exe
      - OriginalFileName: WBADMIN.EXE
      selection_backup:
        CommandLine|contains:
        - start
        - backup
      selection_path:
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \Windows\NTDS\NTDS.dit
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be exposed by this backup, and do any have elevated privileges?
  context: The severity depends on what the captured store contains. A SAM/SYSTEM capture exposes local accounts on the
    host; a SECURITY hive exposes LSA secrets and cached domain credentials; an NTDS.dit capture exposes every domain
    account including Domain Admins, Enterprise Admins, and krbtgt, which enables Golden Ticket forgery and warrants
    incident escalation. Cross-reference the affected scope against directory group memberships to decide remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this backup-of-sensitive-files pattern in the past week, and does the cross-host
    distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    exact firing pattern rather than generic wbadmin usage. Many hosts firing the same pattern points to a backup policy or
    automation that legitimately captures these files; one host or a small unrelated set points to manual operator activity
    worth closer review. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wbadmin.exe
      - OriginalFileName: WBADMIN.EXE
      selection_backup:
        CommandLine|contains:
        - start
        - backup
      selection_path:
        CommandLine|contains:
        - \config\SAM
        - \config\SECURITY
        - \config\SYSTEM
        - \Windows\NTDS\NTDS.dit
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Credential dumping rarely happens in isolation. Earlier detections on the same host, such as reconnaissance,
    privilege escalation, defense tampering, or shadow-copy manipulation, may reveal the stages that preceded this backup
    and help reconstruct how the operator obtained the access needed to back up these files.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.003
      name: NTDS
      role: primary
    - id: T1003
      name: OS Credential Dumping
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: backup_of_sensitive_store
      description: "wbadmin start backup captures a credential-bearing hive or NTDS.dit into a backup image"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: store_and_target_identification
      description: "Identify which sensitive store was targeted and where the backup image was written (off-host target staging)"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 3
      label: backup_target_and_item_set
      description: "Recover -backupTarget destination and -include/-systemState item set to judge routine backup vs staged dump"
      mapped_to: Q4
      source: attack:T1003.003.tuning_parameter
    - stage: 4
      label: offline_extraction_followon
      description: "Image is retrieved and parsed, or stolen credentials are used for lateral movement"
      mapped_to: Q6
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1003.003
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.003
      covered_by: Q14
      source: template:credential_access.post_access_lateral_movement
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1003.003
      covered_by: Q4
      source: attack:T1003.003.tuning_parameter
      coverage_type: direct
    - dimension: user_context
      technique: T1003.003
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: access_verification
      technique: T1003
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: indirect
  mutable_dispositions:
    - element: TargetFilePath
      dimension: tuning_parameter
      specific_values: "\\config\\SAM, \\config\\SECURITY, \\config\\SYSTEM, \\Windows\\NTDS\\NTDS.dit (rule paths); -backupTarget destination"
      disposition: new_question
      target_question: Q4
      notes: "Backup destination and item set surfaced via attack:T1003.003.tuning_parameter question (Q4); rule paths confirmed in Q1/Q3"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Parent-chain expectation covered by process_chain question (Q5) and host baseline_deviation (Q7)"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (e.g. 5 min shadow-copy/NTDS correlation window)"
      disposition: context
      target_question: Q6
      notes: "Close-in-time follow-on capture/parse activity surfaced via follow_on (Q6); post-event lateral movement via Q14"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q2
      notes: "Privileged/service-account expectation covered by host_user_context (Q2) and user baseline (Q9)"
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF (T1003, LSASS/SAM handle-access analytic)"
      disposition: context
      target_question: Q3
      notes: "T1003 secondary analytic targets process_access handle masks; wbadmin dumps via backup image, not a process handle to LSASS/SAM, so no GrantedAccess query applies. Store-targeting covered indirectly by Q3."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification — on-target Phase 2 for a credential-access rule"
      kept: credential_access.credential_target_identification
  skipped_templates:
    - template: defense_tampering.what_was_disabled
      reason: "Off-target. defense_tampering was confirmed via the wbadmin keyword (wbadmin can delete backups under T1490), but THIS rule's detection (start+backup against SAM/SYSTEM/SECURITY/NTDS.dit) is credential dumping, not control tampering. Phase 2 is served by credential_access.credential_target_identification, which is specific to the store targeted."
    - template: defense_tampering.protection_status
      reason: "Off-target — no security control was disabled by a backup-of-sensitive-files command; nothing to confirm restored."
    - template: defense_tampering.activity_after_tampering
      reason: "Off-target — no tampering event to anchor a post-tampering window. Post-event activity is covered by follow_on (Q6) and post_access_lateral_movement (Q14)."
    - template: defense_tampering.other_tampering_activity
      reason: "Off-target for this credential-dumping rule. Prior/other tamper activity on the host is covered generically by cue_host_context.precursor (Q16) via prior sigma alerts."
    - template: persistence_scope.process_based_persistence
      reason: "Off-target. persistence_scope cascaded from defense_tampering's binary match, not from any persistence behavior in this rule. wbadmin start backup is not a persistence install; no schtasks/sc/reg/bitsadmin/wmic persistence angle applies."
    - template: persistence_scope.file_based_persistence
      reason: "Off-target — the rule detects credential capture into a backup image, not a Startup-folder or Run-key file drop. No persistence dimension to cover."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED universal sweep; dropped to keep the playbook on-target. Prior tamper-as-precursor is covered by cue_host_context.precursor (Q16) via prior sigma alerts on the host, and the credential-dump disposition turns on the backup target and affected accounts (Q3/Q4/Q13), not on whether AV/EDR was weakened first."
  reflection_overrides: []
  hint_dispositions:
    - hint: "defense_tampering.what_was_disabled replaces cue_process_tool.mechanism_decode"
      disposition: declined
      reason: >-
        Two hints both retire cue_process_tool.mechanism_decode; the template's own
        replaced_by field names credential_access.credential_target_identification.
        For a credential-access rule the credential_access Phase 2 is the on-target
        replacement (it asks which store was targeted), so mechanism_decode is dropped
        in favor of credential_target_identification (Q3), not what_was_disabled.
        what_was_disabled is off-target (no control was disabled) and is recorded in
        skipped_templates.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      sigma_alert: 2
