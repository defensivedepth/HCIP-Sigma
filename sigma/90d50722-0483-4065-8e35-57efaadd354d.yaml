name: Arbitrary MSI Download Via Devinit.EXE
id: b144d59e-206e-4e7e-a59b-4f6433b57764
description: |
  Detects devinit.exe, a Microsoft-signed Visual Studio helper, invoked with the
  argument combination ' -t msi-install ' AND ' -i http', which instructs it to
  download an MSI package from an HTTP/HTTPS URL into the Windows installer cache
  and install it. As a signed binary, devinit.exe can be abused to proxy execution
  of remote, attacker-supplied MSI payloads past application-control and signature
  checks. Legitimate use is largely confined to developer workstations running
  Visual Studio tooling; on servers or non-developer endpoints this argument
  pattern is unusual and warrants confirming the source URL, the downloaded
  package, and what installed afterward.
type: detection
detection_id: 90d50722-0483-4065-8e35-57efaadd354d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the devinit.exe command line contain both the msi-install mode flag and an HTTP/HTTPS URL?
  context: Confirm the alert is a true positive by reviewing the exact command line.
    The rule fires only when both ' -t msi-install ' and ' -i http' appear together,
    indicating devinit.exe was told to download and install a remote MSI. Verify both
    elements are present and capture the full URL that follows the -i flag.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
  answer_sources:
  - alert
  - process_creation
- source: template:universal.host_user_context
  question: Is this user and host expected to run Visual Studio developer tooling like devinit.exe?
  context: devinit.exe ships with Visual Studio and is normally seen on developer
    workstations. On a server, kiosk, or general-purpose endpoint, or under a user
    who does no development work, the msi-install download pattern is out of place
    and raises the priority of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
  answer_sources:
  - asset_inventory
  - organizational_knowledge
- source: template:cue_process_tool.mechanism_decode
  question: What is the full download URL and MSI target in the devinit.exe command line?
  context: The full command line reveals the remote source of the MSI. Decode the
    value following ' -i http' to extract the host, path, and file name being
    retrieved, and note any additional flags. The downloaded package lands in the
    Windows installer cache before installation, so the URL identifies where the
    payload originated.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
  answer_sources:
  - process_creation
  - manual_review
- source: attack:T1218.network_retrieval
  question: Did devinit.exe make an outbound HTTP/HTTPS connection to the URL host around the time of the alert?
  context: The msi-install mode retrieves the package over the network before
    installing it. Correlating an outbound connection from devinit.exe to the URL
    host confirms the download actually executed rather than failing at the
    parsing stage, and surfaces the destination address for reputation review. If
    network telemetry for this host is not ingested, treat zero results as a
    visibility gap rather than proof no transfer occurred.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: '\devinit.exe'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
      - Initiated
  answer_sources:
  - network_connection
  - external_threat_intel
- source: template:download_tool.download_success_verification
  question: Was an MSI file written to the Windows installer cache after devinit.exe ran?
  context: A download command does not guarantee the file was retrieved. Network
    errors or blocked URLs may have prevented the transfer. devinit.exe writes the
    fetched package into the Windows installer directory before installing it, so
    confirming an MSI write there establishes whether the activity progressed past
    delivery.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains: '\Windows\Installer\'
      filter:
        TargetFilename|endswith: '.msi'
      condition: selection and filter
    fields:
      - TargetFilename
      - Image
  answer_sources:
  - file_event
- source: template:download_tool.payload_analysis
  question: What MSI did devinit cache to disk, and can you retrieve and hash it?
  context: devinit fetches the MSI into the Windows Installer cache. The MSI is installed
    by msiexec, not run as a process, and file-creation events carry no hash — so the path
    is the artifact here. Retrieve the MSI from the cache below and submit its hash to threat
    intelligence, or detonate it. Pivot on the URL host from the command line for reputation.
    What the install then drops and runs has its own process hashes — check the host's
    subsequent process activity for those.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains: '\Windows\Installer\'
      filter:
        TargetFilename|endswith: '.msi'
      condition: selection and filter
    fields:
      - TargetFilename
  answer_sources:
  - safe_environment
  - external_threat_intel
- source: template:cue_process_tool.process_chain
  question: What process spawned devinit.exe, and is that parent chain expected?
  context: The parent process reveals how devinit.exe was invoked. Legitimate use
    is typically driven by Visual Studio components or developer shells. Unusual
    parents such as a browser, mshta.exe, wscript.exe, an Office application, or a
    download utility suggest the binary was launched as a proxy-execution step
    rather than during normal development.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
  answer_sources:
  - process_creation
- source: template:cue_process_tool.follow_on
  question: What did the downloaded MSI do after installation, and what child processes did devinit.exe spawn?
  context: The processes spawned after the MSI installs reveal its actual impact.
    MSI installation runs through the Windows installer service and may launch
    custom actions; attacker MSIs commonly drop and execute follow-on payloads such
    as cmd.exe, powershell.exe, rundll32.exe, or a dropped binary. Unexpected
    children point to code execution beyond a benign install.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\devinit.exe'
      condition: selection
    fields:
      - Image
      - CommandLine
  answer_sources:
  - process_creation
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously run the msi-install download pattern, what parents and users drove it, and does the current alert fit?
  context: Even signed developer tools can be abused. This query mirrors the rule's
    selection scoped to this host, so the parent and user distribution reflects how
    this specific download pattern has historically run here rather than generic
    devinit.exe usage. A new parent process, an unexpected account, or a first-ever
    occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
  answer_sources:
  - process_creation
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around the time of the alert?
  context: Tracking the user's activity across all hosts reveals lateral movement or
    activity spanning multiple endpoints. If the same user drove similar MSI download
    or installation activity on other hosts, the scope of the operation may be larger
    than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
  answer_sources:
  - process_creation
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts run the msi-install download pattern in the past week, and does the distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection with no host scope, so the result
    is a fleet-wide count of the download pattern rather than generic devinit.exe
    usage. Many hosts firing the same pattern points to scripted deployment or build
    automation; one host or a small unrelated set points to manual operator activity.
    Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - ' -t msi-install '
        - ' -i http'
      condition: selection
    fields:
    - host.name
  answer_sources:
  - process_creation
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert
    reveals the immediate context: what led to this event, what happened after, and
    whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, persistence installation, or additional downloads.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
  answer_sources:
  - process_creation
  - file_event
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host,
    such as defense tampering, reconnaissance, or initial-access activity, may reveal
    the stages that preceded this download and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
  answer_sources:
  - alert
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_binary_invocation
      description: "devinit.exe (Microsoft-signed) invoked with msi-install download arguments"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_argument_decode
      description: "Decode the msi-install flag and remote URL from the command line"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: network_retrieval
      description: "Outbound HTTP/HTTPS retrieval of the remote MSI payload"
      mapped_to: Q4
      source: attack:T1218.network_retrieval
    - stage: 4
      label: payload_landed_and_executed
      description: "MSI written to installer cache and installed, spawning follow-on processes"
      mapped_to: Q8
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q14
      source: template:cue_process_tool.scope
      coverage_type: partial
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (regsvr32.exe not launched by explorer.exe, etc.)"
      disposition: new_question
      target_question: Q7
      notes: "Parent-chain question profiles unexpected parents of devinit.exe; baseline_svc and clustering_svc pivot on the parent+binary pair."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "msiexec.exe, regsvr32.exe (rule-specific binary is devinit.exe)"
      disposition: context
      target_question: Q2
      notes: "devinit.exe is the signed binary for this rule; description and host_user_context note its signed VS-tooling provenance."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs / script paths / DLL entrypoints (rule encodes ' -t msi-install ' + ' -i http')"
      disposition: query_injection
      target_question: Q3
      notes: "Rule's own CommandLine|contains|all values drive mechanism_decode, baseline_deviation, scope, and user-actor scoping queries."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q4
      notes: "Network-retrieval question surfaces DestinationHostname/Ip so the analyst can apply local allowlisting judgement."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context.clustering (Q15) provides the same +/-15m host-wide co-occurrence view; host_context version aggregates by Image+ParentImage for better noise collapse"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "Image-only baseline subsumed by cue_user_actor.baseline (Q10) and baseline_deviation (Q9), both of which scope to the rule's msi-install pattern per the CommandLine-only hint rather than generic devinit.exe volume"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair
      reason: "user_actor pair — non-deduplicable; both emitted (Q10 user-pivot, Q11 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair
      reason: "user_actor pair — non-deduplicable; both emitted (Q12 user-pivot, Q13 parent+binary substitute)"
      kept: both
  skipped_templates:
    - template: download_tool.directory_activity
      reason: "Subsumed by cue_process_tool.follow_on (Q8). The file-event directory sweep needs %file_dirs%, but this rule's detection carries no destination path (CommandLine-only on the URL), so file_dirs would not resolve; the install-time child-process question is the load-bearing 'what did the MSI do' view. download_success_verification (Q5) and payload_analysis (Q6) cover the file-write/hash angle scoped to the installer cache."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include; dropped to stay within the 14-18 cap. This is a LOLBIN download/execution rule, not a credential-access or hands-on-keyboard impact action where the disposition turns on prior AV/EDR weakening; cue_host_context.precursor (Q16) already surfaces prior tamper alerts on the host"
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule's abuse path is devinit.exe downloading and installing a remote
        MSI via command-line arguments, not in-process DLL/proxy loading by
        devinit.exe itself. The load-bearing evidence is the command line
        (Q3, command_analysis), the network retrieval (Q4), the MSI write
        (Q5/Q6), and the install-time child processes (Q8). image_load on
        devinit.exe would return its normal VS dependency loads without
        precision for this MSI-download technique. No compensating
        attack:T1218.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - download_tool
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 12
      file_event: 2
      network_connection: 1
      sigma_alert: 1
