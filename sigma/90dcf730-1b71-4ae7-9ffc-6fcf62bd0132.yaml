name: Suspicious ZipExec Execution
id: d2481075-4715-4d24-bc84-cd14d3450a6f
description: |
  Detects command lines consistent with ZipExec, a tool that wraps a payload binary into a
  password-protected zip and executes it from inside the archive without unzipping to disk first.
  The rule fires when EITHER a "run" command line is seen (a /generic:Microsoft_Windows_Shell_ZipFolder
  filename argument together with a .zip path, a /pass: password, and a /user: argument) OR a "delete"
  command line is seen (a /delete operation against a Microsoft_Windows_Shell_ZipFolder .zip entry).
  The technique uses COM objects to drive the built-in Windows shell zip-folder functions from a
  generated script loader, which keeps the payload inside a password-protected archive that on-disk
  and signature-based scanning cannot read. Legitimate use of this exact argument shape is rare;
  determine which branch fired, decode the full command line, identify the wrapped payload, and
  establish whether the activity belongs to an authorized engagement.
type: detection
detection_id: 90dcf730-1b71-4ae7-9ffc-6fcf62bd0132
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match either ZipExec branch this rule detects — the run pattern or the delete pattern?
  context: >-
    Confirm the alert is a true positive by reviewing the exact command line. The rule fires on
    EITHER of two branches, so note which one matched. The run branch requires all of a
    /generic:Microsoft_Windows_Shell_ZipFolder filename argument, a .zip path, a /pass: password, and
    a /user: argument. The delete branch requires a /delete operation against a
    Microsoft_Windows_Shell_ZipFolder .zip entry. Which branch fired shapes the rest of the
    investigation: the run branch points to payload execution, the delete branch to cleanup of a zip
    artifact that may already have run.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to execute payloads from password-protected zip archives?
  context: >-
    Legitimate administrative activity rarely uses the shell zip-folder COM interface with a
    /pass: password and /user: argument to launch a binary. Knowing whether the user role and host
    function involve software packaging or security testing helps separate a true positive from a
    rare-but-sanctioned operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this ZipExec execution part of an authorized penetration test or red team engagement?
  context: >-
    Tools that wrap payloads in password-protected archives to bypass on-disk scanning are used in
    authorized security assessments. Before investing investigation time, check whether a pentest or
    red team exercise is active on this host or network segment. An authorized engagement with
    matching tool usage and timing resolves the alert without further analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: Which ZipExec operation was invoked, and what payload zip and account does the command line name?
  context: >-
    The command line reveals which branch ran and the operational detail behind it. From the
    /generic:Microsoft_Windows_Shell_ZipFolder filename= argument, identify the path of the .zip being
    extracted or executed; from /user: and /pass:, note the account and password used to open the
    archive. A /delete invocation indicates cleanup rather than execution. These values determine the
    wrapped payload and the scope of potential compromise.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: Decode the full ZipExec command line — what zip path, account, and operation did it carry, and which other ZipExec command lines ran on this host nearby?
  context: >-
    The full command line reveals the specific action taken. Decode the .zip path from the
    /generic:Microsoft_Windows_Shell_ZipFolder filename= argument, the account from /user:, and
    whether a /delete cleanup followed an execution. ZipExec typically runs as a pair — a run
    invocation that rebuilds and executes the archive followed by a delete invocation that removes it
    — so this host-scoped view across a short window shows both halves of the operation rather than a
    single event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Microsoft_Windows_Shell_ZipFolder:filename='
      condition: selection
    fields:
      - CommandLine
      - Image
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this command and is that parent chain expected?
  context: >-
    The parent process reveals how the command was invoked. ZipExec drives the shell zip-folder COM
    functions from a generated script loader, so script hosts (wscript.exe, cscript.exe) or an office
    or browser process spawning the chain are notable. Predictable parents such as explorer.exe for a
    user action are less suspicious; walk up the parent to see what launched it and what else it
    spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes ran on this host immediately after the ZipExec command, and do any look like the unwrapped payload?
  context: >-
    The processes that run after the archive is opened reveal the actual payload. ZipExec exists to
    execute a wrapped binary, so a new or unusual child process started within minutes of the run
    command line — particularly a script host, a command interpreter, or an unsigned binary from a
    user-writable path — is the likely payload. This host-scoped view over a short window surrounds
    the alert without depending on a parent linkage that may be obscured by the COM-driven launch.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
      - CommandLine
- source: attack:T1218.network_retrieval
  question: Did the alerting host make outbound network connections from script hosts or the payload process around the time of the alert?
  context: >-
    Proxy execution through trusted utilities is frequently used to retrieve or stage an externally
    hosted payload, and the script loader that drives the archive extraction can also reach out to a
    remote endpoint. Network connections from script hosts (wscript.exe, cscript.exe), command
    interpreters, or the unwrapped payload process near the alert point to retrieval or command and
    control. Correlate destination addresses against known-good enterprise egress before treating
    them as malicious.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      filter_known_good:
        Image|endswith:
          - '\svchost.exe'
          - '\services.exe'
          - '\lsass.exe'
      condition: selection and not filter_known_good
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1202.zip_artifact_on_disk
  question: Was the payload zip rebuilt on disk, and where does the named archive live?
  context: >-
    ZipExec base64-encodes the payload zip and rebuilds it on disk before opening it through the COM
    shell functions, so a .zip file matching the path in the command line should appear as a file
    creation event around the alert. Locating that archive — and any loader script written alongside
    it — confirms the technique and provides the artifact to collect for analysis. Common write
    locations are user temp and other user-writable directories.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith: '.zip'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the ZipExec pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    Even rare command shapes can have a benign local cause. This query mirrors the rule's run and
    delete selections scoped to this host, so the resulting parent and user distribution reflects how
    this specific firing pattern — not generic process activity — has historically run here. A new
    parent process, an unexpected user account, or an unusual command-line shape relative to that
    history is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      run:
        CommandLine|contains|all:
        - /generic:Microsoft_Windows_Shell_ZipFolder:filename=
        - .zip
        - '/pass:'
        - '/user:'
      delete:
        CommandLine|contains|all:
        - /delete
        - Microsoft_Windows_Shell_ZipFolder:filename=
        - .zip
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (run or delete) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run command lines with the shell zip-folder COM interface?
  context: >-
    Compare this user's current action against their historical behavior. A user who has never
    executed the /generic:Microsoft_Windows_Shell_ZipFolder pattern is more suspicious than one who
    does so as part of a known packaging or testing workflow. User role and typical toolset should
    align with the activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: 'Microsoft_Windows_Shell_ZipFolder:filename='
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-pattern combination historically routine on this host or across the environment, or is the parent unusual?
  context: >-
    When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism — which parent process produced this command line, and
    whether that pairing is historically common. A new or rare parent for the ZipExec pattern is the
    deviation worth chasing; a parent that already accounts for many prior events is automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Microsoft_Windows_Shell_ZipFolder:filename='
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar activity on more than one host, the scope
    of the operation may be larger than the single alert suggests.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same parent-plus-pattern combination fire on other hosts in the same window — a coordinated rollout, or isolated to this host?
  context: >-
    Under a service principal, asking what else the user did returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism — parent process plus the shell zip-folder
    command pattern — fired on other hosts in the alert window. Multiple hosts in the same window
    points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Microsoft_Windows_Shell_ZipFolder:filename='
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired the ZipExec pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    This query mirrors the rule's run and delete selections with no host scope, so the result is a
    fleet-wide count of the firing pattern rather than generic process activity. Many hosts firing the
    same pattern points to scripted deployment or vendor automation; one host or a small unrelated set
    points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      run:
        CommandLine|contains|all:
        - /generic:Microsoft_Windows_Shell_ZipFolder:filename=
        - .zip
        - '/pass:'
        - '/user:'
      delete:
        CommandLine|contains|all:
        - /delete
        - Microsoft_Windows_Shell_ZipFolder:filename=
        - .zip
      condition: run or delete
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering,
    reconnaissance, or credential access — may reveal the attack stages that preceded the wrapped-payload
    execution and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
    - id: T1202
      name: Indirect Command Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: indirection_utility_launch
      description: "A command line drives the shell zip-folder COM interface to open a password-protected archive (the run branch) or delete a zip artifact (the delete branch)."
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_rebuild_on_disk
      description: "The base64-encoded payload zip is rebuilt on disk before being opened through the COM shell functions."
      mapped_to: Q9
      source: attack:T1202.zip_artifact_on_disk
    - stage: 3
      label: payload_execution
      description: "The wrapped binary executes from inside the archive, spawning a secondary process."
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
    - stage: 4
      label: network_retrieval_or_c2
      description: "The script loader or unwrapped payload may retrieve an externally hosted payload or open outbound connections."
      mapped_to: Q8
      source: attack:T1218.network_retrieval
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q15
      source: template:cue_process_tool.scope
      coverage_type: partial
    - dimension: time_correlation
      technique: T1202
      covered_by: Q8
      source: attack:T1218.network_retrieval
      coverage_type: direct
    - dimension: user_context
      technique: T1202
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (e.g., regsvr32.exe not launched by explorer.exe)"
      disposition: context
      target_question: Q6
      notes: "Profiled in process_chain context; script-host parents called out as notable."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "msiexec.exe, regsvr32.exe (examples of signed proxy binaries)"
      disposition: context
      target_question: Q15
      notes: "Rule is CommandLine-only with no Image; the proxy binary is whatever script host drives the COM call, so the signed-binary allowlist is a tuning rationale rather than a queryable value here."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, script paths, DLL entrypoints"
      disposition: query_injection
      target_question: Q5
      notes: "mechanism_decode scopes on the Microsoft_Windows_Shell_ZipFolder:filename= substring and decodes zip path/account/operation."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (legitimate enterprise domains)"
      disposition: query_injection
      target_question: Q8
      notes: "network_retrieval query carries a not-filter for known-good system processes; enterprise-domain allowlisting noted in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "10-30 minutes"
      disposition: context
      target_question: Q8
      notes: "Correlation window between launcher and child/network activity reflected in +/-15m range and context."
    - element: AllowedUtilities
      dimension: tuning_parameter
      specific_values: "forfiles, wsl, ssh"
      disposition: context
      target_question: Q15
      notes: "Permitted-utility tuning is a rationale; cross-host scope question surfaces automation that would warrant allowlisting."
    - element: HighRiskChildren
      dimension: tuning_parameter
      specific_values: "powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, curl.exe, bitsadmin.exe"
      disposition: context
      target_question: Q7
      notes: "follow_on context calls out script hosts, command interpreters, and unsigned binaries as the likely unwrapped payload."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (standard interactive user vs server/CI agent)"
      disposition: context
      target_question: Q2
      notes: "host_user_context asks whether the user/host is expected to run this operation."
    - element: DestCIDRs
      dimension: tuning_parameter
      specific_values: "rationale only (known-good egress for SSH/WSL)"
      disposition: context
      target_question: Q8
      notes: "network_retrieval context instructs correlating destinations against known-good enterprise egress."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context.clustering and cue_user_actor.clustering already cover same-host and cross-host activity around the alert; a third host-scoped Image aggregation would duplicate them."
      kept: cue_user_actor.clustering
    - dropped: cue_host_context.clustering
      reason: "around-the-alert host activity is covered by cue_process_tool.follow_on (host-scoped, +/-window) and the precursor alert sweep; kept follow_on which is more pointed at the payload."
      kept: cue_process_tool.follow_on
    - dropped: null
      reason: "user_actor pair — non-deduplicable (cue_user_actor.baseline + baseline_svc, cue_user_actor.clustering + clustering_svc) emitted as both variants per AGENTS.md."
      kept: cue_user_actor.baseline_svc, cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "Rule is CommandLine-only with null primary_binary; an Image|endswith baseline would substitute an empty %Image% and return meaningless counts. cue_process_tool.baseline_deviation (reflected, CommandLine-pattern scoped) provides the historical baseline for this host instead."
    - template: universal.endpoint_protection_health
      reason: "Generic host-tamper sweep is recommended, not must_include. ZipExec wraps a payload to evade on-disk scanning rather than disabling protection, and cue_host_context.precursor already surfaces prior tamper alerts on the host; dropped to stay within the 18-question cap without losing precursor coverage."
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule fires purely on a process_creation command-line shape and names no DLL or proxy
        binary; ZipExec's evasion is the password-protected archive, not a reflectively loaded module.
        Image-load telemetry would return module activity for whatever script host drove the COM call
        without precision for this pattern. The payload-on-disk and follow-on-child questions
        (attack:T1202.zip_artifact_on_disk and cue_process_tool.follow_on) cover the rebuilt archive
        and unwrapped payload more directly than an image_load sweep. No compensating
        attack:T1218.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - hacking_tool
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 13
      network_connection: 1
      file_event: 1
      sigma_alert: 1
