name: Registry Modification Attempt Via VBScript
id: 73109601-02c5-46b4-aebe-c4f94c62bf93
description: |
  Detects command lines that combine CreateObject, Wscript.Shell, and the .RegWrite method, a VBScript pattern used to write
  Windows registry values without invoking reg.exe, regedit.exe, or PowerShell. The technique is commonly executed by script
  hosts such as wscript.exe, cscript.exe, mshta.exe, or rundll32.exe, and is frequently used to land Run/RunOnce autorun
  persistence keys while avoiding tools that direct registry edits would normally produce. The investigation should determine
  what registry key and value were written, what executes as a result, which script host carried out the write, and whether
  the action was authorized.
type: detection
detection_id: 921aa10f-2e74-4cca-9498-98f9ca4d6fdf
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-08'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain CreateObject, Wscript.Shell, and the .RegWrite method together?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line. The
    rule fires only when all three tokens appear together, which is the signature of a VBScript RegWrite registry write rather
    than a direct edit via reg.exe or regedit.exe.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to script registry changes via a VBScript RegWrite?
  context: Legitimate administrative activity can trigger this detection. Knowing whether the user role and host function are
    consistent with scripted registry writes is often the fastest path to distinguishing true positives from routine operations.
    Most users and endpoints never write registry values through a VBScript RegWrite call.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Which script host carried out the RegWrite, and what started that script host?
  context: The script host that executes the VBScript determines how stealthy the write is. wscript.exe and cscript.exe read
    a script from disk, while mshta.exe and rundll32 can run inline VBScript without a script file. Tracing the parent's own
    origin reveals whether the chain is legitimate (a user double-clicked a script) or suspicious (an Office application or
    a download spawned the script host).
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent spawn around this time?
  context: If the parent that launched the script host spawned other processes too, examining siblings reveals the full scope
    of activity from that parent. Multiple script hosts or post-exploitation binaries from the same parent points to orchestrated
    activity rather than an isolated script run.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for this registry change?
  context: Many organizations require change tickets for system modifications, especially persistence-relevant registry writes.
    A corresponding ticket suggests authorized activity; absence of one when policy requires it suggests the action was unauthorized.
    This check can quickly close false positives or escalate true positives.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:organizational_context.timing_alignment
  question: Does the timing of this activity align with normal work hours and maintenance windows?
  context: Legitimate system changes typically occur during business hours or scheduled maintenance windows. A scripted registry
    write at 3 AM on a Saturday is more suspicious than the same activity during a documented weekday maintenance window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:registry.registry_key_context
  question: What specific registry key and value did the RegWrite call target, and is it a known persistence or evasion location?
  context: The registry path reveals intent. Run/RunOnce keys under HKCU or HKLM CurrentVersion\Run indicate autorun persistence;
    keys under Windows Defender policy paths indicate security tool tampering; Image File Execution Options keys indicate debugger-based
    hijacking. If registry auditing is collected, the linked registry_set event shows the target key and value data as structured
    fields; otherwise reconstruct them from the RegWrite arguments in the command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:obfuscation.encoded_payload_decode
  question: Is the VBScript command line encoded or obfuscated, and what does the reconstructed string write to the registry?
  context: VBScript registry writers frequently hide the target key, value, and payload behind Chr() chains, string concatenation,
    or environment-variable substitution so the RegWrite arguments are not readable at a glance. Reconstruct the string from
    any Chr() chain or concatenation to recover the actual key path and the executable or script that will run from it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session, whether via script hosts or reg.exe?
  context: Operators who write one registry key often write several -- for example, setting a Run key for persistence while
    also disabling a security setting. Finding clusters of registry-write activity on the host within a short window reveals
    the full scope of the configuration changes and whether multiple objectives were pursued.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        - CommandLine|contains|all:
            - CreateObject
            - .RegWrite
        - Image|endswith:
            - '\reg.exe'
            - '\regedit.exe'
          CommandLine|contains:
            - ' add '
            - ' delete '
            - ' import '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:registry.registry_value_persistence_check
  question: Does the registry value reference an executable or script that is still present on disk?
  context: A registry autorun entry is only effective if the referenced payload exists at the path written to the value. If
    the RegWrite call pointed a Run key at a path under a user-writable directory, checking whether that file exists and analyzing
    its hash determines whether the persistence mechanism is active and what it would execute on next logon. If the value data
    cannot be resolved to a path from logs, examine the registry value directly to identify the referenced file.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - Hashes
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this RegWrite pattern, what parents and users normally drive it, and does the
    current alert fit?
  context: Even routine automation can use VBScript registry writes. This query mirrors the rule's command-line pattern scoped
    to this host, so the resulting parent and user distribution reflects how this specific RegWrite pattern has historically
    run here. A new parent process, an unexpected user account, or an unusual command-line shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - CreateObject
        - Wscript.shell
        - .RegWrite
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run VBScript that writes to the registry?
  context: Compare this user's current action against their historical behavior. A user who has never executed a VBScript RegWrite
    is more suspicious than one who does so regularly. The user's role and typical toolset should align with scripted registry
    changes.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
        - CreateObject
        - Wscript.shell
        - .RegWrite
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and script-host combination historically routine on this host or across the environment?
  context: When the alert fires under a service principal, user identity does not attribute the behavior; the meaningful pivot
    is the launch mechanism. A new or rare parent for this script-host activity is the deviation worth chasing, while a pair
    that already accounts for many prior events points to vendor or operating-system automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
        - CreateObject
        - Wscript.shell
        - .RegWrite
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If
    the same user triggered similar activity on several hosts, the scope of the activity may be larger than a single registry
    write.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same RegWrite pattern fire on other hosts in the same window — is this a coordinated rollout or isolated
    activity?
  context: Under a service principal, user-pivoted clustering returns agent-telemetry noise. The meaningful question is whether
    the same VBScript RegWrite pattern fired on other hosts in the alert window. Multiple hosts in the same window points to
    scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - CreateObject
        - Wscript.shell
        - .RegWrite
      condition: selection
    fields:
      - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Operators rarely rely on a single persistence mechanism. After establishing one foothold via a registry RegWrite,
    they commonly install backup mechanisms using different techniques such as scheduled tasks, services, or BITS jobs. Multiple
    persistence installations in the same timeframe indicate intentional stacking rather than isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to persistence locations such as the Startup folder or Run key paths on this host?
  context: File-based persistence bypasses command-line detection entirely. Dropping an executable or script into the Startup
    folder or a path referenced by a registry Run key achieves persistence without any registry tool. File event monitoring
    captures these drops even when the operator avoids standard persistence tooling.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or
    credential access -- may reveal the attack stages that preceded this registry write and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1112
      name: Modify Registry
      role: primary
    - id: T1059.005
      name: Visual Basic
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: script_host_execution
      description: A script host (wscript/cscript/mshta/rundll32) executes VBScript via CreateObject(Wscript.Shell)
      mapped_to: Q3
      source: template:parent_child.parent_process_legitimacy
    - stage: 2
      label: registry_write
      description: The script calls .RegWrite to set a registry value (often a Run/RunOnce autorun key)
      mapped_to: Q7
      source: template:registry.registry_key_context
    - stage: 3
      label: payload_reference
      description: The written value references an executable or script that runs on next logon/reboot
      mapped_to: Q10
      source: template:registry.registry_value_persistence_check
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1112
      covered_by: Q3
      source: template:parent_child.parent_process_legitimacy
      coverage_type: direct
    - dimension: time_correlation
      technique: T1112
      covered_by: Q9
      source: template:registry.registry_related_modifications
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1112
      covered_by: Q7
      source: template:registry.registry_key_context
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1059.005
      covered_by: Q8
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1059.005
      covered_by: Q12
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059
      covered_by: Q8
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
  mutable_dispositions:
    - element: RegistryKeyPathPatterns
      dimension: tuning_parameter
      specific_values: 'Run, RunOnce, Services, Windows Defender policy, IFEO'
      disposition: context
      target_question: Q7
      notes: Specific known persistence/evasion key locations named in registry_key_context context for the analyst to compare against.
    - element: ParentProcessAllowList
      dimension: parent_process_analysis
      specific_values: 'regedit.exe, msiexec.exe (legit registry tools)'
      disposition: context
      target_question: Q3
      notes: Parent legitimacy framed around expected vs unusual script-host parents; rule has no parent in detection so no query selection.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: rationale only
      disposition: context
      target_question: Q9
      notes: Correlation-window rationale captured in registry_related_modifications (+/-30m) and timing_alignment context.
    - element: SignatureCheck
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q10
      notes: Persistence-check question directs analyst to hash and assess the referenced executable.
    - element: ParentProcess
      dimension: parent_process_analysis
      specific_values: 'Office apps, mshta.exe spawning script host'
      disposition: context
      target_question: Q3
      notes: Office/mshta parent chain called out in parent_process_legitimacy context.
    - element: UserContext
      dimension: user_context
      specific_values: 'non-admin users, service accounts'
      disposition: context
      target_question: Q12
      notes: User baseline and service-principal substitute together cover user vs service-principal attribution.
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: rationale only
      disposition: context
      target_question: Q8
      notes: Obfuscation question covers Chr()/concatenation reconstruction; entropy is a tuning rationale without a queryable value.
    - element: ModuleName
      dimension: tuning_parameter
      specific_values: 'vbscript.dll, scrrun.dll'
      disposition: blocked_query
      target_question: Q8
      notes: >-
        image_load logsource not present in playbook; values would belong in an image_load query. Declined per hint
        disposition below — rule is CommandLine-only and process_creation evidence covers triage. No compensating
        attack:T1059.005.image_load question authored.
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: 'CreateObject, Wscript.shell, .RegWrite'
      disposition: query_injection
      target_question: Q1
      notes: Rule's own three-token pattern is the trigger; reflected/baseline questions inject it into scoping queries.
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: 'replaced_by: registry.registry_key_context — registry key context is the more specific Phase 2 analysis for this registry rule'
      kept: registry.registry_key_context
    - dropped: cue_process_tool.process_chain
      reason: parent_child technique set active — parent_process_legitimacy subsumes process_chain
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_host_context.clustering
      reason: registry.registry_related_modifications and cue_process_tool.clustering both cover same-host same-window activity more specifically for a registry rule
      kept: registry.registry_related_modifications
    - dropped: cue_user_actor.clustering / clustering_svc pairing
      reason: user_actor pair — non-deduplicable; both emitted (Q15 user-pivot, Q16 parent+pattern substitute)
      kept: both
    - dropped: cue_user_actor.baseline / baseline_svc pairing
      reason: user_actor pair — non-deduplicable; both emitted (Q12 user-pivot, Q13 parent+pattern substitute)
      kept: both
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: Image-scoped baseline; rule is CommandLine-only with no primary_binary, so Image|endswith scope is unresolvable. baseline_deviation (reflected, CommandLine-pattern) covers host historical pattern instead.
    - template: cue_process_tool.follow_on
      reason: ParentImage|endswith|expand %Image% scope is unresolvable for a CommandLine-only rule with null primary_binary. sibling_processes (ParentProcessGuid-scoped) covers child-process scope instead.
    - template: cue_process_tool.clustering
      reason: host-wide Image aggregation overlaps registry_related_modifications and user clustering; dropped to stay within 18-question cap.
    - template: cue_process_tool.scope
      reason: fleet-wide RegWrite-pattern count overlaps cue_user_actor.clustering_svc (which also surfaces cross-host firing of the same pattern in the alert window); dropped to stay within cap.
    - template: universal.endpoint_protection_health
      reason: This is a persistence/defense-evasion registry rule, not an execution or credential-access rule; tamper-as-precursor is covered by cue_host_context.precursor and registry_related_modifications (which catches Defender-policy registry writes). Dropped per the template's own RECOMMENDED guidance.
    - template: organizational_context.human_recall
      reason: Overlaps change_ticket and procedural_consistency org questions; dropped to stay within 18-question cap while retaining two org-context checks.
    - template: organizational_context.procedural_consistency
      reason: Overlaps change_ticket (both establish whether the action was sanctioned); dropped to stay within cap.
  hint_dispositions:
    - hint: registry.registry_key_context replaces cue_process_tool.mechanism_decode
      disposition: acted-on
      reason: cue_process_tool.mechanism_decode skipped; registry.registry_key_context authored as the Phase 2 analysis question (Q7). Recorded in dedup_decisions.
    - hint: CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries
      disposition: acted-on
      reason: >-
        All scoping queries use the rule's CreateObject/Wscript.shell/.RegWrite CommandLine|contains|all pattern instead of
        Image|endswith %Image%. Image-scoped cue templates (baseline, follow_on, scope) were skipped or replaced with
        CommandLine-pattern reflected variants; user_actor baseline/clustering queries had the CommandLine pattern injected.
    - hint: 'logsource ''image_load'' (recommended by techniques: T1059.005) is not covered'
      disposition: declined
      reason: >-
        The rule fires purely on a process_creation CommandLine pattern (CreateObject + Wscript.shell + .RegWrite); the
        investigation centers on what the RegWrite wrote and what executes from it, which Q7 (registry_key_context) and Q10
        (registry_value_persistence_check) answer from process_creation and file_event evidence. image_load of vbscript.dll
        / scrrun.dll would only confirm a scripting engine loaded, which the CommandLine match already establishes, and would
        add volume without precision for triage. No compensating attack:T1059.005.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - registry
    - organizational_context
    - obfuscation
    - persistence_scope
    - parent_child
  generation_stats:
    total_questions: 18
    source_counts:
      template: 18
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 14
      registry_set: 1
      file_event: 2
      sigma_alert: 1
