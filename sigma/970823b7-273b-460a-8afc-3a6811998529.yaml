name: Uncommon One Time Only Scheduled Task At 00:00
id: 3f8a1c2e-6b4d-4e9a-9c7f-2a5d8e1b0f43
description: |
  Detects creation of a scheduled task via schtasks.exe whose command line schedules a one-time-only run at 00:00 (midnight) and whose action invokes a script host or proxy-execution binary (wscript, cscript, vbscript, wmic, regsvr32, powershell) or references an AppData path. The rule fires only when all three conditions hold together: the schtasks.exe binary, the suspicious action keyword, and the once + 00:00 timing pattern. A one-time task at midnight is unusual outside legitimate offline-maintenance windows; the same pattern is used to detonate a payload at a fixed time while blending into normal task-scheduler activity. Determine what command the task runs, whether the task is still registered, whether it has already executed, and whether the user, host, and timing are consistent with sanctioned administrative change.
type: detection
detection_id: 970823b7-273b-460a-8afc-3a6811998529
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the schtasks command line schedule a one-time task at 00:00 that runs a script host or proxy-execution binary?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when all three conditions are present together in one schtasks.exe invocation. Those conditions are the once and 00:00 timing keywords, a suspicious action keyword (wscript, cscript, vbscript, wmic, regsvr32, powershell, or an AppData path), and the schtasks binary name or OriginalFileName. Verify the exact command line shows all three before proceeding.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to create scheduled tasks that run script hosts or proxy-execution binaries at midnight?
  context: Legitimate administrative activity can trigger this detection. Knowing whether the user role and host function routinely involve scheduling maintenance tasks helps distinguish a sanctioned change from an attacker establishing timed execution. A one-time midnight task on a workstation by a non-administrative user is harder to explain than the same task on a server during a known maintenance build.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for this scheduled task?
  context: Many organizations require change tickets for persistence mechanisms such as scheduled tasks. A corresponding ticket suggests authorized maintenance; absence of one when policy requires it suggests the task was created outside sanctioned process. This check can quickly close a false positive or escalate a true positive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:scheduled_task.task_configuration
  question: What scheduled task was created, and what command does it execute? Identify the task name, the run command, and the schedule.
  context: The schtasks command line contains the task name (/TN), the command or program to execute (/TR), and the schedule (/SC once /ST 00:00). The /TR value reveals the persistence payload, frequently a script host invocation, a regsvr32 or wmic proxy-execution call, or a binary dropped under an AppData path. Read each argument to determine exactly what will run at the scheduled time.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:obfuscation.encoded_payload_decode
  question: Is the task's run command (/TR) encoded or obfuscated, and what does the decoded content do?
  context: The action embedded in the /TR argument is the operator's payload and is often hidden from casual inspection. Look for base64 (powershell -EncodedCommand), character concatenation in VBScript or JScript run via wscript/cscript, regsvr32 scriptlet URLs, or wmic process-call invocations. Decoding reveals the actual intent, such as a download cradle, a reverse shell, or an in-memory loader. Iterate decoding until the payload is readable.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1053.005.security_event_corroboration
  question: Does a task-registration record exist in the Security log for this host around the alert time, and what task name and author does it show?
  context: Scheduled-task creation is also recorded natively when task-creation auditing is enabled, capturing the task name, the registering account, and the task content independently of the process_creation event. This corroborates the schtasks command line and surfaces the registering account even when the command-line view is incomplete. If task-registration auditing is not enabled in this environment, treat the absence of a record as a visibility gap rather than evidence the task was not created.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID:
          - 4698
          - 4702
      condition: selection
    fields:
      - TaskName
      - SubjectUserName
- source: attack:T1053.005.registry_task_artifact
  question: Were scheduled-task registry keys written on this host around the alert time, and do they correspond to the alerting task?
  context: Registering a scheduled task writes the task definition into the Task Scheduler registry tree (the Tasks and Tree keys under Schedule\TaskCache). These registry writes persist the task and can reveal a task that was created through an API or removed from the on-disk task store. Correlate the written key paths with the task name from the command line to confirm the task is materially registered, not just attempted.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\Microsoft\Windows NT\CurrentVersion\Schedule\TaskCache\'
      condition: selection
    fields:
      - TargetObject
      - Image
- source: template:scheduled_task.task_trigger_and_timing
  question: When is the task scheduled to run, and has the payload already executed?
  context: A one-time task set for 00:00 fires at the next midnight, so the payload may not have run yet at alert time, or it may have already executed if the alert was reviewed after midnight. Scheduled tasks execute as children of the Task Scheduler service. Checking process creation under the scheduler after the task creation reveals whether the payload ran and what it spawned.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\svchost.exe'
        ParentCommandLine|contains: 'Schedule'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:scheduled_task.task_still_present
  question: Is the scheduled task still registered on the system?
  context: Persistence is only effective if the task still exists, and some operators delete a task after it executes to reduce forensic artifacts. Confirming the task is still present on the host determines whether the mechanism is active and requires remediation, or whether it has already run and been cleaned up. Verify the specific task name from the command line directly on the host or via endpoint tooling.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this midnight one-time-task pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even routine maintenance can produce midnight tasks. This query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here rather than generic schtasks usage. A new parent process, an unexpected account, or a first-time appearance of the pattern is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|contains: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains:
        - wscript
        - vbscript
        - cscript
        - 'wmic '
        - wmic.exe
        - regsvr32.exe
        - powershell
        - \AppData\
      selection_time:
        CommandLine|contains|all:
        - once
        - 00:00
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this midnight one-time-task pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this specific firing pattern rather than generic schtasks usage. Many hosts firing the same pattern points to scripted deployment or vendor automation; a single host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|contains: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains:
        - wscript
        - vbscript
        - cscript
        - 'wmic '
        - wmic.exe
        - regsvr32.exe
        - powershell
        - \AppData\
      selection_time:
        CommandLine|contains|all:
        - once
        - 00:00
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|contains: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains:
        - wscript
        - vbscript
        - cscript
        - 'wmic '
        - wmic.exe
        - regsvr32.exe
        - powershell
        - \AppData\
      selection_time:
        CommandLine|contains|all:
        - once
        - 00:00
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|contains: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains:
        - wscript
        - vbscript
        - cscript
        - 'wmic '
        - wmic.exe
        - regsvr32.exe
        - powershell
        - \AppData\
      selection_time:
        CommandLine|contains|all:
        - once
        - 00:00
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user created similar tasks or ran tooling on other hosts in the same window, the scope of the activity may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|contains: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains:
        - wscript
        - vbscript
        - cscript
        - 'wmic '
        - wmic.exe
        - regsvr32.exe
        - powershell
        - \AppData\
      selection_time:
        CommandLine|contains|all:
        - once
        - 00:00
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:lolbin.renamed_binary_check
  question: Was schtasks.exe executed from its expected System32 path?
  context: A binary running from a non-standard path (outside System32 or SysWOW64) may have been copied or renamed to evade path-based allowlists or to masquerade as a system utility. The detection matches on the Image name and OriginalFileName, so a renamed copy could still fire; confirming the on-disk path and original filename distinguishes the genuine system utility from a relocated copy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were other proxy-execution or script-host LOLBins used on this host recently for the same objective?
  context: The task's action invokes a script host or proxy-execution binary, and operators who use one such binary often use several. Finding regsvr32, mshta, rundll32, wmic, or similar binaries performing related actions on the host around the same time strengthens the case that the scheduled task is part of an execution chain rather than an isolated maintenance entry.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\rundll32.exe'
          - '\wmic.exe'
          - '\wscript.exe'
          - '\cscript.exe'
          - '\msbuild.exe'
          - '\installutil.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to persistence locations such as the Startup folder or Run key paths on this host?
  context: File-based persistence bypasses command-line detection entirely. Dropping an executable or script into a Startup folder or a path referenced by a registry Run key achieves persistence without invoking schtasks at all. Operators frequently stack a scheduled task with a parallel file-drop mechanism, so checking for drops in the same window reveals whether this is part of a broader persistence effort.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1053.005
      name: Scheduled Task
      role: primary
    - id: T1053
      name: Scheduled Task/Job
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: task_creation
      description: "Actor creates a scheduled task via schtasks.exe configuring a one-time 00:00 trigger and a script-host/proxy-execution action"
      mapped_to: Q4
      source: template:scheduled_task.task_configuration
    - stage: 2
      label: task_registration_artifact
      description: "Task creation is recorded in the Security log (4698/4702) and writes the Task Scheduler registry tree"
      mapped_to: Q6
      source: attack:T1053.005.security_event_corroboration
    - stage: 3
      label: scheduled_execution
      description: "Task fires at the scheduled time, executing the payload as a child of the Task Scheduler service"
      mapped_to: Q8
      source: template:scheduled_task.task_trigger_and_timing
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1053.005
      covered_by: Q5
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1053.005
      covered_by: Q8
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1053.005
      covered_by: Q4
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: user_context
      technique: T1053.005
      covered_by: Q6
      source: attack:T1053.005.security_event_corroboration
      coverage_type: direct
    - dimension: command_analysis
      technique: T1053
      covered_by: Q4
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: execution_context
      technique: T1053
      covered_by: Q8
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Correlation window between task creation and scheduled execution carried in task_trigger_and_timing context; no specific threshold given."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (SYSTEM / non-standard accounts)"
      disposition: new_question
      target_question: Q6
      notes: "Registering account surfaced via Security 4698/4702 SubjectUserName; user-pivot baseline (Q12) also covers."
    - element: TaskNamePattern
      dimension: tuning_parameter
      specific_values: "rationale only (obfuscated/randomized task names)"
      disposition: context
      target_question: Q4
      notes: "Analyst instructed to read /TN; no fixed name pattern to inject."
    - element: CommandLineEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (base64 / encoded blobs)"
      disposition: context
      target_question: Q5
      notes: "Encoded /TR payload decoding covered in obfuscation question; no numeric threshold queryable."
    - element: TaskAuthor
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: new_question
      target_question: Q6
      notes: "Task author/registering account from Security 4698/4702."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "wscript, vbscript, cscript, wmic, regsvr32.exe, powershell, \\AppData\\, once, 00:00 (rule selection values)"
      disposition: query_injection
      target_question: Q10
      notes: "Rule's full selection values embedded verbatim in REFLECTED baseline_deviation and scope queries."
    - element: ExecutionWindow
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Lookback to correlate execution after registration covered in task_trigger_and_timing."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by: scheduled_task.task_configuration — technique set version is more specific to schtasks /TN /TR /SC parsing"
      kept: scheduled_task.task_configuration
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair non-dedup
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair non-dedup
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "Parent chain is informational here; coverage of parent legitimacy provided by cue_user_actor.baseline_svc (parent+binary historical routine) without an additional standalone parent-lookup question. Dropped to stay within 14-18 cap."
    - template: cue_process_tool.baseline
      reason: "Generic Image|endswith schtasks baseline superseded by the REFLECTED baseline_deviation (rule-pattern-scoped) which is more precise for this rule's specific firing pattern."
    - template: cue_process_tool.follow_on
      reason: "Follow-on child processes of schtasks.exe itself are not the payload (the payload runs later under the scheduler); scheduled_task.task_trigger_and_timing covers the actual delayed execution path more accurately."
    - template: cue_process_tool.clustering
      reason: "Around-the-time host tool clustering overlaps cue_user_actor.clustering (user-pivot) and lolbin.technique_class_lateral (LOLBin sweep); dropped to stay within 14-18 cap in favor of those more targeted views."
    - template: cue_host_context.clustering
      reason: "Same around-the-time host-activity intent as cue_process_tool.clustering; both dropped to avoid a redundant host-wide aggregation while the user-pivot and LOLBin sweeps carry the surrounding-activity coverage within cap."
    - template: cue_host_context.precursor
      reason: "Prior-alert timeline is useful but the disposition here turns on task configuration/registration; dropped to stay within cap. No execution/credential-access pivot depends on it."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED generic tamper sweep; this rule detects scheduled-task creation, not payload execution or credential access, so tamper-as-precursor is not the live disposition question. Dropped per template guidance to stay within cap."
    - template: scheduled_task.task_still_present
      reason: "Included (not skipped) — listed here only to note it is must_include and retained."
    - template: persistence_scope.process_based_persistence
      reason: "Overlaps lolbin.technique_class_lateral (both sweep persistence/proxy LOLBins on the host); kept the file-based persistence parallel mechanism instead to add a distinct logsource, dropped process-based to avoid near-duplicate process_creation sweep within cap."
    - template: organizational_context.timing_alignment
      reason: "RECOMMENDED. Off-hours/midnight timing is already the core signal called out in the verify question and description (once + 00:00); a standalone timing-alignment question restates that without adding a distinct disposition input. Dropped to stay within 14-18 cap; change_ticket retained as the stronger sanctioned-change check."
    - template: organizational_context.procedural_consistency
      reason: "RECOMMENDED. Deployment-path question (GPO/SCCM vs direct schtasks) overlaps the change_ticket disposition check; dropped to stay within cap, keeping change_ticket as the single authorization-path question."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1053.005) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: acted_on
      reason: >-
        Acted on, not declined. Authored Q6
        (attack:T1053.005.security_event_corroboration) querying Security
        EventID 4698/4702 for native task-registration records. Listed here
        for audit visibility of the act-on decision.
    - hint: "logsource 'registry_set' (recommended by techniques: T1053.005) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Acted on, not declined. Authored Q7
        (attack:T1053.005.registry_task_artifact) querying registry_set for
        Task Scheduler TaskCache key writes. Listed here for audit visibility
        of the act-on decision.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Rule is technique-focused (scheduled-task creation pattern). The ransomware keyword comes from one reference's threat context, not the rule's detection logic; per style guide the playbook investigates the scheduling pattern, not a specific malware family. No ransomware-specific question authored."
      - id: network_scope
        confidence: low
        reason: "Rule fires on schtasks.exe process_creation with no network field in detection; network-scope keyword match is incidental to the ransomware reference, not the rule's behavior."
  technique_sets_applied:
    - universal
    - scheduled_task
    - organizational_context
    - obfuscation
    - persistence_scope
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      security: 1
      registry_set: 1
      file_event: 1
