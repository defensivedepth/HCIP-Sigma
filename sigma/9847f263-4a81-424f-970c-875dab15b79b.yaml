name: Suspicious TSCON Start as SYSTEM
id: 3cc48f1b-7167-4301-9772-bda00af23b4e
description: |
  Detects tscon.exe running under the LOCAL SYSTEM identity (User containing AUTHORI/AUTORI). When tscon runs as
  SYSTEM it can reconnect to any user's active or disconnected terminal-services session without supplying that
  user's password, silently taking over the session and inheriting its process tree, drive mappings, and cached
  credentials. Legitimate SYSTEM-context tscon is rare and usually tied to a service or scheduled task; the
  investigation should determine which session was targeted, what spawned tscon, and whether a service or task was
  created to obtain the SYSTEM context.
type: detection
detection_id: 9847f263-4a81-424f-970c-875dab15b79b
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did tscon.exe run under a LOCAL SYSTEM identity?
  context: Before investing investigation time, confirm the alert is a true positive by checking that Image ends in
    tscon.exe and User resolves to a SYSTEM/AUTHORITY principal. SYSTEM-context tscon is the heuristic this rule
    fires on, so this pairing is the core verification.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - User
- source: template:universal.host_user_context
  question: Is this host expected to run terminal-services session-connect operations as SYSTEM?
  context: SYSTEM-context tscon is uncommon outside of session brokers, RDS hosts, or specific service automation.
    Knowing whether this host brokers remote desktop sessions and whether SYSTEM-owned tscon fits its normal role
    is often the fastest path to separating a true positive from routine terminal-services maintenance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:remote_access_tool.tool_authorization
  question: Is SYSTEM-context use of tscon.exe sanctioned for remote session management on this host?
  context: tscon is a native Windows terminal-services binary, so software allowlisting will not flag it. The
    relevant question is whether an approved administrative process on this host is supposed to switch terminal
    sessions as SYSTEM. Unsanctioned SYSTEM-context tscon, even from a built-in tool, can represent session
    hijacking or an operator moving laterally through an existing desktop session.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a helpdesk ticket or maintenance window explaining a SYSTEM session reconnect on this host?
  context: Legitimate remote session management typically correlates with an active support ticket, a scheduled
    maintenance window, or a documented administrative procedure. If no support context exists, a SYSTEM-context
    session reconnect may be an attacker taking over another user's session without their credentials.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What session did tscon reconnect to? Decode the session ID and /dest target in the command line.
  context: The full command line reveals which session was hijacked and where it was redirected. tscon takes a
    source session ID or name and a /dest target (for example "tscon 2 /dest:console" or "tscon 1
    /dest:rdp-tcp#55"); the /dest value identifies the session that received control. Map the source session ID to
    a user via the session-inventory tooling to learn whose desktop was taken over.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned tscon.exe, and is that parent chain expected?
  context: The parent reveals how the SYSTEM context was obtained. tscon launched by services.exe indicates a
    service whose image path runs the reconnect command (a common way to gain LocalSystem); a cmd.exe or
    powershell.exe parent points to an interactive SYSTEM shell. Examine what spawned the parent and what else the
    parent launched to reconstruct how SYSTEM was reached.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1219.002.persistence_service
  question: Was a new Windows service created or started on this host shortly before tscon ran as SYSTEM?
  context: A frequent route to SYSTEM-context tscon is creating a service whose binary path runs the reconnect
    command, then starting it (for example "sc create" with a binpath calling tscon, followed by "sc start"); the
    service inherits LocalSystem. Service-control activity in the window before the alert suggests the SYSTEM
    context was manufactured for the hijack rather than arising from routine session brokering.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_sc:
        CommandLine|contains:
          - 'sc create'
          - 'sc  create'
          - 'sc start'
          - 'sc  start'
          - 'New-Service'
          - 'net start '
      condition: selection_host and selection_sc
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Obtaining SYSTEM and taking over a privileged session is the kind of hands-on action an operator may
    precede by weakening protection. The query surfaces direct attempts to disable AV/EDR services, flip Defender
    flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy"
    signal; populated rows shift the disposition toward operator preparation, so review the parent process and
    signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:remote_access_tool.connection_destination
  question: What network connections did tscon.exe make around the reconnect?
  context: A reconnected terminal session may carry outbound activity tied to the binary or feed a remote operator.
    Connections to internal RDP listeners or to unexpected external addresses around the reconnect help distinguish
    a local console switch from remote operator control. tscon often makes no outbound connections of its own; if
    this returns nothing, fall back to the /dest target and session-name parameters in the reconnect command line.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:cue_process_tool.follow_on
  question: What child processes did the reconnected session spawn afterward?
  context: After a session takeover, the inherited or newly spawned process tree reveals the operator's intent.
    Reconnaissance commands, credential-access tooling, or lateral-movement utilities launched on this host shortly
    after the reconnect indicate hands-on activity inside the hijacked session rather than a benign session switch.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and
    does the current alert fit?
  context: Even native tools have a baseline. The query mirrors the rule's selection structure scoped to this host,
    so the resulting parent/user distribution reflects how SYSTEM-context tscon has historically run here. A new
    parent process such as services.exe, an unfamiliar SYSTEM-derived account, or an unusual command-line shape is
    the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|contains:
        - AUTHORI
        - AUTORI
        Image|endswith: \tscon.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host distribution look
    like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, giving a fleet-wide count of
    SYSTEM-context tscon executions rather than generic tscon usage. Many hosts firing the same pattern can point
    to a session broker or a management product behaving consistently; one host or a small unrelated set points to
    manual operator activity worth prioritising.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|contains:
        - AUTHORI
        - AUTORI
        Image|endswith: \tscon.exe
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user identity normally run terminal-services session-connect operations?
  context: Compare the firing identity against its historical behavior. A SYSTEM-derived principal that has never
    driven tscon, or a user account that does not normally manage sessions, is more suspicious than one that does
    so routinely. When the alert fires under SYSTEM the user pivot may degenerate, so read this alongside the
    parent+binary baseline.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+tscon launch combination historically routine on this host or across the environment?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism — which parent process spawned tscon, and whether that pair is
    historically common. A new or rare parent for tscon (such as a freshly created service) is the deviation worth
    chasing; a pair already accounting for thousands of prior events is OS or management automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user identity do across the network around this time?
  context: Tracking the firing identity's activity across all hosts reveals lateral movement following a session
    takeover. If the same identity triggered similar activity on multiple hosts, the scope may extend beyond this
    endpoint. When the alert fires under SYSTEM this pivot returns broad agent telemetry, so weigh it together with
    the parent+binary cross-host view.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+tscon combination fire on other hosts in the same window?
  context: Under a service principal, "what else did this user do" returns agent-telemetry noise because every host
    runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism — parent
    process plus tscon — fired on other hosts in the alert window. Multiple hosts in the same window points to
    pushed or scheduled automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Session hijacking rarely happens in isolation. Earlier detections on the same host — privilege
    escalation, service creation, or credential access — may reveal how the operator reached SYSTEM and help
    reconstruct the timeline leading up to the reconnect.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: attack:T1219.002.file_event
  question: Were any executables or remote-access agent files written to this host around the time of the alert?
  context: Beyond native session takeover, remote-access tradecraft often drops an agent or helper binary to
    establish persistent control. Files written to user-writable or temp locations around the reconnect — service
    binaries, remote-control agents, or staged tooling — indicate the operator is installing follow-on access
    rather than only switching sessions. Scope to the written file paths so the result is not the host's full file
    activity.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith:
          - '.exe'
          - '.dll'
          - '.sys'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219.002
      name: Remote Desktop Software
      role: primary
    - id: T1219
      name: Remote Access Tools
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: initial_execution
      description: "Execution of the remote-control mechanism under a privileged context (here, tscon.exe as SYSTEM reconnecting a terminal session)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: privilege_persistence
      description: "Acquisition of SYSTEM context, commonly via service/autorun creation (sc create + start)"
      mapped_to: Q7
      source: attack:T1219.002.persistence_service
    - stage: 3
      label: session_egress
      description: "Long-lived outbound connection / remote session establishment to operate the controlled host"
      mapped_to: Q9
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: interactive_control
      description: "Interactive control signals: shell or tooling child processes spawned inside the hijacked session"
      mapped_to: Q10
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: time_correlation
      technique: T1219.002
      covered_by: Q7
      source: attack:T1219.002.persistence_service
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219.002
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: process_identification
      technique: T1219
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q13
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: Image
      dimension: tuning_parameter
      specific_values: "rationale only (RMM binaries vary; rule constrains Image to tscon.exe)"
      disposition: context
      target_question: Q18
      notes: "Rule is native-binary RDP hijack, not third-party RMM. File-write sweep (Q18) covers dropped agent/service binaries that this mutable element anticipates."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "rationale only (RMM ports e.g. 7070, 5650 vary)"
      disposition: context
      target_question: Q9
      notes: "tscon does not itself beacon; connection_destination question surfaces DestinationPort without hardcoding RMM-specific values that do not apply to native session takeover."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: new_question
      target_question: Q6
      notes: "Parent chain investigated directly; services.exe parent is the load-bearing hijack signal called out in context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window per environment)"
      disposition: context
      target_question: Q7
      notes: "Service-creation precursor scoped to -1h; install-to-reconnect correlation stated in context."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q13
      notes: "User baseline and host-role question separate SYSTEM-context tscon from session-broker hosts."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "tool_authorization frames native tscon allowlisting; software allowlists do not flag built-in binaries."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "user-writable / temp paths"
      disposition: context
      target_question: Q18
      notes: "File-write question flags executables written around the alert; user-writable/temp path emphasis in context."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (vendor support clouds)"
      disposition: context
      target_question: Q9
      notes: "Connection-destination question interprets internal RDP vs. unexpected external addresses."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q10
      notes: "follow_on surfaces child shells/tooling spawned inside the hijacked session."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only (session duration, byte ratios)"
      disposition: context
      target_question: Q9
      notes: "Egress thresholds are not directly queryable; connection-destination context covers the egress angle."
  dedup_decisions:
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable; emit BOTH user-pivot (Q13) and service-principal substitute (Q14)"
      kept: cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable; emit BOTH user-pivot (Q15) and service-principal substitute (Q16)"
      kept: cue_user_actor.clustering_svc
    - dropped: cue_process_tool.baseline
      reason: "Host-frequency of tscon subsumed by user-actor baseline (Q13) and rule-pattern baseline_deviation (Q11); generic Image baseline would duplicate without rule-pattern precision."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_host_context.clustering
      reason: "Same-window host activity already surfaced by follow_on (Q10), service-creation precursor (Q7), and parent+binary cross-host (Q16); generic host aggregation adds no distinct angle under the 18-question cap."
      kept: cue_host_context.precursor
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      file_event: 1
      sigma_alert: 1
