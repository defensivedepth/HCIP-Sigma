name: Service Security Descriptor Tampering Via Sc.EXE
id: ee7150ca-01d5-4034-9ce2-1863fafcfe25
description: |
  Detects sc.exe invoked with the sdset subcommand, which writes a new security descriptor (SDDL) to a Windows service. The rule fires when the process is sc.exe (by image name or original filename) AND the command line contains sdset. This is the mechanism used to hide a service from enumeration tools by denying the List-Children right to Interactive Users, Service logon, and Built-in Administrators, or to loosen a service descriptor so a non-admin can start or modify a privileged service. Legitimate use exists (administrators occasionally repair service permissions), so the investigation should decode the SDDL, identify which service was retargeted, confirm whether the service is running and under what account, and determine whether the change was authorized.
type: detection
detection_id: 98c5aeef-32d5-492f-b174-64a691896d25
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show sc.exe running the sdset subcommand against a service?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires when the process is sc.exe (matched by image name or original filename) and the command line contains sdset, which writes a new security descriptor to a named service. Read the full command line to confirm both the sdset verb and the target service name are present.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to modify service security descriptors?
  context: Editing a service security descriptor with sc.exe sdset is an administrative action normally limited to a small set of operators on servers or managed endpoints. If the account or host has no role in service administration, the activity is harder to explain as routine maintenance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: attack:T1574.011.sddl_decode
  question: Decode the SDDL string passed to sdset - which principals are denied access and which rights are involved?
  context: The security descriptor in the sdset command line is the payload of this technique. A Deny ACE of the form (D;;...;;;IU), (D;;...;;;SU), or (D;;...;;;BA) that removes the List-Children (LC) right hides the service from services.exe, Get-Service, and sc query. An Allow ACE granting start or write rights to Everyone (WD) or Authenticated Users (AU) loosens the descriptor so a non-admin can control a privileged service. Identify the target service name, the denied principals, and the rights in each ACE to determine whether this hides or loosens the service.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Image
      - User
- source: template:service_persistence.service_configuration
  question: Which service was created or modified, and what binary does it execute?
  context: The descriptor change targets a named service. Resolve that service to its installation record to read the binary path it executes, its start type, and the account it runs under. A binary placed in a world-writable directory such as C:\Users\Public or C:\Windows\Temp, or one masquerading as a legitimate service name, is the persistence payload and should be analyzed directly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 7045
      condition: selection
    fields:
      - ServiceName
      - ImagePath
      - StartType
      - AccountName
- source: template:service_persistence.service_current_status
  question: Is the target service currently running, and what account is it using?
  context: A service whose descriptor was tampered may already be running and hidden, or may be set to start on demand and waiting. Run sc query and sc qc against the target service name on the host to confirm whether the persistence is active. A service running as LocalSystem has full system privileges; a specific user account indicates the access level the operator had.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1574.011.registry_set
  question: Were the service's registry keys modified around the same time, and do ImagePath or ServiceDll point to an unexpected binary?
  context: A service descriptor change frequently accompanies modification of the service's configuration under HKLM\SYSTEM\CurrentControlSet\Services. Review registry-set events on this host for changes to ImagePath, ServiceDll, FailureCommand, or Parameters subkeys of service entries near the alert time. A path written into a non-standard directory, or a ServiceDll loading from a user-writable location, indicates the descriptor change is part of execution-flow hijack rather than a benign permission repair.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\Services\'
      selection_keys:
        TargetObject|endswith:
          - '\ImagePath'
          - '\ServiceDll'
          - '\FailureCommand'
      condition: selection and selection_keys
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned sc.exe and is that parent chain expected?
  context: The parent process reveals how sc.exe was invoked. Service administration normally runs under an interactive administrator shell, a deployment agent, or services.exe. A parent such as a web server worker, an Office application, or a script host (wscript.exe, mshta.exe) indicates the descriptor change followed exploitation or a malicious script rather than routine maintenance.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously run sc.exe sdset, what parents and users normally drive it, and does the current alert fit that pattern?
  context: The query mirrors the rule's selection structure scoped to this host, so the parent and user distribution reflects how the sdset pattern specifically has run here rather than generic sc.exe usage. A new parent process, an unexpected user account, or a first-time appearance of the pattern is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_cli:
        CommandLine|contains: sdset
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.renamed_binary_check
  question: Was sc.exe executed from its expected System32 or SysWOW64 path?
  context: sc.exe run from outside System32 or SysWOW64 may have been copied or renamed to evade path-based allowlists or to ship a tampered build. Compare the image path and the original filename - a sc.exe running from a user temp or download directory is more suspicious than one in the system directory.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host run the sc.exe sdset pattern over the past 90 days, and what command-line structures are typical?
  context: A descriptor-tampering pattern with no prior history on a host is more suspicious than one seen regularly under stable automation. The query mirrors the rule's selection structure scoped to this host, so the count reflects the sdset pattern specifically rather than all sc.exe usage. The command-line projection lets you compare typical descriptor strings against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_cli:
        CommandLine|contains: sdset
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:lolbin.technique_class_lateral
  question: Were other persistence or defense-evasion LOLBins used on this host recently?
  context: Operators who tamper with a service descriptor often pair it with other built-in tools for persistence and evasion - schtasks, reg, at, netsh, wevtutil, bcdedit, or vssadmin. Finding several of these performing related actions in the same window strengthens the case that the sdset call is part of an intentional foothold rather than an isolated administrative edit.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\reg.exe'
          - '\netsh.exe'
          - '\wevtutil.exe'
          - '\bcdedit.exe'
          - '\vssadmin.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_cli:
        CommandLine|contains: sdset
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_cli:
        CommandLine|contains: sdset
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar service or persistence activity on several hosts, the scope of the operation may be larger than this one alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \sc.exe
      - OriginalFileName: sc.exe
      selection_cli:
        CommandLine|contains: sdset
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to the descriptor change and what followed it - reconnaissance, privilege escalation, or additional persistence installation. Novel parent-child process pairs at low counts are what to scan for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - defense tampering, reconnaissance, credential access - may reveal the attack stages that preceded the descriptor change and help reconstruct the timeline. Service hiding is typically a late-stage persistence step, so prior execution or exploitation alerts are especially relevant.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After hiding or retargeting a service, they commonly install backup footholds using scheduled tasks, registry Run keys, BITS jobs, or WMI subscriptions. Multiple persistence installations in the same timeframe indicate intentional stacking rather than an isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1574.011
      name: Services Registry Permissions Weakness
      role: primary
    - id: T1574
      name: Hijack Execution Flow
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: descriptor_modification
      description: "sc.exe sdset writes a new security descriptor (SDDL) to a named service, denying enumeration rights or loosening access"
      mapped_to: Q3
      source: attack:T1574.011.sddl_decode
    - stage: 2
      label: service_config_change
      description: "The retargeted service's configuration (binary path, registry keys) is modified to execute the persistence payload"
      mapped_to: Q6
      source: attack:T1574.011.registry_set
    - stage: 3
      label: service_execution
      description: "The service runs (often hidden) under a privileged account, deviating from baseline configuration"
      mapped_to: Q5
      source: template:service_persistence.service_current_status
  dimension_coverage:
    - dimension: time_correlation
      technique: T1574.011
      covered_by: Q6
      source: attack:T1574.011.registry_set
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1574.011
      covered_by: Q3
      source: attack:T1574.011.sddl_decode
      coverage_type: direct
    - dimension: file_path_analysis
      technique: T1574
      covered_by: Q4
      source: template:service_persistence.service_configuration
      coverage_type: direct
  mutable_dispositions:
    - element: MonitoredServiceKeys
      dimension: tuning_parameter
      specific_values: "ImagePath, ServiceDll, FailureCommand, Parameters"
      disposition: query_injection
      target_question: Q6
      notes: "Injected as TargetObject|endswith filter list in the registry_set question"
    - element: BaselineServiceConfig
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "No fixed values; service_configuration question reads ImagePath/StartType/AccountName for comparison against known-good"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Correlation interval between descriptor/registry change and service execution carried as +/-1h range and prose"
    - element: PrivilegedAccounts
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q2
      notes: "Accounts permitted to administer services covered by host_user_context legitimacy check"
    - element: ServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "T1574 secondary; expected service paths read by service_configuration"
    - element: AllowedDllPaths
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "T1574 secondary; ServiceDll path scrutiny folded into registry_set question prose"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivot); both emitted"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: service_persistence.service_configuration (hint). The SDDL-specific decode is authored as attack:T1574.011.sddl_decode, which is more precise than the generic mechanism_decode for this rule."
    - template: cue_process_tool.baseline
      reason: "superseded by lolbin.historical_lolbin_baseline (REFLECTED) which counts the sdset pattern specifically rather than all sc.exe usage; generic host baseline would be noisier and less aligned"
    - template: cue_process_tool.follow_on
      reason: "sc.exe sdset modifies a descriptor and exits; it does not spawn the persistence payload as a child. follow_on child-process scope is not the mechanism here — the payload runs later as the service. Covered topically by process_based_persistence and host clustering"
    - template: cue_process_tool.scope
      reason: "REFLECTED fleet-wide variant; cross-host coverage of the sdset pattern provided by cue_user_actor.clustering_svc (same window, parent+binary) and process_based_persistence. Dropped to stay within the 18-question cap"
    - template: cue_process_tool.clustering
      reason: "duplicated by cue_host_context.clustering (same host-scoped around-the-alert process aggregation); kept the host_context version"
    - template: universal.endpoint_protection_health
      reason: "cue_host_context.precursor is present and the rule is persistence/defense-evasion (not execution or credential-access), so prior-alert context already carries any tamper signal; defense_tampering technique set is not confirmed for this rule. Dropped per the template's documented condition (b)"
    - template: organizational_context.change_ticket
      reason: "Dropped to stay within the 18-question cap; host_user_context and user_actor baselines already establish whether the action is authorized/expected"
    - template: organizational_context.human_recall
      reason: "Dropped to stay within the 18-question cap; legitimacy coverage provided by host_user_context and user_actor baseline"
    - template: organizational_context.timing_alignment
      reason: "Dropped to stay within the 18-question cap; temporal context provided by host clustering and precursor questions"
    - template: organizational_context.procedural_consistency
      reason: "Dropped to stay within the 18-question cap; procedural legitimacy partially covered by host_user_context"
    - template: persistence_scope.file_based_persistence
      reason: "Dropped to stay within the 18-question cap; sc.exe sdset is a descriptor change, not a file drop. Process-based persistence (process_based_persistence) is the more relevant parallel-mechanism check for this rule"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1574.011) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Authored attack:T1574.011.registry_set (Q6) with category: registry_set,
        filtering Services subkey modifications to ImagePath/ServiceDll/FailureCommand.
        This crosses the process_creation → registry_set boundary the technique's
        analytics call for. Listed here for the audit trail; the action is a new
        question, not a decline.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - service_persistence
    - persistence_scope
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 15
      system: 1
      registry_set: 1
      sigma_alert: 1
