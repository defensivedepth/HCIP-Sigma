name: Setup16.EXE Execution With Custom .Lst File
id: 95f99dd2-6b22-4b7d-acd1-ee03c261a605
description: |
  Detects setup16.exe (a legacy InstallShield-era Windows setup binary in SysWOW64) running with a ' -m ' argument that points it at a custom .lst response file, while the child it spawns does NOT come from the legitimate C:\~MSSETUP.T\ extraction path. A .lst file can carry a RunCmd-style stanza that names an arbitrary program for setup16 to execute, so the real command never appears on setup16's own command line — making this a proxy-execution vector that defeats naive command-line monitoring. On modern systems setup16 is essentially never used legitimately, so the investigation should determine which child command setup16 launched, where the .lst file came from, and whether the activity ties to authorized maintenance or to an operator using a living-off-the-land binary.
type: detection
detection_id: 99c8be4f-3087-4f9f-9c24-8c7e257b442e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Did setup16.exe run with a ' -m ' argument pointing at a custom .lst file, and is the spawned child outside the C:\~MSSETUP.T\ extraction path?
  context: Confirm the alert is a true positive before investing time. The rule fires when the parent is setup16.exe in SysWOW64, its command line contains ' -m ' (the marker for a response/answer .lst file), AND the child it spawned does not start in C:\~MSSETUP.T\ — the normal install-extraction directory. Review the parent command line and the child image path together; a child running from a user-writable or unexpected location is the load-bearing part of the match.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run the legacy setup16.exe installer utility?
  context: On modern Windows, setup16.exe is practically never invoked, so legitimate use is rare and usually tied to ancient InstallShield-based installers. Knowing whether this host's function (e.g., a software-packaging or legacy-app workstation) and this user's role make setup16 plausible is often the fastest way to separate a true positive from an obscure but authorized install.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What was setup16.exe's full command line, and which .lst response file did the ' -m ' argument point at?
  context: The parent's command line names the .lst file that drives setup16's actions. Decode the full argument string to recover the path to the .lst (often under %TEMP%, %APPDATA%, or %PUBLIC% if attacker-supplied) so it can be retrieved and inspected for the RunCmd-style stanza that specifies the embedded program. The actual executed command lives inside that file, not on the command line.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1574.005.embedded_command
  question: What child process did setup16.exe spawn, and what is that child's full command line?
  context: Because the real instruction is hidden in the .lst file, the spawned child process is the clearest evidence of what setup16 actually executed. Inspect the child image and its arguments — script interpreters (powershell.exe, cmd.exe, wscript.exe), rundll32, or any non-Microsoft binary launched this way is the operational payload of the proxy execution. The child running from outside C:\~MSSETUP.T\ is what the rule keyed on.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: attack:T1574.005.file_path_analysis
  question: Where did the .lst file come from — was it written to a user-writable directory shortly before setup16 ran?
  context: A response file dropped to %TEMP%, %APPDATA%, %PUBLIC%, or C:\ProgramData and then handed to setup16 indicates attacker staging rather than a vendor install (which would extract to C:\~MSSETUP.T\). Look for a recent file write of a .lst (or the binary it references) to a user-writable path on this host, and correlate its write time to the setup16 execution.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_lst:
        TargetFilename|endswith: '.lst'
      condition: selection and selection_lst
    fields:
      - TargetFilename
      - Image
      - User
- source: attack:T1574.005.image_load
  question: What modules did the setup16-spawned child load, and were any loaded from user-writable or non-system paths?
  context: The proxy-executed child may side-load a DLL or load a module from an unusual location, which is the hijacked execution flow this technique describes. Modules loaded from %TEMP%, %APPDATA%, the install staging directory, or other user-writable paths — rather than System32/SysWOW64 — point to an attacker-supplied component rather than a normal install. If image_load events are not collected in this environment, treat zero results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_path:
        ImageLoaded|contains:
          - '\Users\'
          - '\Temp\'
          - '\AppData\'
          - '\ProgramData\'
          - '\Public\'
      condition: selection and selection_path
    fields:
      - Image
      - ImageLoaded
      - User
- source: attack:T1574.005.create_stream_hash
  question: Does the .lst file or the executed binary carry a Mark-of-the-Web or alternate data stream indicating it was downloaded?
  context: A Zone.Identifier or other alternate data stream on the response file or the binary it references shows the artifact arrived from outside the host — a browser download, email attachment, or remote copy — rather than being placed by a trusted installer. Files staged for living-off-the-land execution frequently retain this download mark. If alternate-data-stream events are not collected here, treat zero results as a visibility gap rather than evidence of nothing.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: create_stream_hash
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_parent_process.baseline
  question: Has setup16.exe ever spawned this child binary on this host before?
  context: setup16 spawning a child from outside the install-extraction path is, by itself, unusual on a modern system. A historical count of zero or near-zero for this specific setup16-to-child pairing on the host confirms a novel spawning relationship; a non-trivial prior history would point toward a recurring legacy-install process worth confirming with the owning team.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did setup16.exe spawn on this host?
  context: A single .lst-driven invocation can chain multiple commands. Enumerating every child of setup16 on this host reveals whether the proxy execution launched additional tools — reconnaissance, credential access, or further staging — beyond the one that tripped the alert, exposing the full scope of activity routed through this binary.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.clustering
  question: What other activity occurred around the setup16.exe execution on this host?
  context: Beyond child processes, the surrounding window may contain the file write that staged the .lst, network connections that fetched a payload, or follow-on tooling. Pulling host activity tightly around the setup16 execution helps reconstruct how the .lst arrived and what the embedded command went on to do.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is the setup16.exe parent spawning this child binary on other hosts in the environment?
  context: The same setup16-driven spawning pattern appearing on multiple hosts suggests a pushed or staged operation rather than a one-off, since legitimate use of this legacy binary is rare and isolated. A fleet-wide view calibrates severity and identifies the scope of any compromise.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run the setup16.exe installer utility or stage installer response files?
  context: Compare this user's current action against their history. A user who has never invoked setup16 or worked with .lst response files is more suspicious than one whose role (software packaging, legacy-app support) routinely involves installer tooling. User role and typical toolset should align with running a 16-bit-era setup binary.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is the setup16.exe parent paired with this spawned binary historically routine on this host or across the environment, or is this pairing new?
  context: When the alert fires under a service or computer account, user identity does not attribute behavior; the launch mechanism does. Check whether this parent-process-to-spawned-binary pairing already accounts for prior events (an existing legacy-install workflow) or is a first-time pairing. A new or rare pairing concentrated on the alerting host is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of this alert?
  context: Tracking the user's activity across all hosts in the surrounding window reveals lateral movement or activity spanning multiple endpoints. If the same user drove similar living-off-the-land execution elsewhere, the scope of compromise is larger than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same setup16.exe parent and spawned-binary pairing fire on other hosts in the same window, indicating coordinated rollout rather than isolated activity?
  context: Under a service or computer account, a user-pivot returns agent-telemetry noise. The meaningful question is whether the same launch mechanism — setup16 spawning this binary — appeared on other hosts in the alert window. Multiple hosts at once points to pushed or scheduled automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining what ran before and after on this host reveals the immediate context — what staged the .lst, what the embedded command did next, and whether this is part of a larger sequence such as reconnaissance, privilege escalation, or persistence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access, or staging activity — may reveal the attack stages that preceded the setup16 proxy execution and help reconstruct the full timeline leading up to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host around the same time?
  context: Proxy execution via a legacy installer can be a foothold step, and attackers rarely rely on a single persistence mechanism. Surfacing scheduled tasks, services, registry, BITS, or WMIC activity in the surrounding window shows whether the embedded command went on to install backup persistence — intentional stacking rather than an isolated install.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1574.005
      name: Executable Installer File Permissions Weakness
      role: primary
    - id: T1574
      name: Hijack Execution Flow
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: lst_file_staging
      description: "Attacker drops a custom .lst response file (and any referenced binary) to a user-writable path"
      mapped_to: Q5
      source: attack:T1574.005.file_path_analysis
    - stage: 2
      label: setup16_invocation
      description: "setup16.exe is invoked with ' -m ' pointing at the custom .lst file"
      mapped_to: Q3
      source: template:cue_parent_process.mechanism_decode
    - stage: 3
      label: embedded_command_execution
      description: "setup16 parses the .lst and CreateProcess's the embedded command (the alerting child)"
      mapped_to: Q4
      source: attack:T1574.005.embedded_command
    - stage: 4
      label: hijacked_module_load
      description: "Spawned child loads attacker-supplied module from abnormal path (Hijack Execution Flow)"
      mapped_to: Q6
      source: attack:T1574.005.image_load
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1574.005
      covered_by: Q5
      source: attack:T1574.005.file_path_analysis
      coverage_type: direct
    - dimension: time_correlation
      technique: T1574.005
      covered_by: Q5
      source: attack:T1574.005.file_path_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1574.005
      covered_by: Q8
      source: template:cue_parent_process.baseline
      coverage_type: partial
    - dimension: user_context
      technique: T1574.005
      covered_by: Q13
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: MonitoredDirectories
      dimension: file_path_analysis
      specific_values: "%TEMP%, C:\\ProgramData, installer unpack paths, %APPDATA%, %PUBLIC%"
      disposition: query_injection
      target_question: Q6
      notes: "User-writable path list injected into image_load ImageLoaded|contains; also surfaced in Q5 context (.lst staging) and Q4 context"
    - element: HashBaseline
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "No concrete hashes in brief; replacement/download detection surfaced via create_stream_hash MOTW question"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Correlation between .lst write and setup16 execution framed as +/-4h scoping in the file-staging question"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q13
      notes: "Expected admin-installer vs anomalous user write distinguished in user baseline question and host_user_context"
    - element: ServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "T1574 secondary; service/registry path baseline not applicable to this LOLBin spawn — covered as parent-child pairing baseline"
    - element: AllowedDllPaths
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: query_injection
      target_question: Q6
      notes: "Abnormal DLL load directories operationalized as non-system ImageLoaded paths in image_load question"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "parent_process is primary cue; the .lst-driven mechanism lives on setup16's parent command line, so cue_parent_process.mechanism_decode (Q3) is the more specific decode. cue_user_actor pairs kept as non-deduplicable."
      kept: cue_parent_process.mechanism_decode
    - dropped: cue_process_tool.baseline
      reason: "host-runs-this-tool baseline subsumed by cue_parent_process.baseline (Q8), which scopes the parent-child pairing rather than the binary alone — more specific for a parent-driven rule"
      kept: cue_parent_process.baseline
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q13/Q14)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q15/Q16)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain (acted on)"
    - template: universal.endpoint_protection_health
      reason: "recommended, not must_include; dropped under 18-question cap. Rule is not a credential-access/payload-execution-on-keyboard rule where tamper-as-precursor is the deciding question; cue_host_context.precursor (Q17) already surfaces prior tamper alerts on the host"
    - template: organizational_context.change_ticket
      reason: "recommended; dropped under 18-question cap to preserve the four non-deduplicable user_actor questions and the four cross-logsource attack chain questions"
    - template: organizational_context.human_recall
      reason: "recommended; dropped under 18-question cap. Legitimacy-from-human-recall is partially covered by host_user_context (Q2)"
    - template: organizational_context.timing_alignment
      reason: "recommended; dropped under 18-question cap"
    - template: organizational_context.procedural_consistency
      reason: "recommended; dropped under 18-question cap. setup16 has no standard organizational deployment procedure to compare against"
    - template: persistence_scope.file_based_persistence
      reason: "recommended; dropped under 18-question cap. File-drop persistence partially covered by the .lst file-staging question (Q5) which already pivots to file_event"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - persistence_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 14
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 14
      file_event: 1
      image_load: 1
      create_stream_hash: 1
      sigma_alert: 1
