name: Scheduled Task Creation Masquerading as System Processes
id: 1aa8d583-7865-4b82-a199-a0e2993eaefe
description: |
  Detects creation of a scheduled task via schtasks.exe (by image name or PE OriginalFileName) whose command line contains
  the /create switch together with the name of a core Windows system process (svchost, lsass, services, winlogon, explorer,
  rundll32, and similar). Naming a task or its run-as target after a trusted system process is a masquerading technique used
  to blend persistence into the normal task inventory; the same pattern also appears in legitimate administration when a task
  is built to wrap or restart a system component. The investigation should decode the task's full configuration (name,
  trigger, run-as account, and the command it executes), confirm whether the task is still registered, and determine whether
  the activity was authorized.
type: detection
detection_id: 9f8573c9-22b4-40e3-89c1-72bc2b8d49ab
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Does the schtasks command line contain /create together with a system-process name this rule matches?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when schtasks.exe
    (by image name or PE OriginalFileName) runs with the /create switch AND the command line references a core system-process
    name such as svchost, lsass, services, winlogon, explorer, rundll32, or msiexec. Read the exact command line and verify
    both conditions are present rather than an incidental substring match.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to create scheduled tasks that wrap or restart system processes?
  context: Legitimate administrative activity can trigger this detection. Knowing whether the user role and host function are
    consistent with hands-on scheduled-task creation referencing system components is often the fastest path to distinguishing
    routine maintenance from masquerading persistence. Direct command-line schtasks use on a user workstation by a non-admin
    is more notable than the same on a managed server during a maintenance window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:scheduled_task.task_configuration
  question: What scheduled task was created, and what command does it execute? Identify the task name, run-as account, and trigger.
  context: The schtasks command line carries the full task definition. Identify the task name (/TN), the command or program
    to run (/TR), the schedule type (/SC, such as ONLOGON, MINUTE, or DAILY), and the run-as account (/RU). A task running as
    SYSTEM or at logon/startup is higher-risk. The /TR value is the persistence payload -- often a script, binary, or a
    download cradle wrapped in cmd or powershell. Note whether the task name itself imitates a system process.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:obfuscation.encoded_payload_decode
  question: Is the task's command (/TR) encoded or obfuscated, and what does the decoded content do?
  context: 'Operators frequently hide the payload inside the /TR value. Common encodings: base64 (powershell -EncodedCommand,
    certutil -decode), hex, character concatenation (set-variable chains assembled at runtime), environment-variable
    substitution, and caret or backtick escaping. Decoding reveals the real intent -- download cradle, reverse shell,
    in-memory loader, or credential access. Iterate decoding for multi-layer payloads until the command is readable.'
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:masquerading.true_identity
  question: What is the actual identity of the schtasks binary based on its metadata and hash?
  context: The rule matches either the image name or the PE OriginalFileName, so a renamed copy of schtasks.exe also fires.
    Compare the Image path on disk against the embedded OriginalFileName and confirm the binary is the Microsoft-signed
    System32 schtasks.exe rather than a renamed or relocated copy. The file hash enables a reputation lookup to confirm it is
    the genuine tool and not a tampered build.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:lolbin.renamed_binary_check
  question: Was schtasks.exe executed from its expected System32 path?
  context: The legitimate scheduler client lives in C:\Windows\System32 (and SysWOW64). A schtasks.exe running from a user
    temp directory, AppData, or another non-standard location suggests the binary was copied or relocated to evade path-based
    allowlisting, which combined with the system-process masquerade raises the likelihood of deliberate evasion.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:cue_process_tool.process_chain
  question: What process spawned this schtasks invocation, and is that parent chain expected?
  context: The parent process reveals how the task creation was driven. Interactive administration typically descends from
    explorer.exe or a console shell; scripted deployment descends from a management agent or installer. Unusual parents such
    as mshta.exe, wscript.exe, an Office application, or a browser child point to exploitation or a malicious script rather
    than routine administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1053.005.task_registration_event
  question: Does the Security log show a task-registration event for this task, and what user and task content does it record?
  context: Task creation through schtasks.exe, the COM Task Scheduler API, or PowerShell all converge on a task-registration
    event in the Security log (event ID 4698) that records the registering account and the serialized task definition,
    including the action and trigger. This is an independent record of the same task and surfaces a creation path that did not
    go through a visible schtasks command line. If Security task-creation auditing is not enabled in this environment, treat
    zero results as a visibility gap rather than evidence the task was not registered.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: security
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4698
      condition: selection
    fields:
      - TaskName
      - SubjectUserName
      - TaskContent
- source: template:scheduled_task.task_trigger_and_timing
  question: When is the task scheduled to run, and has it already executed via the Task Scheduler service?
  context: The trigger schedule determines when the persistence activates. Tasks set for boot or logon fire on the next
    restart; recurring tasks (every few minutes, daily) may have already run multiple times. Scheduled tasks execute as
    children of the Task Scheduler service hosted in svchost.exe, so processes spawned by that service after task creation
    indicate the payload has already run.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\svchost.exe'
        ParentCommandLine|contains: 'Schedule'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:scheduled_task.task_still_present
  question: Is the scheduled task still registered on the system?
  context: Persistence is only effective if the task still exists. Some operators delete a task after execution to reduce
    forensic artifacts. Confirming the task is still present -- via schtasks /query /tn <taskname> /v or the Task Scheduler
    interface on the host -- determines whether the persistence mechanism is active and requires remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|windash: ' /create '
        CommandLine|contains:
        - ' audiodg'
        - ' conhost'
        - ' dwm.exe'
        - ' explorer'
        - ' lsass'
        - ' lsm'
        - ' mmc'
        - ' msiexec'
        - ' regsvr32'
        - ' rundll32'
        - ' services'
        - ' spoolsv'
        - ' svchost'
        - ' taskeng'
        - ' taskhost'
        - ' wininit'
        - ' winlogon'
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|windash: ' /create '
        CommandLine|contains:
        - ' audiodg'
        - ' conhost'
        - ' dwm.exe'
        - ' explorer'
        - ' lsass'
        - ' lsm'
        - ' mmc'
        - ' msiexec'
        - ' regsvr32'
        - ' rundll32'
        - ' services'
        - ' spoolsv'
        - ' svchost'
        - ' taskeng'
        - ' taskhost'
        - ' wininit'
        - ' winlogon'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated activity across endpoints. If
    the same user created similar tasks or ran related tooling on multiple hosts, the scope of the activity is broader than a
    single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|windash: ' /create '
        CommandLine|contains:
        - ' audiodg'
        - ' conhost'
        - ' dwm.exe'
        - ' explorer'
        - ' lsass'
        - ' lsm'
        - ' mmc'
        - ' msiexec'
        - ' regsvr32'
        - ' rundll32'
        - ' services'
        - ' spoolsv'
        - ' svchost'
        - ' taskeng'
        - ' taskhost'
        - ' wininit'
        - ' winlogon'
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: Even routine schtasks use can be abused. The query mirrors the rule's selection structure scoped to this host, so
    the resulting parent and user distribution reflects how this specific masquerade-task pattern has historically run here.
    A new parent process, an unexpected user account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|windash: ' /create '
        CommandLine|contains:
        - ' audiodg'
        - ' conhost'
        - ' dwm.exe'
        - ' explorer'
        - ' lsass'
        - ' lsm'
        - ' mmc'
        - ' msiexec'
        - ' regsvr32'
        - ' rundll32'
        - ' services'
        - ' spoolsv'
        - ' svchost'
        - ' taskeng'
        - ' taskhost'
        - ' wininit'
        - ' winlogon'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    masquerade-task pattern rather than generic schtasks usage. Many hosts firing the same pattern points to scripted
    deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use the host
    distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|windash: ' /create '
        CommandLine|contains:
        - ' audiodg'
        - ' conhost'
        - ' dwm.exe'
        - ' explorer'
        - ' lsass'
        - ' lsm'
        - ' mmc'
        - ' msiexec'
        - ' regsvr32'
        - ' rundll32'
        - ' services'
        - ' spoolsv'
        - ' svchost'
        - ' taskeng'
        - ' taskhost'
        - ' wininit'
        - ' winlogon'
      condition: all of selection_*
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host around the same time?
  context: Operators rarely rely on a single persistence mechanism. After establishing one foothold they commonly install
    backups using different techniques -- additional scheduled tasks, services, registry Run keys, BITS jobs, or WMI
    subscriptions. Multiple persistence installations in the same timeframe indicate intentional stacking rather than an
    isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or
    credential access -- may reveal the stages that preceded the task creation and help reconstruct the timeline. Defense
    tampering and credential access often precede the installation of persistence.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1053.005
      name: Scheduled Task
      role: primary
    - id: T1036.004
      name: Masquerade Task or Service
      role: secondary
    - id: T1036.005
      name: Match Legitimate Resource Name or Location
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: task_creation_config
      description: "Actor creates a scheduled task via schtasks.exe /create whose name/run-as masquerades as a system process"
      mapped_to: Q3
      source: template:scheduled_task.task_configuration
    - stage: 2
      label: task_registration_record
      description: "Task creation lands as a Security task-registration event (4698) recording author and serialized definition"
      mapped_to: Q8
      source: attack:T1053.005.task_registration_event
    - stage: 3
      label: task_execution
      description: "Task fires from the Task Scheduler service (svchost.exe -s Schedule), executing the payload"
      mapped_to: Q9
      source: template:scheduled_task.task_trigger_and_timing
    - stage: 4
      label: masquerade_verification
      description: "Verify the binary's true identity and that the task name/target masquerades as a legitimate system process"
      mapped_to: Q5
      source: template:masquerading.true_identity
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1053.005
      covered_by: Q4
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1053.005
      covered_by: Q9
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1053.005
      covered_by: Q3
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: user_context
      technique: T1053.005
      covered_by: Q8
      source: attack:T1053.005.task_registration_event
      coverage_type: direct
    - dimension: command_analysis
      technique: T1053
      covered_by: Q3
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: execution_context
      technique: T1036.004
      covered_by: Q9
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Correlation window between task creation and execution; expressed as the +4h scheduler-child range, no literal threshold to inject"
    - element: UserContext
      dimension: user_context
      specific_values: "SYSTEM / non-standard accounts (rationale)"
      disposition: context
      target_question: Q8
      notes: "Registering account surfaced via SubjectUserName in the 4698 query; SYSTEM/anomalous-account framing in Q3 and Q8 context"
    - element: TaskNamePattern
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Obfuscated/imitating task names called out in task_configuration context (note whether /TN imitates a system process)"
    - element: CommandLineEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Encoding/obfuscation of the /TR payload handled by the encoded_payload_decode question; no numeric entropy threshold is queryable"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Suspicious binary/script usage in the task command surfaced by decoding /TR"
    - element: ExecutionContext
      dimension: execution_context
      specific_values: "SYSTEM / service vs user (rationale)"
      disposition: context
      target_question: Q9
      notes: "Whether execution came from the scheduler service; run-as account read from /RU in task_configuration"
    - element: OriginalFilenameMismatch
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Image vs OriginalFileName comparison is the core of the true_identity question"
  dedup_decisions:
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable (user-pivot baseline kept alongside parent+binary substitute)"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable (user-pivot clustering kept alongside parent+binary substitute)"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: scheduled_task.task_configuration (more specific Phase 2 decode for schtasks)"
    - template: cue_process_tool.baseline
      reason: "Superseded by cue_process_tool.baseline_deviation (REFLECTED host_pattern) which counts the rule's specific masquerade-task pattern rather than generic schtasks usage on the host"
    - template: cue_process_tool.follow_on
      reason: "Subsumed by scheduled_task.task_trigger_and_timing, which is the more specific follow-on (scheduler-service children) for a task-creation rule"
    - template: cue_process_tool.clustering
      reason: "Host-wide tool clustering overlaps cue_host_context.precursor (prior alerts) and persistence_scope (persistence tooling); dropped to stay within question cap"
    - template: cue_host_context.clustering
      reason: "Generic host activity clustering overlaps cue_host_context.precursor and the persistence-scope sweep; dropped to stay within question cap"
    - template: universal.endpoint_protection_health
      reason: "Recommended generic tamper sweep; this is a persistence-creation rule, not an execution/credential-access rule, and cue_host_context.precursor already surfaces prior tamper alerts on the host. Dropped per its own RECOMMENDED guidance to stay within the 18-question cap."
    - template: organizational_context.change_ticket
      reason: "Coverage of authorization/legitimacy is carried by host_user_context and the baseline questions; dropped to stay within cap. Rule is technique-focused (not malware-specific), so a change-ticket check would apply but is the lowest-yield organizational question here."
    - template: organizational_context.human_recall
      reason: "Redundant with host_user_context for the legitimacy determination; dropped to stay within cap"
    - template: organizational_context.timing_alignment
      reason: "Timing legitimacy partially carried by task_trigger_and_timing and baseline_deviation; dropped to stay within cap"
    - template: organizational_context.procedural_consistency
      reason: "Overlaps host_user_context (whether direct schtasks use fits normal procedure); dropped to stay within cap"
    - template: persistence_scope.file_based_persistence
      reason: "file_event Startup/Run-key drop is a distinct parallel mechanism but lower-yield for a schtasks-specific masquerade rule; process_based_persistence covers the in-scope tooling. Dropped to stay within cap."
    - template: masquerading.binary_origin
      reason: "file_event basename lookup targets dropped/renamed payloads; schtasks.exe here is the genuine system tool by name, so origin-tracing adds little. true_identity + lolbin.renamed_binary_check cover the masquerade dimension."
    - template: masquerading.renamed_binary_activity
      reason: "Renamed-binary child-activity/scope angle overlaps cue_process_tool.scope (fleet pattern) and task_trigger_and_timing (what executed); dropped to stay within cap"
    - template: lolbin.technique_class_lateral
      reason: "Other-LOLBins-for-same-objective is a download/proxy-exec angle; this rule's objective is persistence via schtasks, already covered by persistence_scope.process_based_persistence which sweeps the persistence LOLBin class. Dropped to avoid duplicate persistence-tool sweep."
    - template: lolbin.historical_lolbin_baseline
      reason: "REFLECTED host_pattern query is identical in selection to cue_process_tool.baseline_deviation (same rule detection block, same host scope); baseline_deviation is kept (projects ParentImage+User). Dropping avoids a near-duplicate aggregation."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1053.005) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        This rule's detection is the schtasks.exe /create command line; the
        task's registration and serialized definition are fully captured by the
        process_creation command-line decode (task_configuration) and the
        Security 4698 task-registration question. registry_set (Tasks tree under
        the registry / TaskCache keys) would surface the same task by a parallel
        artifact without adding an investigative angle the command line and 4698
        record do not already provide. No compensating registry_set question
        authored.
    - hint: "logsource 'security' (recommended by techniques: T1053.005,T1036.004) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: acted_on
      reason: >-
        Authored attack:T1053.005.task_registration_event (Q8) with
        category: security querying EventID 4698 (task created), projecting
        TaskName, SubjectUserName, and TaskContent — covers the security
        logsource and the user_context dimension via the registering account.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - scheduled_task
    - obfuscation
    - persistence_scope
    - masquerading
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      security: 1
      sigma_alert: 1
