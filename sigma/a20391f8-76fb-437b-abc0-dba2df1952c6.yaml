name: Visual Studio NodejsTools PressAnyKey Arbitrary Binary Execution
id: ab95398f-a39a-440b-aabd-c65078681c0f
description: |
  Detects any child process spawned by Microsoft.NodejsTools.PressAnyKey.exe, a Microsoft-signed
  utility shipped with the Visual Studio Node.js tooling. The binary takes a status keyword, a PID
  file path, and a path to an executable, then starts that third argument directly via the process
  API and writes its own PID to the second argument's path — so it can launch any local or
  file-share-hosted binary as a child while inheriting the trust of a signed Microsoft process.
  Legitimately it runs only as part of Visual Studio Node.js debugging on developer machines; outside
  that context an unexpected child indicates trusted-binary proxy execution (T1218). Determine what
  child was launched, where it came from, whether the PressAnyKey parent itself was legitimately
  invoked, and whether the child went on to load unsigned modules or open network connections.
type: detection
detection_id: a20391f8-76fb-437b-abc0-dba2df1952c6
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Was the parent of the alerting process Microsoft.NodejsTools.PressAnyKey.exe?
  context: Confirm the alert is a true positive by checking that the parent image of the triggering
    event ends in Microsoft.NodejsTools.PressAnyKey.exe. The alerting process is the child the LOLBIN
    was directed to execute; note its Image and command line from the alert, as that child drives the
    rest of this investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to run Visual Studio Node.js debugging tooling?
  context: PressAnyKey.exe spawning a child is routine only on developer machines actively running
    Node.js debugging under Visual Studio. On a server, a non-developer endpoint, or under a service
    account, the parent should not be present at all, which raises the likelihood of trusted-binary
    proxy execution over legitimate use.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly is the child binary the LOLBIN launched, and what does its command line do?
  context: The third argument PressAnyKey.exe receives is passed straight to the process-start API,
    so the child Image and its full command line define the actual action taken. Decode any flags,
    encoded content, file paths, or URLs in the child's command line, and note whether the executable
    path points to a local install directory, a user-writable location, or a remote file share.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: template:cue_parent_process.mechanism_decode
  question: What invoked Microsoft.NodejsTools.PressAnyKey.exe itself, and with what arguments?
  context: The parent's own process event shows how the LOLBIN was reached. In legitimate use the
    grandparent is the Visual Studio IDE (devenv.exe) or a Node.js debug launcher. An interactive
    shell, a script host, an Office application, or a remote-execution service as the grandparent
    indicates the binary was repurposed for proxy execution rather than invoked by the IDE.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1218.command_analysis
  question: Does the child command line contain a remote path, URL, or download/execute pattern?
  context: Proxy execution through a signed binary is most often used to run a payload pulled from a
    file share or remote host. This sweep flags children on this host whose command line references a
    UNC share, an HTTP or HTTPS URL, or a path under a user-writable directory — arguments that
    indicate the proxied binary or its input came from outside the local trusted install.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      selection_remote:
        CommandLine|contains:
          - '\\\\'
          - 'http://'
          - 'https://'
          - '\Users\'
          - '\Temp\'
          - '\AppData\'
          - '\ProgramData\'
      condition: selection_host and selection_remote
    fields:
      - Image
      - CommandLine
      - User
- source: attack:T1218.image_load
  question: Did the child process load any unsigned or unusual modules after it started?
  context: A binary launched through the LOLBIN may side-load attacker-supplied libraries to run code
    while the on-disk executable looks benign. Reviewing modules loaded by the child reveals DLLs
    pulled from user-writable or temporary locations, which are inconsistent with a binary running
    only its own signed dependencies. If image-load telemetry is not collected in this environment,
    treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - CommandLine
- source: attack:T1218.network_connection
  question: Did the child process open any network connections after it started?
  context: The reference behavior describes executing a binary located on a remote file share, and a
    proxied payload commonly retrieves further stages or beacons outbound. Network connections
    originating from the child binary — especially to external addresses or non-standard ports —
    indicate the proxied execution had a network objective beyond local-only activity.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_parent_process.follow_on
  question: What other child processes did Microsoft.NodejsTools.PressAnyKey.exe spawn on this host?
  context: An operator using the LOLBIN as an execution launcher may drive several stages through it
    in sequence. Listing every child of the PressAnyKey parent on this host reveals whether the
    alerting child was a one-off or one step in a chain of proxied executions.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.clustering
  question: What other process activity surrounds the PressAnyKey parent on this host in the alert window?
  context: Beyond its direct children, activity near the parent helps place the execution in context —
    what ran just before to set it up and what ran just after. Reconnaissance, credential-access, or
    persistence tooling clustered around the same window strengthens a malicious reading.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has Microsoft.NodejsTools.PressAnyKey.exe spawned this child binary before on this host?
  context: Routine Visual Studio debugging spawns a predictable, narrow set of children (the program
    under debug, node.exe). A child binary this parent has never launched on this host is a novel
    spawning relationship and is more likely to be proxied execution than developer activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is this parent-child spawning pattern occurring on other hosts?
  context: The same PressAnyKey parent launching the same child across multiple hosts points to either
    a fleet-wide developer toolset or a repeated technique deployed across endpoints. A single
    occurrence is more consistent with targeted or one-off use.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run Visual Studio Node.js debugging tooling?
  context: Compare the acting user against their history. A developer who routinely debugs Node.js
    projects under Visual Studio is an expected source of this parent; a user with no development
    role who has never produced this activity is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this PressAnyKey-parent and child-binary combination historically routine on this host or
    across the environment?
  context: When the alert fires under a service principal, user identity does not attribute behavior;
    the meaningful pivot is the launch mechanism. A parent-and-child pair that already accounts for
    many prior events on this host is developer or vendor tooling, while a new or rare pair is the
    deviation to investigate.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around the time of the alert?
  context: Tracking the acting user across all hosts in the alert window can reveal lateral movement
    or activity on additional endpoints. The same user driving similar execution on multiple hosts
    widens the apparent scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same PressAnyKey-parent and child-binary combination fire on other hosts in the same window?
  context: Under a service principal, cross-host user activity is dominated by agent telemetry, so the
    sharper question is whether the same launch mechanism appeared elsewhere at the same time. The
    same parent-and-child pair on several hosts in the window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, or credential
    access — may reveal the stages that preceded this execution and help reconstruct the timeline that
    led to the trusted-binary proxy run.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_binary_invocation
      description: "Microsoft-signed PressAnyKey.exe is invoked with a status keyword, PID file, and target executable path"
      mapped_to: Q4
      source: template:cue_parent_process.mechanism_decode
    - stage: 2
      label: proxied_child_execution
      description: "PressAnyKey passes its third argument to the process-start API, launching an arbitrary binary as a child"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_objective
      description: "The proxied child loads modules and/or contacts a remote host or file share to carry out its objective"
      mapped_to: Q6
      source: attack:T1218.image_load
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q5
      source: attack:T1218.command_analysis
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_parent_process.mechanism_decode
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q10
      source: template:cue_parent_process.baseline
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "Microsoft.NodejsTools.PressAnyKey.exe (parent is the detection field)"
      disposition: query_injection
      target_question: Q4
      notes: "Parent grandparent chain decoded via ParentProcessGuid lookup; baseline/scope questions pivot on ParentName"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (rule targets a specific signed binary, not the generic msiexec/regsvr32 list)"
      disposition: context
      target_question: Q2
      notes: "Rule already fixes the signed binary to PressAnyKey.exe; legitimate-context framing carries the allowlist rationale"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URL / UNC share / user-writable path patterns"
      disposition: query_injection
      target_question: Q5
      notes: "Injected as CommandLine|contains URL, UNC, and user-writable path tokens into the remote-path sweep"
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (no enterprise allowlist available at generation time)"
      disposition: context
      target_question: Q7
      notes: "Network-connection question context directs analyst to distinguish external/non-standard from known-good endpoints"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue — its mechanism_decode and follow_on subsume process_chain; hint 1"
      kept: cue_parent_process.mechanism_decode
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "cue_parent_process.baseline is more specific (parent+child pair) and covers host-frequency for this rule"
    - template: cue_host_context.clustering
      reason: "cue_parent_process.clustering already provides parent-scoped host activity in the same window; whole-host aggregation would duplicate without adding precision"
    - template: universal.endpoint_protection_health
      reason: "rule fires on the proxy-execution launch itself, not on a hands-on-keyboard impact action; cue_host_context.precursor already surfaces prior tamper/recon alerts on the host, and no defense_tampering set is confirmed to require the generic sweep"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 13
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 13
      image_load: 1
      network_connection: 1
      sigma_alert: 1
