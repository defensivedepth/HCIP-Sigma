name: Raccine Uninstall
id: 82eef155-b250-4708-9ab1-baa975526757
description: |
  Detects command lines that remove Raccine, a free anti-ransomware tool that registers itself as a debugger for shadow-copy
  and recovery binaries and kills malicious parent process trees. The rule fires when EITHER a taskkill targeting
  RaccineSettings.exe runs, OR a reg delete removes the "Raccine Tray" Run key, OR a schtasks /DELETE removes the
  "Raccine Rules Updater" scheduled task. Legitimate context is an administrator decommissioning the tool; the malicious
  context is an operator stripping ransomware protection before deploying impact, since removing Raccine restores the
  attacker's ability to delete shadow copies and disable recovery. Determine whether the removal was authorized, what else
  the same user or host did around the removal, and whether shadow-copy or recovery tampering followed.
type: detection
detection_id: a31eeaed-3fd5-478e-a8ba-e62c6b3f9ecc
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match one of the Raccine removal actions this rule detects?
  context: Confirm the alert is a true positive before investing time. The rule fires on EITHER a taskkill of
    RaccineSettings.exe, OR a reg delete of the "Raccine Tray" Run key, OR a schtasks /DELETE of the "Raccine Rules
    Updater" task. Read the exact command line and note which of the three branches fired — the branch determines which
    downstream questions carry weight, since a Run-key delete and a task delete touch different persistence surfaces.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to uninstall or remove anti-ransomware tooling?
  context: Removing a security control is a routine administrative action on a managed workstation but an unexpected one on
    a server or a user endpoint where the tool was centrally deployed. Knowing whether the user role and host function are
    consistent with decommissioning Raccine is often the fastest way to separate authorized cleanup from operator activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Which Raccine component was removed — the running tray process, the Run-key autostart, or the update task?
  context: Each removal branch strips a different part of the tool. Killing RaccineSettings.exe stops the running tray
    process; deleting the "Raccine Tray" Run key prevents it from relaunching at logon; deleting the "Raccine Rules
    Updater" task halts rule updates. Read the command line to identify which component was targeted, since a full
    uninstall touches all three and a partial removal may indicate a targeted disable of just the protection that blocks
    the operator's next step.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is Raccine still installed and enforcing, or has it been fully removed from this host?
  context: A single command may be one step of a multi-command uninstall. Check whether the Raccine debugger registry
    entries, the tray process, and the update task are still present. If Raccine is fully removed, shadow-copy deletion and
    recovery-disable commands that it previously intercepted will now run unimpeded, so the protection state directly
    affects how urgently recovery artifacts must be verified.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after Raccine was removed?
  context: Removing anti-ransomware protection is preparatory — it restores the ability to delete shadow copies, disable
    recovery, or run encoded PowerShell that Raccine would otherwise have killed. Examining process creation in the window
    immediately after the removal reveals what the removal was meant to enable. Watch specifically for vssadmin, wmic,
    bcdedit, wbadmin, or diskshadow invocations touching shadow storage or recovery state.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense-tampering actions performed on this host in the same window?
  context: An operator who removes one control often removes several. Beyond the Raccine removal, look for log clearing,
    shadow-copy deletion, recovery-disable, firewall changes, or service stops from the same user or parent process. The
    deployment-specific tuning of which service and process names count as security-critical varies per environment, so
    treat any termination of AV, EDR, or backup-related processes in this window as part of the same operation.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned the removal command, and is that parent chain expected?
  context: The parent reveals how the removal was invoked. An interactive shell launched from explorer.exe under an
    administrator is consistent with manual decommissioning; a parent such as a remote-execution service, a scripting host,
    or an unexpected automation chain points to scripted or remote operator activity. Walk up from the parent to see what
    launched it and what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this Raccine-removal pattern, what parents and users drove it, and does the
    current alert fit?
  context: The query mirrors the rule's three removal branches scoped to this host, so the resulting parent/user
    distribution reflects how the removal pattern specifically has run here — not generic taskkill, reg, or schtasks usage.
    A removal driven by a new parent process, an unexpected user account, or an unusual command shape is the deviation
    worth chasing; a recurring administrative pattern points to routine maintenance.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection1:
        CommandLine|contains|all:
        - 'taskkill '
        - RaccineSettings.exe
      selection2:
        CommandLine|contains|all:
        - reg.exe
        - delete
        - Raccine Tray
      selection3:
        CommandLine|contains|all:
        - schtasks
        - /DELETE
        - Raccine Rules Updater
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: attack:T1562.001.registry_set
  question: Were the Raccine debugger registry entries or the Run-key autostart modified directly through registry edits?
  context: Raccine installs itself through Image File Execution Options "Debugger" values that redirect shadow-copy and
    recovery binaries to raccine.exe, plus a "Raccine Tray" Run key for the tray process. A complete removal deletes those
    registry entries; an operator may edit the registry directly rather than going through the documented uninstall, which
    would not always surface in process command lines. Checking registry-set events for changes to the Image File Execution
    Options debugger paths and the Run key confirms whether the protection mechanism itself was dismantled.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_paths:
        TargetObject|contains:
          - '\Image File Execution Options\'
          - '\CurrentVersion\Run\Raccine'
      condition: selection_host and selection_paths
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_user_actor.baseline
  question: Does this user normally run anti-ransomware uninstall or security-tool removal commands?
  context: Compare this user's current action against their historical behavior. A user who has never removed a security
    tool is more notable than an administrator who routinely decommissions software as part of their role. User role and
    typical toolset should align with removing endpoint protection.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-binary launch mechanism historically routine on this host or across the environment, or is
    the parent unusual for this binary?
  context: When the alert fires under a service principal such as SYSTEM, user identity does not attribute the behavior; the
    meaningful pivot is which parent process spawned this binary and whether that pair is historically common. A new or rare
    parent for this binary is the deviation worth chasing, whereas a pair that already accounts for many prior events is
    automation or OS-driven activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around the time of the removal?
  context: Tracking the user's activity across all hosts reveals whether the removal was an isolated administrative action
    or one step in broader activity. If the same user triggered similar removals or tampering on multiple hosts, the scope
    is larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-binary combination fire on other hosts in the same window — a coordinated rollout, or
    activity isolated to this host?
  context: Under a service principal, asking what the user did across the network returns agent-telemetry noise because every
    host runs SYSTEM-owned processes constantly. The meaningful question is whether the same launch mechanism — parent
    process plus spawned binary — fired on other hosts in the alert window. Multiple hosts point to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Aggregating process activity around the removal reveals what led to it and what
    followed — reconnaissance, privilege escalation, or staging that provides broader context. Novel parent-child pairs
    standing out at low counts against high-count routine noise are what to scan for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before the removal?
  context: Earlier detections on the same host — reconnaissance, credential access, or other defense tampering — may reveal
    the attack stages that preceded the removal and help reconstruct the timeline. A removal of anti-ransomware protection
    that follows credential access or lateral movement reads very differently from one that stands alone.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this Raccine-removal pattern in the past week, and does the distribution look like
    automation or one-off activity?
  context: The query mirrors the rule's three removal branches with no host scope, so the result is a fleet-wide count of
    the removal pattern rather than generic binary usage. Many hosts firing the same pattern points to a scripted
    deployment or an administrator decommissioning the tool fleet-wide; one host or a small unrelated set points to manual
    operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection1:
        CommandLine|contains|all:
        - 'taskkill '
        - RaccineSettings.exe
      selection2:
        CommandLine|contains|all:
        - reg.exe
        - delete
        - Raccine Tray
      selection3:
        CommandLine|contains|all:
        - schtasks
        - /DELETE
        - Raccine Rules Updater
      condition: 1 of selection*
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed or removed on this host around the same time?
  context: Operators who strip a protection's scheduled task and Run key often touch other persistence surfaces in the same
    operation — additional scheduled tasks, services, registry Run keys, or BITS jobs. Multiple persistence changes from the
    same timeframe indicate intentional staging rather than an isolated, authorized uninstall.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: control_removal
      description: "Actor removes the anti-ransomware tool (taskkill of tray process, reg delete of Run key, or schtasks delete of update task)"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: protection_state
      description: "Targeted control is left disabled / fully removed (abnormal absence of expected protection telemetry)"
      mapped_to: Q4
      source: template:defense_tampering.protection_status
    - stage: 3
      label: enabled_activity
      description: "Post-removal activity the tampering was meant to enable (shadow-copy / recovery tampering, payload execution)"
      mapped_to: Q5
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q5
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q6
      source: template:defense_tampering.other_tampering_activity
      coverage_type: indirect
    - dimension: user_context
      technique: T1562.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1562
      covered_by: Q14
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562
      covered_by: Q6
      source: template:defense_tampering.other_tampering_activity
      coverage_type: indirect
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Tuning rationale (expected admin tools to suppress) with no queryable values; folded into other_tampering_activity context about deployment-specific service/process name tuning."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Correlation-window rationale; expressed as the +1h post-removal window in activity_after_tampering and the +/-1h window in other_tampering_activity."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Customizable security service name list; no fixed values in brief. other_tampering_activity already enumerates a service/process tamper pattern set."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Exclude authorized admin tools; rationale only. process_chain question establishes whether the launching process is an authorized admin context."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Refine which services count as security-critical; no fixed values. Covered by other_tampering_activity context."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by defense_tampering.what_was_disabled (more specific; identifies the targeted control)"
      kept: defense_tampering.what_was_disabled
    - dropped: cue_process_tool.baseline
      reason: "generic Image-baseline of a single binary is weaker than the rule-pattern-scoped reflected baseline_deviation, which mirrors all three removal branches on this host"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "post-tool child-process question is subsumed by defense_tampering.activity_after_tampering (post-removal activity) and persistence_scope.process_based_persistence (persistence tooling)"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_process_tool.clustering
      reason: "host-wide concurrent-tool aggregation duplicates cue_host_context.clustering, which uses the same host-scoped +/-15m aggregation"
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject identity vs. launch-mechanism identity)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (user network-wide activity vs. parent+binary cross-host rollout)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed for this rule; its what_was_disabled / protection_status / activity_after_tampering / other_tampering_activity questions are more specific than the generic host-tamper sweep and supersede it (per template notes_to_llm drop condition a)."
    - template: persistence_scope.file_based_persistence
      reason: "recommended (not must_include). Dropped to keep the question set within the 18-cap after emitting both mandatory user_actor pairs; the rule's persistence surface is the Run key + scheduled task, both command-line driven and already covered by what_was_disabled, the registry_set question, and process_based_persistence. Startup-folder/file drops are not part of this rule's behavior."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: medium
        reason: "Keyword match on 'ransomware' is incidental to the rule's behavior. The rule detects REMOVAL of an anti-ransomware tool, not ransomware execution itself; no encryption/ransom-note/shadow-deletion impact behavior is in the detection. The downstream impact risk (shadow-copy/recovery tampering) is already surfaced by defense_tampering.activity_after_tampering. Adding ransomware_impact questions would investigate a stage the rule does not detect."
      - id: network_scope
        confidence: medium
        reason: "Same incidental 'ransomware' keyword match. The rule is a local process_creation removal pattern with no network indicator; cue_user_actor.clustering already provides cross-host scope. A network_scope (network_connection) question would have no rule-derived pivot."
  technique_sets_applied:
    - universal
    - defense_tampering
    - persistence_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      sigma_alert: 1
