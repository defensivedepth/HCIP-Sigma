name: Invoke-Obfuscation Via Use MSHTA
id: 43d83ffc-5b79-4b61-88fd-9f463ee85140
description: |
  Detects a command line that builds and runs an obfuscated mshta one-liner of
  the form produced by the Invoke-Obfuscation MSHTA transform: it uses set to
  stage environment variables, chains with &&, then invokes mshta with a
  vbscript:CreateObject(...).Run(...) payload that calls (window.close) to dismiss
  the dialog. The rule fires only when set, &&, mshta, vbscript:createobject, .run,
  and (window.close) all appear in the same command line, so the trigger is a
  single self-contained launcher rather than a bare mshta call. The construction
  uses mshta as a script-proxy host to run inline VBScript, which in turn typically
  spawns an obfuscated PowerShell payload while the closing window hides the
  activity from the user. Legitimate use of this exact pattern is rare; the
  investigation should decode the staged command and the proxied script, recover
  any payload it launches or drops, and establish whether the user, host, and
  parent chain are consistent with sanctioned automation.
type: detection
detection_id: ac20ae82-8758-4f38-958e-b44a3140ca88
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line stage variables with set, chain with &&, and invoke
    mshta with a vbscript:createobject().run() payload that closes the window?
  context: Before investing investigation time, confirm the alert is a true positive
    by reviewing the exact command line. The rule fires only when set, &&, mshta,
    vbscript:createobject, .run, and (window.close) all appear together, so verify
    every element is present rather than a bare mshta invocation. The set and &&
    sequence stages variables that the mshta payload later expands, and the
    (window.close) call dismisses the dialog so the activity runs without a visible
    window.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to launch mshta with an inline VBScript
    payload from a command line?
  context: mshta running an inline vbscript:createobject payload assembled from
    staged variables is not a pattern that legitimate administration produces, and
    most accounts and hosts never invoke mshta this way. Knowing whether the acting
    account and the host function involve any sanctioned automation that drives mshta
    is often the fastest way to separate a rare legitimate launcher from operator
    activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this obfuscated mshta launcher part of an authorized penetration test
    or red team engagement?
  context: Offensive tooling, including the Invoke-Obfuscation framework that
    produces this exact mshta one-liner, is used in authorized security assessments.
    Before investing investigation time, check whether a penetration test or red team
    exercise is currently active on this host or network segment. An authorized
    engagement with matching tooling and timing resolves the alert without further
    analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What does this command actually do once the staged variables are
    expanded into the mshta payload?
  context: Decode the full command line. Resolve every set assignment and substitute
    the variables into the mshta argument to reconstruct the real vbscript:createobject
    payload that ran, since the obfuscation transform deliberately splits the payload
    across variables to defeat string matching. Identify which object the script
    creates (commonly WScript.Shell), what the .Run call executes, and whether that
    inner command is itself an encoded or obfuscated PowerShell invocation. The
    query scopes to this host's mshta launcher activity in the surrounding window so
    sequential staging commands are visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
        - mshta
        - vbscript:createobject
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: What command does the mshta VBScript proxy run, and is that inner payload
    encoded or obfuscated?
  context: The vbscript:createobject(...).Run() call is a proxy that launches a
    second command, frequently an obfuscated or base64-encoded PowerShell invocation
    carrying the actual payload such as a download cradle, a reverse shell, or
    in-memory loading of a .NET assembly. Extract the string passed to .Run, then
    decode any -EncodedCommand or -enc argument, reverse any backtick escaping,
    string concatenation, or variable substitution, and identify the source URL and
    what gets executed. The window-close call exists to hide this proxied execution
    from the user.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1059.001.image_load
  question: Did mshta load a scripting-engine module such as vbscript.dll or
    scrrun.dll around the time of the alert?
  context: When mshta evaluates the inline vbscript:createobject payload it loads a
    script engine into its own process, leaving no separate cscript.exe or
    wscript.exe in the tree. A module load of vbscript.dll, scrrun.dll, or jscript.dll
    by mshta.exe is direct evidence that the binary executed script inline rather
    than rendering an HTML application. If image-load events are not collected in
    this environment, treat zero results as a visibility gap rather than evidence no
    script ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \mshta.exe
        ImageLoaded|endswith:
          - \vbscript.dll
          - \scrrun.dll
          - \jscript.dll
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - ProcessGuid
- source: template:cue_process_tool.process_chain
  question: What process spawned this command, and is that parent chain expected?
  context: The parent process reveals how the obfuscated launcher was invoked. A
    command-shell one-liner driven by explorer.exe during interactive use differs
    sharply from one launched by an Office application, a script host such as
    wscript.exe or cscript.exe, or a freshly written executable, which are common
    precursors to scripted execution. Walk up the tree to see what spawned the
    parent and what else it spawned before treating an unusual parent as the anomaly.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this launcher spawn afterward?
  context: The processes spawned after the launcher reveal its actual impact. The
    mshta proxy commonly spawns mshta.exe and then powershell.exe or another
    interpreter carrying the real payload, and a successful operation may continue
    into reconnaissance, credential access, or a follow-on loader. Scope to the same
    host and the alerting process to see the execution that followed the obfuscated
    command.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated content of
    the proxied payload?
  context: When the mshta VBScript proxy launches PowerShell, Windows Event ID 4104
    records the deobfuscated script content that PowerShell actually executed, even
    when the launching command line is staged and obfuscated. This provides ground
    truth about what ran, bypassing the variable-splitting and string concatenation
    the transform applied. If PowerShell operational logs are not ingested in this
    environment, treat the absence of results as a visibility gap rather than
    evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:universal.evidence_preservation
  question: Did the proxied script write a payload to disk, and is that file still
    available for analysis?
  context: The .Run call frequently drops or retrieves a payload before executing it,
    landing in user-writable staging locations such as the user profile Temp folder,
    C:\Users\Public, or ProgramData. Checking whether such a file still exists
    enables deeper analysis through hash lookups, sandbox submission, or content
    review. If file events are not collected here, fall back to any output or path
    named in the decoded mshta payload.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this obfuscated mshta pattern, what
    parents and users normally drove it, and does the current alert fit that pattern?
  context: Even when a pattern recurs on a host, the parent and user distribution
    behind it is what distinguishes routine from anomalous. This query mirrors the
    rule's own selection scoped to this host, so the resulting parent and user
    distribution reflects how this specific firing pattern has run here historically.
    A new parent process, an unexpected user account, or an unusual command-line
    pattern relative to that history is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - set
        - '&&'
        - mshta
        - vbscript:createobject
        - .run
        - (window.close)
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this obfuscated mshta pattern in the past week, and
    does the cross-host distribution look like consistent automation or one-off
    activity?
  context: This query mirrors the rule's selection with no host scope, returning a
    fleet-wide count of the firing pattern rather than generic mshta usage. Many
    hosts firing the same staged-and-obfuscated pattern points to a scripted
    deployment; one host or a small unrelated set points to manual operator activity.
    Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - set
        - '&&'
        - mshta
        - vbscript:createobject
        - .run
        - (window.close)
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - set
        - '&&'
        - mshta
        - vbscript:createobject
        - .run
        - (window.close)
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - set
        - '&&'
        - mshta
        - vbscript:createobject
        - .run
        - (window.close)
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of the
    alert?
  context: Tracking the user's activity across all hosts reveals lateral movement or
    activity spanning multiple endpoints. If the same user drove similar activity on
    more than one host in this window, the scope of the operation may be larger than
    a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - set
        - '&&'
        - mshta
        - vbscript:createobject
        - .run
        - (window.close)
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert
    reveals what led to this event, what happened after, and whether it is part of a
    larger sequence. Look for reconnaissance, privilege escalation, persistence, or
    data staging surrounding the obfuscated launcher. Novel parent-child pairs at low
    counts are what to scan for against the high-count noise of routine system
    processes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host -- defense tampering, reconnaissance,
    credential access -- may reveal the stages that preceded this event and help
    reconstruct the timeline. An obfuscated proxy launcher that follows earlier
    suspicious activity reads very differently from one that appears in isolation.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1027
      name: Obfuscated Files or Information
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: staged_obfuscated_launcher
      description: "A command line stages variables with set, chains with &&, and invokes mshta with a vbscript:createobject(...).Run() payload that closes the window -- the obfuscated launcher the rule fires on."
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: inline_script_proxy
      description: "mshta evaluates the inline VBScript by loading a script engine into its own process, proxying execution without a separate cscript/wscript host."
      mapped_to: Q6
      source: attack:T1059.001.image_load
    - stage: 3
      label: proxied_payload_execution
      description: "The VBScript .Run call launches the real payload, frequently an obfuscated or encoded PowerShell invocation, and may write a file to disk."
      mapped_to: Q5
      source: template:powershell.encoded_command_decode
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q4
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q7
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q17
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q9
      source: template:powershell.script_block_logging
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q5
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1027
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rationale only (regex for encoded/obfuscated/hidden PowerShell arguments, e.g. -enc, -nop)"
      disposition: context
      target_question: Q5
      notes: "No concrete literals beyond rationale; realized as the decode-the-proxied-payload guidance (extract the .Run string, decode -EncodedCommand/-enc, reverse backtick/concat/variable obfuscation) in encoded_command_decode."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "abnormal parents like Office, mshta spawning PowerShell (rationale)"
      disposition: context
      target_question: Q7
      notes: "process_chain names Office apps and script hosts (wscript/cscript) as common precursors; baseline_svc/clustering_svc pivot on ParentImage to test whether the launcher's parent is routine."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "off-hours / non-maintenance correlation window (rationale)"
      disposition: context
      target_question: Q17
      notes: "Realized as the +/-15m host-clustering scope and the +/-5m image_load / follow_on windows correlating the launcher with the script-engine load and child execution."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "vbscript.dll, scrrun.dll, jscript.dll (script-engine modules for the inline VBScript host)"
      disposition: query_injection
      target_question: Q6
      notes: "Concrete script-engine module names injected into an image_load query (ImageLoaded|endswith) scoped to mshta.exe on the host -- evidence of inline VBScript evaluation. T1059.001 analytic frames LoadedModuleList around .NET assemblies; for this mshta-proxied rule the load-bearing modules are the VBScript engine DLLs the proxy loads."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (Event ID 4104 script-block length filter)"
      disposition: context
      target_question: Q9
      notes: "Tunable 4104 length threshold; realized as the script_block_logging question that recovers the deobfuscated proxied PowerShell content (ground truth) when PS operational logs are ingested."
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (entropy threshold separating obfuscation from legitimate compression)"
      disposition: context
      target_question: Q5
      notes: "T1027 entropy-tuning rationale; not directly queryable on a command-line-only rule. Realized as the decode-and-deobfuscate task in encoded_command_decode and the variable-reconstruction task in mechanism_decode."
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "rationale only (LOLBins / admin tools misused for obfuscation, env-specific)"
      disposition: context
      target_question: Q7
      notes: "T1027 parent-list tuning rationale; realized as the process_chain parent-expectation judgment and the ParentImage-pivot baseline/clustering questions."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "Generic binary-frequency baseline (host.name + Image|endswith) is uninformative for a CommandLine-only rule with no Image in the detection -- Image resolves to the shell host (cmd.exe), which is universally present. Superseded by cue_process_tool.baseline_deviation (REFLECTED), which counts the rule's actual obfuscated-mshta firing pattern on this host."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "Near-identical to cue_host_context.clustering -- both aggregate process_creation on host.name in a +/-15m window. host_context version groups by Image+ParentImage (richer parent-child surface) and is retained."
      kept: cue_host_context.clustering
    - dropped: null
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair -- non-deduplicable (subject identity vs launch-mechanism identity); both emitted per service_principals.yaml default."
    - dropped: null
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair -- non-deduplicable; both emitted per service_principals.yaml default."
    - dropped: hacking_tool.tool_capability_used
      reason: "Re-displays the triggering event asking which tool capability was invoked. For this rule the rule fires on ONE specific obfuscation transform, so 'which capability' collapses onto the mechanism_decode/encoded_command_decode decode of the single staged payload -- no distinct subcommand surface to enumerate."
      kept: cue_process_tool.mechanism_decode + powershell.encoded_command_decode
    - dropped: hacking_tool.tool_specific_artifacts
      reason: "file_event sweep scoped by Image|endswith %Image%; for a CommandLine-only rule %Image% resolves to the shell host (cmd.exe), making the artifact search broad and low-precision. The payload-drop angle is covered more precisely by universal.evidence_preservation (%file_paths% scope) and the file/network follow-up after the proxied execution."
      kept: universal.evidence_preservation
  skipped_templates:
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by: powershell.encoded_command_decode (hint 1) -- the proxied .Run payload is a PowerShell invocation, so the PowerShell-specific decode is more precise than the generic obfuscation decode."
    - template: powershell.powershell_security_bypass
      reason: "must_include in the powershell set, dropped under the 18-question cap. It is a host-scoped -1h sweep for AV/EDR and AMSI/ExecutionPolicy bypass strings. For this rule the load-bearing PowerShell coverage is decoding the obfuscated proxied payload (encoded_command_decode) and recovering its deobfuscated content (script_block_logging, EID 4104); cue_host_context.precursor already surfaces prior tamper alerts on the host. No bypass string is part of this rule's detection, so the generic bypass sweep is the lowest-value PowerShell slot here."
  reflection_overrides: []
  hint_dispositions: []
  # All four brief hints were acted on (no declined entries):
  #  1. "powershell.encoded_command_decode replaces obfuscation.encoded_payload_decode" ->
  #     obfuscation.encoded_payload_decode skipped (skipped_templates); Q5 is encoded_command_decode.
  #  2. "CommandLine-only detection -- use CommandLine pattern matching for scoping, not Image" ->
  #     mechanism_decode (Q4) scopes on CommandLine|contains|all [mshta, vbscript:createobject] + host;
  #     baseline/clustering user pivots (Q13,Q15,Q16... actually Q14,Q16) use CommandLine|contains
  #     'vbscript:createobject' or ParentImage rather than %Image%; baseline_deviation/scope (Q11,Q12)
  #     reflect the full CommandLine|contains|all selection.
  #  3. "File paths derived from CommandLine -- use %file_paths%" -> evidence_preservation (Q10)
  #     scopes on TargetFilename|endswith %file_paths%.
  #  4. "logsource image_load (T1059.001) not covered" -> Q6 authored as attack:T1059.001.image_load
  #     (mshta loading vbscript.dll/scrrun.dll/jscript.dll -- the inline-script-proxy stage).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - hacking_tool
    - obfuscation
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 14
      image_load: 1
      ps_script: 1
      file_event: 1
      sigma_alert: 1
