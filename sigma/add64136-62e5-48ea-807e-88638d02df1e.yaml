name: Fsutil Suspicious Invocation
id: e4e6acc1-bdb8-45a0-b146-0f09f729f094
description: |
  Detects fsutil.exe invoked with USN change journal verbs (deletejournal, createjournal,
  setZeroData) that alter or destroy the NTFS volume-wide record of file creations, deletions,
  and modifications. The rule fires when the process is fsutil.exe (by image name or original
  file name) AND the command line contains one of those verbs. Deleting or shrinking the USN
  journal is an anti-forensics step that wipes the change history DFIR relies on to reconstruct
  file activity, and is commonly chained with recovery destruction during impact operations; the
  same verbs also have legitimate administrative and backup-tooling uses. Determine which verb ran,
  against which volume, who launched it, and whether file deletion, recovery inhibition, or other
  tampering surrounds it.
type: detection
detection_id: add64136-62e5-48ea-807e-88638d02df1e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show fsutil run with deletejournal, createjournal, or setZeroData?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires
    only when the process is fsutil.exe (by image name or original file name) and the command line
    contains deletejournal, createjournal, or setZeroData. Confirm both the binary identity and the
    exact verb, and note the target volume.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run fsutil against the USN change journal?
  context: Legitimate administrative activity, backup tooling, and storage maintenance scripts can
    invoke these verbs. Knowing whether the user role and host function involve volume or journal
    maintenance is often the fastest path to distinguishing routine operations from anti-forensics.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Which USN journal action was taken, and against which volume?
  context: The three verbs differ in impact. deletejournal disables and erases the volume-wide change
    history (anti-forensics). createjournal can resize the journal in place, and a small max size
    effectively truncates history. setZeroData zeroes file data ranges, destroying file content.
    Decode the full command line to identify the verb, the target volume path, and any size or range
    parameters, since these determine investigation priority and what evidence may already be gone.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned fsutil and is that parent chain expected?
  context: The parent process reveals how fsutil was invoked. Storage-maintenance and backup workflows
    have predictable parents; an interactive shell, a script host, or an unexpected application parent
    points to hands-on-keyboard or scripted operation rather than scheduled maintenance.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this fsutil pattern, what parents and users normally
    drive it, and does the current alert fit?
  context: Even routine tooling can be abused. This query mirrors the rule's selection scoped to this
    host, so the parent and user distribution reflects how this specific firing pattern has historically
    run here rather than generic fsutil usage. A new parent, an unexpected account, or a first-time
    occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \fsutil.exe
      - OriginalFileName: fsutil.exe
      selection_cli:
        CommandLine|contains:
        - deletejournal
        - createjournal
        - setZeroData
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: attack:T1070.file_path_analysis
  question: What file deletion activity occurred on this host in the window around the journal change?
  context: Deleting or shrinking the USN journal destroys the volume-wide record of file deletions
    that investigation relies on. Capturing file-delete events from the surrounding window, before
    the journal change takes hold, helps reconstruct what files were removed and which process and
    user removed them. Sparse or absent results after the journal action may itself reflect the lost
    visibility the action was meant to create.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: file_delete
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: attack:T1070.registry_indicator_removal
  question: Were registry keys associated with execution history or persistence deleted on this host
    around the same time?
  context: Indicator removal often pairs file-system history destruction with deletion of registry
    artifacts that record execution or maintain footholds. Registry value and key deletions in the
    surrounding window may show the same operator clearing additional evidence alongside the journal
    action.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventType: DeleteValue
      condition: selection
    fields:
      - TargetObject
      - Image
      - User
- source: attack:T1070.log_clearing
  question: Were Windows event logs cleared on this host around the time of the journal change?
  context: Wiping the file-system change journal and clearing the Windows event logs are parallel
    indicator-removal steps that frequently occur together. A security log cleared event (1102) in the
    surrounding window indicates broader evidence destruction and that authentication and audit history
    may be gone. If security event collection is not in place in this environment, treat zero results
    as a visibility gap rather than evidence nothing happened.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        EventID: 1102
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - host.name
      - SubjectUserName
      - EventID
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the USN journal was altered?
  context: Operators destroy file-system history for a reason, to hide what follows or what already
    ran. Examining process activity immediately after the journal change reveals what the action was
    meant to conceal or enable. Because the change destroys later deletion evidence, correlate process
    creation in this window with surviving file and network events.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host?
  context: An operator who wipes the file-system change journal often disables or clears several
    controls in the same operation, stopping protection services, clearing logs, and deleting recovery
    data to maximize coverage. Finding multiple tampering events from the same user or parent process
    indicates a coordinated indicator-removal step rather than isolated maintenance.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:ransomware_impact.recovery_options_assessment
  question: What recovery-inhibiting commands were executed on this host?
  context: 'Journal destruction frequently sits alongside recovery inhibition in impact operations:
    deleting shadow copies (vssadmin, wmic), disabling boot recovery (bcdedit), deleting backups
    (wbadmin), and disabling the Windows Recovery Environment (reagentc). This query surfaces recovery-inhibiting
    commands on the host, revealing the full scope of recovery destruction. Verify whether each action
    succeeded by checking system state (vssadmin list shadows, bcdedit /enum, reagentc /info).'
  range: +24h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recovery:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\reagentc.exe'
          - '\diskshadow.exe'
      selection_cmdline:
        CommandLine|contains:
          - 'delete shadows'
          - 'shadowcopy delete'
          - 'recoveryenabled'
          - 'bootstatuspolicy'
          - 'delete catalog'
          - 'delete systemstatebackup'
          - 'boot.ini'
          - 'winre.wim'
          - '/disable'
      condition: selection and (selection_recovery or selection_cmdline)
    fields:
      - Image
      - CommandLine
- source: template:ransomware_impact.destruction_scope_on_host
  question: What other destructive or recovery-inhibiting actions occurred on this host?
  context: 'Impact operations typically chain multiple destructive actions: deleting shadow copies,
    disabling recovery, stopping security services, and clearing event logs. Finding the complete set
    of destructive command-line tools used on the host distinguishes a broader destruction sequence
    from an isolated journal-maintenance action.'
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_destructive:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\cipher.exe'
          - '\fsutil.exe'
          - '\icacls.exe'
          - '\takeown.exe'
          - '\attrib.exe'
          - '\sc.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\powershell.exe'
          - '\taskkill.exe'
      condition: selection and selection_destructive
    fields:
      - Image
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run fsutil journal operations?
  context: Compare this user's current action against their historical pattern. A user who has never
    run fsutil is more suspicious than one who does so regularly as part of storage maintenance. User
    role and typical toolset should align with volume and journal operations.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and fsutil combination historically routine on this host or across the
    environment, or is the parent unusual for fsutil?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: which parent spawned fsutil, and is that pair
    historically common. A new or rare parent for fsutil on this host or environment-wide is the
    deviation worth chasing; a pair that already accounts for many prior events is vendor or OS
    automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the
    operation may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and fsutil combination fire on other hosts in the same window, or is
    the activity isolated to this host?
  context: Under a service principal, tracking user activity network-wide returns agent-telemetry noise.
    The meaningful question is whether the same launch mechanism, parent process plus fsutil, fired on
    other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Indicator removal rarely happens in isolation. Earlier detections on the same host, such as
    execution, credential access, or recovery inhibition, may reveal the stages that preceded the
    journal change and help reconstruct the full timeline before the surviving evidence thins out.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this fsutil pattern in the past week, and does the cross-host
    distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection with no host scope, so the result is a fleet-wide
    count of this firing pattern rather than generic fsutil usage. Many hosts firing the same pattern
    points to scripted deployment or vendor automation; one host or a small unrelated set points to
    manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \fsutil.exe
      - OriginalFileName: fsutil.exe
      selection_cli:
        CommandLine|contains:
        - deletejournal
        - createjournal
        - setZeroData
      condition: all of selection_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1070
      name: Indicator Removal
      role: primary
    - id: T1485
      name: Data Destruction
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: indicator_removal_invocation
      description: Adversary spawns fsutil with a USN journal verb to delete or shrink the volume-wide
        file-system change history (anti-forensics indicator removal).
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: surrounding_evidence_destruction
      description: Deletion/modification of logs, registry keys, or file-deletion records correlated
        with the journal change.
      mapped_to: Q8
      source: attack:T1070.log_clearing
    - stage: 3
      label: destructive_overwrite_chain
      description: Command-line tools recursively delete or overwrite files and inhibit recovery,
        correlated with the journal change (T1485).
      mapped_to: Q12
      source: template:ransomware_impact.destruction_scope_on_host
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1070
      covered_by: Q6
      source: attack:T1070.file_path_analysis
      coverage_type: direct
    - dimension: time_correlation
      technique: T1070
      covered_by: Q9
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1070
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: user_context
      technique: T1070
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1485
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: network_indicators
      technique: T1485
      covered_by: Q18
      source: template:cue_process_tool.scope
      coverage_type: indirect
    - dimension: time_correlation
      technique: T1485
      covered_by: Q12
      source: template:ransomware_impact.destruction_scope_on_host
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1485
      covered_by: Q11
      source: template:ransomware_impact.recovery_options_assessment
      coverage_type: direct
  mutable_dispositions:
    - element: TargetFilePathPattern
      dimension: tuning_parameter
      specific_values: "rationale only (log file paths / common registry hives)"
      disposition: new_question
      target_question: Q6
      notes: "T1070 path-pattern tuning realized as a file_delete sweep (Q6) plus a registry_set
        delete sweep (Q7) on the host window, since the rule's own selection carries no path values."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlate removal within X mins of setup activity)"
      disposition: context
      target_question: Q9
      notes: "Realized as forward and surrounding windows in activity_after_tampering (+1h) and the
        evidence-destruction sweeps (+/-1h)."
    - element: TargetFilename
      dimension: tuning_parameter
      specific_values: "rationale only (sensitive locations e.g. %System32%, Documents, DB paths)"
      disposition: context
      target_question: Q6
      notes: "No literal path values supplied; file_delete sweep surfaces TargetFilename for the
        analyst to assess against sensitive locations."
    - element: ProcessCommandLine
      dimension: tuning_parameter
      specific_values: "/w, /q, /s overwrite flags"
      disposition: context
      target_question: Q12
      notes: "Overwrite-flag rationale noted in destruction_scope context; the rule's own verbs
        (deletejournal/createjournal/setZeroData) are the queryable values and are enforced in Q1/Q5/Q18."
    - element: VolumeThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (unique deletions within time window)"
      disposition: context
      target_question: Q6
      notes: "Threshold is not directly queryable; the file_delete sweep returns the deletion set for
        the analyst to assess volume."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by defense_tampering.what_was_disabled — more specific Phase 2 for tampering"
      kept: defense_tampering.what_was_disabled
    - dropped: cue_process_tool.baseline
      reason: "superseded by cue_process_tool.baseline_deviation, which scopes to the rule's firing
        pattern on this host rather than generic fsutil usage"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; emitted BOTH (Q14, Q15)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; emitted BOTH (Q16, Q17)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "fsutil USN-journal deletion does not spawn children as its operation; child-process
        coverage of the host window is provided by defense_tampering.activity_after_tampering (Q9)"
    - template: cue_process_tool.clustering
      reason: "broad same-host tool clustering is covered by destruction_scope_on_host (Q12) and
        other_tampering_activity (Q10) with destructive-tool filters; an unfiltered clustering sweep
        adds noise without new signal"
    - template: cue_host_context.clustering
      reason: "same-host surrounding activity is covered by activity_after_tampering (Q9) and the
        destructive/tamper sweeps (Q10, Q12)"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled and
        other_tampering_activity questions (Q3, Q10) are more specific and supersede the generic
        host-tamper sweep"
    - template: ransomware_impact.network_wide_impact
      reason: "cross-host rule.uuid spread is covered by cue_process_tool.scope (Q18), which counts
        the same firing pattern fleet-wide"
    - template: ransomware_impact.encryption_or_ransom_indicators
      reason: "rule detects anti-forensics journal destruction, not encryption; encryption-phase
        hunting is out of the immediate disposition path and would push past the 18-question cap"
    - template: network_scope.same_action_other_hosts
      reason: "cross-host same-action coverage provided by cue_process_tool.scope (Q18) and
        cue_user_actor.clustering_svc (Q17)"
    - template: network_scope.actor_activity_network_wide
      reason: "user network-wide activity covered by cue_user_actor.clustering (Q16)"
    - template: network_scope.actor_authentication_trail
      reason: "no credential-access or lateral-movement signal in this anti-forensics rule; authentication
        trail is tangential and would displace higher-value indicator-removal coverage under the cap"
    - template: persistence_scope.process_based_persistence
      reason: "see hint_dispositions — persistence_scope cascade declined; USN journal deletion is
        anti-forensics, not foothold installation"
    - template: persistence_scope.file_based_persistence
      reason: "see hint_dispositions — persistence_scope cascade declined; USN journal deletion is
        anti-forensics, not foothold installation"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1070) is not covered — pull in
        cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted-on
      reason: "Authored Q7 (attack:T1070.registry_indicator_removal) with category: registry_set,
        EventType: DeleteValue, scoped to the host window — surfaces registry artifact deletion that
        often accompanies file-system history destruction."
    - hint: "logsource 'security' (recommended by techniques: T1070) is not covered and no template
        family exists — author a custom question with `category: security`"
      disposition: acted-on
      reason: "Authored Q8 (attack:T1070.log_clearing) with service: security, EventID 1102 (log
        cleared), scoped to the host window — surfaces event-log clearing that parallels journal
        destruction."
    - hint: "logsource 'file_delete' (recommended by techniques: T1070,T1485) is not covered — pull
        in cue_artifact.* or author a custom question with `category: file_delete`"
      disposition: acted-on
      reason: "Authored Q6 (attack:T1070.file_path_analysis) with category: file_delete, scoped to the
        host window — recovers file-deletion records the journal change destroys."
    - hint: "defense_tampering.what_was_disabled replaces cue_process_tool.mechanism_decode"
      disposition: acted-on
      reason: "Used defense_tampering.what_was_disabled as the Phase 2 question (Q3); mechanism_decode
        dropped — recorded in dedup_decisions."
    - hint: "defense_tampering → persistence_scope cascade activated"
      disposition: declined
      reason: "The detected action is USN change-journal deletion/resize, an anti-forensics indicator-removal
        step with no foothold-installation component. The persistence_scope questions (process-based
        and file-based persistence sweeps) investigate scheduled tasks, services, Run keys, and Startup
        drops, which are not implied by journal destruction. Coverage of surrounding malicious activity
        is provided by activity_after_tampering (Q9), other_tampering_activity (Q10), and the
        destruction sweeps (Q11, Q12). No compensating persistence question authored."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - ransomware_impact
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 15
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      file_delete: 1
      registry_set: 1
      security: 1
      sigma_alert: 1
