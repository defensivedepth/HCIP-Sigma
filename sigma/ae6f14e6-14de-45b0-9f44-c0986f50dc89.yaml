name: Change Default File Association To Executable Via Assoc
id: 68403871-04c1-48b1-aef7-ddd46e3a2a77
description: |
  Detects cmd.exe running the built-in assoc utility with a command line that contains both 'assoc ' and exefile, while not
  matching the benign '.exe=exefile' restore pattern. Changing a file extension's default handler to exefile (or any
  executable handler) causes an arbitrary program to launch whenever a file of that extension is opened, which establishes
  a user- or system-triggered persistence and privilege-escalation foothold. Legitimate use is rare and almost always an
  administrator repairing a broken association; investigate which extension was remapped, what handler it now points to,
  and whether any process execution followed a subsequent file open.
type: detection
detection_id: ae6f14e6-14de-45b0-9f44-c0986f50dc89
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-08'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show assoc being used to set a file extension's handler to an executable type?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when cmd.exe runs a command
    line containing both 'assoc ' and exefile and the command is not the benign '.exe=exefile' restore. Read the full command
    line and confirm which extension is being remapped and to which handler.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to remap file extension handlers from the command line?
  context: Legitimate administrative activity can trigger this detection. Direct command-line use of assoc to change a default
    handler is unusual outside of break/fix work. Knowing whether the user role and host function involve repairing file
    associations is often the fastest path to distinguishing a true positive from routine maintenance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: Is this parent expected to spawn cmd.exe running assoc, and what started the parent?
  context: The parent-child relationship is a core signal. Interactive administrative use typically has explorer.exe or a
    console host in the chain. A parent such as a script host, an Office application, or a remote-execution service spawning
    cmd.exe to remap a handler points to automated or hands-off-keyboard activity. Tracing the parent's own origin reveals
    whether the execution chain is interactive or scripted.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:parent_child.sibling_processes
  question: What other child processes did the same parent spawn around this time?
  context: If a suspicious parent spawned the cmd.exe that ran assoc, it likely spawned other processes too. Examining sibling
    processes reveals the full scope of activity from that parent; multiple children performing reconnaissance, file drops,
    or additional configuration changes from the same parent suggests orchestration rather than an isolated administrative
    command. host.name is included as a fallback because the parent identifier may be unavailable.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this assoc command do — which extension was remapped and to what handler?
  context: The full command line reveals the specific change. Decode which file extension is on the left of the equals sign
    and which handler type is on the right; a remap to exefile or another executable handler means files of that extension
    will launch a program when opened. This query scopes to assoc executions on this host within the window, so a multi-step
    sequence (for example, querying the current association, then changing it) is visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
          - 'assoc '
          - 'exefile'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1546.001.registry_handler_modification
  question: Was the file association registry key for the remapped extension changed on this host around the same time?
  context: assoc writes the new default handler into the registry under the file extension and file-type keys. Correlating
    the command-line execution with the registry modification confirms the association actually changed and shows the final
    handler path, including any change made directly to the registry without using assoc. A handler value pointing at an
    interpreter or a user-writable path is the anomaly worth chasing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_assoc:
        TargetObject|contains:
          - '\exefile\'
          - '\shell\open\command'
      condition: selection and selection_assoc
    fields:
      - TargetObject
      - Details
      - Image
- source: attack:T1546.001.handler_triggered_execution
  question: After the association changed, did opening a file of the remapped extension launch an unexpected program?
  context: The payoff of this technique is execution when a user or process opens a file of the remapped extension. Look for
    process executions on this host after the change whose parent is explorer.exe or a document or mail handler, and whose
    image is an interpreter (powershell.exe, wscript.exe, cscript.exe, mshta.exe, rundll32.exe) or an unsigned binary in a
    user-writable path. That chain is the triggered execution this association enables.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith:
          - '\explorer.exe'
      filter_interpreters:
        Image|endswith:
          - '\powershell.exe'
          - '\wscript.exe'
          - '\cscript.exe'
          - '\mshta.exe'
          - '\rundll32.exe'
          - '\cmd.exe'
      condition: selection and filter_interpreters
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_process_tool.follow_on
  question: What child processes did this cmd.exe instance spawn afterward?
  context: The processes spawned by the alerting cmd.exe reveal its actual impact beyond the association change itself.
    Follow-on execution of interpreters, download utilities, or additional configuration tools points to a broader operation
    rather than a single one-off command.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this assoc-to-executable pattern, what parents and users normally drive it,
    and does the current alert fit?
  context: Even routine repair work can match this rule. The query mirrors the rule's selection scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent
    process, an unexpected user account, or a first-time appearance of the pattern is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cli:
        CommandLine|contains|all:
        - 'assoc '
        - exefile
      filter:
        CommandLine|contains: .exe=exefile
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not filter) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this assoc-to-executable pattern in the past week, and does the distribution look like
    automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    firing pattern rather than generic cmd.exe usage. Many hosts firing the same pattern points to scripted deployment or
    vendor automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to
    calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cli:
        CommandLine|contains|all:
        - 'assoc '
        - exefile
      filter:
        CommandLine|contains: .exe=exefile
      condition: all of selection_* and not filter
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cli:
        CommandLine|contains|all:
        - 'assoc '
        - exefile
      filter:
        CommandLine|contains: .exe=exefile
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_* and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cli:
        CommandLine|contains|all:
        - 'assoc '
        - exefile
      filter:
        CommandLine|contains: .exe=exefile
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_* and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user triggered similar configuration changes or tool execution on multiple hosts, the scope of the activity
    may be larger than a single association change.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \cmd.exe
      - OriginalFileName: Cmd.Exe
      selection_cli:
        CommandLine|contains|all:
        - 'assoc '
        - exefile
      filter:
        CommandLine|contains: .exe=exefile
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_* and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led
    to this event, what happened after, and whether the association change is part of a larger sequence. Novel parent-child
    pairs at low counts stand out against high-count agent telemetry.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, or credential access — may reveal the
    attack stages that preceded the association change and help reconstruct the full timeline. Persistence is usually
    established after an initial foothold, so prior alerts often carry the explanatory context.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After establishing a file-association foothold, they
    commonly install backup mechanisms using different techniques — scheduled tasks, services, registry Run keys, BITS jobs,
    or WMI subscriptions. Multiple persistence installations from the same timeframe indicate intentional stacking rather
    than isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for this file association change?
  context: Many organizations require change tickets for system modifications, especially persistence-relevant changes such
    as remapping a default file handler. A corresponding ticket suggests authorized break/fix work; absence of one when
    policy requires it suggests the change was unauthorized. This check can quickly close a false positive or escalate a
    true positive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1546.001
      name: Change Default File Association
      role: primary
    - id: T1546
      name: Event Triggered Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: handler_modification
      description: "Actor modifies the registry file-association handler for an extension (here via cmd.exe assoc to exefile)"
      mapped_to: Q5
      source: template:cue_process_tool.mechanism_decode
    - stage: 1b
      label: registry_confirmation
      description: "Registry key for the extension/handler is written — confirm the change landed and capture the final handler path"
      mapped_to: Q6
      source: attack:T1546.001.registry_handler_modification
    - stage: 2
      label: triggered_execution
      description: "Opening a file of the remapped extension launches the new handler / an anomalous child process"
      mapped_to: Q7
      source: attack:T1546.001.handler_triggered_execution
  dimension_coverage:
    - dimension: time_correlation
      technique: T1546.001
      covered_by: Q6
      source: attack:T1546.001.registry_handler_modification
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1546.001
      covered_by: Q6
      source: attack:T1546.001.registry_handler_modification
      coverage_type: direct
    - dimension: user_context
      technique: T1546.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "within ~5 minutes of the registry modification"
      disposition: query_injection
      target_question: Q6
      notes: "registry_set correlation question runs +/-5m so the registry write can be tied to the assoc execution"
    - element: SuspiciousHandlerPathRegex
      dimension: tuning_parameter
      specific_values: "exefile, shell\\open\\command, interpreter images (powershell/wscript/cscript/mshta/rundll32/cmd)"
      disposition: query_injection
      target_question: Q6
      notes: "TargetObject|contains for exefile and shell\\open\\command in Q6; interpreter Image list in Q7 triggered-execution query"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — ignore known admin/installer profiles"
      disposition: context
      target_question: Q2
      notes: "user_context covered by host_user_context (Q2) and user-actor baseline (Q11); no specific queryable value"
    - element: PathAnomalyThreshold
      dimension: tuning_parameter
      specific_values: "rationale only — path-deviation scoring (T1546 secondary)"
      disposition: context
      target_question: Q7
      notes: "T1546 secondary analytic; expressed as user-writable / interpreter image anomaly in the triggered-execution query rather than a numeric score"
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_child technique set active — parent_process_legitimacy (Q3) and sibling_processes (Q4) subsume the cue process_chain"
      kept: parent_child.parent_process_legitimacy
    - dropped: cue_process_tool.baseline
      reason: "cue_user_actor.baseline (Q11) covers host+binary historical frequency by user, and baseline_deviation (Q9) gives the rule-pattern-scoped host history; a third generic host+binary count adds no distinct signal"
      kept: cue_user_actor.baseline
    - dropped: cue_user_actor pair (baseline/baseline_svc, clustering/clustering_svc)
      reason: "user_actor pair — non-deduplicable; both pivot variants emitted per AGENTS.md (Q11/Q12 and Q13/Q14)"
      kept: both variants
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This rule fires on a persistence configuration change, not payload execution or credential access; tamper-as-precursor is not the disposition driver. cue_host_context.precursor (Q16) already surfaces any prior tamper alerts on the host. Dropped to stay within the question budget without diluting persistence-focused coverage."
    - template: persistence_scope.file_based_persistence
      reason: "RECOMMENDED. The triggered-execution payload of this technique is registry-handler-driven, not a Startup-folder/Run-key file drop; the registry-set correlation (Q6) and triggered-execution (Q7) questions already cover the relevant artifact path. Kept process_based_persistence (Q17) for the parallel-mechanism stacking signal."
    - template: organizational_context.human_recall
      reason: "RECOMMENDED. host_user_context (Q2) and change_ticket (Q18) already establish whether the actor/action is expected; a third organizational-recall question over the budget would dilute the technique-specific registry/execution coverage."
    - template: organizational_context.timing_alignment
      reason: "RECOMMENDED. Timing legitimacy is partially covered by change_ticket (Q18, maintenance window) and the baseline_deviation host-pattern history (Q9); a standalone work-hours question adds marginal signal relative to the budget."
    - template: organizational_context.procedural_consistency
      reason: "RECOMMENDED. Whether command-line assoc is the org's normal way to change associations is implicit in host_user_context (Q2) and baseline (Q11); a separate procedural question would overlap without adding a distinct query."
  reflection_overrides: []
  hint_dispositions:
    - hint: "cmd.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: acted_on
      reason: >-
        mechanism_decode (Q5) was adapted from host.name+Image scoping to
        host.name + CommandLine|contains|all ['assoc ', 'exefile'], so the
        query returns only the assoc-to-executable invocations rather than all
        cmd.exe activity on the host.
    - hint: "logsource 'registry_set' (recommended by techniques: T1546.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: acted_on
      reason: >-
        Authored attack:T1546.001.registry_handler_modification (Q6) with
        category: registry_set, matching the registry write that assoc performs
        on the extension/handler keys (TargetObject|contains exefile and
        shell\open\command). This is the chain stage-1b confirmation.
    - hint: "logsource 'security' (recommended by techniques: T1546.001) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The Security-log analytic for T1546.001 centers on EventCode 4672
        (special privileges assigned at logon). Changing a file association via
        assoc already requires administrator privileges per the utility's own
        documentation, so a 4672 elevated-token question does not separate true
        from false positives here — the action is administrative by definition.
        The privilege/attribution question is instead covered by host_user_context
        (Q2) and the parent_process_legitimacy chain (Q3), and the registry write
        itself is confirmed by the registry_set question (Q6). No compensating
        attack:T1546.001.security question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - organizational_context
    - persistence_scope
    - parent_child
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      registry_set: 1
      sigma_alert: 1
