name: Writing Of Malicious Files To The Fonts Folder
id: 72d94347-274b-4c31-9f4e-4ab962f65fd4
description: |
  Detects command-line activity that writes an executable or script file into C:\Windows\Fonts\.
  The rule fires when a command line combines a file-writing verb (echo, copy, type, file createnew, or cacls)
  AND the C:\Windows\Fonts\ path AND an executable or script extension. The Fonts folder is writable and
  executable without administrator privileges, so it is occasionally abused to stage payloads in a directory
  that normally holds only legitimate system files; investigation should determine what file was written,
  what it is, and whether the staging is part of a broader foothold.
type: detection
detection_id: ae9b0bd7-8888-4606-b444-0ed7410cb728
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line write an executable or script into C:\Windows\Fonts\ using a file-writing
    verb?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires only when
    all three conditions hold together — a file-writing verb (echo, copy, type, file createnew, or cacls),
    the C:\Windows\Fonts\ path, and an executable or script extension (.exe, .dll, .bat, .ps, .vbs, and
    similar). Read the command to confirm the target file and the operation performed against it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to write executable files into the Windows Fonts directory?
  context: Legitimate software installation, font management, and administrative scripting rarely place
    executables or scripts directly into C:\Windows\Fonts\. Knowing whether the user role and host function
    are consistent with writing files into a system directory is often the fastest path to distinguishing a
    true positive from routine operations.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this command do? Decode the full command line, the target file path, and the
    operation performed.
  context: The full command line reveals the specific action taken. Identify which verb wrote the file (echo
    appends content, copy/type move existing data, file createnew creates an empty file, cacls/icacls adjusts
    permissions), the exact destination filename and extension under C:\Windows\Fonts\, and any source the
    content was drawn from. A permissions-modification verb against a path suggests the file already exists
    and is being locked down rather than freshly written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_artifact.mechanism_decode
  question: What is the file that was written into the Fonts folder, and what does its content reveal?
  context: Retrieve and analyze the file referenced in the command line. For scripts and text files, check
    for base64 encoding, obfuscated strings, embedded http or https URLs, IP addresses, or interpreter
    commands. For binaries, check imports, embedded strings, packer signatures, and signing status. The
    file extension named in the command line indicates what to expect, but the actual content determines
    whether this is a benign artifact or a staged payload.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_artifact.artifact_verification
  question: What is the hash of the written file, and does it have known threat intelligence detections?
  context: A file hash provides a unique identifier for reputation lookup. Submit the hash to a threat
    intelligence platform to determine whether the file written into the Fonts folder is known malicious,
    known benign, or previously unseen. A file masquerading under a system-directory location may carry a
    name that looks innocuous while the hash tells a different story.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:universal.evidence_preservation
  question: Is the file written into the Fonts folder still on disk for analysis?
  context: If the file referenced by the command line still exists, it can be hashed, submitted for
    reputation analysis, or reviewed directly. If it has already been deleted, the file event and command
    line may be the only surviving record of what was staged. Files placed in C:\Windows\Fonts\ persist
    across reboots and run from that location, so a file that is still present is available for full analysis.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this command interpreter, and is that parent chain expected?
  context: The parent process reveals how the command was invoked. Interactive administration typically
    descends from explorer.exe or a terminal; automation descends from a service or scheduled task host.
    Parents such as script hosts, Office applications, or a self-extracting archive launcher point toward
    automated or scripted delivery rather than a human at a keyboard.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What did this command interpreter execute after writing the file?
  context: The processes spawned afterward reveal the actual impact of the staging. A file written into the
    Fonts folder and then executed, or followed by permission-lockdown commands, scheduled-task creation, or
    service installation, indicates the write was a setup step in a larger sequence rather than an isolated
    action.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this Fonts-folder write pattern, what parents and users drove
    it, and does the current alert fit that pattern?
  context: The query mirrors the rule's selection structure scoped to this host, so the resulting parent and
    user distribution reflects how this specific firing pattern has historically run here, not generic command
    interpreter usage. A new parent process, an unexpected user account, or an unusual command-line shape
    against the Fonts folder is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_1:
        CommandLine|contains:
        - echo
        - copy
        - type
        - file createnew
        - cacls
      selection_2:
        CommandLine|contains: C:\Windows\Fonts\
      selection_3:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin
        - .bat
        - .cmd
        - .js
        - .msh
        - .reg
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
        - '.inf'
        - .cpl
        - .hta
        - .msi
        - .vbs
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this Fonts-folder write pattern in the past week, and does the cross-host
    distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide
    count of this specific firing pattern. Many hosts firing the same pattern points to scripted deployment or
    vendor automation; one host or a small unrelated set points to manual operator activity. Use the host
    distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_1:
        CommandLine|contains:
        - echo
        - copy
        - type
        - file createnew
        - cacls
      selection_2:
        CommandLine|contains: C:\Windows\Fonts\
      selection_3:
        CommandLine|contains:
        - .sh
        - .exe
        - .dll
        - .bin
        - .bat
        - .cmd
        - .js
        - .msh
        - .reg
        - .scr
        - .ps
        - .vb
        - .jar
        - .pl
        - '.inf'
        - .cpl
        - .hta
        - .msi
        - .vbs
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally write files into system directories from the command line?
  context: Compare this user's current action against their historical behavior. A user who has never written
    executable or script files into a system directory from the command line is more suspicious than one who
    does so as part of a documented role. User role and typical toolset should align with the detected action.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains:
        - echo
        - copy
        - type
        - file createnew
        - cacls
      selection_2:
        CommandLine|contains: C:\Windows\Fonts\
      selection_user:
        User|expand: '%User%'
      condition: selection_1 and selection_2 and selection_user
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and command interpreter combination historically routine on this host or across
    the environment, or is the parent unusual?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism — which parent process spawned the interpreter that wrote into
    the Fonts folder, and whether that pair is historically common. A new or rare parent for this activity is
    the deviation worth chasing; a pair that already accounts for many prior events is automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains:
        - echo
        - copy
        - type
        - file createnew
        - cacls
      selection_2:
        CommandLine|contains: C:\Windows\Fonts\
      selection_parent:
        ParentImage|endswith|expand: '%ParentImage%'
      condition: selection_1 and selection_2 and selection_parent
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate
    context — what led to this write, what happened after, and whether it is part of a larger sequence. Look
    for reconnaissance, permission modification, persistence installation, or additional file staging in the
    same window.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access,
    initial access over a remote session — may reveal the attack stages that preceded the file staging and
    help reconstruct the full timeline. File-hiding tradecraft in a system directory often follows an initial
    foothold rather than starting it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host?
  context: Attackers rarely rely on a single persistence mechanism. After staging a file in an unusual
    location, they commonly install backup mechanisms using different techniques — scheduled tasks, services,
    registry Run keys, BITS jobs, or WMI subscriptions. Multiple persistence installations in the same
    timeframe indicate intentional stacking rather than isolated administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to persistence locations such as the Startup folder or Run key paths on this
    host?
  context: File-based persistence bypasses command-line detection entirely. Dropping an executable or script
    into the Startup folder or a path referenced by a registry Run key achieves persistence without invoking
    standard persistence tools. File event monitoring captures these drops, which can run in parallel to the
    Fonts-folder staging that triggered this alert.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for writing this file into
    the Fonts folder?
  context: Many organizations require change tickets for system modifications. Writing an executable into a
    protected system directory is an unusual operation; a corresponding ticket suggests authorized activity,
    while the absence of one when policy requires it suggests the action was unauthorized. This check can
    quickly close a false positive or escalate a true positive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:organizational_context.timing_alignment
  question: Does the timing of this activity align with normal work hours and maintenance windows?
  context: Legitimate system changes typically occur during business hours or scheduled maintenance windows.
    Writing files into a system directory at an unusual hour, or outside any documented maintenance window, is
    more suspicious than the same activity during a planned change. Check whether the local host time was
    reasonable and whether a maintenance window covered it.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059
      name: Command and Scripting Interpreter
      role: primary
    - id: T1211
      name: Exploitation for Defense Evasion
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: interpreter_invocation
      description: A command interpreter is invoked to write a file into the Fonts folder.
      mapped_to: Q3
      source: cue_process_tool.mechanism_decode
    - stage: 2
      label: file_staging
      description: An executable or script file is written into C:\Windows\Fonts\.
      mapped_to: Q4
      source: cue_artifact.mechanism_decode
    - stage: 3
      label: follow_on_execution
      description: Encoded/obfuscated arguments or secondary execution events follow the write.
      mapped_to: Q8
      source: cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059
      covered_by: Q3
      source: cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059
      covered_by: Q7
      source: cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059
      covered_by: Q18
      source: template:organizational_context.timing_alignment
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059
      covered_by: Q9
      source: cue_process_tool.baseline_deviation
      coverage_type: indirect
    - dimension: user_context
      technique: T1059
      covered_by: Q11
      source: cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "echo, copy, type, file createnew, cacls + C:\\Windows\\Fonts\\ + executable/script extensions"
      disposition: query_injection
      target_question: Q9
      notes: >-
        Rule's full selection block embedded verbatim in the REFLECTED baseline_deviation and scope queries;
        also tightened into the user-actor baseline and parent-baseline queries via selection_1/selection_2
        so the historical counts measure the rule pattern, not generic cmd.exe usage.
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Parent chain investigated via process_chain; no fixed parent values in rule."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q18
      notes: "Work-hours/maintenance-window framing carried in timing_alignment question."
    - element: DefensiveProcessList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q14
      notes: >-
        T1211 defensive-process / module-load tuning lists have no concrete values in the brief; prior-alert
        precursor sweep carries the defense-evasion-as-precursor angle for this process_creation rule.
    - element: AllowedModulePaths
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q14
      notes: "image_load module-allowlist not queryable from this rule's process_creation logsource; see hint_dispositions."
    - element: CrashThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q14
      notes: "Abnormal-termination threshold is a tuning rationale, not a queryable value for this rule."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: >-
        Host+Image baseline degenerates on cmd.exe (primary_binary) — host-scoped cmd.exe count is pure
        noise. The rule-pattern-scoped baseline_deviation (Q9) supersedes it with a filter-correct historical
        count.
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "host-context clustering (Q13) covers same-host concurrent activity; cue_process_tool.clustering duplicates it."
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.clustering
      reason: >-
        user_actor clustering pair degenerates under service principals and duplicates host-context
        clustering for this single-host file-staging rule; dropped both user-pivot and substitute clustering
        variants to stay within the 18-question cap. Baseline user-pivot + parent substitute retained.
      kept: cue_host_context.clustering
    - dropped: cue_user_actor.clustering_svc
      reason: "service-principal clustering substitute dropped alongside its user-pivot pair (same reason)."
      kept: cue_host_context.clustering
  skipped_templates:
    - template: cue_artifact.baseline
      reason: >-
        File-presence baseline over %file_paths% on -90d duplicates the evidence_preservation + artifact
        baseline angle already covered; the rule's signal is the write event itself, and Q4/Q5/Q6 cover
        content, reputation, and on-disk presence of the same file.
    - template: universal.endpoint_protection_health
      reason: >-
        Generic host-tamper sweep dropped: cue_host_context.precursor (Q14) already surfaces prior tamper /
        defense-evasion alerts on this host, and this rule detects file staging rather than a hands-on-keyboard
        execution where the disposition turns on AV/EDR being weakened first.
    - template: cue_process_tool.baseline
      reason: "see dedup_decisions — degenerates on cmd.exe."
  reflection_overrides: []
  hint_dispositions:
    - hint: "cmd.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: declined
      reason: >-
        Acted on, recorded here for audit: mechanism_decode (Q3) uses ProcessGuid scoping rather than
        host+Image to avoid cmd.exe volume; the rule-pattern baseline/scope queries use the embedded
        CommandLine selection, and the user/parent baselines are filtered by selection_1/selection_2
        CommandLine patterns instead of Image|endswith on cmd.exe.
    - hint: "cue_artifact: derived: [A-Za-z]:\\ in CommandLine"
      disposition: declined
      reason: >-
        Acted on: cue_artifact mechanism_decode (Q4) and artifact_verification (Q5) author file-content and
        hash questions for the file path embedded in the command line, scoped via %file_paths%.
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: declined
      reason: >-
        Acted on: no host+Image scoping is used anywhere; baseline_deviation/scope use the embedded
        CommandLine selection, and the user-actor and parent baselines scope by selection_1/selection_2
        CommandLine patterns plus User or ParentImage. Image-based template baselines were dropped.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: declined
      reason: >-
        Acted on: %file_paths% drives the file-content (Q4), hash (Q5), and evidence-preservation (Q6)
        queries against TargetFilename|endswith.
    - hint: "logsource 'image_load' (recommended by techniques: T1211) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        Rule detects the act of WRITING a file into C:\Windows\Fonts\ via process_creation command lines, not
        the subsequent loading of a module from that path. T1211 is a secondary technique here and its
        image_load analytic targets unsigned modules loaded into defensive processes, which this staging rule
        does not constrain to. The defense-evasion-as-precursor angle is covered by the prior-alert precursor
        sweep (Q14); no compensating attack:T1211.image_load question authored because the staged file's
        execution, if any, is captured by the follow_on process_creation question (Q8).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - organizational_context
    - persistence_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 18
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 13
    logsource_diversity:
      process_creation: 13
      file_event: 4
      sigma_alert: 1
