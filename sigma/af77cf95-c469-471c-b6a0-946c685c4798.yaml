name: Proxy Execution Via Wuauclt.EXE
id: 601a4156-8251-470c-bd2f-3d14c572213a
description: |
  Detects the Windows Update client binary wuauclt.exe invoked with both the
  /UpdateDeploymentProvider and /RunHandlerComServer arguments, which causes the
  Microsoft-signed process to load an arbitrary DLL and invoke a registered COM
  handler from it — effectively proxying code execution through a trusted binary.
  Legitimate update-agent use of this argument pair is rare and limited to
  specific update-deployment infrastructure paths, several of which the rule
  excludes; an invocation that points at a DLL outside those well-known paths has
  no routine endpoint use. Determine which DLL was loaded, where it lives, how
  wuauclt.exe was launched, and what the process did afterward.
type: detection
detection_id: af77cf95-c469-471c-b6a0-946c685c4798
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the wuauclt.exe command line contain both /UpdateDeploymentProvider and /RunHandlerComServer, and which DLL path does it point at?
  context: Confirm the alert is a true positive by reviewing the exact command line and process details. The rule fires only when the binary resolves to wuauclt.exe (by Image or OriginalFileName) AND the command line contains both UpdateDeploymentProvider and RunHandlerComServer, excluding a short list of known update-deployment DLL paths. Read the DLL path argument carefully — note whether it lives under System32 or points somewhere user-writable or unusual.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run the Windows Update client with a deployment-provider argument?
  context: Legitimate use of this argument pair is confined to specific update-deployment and managed-device infrastructure scenarios. On a typical endpoint the Windows Update client never runs with a caller-supplied DLL path. Knowing whether this host has an update-management role and whether the acting user performs that kind of administration is often the fastest way to separate a true positive from sanctioned tooling.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What DLL did wuauclt.exe load, and where does that DLL path resolve?
  context: The full command line reveals the operational intent. The argument following /UpdateDeploymentProvider is the path to the DLL that the signed Windows Update process is being told to load and execute a COM handler from. Decode the full path — a DLL outside %SystemRoot%\System32, in a user-writable directory, or with a lookalike directory name is the operative signal; the COM handler is invoked regardless of the DLL's location.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains: 'UpdateDeploymentProvider'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1218.image_load
  question: Did wuauclt.exe load the DLL named on its command line, and is that DLL signed and located where expected?
  context: Proxy execution through this binary completes when the signed Windows Update process loads the attacker-named DLL into its address space. Pivot to module-load telemetry for this process to confirm the DLL load and to read its signature status and on-disk path. An unsigned module, a module outside System32, or one whose path matches a lookalike directory name confirms the abuse; if module-load events are not collected in this environment, treat zero results as a visibility gap and fall back to the on-disk path read from the command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: template:cue_process_tool.process_chain
  question: What process spawned wuauclt.exe, and is that parent chain expected for the Windows Update client?
  context: On a healthy system the Windows Update client is launched by the update service or scheduled maintenance, not by interactive or scripting parents. A parent such as explorer.exe driven by a startup-folder shortcut, an Office application, mshta.exe, wscript.exe, or a recently dropped binary points to operator-driven or persistence-driven invocation rather than routine update activity.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did wuauclt.exe spawn afterward?
  context: The COM handler loaded through this technique can carry out arbitrary actions, including spawning further processes. Children such as cmd.exe, powershell.exe, certutil.exe, rundll32.exe, or a newly written binary indicate post-exploitation activity following the DLL load; the Windows Update client has no legitimate reason to spawn interactive shells or download utilities.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_connection
  question: Did wuauclt.exe make outbound network connections around the time of the alert, and to where?
  context: Proxy execution through a signed binary is frequently paired with payload retrieval or command-and-control over the trusted process, so connections appear to originate from the Windows Update client. Review the destinations the process contacted and compare them against known update endpoints. Connections to addresses outside the organization's update infrastructure, or to a single external host repeatedly, point to retrieval or beaconing rather than update traffic. If network telemetry for this host is not collected, treat zero results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run wuauclt.exe with a deployment-provider argument?
  context: A process invocation that has never been seen on a host is more suspicious than one that recurs. The Windows Update client itself runs routinely, so the meaningful baseline is whether this specific argument-driven invocation has any history here. Zero or very low historical counts of the proxy-execution pattern indicate a first-time or rare event worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains: 'UpdateDeploymentProvider'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this proxy-execution pattern, what parents and users drove it, and does the current alert fit that pattern?
  context: Even an update-management host that legitimately uses this argument pair has a stable shape to that activity. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wuauclt.exe
      - OriginalFileName: wuauclt.exe
      selection_cli:
        CommandLine|contains|all:
        - UpdateDeploymentProvider
        - RunHandlerComServer
      filter_main_generic:
        CommandLine|contains: ' /UpdateDeploymentProvider UpdateDeploymentProvider.dll '
      filter_main_wuaueng:
        CommandLine|contains: ' wuaueng.dll '
      filter_main_uus:
        CommandLine|contains:
        - :\Windows\UUS\Packages\Preview\amd64\updatedeploy.dll /ClassId
        - :\Windows\UUS\amd64\UpdateDeploy.dll /ClassId
      filter_main_winsxs:
        CommandLine|contains|all:
        - :\Windows\WinSxS\
        - '\UpdateDeploy.dll /ClassId '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this proxy-execution pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this specific firing pattern rather than generic Windows Update activity. Many hosts firing the same pattern points to a managed update-deployment workflow; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \wuauclt.exe
      - OriginalFileName: wuauclt.exe
      selection_cli:
        CommandLine|contains|all:
        - UpdateDeploymentProvider
        - RunHandlerComServer
      filter_main_generic:
        CommandLine|contains: ' /UpdateDeploymentProvider UpdateDeploymentProvider.dll '
      filter_main_wuaueng:
        CommandLine|contains: ' wuaueng.dll '
      filter_main_uus:
        CommandLine|contains:
        - :\Windows\UUS\Packages\Preview\amd64\updatedeploy.dll /ClassId
        - :\Windows\UUS\amd64\UpdateDeploy.dll /ClassId
      filter_main_winsxs:
        CommandLine|contains|all:
        - :\Windows\WinSxS\
        - '\UpdateDeploy.dll /ClassId '
      condition: all of selection_* and not 1 of filter_main_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run the Windows Update client with a deployment-provider argument?
  context: Compare this user's current action against their historical behavior. A user who has never driven this kind of update-agent invocation is more suspicious than one who does so as part of a defined administrative role. User role and typical toolset should align with update-deployment management for this to be routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and wuauclt.exe combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism. Which parent process spawned the Windows Update client, and is that pair historically common here and environment-wide. A new or rare parent for this binary is the deviation worth chasing; a pair that already accounts for many prior events is update-service or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staged actions across multiple endpoints. If the same user triggered similar activity on more than one host, the scope of the activity may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and wuauclt.exe combination fire on other hosts in the same window, indicating a coordinated rollout, or is it isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise, since every host runs service-owned processes constantly. The meaningful question is whether the same launch mechanism — the parent process spawning the Windows Update client — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence installation, or staging near the alert time.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, credential access, or persistence — may reveal the stages that preceded this event and help reconstruct the full timeline that led to proxy execution through a signed binary.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
  - id: T1218
    name: System Binary Proxy Execution
    role: primary
  attack_chain_coverage:
  - stage: 1
    label: signed_binary_invocation
    description: Microsoft-signed wuauclt.exe invoked with the deployment-provider argument pair that triggers DLL load via COM handler
    mapped_to: Q1
    source: template:universal.verify_trigger_match
  - stage: 2
    label: command_line_decode
    description: The DLL path argument supplied to the signed binary is decoded to determine the loaded payload location
    mapped_to: Q3
    source: template:cue_process_tool.mechanism_decode
  - stage: 3
    label: payload_dll_load
    description: The signed process loads the externally named DLL and invokes its COM handler
    mapped_to: Q4
    source: attack:T1218.image_load
  - stage: 4
    label: network_retrieval_or_c2
    description: The proxied payload retrieves further content or establishes command-and-control through the signed process
    mapped_to: Q7
    source: attack:T1218.network_connection
  dimension_coverage:
  - dimension: command_analysis
    technique: T1218
    covered_by: Q3
    source: template:cue_process_tool.mechanism_decode
    coverage_type: direct
  - dimension: parent_process_analysis
    technique: T1218
    covered_by: Q5
    source: template:cue_process_tool.process_chain
    coverage_type: direct
  - dimension: tuning_parameter
    technique: T1218
    covered_by: Q8
    source: template:cue_process_tool.baseline
    coverage_type: indirect
  mutable_dispositions:
  - element: CommandLineRegex
    dimension: command_analysis
    specific_values: 'rationale only (regex to match suspicious arguments such as URLs, script paths, or DLL entrypoints)'
    disposition: query_injection
    target_question: Q3
    notes: Rule's own CommandLine|contains|all of UpdateDeploymentProvider + RunHandlerComServer is the concrete instance; mechanism_decode adds CommandLine|contains 'UpdateDeploymentProvider' and asks the analyst to read the DLL-path argument.
  - element: ParentProcessName
    dimension: parent_process_analysis
    specific_values: 'rationale only (profile unexpected parent-child relationships)'
    disposition: new_question
    target_question: Q5
    notes: Covered by process_chain; service-principal launch-mechanism pivots extend parent profiling fleet-wide.
  - element: SignedBinaryList
    dimension: tuning_parameter
    specific_values: 'rationale only (allowlist of signed binaries; wuauclt.exe is the binary this rule fixes on)'
    disposition: context
    target_question: Q4
    notes: Rule already fixes the signed binary to wuauclt.exe; image_load question reads the loaded DLL's signature/path to apply the signed-vs-unsigned distinction at the module level.
  - element: RemoteDomainAllowlist
    dimension: tuning_parameter
    specific_values: 'rationale only (suppress connections to legitimate enterprise domains)'
    disposition: context
    target_question: Q7
    notes: network_connection question asks the analyst to compare destinations against known update infrastructure; no fixed allowlist values exist in the rule to inject.
  dedup_decisions: []
  skipped_templates:
  - template: universal.endpoint_protection_health
    reason: >-
      Recommended (not must_include) generic host-tamper sweep. Dropped to stay
      within the question target: cue_host_context.precursor already surfaces
      prior tamper/defense-evasion alerts on this host before the event,
      and the disposition here turns on the DLL load and parent chain rather than
      on whether protection was weakened first. Logsource is Windows
      process_creation so this is a deliberate drop, not a logsource skip.
  reflection_overrides: []
  hint_dispositions:
  - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
    disposition: acted_on
    reason: >-
      Acted on — authored Q4 (attack:T1218.image_load) with category: image_load,
      scoped to host + wuauclt.exe, projecting ImageLoaded/Signed/Signature to
      confirm the proxied DLL load and read its signature and on-disk path.
  - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
    disposition: acted_on
    reason: >-
      Acted on — authored Q7 (attack:T1218.network_connection) with
      category: network_connection, scoped to host + wuauclt.exe, projecting
      DestinationIp/DestinationPort/DestinationHostname to surface payload
      retrieval or C2 proxied through the signed process.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      image_load: 1
      network_connection: 1
      sigma_alert: 1
