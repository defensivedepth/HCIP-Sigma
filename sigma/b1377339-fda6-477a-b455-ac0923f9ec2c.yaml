name: Remote Access Tool - AnyDesk Piped Password Via CLI
id: 045b8c49-7b63-4822-ba6b-bb23eb774641
description: |
  Detects a command shell piping a password into a remote access tool's unattended-access setup via the '--set-password' flag, using the 'echo <password> | <tool>.exe --set-password' pattern run through 'cmd /c'. Setting an unattended-access password from the command line is essentially never part of legitimate interactive enterprise use; it is the scripted way an operator pre-stages persistent, password-protected remote control of a host. The investigation should confirm the exact command, establish whether the remote access tool is approved and supported by an active ticket, identify where the tool binary lives and what it connects to, and determine whether the activity is isolated or part of a wider rollout.
type: detection
detection_id: b1377339-fda6-477a-b455-ac0923f9ec2c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-05
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show a password being piped into a remote access tool's --set-password flag?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires only when a single command line contains all of '/c ', 'echo ', and '.exe --set-password' together, which is the 'echo <password> | <tool>.exe --set-password' unattended-access setup pattern. Read the full command line and confirm those elements are present and that the target executable is a remote access tool rather than an unrelated program that happens to accept a '--set-password' argument.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to configure unattended remote access from the command line?
  context: Legitimate administrative activity can trigger this detection, but scripted password-set of a remote access tool is rare even for IT staff. Knowing whether the user role is a remote-support or administrative function and whether the host is one where such tooling is normally configured is often the fastest way to separate sanctioned setup from operator-staged persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What is the full command, and which executable and password value were passed to --set-password?
  context: The full command line reveals exactly which binary received the password and what that password was. Identify the target executable name and path after the pipe, and capture the password string set, since a hardcoded or reused password is itself an indicator. Scoping is by the '--set-password' command-line pattern on this host rather than by the cmd.exe binary, which is far too common for host-wide binary scoping.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: '.exe --set-password'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this command shell, and is that parent chain expected?
  context: The parent of the command shell shows how the password-set was invoked. A parent like explorer.exe or a support agent suggests interactive or sanctioned setup, while parents such as a script host, an Office application, a downloaded installer, or another remote access tool point to scripted or attacker-driven staging. The expected parent for this activity varies by environment, so compare against what normally launches administrative shells on this host.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this --set-password pattern, what parents and users drove it, and does the current alert fit?
  context: Even sanctioned remote-support tooling leaves a baseline. The query mirrors the rule's selection scoped to this host, so the parent and user distribution reflects how this specific password-set pattern has historically run here rather than generic shell usage. A new parent process, an unexpected user account, or a first-ever appearance of this pattern on the host is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - '/c '
        - 'echo '
        - .exe --set-password
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: attack:T1219.002.install_path
  question: Where does the remote access tool binary live on disk, and was it written to a canonical install directory or a user-writable or temporary path?
  context: Remote access tools have well-known install directories under Program Files; the same binary dropped into a temp, ProgramData, or user-writable path is a strong sign of attacker staging rather than a sanctioned install. Pull recent writes of the named executable and compare the write path against the vendor's canonical install locations. A binary written shortly before the password-set, or placed outside approved paths, raises the priority of the alert.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        TargetFilename|endswith: '.exe'
      filter_main:
        TargetFilename|contains: '\anydesk'
      condition: selection and filter_main
    fields:
      - host.name
      - TargetFilename
      - Image
      - User
- source: template:remote_access_tool.connection_destination
  question: What external endpoint does the remote access tool connect to, and is it the vendor's infrastructure or a custom address?
  context: Remote access tools establish outbound connections to relay or cloud infrastructure. Connections to the vendor's documented endpoints are expected, while connections to unfamiliar addresses, or on configurable high ports rather than the standard service ports, suggest a self-hosted relay or attacker infrastructure. Use the destination address and port to judge whether egress matches the legitimate vendor service or points somewhere unusual.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: template:remote_access_tool.tool_authorization
  question: Is this remote access tool on the organization's approved software list?
  context: Organizations typically sanction a single remote access tool for IT support. Any other remote access tool, even well-known commercial software, may represent shadow IT or attacker-deployed persistence and should be checked regardless of the tool's reputation. Confirm whether this specific tool is approved for use in this environment at all.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a current helpdesk ticket or support session explaining this remote access setup?
  context: Legitimate remote access tool usage typically correlates with an active support ticket, a scheduled maintenance window, or a documented user request. If no support context exists for configuring unattended access on this host, the tool may have been set up by an attacker for persistent access or by a user circumventing controls.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1219.interactive_child
  question: After the password was set, did the remote access tool spawn interactive child processes such as a shell or file manager?
  context: A remote access tool used for hands-on control commonly spawns command shells, script hosts, or file-management processes as the operator works. Children like cmd.exe, powershell.exe, or file-transfer utilities under the tool indicate an active session rather than a dormant install. Their absence does not clear the host, but their presence confirms the tool was driven interactively after the password was set.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|contains: '\anydesk'
      condition: selection
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_user_actor.baseline
  question: Has this user configured unattended remote access via this command pattern before?
  context: Compare this user's current action against their historical behavior. A user who has never piped a password into a remote access tool's setup is more suspicious than one who does so as a routine part of a support role. Zero or very low historical counts across the environment indicate first-time or out-of-role activity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: '.exe --set-password'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this command shell parent combined with this password-set pattern historically routine on this host or across the environment?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism. Check whether the parent process that spawned the shell, paired with this password-set pattern, is a combination that has appeared before. A new or rare parent for this pattern is the deviation worth chasing, while a pair that already accounts for many prior events points to sanctioned automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: '.exe --set-password'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging on multiple endpoints. If the same user configured remote access or ran related tooling on several hosts in this window, the scope of the operation may be larger than a single machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same password-set pattern fire on other hosts in the same window, indicating a coordinated rollout rather than isolated activity?
  context: Under a service principal, asking what else the user did returns agent-telemetry noise. The meaningful question is whether the same password-set pattern appeared on other hosts in the alert window. Multiple hosts in the same window points to scripted or pushed deployment of remote access, while a single host points to a host-local action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: '.exe --set-password'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the surrounding activity reveals what led to the password-set and what followed it. Look for reconnaissance, credential access, the install of the remote access tool itself, or other staging that places this event in a larger sequence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host, such as defense tampering, reconnaissance, or credential access, may reveal the attack stages that preceded the remote access setup. The alert timeline often shows whether this password-set was the entry point or one step in an ongoing intrusion.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this --set-password pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of this password-set pattern rather than generic shell usage. Many hosts firing the same pattern points to a scripted or vendor-driven deployment, while one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate how broadly to respond.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - '/c '
        - 'echo '
        - .exe --set-password
      condition: selection
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219.002
      name: Remote Desktop Software
      role: primary
    - id: T1219
      name: Remote Access Tools
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tool_execution_user_context
      description: "Initial execution / configuration of the remote-control agent under a user context (the password-set invocation)."
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: persistence_unattended_access
      description: "Persistence: unattended-access password set so the tool can be controlled later without prompting; binary placement signals staging."
      mapped_to: Q6
      source: attack:T1219.002.install_path
    - stage: 3
      label: outbound_tunnel
      description: "Long-lived outbound connection / tunnel from the RAT to external infrastructure."
      mapped_to: Q7
      source: template:remote_access_tool.connection_destination
    - stage: 4
      label: interactive_control_children
      description: "Interactive control signals: shell or file-manager child processes spawned by the RAT parent."
      mapped_to: Q10
      source: attack:T1219.interactive_child
  dimension_coverage:
    - dimension: time_correlation
      technique: T1219.002
      covered_by: Q16
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219.002
      covered_by: Q6
      source: attack:T1219.002.install_path
      coverage_type: direct
    - dimension: process_identification
      technique: T1219
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: Image
      dimension: tuning_parameter
      specific_values: "rationale only (RMM binaries vary: ConnectWise, Zoho Assist, TeamViewer, AnyDesk)"
      disposition: context
      target_question: Q6
      notes: "Rule is CommandLine-only with no Image field; binary identity is read from the command line after the pipe. install_path question scopes file writes to the named tool and its canonical-vs-staging paths."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "7070, 5650 (configurable/random high ports)"
      disposition: context
      target_question: Q7
      notes: "Ports surfaced in connection_destination context; query projects DestinationPort so the analyst sees standard-vs-high-port egress without over-constraining the selection."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only (expected parent varies by environment)"
      disposition: context
      target_question: Q4
      notes: "process_chain returns the parent for comparison against environment baseline; baseline_deviation aggregates parents historically."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (install-to-beacon / process-to-network correlation window, default 15m)"
      disposition: context
      target_question: Q16
      notes: "host clustering uses +/-15m; clustering_svc and connection windows cover the correlation span."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (help-desk / jump hosts vs standard endpoints)"
      disposition: context
      target_question: Q2
      notes: "host_user_context asks whether the user/host are a remote-support function."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only (known-good remote support tools)"
      disposition: context
      target_question: Q8
      notes: "tool_authorization checks approved-software-list membership."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "rationale only (alert when agents run from user-writable / temp paths)"
      disposition: new_question
      target_question: Q6
      notes: "install_path question canonical-vs-temp/ProgramData path comparison drives this directly."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (vendor support clouds / CDNs)"
      disposition: context
      target_question: Q7
      notes: "connection_destination distinguishes vendor infrastructure from custom endpoints."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only (acceptable vs suspicious child shells from GUI parents)"
      disposition: new_question
      target_question: Q10
      notes: "interactive_child question targets shell / file-manager children spawned by the RAT."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only (session-duration / byte-ratio thresholds)"
      disposition: context
      target_question: Q7
      notes: "Threshold-based heuristics are not directly queryable from process/network selection here; surfaced as egress-judgment context in connection_destination."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "cue_process_tool.baseline_deviation (REFLECTED, host-scoped to the rule's pattern) is more specific and supersedes generic host-binary baseline; binary is cmd.exe (noisy) so generic Image baseline would be uninformative."
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "overlaps cue_host_context.clustering (both aggregate other activity on the host in the same window); kept the host_context version which projects Image+ParentImage."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.follow_on
      reason: "follow_on scopes ParentImage to the rule binary (cmd.exe), which is too common for meaningful child scoping; the interactive_child attack question covers RAT-spawned children with a tool-scoped parent instead."
      kept: attack:T1219.interactive_child
    - dropped: null
      reason: "cue_user_actor.baseline / baseline_svc and clustering / clustering_svc are emitted as non-deduplicable service-principal pairs per AGENTS.md (subject-pivot vs mechanism-pivot degenerate under different conditions)."
      kept: "all four user_actor variants"
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "Recommended (not must_include) generic host-tamper sweep. This rule fires on remote-access tool configuration (command-and-control), not payload execution or credential access, and cue_host_context.precursor already surfaces prior tamper alerts on the host. Dropped to stay within the question cap; tamper-as-precursor is carried by the precursor question."
  reflection_overrides: []
  hint_dispositions:
    - hint: "cmd.exe: too common for Image-based scoping — use CommandLine patterns for mechanism_decode"
      disposition: acted-on
      reason: >-
        mechanism_decode (Q3) scopes by CommandLine|contains '.exe --set-password'
        on the host instead of Image|endswith %Image% (cmd.exe). Recorded for
        audit trail; not a decline.
    - hint: "CommandLine-only detection — no Image field in rule. Use CommandLine pattern matching for scoping, not Image-based queries"
      disposition: acted-on
      reason: >-
        All scope-by-pattern questions (mechanism_decode, user baseline,
        baseline_svc, clustering_svc, scope, baseline_deviation) pivot on the
        '--set-password' CommandLine pattern rather than %Image%, since the rule
        has no Image field. Recorded for audit trail; not a decline.
    - hint: "logsource 'file_event' (recommended by techniques: T1219.002) is not covered — pull in cue_artifact.* or author a custom question with `category: file_event`"
      disposition: acted-on
      reason: >-
        Authored attack:T1219.002.install_path (Q6) as a custom file_event
        question scoping TargetFilename writes of the remote access binary and
        comparing canonical install paths against temp / ProgramData / user-
        writable staging paths. Recorded for audit trail; not a decline.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - remote_access_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      file_event: 1
      network_connection: 1
      sigma_alert: 1
