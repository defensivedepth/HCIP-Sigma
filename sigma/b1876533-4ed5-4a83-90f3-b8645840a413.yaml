name: HackTool - SafetyKatz Execution
id: 445b5534-3b98-4865-a3e4-4269f119301e
description: |
  Fires when SafetyKatz runs, identified by its default image name, PE OriginalFileName, or Description.
  SafetyKatz writes a full-memory minidump of lsass.exe to disk (by default C:\Windows\Temp\debug.bin),
  then loads a stripped Mimikatz build that reads logon passwords and Kerberos keys from the minidump and
  deletes the dump file afterward. The tool has no legitimate use, so a name/metadata match is high-confidence,
  but the binary can be renamed; the investigation should confirm the LSASS access and dump, identify which
  credentials were exposed, and determine whether the stolen material was used to move laterally.
type: detection
detection_id: b1876533-4ed5-4a83-90f3-b8645840a413
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Is the alerting process actually SafetyKatz, by image name, PE OriginalFileName, or Description?
  context: Confirm the alert is a true positive by reviewing the triggering event. The rule fires when any
    one of three identifiers matches - an image path ending in SafetyKatz.exe, a PE OriginalFileName of
    SafetyKatz.exe, or a Description of SafetyKatz. A binary renamed on disk still matches on the metadata
    branches, so note which identifier fired and treat a renamed match the same as a name match.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Description
- source: template:universal.host_user_context
  question: Is this user and host expected to run a credential-dumping tool?
  context: A general-purpose LSASS dumper has no routine administrative use. Knowing whether the host is a
    security-testing system and whether the user performs authorized offensive work is the fastest way to
    separate a sanctioned test from an intrusion.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this execution part of an authorized penetration test or red team engagement?
  context: Offensive tooling is run during sanctioned assessments. Before investing further analysis, check
    whether a penetration test or red team exercise is active on this host or network segment with matching
    timing. A documented engagement that accounts for the activity resolves the alert.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.credential_target_identification
  question: What does the command line show about the dump target and method, and was LSASS the source?
  context: Decode the command line to identify the credential source and method. By default SafetyKatz dumps
    lsass.exe memory to a minidump and runs sekurlsa modules against it, but operands may redirect the dump
    path or select different modules. The method and target confirm that in-memory credentials were captured
    rather than an unrelated diagnostic dump.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: Which capability of the tool was invoked - default LSASS dump, or a different module or output path?
  context: The specific operands determine the scope of compromise. The default run dumps LSASS and extracts
    logon passwords and Kerberos keys, but flags can change the dump location or the modules executed. Identify
    what was actually requested so the credential-exposure assessment matches what the tool did.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1003.001.access_verification
  question: Did this process open a full-access handle to lsass.exe on this host around the time of the alert?
  context: Dumping LSASS memory requires opening a handle with full access rights (0x1F0FFF) to lsass.exe.
    A process-access event where the alerting binary is the source, lsass.exe is the target, and the granted
    mask is the full-access value confirms the memory read that produces the minidump. If process-access
    telemetry is not collected in this environment, treat the absence of results as a visibility gap, not as
    evidence no dump occurred.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        SourceImage|endswith|expand: '%Image%'
        TargetImage|endswith: '\lsass.exe'
        GrantedAccess: '0x1F0FFF'
      filter_host:
        host.name|expand: '%hostname%'
      condition: selection and filter_host
    fields:
      - SourceImage
      - GrantedAccess
      - CallTrace
- source: attack:T1003.001.dump_file_path
  question: Was an LSASS minidump written to disk on this host, and is the dump file still present?
  context: SafetyKatz writes a minidump of LSASS to disk (by default C:\Windows\Temp\debug.bin) and deletes
    it after extraction, so recovering the file or its creation event confirms genuine credential material was
    captured and indicates whether it is still recoverable. Dumps are commonly written to %TEMP% or
    C:\Windows\Temp; a surviving file there means the exposure is live and the dump can be hashed and reviewed.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - debug.bin
          - \Windows\Temp\
          - .dmp
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:hacking_tool.tool_specific_artifacts
  question: What other files did the alerting process create on this host?
  context: Beyond the minidump, the tool may drop its loader payload, output files, or other staged tooling.
    Files created by this process reveal the scope of activity and the payload source even after the process
    exits and the minidump is deleted.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned the alerting binary, and is that parent chain expected?
  context: The parent reveals how the tool was delivered and launched. An interactive shell from explorer.exe
    points to hands-on-keyboard operation, while parents such as wscript.exe, mshta.exe, an Office application,
    or a remote-execution service point to scripted or remote delivery. An unexpected parent for a credential
    dumper is the lead worth following upstream.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were any new logon sessions or remote connections initiated on this host after the dump?
  context: Captured LSASS credentials are typically used to move laterally. Follow-on alerts on the same host
    shortly after the dump - remote logons, SMB connections, remote service creation, or new authentication
    events - indicate the stolen credentials are being exercised rather than merely staged.
  range: +4h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be exposed by this dump, and do any have elevated privileges?
  context: An LSASS dump exposes every credential cached in memory on the host - interactive logons, service
    accounts, and any cached domain credentials. Cross-reference the accounts that logged on to this host with
    directory group memberships; domain-admin or broad service-account exposure requires escalation, while a
    single standard-user session may only need a password reset.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously run this binary, what parents and users drove it, and does the current alert fit?
  context: The query mirrors the rule's selection scoped to this host, so the resulting parent and user
    distribution reflects how this specific binary has run here historically. For a tool with no legitimate
    purpose a zero or near-zero baseline is itself the finding; any prior runs and the accounts behind them
    are worth correlating with known testing activity.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith: \SafetyKatz.exe
      - OriginalFileName: SafetyKatz.exe
      - Description: SafetyKatz
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts run this binary in the past week, and does the spread look like coordinated activity or one host?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of this binary.
    The same tool appearing on many hosts in a short window points to a pushed or scripted rollout across the
    estate; a single host or a small unrelated set points to isolated operator activity. The host distribution
    calibrates how broad the credential-reset response needs to be.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - Image|endswith: \SafetyKatz.exe
      - OriginalFileName: SafetyKatz.exe
      - Description: SafetyKatz
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user run this binary before anywhere in the environment?
  context: Compare this account's current action against its history. A user who has never run this tool is
    more suspicious than one with a record of sanctioned offensive testing. Zero or very low historical counts
    indicate first-time use for this account and should be weighed against the user's role.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and binary combination historically routine on this host or environment-wide, or is the parent unusual?
  context: When the alert fires under a service or computer account, user identity does not attribute the
    behavior; the meaningful pivot is the launch mechanism. A parent process that already accounts for many
    prior executions of this binary is automation, while a new or rare parent for this dumper is the deviation
    worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: A credential dump is usually one stage of a longer operation. Earlier detections on the same host -
    defense tampering, reconnaissance, privilege escalation - help reconstruct what preceded the dump and
    whether protection was weakened first.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering frequently precedes credential theft - an operator may weaken AV/EDR before running a
    dumper that would otherwise be blocked. The query surfaces direct attempts to disable AV/EDR services,
    change Defender settings, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful
    "protection looks healthy" signal; populated rows shift the disposition toward operator preparation, so
    review the parent process and signer to identify who made the change.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.001
      name: LSASS Memory
      role: primary
    - id: T1003
      name: OS Credential Dumping
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: lsass_access
      description: "Process opens a full-access (0x1F0FFF) handle to lsass.exe to read memory"
      mapped_to: Q6
      source: attack:T1003.001.access_verification
    - stage: 2
      label: minidump_write
      description: "MiniDumpWriteDump writes an LSASS minidump to disk (default C:\\Windows\\Temp\\debug.bin)"
      mapped_to: Q7
      source: attack:T1003.001.dump_file_path
    - stage: 3
      label: credential_extraction
      description: "PE-loaded Mimikatz reads logon passwords and Kerberos keys from the minidump, then deletes it"
      mapped_to: Q4
      source: template:credential_access.credential_target_identification
    - stage: 4
      label: tool_invocation
      description: "SafetyKatz process executes - the rule's triggering event"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 5
      label: credential_use
      description: "Stolen credentials exercised via new logons / lateral movement"
      mapped_to: Q10
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: access_verification
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.access_verification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1003.001
      covered_by: Q4
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1003.001
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.access_verification
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1003.001
      covered_by: Q7
      source: attack:T1003.001.dump_file_path
      coverage_type: direct
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF"
      disposition: query_injection
      target_question: Q6
      notes: "GrantedAccess: '0x1F0FFF' injected into the process_access selection"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "~5 min between LSASS access and dump file creation"
      disposition: context
      target_question: Q6
      notes: "access_verification and dump_file_path both use +/-15m to bracket the access-to-write sequence"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "allowlist AV/EDR accessing lsass"
      disposition: context
      target_question: Q6
      notes: "context tells analyst to exclude known AV/EDR SourceImage; SourceImage projected for that judgment"
    - element: DumpFilePath
      dimension: tuning_parameter
      specific_values: "C:\\Windows\\Temp\\debug.bin, %TEMP%, C:\\Windows\\Temp"
      disposition: query_injection
      target_question: Q7
      notes: "debug.bin and \\Windows\\Temp\\ injected into dump_file_path TargetFilename|contains; named in context"
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "sekurlsa::logonpasswords, sekurlsa::ekeys, minidump"
      disposition: context
      target_question: Q4
      notes: "credential_target_identification context describes the default sekurlsa modules; rule has no commandline_values to inject"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification (Q4) per brief - more specific for the credential-access dimension"
      kept: credential_access.credential_target_identification
    - dropped: cue_user_actor.clustering
      reason: "lateral-movement scope (the analyst's reason to pivot user-wide after a cred dump) is carried by credential_access.post_access_lateral_movement (Q10) and host_context.precursor (Q16); user-pivot baseline pair (Q14/Q15) retained, full clustering pair dropped to keep the essential endpoint_protection_health within 18"
      kept: credential_access.post_access_lateral_movement
    - dropped: cue_user_actor.clustering_svc
      reason: "paired with cue_user_actor.clustering which was dropped for the same reason"
      kept: credential_access.post_access_lateral_movement
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "single-pattern rule - cue_process_tool.baseline (Image|endswith host frequency) and the REFLECTED baseline_deviation are effectively the same query here; kept baseline_deviation (Q12) as the more precise rule-pattern form"
    - template: cue_process_tool.follow_on
      reason: "SafetyKatz spawns no operational children (it PE-loads Mimikatz in-process); post-execution activity is covered by post_access_lateral_movement (Q10) and precursor (Q16)"
    - template: cue_process_tool.clustering
      reason: "around-the-time tool clustering folded into host_context.precursor (Q16, prior sigma alerts) which is the load-bearing prior-stage pivot for a cred-dump rule"
    - template: cue_host_context.clustering
      reason: "overlaps host_context.precursor (Q16) and tool_specific_artifacts (Q8); generic host clustering adds volume without a distinct question within the cap"
  reflection_overrides: []
  hint_dispositions:
    - hint: "credential_access.credential_target_identification replaces cue_process_tool.mechanism_decode"
      disposition: acted-on
      reason: >-
        mechanism_decode dropped (see dedup_decisions); credential_target_identification
        is Q4, carrying the dump-target/method decode for the credential-access dimension.
    - hint: "logsource 'process_access' (recommended by techniques: T1003.001) is not covered and no template family exists - author a custom question with category: process_access"
      disposition: acted-on
      reason: >-
        Authored Q6 (attack:T1003.001.access_verification) with category: process_access,
        SourceImage scoped to the alerting binary and TargetImage \lsass.exe with the
        full-access GrantedAccess mask - the load-bearing evidence that the LSASS memory
        read behind the minidump occurred.
    - hint: "logsource 'registry_set' (recommended by techniques: T1003.001) is not covered - pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: declined
      reason: >-
        SafetyKatz dumps LSASS memory via MiniDumpWriteDump to a disk minidump; it does
        not save SAM/LSA registry hives. The registry_set analytic for T1003.001 targets
        registry-based credential staging (hive saves / LSA secret keys), which this tool
        does not perform and the minidump does not imply. LSASS access is verified directly
        via process_access in Q6. No compensating registry_set question authored.
    - hint: "logsource 'security' (recommended by techniques: T1003.001) is not covered and no template family exists - author a custom question with category: security"
      disposition: declined
      reason: >-
        The T1003.001 security-channel analytic keys on handle-access events (4656/4663/4673)
        as a proxy for LSASS access. The same access is captured with more precision by the
        Sysmon process_access full-handle (0x1F0FFF) query in Q6, making a parallel
        security-log question redundant. No security question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - hacking_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 3
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 12
      process_access: 1
      file_event: 2
      sigma_alert: 2
