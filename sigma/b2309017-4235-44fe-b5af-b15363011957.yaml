name: Uncommon Child Process Of Defaultpack.EXE
id: 53b9dd8a-7f0c-462f-99ad-7e0713665921
description: |
  Detects when DefaultPack.exe spawns a child process. DefaultPack.exe is a Microsoft-signed binary that ships
  alongside certain software downloads and can be used to launch other programs with command-line arguments,
  making it a proxy for executing arbitrary binaries while inheriting the trust of a signed parent. The rule fires
  on the parent-child relationship alone, so the investigation centers on what child was launched, how it was
  invoked, what it loaded or connected to, and whether this spawning pattern is normal for the host.
type: detection
detection_id: b2309017-4235-44fe-b5af-b15363011957
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Is the parent process of the alerting event DefaultPack.exe?
  context: Confirm the alert is a true positive by reviewing the triggering event and verifying the parent
    process path ends in DefaultPack.exe. The child process identity from this event drives the rest of the
    investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to run DefaultPack.exe and have it launch other programs?
  context: DefaultPack.exe is installed when a user leaves a default-search-provider option checked during certain
    software installs, so it appears on ordinary endpoints but rarely spawns child processes during normal use.
    Knowing whether this host and user routinely run it helps separate a benign installer artifact from proxy
    execution.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What is the child process and what did its command line do? Decode any flags, paths, or URLs.
  context: DefaultPack.exe accepts a child command via a switch and passes arguments through to the launched
    program, so the child's full command line shows the operational intent. Decode any encoded content, script
    paths, DLL entrypoints, or an HTTP/HTTPS URL passed to the child.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: template:cue_parent_process.mechanism_decode
  question: What spawned DefaultPack.exe itself, and was that parent chain expected?
  context: The grandparent reveals how DefaultPack.exe came to run. A launch from explorer.exe or an installer is
    consistent with ordinary software setup, while a launch from a script interpreter, an Office application, or a
    remote-access tool points to the proxy being driven by another stage of activity.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1218.image_load
  question: What modules did the child process load, and were any unsigned or loaded from a user-writable path?
  context: Proxy execution through a signed binary is often used to load an externally supplied payload into the
    launched child. Modules loaded from user-writable directories or modules without a valid signature are the
    artifact that distinguishes a benign program launch from a malicious DLL or stager being executed under the
    child.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.network_connection
  question: Did the child process make any network connections, and to what destinations?
  context: A program launched through the signed proxy may retrieve a payload or establish command-and-control.
    Outbound connections from the child — especially to external addresses or on uncommon ports — indicate the
    proxied program is fetching or communicating with remote infrastructure rather than performing local-only work.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_parent_process.baseline
  question: Does DefaultPack.exe normally spawn this child binary on this host?
  context: Some parent-child pairs are routine, but DefaultPack.exe launching another program is uncommon outside
    of its initial install behavior. A zero or very low historical count for this specific parent-child pairing
    marks it as a novel spawning relationship worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_parent_process.follow_on
  question: What other child processes did DefaultPack.exe spawn on this host?
  context: A proxy binary may be used to launch several programs in sequence — reconnaissance, credential access,
    or follow-on payloads. Listing every child of DefaultPack.exe reveals the full scope of what was launched
    through this execution path beyond the one that triggered the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.scope
  question: Is DefaultPack.exe spawning this same child binary on other hosts?
  context: The same parent-child pairing appearing across multiple hosts can indicate a software-deployment
    artifact or a repeated proxy-execution pattern pushed to many endpoints. A single occurrence is more likely
    targeted or host-local.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \DefaultPack.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \DefaultPack.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple
    endpoints. If the same user triggered similar activity elsewhere, the scope may be larger than this single
    host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \DefaultPack.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining process activity around the alert reveals what led to this
    event and what followed, and whether it is part of a larger sequence such as reconnaissance, privilege
    escalation, or persistence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, or credential access — may
    reveal the stages that preceded this event and help reconstruct the timeline leading up to the proxy
    execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: Tampering with protection often precedes payload execution. The query surfaces direct attempts to
    disable AV/EDR services, flip protection flags, add broad exclusions, clear logs, or patch in-process
    instrumentation. Zero rows is a useful "protection looks healthy" signal; populated rows shift the disposition
    toward operator preparation, so review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: proxy_invocation
      description: "DefaultPack.exe (trusted signed binary) is invoked and used to launch another program"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: child_command_execution
      description: "The proxied child program executes with command-line arguments passed through the signed parent"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: payload_load
      description: "The child loads an externally supplied or unsigned module (DLL/stager) through the proxy"
      mapped_to: Q5
      source: attack:T1218.image_load
    - stage: 4
      label: network_retrieval
      description: "The child retrieves a payload or communicates with remote infrastructure"
      mapped_to: Q6
      source: attack:T1218.network_connection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_parent_process.mechanism_decode
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q7
      source: template:cue_parent_process.baseline
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (profile unexpected parent-child relationships)"
      disposition: context
      target_question: Q4
      notes: "Parent-child relationship is the rule's whole detection; the grandparent chain and parent-child baseline questions cover this dimension."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (msiexec.exe, regsvr32.exe example list — not applicable to this rule's fixed parent)"
      disposition: context
      target_question: Q5
      notes: "The signed-binary allowlist is a tuning concept for the technique broadly; here the proxy binary is fixed (DefaultPack.exe). Signature/path scrutiny applied to the loaded child module instead."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (URLs, script paths, DLL entrypoints)"
      disposition: context
      target_question: Q3
      notes: "Rule has no commandline_values; child command line is entity-scoped and decoded manually in mechanism_decode. URL/script/DLL-entrypoint cues described in context."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (suppress legitimate enterprise domains)"
      disposition: context
      target_question: Q6
      notes: "Enterprise-domain allowlisting is environment-coupled tuning, not queryable at authoring time; child network destinations surfaced for analyst review."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue (ParentImage in detection) — subsumes process_chain; per hint"
      kept: cue_parent_process.mechanism_decode
    - dropped: cue_parent_process.clustering
      reason: "host-time clustering of parent activity overlaps cue_host_context.clustering (same host, +/-15m); host_context version kept for the broader sweep"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "host-binary baseline overlaps cue_parent_process.baseline which is more specific (parent+child pair, not child alone)"
      kept: cue_parent_process.baseline
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted"
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted"
      kept: both
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1218) is not covered — pull in cue_image_load.* or author a custom question with category: image_load"
      disposition: acted_on
      reason: >-
        Acted on — authored attack:T1218.image_load (Q5) with category: image_load,
        scoping to the alerting child via ProcessGuid and surfacing loaded modules,
        signature, and load path. This is the payload-load chain stage for proxy
        execution.
    - hint: "logsource 'network_connection' (recommended by techniques: T1218) is not covered — pull in cue_network_endpoint.* or author a custom question with category: network_connection"
      disposition: acted_on
      reason: >-
        Acted on — authored attack:T1218.network_connection (Q6) with category:
        network_connection, scoping to the alerting child via ProcessGuid and
        surfacing destination IP/port/hostname. This is the network-retrieval chain
        stage for proxy execution.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 12
    logsource_diversity:
      process_creation: 13
      image_load: 1
      network_connection: 1
      sigma_alert: 1
