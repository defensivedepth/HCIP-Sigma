name: Using SettingSyncHost.exe as LOLBin
id: 2aa16aba-0485-4810-a2c8-09969e6267b3
description: |
  Detects settingsynchost.exe spawning a child process whose Image does NOT live under
  C:\Windows\System32\ or C:\Windows\SysWOW64\, where the parent command line contains
  cmd.exe /c together with RoamDiag.cmd and -outputpath. settingsynchost.exe can extract
  a batch file from its own resources, write it to a temporary location, and execute it;
  that batch in turn invokes several OS utilities by name without full paths, so an
  attacker who drops a malicious binary named after one of those utilities can have it
  executed through the trusted parent — a search-order / path-interception hijack. The
  investigation should determine whether the child binary is a legitimate system utility
  that happens to live outside the system directories or an attacker-planted file riding
  the trusted parent chain.
type: detection
detection_id: b2ddd389-f676-4ac4-845a-e00781a48e5f
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Did settingsynchost.exe spawn a child binary located outside System32/SysWOW64 with a RoamDiag.cmd / -outputpath parent command line?
  context: Confirm the alert is a true positive before investing further. The rule fires when the parent command line contains cmd.exe /c together with RoamDiag.cmd and -outputpath AND the child process Image does not begin with C:\Windows\System32\ or C:\Windows\SysWOW64\. Read the full ParentCommandLine and the child Image and verify both conditions hold; a child binary running from a system directory would not have triggered this rule.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentCommandLine
      - Image
- source: template:universal.host_user_context
  question: Is this user and host expected to run diagnostic or settings-sync tooling that extracts and executes batch helpers?
  context: Legitimate diagnostic workflows can invoke settingsynchost.exe with these switches. Knowing whether the user role and host function are consistent with running settings-sync diagnostics is often the fastest way to separate routine activity from a path-interception hijack.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:parent_child.parent_process_legitimacy
  question: What spawned settingsynchost.exe, and is that grandparent chain expected?
  context: The parent-child relationship is the core signal for this detection. Tracing the origin of the cmd.exe /c RoamDiag.cmd parent reveals whether the chain began with a user action, a scheduled or pushed task, or an unexpected process such as a script interpreter or remote-access tool — the latter pointing to operator-driven abuse of the trusted binary.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
      - ParentCommandLine
- source: template:cue_parent_process.mechanism_decode
  question: Decode the parent command line — what diagnostic script and output path did it specify?
  context: The parent command line carries the cmd.exe /c RoamDiag.cmd and -outputpath arguments. Decode the full argument list to identify the exact script name, the output path it was told to use, and any additional switches. The extracted batch invokes several OS utilities by name without full paths (for example wevtutil, makecab, reg, ipconfig), so the output and working directory indicate where a hijacking binary could be planted to be picked up by name.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the hijacked child binary do? Decode its command line and working directory.
  context: The child binary runs from a non-system directory through the trusted parent chain. Decode its full command line, flags, and working directory to determine what action it performed. Scoping to the child Image on this host within a short window captures any sequential invocations that together form a single operation rather than only the triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1574.008.file_path_analysis
  question: Was the child binary written or its data streams created in the directory it executed from shortly before it ran?
  context: A search-order hijack requires the attacker to first plant a binary named after a legitimate utility in a directory that is not normally write-protected, then have it executed by name. File creation and alternate-data-stream events for that path establish whether the binary was dropped rather than installed by a trusted process, and which process wrote it. If this stream/file telemetry is not collected in this environment, treat the absence as a visibility gap rather than evidence the binary was always present.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: create_stream_hash
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
      - CommandLine
- source: attack:T1574.008.time_correlation
  question: Was a file write to the child binary's path correlated in time with its execution on this host?
  context: Path-interception hijacks pair a recent file write of the malicious binary with its subsequent execution. A file creation event for the binary's path occurring minutes before the process started — rather than days or weeks earlier as part of a software install — supports a deliberate drop-then-run sequence. The correlation window between the write and the execution is the discriminator.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:parent_child.sibling_processes
  question: What other child processes did the same settingsynchost.exe parent spawn?
  context: If the parent chain spawned the detected child, it may have spawned other processes too. Examining siblings of the same parent reveals the full scope of activity initiated through the trusted binary — multiple non-system children from the same parent points to orchestrated abuse rather than a one-off.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has settingsynchost.exe spawned this child binary on this host before?
  context: Some parent-child pairings are routine; this specific combination — settingsynchost.exe driving the RoamDiag diagnostic batch to launch a binary outside the system directories — is unusual. Zero or very low historical counts for this parent-child pairing indicate a novel spawning relationship worth treating as the deviation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run the child binary from this non-system path?
  context: A binary that has never run on this host, or runs only from a non-standard directory, is more suspicious than one that runs regularly from its expected install location. Zero or very low historical counts indicate first-time or rare execution consistent with a planted file.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      system_utility:
        Image|startswith:
        - C:\Windows\System32\
        - C:\Windows\SysWOW64\
      parent_is_settingsynchost:
        ParentCommandLine|contains|all:
        - cmd.exe /c
        - RoamDiag.cmd
        - -outputpath
      _pb_scope:
        User|expand: '%User%'
      condition: (not system_utility and parent_is_settingsynchost) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      system_utility:
        Image|startswith:
        - C:\Windows\System32\
        - C:\Windows\SysWOW64\
      parent_is_settingsynchost:
        ParentCommandLine|contains|all:
        - cmd.exe /c
        - RoamDiag.cmd
        - -outputpath
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (not system_utility and parent_is_settingsynchost) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar activity on several hosts in the same window, the scope of the activity may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      system_utility:
        Image|startswith:
        - C:\Windows\System32\
        - C:\Windows\SysWOW64\
      parent_is_settingsynchost:
        ParentCommandLine|contains|all:
        - cmd.exe /c
        - RoamDiag.cmd
        - -outputpath
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (not system_utility and parent_is_settingsynchost) and _pb_scope
    fields:
    - host.name
- source: template:cue_parent_process.scope
  question: Is this settingsynchost.exe spawning pattern occurring on other hosts?
  context: The same parent-child relationship appearing on multiple hosts may indicate a broader operation or a repeated technique rather than a one-off. Enterprise-wide comparison helps calibrate the scope and identify additional affected hosts.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
      - CommandLine
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to persistence locations (Startup folder, Run key paths) on this host?
  context: Path interception that hijacks a trusted parent chain is often paired with a separate, more durable persistence mechanism. Dropping an executable or script into a Startup folder or a path referenced by a registry Run key achieves persistence without running standard persistence tools, and file-event monitoring captures these drops even when the attacker avoids schtasks, sc, or reg.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — reconnaissance, file drops, defense tampering, or credential access — may reveal the stages that preceded this execution and help reconstruct the timeline leading to the hijack.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1574.008
      name: Path Interception by Search Order Hijacking
      role: primary
    - id: T1574
      name: Hijack Execution Flow
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: hijack_binary_planted
      description: "Attacker writes a binary named after a legitimate utility into a non-system / non-write-protected directory"
      mapped_to: Q6
      source: attack:T1574.008.file_path_analysis
    - stage: 2
      label: execution_via_trusted_parent
      description: "settingsynchost.exe drives the RoamDiag batch which invokes the planted binary by name from a non-system path"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: write_to_execution_correlation
      description: "File write of the binary is correlated in time with its subsequent execution"
      mapped_to: Q7
      source: attack:T1574.008.time_correlation
  dimension_coverage:
    - dimension: file_path_analysis
      technique: T1574.008
      covered_by: Q6
      source: attack:T1574.008.file_path_analysis
      coverage_type: direct
    - dimension: time_correlation
      technique: T1574.008
      covered_by: Q7
      source: attack:T1574.008.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1574.008
      covered_by: Q9
      source: template:cue_parent_process.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: SuspiciousBinaryList
      dimension: tuning_parameter
      specific_values: "net.exe, cmd.exe, powershell.exe, python.exe (rationale-level examples)"
      disposition: context
      target_question: Q5
      notes: "Rule does not constrain the child binary name (it constrains by path: NOT under System32/SysWOW64); the child Image is parameterized via %Image%, so the alerting binary is whatever fired. List is illustrative, not a queryable constraint here."
    - element: MonitoredDirectories
      dimension: file_path_analysis
      specific_values: "application folders, user profile subdirs (non-write-protected dirs)"
      disposition: query_injection
      target_question: Q6
      notes: "Captured via create_stream_hash / file_event scoped to the child binary's path (TargetFilename|endswith %Image%) on the host."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — correlation window between file creation and execution"
      disposition: query_injection
      target_question: Q7
      notes: "+/-4h file_event window scoped to the binary path establishes write-to-execution correlation."
    - element: ParentProcessBaseline
      dimension: tuning_parameter
      specific_values: "expected parent processes for critical binaries"
      disposition: query_injection
      target_question: Q9
      notes: "Parent+child baseline over 90d (Q9) and parent+binary env-wide baseline (Q12) surface deviations from expected parent."
    - element: ServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only (T1574 secondary — registry/service paths)"
      disposition: context
      target_question: Q17
      notes: "Rule is process_creation path-interception, not service/registry hijack; secondary T1574 registry_set dimension not directly queryable here. Prior-alert sweep (Q17) would surface any related service/registry-set detections."
    - element: AllowedDllPaths
      dimension: tuning_parameter
      specific_values: "rationale only (T1574 secondary — DLL load paths)"
      disposition: context
      target_question: Q5
      notes: "Rule fires on process execution from a non-system path, not on DLL search-order load; image_load/DLL-path dimension is out of scope for the executable-hijack this rule detects."
  dedup_decisions:
    - dropped: cue_parent_process.follow_on
      reason: "subsumed by parent_child.sibling_processes (Q8) — both enumerate children of the same parent; technique-set version is more specific"
      kept: parent_child.sibling_processes
    - dropped: cue_parent_process.clustering
      reason: "subsumed by cue_host_context.precursor (Q17) and cue_parent_process.scope (Q15) — around-the-alert host context and cross-host scope already covered"
      kept: cue_parent_process.scope
    - dropped: persistence_scope.process_based_persistence
      reason: "this hijack plants and runs a file rather than invoking schtasks/sc/reg; file_based_persistence (Q16) is the relevant parallel-mechanism check for a binary-drop hijack"
      kept: persistence_scope.file_based_persistence
    - kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
    - kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
  skipped_templates:
    - template: cue_process_tool.process_chain
      reason: "hint: parent_process is primary — drop cue_process_tool:process_chain (parent cue subsumes it; parent_child.parent_process_legitimacy Q3 covers the parent chain)"
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include; rule fires on a path-interception execution, not a credential-access/impact action where AV/EDR-disable-as-precursor is the live question. Prior-alert sweep (Q17) would surface any tamper detections that preceded this event; no defense_tampering set is confirmed."
    - template: organizational_context.change_ticket
      reason: "RECOMMENDED org-context filler; coverage of authorization/legitimacy is provided by host_user_context (Q2) and the parent-origin chain (Q3). Held to keep the playbook within the 14-18 window without padding."
    - template: organizational_context.human_recall
      reason: "RECOMMENDED org-context filler; legitimacy/recall is addressed by host_user_context (Q2). Held to keep the playbook within the 14-18 window."
    - template: organizational_context.timing_alignment
      reason: "RECOMMENDED org-context filler; timing is covered analytically by the write-to-execution correlation (Q7) and prior-alert timeline (Q17). Held to keep within the 14-18 window."
    - template: organizational_context.procedural_consistency
      reason: "RECOMMENDED org-context filler; procedural-norm coverage overlaps host_user_context (Q2). Held to keep within the 14-18 window."
  hint_dispositions:
    - hint: "parent_process is primary — drop cue_process_tool:process_chain"
      disposition: acted-on
      reason: "cue_process_tool.process_chain skipped (see skipped_templates); parent chain covered by parent_child.parent_process_legitimacy (Q3) and cue_parent_process.mechanism_decode (Q4)."
    - hint: "logsource 'create_stream_hash' (recommended by techniques: T1574.008) is not covered and no template family exists — author a custom question with category: create_stream_hash"
      disposition: acted-on
      reason: "Authored custom question Q6 (attack:T1574.008.file_path_analysis) with logsource category: create_stream_hash, scoped to the child binary path via TargetFilename|endswith %Image%, to capture the planted-binary stream/file creation that the process_creation queries cannot see."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - persistence_scope
    - parent_child
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 13
      file_event: 2
      create_stream_hash: 1
      sigma_alert: 1
