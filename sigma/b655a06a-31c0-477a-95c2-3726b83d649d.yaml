name: Suspicious Userinit Child Process
id: 56e33801-aa64-421a-9fb2-fd8f13f659ff
description: |
  Detects a process whose parent is userinit.exe but which is not one of the
  expected userinit children. At logon, winlogon.exe launches userinit.exe, which
  normally spawns the user shell (explorer.exe) and then exits; it may also run
  domain logon scripts from the netlogon share. The rule fires when userinit.exe
  spawns a child whose Image does not end in explorer.exe, whose OriginalFileName
  is not explorer.exe, whose command line is not the standard Explorer invocation,
  and whose command line does not reference a netlogon path. An unexpected child of
  userinit is a known foothold for execution and persistence — a hijacked Userinit
  registry value, a logon-time loader, or code running in the userinit context to
  gain a logon-session foothold. The investigation should confirm which child fired,
  decode what it did, trace how userinit was started, and determine whether the
  child loaded planted modules or reached into other processes.
type: detection
detection_id: b655a06a-31c0-477a-95c2-3726b83d649d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-08
questions:
- source: template:universal.verify_trigger_match
  question: Is the parent userinit.exe, and is the child something other than explorer.exe or a netlogon logon script?
  context: Confirm the alert is real before investing time. The parent Image should end in
    userinit.exe. The child should not be explorer.exe (by Image, OriginalFileName, or the
    standard Explorer command line) and its command line should not reference a netlogon
    path — those are the expected logon children the rule filters out. Note the exact child
    Image and command line; the rest of the investigation pivots on what that child is.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
- source: template:universal.host_user_context
  question: Is this user and host expected to run this child binary at logon under userinit?
  context: A child of userinit runs in the logon path of whoever is signing in. An
    administrator or IT-managed host running a known logon helper is far less concerning
    than the same binary appearing under a standard user or in an environment with no logon
    automation. Knowing whether the account and host normally see logon-time custom
    execution is often the fastest way to separate a sanctioned helper from a hijack.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did the userinit child do? Decode its command-line arguments, flags, and any file paths or encoded content.
  context: The full command line of the child reveals the action taken in the logon context.
    Decode any flags, encoded payloads, script paths, or downloaded files. A child running
    from a user-writable or temporary directory, invoking a script host, or carrying an
    encoded command is the operational payload; the userinit parentage is only how it gained
    a logon-session foothold.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_parent_process.mechanism_decode
  question: What started userinit.exe itself, and is that chain the normal winlogon logon path?
  context: userinit.exe is normally launched by winlogon.exe during an interactive logon and
    exits quickly after starting the shell. Tracing what spawned this userinit instance shows
    whether it sits on a genuine logon (winlogon parent) or was launched out of band by
    another process to abuse the userinit context. A userinit whose parent is not winlogon, or
    that runs long after the logon, is itself an anomaly.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1055.image_load
  question: What modules did the userinit child load, and were any unsigned or loaded from a user-writable or non-standard path?
  context: Injection and loader behavior frequently surface as a module loaded from outside
    the normal system directories. Reviewing the DLLs the child loaded distinguishes a clean
    binary from one that side-loaded a planted module or mapped attacker code into its own
    address space. A module under a user profile, a temporary directory, or any user-writable
    path, or an unsigned module loaded by a binary that should only load system libraries, is
    the artifact worth chasing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
- source: attack:T1055.access_verification
  question: Did the userinit child open a handle into another process with memory-write or thread-injection rights?
  context: Process injection requires the source process to acquire a handle into a target
    with rights such as PROCESS_VM_WRITE, PROCESS_VM_OPERATION, or PROCESS_CREATE_THREAD —
    commonly seen as a granted-access mask of 0x1F0FFF (full access) or 0x1438. The
    high-value injection targets are long-lived processes such as explorer.exe, lsass.exe,
    and svchost.exe. A handle-open from the userinit child into one of those processes with
    write or thread-creation rights, correlated in time with the child's execution, is direct
    evidence the child is an injector rather than a benign logon helper.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        SourceImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetImage
      - GrantedAccess
      - CallTrace
- source: llm_generated
  question: What other processes did userinit spawn on this host around the logon, and did any besides explorer.exe appear?
  context: A normal userinit spawns explorer.exe and exits. Surfacing every child of userinit
    in the surrounding window shows whether this alert is a single anomalous child or one of
    several, which would indicate a logon-time loader launching multiple stages. Children other
    than explorer.exe, or repeated userinit spawns, point to deliberate use of the logon path.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: \userinit.exe
      condition: selection
    fields:
      - Image
      - CommandLine
- source: llm_generated
  question: Was the Userinit registry value modified on this host before the alert?
  context: The set of programs userinit runs at logon is governed by a registry value under
    the Winlogon key. Appending a binary to that value is a classic logon persistence
    mechanism — the added program then runs as a child of userinit on every logon. A
    process-creation write to that value through reg.exe, cmd, or PowerShell shortly before
    the alert ties the anomalous child to a deliberate persistence change. If registry-set
    events are not ingested in this environment, this process-creation view catches only the
    writes performed through command-line tools.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\reg.exe'
          - '\cmd.exe'
          - '\powershell.exe'
          - '\pwsh.exe'
        CommandLine|contains:
          - 'Winlogon'
          - 'Userinit'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:cue_user_actor.follow_on
  question: What did this user do next on this host after the alert?
  context: Follow-on activity reveals what the logon-context foothold was used for. Look for
    privilege escalation, persistence installation, credential access, data collection, or
    lateral movement by the same user in the hours after the detected child ran.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        User|expand: '%User%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.follow_on_svc
  question: What other launches of this child binary on this host followed in the next four hours, and does the parent or command line change?
  context: Under a service principal, "what did this user do next" is unbounded. A more focused
    follow-on for automation-driven alerts is to track repeat invocations of the same binary on
    the same host. Routine logon helpers repeat with stable parents and command-line structures; a
    deviation in parent or command line in the follow-on window is the signal worth chasing.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ParentImage
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \userinit.exe
      filter_main_netlogon:
        CommandLine|contains: \netlogon\
      filter_main_explorer:
      - Image|endswith: \explorer.exe
      - OriginalFileName: explorer.exe
      - CommandLine: C:\Windows\Explorer.EXE
      filter_main_null:
        Image: null
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \userinit.exe
      filter_main_netlogon:
        CommandLine|contains: \netlogon\
      filter_main_explorer:
      - Image|endswith: \explorer.exe
      - OriginalFileName: explorer.exe
      - CommandLine: C:\Windows\Explorer.EXE
      filter_main_null:
        Image: null
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the account's activity across all hosts in the surrounding window reveals
    lateral movement or coordinated activity on multiple endpoints. If the same user triggered
    similar logon-time activity elsewhere, the scope is larger than this one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \userinit.exe
      filter_main_netlogon:
        CommandLine|contains: \netlogon\
      filter_main_explorer:
      - Image|endswith: \explorer.exe
      - OriginalFileName: explorer.exe
      - CommandLine: C:\Windows\Explorer.EXE
      filter_main_null:
        Image: null
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection and not 1 of filter_main_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.scope
  question: Are there other alerts for this user on other hosts?
  context: If the same user triggered alerts on multiple hosts, the activity may extend beyond a
    single endpoint. Check for lateral movement patterns or credential abuse across the
    environment. If the user field is unavailable in alert data, fall back to process activity
    scoped by user.
  range: -24h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - rule.name
      - event.severity_label
- source: template:cue_user_actor.scope_svc
  question: Did this same rule fire on other hosts in the past 24 hours, and does the cross-host pattern look like coordinated automation or an isolated detection?
  context: Under a service principal the user is omnipresent, so "alerts for this user" is
    meaningless. The triage-useful question is whether this detection fires on multiple hosts. A
    rule firing across many hosts on the same day is probably tracking a pushed logon script or
    deployed package; a rule firing on one host alone is the deviation worth chasing.
  range: -24h
  query: |
    aggregation: true
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.name|expand: '%rule.name%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Surveying activity around the alert reveals what led
    to the logon-time execution and what followed — reconnaissance, persistence, or escalation.
    Aggregating by process and parent collapses repetitive agent telemetry into high-count rows
    while novel parent-child pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense
    tampering, reconnaissance, or credential access — may reveal the stages that preceded this
    event and help reconstruct the timeline leading up to the logon-context execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1055
      name: Process Injection
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: userinit_invocation
      description: "userinit.exe is started during logon (normally by winlogon.exe) and spawns its children"
      mapped_to: Q4
      source: template:cue_parent_process.mechanism_decode
    - stage: 2
      label: anomalous_child_execution
      description: "userinit spawns a child that is not explorer.exe and not a netlogon logon script — the detection point"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: child_payload_action
      description: "The anomalous child executes its operational payload (decode command line, loaded modules, cross-process injection)"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
  dimension_coverage:
    - dimension: access_verification
      technique: T1055
      covered_by: Q6
      source: attack:T1055.access_verification
      coverage_type: direct
    - dimension: time_correlation
      technique: T1055
      covered_by: Q17
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1055
      covered_by: Q6
      source: attack:T1055.access_verification
      coverage_type: direct
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF (full access), 0x1438; PROCESS_VM_WRITE / PROCESS_VM_OPERATION / PROCESS_CREATE_THREAD"
      disposition: context
      target_question: Q6
      notes: "access_verification question is a custom attack: question on process_access. GrantedAccess is projected as a field rather than filtered in the selection so the analyst sees every handle-open and reads the mask; the specific masks (0x1F0FFF, 0x1438) and rights are named in context. A selection filter would hide handle-opens with non-listed masks that may still be injection."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — correlation window between handle-open/memory-write API calls and thread creation"
      disposition: context
      target_question: Q17
      notes: "Folded into the +/-15m host-context window and the +/-5m process_access/image_load windows; the brief gives no concrete threshold value, so this is tuning rationale rather than a queryable value."
    - element: InjectedProcessList
      dimension: tuning_parameter
      specific_values: "lsass.exe, explorer.exe, svchost.exe (common high-value injection targets)"
      disposition: context
      target_question: Q6
      notes: "Named in the access_verification context as the high-value targets to look for in TargetImage. Not pinned in the selection because constraining TargetImage would suppress injection into other targets; the analyst reads TargetImage from the projected field."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process cue active (cue_parent_process.mechanism_decode included) — the parent lookup subsumes process_chain; both scope by ParentProcessGuid. Per brief hint and AGENTS.md dedup rule."
      kept: cue_parent_process.mechanism_decode
  skipped_templates:
    - template: cue_parent_process.baseline
      reason: "near-duplicate of cue_user_actor.baseline_svc — both aggregate process.parent.name + Image|endswith over -90d to test whether the parent-child pair is historically routine. baseline_svc is a mandatory non-deduplicable service-principal pair."
    - template: cue_process_tool.baseline
      reason: "near-duplicate of cue_user_actor.baseline_svc / baseline coverage of historical binary frequency; host-level binary frequency is subsumed by the parent+binary baseline (baseline_svc) and the user+binary baseline. Dropped to stay within the 14-18 budget while keeping the non-deduplicable user-actor pairs."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. Dropped per its own notes_to_llm condition (b): cue_host_context.precursor is present (prior-alert tamper context) AND this rule is not an execution/credential-access rule whose disposition turns on whether protection was weakened first — it detects an anomalous logon child, and the tamper-as-precursor question is not load-bearing here. precursor (Q18) carries any prior tamper signal as a fired detection."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1055) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: acted-on
      reason: >-
        Acted on — authored attack:T1055.image_load (Q5) with category: image_load,
        querying modules loaded by the userinit child (SourceImage scoped via Image|endswith).
        Listed here for the audit trail; the action is visible in the playbook.
    - hint: "logsource 'process_access' (recommended by techniques: T1055) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: acted-on
      reason: >-
        Acted on — authored attack:T1055.access_verification (Q6) with category: process_access,
        querying handle-opens by the userinit child into other processes. This also covers the
        access_verification dimension hint.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 18
    source_counts:
      template: 14
      attack: 2
      llm_generated: 2
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      image_load: 1
      process_access: 1
      sigma_alert: 2
