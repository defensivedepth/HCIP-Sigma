name: Renamed Sysinternals Sdelete Execution
id: a6ad7325-3f70-42dd-8f36-84184a22837a
description: |
  Detects a process whose PE metadata identifies it as SysInternals SDelete (OriginalFileName "sdelete.exe") while the
  on-disk name is something other than sdelete.exe or sdelete64.exe. SDelete is a legitimate secure-deletion utility that
  overwrites file contents to the DoD 5220.22-M pattern and renames each target 26 times before unlinking it, but it is
  also renamed and used by destruction-oriented operators to wipe victim files, dropped tooling, and logs while evading
  name-based detection. Determine whether the renamed copy and its execution context are consistent with sanctioned
  administrative wiping, or whether the deletion/overwrite activity is part of impact-stage data destruction.
type: detection
detection_id: c1d867fe-8d95-4487-aab4-e53f2d339f90
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the executing binary carry the SDelete OriginalFileName while running under a non-standard on-disk name?
  context: Confirm the alert is a true positive before investing investigation time. The rule fires when the PE
    OriginalFileName resolves to sdelete.exe AND the image on disk is named something other than sdelete.exe or
    sdelete64.exe. Review the alerting event and verify both halves hold for this process.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - OriginalFileName
- source: template:masquerading.true_identity
  question: What is the actual identity of this binary based on its metadata and hash?
  context: When SDelete is renamed, the PE metadata (OriginalFileName sdelete.exe) and the code-signing chain still reflect
    the original Microsoft Sysinternals tool even though the name on disk differs. Compare the Image path against
    OriginalFileName to confirm the mismatch, and use the signing status and file hash to determine whether this is an
    unmodified Microsoft-signed SDelete copy, a tampered build, or an unrelated binary impersonating the metadata.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this renamed SDelete invocation do? Decode the full command line and its flags.
  context: The command line reveals the deletion scope and aggressiveness. Decode each flag and target. Notable switches
    are -s (recurse subdirectories), -r (clear read-only), -p N (overwrite passes, default 1), -c (clean free space), -z
    (zero free space), -q (quiet), and -nobanner. A bare drive letter or a recursive path target indicates broad wiping;
    free-space cleansing (-c/-z) is an anti-forensic pattern aimed at destroying already-deleted content. Pull every
    SDelete-identity execution on this host in the window, since wiping operations often run as multiple sequential
    invocations that together form one logical operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:universal.host_user_context
  question: Is this user and host expected to securely wipe files or free space with a renamed deletion utility?
  context: Legitimate secure-deletion activity is usually tied to a known administrative or decommissioning workflow on a
    specific role of host. Knowing whether the user role and host function are consistent with running a renamed SDelete
    copy is often the fastest way to separate sanctioned wiping from impact-stage destruction. The deliberate renaming
    itself is unusual for routine administration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:masquerading.binary_origin
  question: How did this renamed SDelete binary arrive on the host?
  context: A renamed copy of SDelete does not appear spontaneously. Tracing the file-creation event for this binary by
    basename over a long window reveals the delivery mechanism, such as a download tool, an archive extraction, a manual
    copy via remote access, or a malware dropper. A copy that landed in a user-writable location (Desktop, Downloads,
    Temp, AppData) was likely staged for this operation; one placed by an installer in a system directory may reflect a
    sanctioned deployment.
  range: -30d
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_name%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this renamed SDelete and is that parent chain expected?
  context: The parent process shows how the wiping tool was invoked. Interactive administrative use typically descends from
    explorer.exe or a console shell, while invocation from a script host, a remote-execution service, or an Office
    application points to automated or operator-driven destruction. Walk up to the parent to see what launched it and what
    else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1485.file_delete
  question: What files did this process delete or rename on the host, and do they form a mass-overwrite pattern?
  context: SDelete renames each target file 26 times (AAA.AAA, BBB.BBB ... ZZZ.ZZZ) before unlinking it, so a single wipe
    produces a dense burst of rename and delete events on the filesystem. A high volume of deletions concentrated in
    sensitive locations such as System32, user document directories, database paths, or backup/log directories indicates
    destruction of data rather than cleanup of one stray file. Correlate the deletion timing with the alerting process to
    confirm this binary is the deleter.
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: file_delete
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
- source: template:ransomware_impact.destruction_scope_on_host
  question: What other destructive or recovery-inhibiting actions occurred on this host?
  context: 'Destruction-stage operators typically chain several actions: wiping files, deleting shadow copies, disabling
    recovery, stopping security services, and clearing event logs. Finding the complete set of destructive commands around
    the alert reveals whether the renamed SDelete run is one isolated administrative action or part of a coordinated impact
    sequence. A shared parent across these commands points to scripted execution.'
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_destructive:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\cipher.exe'
          - '\fsutil.exe'
          - '\icacls.exe'
          - '\takeown.exe'
          - '\attrib.exe'
          - '\sc.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\powershell.exe'
          - '\taskkill.exe'
      condition: selection and selection_destructive
    fields:
      - Image
      - User
- source: template:ransomware_impact.recovery_options_assessment
  question: What recovery-inhibiting commands were executed on this host?
  context: 'Destruction operators chain multiple recovery-inhibiting commands: deleting shadow copies (vssadmin, wmic),
    disabling boot recovery (bcdedit), deleting backups (wbadmin), corrupting boot.ini, and disabling the Windows Recovery
    Environment (reagentc). This query surfaces those commands on the host so the analyst can gauge the full scope of
    recovery destruction, then verify whether each action succeeded by checking system state (vssadmin list shadows,
    bcdedit /enum, reagentc /info).'
  range: +24h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recovery:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\reagentc.exe'
          - '\diskshadow.exe'
      selection_cmdline:
        CommandLine|contains:
          - 'delete shadows'
          - 'shadowcopy delete'
          - 'recoveryenabled'
          - 'bootstatuspolicy'
          - 'delete catalog'
          - 'delete systemstatebackup'
          - 'boot.ini'
          - 'winre.wim'
          - '/disable'
      condition: selection and (selection_recovery or selection_cmdline)
    fields:
      - Image
      - CommandLine
- source: template:ransomware_impact.encryption_or_ransom_indicators
  question: Are there signs of file encryption, ransom notes, or unknown binary execution on this host after the wipe?
  context: Secure-deletion and recovery destruction frequently precede or accompany file encryption by minutes. Checking
    for unknown or suspicious binaries executing after the destructive action, especially from temp or user-writable
    paths, determines whether an encryption phase followed the wipe. The analyst should also check for ransom notes
    (README, HOW_TO_DECRYPT, unusual file extensions) via file-creation events or the endpoint console.
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      filter_known:
        Image|startswith:
          - 'C:\Program Files'
          - 'C:\Windows\System32\svchost'
          - 'C:\Windows\System32\wbem'
      condition: selection and not filter_known
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this renamed-SDelete pattern, what parents and users drove it, and does the
    current alert fit?
  context: Even legitimate tools get abused. This query mirrors the rule's selection (SDelete OriginalFileName, excluding
    the canonical sdelete.exe/sdelete64.exe names) scoped to this host, so the resulting parent and user distribution
    reflects how this specific firing pattern has historically run here rather than generic binary usage. A new parent
    process, an unexpected user account, or a first-time appearance is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: sdelete.exe
      filter:
        Image|endswith:
        - \sdelete.exe
        - \sdelete64.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: sdelete.exe
      filter:
        Image|endswith:
        - \sdelete.exe
        - \sdelete64.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: sdelete.exe
      filter:
        Image|endswith:
        - \sdelete.exe
        - \sdelete64.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether destruction is isolated to one endpoint or spans
    several. The same user triggering wiping or destructive activity on multiple hosts in the window widens the scope of
    compromise and the containment footprint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        OriginalFileName: sdelete.exe
      filter:
        Image|endswith:
        - \sdelete.exe
        - \sdelete64.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:ransomware_impact.network_wide_impact
  question: Is this same renamed-SDelete detection firing on other hosts in the environment?
  context: Destruction operations often execute the same sequence across many hosts simultaneously or in rapid succession.
    Finding this same detection on multiple hosts indicates network-wide execution and determines the scope for
    containment and incident response. The first-to-last detection timeline reveals the spread pattern.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.uuid|expand: '%rule.uuid%'
      condition: selection
    fields:
      - host.name
      - rule.name
      - event.severity_label
- source: template:masquerading.renamed_binary_activity
  question: What did this renamed binary do after execution, and is the same renamed copy present on other hosts?
  context: The point of renaming a tool is to keep performing its function while evading name-based detection. Examining
    the child processes and follow-on activity spawned by this binary reveals what the wiping operation actually touched,
    and the same renamed copy appearing on multiple hosts suggests deliberate distribution rather than an isolated
    incident.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1485
      name: Data Destruction
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: destruction_tool_execution
      description: "Adversary spawns a command-line tool (here a renamed SDelete) to recursively delete or overwrite user/system files"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: mass_file_overwrite_delete
      description: "Abnormal file IO: dense rename/delete bursts (SDelete's 26-rename pattern) and overwrites in sensitive directories"
      mapped_to: Q7
      source: attack:T1485.file_delete
    - stage: 3
      label: recovery_and_defense_tampering
      description: "Correlated tampering in critical directories: shadow-copy/backup/recovery destruction and security-service stops surrounding the wipe"
      mapped_to: Q8
      source: template:ransomware_impact.destruction_scope_on_host
  dimension_coverage:
    - dimension: command_analysis
      technique: T1485
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: network_indicators
      technique: T1485
      covered_by: Q16
      source: template:ransomware_impact.network_wide_impact
      coverage_type: indirect
    - dimension: time_correlation
      technique: T1485
      covered_by: Q8
      source: template:ransomware_impact.destruction_scope_on_host
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1485
      covered_by: Q7
      source: attack:T1485.file_delete
      coverage_type: direct
  mutable_dispositions:
    - element: TargetFilename
      dimension: tuning_parameter
      specific_values: "sensitive locations (System32, Documents, DB paths)"
      disposition: new_question
      target_question: Q7
      notes: "Drove the authored file_delete question (attack:T1485.file_delete); context directs the analyst to sensitive locations. Not hard-coded as a path filter because SDelete's 26-rename scrubbing rotates every basename, so a basename/path filter would miss the renamed artifacts; the query surfaces all deletions by this Image and the prose flags sensitive-location concentration."
    - element: ProcessCommandLine
      dimension: tuning_parameter
      specific_values: "aggressive flags -s, -r, -p, -c, -z, -q"
      disposition: context
      target_question: Q3
      notes: "SDelete flag semantics injected into mechanism_decode context. mechanism_decode is ADAPT_STRUCTURALLY host+binary scope; flags are too variable to hard-filter, so values live in context."
    - element: VolumeThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (threshold of unique deletions in window)"
      disposition: context
      target_question: Q7
      notes: "Not directly queryable; the file_delete question aggregates deletions by the alerting binary so volume becomes visible, and context tells the analyst high concentrated counts indicate mass overwrite."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlate rapid delete/overwrite from same process/user)"
      disposition: context
      target_question: Q8
      notes: "Realized as the +/-30m correlation window on destruction_scope_on_host and the file_delete question; context frames the burst-timing correlation."
  dedup_decisions:
    - dropped: cue_process_tool.follow_on
      reason: "identical query (host.name + ParentImage|endswith %Image%) to masquerading.renamed_binary_activity; renamed_binary_activity is more specific to a renamed tool and also carries the cross-host scope thread"
      kept: masquerading.renamed_binary_activity
    - dropped: cue_process_tool.scope
      reason: "REFLECTED fleet-pattern count of the rule's selection across hosts overlaps ransomware_impact.network_wide_impact, which pivots on rule.uuid for the same cross-host scope question; kept the alert-correlation form as more direct"
      kept: ransomware_impact.network_wide_impact
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc (both emitted, Q12/Q13)
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc (both emitted, Q14/Q15)
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "host historical-frequency of the binary is subsumed by cue_process_tool.baseline_deviation (host pattern over the rule's actual selection, Q11) and the user baseline (Q12); a generic Image-only 90d count adds no distinct angle for a rule that fires on a renamed image"
    - template: cue_process_tool.clustering
      reason: "what-else-ran-on-host-around-this-time is covered with more specificity by ransomware_impact.destruction_scope_on_host (Q8) and the encryption-indicator sweep (Q10)"
    - template: cue_host_context.clustering
      reason: "host-wide around-the-alert activity is subsumed by ransomware_impact.destruction_scope_on_host (Q8); both aggregate host process activity, destruction_scope_on_host narrows to the destructive-tool set relevant to this impact rule"
    - template: cue_host_context.precursor
      reason: "prior-alert timeline before the event overlaps ransomware_impact.network_wide_impact (Q16) alert correlation; dropped to stay at the 18-question cap while keeping both user_actor pairs intact"
    - template: network_scope.same_action_other_hosts
      reason: "other-hosts-running-same-Image is subsumed by ransomware_impact.network_wide_impact (Q16, rule.uuid pivot) and masquerading.renamed_binary_activity (Q17, cross-host renamed-copy thread)"
    - template: network_scope.actor_activity_network_wide
      reason: "user-network-wide-activity is the same pivot as cue_user_actor.clustering (Q14), which is already emitted as part of the non-deduplicable user_actor pair"
    - template: network_scope.actor_authentication_trail
      reason: "logon-trail (4624/4625) adds a credential-movement angle, but this rule is impact/data-destruction with no credential-access or lateral-movement signal in its detection; dropped to stay at the 18-question cap"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - ransomware_impact
    - masquerading
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 15
      file_event: 1
      file_delete: 1
      sigma_alert: 1
