name: HackTool - Covenant PowerShell Launcher
id: 85e00a07-ee5d-461a-9311-4998bafbf2b8
description: |
  Detects command lines characteristic of the Covenant C2 framework's PowerShell launcher. The rule fires when EITHER a hidden, no-profile PowerShell launch carries an inline or encoded command (the -Sta -Nop -Window Hidden flag combination paired with -Command or -EncodedCommand), OR the command line contains a Covenant-specific signature such as the GruntHTTP stager string, an mshta file.hta launch, the in-memory MemoryStream assignment, or a known encoded payload prefix. Legitimate hidden encoded PowerShell does occur (installers, packaged automation, management agents), so confirm which branch fired, decode the payload, and check whether the user, host, and parent process are expected to run launchers formatted like this. Malicious use loads a .NET implant in memory, so corroborate with script block logging and loaded-assembly evidence rather than relying on the command line alone.
type: detection
detection_id: c260b6db-48ba-4b4a-a76f-2f67644e99d2
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match either branch this rule detects?
  context: >-
    Confirm the alert is a true positive before investing time. The rule fires on EITHER of two branches, and which one
    matched changes what to look at next. Branch one is a hidden, no-profile PowerShell launch carrying a command -- the
    flags -Sta, -Nop, and -Window Hidden together with -Command or -EncodedCommand. Branch two is a Covenant-specific
    signature in the command line such as the GruntHTTP stager string, an mshta file.hta launch, the in-memory
    MemoryStream assignment, or a known encoded payload prefix. Read the full command line and note which branch fired;
    the signature branch is far more specific to the framework than the generic hidden-launch branch.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to launch hidden or encoded PowerShell of this kind?
  context: >-
    Legitimate administrative tooling, installers, and packaged automation sometimes launch PowerShell with hidden
    windows and encoded commands. Knowing whether this user role and host function normally run launchers formatted like
    this is often the fastest way to separate a true positive from routine operations. A workstation belonging to a
    developer or an administrator differs sharply from a server or a kiosk that should never see hand-driven PowerShell.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this launch part of an authorized penetration test or red team engagement?
  context: >-
    Covenant is an open-source command-and-control framework used in authorized security assessments as well as by
    adversaries. Before investing further analysis, check whether a penetration test or red team exercise is active on
    this host or network segment. An authorized engagement whose tooling and timing match resolves the alert without
    deeper investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: Which Covenant launcher variant and capability does the command line indicate?
  context: >-
    The launcher command line reveals how the implant is being staged. The MemoryStream assignment and known encoded
    prefix indicate an in-memory .NET stager, the GruntHTTP string indicates an HTTP Grunt implant, and an mshta
    file.hta launch indicates an HTA-delivered stage. Identifying the variant determines what the implant can do once
    resident and determines the scope of the response.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.encoded_command_decode
  question: Was the command encoded, and what does the decoded payload do?
  context: >-
    The -EncodedCommand parameter and the known encoded prefix in the signature branch carry a base64 payload that hides
    the actual instructions. Decode it to recover the real script. Covenant launchers typically reconstruct a byte
    stream in memory and load a .NET assembly with no file written to disk, so look for download cradles, MemoryStream
    or assembly-load logic, and any callback host inside the decoded text.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1059.001.image_load
  question: Did the launching process load the .NET runtime or PowerShell automation assemblies in memory?
  context: >-
    A Covenant launcher reflectively loads a .NET implant into the running process rather than writing an executable to
    disk, so image-load evidence is often the clearest artifact of in-memory execution. The common language runtime and
    the PowerShell automation assembly loading into a process that does not normally host managed code, or loading
    shortly after the alerting command line, corroborates fileless .NET execution that the command line alone cannot
    confirm. If image-load events are not collected in this environment, treat the absence of results as a visibility
    gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_modules:
        ImageLoaded|endswith:
          - '\clr.dll'
          - '\clrjit.dll'
          - '\mscoree.dll'
          - '\System.Management.Automation.dll'
          - '\System.Management.Automation.ni.dll'
          - '\amsi.dll'
      condition: selection and selection_modules
    fields:
      - Image
      - ImageLoaded
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated script content?
  context: >-
    Script block logging records the script content PowerShell actually executed, even when the command line was
    encoded or obfuscated, giving ground truth about what ran. For an in-memory Covenant stager this often exposes the
    assembly-load or download-cradle logic the encoded command hid. If PowerShell operational logs are not ingested in
    this environment, treat the absence of results as a visibility gap worth recording rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before the launch?
  context: >-
    Operators frequently disable or evade PowerShell defenses before staging a payload. Common precursors include AMSI
    patching, execution-policy bypass flags, real-time monitoring being turned off, and PowerShell version-2 downgrade
    to escape script block logging. Finding these in the hour before the alert strengthens the case that the hidden
    encoded launch was preparation for an implant rather than benign automation.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this launcher, and is that parent chain expected?
  context: >-
    The parent process reveals how the launcher was invoked. A launch from explorer.exe or a management agent differs
    sharply from one spawned by an Office application, mshta.exe, wscript.exe, or a browser, which point to macro or
    document-delivered execution. Walk up the tree to see what started the parent and what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this launcher spawn afterward?
  context: >-
    The processes a launcher spawns reveal its actual impact. An in-memory implant may spawn cmd.exe, additional
    PowerShell, rundll32, or network utilities for discovery and lateral movement, or it may inject and spawn nothing
    visible. Reviewing the children of the alerting process shows whether post-exploitation activity followed.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this launcher pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    The query mirrors the rule's own selection structure scoped to this host, so the resulting parent and user
    distribution reflects how this specific launcher pattern -- not generic PowerShell usage -- has historically run
    here. A new parent process, an unexpected account, or a first-time appearance of the pattern is the deviation worth
    chasing; a parent and user that already account for prior firings point toward established automation.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains|all:
        - -Sta
        - -Nop
        - -Window
        - Hidden
        CommandLine|contains:
        - -Command
        - -EncodedCommand
      selection_2:
        CommandLine|contains:
        - 'sv o (New-Object IO.MemorySteam);sv d '
        - mshta file.hta
        - GruntHTTP
        - -EncodedCommand cwB2ACAAbwAgA
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains|all:
        - -Sta
        - -Nop
        - -Window
        - Hidden
        CommandLine|contains:
        - -Command
        - -EncodedCommand
      selection_2:
        CommandLine|contains:
        - 'sv o (New-Object IO.MemorySteam);sv d '
        - mshta file.hta
        - GruntHTTP
        - -EncodedCommand cwB2ACAAbwAgA
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains|all:
        - -Sta
        - -Nop
        - -Window
        - Hidden
        CommandLine|contains:
        - -Command
        - -EncodedCommand
      selection_2:
        CommandLine|contains:
        - 'sv o (New-Object IO.MemorySteam);sv d '
        - mshta file.hta
        - GruntHTTP
        - -EncodedCommand cwB2ACAAbwAgA
      _pb_scope:
        User|expand: '%User%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts in the surrounding window reveals lateral movement or staging across
    multiple endpoints. If the same account drove similar activity elsewhere at the same time, the scope of the incident
    may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains|all:
        - -Sta
        - -Nop
        - -Window
        - Hidden
        CommandLine|contains:
        - -Command
        - -EncodedCommand
      selection_2:
        CommandLine|contains:
        - 'sv o (New-Object IO.MemorySteam);sv d '
        - mshta file.hta
        - GruntHTTP
        - -EncodedCommand cwB2ACAAbwAgA
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining the process activity surrounding the alert reveals what led to this
    launch, what followed it, and whether it is part of a larger sequence. Novel parent-child pairs at low counts stand
    out against repetitive agent telemetry; look for reconnaissance, privilege escalation, or persistence around the
    same minutes.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host -- defense tampering, reconnaissance, credential access -- may reveal the
    attack stages that preceded this launch and help reconstruct the timeline. The sequence of alerts on a host often
    tells the story; staging activity commonly precedes implant execution.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this launcher pattern in the past week, and does the distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection structure with no host scope, giving a fleet-wide count of this specific
    launcher pattern rather than generic PowerShell usage. Many hosts firing the same pattern points to scripted
    deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use the host
    spread to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_1:
        CommandLine|contains|all:
        - -Sta
        - -Nop
        - -Window
        - Hidden
        CommandLine|contains:
        - -Command
        - -EncodedCommand
      selection_2:
        CommandLine|contains:
        - 'sv o (New-Object IO.MemorySteam);sv d '
        - mshta file.hta
        - GruntHTTP
        - -EncodedCommand cwB2ACAAbwAgA
      condition: 1 of selection_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1564.003
      name: Hidden Window
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
    - id: T1564
      name: Hide Artifacts
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: launch_verification
      description: "PowerShell launched with hidden/no-profile flags plus an inline or encoded command, OR a Covenant signature string"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_decode
      description: "Encoded command carries a base64 payload reconstructing an in-memory .NET implant"
      mapped_to: Q5
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: in_memory_assembly_load
      description: ".NET runtime / PowerShell automation assemblies loaded into the process for fileless execution"
      mapped_to: Q6
      source: attack:T1059.001.image_load
    - stage: 4
      label: follow_on_execution
      description: "Implant spawns child processes or network activity for post-exploitation"
      mapped_to: Q10
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q5
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q8
      source: template:powershell.powershell_security_bypass
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop, -EncodedCommand, -Window Hidden, -Sta, GruntHTTP, mshta file.hta"
      disposition: query_injection
      target_question: Q11
      notes: "Rule's selection_1 + selection_2 patterns embedded verbatim in the reflected baseline_deviation (Q11) and scope (Q18) queries; selection_1 -Window/Hidden flag pair injected into user_actor baseline/clustering queries (Q12-Q15) for alignment"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "abnormal parents like Excel, WinWord, mshta spawning the launcher"
      disposition: context
      target_question: Q9
      notes: "process_chain walks the parent chain; abnormal-parent examples carried in context (no parent field in rule detection to inject)"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q16
      notes: "Off-hours / non-maintenance scoping is analyst judgment, not directly queryable; surfaced via the +/-15m host clustering window"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rare/never-before-seen .NET assemblies tied to PowerShell abuse"
      disposition: new_question
      target_question: Q6
      notes: "Drove the attack:T1059.001.image_load question; clr.dll/System.Management.Automation.dll/amsi.dll injected into ImageLoaded|endswith"
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "4104 length threshold is a noise-filter tuning rationale; script_block_logging question retrieves ScriptBlockText for manual review"
    - element: HiddenProcessScope
      dimension: tuning_parameter
      specific_values: "PowerShell, cmd, wscript where hidden execution is unexpected"
      disposition: context
      target_question: Q1
      notes: "T1564.003 hidden-window scope; rule fires on -Window Hidden command-line flag, surfaced in Q1 branch-one framing and Q8 tuning"
    - element: ParentProcessCorrelation
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "T1564.003 parent correlation rationale carried by process_chain context"
    - element: FileExtensions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "T1564 file-hiding rationale; Covenant launcher is fileless (in-memory), so file-attribute hiding is not the rule's mechanism — addressed via image_load fileless angle"
    - element: ADSDetection
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "T1564 Alternate Data Stream rationale; not this rule's mechanism (no file reference in detection)"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "Phase-2 analysis collision — powershell.encoded_command_decode (technique set) is more specific for this encoded-PowerShell rule and supersedes the generic cue decode"
      kept: powershell.encoded_command_decode
    - dropped: cue_process_tool.baseline
      reason: "Image-scoped generic-binary baseline is both broken (no Image field in this CommandLine-only rule) and subsumed by the reflected host-pattern baseline that filters on the rule's actual selection"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "Near-identical host-scoped +/-15m activity aggregation; cue_host_context.clustering (Image+ParentImage) is the richer of the two"
      kept: cue_host_context.clustering
    - dropped: hacking_tool.tool_specific_artifacts
      reason: "Covenant PowerShell launcher is a fileless in-memory .NET stager and the references publish no characteristic disk artifacts; the attack:T1059.001.image_load question is the meaningful fileless-artifact angle and supersedes a generic file-creation sweep"
      kept: attack:T1059.001.image_load
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q13 user-pivot, Q12 parent+pattern substitute)
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q14 user-pivot, Q15 parent+pattern substitute)
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "powershell.powershell_security_bypass (must_include, retained at Q8) sweeps host AMSI/Defender/execution-policy tampering in the same -1h window for this PowerShell-execution rule, making the generic host-tamper sweep redundant here"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1564.003) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        The rule's hidden-window mechanism is the -Window Hidden command-line flag (T1564.003 process_creation
        branch), not the off-screen registry window-position edit. No registry_set selection exists in the rule's
        detection, and Covenant's PowerShell launcher hides the window via the launch flag rather than registry
        modification, so registry_set coverage would not reflect this rule's firing logic. The hidden-execution
        dimension is covered by Q1 branch-one framing and Q8 (powershell security bypass tuning).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - hacking_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      image_load: 1
      powershell_script_block: 1
      sigma_alert: 1
