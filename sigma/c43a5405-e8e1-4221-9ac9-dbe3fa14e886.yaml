name: System Language Discovery via Reg.Exe
id: 9f3c1d2a-7b54-4e08-9a6f-2c1d8e4b7a30
description: |
  Detects reg.exe being used to query the system language configuration under the
  registry path Control\Nls\Language. The rule fires when reg.exe (by image name or
  original file name) runs with a command line that contains both a query operation
  and the Control\Nls\Language key path. Reading the system UI language is a benign
  diagnostic on its own, but it is also a common discovery step: operators enumerate
  the system locale to fingerprint a victim's region, tailor a payload, or skip hosts
  in locales they intend to avoid. Investigation should confirm the exact query, identify
  what process invoked reg.exe, and look for the surrounding reconnaissance that locale
  discovery usually accompanies.
type: detection
detection_id: c43a5405-e8e1-4221-9ac9-dbe3fa14e886
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Did reg.exe run a query against the Control\Nls\Language registry path?
  context: Confirm the alert is a true positive by reviewing the exact command line and
    process details. The rule fires only when reg.exe (or a binary whose original file
    name is reg.exe) runs with a command line containing both a query operation and the
    path Control\Nls\Language. Verify both elements are present and note the full key
    path that was queried.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user and host expected to query system locale settings from the command line?
  context: Network administrators and IT staff occasionally read locale or language
    settings while troubleshooting, and some software installers query the system UI
    language. Standard users and most service accounts have no reason to run reg.exe
    against Control\Nls\Language. The user's role and the host's function determine
    whether this read is routine diagnostics or post-compromise fingerprinting.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: attack:T1614.001.query_target_decode
  question: What exact registry key did the query target, and does the full path match the system-language locale keys?
  context: The Control\Nls\Language subtree holds the installed and default UI language
    identifiers (for example InstallLanguage and Default under
    HKLM\SYSTEM\CurrentControlSet\Control\Nls\Language). Because this is a read rather
    than a write, the command line itself is the source of truth for what was queried;
    reg query does not emit a registry-modification event. Parse the full key path and
    any value name from the command line and confirm whether the query targeted the
    system default language, the installed language, or the broader Nls subtree.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned reg.exe, and is that parent chain expected for a locale query?
  context: The parent process reveals how the locale query was invoked. reg.exe launched
    by explorer.exe or an interactive shell points to manual diagnostics, while reg.exe
    launched by a script host (wscript.exe, cscript.exe, mshta.exe), an Office application,
    powershell.exe, or wmiprvse.exe points to automated enumeration. Identify the parent
    and what else it spawned to reconstruct how the query reached the host.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the locale-query command, and is that parent normal for system discovery?
  context: Legitimate discovery commands are usually run from cmd.exe spawned by
    explorer.exe (a user opening a command prompt) or from an automation script. Locale
    queries spawned by Office applications, script interpreters, mshta.exe, or
    wmiprvse.exe indicate automated post-exploitation enumeration rather than an
    operator troubleshooting at a console.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single locale query may be benign diagnostics, but multiple reconnaissance
    commands in rapid succession -- whoami, systeminfo, ipconfig, net user, nltest -- is
    a signature pattern of post-compromise enumeration. Locale discovery is frequently
    one step in a fingerprinting sweep an operator runs to understand a host before
    moving to the next stage.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: attack:T1614.001.locale_api_discovery
  question: Did processes on this host invoke locale-related APIs or query other Nls/locale keys around this time?
  context: System language discovery is not limited to reg.exe. The same intent shows
    up as other reg queries against the Nls subtree or InternationalSettings, and as
    locale-related API calls (GetUserDefaultUILanguage, GetSystemDefaultUILanguage,
    GetKeyboardLayoutList, GetLocaleInfoW). Finding additional locale or keyboard-layout
    reads on the host -- especially from script hosts or unknown binaries -- strengthens
    the case that this is deliberate region fingerprinting rather than an isolated read.
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_locale:
        CommandLine|contains:
          - 'Control\Nls'
          - 'GetUserDefaultUILanguage'
          - 'GetSystemDefaultUILanguage'
          - 'GetKeyboardLayoutList'
          - 'GetLocaleInfo'
          - 'GetTimeZoneInformation'
          - 'Get-WinSystemLocale'
          - 'Get-Culture'
          - 'Get-WinUILanguageOverride'
      condition: selection and selection_locale
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_process_tool.follow_on
  question: What did reg.exe spawn afterward, and did follow-on activity progress beyond discovery?
  context: A locale read on its own changes nothing, so the investigative value is in
    what came next. reg.exe legitimately spawns nothing; child processes following the
    query -- cmd.exe, powershell.exe, certutil.exe, or a payload -- indicate the
    discovery fed directly into an execution or download stage. The forward window
    captures progression from fingerprinting into hands-on action.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining the process activity surrounding
    the locale query shows what led up to it and what followed -- reconnaissance,
    privilege escalation, persistence installation, or staging -- and provides the
    broader context for whether the read was part of a larger sequence. Novel
    parent-child pairs stand out against the high-count agent telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:lolbin.renamed_binary_check
  question: Was reg.exe executed from its expected System32 or SysWOW64 path?
  context: reg.exe executed from outside System32 or SysWOW64, or a binary whose original
    file name is reg.exe running under a different file name, can indicate the binary was
    copied or renamed to evade path-based or name-based controls. Compare the Image path
    against the OriginalFileName to confirm this is the genuine system reg.exe.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were other discovery LOLBins used on this host recently?
  context: Operators who run one discovery binary usually run several. Locale discovery
    commonly sits alongside other living-off-the-land enumeration -- wmic, query.exe,
    dnscmd, or systeminfo -- and finding multiple discovery LOLBins performing
    fingerprinting on the same host in a short window strengthens the case that this is
    deliberate reconnaissance rather than a one-off diagnostic.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\wmic.exe'
          - '\query.exe'
          - '\qprocess.exe'
          - '\quser.exe'
          - '\dnscmd.exe'
          - '\nltest.exe'
          - '\setspn.exe'
          - '\dsquery.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\nbtstat.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli:
        CommandLine|contains|all:
        - query
        - Control\Nls\Language
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli:
        CommandLine|contains|all:
        - query
        - Control\Nls\Language
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the user's activity across all hosts reveals whether the same account
    performed similar enumeration on multiple endpoints. If the user triggered comparable
    discovery elsewhere, the scope of the activity may be larger than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli:
        CommandLine|contains|all:
        - query
        - Control\Nls\Language
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this locale-query pattern in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the
    result is a fleet-wide count of the locale-query pattern, not generic reg.exe usage.
    Many hosts firing the same pattern points to scripted deployment or a software
    installer reading the system language; one host or a small unrelated set points to
    manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli:
        CommandLine|contains|all:
        - query
        - Control\Nls\Language
      condition: all of selection_*
    fields:
    - host.name
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the locale-query pattern over the past 90 days, and what command-line structures are typical?
  context: A locale-query pattern with no prior history on a host is more suspicious than
    one seen regularly under stable parents (for example a recurring inventory script).
    The query mirrors the rule's selection structure scoped to this host, so the count
    reflects the rule's specific firing pattern, not generic reg.exe usage. The
    command-line projection lets the analyst compare typical invocation patterns against
    the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli:
        CommandLine|contains|all:
        - query
        - Control\Nls\Language
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Discovery rarely happens in isolation. Earlier detections on the same host --
    initial execution, script-host activity, credential access, or other reconnaissance
    -- may reveal the stages that preceded the locale query and help reconstruct the full
    timeline of activity on the host.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1614.001
      name: System Language Discovery
      role: primary
    - id: T1614
      name: System Location Discovery
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: locale_query_execution
      description: "reg.exe queries Control\\Nls\\Language to read the system UI language"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: query_target_decode
      description: "Identify the exact key/value queried and confirm it maps to the system-language locale keys"
      mapped_to: Q3
      source: attack:T1614.001.query_target_decode
    - stage: 3
      label: invoking_process_analysis
      description: "Determine what process invoked reg.exe and whether the parent chain is expected"
      mapped_to: Q4
      source: template:cue_process_tool.process_chain
    - stage: 4
      label: discovery_correlation
      description: "Correlate the locale read with other reconnaissance and locale-API activity that fingerprinting accompanies"
      mapped_to: Q6
      source: template:discovery_recon.discovery_burst_detection
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1614.001
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1614.001
      covered_by: Q3
      source: attack:T1614.001.query_target_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1614.001
      covered_by: Q2
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
    - dimension: time_correlation
      technique: T1614
      covered_by: Q6
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessAllowList
      dimension: parent_process_analysis
      specific_values: "rationale only (trusted-parent list; no concrete values in brief)"
      disposition: query_injection
      target_question: Q4
      notes: "Parent identification carried by the process_chain query (ParentProcessGuid lookup) and the discovery_parent_chain question (Q5)"
    - element: QueryThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (frequency threshold; no concrete value in brief)"
      disposition: context
      target_question: Q17
      notes: "Frequency is not directly queryable; historical_lolbin_baseline (Q17) supplies the 90-day count for the analyst to judge against a threshold"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "2m (example correlation window from T1614 analytic)"
      disposition: context
      target_question: Q6
      notes: "Correlation-window rationale; discovery_burst_detection uses a +/-15m window and the locale-API sweep (Q7) +/-30m to catch the burst"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.clustering paired with their _svc substitutes
      reason: "user_actor pair — non-deduplicable (subject vs. launch-mechanism pivots degenerate under service principals)"
      kept: both variants emitted (Q12+Q13 baseline pair, Q14+Q15 clustering pair)
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by registry.registry_key_context per brief, but registry_key_context targets registry_set (write) events with TargetObject/Details fields — a reg QUERY emits no registry_set event, so the template's logsource and fields do not fit. Authored a command-line decode question (attack:T1614.001.query_target_decode) as the Phase 2 decode instead, which reads the query target from the reg.exe CommandLine."
      kept: attack:T1614.001.query_target_decode (Q3)
    - dropped: discovery_recon.discovery_user_context vs universal.host_user_context
      reason: "discovery_user_context is more specific to discovery context; per its notes_to_llm, prefer it and drop universal.host_user_context to avoid a duplicate user-judgment question"
      kept: discovery_recon.discovery_user_context (Q2)
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context (per brief hint and template replaced_by field). The replacement template did not fit either (read vs. write); the Phase 2 decode is authored as attack:T1614.001.query_target_decode (Q3)."
    - template: registry.registry_key_context
      reason: "must_include, but it targets registry_set events with TargetObject/Details fields. reg QUERY reads a key and emits no registry_set modification event, so there is no TargetObject/Details to project. Phase 2 decode of the queried key path is done from the reg.exe CommandLine in attack:T1614.001.query_target_decode (Q3)."
    - template: universal.host_user_context
      reason: "discovery_recon.discovery_user_context (Q2) is the more specific user-judgment question for a discovery rule; per that template's notes_to_llm, the two overlap and the discovery version is preferred. Dropped to avoid duplication."
    - template: cue_process_tool.baseline
      reason: "Generic per-binary reg.exe baseline superseded by the rule-pattern-specific lolbin.historical_lolbin_baseline (Q17, REFLECTED) which counts only the locale-query pattern rather than all reg.exe usage on the host."
    - template: cue_process_tool.baseline_deviation
      reason: "REFLECTED host-pattern parent/user deviation overlaps lolbin.historical_lolbin_baseline (Q17, same host-scoped rule-pattern count) and cue_user_actor.baseline_svc (Q13, parent-routine pivot). Dropped to stay within the 18-question cap without losing the host-pattern signal."
    - template: cue_process_tool.clustering
      reason: "Generic 'other tools around this time' is the same host-wide view as cue_host_context.clustering (Q9, +/-15m), which is kept; a second generic clustering question would duplicate without adding a distinct angle."
    - template: registry.registry_related_modifications
      reason: "must_include, but the rule fires on reg QUERY (a read), not a write. A sweep for reg add/delete/import write operations does not address what this read implies; the relevant 'other locale reads' angle is covered by the locale-API/Nls sweep (Q7, attack:T1614.001.locale_api_discovery)."
    - template: persistence_scope.process_based_persistence
      reason: "recommended, not must_include; this is a discovery (read) rule, not a persistence rule (applicability.is_persistence_rule is false). Locale enumeration does not imply persistence installation, so a schtasks/sc/reg-write persistence sweep adds no decision-relevant angle. Slot spent on discovery and locale-API correlation instead."
    - template: persistence_scope.file_based_persistence
      reason: "recommended, not must_include; a read-only locale query implies no Startup-folder or Run-key file drop. The file_event slot would surface unrelated host activity rather than anything the locale read points to."
    - template: universal.endpoint_protection_health
      reason: "recommended, not must_include; this is a discovery read, not an execution / credential-access / impact action an operator would precede by disabling AV/EDR. defense_tampering is not a confirmed technique set for this rule and no selected question depends on a tamper precursor, so the generic host-tamper sweep does not earn its slot. cue_host_context.precursor (Q18) already surfaces any prior tamper ALERTS on the host."
  reflection_overrides: []
  hint_dispositions:
    - hint: "registry.registry_key_context replaces cue_process_tool.mechanism_decode"
      disposition: declined
      reason: >-
        registry_key_context queries registry_set (write) events and projects
        TargetObject/Details. This rule fires on reg QUERY — a read that emits no
        registry_set modification event, so those fields would never populate (the
        validator confirmed the field mismatch). mechanism_decode is still dropped
        as the brief intends; the Phase 2 decode of the queried key path is authored
        as attack:T1614.001.query_target_decode (Q3), which reads the target from the
        reg.exe CommandLine. Neither template is emitted verbatim; the decode intent
        is preserved.
    - hint: "LOLBin rule — check for renamed binary and other LOLBins achieving same objective"
      disposition: declined
      reason: >-
        Partially acted on, not declined: the renamed-binary half is covered by
        lolbin.renamed_binary_check (Q10) and the other-LOLBins half by
        lolbin.technique_class_lateral (Q11), adapted to discovery-class LOLBins
        (wmic, query.exe, dnscmd, dsquery) since reg query is a discovery action,
        not download/proxy-exec. Recorded here only to note the discovery-class
        adaptation; this hint is acted on, not silently dropped.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' comes only from the DarkSide write-up in db_refs; the rule detects a locale read with no impact behavior. Ransomware-impact questions (shadow-copy deletion, encryption, recovery tampering) have no bearing on a reg query of Control\\Nls\\Language."
      - id: network_scope
        confidence: low
        reason: "Same incidental 'ransomware' keyword origin. The rule has no network detection field and no external entity (applicability.has_external_entity is false); a network-scope question would query a logsource the rule never touches."
  technique_sets_applied:
    - universal
    - discovery_recon
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 16
    logsource_diversity:
      process_creation: 17
      sigma_alert: 1
