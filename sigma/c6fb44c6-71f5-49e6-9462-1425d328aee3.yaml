name: Powershell Base64 Encoded MpPreference Cmdlet
id: a9150228-2790-4b75-810b-f0b9a7b69f12
description: |
  Detects a PowerShell command line that carries a base64-encoded (or base64offset-encoded) Add-MpPreference or
  Set-MpPreference cmdlet, the canonical mechanism for adding Windows Defender exclusions or flipping protection
  flags. Encoding the cmdlet hides the tampering from casual command-line inspection, so the rule fires regardless of
  the surrounding script. Legitimate management of Defender exclusions is normally run in clear text by administrators
  or configuration tooling; an encoded form points to an operator deliberately concealing a Defender carve-out, often
  as a precursor to payload execution. The investigation should decode the cmdlet to identify exactly what exclusion or
  setting was changed, confirm whether protection is currently weakened, and look for the activity the tampering was
  meant to enable.
type: detection
detection_id: c6fb44c6-71f5-49e6-9462-1425d328aee3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain a base64-encoded Add-MpPreference or Set-MpPreference cmdlet?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the full command line of
    the triggering event. The rule fires when a PowerShell command line carries an encoded Add-MpPreference or Set-MpPreference
    cmdlet, so verify the encoded fragment is present and decode it to confirm it is genuinely a Defender preference change.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to change Windows Defender exclusions or protection settings?
  context: Legitimate Defender management is normally performed by administrators or configuration tooling in clear text.
    Knowing whether this account and endpoint routinely manage Defender preferences is often the fastest path to separating
    sanctioned administrative work from an operator concealing a carve-out.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: What specific Defender setting or exclusion did the decoded cmdlet change?
  context: Different Defender changes have different impacts. An -ExclusionProcess or -ExclusionPath entry carves a binary
    or directory out of real-time protection and also stops Attack Surface Reduction and Network Protection from inspecting
    it; -DisableRealtimeMonitoring or -MAPSReporting Disabled weakens detection broadly. Decode the encoded fragment, then
    read the cmdlet parameter and value to determine exactly what was excluded or disabled.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.encoded_command_decode
  question: How was the cmdlet encoded, and what does the full command decode to?
  context: The rule matches base64 and base64offset encodings of the cmdlet, which is how operators hide a Defender carve-out
    from casual inspection. The same command line may also use -EncodedCommand, string concatenation, or backtick escaping.
    Decode every layer to recover the full script, including any download cradle or in-memory loader bundled with the
    preference change.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated cmdlet and surrounding script?
  context: Script block logging records the deobfuscated script content PowerShell actually executes, even when the command
    line is base64-encoded. This is the ground truth for what the encoded cmdlet expanded to and reveals any wider script it
    was embedded in. If PowerShell operational logs are not ingested in this environment, treat zero results as a visibility
    gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell or AMSI security controls bypassed or weakened on this host before this execution?
  context: Operators often weaken PowerShell defenses before running a payload. Common precursors include AMSI bypass (patching
    amsi.dll in memory), execution-policy changes such as -ExecutionPolicy Bypass or -ep bypass, and a downgrade to PowerShell
    v2 which lacks logging. Finding these alongside the Defender carve-out strengthens the case that the tampering was
    deliberate operator preparation.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is Windows Defender protection currently restored, or does the change still stand?
  context: The cmdlet change may have been temporary or may persist. If an exclusion is still in place or real-time monitoring
    is still disabled, the host remains under-protected and the setting should be reverted before further investigation. Check
    the current Defender preference state on the host directly rather than relying on the alert alone.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the Defender setting was changed?
  context: Operators weaken Defender for a reason, typically to run a payload, dump credentials, or stage data that would
    otherwise be blocked or scanned. Examining process activity in the window after the cmdlet ran reveals what the carve-out
    or disabled control was meant to enable, especially execution from a path that was just excluded.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host in the same window?
  context: Operators who weaken one control often weaken several, stacking exclusions with log clearing, shadow-copy deletion,
    or service stops for maximum coverage. Finding additional tampering from the same user or parent process indicates several
    controls were targeted in one operation rather than an isolated setting change.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1562.001.registry_tamper
  question: Were Windows Defender or telemetry settings altered directly in the registry on this host?
  context: Defender preference cmdlets ultimately write to the registry, and operators sometimes edit those keys directly
    to bypass cmdlet-level monitoring, or disable Sysmon and other telemetry the same way. Registry modification events under
    the Defender policy keys or telemetry provider keys surface tampering that does not appear in a PowerShell command line.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_keys:
        TargetObject|contains:
          - '\Windows Defender\Exclusions'
          - '\Windows Defender\Real-Time Protection'
          - '\Windows Defender\Features'
          - '\Windows Advanced Threat Protection'
          - '\Policies\Microsoft\Windows Defender'
      condition: selection and selection_keys
    fields:
      - TargetObject
      - Details
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned the PowerShell that ran the encoded cmdlet, and is that parent chain expected?
  context: The parent process reveals how the encoded cmdlet was launched. Interactive administration typically descends from
    explorer.exe or a console; parents such as mshta.exe, wscript.exe, an Office application, or an unexpected service point
    toward exploitation or a malicious script delivering the Defender carve-out.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this encoded-MpPreference pattern, what parents and users drove it, and does
    the current alert fit?
  context: Even routine Defender management can resemble this pattern. The query mirrors the rule's selection scoped to this
    host, so the resulting parent and user distribution reflects how this specific encoded-cmdlet pattern has run here, not
    generic PowerShell usage. A new parent process, an unexpected account, or a one-off shape against an established baseline
    is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|base64offset|contains:
        - 'Add-MpPreference '
        - 'Set-MpPreference '
        - 'add-mppreference '
        - 'set-mppreference '
      - CommandLine|contains:
        - QQBkAGQALQBNAHAAUAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - EAZABkAC0ATQBwAFAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - BAGQAZAAtAE0AcABQAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - UwBlAHQALQBNAHAAUAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - MAZQB0AC0ATQBwAFAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - TAGUAdAAtAE0AcABQAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - YQBkAGQALQBtAHAAcAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - EAZABkAC0AbQBwAHAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - hAGQAZAAtAG0AcABwAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - cwBlAHQALQBtAHAAcAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - MAZQB0AC0AbQBwAHAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - zAGUAdAAtAG0AcABwAHIAZQBmAGUAcgBlAG4AYwBlACAA
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this encoded-MpPreference pattern in the past week, and does the cross-host distribution
    look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of this specific encoded-cmdlet
    pattern rather than generic PowerShell usage. Many hosts firing the same pattern points to a scripted deployment or
    configuration push; one host or a small unrelated set points to manual operator activity. Use the host spread and
    parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|base64offset|contains:
        - 'Add-MpPreference '
        - 'Set-MpPreference '
        - 'add-mppreference '
        - 'set-mppreference '
      - CommandLine|contains:
        - QQBkAGQALQBNAHAAUAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - EAZABkAC0ATQBwAFAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - BAGQAZAAtAE0AcABQAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - UwBlAHQALQBNAHAAUAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - MAZQB0AC0ATQBwAFAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - TAGUAdAAtAE0AcABQAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - YQBkAGQALQBtAHAAcAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - EAZABkAC0AbQBwAHAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - hAGQAZAAtAG0AcABwAHIAZQBmAGUAcgBlAG4AYwBlACAA
        - cwBlAHQALQBtAHAAcAByAGUAZgBlAHIAZQBuAGMAZQAgA
        - MAZQB0AC0AbQBwAHAAcgBlAGYAZQByAGUAbgBjAGUAIA
        - zAGUAdAAtAG0AcABwAHIAZQBmAGUAcgBlAG4AYwBlACAA
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally change Windows Defender preferences from PowerShell?
  context: Compare this account's current action against its historical behavior. A user who has never run Defender preference
    cmdlets is more suspicious than one who manages exclusions regularly. The role and the expected toolset should align with
    the activity. Aggregating by host shows where this user has previously performed this change.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains:
          - 'Add-MpPreference'
          - 'Set-MpPreference'
          - 'MpPreference'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and PowerShell command combination historically routine on this host or across the environment,
    or is the parent unusual for this activity?
  context: When the alert fires under a service principal, user identity does not attribute the behavior, so the meaningful
    pivot is the launch mechanism. A parent process that already accounts for many prior MpPreference invocations is likely
    configuration automation; a new or rare parent for this activity is the deviation worth chasing. Aggregating by host
    surfaces host concentration that signals a scheduled or pushed task.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith|expand: '%ParentImage%'
        CommandLine|contains:
          - 'Add-MpPreference'
          - 'Set-MpPreference'
          - 'MpPreference'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around the time of the alert?
  context: Tracking the account's activity across all hosts in a tight window reveals lateral movement or coordinated action
    on multiple endpoints. If the same user weakened Defender on several hosts, the scope of the operation is wider than a
    single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same parent and PowerShell command combination fire on other hosts in the same window, indicating an
    automation rollout?
  context: Under a service principal, a user-wide pivot returns agent-telemetry noise because every host runs system-owned
    agents constantly. The meaningful question is whether the same launch mechanism fired on other hosts in the alert window.
    Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith|expand: '%ParentImage%'
        CommandLine|contains:
          - 'Add-MpPreference'
          - 'Set-MpPreference'
          - 'MpPreference'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Tampering with Defender rarely happens in isolation. Earlier detections on the same host, such as reconnaissance,
    credential access, or other defense evasion, may reveal the attack stages that preceded this change and help reconstruct
    the timeline that led to the carve-out.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tamper_action
      description: "Operator runs an encoded Add/Set-MpPreference cmdlet to add a Defender exclusion or flip a protection flag"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: decode_intent
      description: "Encoded cmdlet (and any wrapping script) decoded to recover the full operation"
      mapped_to: Q4
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: protection_state
      description: "Determine whether the weakened control is still in effect"
      mapped_to: Q7
      source: template:defense_tampering.protection_status
    - stage: 4
      label: enabled_activity
      description: "Activity the tampering was meant to enable, executed after the control was weakened"
      mapped_to: Q8
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q8
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q11
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "-ExclusionProcess / -ExclusionPath / -ExclusionExtension / -ExclusionIpAddress"
      disposition: context
      target_question: Q3
      notes: "Exclusion parameter names from the db_ref folded into what_was_disabled context so the analyst reads which
        carve-out the decoded cmdlet set; cmdlet itself is the rule's selection, query stays ProcessGuid-scoped."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q8
      notes: "Correlation-window rationale; expressed as the +1h post-tamper range on activity_after_tampering."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Security-service-name list is a tuning rationale; the other_tampering_activity sweep already enumerates
        service-stop and log-clear patterns."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: "Authorized-admin-tool exclusion is a baselining rationale; process_chain identifies the parent that launched
        the cmdlet so the analyst can judge whether it is sanctioned."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Service-criticality refinement is a tuning rationale, not a queryable value for this CommandLine-only rule."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q14, Q15)
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q16, Q17)
    - dropped: cue_process_tool.clustering
      reason: "near-duplicate of cue_host_context-style host-window survey; host_context.clustering carries the richer
        Image+ParentImage projection. Dropped the process_tool variant; clustering coverage retained via user_actor pair
        and precursor."
      kept: cue_user_actor.clustering
    - dropped: cue_process_tool.baseline
      reason: "Image-based baseline (Image|endswith %Image%) is unusable for this CommandLine-only rule — no Image field in
        detection (hint 4). Rule-pattern baselining provided instead by the REFLECTED baseline_deviation (Q12)."
      kept: cue_process_tool.baseline_deviation
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled (hint 1) — the technique-set version is more specific to the
        Defender-tampering target."
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by: powershell.encoded_command_decode (hint 2) — the PowerShell-specific decode question supersedes
        the generic encoded-payload one for a powershell.exe rule."
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled / other_tampering_activity questions are
        more specific than the generic host-tamper sweep and supersede it (template notes_to_llm drop condition a)."
    - template: cue_process_tool.follow_on
      reason: "follow_on (ParentImage = %Image% child enumeration) requires an Image value to pivot on; the rule has no Image
        field. Post-execution child activity is covered host-scoped by activity_after_tampering (Q8)."
    - template: cue_process_tool.baseline
      reason: "Image-based; no Image field in this CommandLine-only rule — see dedup_decisions."
    - template: cue_host_context.clustering
      reason: "Host-window survey overlaps user_actor.clustering and the precursor question; dropped to keep the count at 18
        while preserving the more identity-specific clustering pivots."
  hint_dispositions:
    - hint: "defense_tampering → persistence_scope cascade activated"
      disposition: declined
      reason: >-
        Rule is defense-evasion (T1562.001), not persistence (applicability.is_persistence_rule is false). The
        persistence_scope.process_based_persistence and file_based_persistence templates are recommended, not must_include.
        Post-tamper scope on this host is already covered by activity_after_tampering (Q8, +1h survey) and
        other_tampering_activity (Q9, tamper-tool sweep). At the 18-question cap the persistence-tool sweep adds less
        investigative value than the retained prior-alert precursor (Q18) and the user_actor pairs, so neither
        persistence_scope template was authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - defense_tampering
    - obfuscation
    - persistence_scope
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 15
      powershell_script_block: 1
      registry_set: 1
      sigma_alert: 1
