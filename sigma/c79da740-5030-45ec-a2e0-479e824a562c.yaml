name: System Disk And Volume Reconnaissance Via Wmic.EXE
id: 50ba86f2-7903-493f-b369-af49ed15175d
description: |
  Detects wmic.exe (identified by Image ending in WMIC.exe or OriginalFileName wmic.exe) running a
  disk and volume enumeration command. The rule fires when the binary matches AND the command line
  carries one of the recon patterns: a ' volumename' or ' logicaldisk' argument, both 'path' and
  'win32_logicaldisk' together, or both ' volume' and ' list ' together. This enumerates logical
  disks, free space, file systems, and volume names — a read-only inventory that needs no elevation
  and is commonly run by attackers establishing situational awareness on a newly accessed host, but
  also by legitimate administrators and inventory scripts. Determine whether the invocation is
  routine administration or part of post-compromise enumeration by examining the full command, the
  parent process, the user and host role, and any surrounding reconnaissance.
type: detection
detection_id: c79da740-5030-45ec-a2e0-479e824a562c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Is the alerting process wmic.exe running a disk or volume enumeration command?
  context: Confirm the alert is a true positive before investing time. The rule fires when the binary is wmic.exe (Image ending in WMIC.exe or OriginalFileName wmic.exe) AND the command line carries a disk/volume recon pattern — ' volumename' or ' logicaldisk', or both 'path' and 'win32_logicaldisk', or both ' volume' and ' list '. Read the exact command line and confirm both the binary identity and one of these argument patterns are present rather than a different wmic subcommand.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run wmic disk and volume enumeration?
  context: Disk and volume inventory through wmic is routine for some administrators, asset-management tooling, and backup or imaging scripts, but unusual from a standard user account or a host with no inventory role. The wmic command-line utility is also deprecated on current Windows builds, so legitimate calls should be rare on modern systems. Knowing whether the account and host function fit administrative inventory work is often the fastest way to separate routine operations from post-compromise enumeration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this wmic command enumerate? Decode the full argument string.
  context: The full command line reveals what was inventoried. Identify whether it used the alias form (' logicaldisk', ' volume list ') or the raw WMI class form ('path win32_logicaldisk get ...'), and which properties were projected — caption, filesystem, freespace, size, volumename, serial. The host-scoped window captures whether this was a single inventory call or one of several wmic queries (service, product, baseboard, volume) run back to back, which together form a broader enumeration sweep rather than an isolated lookup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        CommandLine|contains:
          - ' volumename'
          - ' logicaldisk'
          - 'win32_logicaldisk'
          - ' volume'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned this wmic command, and is that parent chain expected?
  context: The parent reveals how the enumeration was initiated. Legitimate disk inventory typically runs from cmd.exe opened by a user (explorer.exe ancestry) or from an automation or management agent. Suspicious parents include script interpreters (wscript.exe, cscript.exe, mshta.exe), Office applications, wmiprvse.exe (remote WMI execution), or wsmprovhost.exe (WinRM remote execution), which point to automated or remote post-exploitation enumeration rather than an operator at the keyboard.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:cue_process_tool.follow_on
  question: What did wmic.exe spawn afterward on this host?
  context: A read-only disk inventory has no children of its own, so processes that wmic launches afterward reveal whether the tool was used as more than a passive query. Watch for cmd.exe, powershell.exe, or other recon and download tooling spawned from wmic, and for the wmic process call create primitive being used to launch arbitrary commands — patterns that turn a benign-looking inventory into a foothold for further action.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single disk-inventory command may be legitimate troubleshooting, but several discovery commands in rapid succession — whoami, ipconfig, net, systeminfo, nltest, tasklist alongside wmic — is a signature of post-compromise enumeration. Three or more distinct recon binaries within fifteen minutes typically indicates an operator or script building situational awareness before the next stage. Note which other commands fired and under what account.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this disk-recon pattern in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this specific disk-recon firing pattern rather than generic wmic usage. Many hosts firing the same pattern points to scripted inventory or imaging automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
      - CommandLine|contains:
        - ' volumename'
        - ' logicaldisk'
      - CommandLine|contains|all:
        - path
        - win32_logicaldisk
      - CommandLine|contains|all:
        - ' volume'
        - ' list '
      condition: all of selection_*
    fields:
    - host.name
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the disk-recon pattern over the past 90 days, and what command-line structures are typical?
  context: A disk-recon pattern with no prior history on a host is more suspicious than one that runs regularly under stable automation. The query mirrors the rule's selection structure scoped to this host, so the count reflects this specific firing pattern rather than generic wmic usage. The command-line projection lets you compare the typical invocation patterns here against the alerting one — a new pattern, a new projection of properties, or a first-ever occurrence is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
      - CommandLine|contains:
        - ' volumename'
        - ' logicaldisk'
      - CommandLine|contains|all:
        - path
        - win32_logicaldisk
      - CommandLine|contains|all:
        - ' volume'
        - ' list '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was wmic.exe executed from its expected System32 path?
  context: The legitimate binary lives at %SystemRoot%\System32\wbem\wmic.exe. A wmic.exe running from a user temp directory, a download folder, or any path outside System32/SysWOW64 may indicate the binary was copied or renamed to evade path-based allowlists. Compare the Image path against the expected location, and check OriginalFileName to confirm whether a renamed copy is being run under a different file name.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were other discovery or proxy-execution LOLBins used on this host recently?
  context: Operators who use one living-off-the-land binary for enumeration often use several. If wmic was used for disk inventory, check whether other built-in proxy-execution and discovery binaries ran on the same host in the recent window — finding multiple LOLBins building situational awareness or executing code strengthens the case for hands-on post-compromise activity over isolated administration.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\certutil.exe'
          - '\bitsadmin.exe'
          - '\curl.exe'
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\control.exe'
          - '\hh.exe'
          - '\installutil.exe'
          - '\msdt.exe'
          - '\mavinject.exe'
          - '\diskshadow.exe'
          - '\dnscmd.exe'
          - '\esentutl.exe'
          - '\xwizard.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
      - CommandLine|contains:
        - ' volumename'
        - ' logicaldisk'
      - CommandLine|contains|all:
        - path
        - win32_logicaldisk
      - CommandLine|contains|all:
        - ' volume'
        - ' list '
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
      - CommandLine|contains:
        - ' volumename'
        - ' logicaldisk'
      - CommandLine|contains|all:
        - path
        - win32_logicaldisk
      - CommandLine|contains|all:
        - ' volume'
        - ' list '
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user account do across the network around this time?
  context: Tracking the account's activity across all hosts can reveal lateral movement or a wider footprint. If the same account triggered similar enumeration or other activity on multiple endpoints in the alert window, the scope of compromise may be larger than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \WMIC.exe
      - OriginalFileName: wmic.exe
      selection_cli:
      - CommandLine|contains:
        - ' volumename'
        - ' logicaldisk'
      - CommandLine|contains|all:
        - path
        - win32_logicaldisk
      - CommandLine|contains|all:
        - ' volume'
        - ' list '
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Enumeration rarely appears in isolation. Earlier detections on the same host — initial execution, defense tampering, or other reconnaissance — may reveal the stages that preceded this disk inventory and help reconstruct the timeline. Disk and volume recon often sits early in a sequence that continues toward credential access or data staging.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur alone. Examining surrounding activity reveals what led to this enumeration firing and what followed it, including further recon, file staging, or follow-on execution. Novel parent-child pairs stand out at low counts against routine high-count agent telemetry, which is what to scan for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1047
      name: Windows Management Instrumentation
      role: primary
    - id: T1082
      name: System Information Discovery
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: process_creation
      description: wmic.exe is launched (the WMI command-line utility process is created)
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_execution
      description: wmic executes a local disk/volume enumeration query (logicaldisk / win32_logicaldisk / volume list)
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: enumeration_sequence
      description: The disk inventory runs alongside other native discovery utilities as a reconnaissance sweep
      mapped_to: Q6
      source: template:discovery_recon.discovery_burst_detection
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1082
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1047
      covered_by: Q6
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1047
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1047
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: SuspiciousCommandPatterns
      dimension: tuning_parameter
      specific_values: "' volumename', ' logicaldisk', 'path'+'win32_logicaldisk', ' volume'+' list ' (rule); 'wmic process call', 'powershell Invoke-WmiMethod' (analytic)"
      disposition: query_injection
      target_question: Q3
      notes: "The rule's own disk-recon CommandLine|contains terms are surfaced in verify_trigger_match (Q1) and injected into the mechanism_decode selection (Q3) so the host+binary decode is narrowed to the recon shape; the analytic's process-call/Invoke-WmiMethod patterns are flagged as watch-for in the follow_on context (Q5)."
    - element: WMIQueryScope
      dimension: tuning_parameter
      specific_values: "rationale only (\\root\\cimv2, \\root\\subscription namespaces)"
      disposition: context
      target_question: Q3
      notes: "wmic logicaldisk/volume runs in the default root\\cimv2 namespace; the namespace-tuning rationale enriches the decode question but there is no namespace field in process_creation to query."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: rationale only
      disposition: context
      target_question: Q6
      notes: "Discovery-burst question correlates multiple discovery commands in a tight +/-15m window; no fixed numeric threshold in brief to inject."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (interactive vs SYSTEM / low-privileged)"
      disposition: context
      target_question: Q2
      notes: "host_user_context (Q2) and the user_actor baseline/substitute pair (Q11-Q14) cover interactive-vs-service-principal scoping; no specific account value in brief."
    - element: RemoteDestinationThreshold
      dimension: tuning_parameter
      specific_values: rationale only
      disposition: context
      target_question: Q5
      notes: "Counts unique remote hosts contacted via WMI within a window — applies to remote WMI command execution, not this local read-only logicaldisk query which makes no outbound connection. Follow_on (Q5) watches for wmic process call create as the remote/lateral pivot; see also the declined network_connection logsource hint."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted
    - dropped: cue_process_tool.process_chain
      reason: "discovery_recon technique set active — discovery_parent_chain is the more specific parent question for discovery commands"
      kept: template:discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.baseline_deviation
      reason: "REFLECTED host_pattern with the same embedded detection block as lolbin.historical_lolbin_baseline; identical search scoped to host, differing only in field projection. Kept the lolbin version (CommandLine-shape comparison is the more useful baseline for a recon command) over the parent/user projection."
      kept: template:lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.baseline
      reason: "generic host+binary 90d frequency subsumed by lolbin.historical_lolbin_baseline, which counts the rule's specific recon pattern on this host rather than all wmic usage"
      kept: template:lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "host-scoped surrounding-activity aggregation overlaps cue_host_context.clustering (Image+ParentImage) and discovery_recon.discovery_burst_detection (recon-specific); both are more targeted"
      kept: template:cue_host_context.clustering + template:discovery_recon.discovery_burst_detection
    - dropped: discovery_recon.discovery_user_context
      reason: "overlaps universal.host_user_context per its own note; kept the universal version as the Phase 1 triage user/host expectation check"
      kept: template:universal.host_user_context
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "recommended generic tamper sweep; this rule detects read-only disk/volume discovery, not payload execution or credential access, so tamper-as-precursor is not a live disposition driver here. Prior-stage tamper context is carried by cue_host_context.precursor (Q15), which surfaces earlier defense-tampering alerts on the host."
    - template: persistence_scope.process_based_persistence
      reason: "recommended; persistence_scope confirmed by binary match (wmic.exe) but this rule fires on disk/volume discovery, not persistence installation. Concurrent persistence-tool execution on the host is already surfaced by cue_host_context.clustering (Q16) and lolbin.technique_class_lateral (Q10) without an off-topic persistence framing."
    - template: persistence_scope.file_based_persistence
      reason: "recommended; file_event persistence-drop sweep is off-topic for a discovery rule with no file artifact, and would consume a slot better used for the discovery-specific and user-actor coverage required for this rule."
  hint_dispositions:
    - hint: "logsource 'network_connection' (recommended by techniques: T1047) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: declined
      reason: >-
        This rule fires on a local, read-only disk/volume enumeration (wmic
        logicaldisk / win32_logicaldisk / volume list) that makes no outbound
        connection — the network_connection branch of the T1047 analytic applies
        to remote WMI command execution (wmic /node, process call create), not to
        local inventory queries. The remote/lateral pivot is instead watched for
        on the process side in follow_on (Q5), which surfaces wmic spawning
        cmd/powershell or process call create. The RemoteDestinationThreshold
        mutable element is recorded as not-applicable for the same reason. No
        compensating network_connection question authored.
    - hint: "logsource 'wmi' (recommended by techniques: T1047) is not covered and no template family exists — author a custom question with `category: wmi`"
      disposition: declined
      reason: >-
        WMI-Activity/Operational events (5857-5861) record WMI provider and query
        activity, but a local logicaldisk/volume enumeration via the wmic CLI is
        already fully captured by process_creation of wmic.exe with its command
        line, which is the evidence the rule itself fires on and which Q1
        (verify_trigger_match) and Q3 (mechanism_decode) decode directly. A wmi
        logsource question would add provider-trace volume without precision for
        this local read-only query and would not bind to the alerting wmic
        process instance. No compensating wmi question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - discovery_recon
    - lolbin
  generation_stats:
    total_questions: 16
    source_counts:
      template: 16
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 15
      sigma_alert: 1
