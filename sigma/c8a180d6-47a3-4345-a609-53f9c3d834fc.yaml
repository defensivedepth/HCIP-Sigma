name: Suspicious Reconnaissance Activity Using Get-LocalGroupMember Cmdlet
id: ba19f554-e3c4-42dc-ad2d-499c48901ac8
description: |
  Detects use of the PowerShell Get-LocalGroupMember cmdlet to enumerate the membership of privileged
  groups such as local administrators, enterprise admins, Remote Desktop Users, or Exchange Trusted
  Subsystem. The rule fires when a command line contains the Get-LocalGroupMember cmdlet AND references
  one of those privileged group names. Reading group membership is read-only and routine for administrators
  and inventory tooling; the same enumeration is also an early step attackers take to map privileged
  accounts and plan privilege escalation or lateral movement, so the investigation centers on whether the
  user, parent process, and surrounding activity match expected administrative behavior.
type: detection
detection_id: c8a180d6-47a3-4345-a609-53f9c3d834fc
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain Get-LocalGroupMember together with a privileged group name?
  context: Confirm the alert is a true positive before investing further time. The rule fires only when
    the command line contains the Get-LocalGroupMember cmdlet AND names a privileged group (domain admins,
    enterprise admins, an administrator/administrateur variant, Exchange Trusted Subsystem, or a Remote
    Desktop Users localization). Verify both elements are present in the exact command line of the
    triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the Get-LocalGroupMember command enumerate, and were other enumeration
    cmdlets run in the same session?
  context: The full command line reveals which group was queried and whether the call was wrapped in a
    loop, piped into formatting, or chained with other discovery cmdlets. This query returns
    Get-LocalGroupMember invocations on this host in a tight window so a multi-step enumeration script
    is visible as a set, not a single line. Note which privileged groups were targeted and whether the
    output was exported or staged.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Get-LocalGroupMember '
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - User
- source: attack:T1087.001.tuning_parameter
  question: Which privileged groups were named in the enumeration, and is reading those groups expected
    for this user and host?
  context: The rule keys on a specific set of high-value groups - local/domain administrators, enterprise
    admins, Exchange Trusted Subsystem, and Remote Desktop Users (including the French and Spanish
    localizations administrateur, Utilisateurs du Bureau a distance, and Usuarios de escritorio remoto).
    Enumerating these groups maps who holds privileged access on the host. Determine which group names
    appeared and whether routine inventory or troubleshooting on this host legitimately reads them.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - User
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the Get-LocalGroupMember command, and is that parent expected to launch
    PowerShell enumeration?
  context: Group enumeration run interactively typically descends from explorer.exe or a console opened
    by an administrator. The same cmdlet spawned by Office applications, script interpreters (wscript.exe,
    cscript.exe, mshta.exe), wmiprvse.exe, or wsmprovhost.exe points to macro-driven or remote-execution
    enumeration rather than hands-on administration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single group-membership query may be legitimate troubleshooting, but several discovery
    commands in quick succession - whoami, net user, nltest, tasklist, systeminfo - is a signature of
    post-compromise enumeration. Attackers run these to understand their context before choosing the next
    stage. Three or more distinct recon binaries inside fifteen minutes usually indicates automated
    reconnaissance.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:discovery_recon.discovery_user_context
  question: Is this user expected to run administrative discovery commands?
  context: Administrators and IT staff legitimately enumerate group membership for inventory and
    troubleshooting. Standard users rarely have a reason to read who belongs to the administrators or
    enterprise admins groups. The user's role and typical behavior determine whether reading privileged
    group membership is routine or anomalous here.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run PowerShell account-enumeration commands?
  context: Compare this user's current action against their historical behavior. A user who has never run
    Get-LocalGroupMember against privileged groups is more notable than one who does so regularly as part
    of their role. User role and typical toolset should align with reading privileged group membership.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: 'Get-LocalGroupMember '
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-and-PowerShell launch pattern historically routine on this host or across the
    environment, or is the parent unusual for this enumeration?
  context: When the alert fires under a service principal (SYSTEM, LOCAL SERVICE, NETWORK SERVICE, or a
    computer account), user identity does not attribute the behavior. The meaningful pivot is the launch
    mechanism - which parent spawned the enumeration, and whether that pair is historically common. A new
    or rare parent for this enumeration is the deviation worth chasing; a pair already responsible for
    thousands of prior events is inventory or management automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Get-LocalGroupMember '
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or enumeration repeated
    on multiple endpoints. If the same user triggered similar activity elsewhere in this window, the scope
    of the operation may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same parent-and-PowerShell enumeration fire on other hosts in the same window, or is
    it isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns
    agent-telemetry noise, since every host runs service-owned processes constantly. The meaningful
    question is whether the same launch mechanism - parent process plus the enumeration command - fired
    on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed
    automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Get-LocalGroupMember '
      condition: selection
    fields:
      - host.name
- source: attack:T1087.network_indicators
  question: Did remote-access or lateral-movement activity follow the enumeration on this host?
  context: Account discovery is a precursor step; the privileged accounts it surfaces are often used
    shortly afterward for remote access or lateral movement. Look for outbound administrative sessions,
    remote service creation, or remote PowerShell launched from this host in the hours after the
    enumeration, which would indicate the mapped accounts were acted on rather than merely listed.
  range: +4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_remote:
        Image|endswith:
          - '\psexec.exe'
          - '\psexec64.exe'
          - '\wmic.exe'
          - '\winrs.exe'
          - '\mstsc.exe'
          - '\sc.exe'
      condition: selection and selection_remote
    fields:
      - Image
      - CommandLine
      - User
- source: template:cue_process_tool.baseline
  question: Has this host run the Get-LocalGroupMember enumeration pattern before?
  context: A pattern that has never been seen on a host is more notable than one that runs regularly.
    Zero or very low historical counts of this privileged-group enumeration indicate first-time or rare
    execution, while a steady history suggests recurring inventory or management tooling.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Get-LocalGroupMember '
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive
    that activity, and does the current alert fit that pattern?
  context: Even routine enumeration can be abused. The query mirrors the rule's selection structure scoped
    to this host, so the resulting parent and user distribution reflects how this specific privileged-group
    enumeration has historically run here. A new parent process, an unexpected user account, or an unusual
    command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmdlet:
        CommandLine|contains: 'Get-LocalGroupMember '
      selection_group:
        CommandLine|contains:
        - domain admins
        - ' administrator'
        - ' administrateur'
        - enterprise admins
        - Exchange Trusted Subsystem
        - Remote Desktop Users
        - "Utilisateurs du Bureau \xE0 distance"
        - Usuarios de escritorio remoto
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired the rule's pattern in the past week, and does the cross-host
    distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of this privileged-group enumeration pattern. Many hosts firing the same pattern
    points to scripted deployment or inventory automation; one host or a small unrelated set points to
    manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_cmdlet:
        CommandLine|contains: 'Get-LocalGroupMember '
      selection_group:
        CommandLine|contains:
        - domain admins
        - ' administrator'
        - ' administrateur'
        - enterprise admins
        - Exchange Trusted Subsystem
        - Remote Desktop Users
        - "Utilisateurs du Bureau \xE0 distance"
        - Usuarios de escritorio remoto
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to the
    enumeration and what followed it, and whether it is part of a larger sequence. Look for privilege
    escalation attempts, credential access, or persistence mechanisms near the enumeration.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - defense tampering, other reconnaissance, credential
    access - may reveal the stages that preceded this enumeration and help reconstruct the timeline.
    Reconnaissance frequently sits in the middle of a chain, so prior alerts often explain how the
    operator reached this point.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1087.001
      name: Local Account
      role: primary
    - id: T1087
      name: Account Discovery
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: enumeration_execution
      description: "Adversary enumerates local privileged-group membership via the PowerShell Get-LocalGroupMember cmdlet"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: "Parse which privileged groups were queried and whether enumeration was chained or staged"
      mapped_to: Q2
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: post_discovery_use
      description: "Privileged accounts surfaced by enumeration are used for remote access or lateral movement"
      mapped_to: Q11
      source: attack:T1087.network_indicators
  dimension_coverage:
    - dimension: command_analysis
      technique: T1087.001
      covered_by: Q2
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1087.001
      covered_by: Q5
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1087.001
      covered_by: Q3
      source: attack:T1087.001.tuning_parameter
      coverage_type: direct
    - dimension: user_context
      technique: T1087.001
      covered_by: Q6
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
    - dimension: execution_context
      technique: T1087
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: network_indicators
      technique: T1087
      covered_by: Q11
      source: attack:T1087.network_indicators
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1087
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "Get-LocalGroupMember (with privileged group names: domain admins, enterprise admins, administrator, Exchange Trusted Subsystem, Remote Desktop Users)"
      disposition: query_injection
      target_question: Q2
      notes: "Acts on the CommandLine-only-detection hint: rule has null primary_binary/Image, so all adapted scoping queries use CommandLine|contains 'Get-LocalGroupMember ' rather than Image|endswith|expand (Q2 mechanism_decode, Q7 user baseline, Q8 baseline_svc, Q12 host baseline). Full privileged-group list preserved verbatim in the reflected baseline_deviation (Q13) and scope (Q14) queries"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only - restrict to low-privileged or unexpected users"
      disposition: new_question
      target_question: Q6
      notes: "discovery_user_context and user_actor.baseline (Q7) evaluate whether the user is expected to read privileged group membership"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only - bursts of enumeration in short succession"
      disposition: context
      target_question: Q5
      notes: "discovery_burst_detection surfaces 3+ recon binaries within 15m; burst rationale stated in context"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair - non-deduplicable (user-pivot vs service-principal substitute pivot different identity dimensions)"
      kept: both (Q7, Q8)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair - non-deduplicable (user-pivot vs service-principal substitute pivot different identity dimensions)"
      kept: both (Q9, Q10)
    - dropped: cue_process_tool.process_chain
      reason: "discovery_recon.discovery_parent_chain (Q4) is more specific to discovery context and covers the same parent-process pivot"
      kept: template:discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context.clustering (Q15) covers the same 'what else ran on this host' angle with an Image+ParentImage aggregation that surfaces novel parent-child pairs"
      kept: template:cue_host_context.clustering
    - dropped: universal.host_user_context
      reason: "discovery_recon.discovery_user_context (Q6) is more specific to discovery context; template notes direct preferring the discovery version and dropping host_user_context"
      kept: template:discovery_recon.discovery_user_context
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: "Get-LocalGroupMember is a read-only enumeration cmdlet that does not spawn child processes; follow-on remote-access activity is instead covered by attack:T1087.network_indicators (Q11) which scopes to remote-access binaries rather than direct children of the cmdlet"
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This is a read-only discovery rule, not an execution/credential-access/impact rule, so tamper-as-precursor is not a live disposition question; cue_host_context.precursor (Q16) already surfaces any prior tamper alerts on the host"
    - template: account_manipulation.change_success_verification
      reason: "Confirmed set matched on title keyword 'localgroup', but Get-LocalGroupMember READS group membership and makes no change; there is no modification to verify. Security event IDs 4732/4733 (group membership change) do not apply to enumeration"
    - template: account_manipulation.modified_account_post_activity
      reason: "No account is modified by enumeration, so there is no modified account whose post-change activity to track; post-discovery use of surfaced accounts is instead covered by attack:T1087.network_indicators (Q11)"
    - template: account_manipulation.modifying_user_other_changes
      reason: "Query filters for net user / Add-LocalGroupMember / New-LocalUser modification commands; this rule fires on read-only Get-LocalGroupMember, so the modification-command scope does not match the behavior under investigation"
    - template: account_manipulation.change_management_context
      reason: "Change-ticket questions presuppose a configuration change; reading group membership is not a change, so no change-management record would exist for it"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - discovery_recon
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      sigma_alert: 1
