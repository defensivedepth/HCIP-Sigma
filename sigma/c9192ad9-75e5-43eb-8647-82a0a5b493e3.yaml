name: PUA - Mouse Lock Execution
id: c4b9d13b-4d2c-4d10-a7a2-bed4b337386c
description: |
  Detects execution of the "Mouse Lock" utility, identified by its Product name ("Mouse Lock"),
  Company string ("Misc314"), or a command line containing "Mouse Lock_". The tool locks the screen
  behind a password prompt and records attempted passwords, which places it in the credential-access
  and collection space (GUI input capture) even though it is marketed as a benign desktop utility.
  Legitimate use is possible, but the saved-password behaviour means an operator can use it to capture
  whatever a user types at the lock prompt. Determine who ran it, under what parent and account, whether
  this host or user has run it before, and what activity surrounded it.
type: detection
detection_id: c9192ad9-75e5-43eb-8647-82a0a5b493e3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does this process match the Mouse Lock signature the rule detects?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires on
    a Product string containing "Mouse Lock", a Company string containing "Misc314", or a command line
    containing "Mouse Lock_". Review the event's Product, Company, and CommandLine fields against those
    criteria.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Company
      - Product
- source: template:universal.host_user_context
  question: Is this user and host expected to run a screen-lock or password-prompt utility like Mouse Lock?
  context: Legitimate desktop activity can trigger this detection if a user installed Mouse Lock themselves.
    Knowing whether the user role and host function are consistent with running a third-party screen-lock
    tool is often the fastest path to distinguishing personal use from operator-deployed capture.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: What credential input did Mouse Lock prompt for or capture, and where are attempted passwords stored?
  context: Mouse Lock presents a lock-screen password dialog and saves attempted passwords, so the captured
    input is the user's lock password and any guesses an operator entered. Decode the command line for the
    "Mouse Lock_" arguments and identify the configured save location for attempted passwords, because that
    file is the credential-capture artifact for this technique.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned Mouse Lock and is that parent chain expected?
  context: The parent process reveals how the tool was launched. A user double-clicking from explorer.exe
    is consistent with personal use; a launch from a scripting host, a remote-access tool, or an unexpected
    automation parent points to operator deployment for input capture.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Has this host run the Mouse Lock pattern before?
  context: A pattern that has never been seen on a host is more suspicious than one that runs regularly.
    Zero or very low historical counts indicate first-time or rare execution. The query scopes to the
    rule's "Mouse Lock_" command-line pattern rather than a binary name, since the rule has no Image field.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains: 'Mouse Lock_'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the Mouse Lock pattern, what parents and users normally
    drive that activity, and does the current alert fit that pattern?
  context: Even a benign-looking utility can be abused. The query mirrors the rule's selection structure
    scoped to this host, so the resulting parent/user distribution reflects how the rule's specific firing
    pattern has historically run here. A new parent process, an unexpected user account, or an unusual
    command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
      - Product|contains: Mouse Lock
      - Company|contains: Misc314
      - CommandLine|contains: Mouse Lock_
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did Mouse Lock spawn afterward?
  context: The processes spawned by the tool reveal its actual impact. A screen-lock utility has few
    expected children; spawning cmd.exe, powershell.exe, or file-copy tools afterward suggests the
    execution was a foothold for further action rather than a simple desktop lock.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired the Mouse Lock pattern in the past week, and does the cross-host
    distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of the rule's firing pattern. Many hosts firing the same pattern points to scripted
    deployment or a software-distribution rollout; one host or a small unrelated set points to manual
    operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection:
      - Product|contains: Mouse Lock
      - Company|contains: Misc314
      - CommandLine|contains: Mouse Lock_
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run the Mouse Lock pattern?
  context: Compare this user's current action against their historical behaviour. A user who has never
    run this pattern is more suspicious than one who does so regularly. The query scopes to the rule's
    "Mouse Lock_" command-line pattern across all hosts so the result reflects this user's own history
    with the tool.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains: 'Mouse Lock_'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-command pattern historically routine on this host or across the
    environment, or is the parent unusual for Mouse Lock?
  context: When the alert fires under a service principal, user identity does not attribute behaviour.
    The meaningful pivot is the launch mechanism - which parent process spawned the "Mouse Lock_" pattern,
    and whether that pair is historically common. A new or rare parent for this pattern is the deviation
    worth chasing; a pair already accounting for many prior events is automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Mouse Lock_'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the
    incident may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-command pattern fire on other hosts in the same window, indicating
    a coordinated rollout rather than isolated activity?
  context: Under a service principal, asking what else the user did across the network returns
    agent-telemetry noise. The meaningful question is whether the same launch mechanism - parent process
    plus the "Mouse Lock_" command pattern - fired on other hosts in the alert window. Multiple hosts in
    the same window points to scheduled or pushed deployment; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains: 'Mouse Lock_'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate
    context: what led to this event, what happened after, and whether it is part of a larger sequence.
    Look for reconnaissance, credential access, or staging that surrounds the Mouse Lock execution.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host - defense tampering,
    reconnaissance, credential access - may reveal the stages that preceded this event and help
    reconstruct the timeline around the input-capture activity.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be exposed by captured input, and do any have elevated privileges?
  context: The severity of credential capture depends on whose password could have been recorded. The
    account that unlocks at this host's lock prompt - and any privileged account a user might type while
    the tool is running - determines remediation. A standard user may only need a password reset; a domain
    admin or service account requires escalation. Cross-reference the user with directory group memberships.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were any new logon sessions or remote connections initiated after this event?
  context: Captured credentials are typically followed by lateral movement - the operator uses recorded
    passwords to access other systems. New logon events or outbound remote connections shortly after this
    execution indicate active use of the captured input. Look specifically for RDP, SMB, WinRM, or new
    authentication activity on this host after the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1056.002
      name: GUI Input Capture
      role: primary
    - id: T1056
      name: Input Capture
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: spoofed_prompt_execution
      description: "Tool is launched and presents a GUI/lock prompt that captures typed input"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 2
      label: parent_invocation
      description: "Prompt-presenting process is spawned from a parent (legitimate desktop vs. operator/script)"
      mapped_to: Q4
      source: template:cue_process_tool.process_chain
    - stage: 3
      label: captured_credential_use
      description: "Captured input is used for follow-on access / lateral movement"
      mapped_to: Q16
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: command_analysis
      technique: T1056.002
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1056.002
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1056.002
      covered_by: Q13
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1056.002
      covered_by: Q6
      source: template:cue_process_tool.baseline_deviation
      coverage_type: direct
    - dimension: network_indicators
      technique: T1056
      covered_by: Q16
      source: template:credential_access.post_access_lateral_movement
      coverage_type: indirect
    - dimension: process_identification
      technique: T1056
      covered_by: Q7
      source: template:cue_process_tool.follow_on
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLine
      dimension: command_analysis
      specific_values: "Mouse Lock_"
      disposition: query_injection
      target_question: Q5
      notes: "Rule's CommandLine|contains value injected into baseline/user-baseline/svc queries (Q5, Q9, Q10, Q12) for CommandLine-only scoping; also enforced in reflected Q6/Q8."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (tune for unexpected parents like cmd.exe / scripts)"
      disposition: context
      target_question: Q4
      notes: "Parent legitimacy framed in process_chain context; no specific parent values in the analytic to inject."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (scope correlation window)"
      disposition: context
      target_question: Q13
      notes: "Correlation window expressed via the +/-15m range on host clustering; no threshold value to inject."
    - element: TargetImage
      dimension: tuning_parameter
      specific_values: "rationale only (sensitive GUI processes like explorer.exe / winlogon.exe)"
      disposition: context
      target_question: Q6
      notes: "T1056 process_access tuning has no value applicable to this CommandLine-only process_creation rule; deviation pattern captured via reflected baseline_deviation."
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "host-scoped 'what else ran on this host around the same time' duplicates cue_host_context.clustering (same host.name aggregation, same window intent); kept the host_context version."
      kept: cue_host_context.clustering
    - dropped: null
      reason: "user_actor pair — non-deduplicable; cue_user_actor.baseline (Q9) + baseline_svc (Q10) and clustering (Q11) + clustering_svc (Q12) emitted as paired-and-distinct identity/mechanism pivots per AGENTS.md."
      kept: cue_user_actor.baseline / baseline_svc / clustering / clustering_svc
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (Q3) — hint acted on."
    - template: universal.endpoint_protection_health
      reason: "Recommended-only. Mouse Lock is a GUI screen-lock PUA that captures input; it is not a hands-on payload an operator would precede by disabling AV/EDR, and no host-tamper-as-precursor question is otherwise needed. Prior-alert tamper context is still surfaced by cue_host_context.precursor (Q14)."
    - template: data_staging_exfil.data_scope
      reason: "data_staging_exfil set is tactic-keyword (collection) driven; its archive/export questions (rar/7z/rclone file selection) do not apply to a screen-lock input-capture tool with no archiving behaviour."
    - template: data_staging_exfil.archive_destination
      reason: "Archive-move question is archiving-tool specific; Mouse Lock produces no archive. No applicable adaptation."
    - template: data_staging_exfil.encryption_password
      reason: "Archive encryption-flag question (-p/-hp/--passphrase) has no analogue for this tool; not applicable."
    - template: remote_access_tool.tool_authorization
      reason: "remote_access_tool set is filename_prefix (pua) driven; Mouse Lock is a screen-lock utility, not a remote access / RMM / tunneling tool, so the approved-RAT-list question does not fit."
    - template: remote_access_tool.active_session_context
      reason: "Helpdesk-session-for-remote-access framing does not apply; Mouse Lock establishes no remote session. host_user_context (Q2) already covers expected-use judgement."
    - template: remote_access_tool.connection_destination
      reason: "External-endpoint question targets RAT relay/tunnel infrastructure; Mouse Lock makes no such outbound connection. Follow-on remote connections are covered by post_access_lateral_movement (Q16)."
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 16
    source_counts:
      template: 16
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 14
      sigma_alert: 2
