name: Obfuscated IP Download Activity
id: 55bd6f22-8a2a-44ca-8781-60a98bab611a
description: |
  Detects a command-line download invocation (Invoke-WebRequest/iwr, Invoke-RestMethod/irm, wget, curl, DownloadFile,
  DownloadString) whose target is an obfuscated representation of an IP address rather than a normal dotted-quad. The rule
  fires when the command line contains a download verb AND at least one obfuscated-address pattern matches -- either a
  hex form (' 0x', '//0x', '.0x'), a URL-encoded host ('http://' combined with '%2e'), or a regex-matched octal/decimal
  packed-integer host -- AND the command line does NOT contain a plain valid dotted-quad IP. Encoding an IP as hex, octal,
  or a single packed integer hides the destination from casual inspection and from naive IOC string matching; it is a
  common evasion in download cradles delivering follow-on payloads. Determine what address the obfuscated host decodes
  to, whether the download succeeded, and whether the destination and behavior fit any sanctioned activity on this host.
type: detection
detection_id: cb5a2333-56cf-4562-8fcb-22ba1bca728d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain a download verb AND an obfuscated IP host, with no plain dotted-quad IP present?
  context: Confirm the alert is a true positive before investing time. The rule fires only when ALL of these hold together
    on one command line - a download verb is present (Invoke-WebRequest, iwr, Invoke-RestMethod, irm, wget, curl, DownloadFile,
    or DownloadString); AND at least one obfuscated-host pattern matches - EITHER a hex form (' 0x', '//0x', '.0x', '.00x'),
    OR a URL-encoded host ('http://' together with '%2e'), OR a regex-matched octal/decimal packed-integer host; AND no plain
    valid dotted-quad IP appears in the same command line. Note which obfuscation branch fired, because that drives how you
    decode the host below.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to download files from raw numeric IP addresses via command-line tools?
  context: Legitimate administrative scripting occasionally pulls files with curl or PowerShell download methods, but pointing
    those tools at an obfuscated numeric IP rather than a hostname is unusual for routine operations. Knowing whether the
    user role and host function include command-line downloads helps separate sanctioned automation from operator activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:obfuscation.encoded_payload_decode
  question: What address does the obfuscated host decode to, and what does the download command actually fetch?
  context: The obfuscated host is the operator hiding the destination from casual inspection and from naive IOC matching.
    Convert the host to its canonical dotted-quad - hex forms (0x25.0x1b.0x29.0xc3 or a packed 0x251b29c3), octal forms
    (045.033.051.0303 or packed 04506624703), packed decimal integers (622537155), and URL-encoded hosts ('%33%37%2e...'
    decode to '37.27...'). Then read the full command to identify the download verb, the requested resource path, and any
    output file path so you know what was retrieved and where it was written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_network_endpoint.mechanism_decode
  question: Once decoded to a canonical IP, what does threat intel, WHOIS, and passive DNS reveal about the destination?
  context: After resolving the obfuscated host to a dotted-quad, investigate the destination directly - registration age,
    hosting/ASN, passive DNS history, and any known threat-intel hits. Raw-IP destinations with no associated domain,
    recently allocated ranges, or bulletproof-hosting ASNs point to attacker-controlled infrastructure rather than a known
    service. The decoded address comes from the alerting command line; this lookup is performed manually against intel
    sources.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationIp|expand: '%related_ip%'
      condition: selection
    fields:
      - DestinationHostname
      - DestinationPort
      - Image
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the download command, and is that parent chain expected?
  context: A command-line download from an obfuscated IP launched from explorer.exe or an interactive shell is a different
    story from the same command spawned by winword.exe, wscript.exe, mshta.exe, or a PowerShell download cradle. An office
    application or script-host parent indicates the download was driven by a macro or staged loader rather than a user
    typing the command.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single download may be isolated, but obfuscated-IP retrieval clustered with rapid enumeration - whoami, ipconfig,
    net user, systeminfo, nltest - is a signature of post-compromise staging. Attackers commonly pull a payload, then survey
    the host before moving to the next stage. Multiple distinct recon binaries within minutes points to automated activity
    rather than ad-hoc troubleshooting.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:download_tool.download_success_verification
  question: Was the download successful? Check for file creation at the destination path.
  context: A download command does not guarantee the file was retrieved - network errors, a blocked or dead obfuscated host,
    or security controls may have stopped the transfer. Confirming a file was written establishes whether the activity
    progressed past the delivery stage. The destination path comes from the download command's output argument; if none is
    visible, look for new files written by the downloading process.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: Can you retrieve and analyze the downloaded payload from a safe environment?
  context: If the fetched file is still on disk, submitting its hash to threat-intel services or detonating it in a sandbox
    reveals whether it is a known malicious payload, a legitimate tool, or something novel needing deeper analysis. The
    obfuscated destination strongly suggests the operator did not want this file inspected.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the target directory?
  context: Operators often stage multiple files in the same directory, or the fetched file triggers further writes
    (extraction, decompression, additional downloads). Examining directory-level file activity reveals the broader scope
    of the delivery operation beyond the single file the command named.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_dirs%'
      filter_self:
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection and not filter_self
    fields:
      - TargetFilename
      - Image
- source: template:universal.evidence_preservation
  question: Is the downloaded or referenced file still on disk for analysis?
  context: If the command wrote a payload to disk, checking whether it still exists enables hash lookups, sandbox
    submission, and content review. Files that have already been deleted or moved suggest the operator cleaned up after
    staging, which itself is a meaningful signal.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this obfuscated-download pattern, what parents and users drove it, and does
    the current alert fit?
  context: The query mirrors the rule's full selection structure scoped to this host, so the parent/user distribution
    reflects how this specific obfuscated-IP download pattern - not generic curl or PowerShell usage - has historically run
    here. A new parent process, an unexpected user account, or a first-time appearance of the pattern is the deviation worth
    chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_command:
        CommandLine|contains:
        - Invoke-WebRequest
        - 'iwr '
        - Invoke-RestMethod
        - 'irm '
        - 'wget '
        - 'curl '
        - DownloadFile
        - DownloadString
      selection_ip_1:
        CommandLine|contains:
        - ' 0x'
        - //0x
        - .0x
        - .00x
      selection_ip_2:
        CommandLine|contains|all:
        - http://%
        - '%2e'
      selection_ip_3:
      - CommandLine|re: https?://[0-9]{1,3}\.[0-9]{1,3}\.0[0-9]{3,4}
      - CommandLine|re: https?://[0-9]{1,3}\.0[0-9]{3,7}
      - CommandLine|re: https?://0[0-9]{3,11}
      - CommandLine|re: https?://(?:0[0-9]{1,11}\.){3}0[0-9]{1,11}
      - CommandLine|re: https?://0[0-9]{1,11}
      - CommandLine|re: ' [0-7]{7,13}'
      filter_main_valid_ip:
        CommandLine|re: https?://(?:(?:25[0-5]|(?:2[0-4]|1\d|[1-9])?\d)(?:\.|\b)){4}
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection_command and 1 of selection_ip_* and not 1 of filter_main_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this obfuscated-download pattern in the past week, and does the distribution look like
    automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of this
    obfuscated-IP download pattern - not generic download-tool usage. Many hosts firing the same pattern points to scripted
    deployment or vendor automation; one host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_command:
        CommandLine|contains:
        - Invoke-WebRequest
        - 'iwr '
        - Invoke-RestMethod
        - 'irm '
        - 'wget '
        - 'curl '
        - DownloadFile
        - DownloadString
      selection_ip_1:
        CommandLine|contains:
        - ' 0x'
        - //0x
        - .0x
        - .00x
      selection_ip_2:
        CommandLine|contains|all:
        - http://%
        - '%2e'
      selection_ip_3:
      - CommandLine|re: https?://[0-9]{1,3}\.[0-9]{1,3}\.0[0-9]{3,4}
      - CommandLine|re: https?://[0-9]{1,3}\.0[0-9]{3,7}
      - CommandLine|re: https?://0[0-9]{3,11}
      - CommandLine|re: https?://(?:0[0-9]{1,11}\.){3}0[0-9]{1,11}
      - CommandLine|re: https?://0[0-9]{1,11}
      - CommandLine|re: ' [0-7]{7,13}'
      filter_main_valid_ip:
        CommandLine|re: https?://(?:(?:25[0-5]|(?:2[0-4]|1\d|[1-9])?\d)(?:\.|\b)){4}
      condition: selection_command and 1 of selection_ip_* and not 1 of filter_main_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run command-line download tools?
  context: Compare this user's current action against their historical behavior. A user who has never invoked curl,
    wget, or PowerShell download methods is more suspicious than one who does so as part of a documented role. User role
    and typical toolset should align with command-line file retrieval.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains:
        - Invoke-WebRequest
        - 'iwr '
        - Invoke-RestMethod
        - 'irm '
        - 'wget '
        - 'curl '
        - DownloadFile
        - DownloadString
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is the parent+download-tool combination historically routine on this host or environment-wide, or is the parent
    unusual?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot
    is the launch mechanism - which parent spawned the download tool, and whether that pair is historically common. A new or
    rare parent for command-line downloads on this host is the deviation worth chasing; a pair already accounting for
    thousands of prior events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains:
        - Invoke-WebRequest
        - 'iwr '
        - Invoke-RestMethod
        - 'irm '
        - 'wget '
        - 'curl '
        - DownloadFile
        - DownloadString
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging across multiple endpoints. If
    the same user drove similar download or enumeration activity on other hosts, the scope of the operation may be larger
    than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did the same parent+download-tool combination fire on other hosts in the same window, indicating a coordinated
    rollout?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism - parent process plus download tool - fired on other hosts in
    the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains:
        - Invoke-WebRequest
        - 'iwr '
        - Invoke-RestMethod
        - 'irm '
        - 'wget '
        - 'curl '
        - DownloadFile
        - DownloadString
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host - defense tampering, reconnaissance, or
    other execution events - may reveal the attack stages that preceded the download and help reconstruct the timeline. An
    obfuscated-IP download is frequently a mid-chain delivery step rather than the first action.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: false
  techniques: []
  attack_chain_coverage: []
  dimension_coverage: []
  mutable_dispositions: []
  dedup_decisions:
  - dropped: cue_process_tool.mechanism_decode
    reason: "obfuscation technique-set encoded_payload_decode is the more specific Phase 2 mechanism-decode for this
      obfuscated-IP rule, and the cue version scopes by Image|endswith %Image% which has no value (CommandLine-only rule,
      primary_binary null). Kept the obfuscation question."
    kept: obfuscation.encoded_payload_decode
  - dropped: cue_process_tool.process_chain
    reason: "parent-chain coverage provided by discovery_recon.discovery_parent_chain (ProcessGuid-scoped parent lookup),
      which is more specific to this discovery-tactic rule than the generic ParentProcessGuid cue lookup."
    kept: discovery_recon.discovery_parent_chain
  - dropped: discovery_recon.discovery_user_context
    reason: "overlaps universal.host_user_context per the template note; kept the universal triage anchor (host_user_context)
      which is already adapted to the download operation, dropped the discovery duplicate."
    kept: universal.host_user_context
  - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
    reason: "user_actor pair — non-deduplicable; emitted BOTH per AGENTS.md (user-pivot vs parent+binary substitute)."
    kept: both
  - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
    reason: "user_actor pair — non-deduplicable; emitted BOTH per AGENTS.md (user-pivot vs parent+binary substitute)."
    kept: both
  skipped_templates:
  - template: cue_process_tool.baseline
    reason: "scopes by Image|endswith %Image% (null on this CommandLine-only rule); the reflected baseline_deviation question
      provides historical-pattern baseline scoped by the rule's actual CommandLine selection instead."
  - template: cue_process_tool.follow_on
    reason: "scopes children by ParentImage|endswith %Image% (null on this CommandLine-only rule, no primary_binary); cannot
      compose a meaningful binary-scoped follow-on query."
  - template: cue_process_tool.clustering
    reason: "host-wide process clustering is covered functionally by discovery_recon.discovery_burst_detection (same +/-15m
      host window, recon-focused) and the user-actor clustering pair; dropped to stay within the 18-question cap."
  - template: cue_network_endpoint.baseline
    reason: "network_connection baseline keys on DestinationIp; the destination is only present as an obfuscated string in
      CommandLine and is not reliably resolvable to a network_connection event at generation time. Destination
      reputation/history is covered by cue_network_endpoint.mechanism_decode."
  - template: cue_artifact.mechanism_decode
    reason: "file content analysis; the downloaded payload (if written) is covered by download_tool.payload_analysis (hash +
      sandbox) and download_tool.download_success_verification (file creation). No separate file_event content question
      added to stay within the cap."
  - template: cue_artifact.artifact_verification
    reason: "hash/reputation lookup of the downloaded file is provided by download_tool.payload_analysis, which returns
      TargetFilename + Hashes for the same file_paths scope."
  - template: cue_artifact.baseline
    reason: "first-seen baseline of the downloaded file overlaps universal.evidence_preservation and
      download_tool.download_success_verification on the same %file_paths% scope; dropped to stay within the cap."
  - template: cue_host_context.clustering
    reason: "host-wide process clustering overlaps discovery_recon.discovery_burst_detection (recon-focused, same host
      +/-15m window) and the user-actor clustering pair; dropped to stay within the 18-question cap."
  - template: universal.endpoint_protection_health
    reason: "recommended, not must_include. This is a delivery/download rule, not an execution or credential-access rule
      whose disposition turns on prior AV/EDR tampering; cue_host_context.precursor already surfaces any prior tamper alerts
      on the host. Dropped to stay within the 18-question cap."
  reflection_overrides: []
  hint_dispositions:
  - hint: "cue_network_endpoint: derived: https?:// in CommandLine"
    disposition: declined
    reason: "Not declined as coverage — acted on via the cue_network_endpoint.mechanism_decode question (decode obfuscated
      host, then WHOIS/passive-DNS/threat-intel on the canonical IP). Recorded here only to note the derived-cue origin; the
      derived cue drove a real question."
  - hint: "cue_artifact: derived: \\bDownloadFile\\b in CommandLine"
    disposition: declined
    reason: "Derived artifact cue is addressed by the download_tool technique set (download_success_verification,
      payload_analysis, directory_activity) and universal.evidence_preservation, which together cover file creation, hash,
      sandbox, and directory scope for the downloaded payload. No separate cue_artifact.* question authored because the
      download_tool set is more specific to a download rule."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
  - universal
  - discovery_recon
  - download_tool
  - obfuscation
  - cue_process_tool
  - cue_network_endpoint
  - cue_user_actor
  - cue_host_context
  generation_stats:
    total_questions: 17
    source_counts:
      template: 17
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 11
      file_event: 4
      network_connection: 1
      sigma_alert: 1
