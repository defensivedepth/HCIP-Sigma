name: Registry Export of Third-Party Credentials
id: 6b731ba6-eb06-436f-8eb4-70c6a48640f7
description: |
  Detects reg.exe invoked with a save or export operation against a registry path that stores third-party
  application credentials (FTP clients, SSH/SFTP session managers, VNC servers, mail clients). The rule fires
  only when all three conditions match together: the image is reg.exe (or OriginalFileName reg.exe), the
  command line contains a save/export verb, and it contains one of the watched credential subkeys. Manually
  backing up a single application's settings can produce one such command, but credential-stealer families
  dump several of these subkeys in quick succession to harvest stored passwords offline. Determine whether the
  exported subkey, the parent process, the user, and any follow-on activity point to a legitimate backup or to
  credential theft.
type: detection
detection_id: cc1abf27-78a3-4ac5-a51c-f3070b1d8e40
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show reg.exe running a save or export against a third-party credential registry path?
  context: Confirm the alert is a true positive before investing time. The rule fires only when all three pieces
    are present together — the image is reg.exe (or OriginalFileName is reg.exe), the command line contains a
    save or export verb, and it contains one of the watched credential subkeys (for example WinSCP Sessions,
    Sota FFFTP, PuTTY SshHostKeys, OpenSSH Agent Keys). Read the exact command and confirm which subkey was
    targeted and where the output was written.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to back up third-party application credentials from the registry?
  context: Legitimate backup of an FTP client, SSH session manager, or VNC server's saved settings can trigger
    this rule when an admin or the application's own owner exports their configuration. Knowing whether this
    user's role and this host's function involve maintaining those applications is often the fastest way to
    separate a routine backup from credential theft.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: Which third-party credential store was targeted by the export?
  context: Different watched subkeys hold different secrets — WinSCP Sessions and Sota FFFTP store saved FTP/SFTP
    server passwords (FFFTP keeps base64-obfuscated passwords, WinSCP uses a reversible host-and-user XOR scheme),
    PuTTY SshHostKeys and OpenSSH Agent Keys hold SSH material, and the VNC entries hold remote-access passwords.
    Identifying the exact subkey determines which application accounts may be exposed and what must be rotated.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1552.002.command_analysis
  question: Decode the full reg export command — which subkey was exported, what output file was written, and was the overwrite flag set?
  context: The full command line reveals the operation's intent. Identify the exact source subkey, the destination
    .reg file path (these families write to %LOCALAPPDATA% with names like FFFTP_registry.reg or WinSCP_registry.reg),
    and whether a silent-overwrite flag such as /y was used. The destination path is what a follow-on file query
    looks for on disk. A single subkey export under a /y flag pointed at a temp or AppData path is consistent with
    automated harvesting rather than an interactive backup.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:universal.evidence_preservation
  question: Is the exported .reg file still on disk for analysis?
  context: The export writes a .reg file containing the harvested credential subkey (commonly under %LOCALAPPDATA%
    with names like FFFTP_registry.reg or WinSCP_registry.reg). If it is still present, it can be hashed, reviewed,
    or submitted for analysis to confirm exactly which saved sessions and passwords were captured. Its absence may
    mean it was already exfiltrated and deleted.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned reg.exe, and is that parent chain expected for a credential export?
  context: The parent reveals how the export was launched. Interactive backups are typically driven by explorer.exe
    or a console the user opened. A command shell spawned by an unsigned executable from a user-writable path, or
    a chain that does not correspond to any administrative task, points to automated harvesting. Walk up the tree
    to see what spawned the parent and what else that parent launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What other processes did the same parent spawn after the export?
  context: The processes launched alongside or after the export reveal the operation's broader goal. Credential-stealer
    families frequently pair these registry exports with a headless browser launch (chrome.exe with --headless and
    --silent-launch to crawl browser-stored secrets without a visible window) and with archiving or network tools
    used to stage and exfiltrate the collected data. Unexpected children indicate the export was one stage of a
    multi-step harvest.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Registry credential exports rarely happen in isolation. Activity in the surrounding window may reveal
    parallel exports of other watched subkeys, browser-data harvesting, reconnaissance, or staging that together
    define the scope. Novel parent-child pairs at low counts are what to scan for against the high-count background
    of routine host telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether the same account exported credential
    subkeys on multiple endpoints or moved between them. If the same user triggered similar activity on several
    hosts, the scope of credential exposure is larger than a single machine.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:credential_access.credential_exposure_assessment
  question: Which application accounts may be compromised, and do any grant access to sensitive systems?
  context: The severity depends on what the exported saved sessions can reach. FTP/SFTP and SSH session stores
    often hold credentials for production servers, code repositories, or transfer endpoints; VNC and Radmin entries
    grant interactive remote control. Enumerate the saved hosts and accounts in the targeted store and assess which
    grant access to sensitive systems requiring rotation or escalation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this reg.exe credential-export pattern over the past 90 days, and what command-line structures are typical?
  context: A credential-export pattern with no prior history on a host is more suspicious than one that recurs under
    a stable parent. The query mirrors the rule's selection structure scoped to this host, so the count reflects this
    specific firing pattern rather than generic reg.exe usage. The command-line projection lets you compare typical
    invocation patterns against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this credential-export pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even routine maintenance scripts can match this pattern. The query mirrors the rule's selection structure
    scoped to this host — the resulting parent and user distribution reflects how this specific firing pattern has
    historically run here. A new parent process, an unexpected user account, or an unusual command-line structure is the
    anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_cli_save:
        CommandLine|contains:
        - save
        - export
      selection_cli_path:
        CommandLine|contains:
        - \Software\Aerofox\Foxmail\V3.1
        - \Software\Aerofox\FoxmailPreview
        - \Software\DownloadManager\Passwords
        - \Software\FTPWare\COREFTP\Sites
        - \Software\IncrediMail\Identities
        - \Software\Martin Prikryl\WinSCP 2\Sessions
        - \Software\Mobatek\MobaXterm
        - \Software\OpenSSH\Agent\Keys
        - \Software\OpenVPN-GUI\configs
        - \Software\ORL\WinVNC3\Password
        - \Software\Qualcomm\Eudora\CommandLine
        - \Software\RealVNC\WinVNC4
        - \Software\RimArts\B2\Settings
        - \Software\SimonTatham\PuTTY\Sessions
        - \Software\SimonTatham\PuTTY\SshHostKeys
        - \Software\Sota\FFFTP
        - \Software\TightVNC\Server
        - \Software\WOW6432Node\Radmin\v3.0\Server\Parameters\Radmin
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:lolbin.technique_class_lateral
  question: Were other credential or registry LOLBins used on this host recently for the same objective?
  context: Operators who use reg.exe to dump one credential store often reach for additional tools — esentutl or
    extrac32 to copy locked files, certutil to encode the staged data, or other registry-capable binaries — to
    complete a harvest. Finding several of these performing related work in the same window strengthens the case
    that this is deliberate credential collection rather than an isolated backup.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\certutil.exe'
          - '\esentutl.exe'
          - '\extrac32.exe'
          - '\expand.exe'
          - '\makecab.exe'
          - '\wmic.exe'
          - '\regsvr32.exe'
          - '\rundll32.exe'
          - '\bitsadmin.exe'
          - '\curl.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was reg.exe executed from its expected System32 or SysWOW64 path?
  context: reg.exe run from outside System32 or SysWOW64 may have been copied or renamed to evade path-based
    allowlists. A 32-bit stealer running under WOW64 will legitimately invoke SysWOW64\reg.exe, but reg.exe
    launched from a user temp or AppData directory is more suspicious than one in its standard location. Confirm
    the image path and OriginalFileName agree with a genuine system reg.exe.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1552.002
      name: Credentials in Registry
      role: primary
    - id: T1552
      name: Unsecured Credentials
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: registry_credential_read
      description: "reg.exe save/export invoked against a watched third-party credential subkey (FTP/SSH/VNC/mail client)"
      mapped_to: Q4
      source: attack:T1552.002.command_analysis
    - stage: 2
      label: credential_store_identification
      description: "Identify which specific credential store / saved sessions were captured"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 3
      label: exported_artifact_on_disk
      description: "The .reg output file written to disk before exfiltration"
      mapped_to: Q5
      source: template:universal.evidence_preservation
    - stage: 4
      label: follow_on_harvest_and_use
      description: "Headless-browser harvest, staging, or use of the stolen credentials (lateral movement / remote sessions)"
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1552.002
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1552.002
      covered_by: Q14
      source: template:credential_access.post_access_lateral_movement
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1552.002
      covered_by: Q4
      source: attack:T1552.002.command_analysis
      coverage_type: direct
    - dimension: command_analysis
      technique: T1552
      covered_by: Q4
      source: attack:T1552.002.command_analysis
      coverage_type: direct
    - dimension: process_identification
      technique: T1552
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1552
      covered_by: Q9
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: KeywordMatch
      dimension: tuning_parameter
      specific_values: "watched credential subkeys (WinSCP Sessions, Sota FFFTP, PuTTY SshHostKeys, OpenSSH Agent Keys, VNC/Radmin, etc.) + save/export verbs"
      disposition: query_injection
      target_question: Q4
      notes: "Rule's selection_cli_save + selection_cli_path embedded verbatim in Q4 (and the reflected Q15/Q16); these ARE the credential keyword set this technique's mutable element calls for."
    - element: ParentProcessFilter
      dimension: parent_process_analysis
      specific_values: "rationale only (cmd.exe>reg.exe vs services.exe>reg.exe)"
      disposition: new_question
      target_question: Q6
      notes: "Parent-ancestry analysis surfaced via process_chain (Q6) and the reflected parent/user distribution (Q16)."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window between read and subsequent use)"
      disposition: context
      target_question: Q14
      notes: "Realized as the +4h follow-on lateral-movement window (Q14) and +/-15m host clustering (Q8)."
    - element: RegistryHiveScope
      dimension: tuning_parameter
      specific_values: "HKCU vs HKLM"
      disposition: context
      target_question: Q3
      notes: "All watched paths are HKCU\\Software\\*; Q3 context names the per-application user-hive stores. No HKLM angle to query."
    - element: SuspiciousProcessList
      dimension: tuning_parameter
      specific_values: "reg.exe (rule-fixed); cmd.exe/powershell.exe parents"
      disposition: context
      target_question: Q6
      notes: "Rule is reg.exe-fixed; parent-process suspicion handled in Q6 process_chain context."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "replaced_by credential_access.credential_target_identification (Q3) per template; the command decode angle is additionally carried by attack:T1552.002.command_analysis (Q4)"
      kept: credential_access.credential_target_identification
    - dropped: cue_process_tool.baseline
      reason: "lolbin.historical_lolbin_baseline (Q15) is the rule-pattern-scoped baseline (more specific than generic Image|endswith host baseline) and supersedes it"
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_user_actor pair non-dedup
      reason: "user_actor pair — non-deduplicable: Q9/Q11 (user-pivot) and Q10/Q12 (service_principal_substitute) pivot on different identity dimensions; both emitted"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc + cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: cue_process_tool.scope
      reason: "Fleet-wide reflected pattern count; the host-scoped reflected baseline (Q15) plus host clustering (Q8) already calibrate severity, and the cross-host rollout angle is covered by the service-principal clustering substitute (Q12). Dropped to stay within the 18-question cap."
    - template: cue_host_context.precursor
      reason: "Prior-alert-on-host timeline is partly carried by the +4h follow-on alert correlation (Q14); dropped to stay within cap. Rule is credential-read, not execution, so a dedicated -6h precursor sweep adds less than the retained credential-specific questions."
    - template: universal.endpoint_protection_health
      reason: "Generic host tamper-precursor sweep; reg.exe credential export is a quiet read operation, not a hands-on-keyboard payload-execution step that an operator typically precedes by disabling AV/EDR. Tamper-as-precursor is not the live question for this alert; dropped to stay within cap."
    - template: registry.registry_key_context
      reason: "Query targets registry_set (EventCode 13/14 write) events scoped to the alert ProcessGuid; reg.exe save/export READS the hive and produces no registry_set write event, so the query returns nothing. The actual targeted key is read from the CommandLine in Q3/Q4 instead."
    - template: registry.registry_related_modifications
      reason: "Looks for reg.exe write operations (add/delete/import/s) on the host; this rule fires on read-only export, and the multi-export harvest pattern is already surfaced by host clustering (Q8) and the reflected host baseline (Q15/Q16). The write-operation filter would miss the export verbs this rule cares about."
    - template: persistence_scope.process_based_persistence
      reason: "persistence_scope confirmed by reg.exe binary, but this rule fires on credential-access (T1552.002), not persistence; no Run-key/service write is involved. Dropped to stay within cap in favor of credential-specific coverage."
    - template: persistence_scope.file_based_persistence
      reason: "Startup-folder / Run-key file-drop sweep; the load-bearing file artifact here is the exported .reg credential file, already covered by evidence_preservation (Q5). No persistence-drop angle for a read-only credential export."
  reflection_overrides: []
  hint_dispositions:
    - hint: "registry.registry_key_context replaces cue_process_tool.mechanism_decode"
      disposition: declined
      reason: >-
        registry_key_context's query targets registry_set (EventCode 13/14 write) events scoped to the alert
        ProcessGuid, but reg.exe save/export READS the hive and produces no registry_set write event, so that
        query returns nothing for this rule. The mechanism_decode slot it would fill is instead occupied by the
        sibling replacement credential_access.credential_target_identification (Q3) plus the command decode in
        Q4, both reading the targeted subkey from the CommandLine. No registry_set-scoped question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - lolbin
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 16
      file_event: 1
      sigma_alert: 1
