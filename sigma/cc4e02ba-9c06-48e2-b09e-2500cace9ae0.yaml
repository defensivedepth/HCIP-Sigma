name: Tasks Folder Evasion
id: 6453066f-2852-43ec-aeae-baf66873da78
description: |
  Detects a command line that both invokes a file-writing verb (echo, copy, type, or file createnew)
  AND references the globally writable Tasks folders under C:\Windows\System32\Tasks\ or C:\Windows\SysWow64\Tasks\.
  Both conditions must be present together. These directories are writable by unprivileged users, so an actor can
  plant a script or .NET assembly there that script hosts and .NET applications (cscript, wscript, regsvr32, mshta,
  eventvwr) subsequently load, achieving execution and persistence with hijacked execution flow. Legitimate writes to
  these paths are rare; the investigation should confirm what file was written, whether it was later loaded as a module,
  and whether the writing process and user are expected to touch these system directories.
type: detection
detection_id: cc4e02ba-9c06-48e2-b09e-2500cace9ae0
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line both use a file-writing verb and reference a System32\Tasks or SysWow64\Tasks path?
  context: Confirm the alert is a true positive before investing further. The rule fires only when the command line
    contains a file-writing verb (echo, copy, type, or file createnew) AND a reference to C:\Windows\System32\Tasks\
    or C:\Windows\SysWow64\Tasks\. Verify both elements are present in the exact command line and note which Tasks path
    and which verb were used.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to write files into the System32\Tasks or SysWow64\Tasks directories?
  context: Writing into the globally writable Tasks folders is unusual for ordinary users and most automation. Knowing
    whether the user role and host function legitimately involve placing files in these system directories is often the
    fastest way to separate a true positive from a rare but sanctioned operation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What did the full command do — which file was written into the Tasks folder and with what content?
  context: The full command line reveals the source and destination of the write. Identify the target file name under
    the Tasks path, the content being written (echoed text, copied source file, redirected output), and any chained
    commands. Because cmd.exe is high volume, this query is scoped to invocations on this host that reference the Tasks
    paths so related sequential write commands are captured without unrelated shell activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains:
          - ' C:\Windows\System32\Tasks\'
          - ' C:\Windows\SysWow64\Tasks\'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_artifact.mechanism_decode
  question: What is the content of the file written into the Tasks folder?
  context: Read the file planted under the Tasks path. For scripts and text check for base64 encoding, obfuscated strings,
    embedded URLs or IP addresses, and known malicious patterns; for a .NET assembly or binary check imports, embedded
    strings, packer signatures, and signing status. The file's content determines what executes when a script host or
    .NET application loads it. Content retrieval is a manual step from the endpoint or EDR.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_artifact.artifact_verification
  question: What is the file hash of the planted file and does it have known threat intel detections?
  context: A hash uniquely identifies the planted file for reputation lookup against threat intel platforms to determine
    whether it is known malicious, known benign, or previously unseen. A previously unseen file written into a system
    Tasks directory warrants close inspection.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:cue_process_tool.process_chain
  question: What process spawned this command interpreter and is that parent chain expected?
  context: The parent process reveals how the write into the Tasks folder was initiated. Interactive use from explorer.exe
    differs from a script host (wscript.exe, mshta.exe), an Office application, or a remote-execution service spawning
    the interpreter. An unexpected parent points toward scripted or exploited execution rather than an administrator at
    a console.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:universal.evidence_preservation
  question: Is the file referenced in the Tasks path still on disk for analysis?
  context: The command references a file under the Tasks folder. Checking whether it still exists on disk enables deeper
    analysis — hash lookups, sandbox submission, or content review. If the file has already been removed or loaded and
    deleted, capture whatever telemetry recorded its prior existence.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: attack:T1574.001.image_load
  question: Was a module loaded from a System32\Tasks or SysWow64\Tasks path on this host?
  context: The payoff of this technique is execution flow hijack — a script host or .NET application loading the planted
    assembly from the Tasks directory. A module load whose path is under \System32\Tasks\ or \SysWow64\Tasks\ confirms
    the planted file was actually executed as code rather than merely staged. Correlate the loading process and the load
    time with the original write to establish the file-creation-to-load interval.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ImageLoaded|contains:
          - '\System32\Tasks\'
          - '\SysWow64\Tasks\'
      condition: selection
    fields:
      - Image
      - ImageLoaded
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users drove it, and does the current
    alert fit?
  context: Even routine tools can be abused. This query mirrors the rule's selection structure scoped to this host, so the
    resulting parent and user distribution reflects how this specific write-into-Tasks pattern has historically run here.
    A new parent process, an unexpected user account, or an unusual command-line structure is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection1:
        CommandLine|contains:
        - 'echo '
        - 'copy '
        - 'type '
        - file createnew
      selection2:
        CommandLine|contains:
        - ' C:\Windows\System32\Tasks\'
        - ' C:\Windows\SysWow64\Tasks\'
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection1:
        CommandLine|contains:
        - 'echo '
        - 'copy '
        - 'type '
        - file createnew
      selection2:
        CommandLine|contains:
        - ' C:\Windows\System32\Tasks\'
        - ' C:\Windows\SysWow64\Tasks\'
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection1:
        CommandLine|contains:
        - 'echo '
        - 'copy '
        - 'type '
        - file createnew
      selection2:
        CommandLine|contains:
        - ' C:\Windows\System32\Tasks\'
        - ' C:\Windows\SysWow64\Tasks\'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user triggered similar activity on multiple hosts, the scope of the operation may be larger than the single
    alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection1:
        CommandLine|contains:
        - 'echo '
        - 'copy '
        - 'type '
        - file createnew
      selection2:
        CommandLine|contains:
        - ' C:\Windows\System32\Tasks\'
        - ' C:\Windows\SysWow64\Tasks\'
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to the write into the
    Tasks folder, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, or additional persistence mechanisms in the same window.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the
    stages that preceded this write into the Tasks folder and help reconstruct the timeline leading up to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the cross-host distribution look like
    automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, giving a fleet-wide count of the write-into-Tasks
    pattern rather than generic binary usage. Many hosts firing the same pattern points to scripted deployment or vendor
    automation; a single host or small unrelated set points to manual operator activity. Use the host distribution and
    parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection1:
        CommandLine|contains:
        - 'echo '
        - 'copy '
        - 'type '
        - file createnew
      selection2:
        CommandLine|contains:
        - ' C:\Windows\System32\Tasks\'
        - ' C:\Windows\SysWow64\Tasks\'
      condition: all of selection*
    fields:
    - host.name
- source: template:persistence_scope.file_based_persistence
  question: Were files dropped to other persistence locations on this host in the surrounding window?
  context: Writing into the Tasks folder is one file-based foothold; attackers commonly stack backups. File event monitoring
    captures drops to the Startup folder and registry Run/RunOnce key paths even when the attacker avoids standard persistence
    tools. Drops to multiple persistence locations in the same timeframe indicate intentional stacking rather than isolated
    administrative action.
  range: +/-4h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist_paths:
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\CurrentVersion\Run'
          - '\CurrentVersion\RunOnce'
          - '\System32\Tasks\'
          - '\SysWow64\Tasks\'
      condition: selection and selection_persist_paths
    fields:
      - TargetFilename
      - Image
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1574.001
      name: DLL
      role: primary
    - id: T1574
      name: Hijack Execution Flow
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: file_write_to_tasks
      description: "Actor writes a script or assembly into the globally writable System32\\Tasks or SysWow64\\Tasks folder via a file-writing command"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: payload_staged
      description: "The planted file persists on disk in the Tasks directory as the staged payload"
      mapped_to: Q4
      source: template:cue_artifact.mechanism_decode
    - stage: 3
      label: module_load_from_tasks
      description: "A script host or .NET application loads the planted file from the Tasks directory, hijacking execution flow"
      mapped_to: Q8
      source: attack:T1574.001.image_load
  dimension_coverage:
    - dimension: time_correlation
      technique: T1574.001
      covered_by: Q8
      source: attack:T1574.001.image_load
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1574.001
      covered_by: Q9
      source: template:cue_process_tool.baseline_deviation
      coverage_type: indirect
    - dimension: file_path_analysis
      technique: T1574
      covered_by: Q4
      source: template:cue_artifact.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: AllowedDllPaths
      dimension: tuning_parameter
      specific_values: "rationale only (e.g., C:\\Windows\\System32)"
      disposition: context
      target_question: Q8
      notes: "Tuning rationale, no queryable value. The Tasks-path module-load query inverts this — it surfaces loads from the non-standard Tasks paths that AllowedDllPaths would otherwise have to exclude."
    - element: ProcessAllowList
      dimension: tuning_parameter
      specific_values: "rationale only (development tools expected to load from non-standard locations)"
      disposition: context
      target_question: Q11
      notes: "Tuning rationale. The parent+binary baseline addresses whether the loading/writing process is an expected actor for this host."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation interval between file creation, registry change, and module load)"
      disposition: context
      target_question: Q8
      notes: "No fixed value supplied. Module-load question instructs correlating write time to load time to establish the creation-to-load interval."
    - element: HashBaseline
      dimension: tuning_parameter
      specific_values: "rationale only (baseline hashes for legitimate DLLs)"
      disposition: context
      target_question: Q5
      notes: "Tuning rationale. Hash/reputation question retrieves the planted file's hash for comparison against known-good and threat intel."
    - element: ServiceBaseline
      dimension: tuning_parameter
      specific_values: "rationale only (expected registry keys and service paths)"
      disposition: context
      target_question: Q9
      notes: "T1574 parent element. Rule detects file writes, not service/registry modification; baseline_deviation establishes the expected parent/user pattern instead."
  dedup_decisions:
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject pivot vs. launch-mechanism pivot)"
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (subject pivot vs. launch-mechanism pivot)"
      kept: both
    - dropped: cue_process_tool.baseline
      reason: "host+binary baseline of cmd.exe is too noisy to be informative; baseline_deviation (rule-pattern-scoped) is the more specific historical-frequency answer"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.follow_on
      reason: "child processes of cmd.exe are not the technique payoff here — the payoff is a later module load of the planted file, covered by the image_load question; follow_on would return generic shell children"
      kept: attack:T1574.001.image_load
    - dropped: cue_process_tool.clustering
      reason: "redundant with cue_host_context.clustering (same +/-15m host-scoped around-the-alert view); host_context version kept"
      kept: cue_host_context.clustering
    - dropped: cue_artifact.baseline
      reason: "historical file-event frequency for the planted file is low-value (a freshly written file in Tasks is new by construction); evidence_preservation and content/hash questions cover the file artifact"
      kept: template:universal.evidence_preservation
    - dropped: persistence_scope.process_based_persistence
      reason: "this technique is file-write-based, not tool-based (schtasks/sc/reg); file_based_persistence is the on-point parallel-mechanism check and was extended with the Tasks paths"
      kept: persistence_scope.file_based_persistence
  skipped_templates:
    - template: organizational_context.change_ticket
      reason: "trimmed to hold count at 18-cap while keeping the four non-deduplicable user_actor questions and the image_load payoff; organizational legitimacy is carried by host_user_context and baseline questions"
    - template: organizational_context.human_recall
      reason: "trimmed for count; legitimacy/recall angle carried by host_user_context"
    - template: organizational_context.timing_alignment
      reason: "trimmed for count; timing is addressed by clustering and baseline_deviation"
    - template: organizational_context.procedural_consistency
      reason: "trimmed for count; procedural-normalcy angle carried by host_user_context and the user/parent baselines"
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This rule fires on a file-write into Tasks, not on payload execution or credential access where tamper-as-precursor turns the disposition; cue_host_context.precursor already surfaces prior tamper alerts on the host. No execution/credential-access pivot makes the generic tamper sweep essential here."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'create_stream_hash' (recommended by techniques: T1574.001) is not covered and no template family exists — author a custom question with `category: create_stream_hash`"
      disposition: declined
      reason: >-
        The rule detects a synchronous file-write command (echo/copy/type into Tasks),
        which produces a file_event (EventCode 11) directly captured by the content/hash
        questions over %file_paths%. create_stream_hash (EventCode 15) records alternate-data-stream
        and zone-identifier writes, which this command-line write pattern does not generate;
        a stream-hash query would return nothing for this rule's firing pattern. The
        file-creation evidence is already covered by the artifact content and hash questions.
    - hint: "logsource 'registry_set' (recommended by techniques: T1574.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        registry_set covers the DLL-search-order/registry redirection variant of T1574.001.
        This rule's detection is exclusively a file-write into the Tasks folder — it never
        touches the registry, and no registry key participates in the Tasks-folder load path
        (script hosts and .NET apps load the planted file by path, not via a registry redirect).
        A registry_set query would be orthogonal to this rule's firing pattern. The file-plant-to-load
        chain is covered by the file content/hash questions and the image_load question.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - persistence_scope
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 12
    logsource_diversity:
      process_creation: 11
      file_event: 4
      image_load: 1
      sigma_alert: 1
