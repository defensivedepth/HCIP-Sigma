name: Suspicious Windows Trace ETW Session Tamper Via Logman.EXE
id: 4db1fc10-cdee-4dab-82ef-7cd6353353c9
description: |
  Detects logman.exe (or a renamed binary whose OriginalFileName is Logman.exe) invoked to stop or delete a Windows Event Tracing (ETW) session whose name matches a security-telemetry collector — Circular Kernel Context Logger, an EventLog- session, the Sysmon trace session, or SysmonDnsEtwSession. The rule fires only when all three conditions hold at once: the binary is logman, the command carries a stop or delete verb, and the targeted session name matches one of those telemetry collectors. Stopping or deleting these sessions blinds the host's low-level event capture, so legitimate firings come from administrative maintenance or agent installers, while malicious use precedes hands-on-keyboard activity meant to run without telemetry. Determine which session was targeted, whether collection is still down, who and what drove the action, and what happened on the host once visibility was reduced.
type: detection
detection_id: cd1f961e-0b96-436b-b7c6-38da4583ec00
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the logman command line match what this rule detects — a stop or delete verb against a security ETW session name?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule requires all three conditions together — the binary resolves to logman.exe (by image name or OriginalFileName), the command carries 'stop ' or 'delete ', and it names one of the security trace sessions (Circular Kernel Context Logger, an EventLog- session, SYSMON TRACE, or SysmonDnsEtwSession). Note which session name appears; it determines which telemetry went dark and which downstream questions matter most.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manage ETW trace sessions with logman?
  context: Legitimate logman stop/delete activity comes from administrators tuning performance collectors or from agent installers that reset trace sessions during setup. A workstation or an account with no operational reason to touch ETW sessions is the disposition-changing case. Compare the user role and host function against routine telemetry maintenance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Which ETW trace session was stopped or deleted, and what telemetry does that session feed?
  context: Different session names blind different collectors. Circular Kernel Context Logger and SYSMON TRACE feed Sysmon's process, network, and registry events; EventLog- sessions back the classic Windows event channels; SysmonDnsEtwSession captures DNS query telemetry. Decode the full command line to identify the exact session name and verb (stop versus delete — delete removes the session definition, not just the running instance). The targeted collector determines what evidence is now missing for the rest of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is the targeted ETW session running again, or is that telemetry still not being collected?
  context: Stopping or deleting a trace session may be transient (reset during a maintenance window) or persistent (left down to suppress evidence). Check whether the session has been restarted and is collecting again, or whether events from that collector are still absent. A session that is still down means the host has an active visibility gap that should be restored before further analysis depends on its telemetry.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1070.001.command_analysis
  question: After the trace session was tampered with, is there a gap in this host's telemetry where events stop arriving?
  context: Stopping the Sysmon or EventLog- ETW session produces an observable absence — process, network, registry, or event-channel records that normally stream continuously go quiet right after the logman command. A clean break in collected events at that timestamp confirms the tamper took effect, and the length of the gap bounds the window in which other activity could have run unrecorded. If the telemetry from the targeted collector is not retained in this environment, treat zero results as a visibility gap rather than evidence nothing happened.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned logman, and is that parent chain expected for ETW session management?
  context: The parent reveals how logman was invoked. Administrative maintenance and installer activity have predictable parents (an interactive shell launched by explorer.exe, services.exe, or an installer process). A parent such as a script host, an Office application, or an unsigned binary points to scripted or hands-on-keyboard abuse rather than routine telemetry tuning.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What ran on this host in the hour after the trace session was tampered with?
  context: Telemetry is disabled to enable something. Activity in the window right after the logman command — process execution, credential access tooling, archiving, or data movement — is the activity the tamper was meant to run without recording. Where the targeted session feeds the very telemetry you would use here, correlate with network connections and file events for the same window, since process records may be incomplete.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense-tampering actions performed on this host around the same time?
  context: Operators who disable one telemetry collector often disable several or clear logs outright to maximize coverage. Finding additional tampering — log clearing, shadow-copy deletion, service stops, Defender changes, or firewall edits — from the same user or parent process indicates a coordinated effort to reduce visibility rather than an isolated administrative action.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1070.001.file_path_analysis
  question: Were Windows event log files (.evtx) deleted or modified on this host in the surrounding window?
  context: Stopping an EventLog- ETW session and deleting the underlying .evtx files are complementary indicator-removal steps — the first halts collection, the second destroys what was already recorded. File-deletion events touching the event-log store under the System32 winevt directory in the same timeframe point to a deliberate effort to erase historical evidence rather than a transient session reset.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: file_delete
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_evtx:
        TargetFilename|contains:
          - '\winevt\Logs\'
          - '.evtx'
      condition: selection and selection_evtx
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this logman ETW-tamper pattern, what parents and users drove it, and does the current alert fit that history?
  context: Even on hosts where ETW session maintenance is routine, the firing pattern has a normal pattern. The query mirrors the rule's selection — logman stopping or deleting one of the named security sessions — scoped to this host, so the resulting parent and user distribution reflects how this specific tamper pattern has historically run here. A new parent process, an unexpected account, or a first-ever firing is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \logman.exe
      - OriginalFileName: Logman.exe
      selection_action:
        CommandLine|contains:
        - 'stop '
        - 'delete '
      selection_service:
        CommandLine|contains:
        - Circular Kernel Context Logger
        - EventLog-
        - SYSMON TRACE
        - SysmonDnsEtwSession
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \logman.exe
      - OriginalFileName: Logman.exe
      selection_action:
        CommandLine|contains:
        - 'stop '
        - 'delete '
      selection_service:
        CommandLine|contains:
        - Circular Kernel Context Logger
        - EventLog-
        - SYSMON TRACE
        - SysmonDnsEtwSession
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \logman.exe
      - OriginalFileName: Logman.exe
      selection_action:
        CommandLine|contains:
        - 'stop '
        - 'delete '
      selection_service:
        CommandLine|contains:
        - Circular Kernel Context Logger
        - EventLog-
        - SYSMON TRACE
        - SysmonDnsEtwSession
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around the time of this alert?
  context: Tracking the user's activity across all hosts in the alert window reveals lateral movement or coordinated activity on multiple endpoints. If the same account stopped or deleted trace sessions, or ran other tooling, on several hosts close together, the scope is broader than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \logman.exe
      - OriginalFileName: Logman.exe
      selection_action:
        CommandLine|contains:
        - 'stop '
        - 'delete '
      selection_service:
        CommandLine|contains:
        - Circular Kernel Context Logger
        - EventLog-
        - SYSMON TRACE
        - SysmonDnsEtwSession
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection*) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this logman ETW-tamper pattern in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of this specific tamper pattern rather than generic logman usage. Many hosts firing the same pattern points to scripted deployment or agent-installer behavior that resets trace sessions; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \logman.exe
      - OriginalFileName: Logman.exe
      selection_action:
        CommandLine|contains:
        - 'stop '
        - 'delete '
      selection_service:
        CommandLine|contains:
        - Circular Kernel Context Logger
        - EventLog-
        - SYSMON TRACE
        - SysmonDnsEtwSession
      condition: all of selection*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Telemetry tampering rarely happens in isolation. Earlier detections on the same host — reconnaissance, credential access, or other defense-evasion activity — may reveal the attack stages that preceded the trace-session tamper and help reconstruct the full timeline. If the targeted ETW session feeds the very telemetry those earlier detections rely on, alerts may thin out after this point.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:persistence_scope.process_based_persistence
  question: Were process-based persistence mechanisms installed on this host around the same time?
  context: Reducing telemetry and establishing persistence often go together — an operator who blinds a collector frequently follows with a foothold that survives reboot. Scheduled tasks, services, registry Run-key edits, BITS jobs, or WMI subscriptions created in the surrounding window indicate intentional stacking rather than isolated maintenance.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
    - id: T1070.001
      name: Clear Windows Event Logs
      role: secondary
    - id: T1070
      name: Indicator Removal
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: invoke_tampering_tool
      description: "Actor invokes logman to stop or delete a security ETW trace session"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: identify_disabled_control
      description: "Identify which telemetry collector / ETW session was disabled and how"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 3
      label: telemetry_absence
      description: "Targeted collector goes quiet — observable gap in expected telemetry"
      mapped_to: Q5
      source: attack:T1070.001.command_analysis
    - stage: 4
      label: control_status
      description: "Determine whether the disabled collector is still down"
      mapped_to: Q4
      source: template:defense_tampering.protection_status
    - stage: 5
      label: enabled_activity
      description: "Activity the tampering was meant to run under reduced visibility"
      mapped_to: Q7
      source: template:defense_tampering.activity_after_tampering
    - stage: 6
      label: indicator_removal_files
      description: "Adjacent .evtx deletion destroying already-recorded evidence (T1070.001)"
      mapped_to: Q9
      source: attack:T1070.001.file_path_analysis
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q3
      source: template:defense_tampering.what_was_disabled
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q6
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: command_analysis
      technique: T1070.001
      covered_by: Q5
      source: attack:T1070.001.command_analysis
      coverage_type: direct
    - dimension: file_path_analysis
      technique: T1070
      covered_by: Q9
      source: attack:T1070.001.file_path_analysis
      coverage_type: direct
  mutable_dispositions:
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "Circular Kernel Context Logger, EventLog-, SYSMON TRACE, SysmonDnsEtwSession (from rule detection); session feeds: Sysmon process/net/registry, Windows event channels, Sysmon DNS"
      disposition: query_injection
      target_question: Q10
      notes: "ETW session names live in the reflected baseline_deviation/scope detection blocks (Q10, Q15); also decoded in Q3 what_was_disabled context"
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only — list of expected admin tools/installers"
      disposition: context
      target_question: Q2
      notes: "No concrete values; the installer/admin-maintenance false-positive framing is carried in Q2 and Q12 context"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — correlation window between tamper and downstream activity"
      disposition: context
      target_question: Q7
      notes: "Realized as the +1h activity_after window (Q7) and +/-1h telemetry-gap window (Q5)"
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only — which collectors count as security-critical"
      disposition: context
      target_question: Q3
      notes: "Realized as the per-session telemetry-mapping prose in Q3"
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only — authorized admin tools that stop services"
      disposition: context
      target_question: Q6
      notes: "Parent-chain legitimacy assessment carries this in Q6"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — admin/elevated user filter"
      disposition: context
      target_question: Q11
      notes: "User baseline (Q11) plus service-principal substitute (Q12) cover the human-vs-service identity split"
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "Remove-EventLog, wevtutil cl (T1070.001 log-clearing variants)"
      disposition: query_injection
      target_question: Q8
      notes: "Log-clearing verbs are part of the other_tampering_activity sweep (Q8: 'cl ', 'clear-log'); the rule itself fires on the logman/ETW variant decoded in Q3"
    - element: TargetLogName
      dimension: tuning_parameter
      specific_values: "Security, System, Application, custom logs"
      disposition: context
      target_question: Q3
      notes: "EventLog- session naming maps to specific channels; covered in Q3 telemetry-mapping prose"
    - element: TargetFilePathPattern
      dimension: file_path_analysis
      specific_values: "log file paths / event-log store"
      disposition: query_injection
      target_question: Q9
      notes: "Injected as TargetFilename|contains ['\\winevt\\Logs\\', '.evtx'] in Q9 file_delete query"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "defense_tampering.what_was_disabled is more specific for a tampering rule — replaces mechanism_decode per brief hint"
      kept: defense_tampering.what_was_disabled
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pairing
      reason: "user_actor pair — non-deduplicable (subject identity vs launch mechanism); emit BOTH per service_principals.yaml"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pairing
      reason: "user_actor pair — non-deduplicable (subject identity vs launch mechanism); emit BOTH per service_principals.yaml"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled"
    - template: cue_process_tool.baseline
      reason: "host-baseline frequency of generic logman usage is subsumed by the rule-specific host_pattern baseline_deviation (Q10), which counts the actual tamper firing pattern rather than all logman runs"
    - template: cue_process_tool.follow_on
      reason: "logman is a single-shot config tool that spawns no children of investigative value; child-process pivot adds no signal. Post-tamper activity is covered by activity_after_tampering (Q7), which is the relevant follow-on dimension for a tampering rule"
    - template: cue_process_tool.clustering
      reason: "host_context.precursor (Q16) and activity_after_tampering (Q7) already cover what-else-ran-on-this-host around the alert; the generic +/-15m image-count clustering would duplicate that coverage"
    - template: cue_host_context.clustering
      reason: "duplicates the +/-1h host activity coverage in activity_after_tampering (Q7) and the precursor alert sweep (Q16); held under the 18-question cap"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering set is confirmed for this rule; what_was_disabled (Q3), protection_status (Q4), and other_tampering_activity (Q8) are more specific tamper coverage and supersede the generic host-tamper sweep per the template's own drop condition (a)"
    - template: persistence_scope.file_based_persistence
      reason: "rule is defense-evasion, not persistence; the process-based persistence sweep (Q17) covers the cascade adequately, and a Startup/Run-key file-event question adds no coverage for an ETW-tamper alert. Held under the 18-question cap"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1562.001) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        This rule's detection is fully constrained to logman stopping/deleting
        live ETW trace sessions, which act on in-memory sessions via the
        Event Trace Session interface, not on registry-stored autologger
        configuration. The registry_set analytic for T1562.001 targets Sysmon
        registry-key edits (a different disable mechanism); none of the rule's
        command-line patterns touch the registry. No compensating
        attack:T1562.001.registry_set question authored because the ETW-session
        tamper this rule fires on leaves no registry artifact.
    - hint: "logsource 'security' (recommended by techniques: T1070.001) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The security-channel analytic for T1070.001 keys on EventCode 1102
        (audit-log-cleared), but this rule fires on logman stopping/deleting an
        ETW session, which halts collection rather than emitting a 1102 clear
        record — and stopping the EventLog- session can suppress the very 1102
        event a security-category query would look for. The telemetry-absence
        question (Q5, attack:T1070.001.command_analysis) covers the analyst-side
        effect (events stop arriving after the tamper) without depending on a
        1102 record that the tamper itself may prevent.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - persistence_scope
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      file_delete: 1
      sigma_alert: 1
      process_creation_pivot: 1
