name: DLL Execution via Rasautou.exe
id: f1ec3e81-7fe8-44f4-82c3-ef3f2ab8a052
description: |
  Detects rasautou.exe (or a renamed copy whose OriginalFileName is rasdlui.exe) run with a command line that contains both the -d and -p switches. The rule fires only when the image/original-name condition AND the command-line condition both hold. rasautou.exe is the Microsoft-signed Windows Remote Access AutoDial Dialer; its undocumented -d flag loads an arbitrary DLL and -p names an export to call, letting an operator run shellcode under a trusted signed binary to inherit its trust and bypass application allowlisting. These switches were removed in Windows 10, so the pattern is meaningful only on pre-Windows-10 hosts and where command-line auditing is configured. When it fires, decode the DLL path and export name, determine what the loaded DLL did (in-memory code execution, child processes, network beaconing), and confirm whether the user, host, and parent process are expected to invoke this binary in this way.
type: detection
detection_id: cd3d1298-eb3b-476c-ac67-12847de55813
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects — rasautou.exe (or rasdlui.exe by OriginalFileName) run with both -d and -p?
  context: >-
    Before investing investigation time, confirm the alert is a true positive. The rule fires only when both conditions hold together: the image ends in rasautou.exe (or the OriginalFileName is rasdlui.exe, catching a renamed copy), AND the command line contains both the -d and -p switches. Review the exact command line and process metadata to confirm a DLL path follows -d and an export name follows -p, rather than the switches appearing incidentally.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run rasautou.exe with a DLL-load command line?
  context: >-
    rasautou.exe is the legitimate RAS AutoDial dialer, but its -d/-p DLL-loading capability is undocumented and has no routine administrative use. The switches were removed in Windows 10, so a fire generally implies a pre-Windows-10 host. Knowing whether the user role and host function involve dial-up/RAS configuration, legacy software testing, or known automation is often the fastest path to separating a true positive from a sanctioned activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What DLL was loaded and which export was called — decode the -d path and the -p export name from the command line.
  context: >-
    The -d switch names the DLL to load and -p names the export to invoke; together they reveal exactly what code rasautou.exe executed. Identify the full DLL path (where it lives and whether the directory is user-writable), the export name (a generic-sounding export such as a download/run handler is a common shellcode-runner convention), and any -a/-e arguments passed through. The host+binary scope with the -d/-p filters captures the operation even if it spanned more than one invocation, while excluding unrelated rasautou activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_img:
      - Image|endswith: \rasautou.exe
      - OriginalFileName: rasdlui.exe
      selection_cli:
        CommandLine|contains|all:
        - ' -d '
        - ' -p '
      condition: selection_host and selection_img and selection_cli
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned rasautou.exe, and is that parent chain expected?
  context: >-
    The parent process reveals how the binary was invoked. A legitimate RAS-related launch comes from a service or a user-initiated networking action. A scripting host (cmd.exe, powershell.exe, wscript.exe, mshta.exe), an Office application, or an unfamiliar binary spawning rasautou.exe with a DLL-load command line points to operator-driven proxy execution rather than a system function.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.dll_load
  question: What DLL did rasautou.exe load into its process, and is it signed and from an expected location?
  context: >-
    The core of this technique is rasautou.exe loading an arbitrary DLL named by -d so the payload runs under a trusted signed binary. Image-load events for this process surface the modules it pulled in. An unsigned or oddly named DLL, or one loaded from a user-writable path (a profile, temp, or public directory) rather than System32, is the artifact that confirms the proxy load. If image-load telemetry is not collected in this environment, recover the DLL path from the decoded command line instead and treat the absence of load events as a visibility gap.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \rasautou.exe
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.network_connection
  question: Did rasautou.exe make any network connections after loading the DLL?
  context: >-
    rasautou.exe performing genuine RAS dialing has a narrow, predictable network profile. A DLL loaded through -d that runs shellcode commonly opens an outbound connection to retrieve a second stage or to beacon to a controller. Outbound connections from this process to an external or unexpected address — particularly on non-standard ports or to a newly seen destination — indicate the loaded code is doing more than dialing. Correlate any destination with the timing of the DLL load.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \rasautou.exe
        Initiated: 'true'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.follow_on
  question: What child processes did rasautou.exe spawn afterward?
  context: >-
    The processes the loaded DLL launches reveal its real impact. Shellcode delivered through a DLL-proxy load frequently spawns cmd.exe, powershell.exe, an injection target such as svchost.exe or explorer.exe, or an unfamiliar binary dropped to a user-writable path. Legitimate RAS dialing does not spawn interpreter or post-exploitation children.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this rasautou.exe DLL-load pattern, what parents and users drove it, and does the current alert fit that history?
  context: >-
    The query mirrors the rule's full selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific DLL-load pattern has run here historically — not generic rasautou.exe usage. Because -d/-p were removed in Windows 10, any prior firing is itself notable; a new parent process, an unexpected user account, or a first-ever appearance of the pattern on this host is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
      definition: Since options '-d' and '-p' were removed in Windows 10 this rule is relevant only for Windows before 10. And as Windows 7 doesn't log command line in 4688 by default, to detect this attack you need Sysmon 1 configured or KB3004375 installed for command-line auditing (https://support.microsoft.com/en-au/help/3004375/microsoft-security-advisory-update-to-improve-windows-command-line-aud)
    detection:
      selection_img:
      - Image|endswith: \rasautou.exe
      - OriginalFileName: rasdlui.exe
      selection_cli:
        CommandLine|contains|all:
        - ' -d '
        - ' -p '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run rasautou.exe or perform RAS/dial-up configuration?
  context: >-
    Compare this user's current action against their historical behavior across all hosts. A user who has never invoked rasautou.exe is more suspicious than one who does so routinely. The user role and typical toolset should align — RAS configuration is rare for ordinary accounts and the DLL-load form has no legitimate user.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+rasautou.exe launch combination historically routine on this host or environment-wide, or is the parent unusual for this binary?
  context: >-
    When the alert fires under a service principal (SYSTEM, LOCAL SERVICE, NETWORK SERVICE, or a computer account), user identity does not attribute behavior. The meaningful pivot is the launch mechanism — which parent spawned rasautou.exe, and whether that pair is historically common. A new or rare parent for this binary on this host or environment-wide is the deviation; a pair already accounting for many prior events is OS or vendor automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar proxy-execution or other notable activity on other hosts in the same window, the scope of the incident may be larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+rasautou.exe combination fire on other hosts in the same window — a coordinated automation rollout, or isolated to this host?
  context: >-
    Under a service principal, asking what else the user did network-wide returns agent-telemetry noise. The meaningful question is whether the same launch mechanism — parent process plus spawned rasautou.exe — fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rasautou.exe DLL-load pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's full selection structure with no host scope, so the result is a fleet-wide count of this DLL-load pattern rather than generic rasautou.exe usage. Many hosts firing the same pattern points to scripted deployment or a sanctioned legacy tool; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
      definition: Since options '-d' and '-p' were removed in Windows 10 this rule is relevant only for Windows before 10. And as Windows 7 doesn't log command line in 4688 by default, to detect this attack you need Sysmon 1 configured or KB3004375 installed for command-line auditing (https://support.microsoft.com/en-au/help/3004375/microsoft-security-advisory-update-to-improve-windows-command-line-aud)
    detection:
      selection_img:
      - Image|endswith: \rasautou.exe
      - OriginalFileName: rasdlui.exe
      selection_cli:
        CommandLine|contains|all:
        - ' -d '
        - ' -p '
      condition: all of selection*
    fields:
    - host.name
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: >-
    DLL-proxy execution is itself an evasion move; operators frequently weaken endpoint protection before running a payload under a signed binary. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal — kernel-level driver tampering and in-memory patches will not appear here. Populated rows shift the disposition toward operator preparation; review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining the surrounding activity reveals what led to this event and what followed — reconnaissance, privilege escalation, persistence installation, or data staging. Novel parent-child pairs at low counts stand out against high-count agent-telemetry noise and are what to scan for.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the attack stages that preceded the proxy-execution event and help reconstruct the timeline. Defense tampering followed by execution, or reconnaissance followed by exploitation, are the sequences to look for.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_binary_launch
      description: Microsoft-signed rasautou.exe launched with -d (DLL path) and -p (export name) to proxy execution
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: Decode the -d DLL path and -p export to identify what code was loaded
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: dll_load
      description: rasautou.exe loads the arbitrary DLL into its process (proxy load of unsigned/relocated payload)
      mapped_to: Q5
      source: attack:T1218.dll_load
    - stage: 4
      label: payload_execution
      description: Loaded DLL runs shellcode — spawns child processes and/or beacons to a network endpoint
      mapped_to: Q7
      source: template:cue_process_tool.follow_on
    - stage: 5
      label: network_retrieval
      description: Loaded code retrieves a second stage or beacons to a controller
      mapped_to: Q6
      source: attack:T1218.network_connection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q8
      source: template:cue_process_tool.baseline_deviation
      coverage_type: partial
  mutable_dispositions:
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "' -d ', ' -p ' (DLL path + export-name switches)"
      disposition: query_injection
      target_question: Q3
      notes: "Rule's selection_cli CommandLine|contains|all (-d / -p) injected into the host-scoped mechanism_decode query so the decode is filtered to the rule's DLL-load pattern, not all rasautou activity."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (no parent in rule detection; abnormal parents such as cmd/powershell/wscript/mshta/Office)"
      disposition: context
      target_question: Q4
      notes: "Abnormal-parent rationale carried in process_chain context and in the baseline_svc / clustering_svc parent-pivot questions."
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (rasautou.exe is the signed proxy binary; allowlist-of-signed-binaries is a defender tuning knob, not a queryable value here)"
      disposition: context
      target_question: Q2
      notes: "rasautou.exe is the single signed binary this rule targets; the 'list of allowed signed binaries' is a defender tuning concept surfaced in host_user_context (expected-use) framing rather than a query value."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (enterprise-domain suppression list)"
      disposition: new_question
      target_question: Q6
      notes: "Drove the attack:T1218.network_connection question; the allowlist concept is the inverse of 'unexpected destination', which that question asks the analyst to evaluate."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q9, Q10)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q11, Q12)
    - dropped: cue_process_tool.baseline
      reason: "generic Image-only -90d host baseline is non-discriminating here; cue_process_tool.baseline_deviation (rule-pattern-scoped, including the -d/-p filter) is the meaningful host-frequency question and supersedes it"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "near-identical host-scoped aggregation to cue_host_context.clustering (both group nearby process_creation on the host); cue_host_context.clustering kept because it projects ParentImage as well as Image"
      kept: cue_host_context.clustering
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 14
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      image_load: 1
      network_connection: 1
      sigma_alert: 1
