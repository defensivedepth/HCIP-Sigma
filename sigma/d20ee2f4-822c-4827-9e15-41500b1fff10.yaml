name: Potential Amazon SSM Agent Hijacking
id: a2e8c38b-a9e5-479c-b23f-04dabfb5cd73
description: |
  Detects amazon-ssm-agent.exe running with -register together with -code, -id, and -region. The legitimate AWS Systems Manager agent is Amazon-signed and pre-installed on cloud instances, but re-registering it with an activation code, activation id, and region binds the agent to a different AWS account. An operator can use that re-registered agent as a remote access tool, issuing commands, transferring files, and opening sessions through legitimate AWS APIs and traffic that blends with normal cloud infrastructure. Determine whether the registration was an authorized provisioning action or an attempt to repoint the agent to an externally controlled account.
type: detection
detection_id: d20ee2f4-822c-4827-9e15-41500b1fff10
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-06'
questions:
- source: template:universal.verify_trigger_match
  question: Did amazon-ssm-agent.exe run with -register and the -code, -id, and -region flags together?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires only when all four registration flags appear on a single amazon-ssm-agent.exe invocation, which is the command that binds the agent to an AWS account. Read the activation code, activation id, and region values from the command line before continuing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this host and account expected to register or re-register the SSM agent?
  context: Agent registration is a provisioning action normally tied to instance setup or a documented re-enrollment, performed by infrastructure automation or an administrator. Knowing whether this host and account would legitimately run an SSM registration is often the fastest way to separate routine provisioning from a repoint to an unfamiliar account.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What activation id, region, and working directory did this registration use?
  context: The registration parameters reveal which AWS account and region the agent is being bound to. An activation id or region that does not match the organization's own AWS tenancy points to an externally controlled account. A non-default working directory or a second agent install indicates a run-as-fresh-instance variant rather than a normal re-enrollment of the existing agent.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned this SSM agent registration and is that parent chain expected?
  context: The parent process shows how the registration was launched. A re-enrollment driven by a provisioning tool or service has a predictable parent, while invocation from an interactive shell, a script host, or an unexpected installer suggests an operator manually repointing the agent.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did the SSM agent spawn afterward?
  context: Once an agent is bound to an account, that account can issue commands that the agent executes locally, surfacing as shells, script hosts, or file-transfer utilities spawned by the agent process. Command interpreters or file-manager children spawned by amazon-ssm-agent.exe are interactive control signals consistent with the agent being driven as a remote access tool.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:remote_access_tool.connection_destination
  question: What SSM endpoints and regions is this agent connecting to?
  context: The agent communicates with ssm, ec2messages, and ssmmessages endpoints under amazonaws.com, and the region in those hostnames reflects which account it now serves. Connections to an SSM region the organization does not use, or sustained outbound sessions on non-standard high ports, indicate the agent is beaconing to an account outside the environment's normal AWS footprint.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationHostname
      - DestinationPort
- source: attack:T1219.install_path
  question: Is amazon-ssm-agent.exe running from its canonical install path, and is more than one agent process present on this host?
  context: The supported agent lives under the Amazon SSM program files directory; an agent executing from a user-writable, temporary, or otherwise non-canonical path is a re-deployment rather than the pre-installed service. Two or more concurrent amazon-ssm-agent processes on one host is the run-as-fresh-instance pattern, where a second agent is registered to a different account while the original keeps running.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith: \amazon-ssm-agent.exe
      condition: selection
    fields:
      - Image
      - CurrentDirectory
- source: template:cue_process_tool.baseline
  question: Does this host normally run amazon-ssm-agent.exe registration?
  context: A registration that has never been seen on this host is more notable than one that recurs, since the agent is normally registered once at provisioning and then runs as a service. Zero or very low historical counts indicate a first-time or rare re-enrollment worth scrutiny.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously run the SSM registration pattern, what parents and users normally drove it, and does the current alert fit?
  context: The query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how the registration pattern has historically run here rather than generic agent activity. A new parent process, an unexpected account, or an unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \amazon-ssm-agent.exe
        CommandLine|contains|all:
        - '-register '
        - '-code '
        - '-id '
        - '-region '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts run the SSM registration pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, giving a fleet-wide count of the registration pattern rather than generic agent usage. Many hosts running the same pattern points to a provisioning or re-enrollment campaign; a single host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate scope.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \amazon-ssm-agent.exe
        CommandLine|contains|all:
        - '-register '
        - '-code '
        - '-id '
        - '-region '
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally register or manage the SSM agent?
  context: Compare this account's current action against its historical behavior. An account that has never registered the agent is more notable than one that does so as part of routine provisioning. The user's role should align with managing cloud agent enrollment.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and amazon-ssm-agent.exe combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute the behavior, so the meaningful pivot is the launch mechanism. A new or rare parent for the agent on this host, or environment-wide, is the deviation worth chasing; a pair that already accounts for many prior events is provisioning or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the account's activity across all hosts reveals whether the registration is isolated or part of broader hands-on activity. If the same account drove similar actions on multiple endpoints, the scope of the operation may be larger than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and amazon-ssm-agent.exe combination fire on other hosts in the same window, indicating a coordinated rollout rather than isolated activity?
  context: Under a service principal, surveying everything the account did across the network returns agent-telemetry noise. The meaningful question is whether the same parent and agent launch fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:remote_access_tool.tool_authorization
  question: Is the SSM agent sanctioned for remote management on this host, and is the registering AWS account one the organization controls?
  context: The SSM agent itself may be approved infrastructure, but its value as a remote access tool depends on which account it answers to. Verify both that the agent belongs on this host and that the activation code, id, and region map to the organization's own AWS tenancy. Registration against an account or region the organization does not own is the abuse condition regardless of the agent's reputation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a change ticket or provisioning record explaining this SSM agent registration?
  context: Legitimate agent enrollment correlates with instance provisioning, a documented re-enrollment, or a maintenance window. If no provisioning or support context exists for this registration, the agent may have been repointed by an operator to establish persistent remote access through legitimate cloud APIs.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host around the registration?
  context: A re-registered agent provides one persistent foothold through legitimate cloud APIs, and operators commonly stack additional mechanisms. Scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions installed in the same timeframe indicate intentional stacking rather than an isolated provisioning action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1219
      name: Remote Access Tools
      role: primary
    - id: T1219.002
      name: Remote Desktop Software
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: initial_execution
      description: "Initial execution of the remote-access agent under a user/host context (the SSM registration command)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: account_binding
      description: "Registration parameters bind the agent to an AWS account/region (activation code, id, region, working directory)"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: persistence
      description: "Persistence via re-registered agent and additional process-based mechanisms"
      mapped_to: Q17
      source: template:persistence_scope.process_based_persistence
    - stage: 4
      label: outbound_beacon
      description: "Long-lived outbound connection to SSM/AWS infrastructure under the new account"
      mapped_to: Q6
      source: template:remote_access_tool.connection_destination
    - stage: 5
      label: interactive_control
      description: "Interactive control signals: shell or file-manager children spawned by the agent parent"
      mapped_to: Q5
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: time_correlation
      technique: T1219
      covered_by: Q14
      source: template:cue_user_actor.clustering_svc
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1219
      covered_by: Q7
      source: attack:T1219.install_path
      coverage_type: direct
    - dimension: user_context
      technique: T1219
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1219
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: Image
      dimension: tuning_parameter
      specific_values: "amazon-ssm-agent.exe (canonical %PROGRAMFILES%\\Amazon\\SSM path); non-canonical/user-writable path variant"
      disposition: new_question
      target_question: Q7
      notes: "Install-path and multi-agent check (attack:T1219.install_path) targets non-canonical execution paths."
    - element: DestinationPort
      dimension: tuning_parameter
      specific_values: "configurable/random high ports outside standard"
      disposition: query_injection
      target_question: Q6
      notes: "connection_destination query projects DestinationPort; context notes non-standard high-port egress."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Parent-process expectation framed in process_chain and the service-principal baseline questions."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "default 15m correlation; adjust per environment"
      disposition: context
      target_question: Q14
      notes: "Clustering questions use +/-1h windows binding registration to cross-host activity."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (admin/jump host vs standard endpoint)"
      disposition: context
      target_question: Q11
      notes: "user_actor baseline pivots on account; host_user_context establishes role expectation."
    - element: ProcessAllowlist
      dimension: process_identification
      specific_values: "rationale only (known-good remote support tools)"
      disposition: context
      target_question: Q15
      notes: "tool_authorization establishes whether the agent and its registering account are sanctioned."
    - element: InstallPathRegex
      dimension: tuning_parameter
      specific_values: "user-writable / temp paths vs canonical Amazon SSM directory"
      disposition: new_question
      target_question: Q7
      notes: "Install-path check surfaces agents running outside the canonical directory."
    - element: ExternalIPAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only (vendor support clouds/CDNs)"
      disposition: context
      target_question: Q6
      notes: "connection_destination context calls out SSM region/endpoint mismatch against the org's AWS footprint."
    - element: ShellSpawnRegex
      dimension: tuning_parameter
      specific_values: "rationale only (acceptable vs suspicious child shells)"
      disposition: context
      target_question: Q5
      notes: "follow_on surfaces shell/file-manager children spawned by the agent."
    - element: EgressHeuristics
      dimension: tuning_parameter
      specific_values: "rationale only (session duration, connection counts, byte ratios)"
      disposition: context
      target_question: Q6
      notes: "connection_destination context frames sustained outbound sessions; thresholds not directly queryable."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable (subject identity vs launch-mechanism identity)"
      kept: both (Q11 user-pivot, Q12 service-principal substitute)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable (subject identity vs launch-mechanism identity)"
      kept: both (Q13 user-pivot, Q14 service-principal substitute)
  skipped_templates:
    - template: cue_process_tool.clustering
      reason: "Overlaps cue_user_actor.clustering_svc (Q14) and cross-host scope (Q10); host-wide tool clustering adds noise without a distinct angle for a single-binary registration rule."
    - template: cue_host_context.clustering
      reason: "Same-host activity context is already provided by follow_on (Q5, agent children) and persistence_scope (Q17); a generic Image+ParentImage host sweep duplicates that coverage."
    - template: cue_host_context.precursor
      reason: "Prior-alert timeline is informational here; the rule fires on agent re-registration (a binding action), not payload execution that a precursor chain would contextualize. Disposition turns on account ownership (Q3/Q15), not earlier alerts."
    - template: universal.endpoint_protection_health
      reason: "Rule detects agent re-registration (C2/persistence setup via legitimate APIs), not payload execution or credential access; tamper-as-precursor is not the live question. defense_tampering set not confirmed for this rule."
    - template: organizational_context.change_ticket
      reason: "Provisioning/approval check is consolidated into remote_access_tool.active_session_context (Q16), which asks for the change ticket or provisioning record for this registration."
    - template: organizational_context.human_recall
      reason: "Account-recall is covered by host_user_context (Q2) role expectation and active_session_context (Q16) provisioning record; a standalone recall question adds no distinct pivot for an automated agent action."
    - template: organizational_context.timing_alignment
      reason: "Timing-vs-maintenance-window judgment is folded into active_session_context (Q16, provisioning/maintenance context) and the clustering questions' windows; a separate timing question is redundant."
    - template: organizational_context.procedural_consistency
      reason: "How agents are normally enrolled is covered by tool_authorization (Q15, sanctioned tool + org-owned account) and host_user_context (Q2); a separate procedural-method question overlaps without adding a pivot."
    - template: persistence_scope.file_based_persistence
      reason: "Kept the playbook within the 14-18 cap; file-drop persistence is a parallel mechanism less central than process-based stacking (Q17) for an agent-reregistration foothold that itself needs no dropped file."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - organizational_context
    - remote_access_tool
    - persistence_scope
    - cue_process_tool
    - cue_user_actor
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 16
      network_connection: 1
