name: HackTool - WinPwn Execution
id: d923c906-1a00-4229-92ff-2266277069a0
description: |
  Fires when a process command line contains one of the WinPwn invocation strings
  (Offline_Winpwn, "WinPwn ", WinPwn.exe, WinPwn.ps1). WinPwn is a PowerShell
  post-exploitation framework that wraps reconnaissance, UAC bypass, credential
  harvesting, and lateral-movement tooling behind a single menu-driven entry point,
  and is commonly delivered through a download cradle or an offline bundle. The same
  strings also appear in authorized penetration tests and in atomic test harnesses,
  so the investigation should confirm the command, identify which capability was
  invoked, and determine whether the activity maps to a sanctioned engagement or to
  hands-on-keyboard operator activity.
type: detection
detection_id: d557dc06-62e8-4468-a8e8-7984124908ce
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-06'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain a WinPwn invocation string?
  context: Confirm the alert is a true positive by reviewing the full command line
    against the rule's detection criteria, which match the literal strings
    Offline_Winpwn, "WinPwn ", WinPwn.exe, or WinPwn.ps1. Note which string and which
    delivery pattern fired (a download cradle pulling WinPwn.ps1, the Offline_Winpwn
    bundle, or a direct WinPwn.exe/.ps1 invocation), since that frames the rest of
    the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.authorized_engagement
  question: Is this WinPwn execution part of an authorized penetration test or red team engagement?
  context: WinPwn is an offensive framework used in sanctioned security assessments
    and in atomic test harnesses. Check whether a pentest or red team exercise is
    active on this host or network segment. An authorized engagement with matching
    tool usage and timing resolves the alert without further analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user expected to run offensive or reconnaissance tooling?
  context: Penetration testers and IT staff may legitimately run security tooling,
    but standard users rarely launch a post-exploitation framework. The user's role
    and typical behavior baseline determine whether running WinPwn is routine or
    anomalous.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:credential_access.credential_target_identification
  question: What credential stores or sensitive resources did this WinPwn invocation target?
  context: WinPwn bundles modules that read LSASS memory, the SAM database, DPAPI
    secrets, and browser credential stores (via SafetyKatz, SessionGopher, SharpWeb,
    and similar). Decode the full command line to identify which module or subcommand
    was requested -- the target store determines which accounts may be compromised
    and what remediation is needed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: Which WinPwn capability or module was invoked, and were any console or non-interactive flags set?
  context: WinPwn is multi-purpose -- the menu exposes credential harvesting
    (Kittielocal), AD reconnaissance (Domainreconmodules), UAC bypass (UACBypass),
    kerberoasting, and others. The command-line module name and flags such as
    -noninteractive, -consoleoutput, or -repo reveal which capability ran and whether
    the run was scripted for an agent rather than driven interactively, which scopes
    potential compromise and response actions.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_specific_artifacts
  question: Did the WinPwn command write known artifacts to disk on this host?
  context: WinPwn modules drop characteristic files -- loot folders, LSASS dump
    files, .kirbi tickets, BloodHound JSON or zip collections, and obfuscated script
    bundles such as the Offline_Winpwn package. Checking for these confirms execution
    and reveals scope even if the original process has exited. The query scopes file
    creation to this host and to the WinPwn-related paths derived from the command line.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - 'WinPwn'
          - 'Offline_Winpwn'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:universal.evidence_preservation
  question: Is the WinPwn script or downloaded payload still on disk for analysis?
  context: The command may reference a script (WinPwn.ps1, Offline_Winpwn.ps1) or a
    payload pulled by a download cradle. Checking whether the file still exists
    enables deeper analysis -- hash lookups, sandbox submission, or content review of
    the menu and module strings it carries.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this WinPwn execution and is that parent chain expected?
  context: The parent process reveals how WinPwn was invoked. A PowerShell download
    cradle, an Office application, or a script interpreter (wscript.exe, mshta.exe)
    as the parent points to delivery through phishing or remote loading; explorer.exe
    or a developer shell points to interactive operator use. Walk the parent chain to
    establish how the framework reached this host.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: WinPwn drives bursts of enumeration. Multiple reconnaissance commands in
    rapid succession -- whoami, net, nltest, systeminfo, dsquery -- alongside the
    WinPwn invocation is a signature of post-compromise enumeration. Three or more
    distinct recon binaries within the window typically indicates automated discovery
    rather than isolated troubleshooting.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be compromised, and do any have elevated privileges?
  context: The severity of credential theft depends on the privilege level of the
    affected accounts. Domain admin or service-account exposure requires escalation;
    a standard user account may only need monitoring. Cross-reference the running user
    and any accounts named in the command line with directory group memberships.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:uac_bypass.elevated_action_after_bypass
  question: What action ran at high or system integrity shortly after this event?
  context: WinPwn ships a UAC bypass menu (ccmstp, UACMagic, diskcleanup, DccwBypass)
    used to reach high integrity before credential dumping, persistence, or security
    tampering. Processes spawned at High or System integrity on this host shortly
    after the WinPwn invocation reveal what the elevated context was used for.
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        IntegrityLevel:
          - 'High'
          - 'System'
          - 'S-1-16-16384'
          - 'S-1-16-12288'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:uac_bypass.bypass_technique_registry_artifacts
  question: Were registry modifications made to set up a UAC bypass before this event?
  context: Several WinPwn bypass methods write a command to a registry key that an
    auto-elevated process reads and executes -- common targets include
    HKCU\Software\Classes\ms-settings\Shell, mscfile\shell, and HKCU\Environment.
    Finding the registry write confirms the bypass mechanism and reveals the payload
    that ran with elevated privileges.
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\reg.exe'
          - '\powershell.exe'
          - '\cmd.exe'
        CommandLine|contains:
          - 'ms-settings'
          - 'mscfile'
          - 'Classes\exefile'
          - 'Classes\Folder'
          - 'Environment'
          - 'Shell\Open\command'
          - 'DelegateExecute'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - Offline_Winpwn
        - 'WinPwn '
        - WinPwn.exe
        - WinPwn.ps1
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - Offline_Winpwn
        - 'WinPwn '
        - WinPwn.exe
        - WinPwn.ps1
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals whether the WinPwn
    run is isolated or part of broader hands-on activity. The same user triggering
    activity on multiple endpoints around this time widens the potential scope.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - Offline_Winpwn
        - 'WinPwn '
        - WinPwn.exe
        - WinPwn.ps1
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: attack:T1106.process_identification
  question: What is the identity of the process running the WinPwn command line, and is it the host's genuine PowerShell binary?
  context: WinPwn modules load native API surfaces (ntdll.dll, kernel32.dll) to run
    in-memory .NET assemblies and direct syscalls, so the process hosting the command
    line matters. Confirm the executing image is the real powershell.exe in
    System32/SysWOW64 rather than a renamed or relocated copy used to blend the
    framework into expected process names. The query returns the image path, hashes,
    and signature for the triggering event.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1555
      name: Credentials from Password Stores
      role: primary
    - id: T1555.003
      name: Credentials from Web Browsers
      role: secondary
    - id: T1552.001
      name: Credentials In Files
      role: secondary
    - id: T1548.002
      name: Bypass User Account Control
      role: secondary
    - id: T1082
      name: System Information Discovery
      role: secondary
    - id: T1046
      name: Network Service Discovery
      role: secondary
    - id: T1518
      name: Software Discovery
      role: secondary
    - id: T1106
      name: Native API
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tool_delivery_and_invocation
      description: "WinPwn reaches the host via download cradle or Offline_Winpwn bundle and is invoked"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: capability_selection
      description: "Operator selects a WinPwn module (recon, UAC bypass, credential harvest)"
      mapped_to: Q5
      source: template:hacking_tool.tool_capability_used
    - stage: 3
      label: privilege_elevation
      description: "UAC bypass module raises integrity before high-integrity actions"
      mapped_to: Q12
      source: template:uac_bypass.elevated_action_after_bypass
    - stage: 4
      label: credential_access
      description: "Modules read LSASS, SAM, DPAPI, or browser credential stores"
      mapped_to: Q4
      source: template:credential_access.credential_target_identification
    - stage: 5
      label: lateral_movement
      description: "Harvested credentials drive follow-on remote sessions"
      mapped_to: Q11
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: time_correlation
      technique: T1082
      covered_by: Q9
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1555
      covered_by: Q5
      source: template:hacking_tool.tool_capability_used
      coverage_type: direct
    - dimension: user_context
      technique: T1082
      covered_by: Q3
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1548.002
      covered_by: Q8
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: process_identification
      technique: T1106
      covered_by: Q18
      source: attack:T1106.process_identification
      coverage_type: direct
    - dimension: network_indicators
      technique: T1518
      covered_by: Q11
      source: template:credential_access.post_access_lateral_movement
      coverage_type: indirect
    - dimension: command_analysis
      technique: T1552
      covered_by: Q4
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: file_path_analysis
      technique: T1555.003
      covered_by: Q6
      source: template:hacking_tool.tool_specific_artifacts
      coverage_type: direct
  mutable_dispositions:
    - element: KeywordPatterns
      dimension: tuning_parameter
      specific_values: "WinPwn module/flag strings (-noninteractive, -consoleoutput, -repo)"
      disposition: context
      target_question: Q5
      notes: "Capability/flag keywords called out in tool_capability_used context; not added to a host-scoped selection because Q5 is ProcessGuid-scoped to the triggering event."
    - element: FileNamePattern
      dimension: tuning_parameter
      specific_values: "WinPwn, Offline_Winpwn"
      disposition: query_injection
      target_question: Q6
      notes: "Injected into tool_specific_artifacts TargetFilename|contains to scope file_event to WinPwn artifacts."
    - element: MonitoredPaths
      dimension: file_path_analysis
      specific_values: "browser/credential store paths (rationale)"
      disposition: context
      target_question: Q4
      notes: "Credential store targets described in credential_target_identification context; query stays ProcessGuid-scoped to read the triggering command."
    - element: ElevatedProcessNameList
      dimension: tuning_parameter
      specific_values: "High/System IntegrityLevel + auto-elevated bypass binaries"
      disposition: query_injection
      target_question: Q12
      notes: "IntegrityLevel selection carried verbatim from uac_bypass template."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window)"
      disposition: context
      target_question: Q9
      notes: "Burst-detection window expressed via the +/-15m range on discovery_burst_detection."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "User-role scoping handled by discovery_user_context."
    - element: DllName
      dimension: tuning_parameter
      specific_values: "ntdll.dll, kernel32.dll (rationale)"
      disposition: context
      target_question: Q18
      notes: "Native-API DLL rationale described in the process_identification context; no image_load query authored (see hint_dispositions)."
  dedup_decisions:
    - dropped: discovery_recon.discovery_parent_chain
      reason: "parent-chain intent covered by cue_process_tool.process_chain (Q8) — same ParentImage pivot; kept the cue version with the -7d ProcessGuid lookup for long-running parents"
      kept: cue_process_tool.process_chain
    - dropped: universal.host_user_context
      reason: "superseded by discovery_recon.discovery_user_context (Q3), which is more specific to this rule's discovery/tooling context per the template's own note"
      kept: discovery_recon.discovery_user_context
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both (Q14, Q15)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both (Q16, Q17)
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (Q4)"
    - template: universal.endpoint_protection_health
      reason: "dropped under the 18-question cap; prior tamper-as-precursor signal is partially carried by post_access_lateral_movement's alert correlation, and the rule fires on a string-match tool invocation rather than a direct defense-tampering action, so the generic host-tamper sweep is lower value than the credential/UAC/recon technique-set coverage retained"
    - template: cue_process_tool.baseline
      reason: "Image-based 90d baseline is meaningless for a CommandLine-only rule (primary_binary is null); the rule's-pattern baseline is instead carried by the reflected user_actor baseline (Q14) and is not duplicated"
    - template: cue_process_tool.baseline_deviation
      reason: "reflected host-pattern baseline dropped under the cap in favor of the user_actor reflected baseline (Q14, same WinPwn CommandLine selection) and the cross-host pattern handled by user_actor clustering_svc (Q17)"
    - template: cue_process_tool.scope
      reason: "reflected fleet-pattern count dropped under the cap; cross-host distribution of the WinPwn pattern is covered by cue_user_actor.clustering_svc (Q17), which counts the same pattern across hosts in the window"
    - template: cue_process_tool.follow_on
      reason: "child-process follow-on dropped under the cap; post-invocation activity is covered by elevated_action_after_bypass (Q12) and discovery_burst_detection (Q9)"
    - template: cue_process_tool.clustering
      reason: "host-wide tool clustering dropped under the cap; concurrent recon activity is covered more precisely by discovery_burst_detection (Q9)"
    - template: cue_host_context.clustering
      reason: "generic host activity clustering dropped under the cap; covered by discovery_burst_detection (Q9) and elevated_action_after_bypass (Q12)"
    - template: cue_host_context.precursor
      reason: "prior-alert precursor dropped under the cap; on-host alert correlation is carried by post_access_lateral_movement (Q11)"
    - template: uac_bypass.other_escalation_attempts
      reason: "additional-escalation alert sweep dropped under the cap; UAC coverage is provided by elevated_action_after_bypass (Q12) and bypass_technique_registry_artifacts (Q13), and on-host alert correlation by post_access_lateral_movement (Q11)"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1106,T1548.002) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule fires on a literal WinPwn string in the process command line, not
        on any module-load behavior. image_load on the host's powershell.exe returns
        volume without precision for which run was the WinPwn invocation. The native
        in-memory .NET / direct-syscall angle this logsource targets is instead
        addressed at process_creation granularity by Q18
        (attack:T1106.process_identification), which confirms the executing image
        identity for the specific triggering event. No compensating image_load
        question authored.
    - hint: "logsource 'network_connection' (recommended by techniques: T1046) is not covered — pull in cue_network_endpoint.* or author a custom question with `category: network_connection`"
      disposition: declined
      reason: >-
        WinPwn's network footprint (download-cradle pull, port/service scanning) is a
        downstream module behavior, not what this command-line-string rule detects.
        A host-scoped network_connection query at the alert would return ambient
        traffic without tying it to the WinPwn run. Follow-on remote activity is
        instead surfaced via alert correlation in Q11
        (credential_access.post_access_lateral_movement). No compensating
        network_connection question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1106,T1548.002,T1555,T1555.003) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (cross-process memory reads against LSASS/browser stores) is a
        capability a WinPwn module may invoke downstream, not the behavior the rule
        matches. The credential-target angle is covered by Q4
        (credential_access.credential_target_identification) which decodes which store
        the run targeted, and high-integrity post-bypass actions by Q12. A standalone
        process_access query is not authored because it cannot be scoped to this run
        without the module having already been identified in Q4.
    - hint: "logsource 'security' (recommended by techniques: T1548.002,T1552.001) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The Windows Security-log signals these techniques call for (4672 elevated
        logon, 4624/4648 credential reuse) are post-credential-access effects, not the
        command-line invocation this rule detects. Their investigative intent is
        covered platform-neutrally by Q10 (credential_exposure_assessment, account
        privilege review) and Q11 (post_access_lateral_movement, follow-on logon/remote
        activity via alert correlation). No raw security-logsource question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - discovery_recon
    - hacking_tool
    - uac_bypass
    - cue_process_tool
    - cue_user_actor
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 2
      context: 15
    logsource_diversity:
      process_creation: 15
      file_event: 2
      sigma_alert: 1
