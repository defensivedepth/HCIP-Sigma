name: Add SafeBoot Keys Via Reg Utility
id: 23adf3c3-d226-484f-840b-5dd55bb55f25
description: |
  Detects reg.exe executed with an "add" or "copy" operation against the SafeBoot control registry path
  (\SYSTEM\CurrentControlSet\Control\SafeBoot). The rule fires when the reg.exe image (or OriginalFileName reg.exe)
  AND the SafeBoot key substring AND a copy/add flag are all present. Legitimately this branch is rarely modified;
  malicious use stages a service or remote-access tool to survive a Safe Mode reboot, where many security products
  do not load, so an encryptor or other payload can run unimpeded. Determine which key was written, what service or
  value it points at, whether the host was subsequently rebooted into Safe Mode, and whether other defense-tampering
  or persistence actions accompanied it.
type: detection
detection_id: d7662ff6-9e97-4596-a61d-9839e32dee8d
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the reg.exe command line match the SafeBoot add/copy pattern this rule detects?
  context: >-
    Confirm the alert is a true positive before investing time. The rule fires only when all three conditions hold
    together: the image is reg.exe (or OriginalFileName reg.exe), the command line contains the SafeBoot control path
    \SYSTEM\CurrentControlSet\Control\SafeBoot, and it carries a copy or add operation. Read the exact command line and
    note whether it writes into the SafeBoot or SafeBoot\Network branch and what key it sources from.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to modify SafeBoot registry keys?
  context: >-
    Writing into the SafeBoot control branch is uncommon outside of specialized recovery or imaging workflows. Knowing
    whether the acting user role and host function (administrative workstation, server, recovery imaging host) routinely
    touch boot configuration is often the fastest way to separate a routine operation from operator preparation. SafeBoot
    edits driven by an interactive user on a normal endpoint warrant closer review.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:registry.registry_key_context
  question: What specific SafeBoot key and value were written, and what service or component does it point at?
  context: >-
    The registry_set event gives the actual key path and value written as structured fields rather than parsing them
    from reg.exe's command line. A copy into \Control\SafeBoot\Network\<service> stages that service to load during a
    Safe Mode with Networking boot; an add of a Minimal or Network subkey enables a component in Safe Mode. Identify the
    exact subkey and the service or driver name it registers, since that names what the operator wants running when most
    protection is offline.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:defense_tampering.what_was_disabled
  question: Which security or boot-mode control did this SafeBoot modification target?
  context: >-
    SafeBoot edits weaken the host's protection posture by design. Copying a service into the SafeBoot\Network branch
    lets that service start under Safe Mode with Networking, an environment where many AV/EDR products do not load,
    leaving the host effectively unprotected after a reboot. Read the command line to identify which service or value
    the operation enables and whether it is a remote-access tool, encryptor staging, or a legitimate recovery component.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this reg.exe execution, and is that parent chain expected?
  context: >-
    The parent process reveals how reg.exe was invoked. SafeBoot edits driven by interactive administration usually
    descend from explorer.exe or a console shell a human is using. A parent such as a scripting host, an Office
    application, a remote-execution service, or an unfamiliar binary points to automated or operator-driven tampering
    rather than maintenance. Note what spawned the parent and what else that parent launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Has the host been rebooted into Safe Mode since this modification, and is protection currently active?
  context: >-
    A SafeBoot edit only takes effect on the next Safe Mode boot. Check whether the host has since rebooted into Safe
    Mode (or has a pending Safe Mode boot configured via bcdedit) and whether AV/EDR is reporting healthy now. If the
    staged service is still present in the SafeBoot branch, the host remains primed to run that component with protection
    offline and the entry should be removed before further investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the SafeBoot key was modified?
  context: >-
    Operators stage Safe Mode persistence to enable an action that protection would otherwise block, such as deploying
    an encryptor or a remote-access tool. Examining process activity in the window after the modification reveals what
    the SafeBoot change was meant to enable, including a forced reboot, service installation, or payload execution.
    Correlate this with file and network activity over the same window.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense-tampering or service-control actions performed on this host in the same window?
  context: >-
    Operators who stage Safe Mode persistence commonly also disable AV/EDR services, clear logs, delete shadow copies,
    or change firewall rules in the same operation to maximize coverage. Finding additional tampering events from the
    same user or parent process indicates multiple controls were targeted together rather than an isolated registry edit.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:registry.registry_related_modifications
  question: Were other registry modifications made on this host in the same session?
  context: >-
    Operators who modify one registry key often modify several, such as staging a SafeBoot service AND enabling a Run
    key AND disabling a Defender policy. Finding clusters of reg.exe or regedit write operations from the same user
    within a short window reveals the full scope of the configuration changes and whether multiple objectives were
    pursued alongside the SafeBoot edit.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run reg.exe?
  context: >-
    Compare this user's current action against their historical pattern. A user who has never run reg.exe against boot
    or service configuration is more suspicious than one who does so regularly as part of a documented role. User role
    and typical toolset should align with editing the SafeBoot branch.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and reg.exe combination historically routine on this host or across the environment, or is the parent unusual?
  context: >-
    When the alert fires under a service principal, user identity does not attribute the behavior; the meaningful pivot
    is the launch mechanism, namely which parent process spawned reg.exe and whether that pair is historically common. A
    new or rare parent for reg.exe on this host or environment-wide is the deviation worth chasing, while a pair that
    already accounts for thousands of prior events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or actions on multiple endpoints. If the same
    user triggered similar boot-configuration or tampering activity on other hosts, the scope of the operation may be
    larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and reg.exe combination fire on other hosts in the same window?
  context: >-
    Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism, the parent process plus reg.exe, fired on other hosts in
    the alert window. Multiple hosts in the same window points to scheduled or pushed automation, while a single host
    points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host, such as credential access, reconnaissance,
    or other defense tampering, may reveal the attack stages that preceded the SafeBoot edit and help reconstruct the
    full timeline leading up to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:lolbin.renamed_binary_check
  question: Was this reg.exe executed from its expected System32 or SysWOW64 path?
  context: >-
    reg.exe running from outside System32 or SysWOW64 may indicate the binary was copied or renamed to evade path-based
    allowlists, or that a renamed binary reported reg.exe as its OriginalFileName while living in a user-writable
    directory. A reg.exe in a temp or profile directory is more suspicious than one in System32.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the SafeBoot add/copy pattern over the past 90 days, and what command-line shapes are typical?
  context: >-
    A SafeBoot reg.exe pattern with no prior history on a host is more suspicious than one that recurs under stable
    parents. The query mirrors the rule's selection structure scoped to this host, so the count reflects the rule's
    specific firing pattern rather than generic reg.exe usage. The command-line projection lets the analyst compare
    typical invocation shapes against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_safeboot:
        CommandLine|contains: \SYSTEM\CurrentControlSet\Control\SafeBoot
      selection_flag:
        CommandLine|contains:
        - ' copy '
        - ' add '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired the SafeBoot add/copy pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of the
    SafeBoot add/copy firing pattern rather than generic reg.exe usage. Many hosts firing the same pattern points to
    scripted deployment or imaging automation; one host or a small unrelated set points to manual operator activity. Use
    the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \reg.exe
      - OriginalFileName: reg.exe
      selection_safeboot:
        CommandLine|contains: \SYSTEM\CurrentControlSet\Control\SafeBoot
      selection_flag:
        CommandLine|contains:
        - ' copy '
        - ' add '
      condition: all of selection*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: safeboot_key_write
      description: "reg.exe adds or copies a service/value into the SafeBoot control registry branch"
      mapped_to: Q3
      source: template:registry.registry_key_context
    - stage: 2
      label: protection_weakening
      description: "Staged Safe Mode boot weakens the host's protection posture (AV/EDR does not load in Safe Mode)"
      mapped_to: Q4
      source: template:defense_tampering.what_was_disabled
    - stage: 3
      label: safe_mode_boot_and_payload
      description: "Host reboots into Safe Mode and the staged component runs / payload executes with protection offline"
      mapped_to: Q7
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q3
      source: template:registry.registry_key_context
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: "Expected-tools exclusion is a tuning rationale, not a queryable value; baseline_svc parent+binary pivot lets the analyst identify routine automation pairs in lieu of a static exclusion list"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Correlation-window rationale; expressed as the +1h forward range on activity_after_tampering and +/-1h on other_tampering_activity"
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Per-deployment security service list is a tuning rationale; registry_key_context directs the analyst to identify the specific service the SafeBoot copy stages"
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q5
      notes: "Authorized-admin-tool whitelist is a tuning rationale; process_chain has the analyst judge whether the spawning parent is expected"
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "Security-critical service pattern list is a tuning rationale; addressed via registry_key_context service identification"
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "Generic Image|endswith host baseline superseded by lolbin.historical_lolbin_baseline (reflected, -90d), which counts the rule's specific SafeBoot pattern rather than all reg.exe usage"
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.baseline_deviation
      reason: "Reflected host-scoped parent/user distribution overlaps cue_user_actor.baseline_svc (parent+binary baseline) and historical_lolbin_baseline; dropped to stay under cap while keeping both the user_actor pair and the CommandLine-shape baseline"
      kept: cue_user_actor.baseline_svc
    - dropped: cue_process_tool.follow_on
      reason: "Forward-looking host activity duplicates defense_tampering.activity_after_tampering, which is the more specific post-tampering question for this rule"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_process_tool.clustering
      reason: "Around-the-time host activity duplicates defense_tampering.other_tampering_activity and cue_user_actor.clustering coverage"
      kept: defense_tampering.other_tampering_activity
    - dropped: cue_host_context.clustering
      reason: "Host-wide around-time activity already covered by defense_tampering.activity_after_tampering and defense_tampering.other_tampering_activity"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable; both baseline (user) and baseline_svc (parent+binary) emitted as Q10 and Q11"
      kept: cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable; both clustering (user) and clustering_svc (parent+binary) emitted as Q12 and Q13"
      kept: cue_user_actor.clustering_svc
    - dropped: lolbin.technique_class_lateral
      reason: "Same-objective LOLBin sweep targets download/proxy-execution binaries; the SafeBoot copy is a persistence/evasion action and the persist/tamper tool sweep is covered more specifically by defense_tampering.other_tampering_activity"
      kept: defense_tampering.other_tampering_activity
    - dropped: persistence_scope.process_based_persistence
      reason: "Process-based persistence tool sweep overlaps defense_tampering.other_tampering_activity (schtasks/sc/reg/etc.); recommended-priority, dropped to stay under cap"
      kept: defense_tampering.other_tampering_activity
    - dropped: persistence_scope.file_based_persistence
      reason: "reg.exe SafeBoot copy is a registry-to-registry operation that drops no Startup-folder or Run-path file; the file_event persistence-drop query does not fit this rule's mechanism"
      kept: none
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context — the registry_set event provides the SafeBoot key path and value as structured fields"
    - template: cue_artifact.mechanism_decode
      reason: "cue_artifact is derived from \\bcopy\\s but reg.exe copy is a registry-key-to-registry-key operation, not a file copy; there is no file artifact to analyze for content"
    - template: cue_artifact.artifact_verification
      reason: "No file artifact produced by a registry copy; hash/reputation lookup does not apply"
    - template: cue_artifact.baseline
      reason: "No file artifact produced by a registry copy; file-appearance baseline does not apply"
    - template: universal.evidence_preservation
      reason: "applicability has_file_reference matched on the registry path, not an on-disk payload; the SafeBoot copy references registry keys, so there is no dropped file to preserve"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled / other_tampering_activity questions are more specific than the generic host-tamper sweep (notes_to_llm drop condition (a))"
  reflection_overrides: []
  hint_dispositions:
    - hint: "cue_artifact: derived: \\bcopy\\s in CommandLine"
      disposition: declined
      reason: >-
        The \bcopy\s match fired on reg.exe's copy verb, but reg.exe copy
        duplicates a registry key to another registry key — it produces no file
        artifact. All three cue_artifact templates (mechanism_decode,
        artifact_verification, baseline) assume a file to read, hash, or
        baseline, none of which exists here. registry.registry_key_context (Q3)
        decodes the actual SafeBoot key/value instead.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: medium
        reason: "Reference mentions ransomware, but the rule detects a SafeBoot registry edit (defense evasion), not impact actions (encryption, shadow-copy deletion); the keyword match is incidental to this detection"
      - id: network_scope
        confidence: medium
        reason: "Reference mentions ransomware, but the rule fires on a local registry operation with no network indicator; network-scope questions would not pivot on any field this rule surfaces"
  technique_sets_applied:
    - universal
    - defense_tampering
    - registry
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 17
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 3
      context: 12
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      sigma_alert: 1
