name: Arbitrary DLL or Csproj Code Execution Via Dotnet.EXE
id: ab33b9af-db8c-463f-b4c1-ff0ae10e7aec
description: |
  Detects dotnet.exe (or a binary whose OriginalFileName is ".NET Host") invoked with a command line ending in a ".csproj" or ".dll" path, excluding the known-benign CSScriptNpp Notepad++ integration. dotnet.exe is a Microsoft-signed management CLI that will load and run arbitrary class-library DLLs and compile inline C# from a RoslynCodeTaskFactory .csproj, providing an application-allowlisting bypass that does not integrate with in-process malicious-code inspection. Legitimate use is a developer or build agent running or compiling a project; malicious use stages unsigned code under a trusted binary. Determine whether the invoked DLL or project is expected on this host, who launched it, and what it did after loading.
type: detection
detection_id: d80d5c81-04ba-45b4-84e4-92eba40e0ad3
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show dotnet.exe loading a .dll or compiling/running a .csproj?
  context: Confirm the alert is a true positive before investing time. The rule fires when the process is dotnet.exe (or carries the ".NET Host" OriginalFileName) AND the command line ends in a .csproj or .dll path. Read the full command line and note whether it points at a DLL (direct class-library load) or a .csproj (inline-task compile or project run), and where that file lives.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to build, run, or execute .NET projects and assemblies?
  context: Legitimate dotnet.exe project execution is normal on developer workstations and build agents. On a server or end-user endpoint with no development role, a .NET host loading a DLL or compiling a .csproj is harder to explain and shifts the disposition toward investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What DLL or .csproj did dotnet.exe load, and what does the full command line reveal about how it was invoked?
  context: The full command line reveals the specific operation. Identify the target file path and the invocation verb -- exec or a bare path runs a class-library DLL directly, run builds and then runs a project, and msbuild on a .csproj compiles inline C# via RoslynCodeTaskFactory. Note where the DLL or project file resides; a path under a user-writable or temporary directory is a stronger signal than an installed application directory.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned dotnet.exe, and is that parent chain expected?
  context: The parent process reveals how the .NET host was invoked. A build IDE, terminal, or CI agent is a routine parent; mshta.exe, wscript.exe, an Office application, or a freshly written downloader spawning dotnet.exe points to scripted or exploitation-driven execution rather than developer activity.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.image_load
  question: Did dotnet.exe load the MSBuild assemblies that indicate an inline-compile or project-build operation?
  context: Module loads of MSBuild.dll, Microsoft.Build.dll, or Microsoft.Build.Framework.dll inside dotnet.exe indicate an MSBuild operation regardless of the command-line arguments, which is the pathway used to compile and run inline C# from a .csproj. These loads inside a .NET host outside a normal build context corroborate an allowlisting-bypass attempt. If image-load events are not collected in this environment, treat zero results as a visibility gap rather than evidence nothing loaded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
        ImageLoaded|endswith:
          - \MSBuild.dll
          - \Microsoft.Build.dll
          - \Microsoft.Build.Framework.dll
      condition: selection
    fields:
      - ImageLoaded
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did dotnet.exe spawn afterward?
  context: The processes a .NET host spawns reveal the loaded code's actual impact. A project build legitimately re-invokes dotnet.exe for build and compile stages, but dotnet.exe spawning cmd.exe, powershell.exe, certutil.exe, conhost.exe in a non-build context, or an unknown executable is a post-exploitation signal worth following.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1218.network_connection
  question: Did dotnet.exe make outbound network connections around the time of the alert?
  context: System binary proxy execution often pairs with retrieving or staging a payload over the network. Outbound connections from dotnet.exe to external addresses -- particularly directly after the DLL or .csproj load -- suggest the loaded code is fetching or exfiltrating rather than running a self-contained local project. Connections to internal package or build infrastructure are the routine explanation.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: template:cue_process_tool.baseline
  question: Does this host normally run dotnet.exe with a .dll or .csproj target?
  context: A .NET host that has never run on an endpoint, or runs only rarely, is more suspicious than one that runs constantly on a developer or build machine. Zero or very low historical counts indicate first-time or rare execution and raise the priority of the other findings.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this dotnet.exe pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even a routine build host fires this rule. The query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific .csproj/.dll execution pattern has historically run here. A new parent process, an unexpected user account, or an unusual command-line shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \dotnet.exe
      - OriginalFileName: .NET Host
      selection_cli:
        CommandLine|endswith:
        - .csproj
        - .csproj"
        - .dll
        - .dll"
        - .csproj'
        - .dll'
      filter_optional_notepadplus_plus:
        ParentImage:
        - C:\Program Files (x86)\Notepad++\notepad++.exe
        - C:\Program Files\Notepad++\notepad++.exe
        CommandLine|contains|all:
        - C:\ProgramData\CSScriptNpp\
        - '-cscs_path:'
        - \cs-script\cscs.dll
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally build, run, or execute .NET projects and assemblies?
  context: Compare this user's current action against their historical behavior. A user with no development role who has never run dotnet.exe against a DLL or project is more suspicious than a developer who does so daily. User role and typical toolset should align with .NET host execution.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and dotnet.exe combination historically routine on this host or across the environment, or is the parent unusual for the .NET host?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the meaningful pivot is the launch mechanism. A parent process that already accounts for thousands of prior dotnet.exe events is build or vendor automation, while a new or rare parent for the .NET host on this host or environment-wide is the deviation worth chasing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar .NET host execution on several hosts, the scope of the activity may be broader than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and dotnet.exe combination fire on other hosts in the same window -- a coordinated rollout, or isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The meaningful question is whether the same launch mechanism -- parent process plus the .NET host -- fired on other hosts in the alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining process activity around the alert reveals what led to this event and what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, persistence, or data staging surrounding the .NET host execution.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired this dotnet.exe pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of this .csproj/.dll execution pattern rather than generic dotnet.exe usage. Many hosts firing the same pattern points to scripted deployment or build automation; one host or a small unrelated set points to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \dotnet.exe
      - OriginalFileName: .NET Host
      selection_cli:
        CommandLine|endswith:
        - .csproj
        - .csproj"
        - .dll
        - .dll"
        - .csproj'
        - .dll'
      filter_optional_notepadplus_plus:
        ParentImage:
        - C:\Program Files (x86)\Notepad++\notepad++.exe
        - C:\Program Files\Notepad++\notepad++.exe
        CommandLine|contains|all:
        - C:\ProgramData\CSScriptNpp\
        - '-cscs_path:'
        - \cs-script\cscs.dll
      condition: all of selection_* and not 1 of filter_optional_*
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host -- defense tampering, reconnaissance, credential access -- may reveal the attack stages that preceded this event and help reconstruct the timeline. An allowlisting bypass via a trusted binary is often a mid-chain step rather than the start.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.endpoint_protection_health
  question: Were any tamper attempts against endpoint protection seen on this host in the hour before the alert?
  context: dotnet.exe does not integrate with in-process malicious-code inspection, so it is an attractive way to run unsigned code; operators often weaken protection before such execution. The query surfaces direct attempts to disable AV/EDR services, flip Defender flags, add broad exclusions, clear logs, or patch AMSI/ETW. Zero rows is a useful "protection looks healthy" signal; populated rows shift the disposition toward operator preparation, so review the parent process and signing chain to identify the writer.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_defender_disable:
        CommandLine|contains:
          - 'Set-MpPreference -Disable'
          - 'Set-MpPreference -MAPSReporting Disabled'
          - 'Set-MpPreference -SubmitSamplesConsent NeverSend'
          - 'Set-MpPreference -PUAProtection Disabled'
          - 'Set-MpPreference -ScanScheduleDay Never'
          - 'MpCmdRun.exe -RemoveDefinitions'
          - 'MpCmdRun.exe -DisableIOAVProtection'
      selection_exclusions:
        CommandLine|contains:
          - 'Add-MpPreference -ExclusionPath'
          - 'Add-MpPreference -ExclusionProcess'
          - 'Add-MpPreference -ExclusionExtension'
          - 'Add-MpPreference -ExclusionIpAddress'
      selection_service_tamper:
        - CommandLine|contains|all:
            - 'config'
            - 'start='
            - 'disabled'
        - CommandLine|contains:
            - 'Stop-Service '
            - 'Remove-Service '
            - 'sc stop '
            - 'sc delete '
            - 'net stop '
            - 'taskkill /F /IM '
            - 'taskkill /IM '
      selection_service_names:
        CommandLine|contains:
          - 'WinDefend'
          - 'WdNisSvc'
          - 'Sense'
          - 'SecurityHealthService'
          - 'mssecflt'
          - 'mpssvc'
          - 'MsMpEng'
          - 'MsSense'
          - 'CSFalconService'
          - 'CSAgent'
          - 'SentinelAgent'
          - 'SentinelHelperService'
          - 'SentinelStaticEngine'
          - 'LogProcessorService'
          - 'elastic-endpoint'
          - 'ElasticEndpoint'
          - 'Elastic Agent'
          - 'AVP'
          - 'klnagent'
          - 'KAVFS'
          - 'CbDefense'
          - 'CarbonBlack'
          - 'McAfeeFramework'
          - 'McShield'
          - 'McTaskManager'
          - 'mfemms'
          - 'mfevtp'
          - 'mfefire'
          - 'masvc'
          - 'macmnsvc'
          - 'TmListen'
          - 'ntrtscan'
          - 'TmCCSF'
          - 'SepMasterService'
          - 'SmcService'
          - 'ccSvcHst'
          - 'SAVService'
          - 'SAVAdminService'
          - 'Sophos Endpoint Defense'
          - 'hmpalertsvc'
          - 'CylanceSvc'
          - 'CyOptics'
      selection_log_tamper:
        - CommandLine|contains:
            - 'wevtutil cl '
            - 'wevtutil clear-log'
            - 'Clear-EventLog'
            - 'Limit-EventLog'
            - 'auditpol /clear'
            - 'auditpol /remove'
        - CommandLine|contains|all:
            - 'wevtutil sl'
            - '/e:false'
        - CommandLine|contains|all:
            - 'auditpol /set'
            - ':disable'
      selection_amsi_etw:
        CommandLine|contains:
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'AmsiScanBuffer'
          - 'amsiContext'
          - 'EtwEventWrite'
          - 'NtTraceEvent'
          - 'EventWriteEndpoint'
      condition: selection_host and (selection_defender_disable or selection_exclusions or (selection_service_tamper and selection_service_names) or selection_log_tamper or selection_amsi_etw)
    fields:
      - Image
      - CommandLine
      - User
      - ParentImage
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218
      name: System Binary Proxy Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: signed_binary_invocation
      description: "dotnet.exe (a Microsoft-signed binary) is invoked with a .dll or .csproj target to proxy execution of unsigned code"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_load_and_decode
      description: "The .NET host loads the class-library DLL or compiles/runs the inline-task .csproj; MSBuild assemblies load for the compile path"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: module_load_corroboration
      description: "MSBuild.dll / Microsoft.Build*.dll load inside dotnet.exe, indicating an MSBuild compile operation regardless of argv"
      mapped_to: Q5
      source: attack:T1218.image_load
    - stage: 4
      label: post_load_execution
      description: "Loaded code runs: child processes spawned and/or outbound network retrieval/exfil"
      mapped_to: Q6
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1218
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218
      covered_by: Q8
      source: template:cue_process_tool.baseline
      coverage_type: indirect
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: "Unexpected parent-child profiling addressed by process_chain (Q4) and the parent+binary baseline/clustering substitutes (Q11/Q13)"
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only (dotnet.exe is the in-scope signed binary; rule already constrains Image)"
      disposition: context
      target_question: Q8
      notes: "Rule pins the signed binary to dotnet.exe / .NET Host; baseline (Q8) and fleet scope (Q15) calibrate how routine that signed-binary execution is"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: ".dll / .csproj target paths and invocation verbs (exec, run, msbuild)"
      disposition: context
      target_question: Q3
      notes: "Rule's CommandLine|endswith selection already encodes the suspicious-argument match; mechanism_decode (Q3) reads the verb and target path. No CommandLineRegex query needed beyond the rule's own selection."
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Network-retrieval angle covered by the dotnet.exe network_connection question (Q7); allowlisting of legitimate enterprise/build domains is an environment-specific tuning note, not a queryable value"
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "host-scoped +/-15m process aggregation duplicates cue_host_context.clustering, which projects Image+ParentImage (superset of fields)"
      kept: cue_host_context.clustering
    - dropped: none
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: none
      reason: "user_actor pair — non-deduplicable"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 17
    source_counts:
      template: 15
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      image_load: 1
      network_connection: 1
      sigma_alert: 1
