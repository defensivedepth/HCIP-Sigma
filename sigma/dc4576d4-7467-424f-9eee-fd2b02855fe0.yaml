name: Suspicious Cabinet File Execution Via Msdt.EXE
id: b084ed84-12f0-4b6b-b93a-b3ea2fa02b73
description: |
  Detects msdt.exe (Microsoft Support Diagnostic Tool) invoked with the "-cab" flag, which loads a .diagcab cabinet
  file. The rule fires when the process Image ends in msdt.exe OR its OriginalFileName is msdt.exe, AND the command
  line contains "-cab". A .diagcab archive wraps diagnostic configuration XML that can reference local or network paths,
  and crafted packages can write attacker-controlled files outside the temp directory or proxy execution of secondary
  programs through this Microsoft-signed binary. Legitimate use exists when support staff or vendor tooling run packaged
  troubleshooters, so the investigation should determine the cabinet source, the parent that launched msdt.exe, and
  whether any child process, file write, or outbound connection followed.
type: detection
detection_id: dc4576d4-7467-424f-9eee-fd2b02855fe0
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show msdt.exe running with the -cab flag?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line
    against the rule's criteria - the process must be msdt.exe (by Image name or OriginalFileName) AND the command line
    must contain the "-cab" flag, which is what loads a .diagcab cabinet file. Note the cabinet path or filename in the
    command line, as the source of that file drives the rest of the investigation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run packaged Windows troubleshooters from a cabinet file?
  context: Legitimate administrative or support activity can trigger this detection - a help-desk session or vendor
    diagnostic package may run msdt.exe against a .diagcab. Knowing whether the user role and host function are consistent
    with running diagnostic cabinets is often the fastest path to distinguishing routine support from abuse.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What cabinet file did msdt.exe load, and where does that file path point?
  context: The full command line reveals the specific .diagcab the tool was told to process. Decode the path after the
    "-cab" flag - a local user-writable directory, a temp folder, or a UNC/WebDAV path each carry different risk. A
    network path or a recently dropped local cabinet is the indicator worth chasing, since the diagnostic XML inside can
    reference further local or remote paths.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned msdt.exe and is that parent chain expected?
  context: The parent process reveals how msdt.exe was invoked. A diagnostic cabinet opened by a user typically descends
    from explorer.exe or a browser/mail client; a parent such as winword.exe, an Office application, wscript.exe, or a
    scripting host points to a document or script triggering the load rather than a deliberate support action.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did msdt.exe spawn after loading the cabinet?
  context: msdt.exe processing a malicious cabinet can proxy execution of a secondary program. Spawned children such as
    powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, or curl.exe indicate the cabinet
    carried a payload rather than a benign troubleshooter. A clean diagnostic run usually spawns only Windows diagnostic
    helpers.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: attack:T1202.network_egress
  question: Did msdt.exe or its children open outbound network connections around the time of the alert?
  context: A crafted .diagcab can reference a UNC/WebDAV source, causing the diagnostic engine or a spawned child to reach
    out to a remote host to retrieve files or stage further execution. Outbound connections from msdt.exe or a child it
    launched - especially to an external address or a non-standard port - point to remote retrieval rather than local
    diagnostics. Correlate the connecting process against the process tree from the alert.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\msdt.exe'
          - '\powershell.exe'
          - '\cmd.exe'
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msiexec.exe'
          - '\curl.exe'
      condition: selection
    fields:
      - Image
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1202.diagcab_artifact
  question: What files were written on this host around the alert, and did any land in startup or autorun locations?
  context: The diagcab handling flaw copies files from the cabinet source into a temp directory before signature
    verification, and a path-traversal filename can redirect that write into the user's Startup folder so the file
    executes at next logon. File writes under the Startup folder, Roaming AppData, or a SDIAG temp directory in the same
    window are the artifact that distinguishes a benign troubleshooter from a planted payload.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - '\Start Menu\Programs\Startup\'
          - '\AppData\Roaming\'
          - '\AppData\Local\Temp\SDIAG_'
      condition: selection
    fields:
      - Image
      - TargetFilename
      - User
- source: template:cue_process_tool.baseline
  question: Does this host normally run msdt.exe?
  context: A binary that has never been seen on a host is more suspicious than one that runs regularly. msdt.exe is rarely
    invoked outside of an active support session, so zero or very low historical counts indicate first-time or rare
    execution worth scrutinizing.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired the rule's pattern, what parents and users normally drive it, and does the
    current alert fit?
  context: Even Microsoft-signed tools can be abused. The query mirrors the rule's selection structure scoped to this host,
    so the resulting parent/user distribution reflects how this specific msdt.exe -cab firing pattern has historically run
    here. A new parent process, an unexpected user account, or an unusual command-line shape is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \msdt.exe
      - OriginalFileName: msdt.exe
      selection_cmd:
        CommandLine|contains|windash: ' -cab '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the alert reveals what led to this event and what
    followed - look for reconnaissance, payload retrieval, privilege escalation, or persistence mechanisms that surround
    the cabinet execution and provide broader context.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - phishing-payload execution, reconnaissance, or defense tampering - may
    reveal the attack stages that preceded the cabinet execution and help reconstruct the full timeline. A .diagcab is
    commonly delivered by attachment or download, so prior alerts around document or download activity are relevant.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_user_actor.baseline
  question: Does this user normally run msdt.exe or diagnostic cabinets?
  context: Compare this user's current action against their historical behavior. A user who has never run msdt.exe is more
    suspicious than one who does so as part of a support role. User role and typical toolset should align with running
    packaged troubleshooters.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and msdt.exe combination historically routine on this host or across the environment, or is the
    parent unusual for this binary?
  context: When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot
    is the launch mechanism - which parent process spawned msdt.exe, and whether that pair is historically common. A new
    or rare parent for msdt.exe is the deviation worth chasing; a pair already accounting for many prior events is vendor
    or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints.
    If the same user triggered similar activity on multiple hosts, the scope of the event may be larger than a single
    cabinet execution.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and msdt.exe combination fire on other hosts in the same window - a coordinated rollout, or
    isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise. The
    meaningful question is whether the same launch mechanism - parent process plus msdt.exe - fired on other hosts in the
    alert window. Multiple hosts in the same window points to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:lolbin.technique_class_lateral
  question: Were any other proxy-execution LOLBins used on this host recently?
  context: Attackers who abuse one signed proxy-execution binary often use several. If msdt.exe loaded a malicious cabinet,
    check whether rundll32, regsvr32, mshta, msiexec, or similar binaries were also used to run arbitrary code. Finding
    multiple proxy-execution LOLBins performing the same objective strengthens the case for malicious activity.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\msbuild.exe'
          - '\cmstp.exe'
          - '\msiexec.exe'
          - '\forfiles.exe'
          - '\pcalua.exe'
          - '\presentationhost.exe'
          - '\control.exe'
          - '\infdefaultinstall.exe'
          - '\mavinject.exe'
          - '\installutil.exe'
          - '\xwizard.exe'
          - '\ie4uinit.exe'
          - '\wmic.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was msdt.exe executed from its expected System32 path?
  context: A LOLBin executed from a non-standard path (outside System32 or SysWOW64) may have been copied or renamed to
    evade path-based allowlists. An msdt.exe running from a user temp directory or a download folder is more suspicious
    than the genuine binary in System32; compare the Image path against the OriginalFileName.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired the msdt.exe -cab pattern over the past 90 days, and what command-line shapes are
    typical?
  context: A LOLBin pattern with no prior history on a host is more suspicious than one used under stable parents. The
    query mirrors the rule's selection structure scoped to this host, so the count reflects this specific msdt.exe -cab
    firing pattern rather than generic msdt.exe usage. The command-line projection lets the analyst compare typical
    cabinet invocations against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: \msdt.exe
      - OriginalFileName: msdt.exe
      selection_cmd:
        CommandLine|contains|windash: ' -cab '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1202
      name: Indirect Command Execution
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: indirection_utility_launch
      description: "msdt.exe launched with the -cab flag, loading a .diagcab cabinet file (the indirection utility)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: secondary_program_or_network
      description: "msdt.exe spawns a secondary program and/or opens outbound network connections to retrieve/stage payload"
      mapped_to: Q5
      source: template:cue_process_tool.follow_on
    - stage: 2b
      label: network_egress
      description: "Outbound network connection from msdt.exe or its child (e.g., WebDAV/UNC retrieval)"
      mapped_to: Q6
      source: attack:T1202.network_egress
    - stage: 3
      label: file_artifact_drop
      description: "File written via cabinet copy (traversal into Startup/Roaming or SDIAG temp) as persistence/payload"
      mapped_to: Q7
      source: attack:T1202.diagcab_artifact
  dimension_coverage:
    - dimension: time_correlation
      technique: T1202
      covered_by: Q6
      source: attack:T1202.network_egress
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1202
      covered_by: Q5
      source: template:cue_process_tool.follow_on
      coverage_type: direct
    - dimension: user_context
      technique: T1202
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "10-30 minutes between launcher and child/network activity"
      disposition: query_injection
      target_question: Q6
      notes: "network_egress and diagcab_artifact queries use +/-30m windows to correlate msdt.exe with spawned children and file writes"
    - element: HighRiskChildren
      dimension: tuning_parameter
      specific_values: "powershell.exe, cmd.exe, rundll32.exe, regsvr32.exe, mshta.exe, msiexec.exe, curl.exe, bitsadmin.exe"
      disposition: query_injection
      target_question: Q6
      notes: "High-risk child Image list embedded in the network_egress selection; also named in follow_on (Q5) context"
    - element: AllowedUtilities
      dimension: tuning_parameter
      specific_values: "forfiles, wsl, ssh"
      disposition: context
      target_question: Q9
      notes: "Tuning rationale for suppressing admin/jumphost noise; reflected in baseline_deviation parent/user framing. msdt.exe is not in the allowed-utility list, so no direct query change"
    - element: UserContext
      dimension: user_context
      specific_values: "standard/interactive user on workstation vs server/CI agent"
      disposition: context
      target_question: Q2
      notes: "host_user_context asks whether the user/host is expected to run diagnostic cabinets; severity rationale carried in context"
    - element: DestCIDRs
      dimension: tuning_parameter
      specific_values: "known-good egress networks for SSH/WSL"
      disposition: context
      target_question: Q6
      notes: "Not applicable to msdt.exe cabinet retrieval; network_egress surfaces DestinationIp/Port for the analyst to triage against known-good egress"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair - non-deduplicable; emitted both (Q12 user-pivot + Q13 parent+binary substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair - non-deduplicable; emitted both (Q14 user-pivot + Q15 parent+binary substitute)"
      kept: both
    - dropped: cue_process_tool.scope
      reason: "tier-3 fleet REFLECTED scope subsumed by lolbin.technique_class_lateral (fleet proxy-execution view) and historical_lolbin_baseline (host-pattern count); dropped to stay at 18 questions without losing the rule-specific cross-host signal"
      kept: template:lolbin.historical_lolbin_baseline
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. Rule fires on proxy-execution of a cabinet, not a hands-on-keyboard payload where the disposition turns on prior AV/EDR tamper. cue_host_context.precursor (Q11) already surfaces prior tamper/recon alerts on the host. Dropped to stay within the 18-question cap."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      network_connection: 1
      file_event: 1
      sigma_alert: 1
