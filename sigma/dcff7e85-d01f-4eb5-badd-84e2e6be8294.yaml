name: Windows Default Domain GPO Modification via GPME
id: 54ef48c1-1cbb-4c7a-bd33-221a7673c892
description: |
  Detects the Group Policy Management Editor launched from mmc.exe (by Image name or original
  filename MMC.exe) with a command line that contains both gpme.msc and a gpobject reference,
  and that references one of the two well-known default GPO GUIDs — the Default Domain Policy
  (31B2F340-016D-11D2-945F-00C04FB984F9) or the Default Domain Controllers Policy
  (6AC1786C-016F-11D2-945F-00C04FB984F9). The rule fires only when all three conditions match
  together. Editing these default GPOs is discouraged by hardening guidance, so the action is
  uncommon for routine administration; an operator can use it to push configuration changes that
  apply domain-wide — scheduled tasks, registry settings, or security policy — without creating a
  new, more visible GPO. Legitimate administrative edits do occur, so the investigation should
  decode the exact GPO and command targeted, confirm the corresponding directory-object and SYSVOL
  changes, establish whether the user, host, and parent process are expected to administer Group
  Policy, and look for surrounding tooling that points to a domain-wide push.
type: detection
detection_id: dcff7e85-d01f-4eb5-badd-84e2e6be8294
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line launch gpme.msc against a default domain GPO via mmc.exe?
  context: Confirm the alert is a true positive by reviewing the exact command line and process
    details. The rule fires only when the mmc.exe binary (by Image or original filename MMC.exe),
    the gpme.msc and gpobject tokens, and one of the two default GPO GUIDs all appear together.
    Verify the full command, which default GPO GUID the gpobject LDAP path references, and that
    the binary is genuinely mmc.exe before investing further time.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to edit Group Policy Objects?
  context: Legitimate administrative activity can trigger this detection — a domain admin may
    deliberately edit a default GPO despite hardening guidance that discourages it. Knowing whether
    the user role and host function include Group Policy administration, and whether this host is a
    domain controller or a management workstation, is often the fastest way to separate routine
    operations from an operator pushing changes domain-wide.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: Which default GPO and LDAP object does the gpobject argument reference, and what is the full command?
  context: The full command line reveals exactly which policy was opened for editing. Decode the
    gpobject LDAP path to identify the domain and the GPO GUID — 31B2F340-016D-11D2-945F-00C04FB984F9
    is the Default Domain Policy and 6AC1786C-016F-11D2-945F-00C04FB984F9 is the Default Domain
    Controllers Policy — and note any /s or /gpobject switches. The query scopes to all mmc.exe
    executions on this host within a few minutes so that related console invocations are visible
    together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: attack:T1484.001.directory_object_change
  question: Did a Group Policy directory object under the policies container change on this host or domain controllers around the time of the alert?
  context: Editing a GPO through the management editor writes changes to the GPO's directory object
    under CN=Policies,CN=System in the domain naming context. Confirming a directory-service object
    modification ties the editor session to an actual policy change and surfaces which attributes
    were altered — high-risk ones include gPCFileSysPath and ntSecurityDescriptor, the latter
    governing delegation on the GPO itself. A modification with no corresponding approved change
    record is the deviation worth chasing. Correlate the object-change timestamp with the editor
    process execution to confirm they belong to the same operation. If directory-service object
    auditing is not collected in this environment, treat the absence of results as a visibility gap
    rather than evidence nothing changed.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      category: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        winlog.event_id: '5136'
      filter:
        winlog.event_data.ObjectDN|contains: 'CN=Policies,CN=System'
      condition: selection and filter
    fields:
      - winlog.event_data.ObjectDN
      - winlog.event_data.AttributeLDAPDisplayName
      - winlog.event_data.SubjectUserName
- source: attack:T1484.001.sysvol_file_write
  question: Were policy files under the SYSVOL Policies share written or modified on a domain controller around the time of the alert?
  context: A GPO edit that adds a scheduled task, registry setting, or security policy is persisted
    as files under the SYSVOL Policies tree on domain controllers — for example ScheduledTasks.xml
    or GptTmpl.inf. Confirming a SYSVOL file write that matches the edited GPO GUID ties the editor
    session to a concrete on-disk change and shows what the policy now deploys to domain members.
    A new or modified ScheduledTasks.xml is especially worth inspecting because it can run code on
    every machine the GPO applies to. If file-write telemetry from domain controllers is not
    collected in this environment, treat the absence of results as a visibility gap.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        TargetFilename|contains: '\SYSVOL\'
      filter:
        TargetFilename|contains:
          - '\Policies\'
      condition: selection and filter
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned mmc.exe, and is that parent chain expected?
  context: The parent process reveals how the management editor was invoked. A GPO edit driven by an
    interactive admin session under explorer.exe on a management workstation or domain controller
    differs sharply from mmc.exe spawned by cmd.exe, powershell.exe under an unusual script host,
    mshta.exe, wscript.exe, or an Office application — parents that point to scripted delivery or a
    macro rather than deliberate policy administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run mmc.exe?
  context: A binary that has never been seen on a host is more suspicious than one that runs
    regularly. mmc.exe is common on administrative hosts and domain controllers but rare on ordinary
    workstations; zero or very low historical counts on the alerting host raise the weight of a
    default-GPO edit, while a host that routinely runs management consoles lowers it.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously launched gpme.msc against a default domain GPO, what parents and users normally drive it, and does this alert fit?
  context: Even legitimate Group Policy administration leaves a pattern. The query mirrors the rule's
    selection structure scoped to this host, so the resulting parent and user distribution reflects
    how the rule's specific firing pattern has historically run here — not generic mmc.exe usage. A
    new parent process, an unexpected user account, or a first-time appearance of this exact pattern
    is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_mmc:
      - Image|endswith: \mmc.exe
      - OriginalFileName: MMC.exe
      selection_gpme:
        CommandLine|contains|all:
        - gpme.msc
        - 'gpobject:'
      selection_default_gpos:
        CommandLine|contains:
        - 31B2F340-016D-11D2-945F-00C04FB984F9
        - 6AC1786C-016F-11D2-945F-00C04FB984F9
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did mmc.exe spawn afterward?
  context: The processes spawned by the management editor reveal its actual impact. A GPO edit is
    normally self-contained within the console; mmc.exe spawning cmd.exe, powershell.exe, or network
    tooling afterward points to follow-on activity — for example launching a remote-execution tool to
    apply the new policy immediately, or staging further payloads to the SYSVOL share. Unexpected
    children, including processes that open network connections to domain controllers or member
    hosts, broaden the scope of the investigation.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally administer Group Policy?
  context: Compare this user's current action against their historical behavior. A user who has never
    launched the Group Policy editor is more suspicious than one for whom it appears regularly as part
    of a known domain-administration role. The query counts how often this user has run mmc.exe across
    all hosts over 90 days; the result should align with their job function and expected use of default
    GPOs.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-process and mmc.exe combination historically routine on this host or across the environment, or is the parent unusual for it?
  context: When the alert fires under a service principal, user identity does not attribute behavior;
    the meaningful pivot is the launch mechanism. The query counts how often this same parent process
    has spawned mmc.exe over 90 days, grouped by host. A new or rare parent for mmc.exe on this host
    or environment-wide is the deviation worth chasing; a parent that already accounts for thousands of
    prior events is vendor or operating-system automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated
    activity on multiple endpoints. If the same user edited policy, ran remote-execution tooling, or
    touched domain controllers elsewhere in the same window, the scope of the incident may be larger
    than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and mmc.exe combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did returns agent-telemetry noise
    because every host runs system-owned agents constantly. The meaningful question is whether the same
    launch mechanism — the parent process spawning mmc.exe — fired on other hosts in the alert window.
    Multiple hosts point to scheduled or pushed automation; a single host points to a host-local
    trigger such as an interactive administrator session.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering,
    reconnaissance, credential access, or privilege escalation — may reveal the attack stages that
    preceded the policy edit and help reconstruct the full timeline. Domain-wide policy changes are
    often a late-stage action that follows the operator gaining the privileges needed to reach a
    domain controller or management host.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:cue_process_tool.scope
  question: Have other hosts launched gpme.msc against a default domain GPO in the past week, and does the distribution look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a
    fleet-wide count of the rule's firing pattern rather than generic mmc.exe usage. Editing a default
    GPO from more than one host in a short window is unusual and may point to a coordinated push or to
    the same operator working from multiple footholds; a single host points to a localized
    administrative action. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_mmc:
      - Image|endswith: \mmc.exe
      - OriginalFileName: MMC.exe
      selection_gpme:
        CommandLine|contains|all:
        - gpme.msc
        - 'gpobject:'
      selection_default_gpos:
        CommandLine|contains:
        - 31B2F340-016D-11D2-945F-00C04FB984F9
        - 6AC1786C-016F-11D2-945F-00C04FB984F9
      condition: all of selection_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1484.001
      name: Group Policy Modification
      role: primary
    - id: T1484
      name: Domain or Tenant Policy Modification
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: gpo_editor_execution
      description: "mmc.exe launches gpme.msc with a gpobject: reference to a default domain GPO GUID, opening the policy for editing"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: target_gpo_identification
      description: "The gpobject: LDAP path and GPO GUID are decoded to identify which default policy (Default Domain or Default Domain Controllers) was opened"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: directory_object_modification
      description: "The edit writes changes to the GPO's directory-service object under CN=Policies,CN=System (5136), altering attributes such as gPCFileSysPath or ntSecurityDescriptor"
      mapped_to: Q4
      source: attack:T1484.001.directory_object_change
    - stage: 4
      label: sysvol_file_write
      description: "The policy change is persisted as files under the SYSVOL Policies tree on domain controllers (e.g. ScheduledTasks.xml, GptTmpl.inf), which deploy to domain members"
      mapped_to: Q5
      source: attack:T1484.001.sysvol_file_write
  dimension_coverage:
    - dimension: command_analysis
      technique: T1484.001
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1484.001
      covered_by: Q4
      source: attack:T1484.001.directory_object_change
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1484.001
      covered_by: Q4
      source: attack:T1484.001.directory_object_change
      coverage_type: direct
    - dimension: user_context
      technique: T1484.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: network_indicators
      technique: T1484
      covered_by: Q9
      source: template:cue_process_tool.follow_on
      coverage_type: indirect
  mutable_dispositions:
    - element: ObjectDN
      dimension: tuning_parameter
      specific_values: "CN=Policies,CN=System,DC=domain,DC=com"
      disposition: query_injection
      target_question: Q4
      notes: "Injected as winlog.event_data.ObjectDN|contains 'CN=Policies,CN=System' filter on the 5136 directory-object-change query."
    - element: AttributeModified
      dimension: tuning_parameter
      specific_values: "gPCFileSysPath, ntSecurityDescriptor"
      disposition: context
      target_question: Q4
      notes: "High-risk attribute names folded into the directory-object-change context; the 5136 query projects AttributeLDAPDisplayName so the analyst sees which attribute changed without an exact-match filter that could hide an unexpected attribute."
    - element: TargetFilename
      dimension: tuning_parameter
      specific_values: "ScheduledTasks.xml, GptTmpl.inf in SYSVOL"
      disposition: query_injection
      target_question: Q5
      notes: "Realized as the SYSVOL Policies file_event query; specific filenames named in context. Query filters on \\SYSVOL\\ + \\Policies\\ rather than the two exact filenames so other policy-file writes (registry.pol, GPT.INI) are not missed."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "correlate GPO object change and SYSVOL file modification within N seconds"
      disposition: query_injection
      target_question: Q4
      notes: "Realized as the +/-15m range on the directory-object-change and SYSVOL-write queries, correlating both with the editor process execution."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (non-admins / uncommon accounts editing policy)"
      disposition: context
      target_question: Q2
      notes: "Unexpected-modifier rationale folded into the host_user_context question and the user-actor baseline/clustering pair."
    - element: CommandLine
      dimension: command_analysis
      specific_values: "GPO manipulation tooling (Set-GPRegistryValue, New-GPOImmediateTask)"
      disposition: context
      target_question: Q3
      notes: "This rule's command is the gpme.msc/gpobject: GUI invocation, not the PowerShell GPO cmdlets the mutable element names; the decode question covers the actual command. The cmdlet-based variant is described as related tooling in the follow-on/precursor context rather than query-injected, since the rule does not fire on it."
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q10 user-pivot, Q11 service-principal substitute)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q12 user-pivot, Q13 service-principal substitute)
  skipped_templates:
    - template: cue_process_tool.clustering
      reason: "host-wide mmc.exe-window Image aggregation adds no unique dimension; cue_host_context.precursor (prior alerts on host) and cue_user_actor.clustering (cross-host user activity) already cover around-time host context, and the 5136 / SYSVOL correlation questions cover the time-correlation dimension specifically."
    - template: cue_host_context.clustering
      reason: "overlaps cue_host_context.precursor and cue_user_actor.clustering for host/around-time context; dropped to keep the count at 15 and avoid duplicating the around-time host view without adding a dimension."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This rule is a single policy-administration action (default-GPO edit), not a payload-execution or credential-access rule where the disposition turns on whether AV/EDR was weakened first; cue_host_context.precursor surfaces prior tamper alerts on the host for this non-execution-centric rule, so the generic host-tamper sweep adds no decisive signal here."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword match on 'ransomware' in a reference is incidental — this rule detects a Group Policy edit, an enabling/defense-evasion action, not encryption, ransom notes, or shadow-copy deletion. ransomware_impact questions (note dropped, encryption process, recovery state) would not align with what the rule fires on."
      - id: network_scope
        confidence: low
        reason: "Keyword match on 'ransomware' in a reference is incidental — the rule is a local process_creation detection of a GUI policy edit with no network indicator in its detection logic. The fleet-wide spread is already covered by cue_process_tool.scope and the SYSVOL/5136 cross-logsource questions; a generic network_scope set adds no aligned coverage."
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 15
    source_counts:
      template: 13
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 12
    logsource_diversity:
      process_creation: 12
      security: 1
      file_event: 1
      sigma_alert: 1
