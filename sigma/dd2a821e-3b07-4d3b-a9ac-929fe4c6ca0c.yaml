name: Suspicious Scheduled Task Creation via Masqueraded XML File
id: 6abd3f06-911a-48d9-8485-66676d924678
description: |
  Detects schtasks.exe creating a scheduled task by importing an XML definition file (/create with /xml) where the
  referenced file does not carry a .xml extension. Importing task properties from XML hides the run-as principal, trigger
  schedule, hidden flag, and executed command from the schtasks command line itself, so the command line alone does not
  reveal what the task does. Legitimate installers and management tooling occasionally import .xml task definitions, but a
  non-.xml import file combined with a user-writable source location is a common persistence-install pattern. Investigation
  should recover the imported task definition, confirm the run-as principal and trigger, and determine whether the task is
  still registered and has executed.
type: detection
detection_id: dd2a821e-3b07-4d3b-a9ac-929fe4c6ca0c
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match what this rule detects - schtasks creating a task by importing a non-.xml file?
  context: >-
    Confirm the alert is a true positive before investing time. The rule fires only when schtasks.exe (or a binary whose
    OriginalFileName is schtasks.exe) runs with a create flag (/create or -create) AND an XML-import flag (/xml or -xml) AND
    the command line does NOT contain the literal string ".xml". Verify all three conditions hold in the alerting command
    line, and note the path passed to /xml - a file without the .xml extension, especially one in a user-writable directory,
    is the masquerading behavior the rule targets.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to create scheduled tasks by importing task definition files?
  context: >-
    Legitimate administrative activity and some installers create scheduled tasks from XML definitions. Knowing whether the
    user role and host function normally involve task scheduling - an admin workstation, a deployment server, a packaging
    host - is often the fastest path to separating routine operations from operator-driven persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:scheduled_task.task_configuration
  question: What scheduled task was created, and what task name and source file were passed on the command line?
  context: >-
    The schtasks command line carries the task name (/tn) and the path to the imported XML definition (/xml). Because the
    task properties live in the imported file rather than on the command line, the /tr command, /sc schedule, and /ru run-as
    principal are NOT visible here - identify the task name and the source file path so the definition can be recovered and
    parsed. A source file in %TEMP%, %APPDATA%, %ProgramData%, %PUBLIC%, or the root of C:\ is a notable signal.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:masquerading.true_identity
  question: Is the process that created the task genuinely schtasks.exe, or a renamed binary, based on its metadata and hash?
  context: >-
    The rule matches either Image ending in schtasks.exe OR an OriginalFileName of schtasks.exe, so a renamed copy of
    schtasks placed elsewhere will also fire. Compare the Image path on disk against the embedded OriginalFileName, and check
    the signing chain and hash. A genuine Microsoft-signed schtasks.exe in System32 narrows the focus to the imported task
    definition; a copy running from a user-writable path or an unsigned binary shifts the disposition toward deliberate
    masquerading.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Hashes
- source: template:obfuscation.encoded_payload_decode
  question: Once the imported task definition is recovered, what command does the task execute, and is it encoded or obfuscated?
  context: >-
    The imported file holds the actual payload - the <Exec><Command> and <Arguments> the task runs. Recover the definition
    file (its path is on the schtasks command line) and inspect the executed command. Common encodings to decode include
    base64 (for example a PowerShell -EncodedCommand in the task arguments), hex, character concatenation, and
    environment-variable substitution. Decoding reveals the real intent - a download cradle, reverse shell, loader, or
    credential-theft action - that the command line deliberately keeps out of view.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:scheduled_task.task_trigger_and_timing
  question: When is the task scheduled to run, and has it already executed?
  context: >-
    The trigger in the imported definition determines when the persistence activates. Tasks set to run at boot or logon fire
    on the next restart; recurring tasks may already have run multiple times. Scheduled tasks execute as children of the
    Task Scheduler service, so processes spawned by svchost.exe hosting the Schedule service after the task was created
    indicate the payload has run.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\svchost.exe'
        ParentCommandLine|contains: 'Schedule'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:scheduled_task.task_still_present
  question: Is the scheduled task still registered on the system?
  context: >-
    Persistence is only effective if the task still exists. Some operators delete the task after a single execution to
    reduce forensic artifacts. Confirming the task is still present, by querying the task name recovered from the command
    line on the host or via endpoint tooling, determines whether the persistence is active and requires remediation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: llm_generated
  question: Does a scheduled-task creation event in the security log confirm this task, and what run-as principal and author does it record?
  context: >-
    Task creation is also recorded as a security-log event (4698) that includes the full task definition - the run-as
    principal, trigger, hidden flag, and executed command - which the schtasks command line hides when properties are
    imported from a file. Correlating the security event for this task name on this host recovers the run-as account (a
    SYSTEM or highly privileged principal is high-risk) and the author, giving an authoritative view of what was registered
    independent of the process command line.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      category: security
    detection:
      selection:
        EventID: 4698
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - TaskName
      - TaskContent
      - SubjectUserName
- source: template:cue_process_tool.process_chain
  question: What process spawned schtasks, and is that parent chain expected for task creation?
  context: >-
    The parent process reveals how the task creation was invoked. Interactive administration typically descends from
    explorer.exe or a console, and software installs from an installer or service. Parents such as a script host, an Office
    application, or a process running from a user-writable directory point to scripted or operator-driven task installation
    rather than routine management.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli_create:
        CommandLine|contains:
        - /create
        - -create
      selection_cli_xml:
        CommandLine|contains:
        - /xml
        - -xml
      filter_main_extension_xml:
        CommandLine|contains: .xml
      filter_main_system_process:
        IntegrityLevel:
        - System
        - S-1-16-16384
      filter_main_rundll32:
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - :\WINDOWS\Installer\MSI
        - .tmp,zzzzInvokeManagedCustomActionOutOfProc
      filter_optional_third_party:
        ParentImage|endswith:
        - :\ProgramData\OEM\UpgradeTool\CareCenter_*\BUnzip\Setup_msi.exe
        - :\Program Files\Axis Communications\AXIS Camera Station\SetupActions.exe
        - :\Program Files\Axis Communications\AXIS Device Manager\AdmSetupActions.exe
        - :\Program Files (x86)\Zemana\AntiMalware\AntiMalware.exe
        - :\Program Files\Dell\SupportAssist\pcdrcui.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli_create:
        CommandLine|contains:
        - /create
        - -create
      selection_cli_xml:
        CommandLine|contains:
        - /xml
        - -xml
      filter_main_extension_xml:
        CommandLine|contains: .xml
      filter_main_system_process:
        IntegrityLevel:
        - System
        - S-1-16-16384
      filter_main_rundll32:
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - :\WINDOWS\Installer\MSI
        - .tmp,zzzzInvokeManagedCustomActionOutOfProc
      filter_optional_third_party:
        ParentImage|endswith:
        - :\ProgramData\OEM\UpgradeTool\CareCenter_*\BUnzip\Setup_msi.exe
        - :\Program Files\Axis Communications\AXIS Camera Station\SetupActions.exe
        - :\Program Files\Axis Communications\AXIS Device Manager\AdmSetupActions.exe
        - :\Program Files (x86)\Zemana\AntiMalware\AntiMalware.exe
        - :\Program Files\Dell\SupportAssist\pcdrcui.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or coordinated activity across multiple
    endpoints. If the same user drove similar task creation on several hosts, the scope of the activity may be larger than a
    single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli_create:
        CommandLine|contains:
        - /create
        - -create
      selection_cli_xml:
        CommandLine|contains:
        - /xml
        - -xml
      filter_main_extension_xml:
        CommandLine|contains: .xml
      filter_main_system_process:
        IntegrityLevel:
        - System
        - S-1-16-16384
      filter_main_rundll32:
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - :\WINDOWS\Installer\MSI
        - .tmp,zzzzInvokeManagedCustomActionOutOfProc
      filter_optional_third_party:
        ParentImage|endswith:
        - :\ProgramData\OEM\UpgradeTool\CareCenter_*\BUnzip\Setup_msi.exe
        - :\Program Files\Axis Communications\AXIS Camera Station\SetupActions.exe
        - :\Program Files\Axis Communications\AXIS Device Manager\AdmSetupActions.exe
        - :\Program Files (x86)\Zemana\AntiMalware\AntiMalware.exe
        - :\Program Files\Dell\SupportAssist\pcdrcui.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining activity around the task creation reveals what led to it and what followed -
    reconnaissance, privilege escalation, payload staging, or additional persistence - and whether the task creation is part
    of a larger sequence.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host - defense tampering, reconnaissance, credential access - may reveal the attack
    stages that preceded the task creation and help reconstruct the timeline. A persistence install late in a sequence
    carries different weight than an isolated event.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this schtasks XML-import pattern over the past 90 days, and what command-line structures are typical?
  context: >-
    A task-creation pattern with no prior history on a host is more suspicious than one used regularly under stable parents.
    The query mirrors the rule's selection structure scoped to this host, so the count reflects the rule's specific firing
    pattern rather than generic schtasks usage. The CommandLine projection lets the analyst compare typical invocation patterns
    against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli_create:
        CommandLine|contains:
        - /create
        - -create
      selection_cli_xml:
        CommandLine|contains:
        - /xml
        - -xml
      filter_main_extension_xml:
        CommandLine|contains: .xml
      filter_main_system_process:
        IntegrityLevel:
        - System
        - S-1-16-16384
      filter_main_rundll32:
        ParentImage|endswith: \rundll32.exe
        ParentCommandLine|contains|all:
        - :\WINDOWS\Installer\MSI
        - .tmp,zzzzInvokeManagedCustomActionOutOfProc
      filter_optional_third_party:
        ParentImage|endswith:
        - :\ProgramData\OEM\UpgradeTool\CareCenter_*\BUnzip\Setup_msi.exe
        - :\Program Files\Axis Communications\AXIS Camera Station\SetupActions.exe
        - :\Program Files\Axis Communications\AXIS Device Manager\AdmSetupActions.exe
        - :\Program Files (x86)\Zemana\AntiMalware\AntiMalware.exe
        - :\Program Files\Dell\SupportAssist\pcdrcui.exe
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_* and not 1 of filter_main_* and not 1 of filter_optional_*) and _pb_scope
    fields:
    - CommandLine
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host around the same time?
  context: >-
    Operators rarely rely on a single persistence mechanism. After establishing one foothold, they commonly add backups
    using different techniques - other scheduled tasks, services, registry Run keys, BITS jobs, or WMI subscriptions.
    Multiple persistence installations in the same timeframe point to intentional stacking rather than an isolated
    administrative action.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1036.005
      name: Match Legitimate Resource Name or Location
      role: primary
    - id: T1053.005
      name: Scheduled Task
      role: primary
    - id: T1036
      name: Masquerading
      role: secondary
    - id: T1053
      name: Scheduled Task/Job
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: task_creation_via_xml_import
      description: "schtasks.exe imports a task definition from a non-.xml file (/create /xml), hiding task properties from the command line"
      mapped_to: Q3
      source: template:scheduled_task.task_configuration
    - stage: 2
      label: masqueraded_artifact_identity
      description: "Confirm the importing binary is genuine schtasks and the import file masquerades by lacking a .xml extension"
      mapped_to: Q4
      source: template:masquerading.true_identity
    - stage: 3
      label: payload_in_definition
      description: "The imported XML definition carries the executed command, run-as principal, and trigger not shown on the command line"
      mapped_to: Q8
      source: llm_generated
    - stage: 4
      label: task_execution
      description: "Task fires from the Task Scheduler service (svchost -k netsvcs Schedule)"
      mapped_to: Q6
      source: template:scheduled_task.task_trigger_and_timing
  dimension_coverage:
    - dimension: tuning_parameter
      technique: T1036.005
      covered_by: Q16
      source: template:lolbin.historical_lolbin_baseline
      coverage_type: direct
    - dimension: execution_context
      technique: T1036
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1036
      covered_by: Q6
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
    - dimension: obfuscation_check
      technique: T1053.005
      covered_by: Q5
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1053.005
      covered_by: Q10
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1053
      covered_by: Q3
      source: template:scheduled_task.task_configuration
      coverage_type: direct
  mutable_dispositions:
    - element: trusted_directory_list
      dimension: tuning_parameter
      specific_values: "C:\\Windows\\System32 (expected schtasks path)"
      disposition: context
      target_question: Q4
      notes: "Expected-path comparison folded into true_identity context; rule matches OriginalFileName so renamed-copy path anomaly is the signal"
    - element: process_baseline_age
      dimension: tuning_parameter
      specific_values: "30 days (novelty window)"
      disposition: context
      target_question: Q16
      notes: "Historical baseline uses a 90-day window; rationale only, no exact value queryable"
    - element: OriginalFilenameMismatch
      dimension: tuning_parameter
      specific_values: "Image vs PE OriginalFilename"
      disposition: query_injection
      target_question: Q4
      notes: "true_identity projects Image + OriginalFileName + Hashes for the mismatch check"
    - element: KnownSystemUtilityPaths
      dimension: tuning_parameter
      specific_values: "expected install directory for schtasks (System32)"
      disposition: context
      target_question: Q4
      notes: "Folded into true_identity context"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "creation-to-execution correlation window"
      disposition: query_injection
      target_question: Q6
      notes: "task_trigger_and_timing uses +4h to catch execution after creation"
    - element: UserContext
      dimension: user_context
      specific_values: "SYSTEM / non-standard run-as principal"
      disposition: query_injection
      target_question: Q8
      notes: "Security 4698 question projects SubjectUserName / run-as principal from the task definition"
    - element: TaskNamePattern
      dimension: tuning_parameter
      specific_values: "obfuscated/randomized task names"
      disposition: context
      target_question: Q3
      notes: "task_configuration directs analyst to recover the /tn task name for naming-convention review"
    - element: CommandLineEntropyThreshold
      dimension: obfuscation_check
      specific_values: "base64 / encoded blob in task command"
      disposition: context
      target_question: Q5
      notes: "encoded_payload_decode covers base64/hex/concatenation in the recovered task command"
    - element: TaskAuthor
      dimension: tuning_parameter
      specific_values: "unexpected account creating the task"
      disposition: query_injection
      target_question: Q8
      notes: "Security 4698 question recovers the task author"
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "suspicious binaries/scripts tied to the task"
      disposition: context
      target_question: Q3
      notes: "task_configuration parses /tn, source file, and (via the recovered definition) /tr command"
    - element: ExecutionWindow
      dimension: tuning_parameter
      specific_values: "lookback to correlate execution after registration"
      disposition: query_injection
      target_question: Q6
      notes: "task_trigger_and_timing +4h lookahead for scheduler-spawned execution"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both
    - dropped: cue_process_tool.process_chain
      reason: "parent cue not active (parent_binaries empty); process_chain kept as the parent-context question, not subsumed"
      kept: cue_process_tool.process_chain
    - dropped: lolbin.technique_class_lateral
      reason: "objective is persistence, not download/proxy-exec; other-persistence-LOLBin coverage provided by persistence_scope.process_based_persistence which enumerates at/sc/reg/bitsadmin/wmic on the host"
      kept: persistence_scope.process_based_persistence
    - dropped: masquerading.binary_origin
      reason: "masqueraded artifact is the imported non-.xml task file, not a renamed binary; schtasks is a genuine system binary, so basename file-event origin tracing of the binary adds no value. Binary identity covered by masquerading.true_identity"
      kept: masquerading.true_identity
    - dropped: masquerading.renamed_binary_activity
      reason: "schtasks is not a renamed hacking tool; its post-execution effect is the created task, already covered by task_trigger_and_timing and task_still_present. Renamed-binary-on-other-hosts scope does not apply to a system binary"
      kept: scheduled_task.task_trigger_and_timing
    - dropped: cue_process_tool.baseline
      reason: "generic Image|endswith baseline superseded by the rule-pattern reflected baseline (lolbin.historical_lolbin_baseline) which counts the rule's specific firing pattern, not generic schtasks usage"
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "host-wide tool clustering duplicated by cue_host_context.clustering (same host-scoped aggregation by Image)"
      kept: cue_host_context.clustering
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: scheduled_task.task_configuration (hint acted on)"
    - template: cue_process_tool.baseline_deviation
      reason: "reflected host-pattern baseline redundant with lolbin.historical_lolbin_baseline (identical reflected detection block, host-scoped)"
    - template: cue_process_tool.follow_on
      reason: "schtasks children are the scheduler-spawned task execution, covered more precisely by scheduled_task.task_trigger_and_timing"
    - template: universal.endpoint_protection_health
      reason: "rule is a persistence-install detection, not execution/credential-access; cue_host_context.precursor already surfaces prior tamper alerts on the host, and defense_tampering technique set is not confirmed"
    - template: organizational_context.change_ticket
      reason: "coverage of authorized-activity check provided by host_user_context and the user/parent baseline questions; omitted to hold question count and because the more decisive check for this rule is recovering the imported task definition"
    - template: organizational_context.human_recall
      reason: "human-recall confirmation overlaps host_user_context; dropped to hold count"
    - template: organizational_context.timing_alignment
      reason: "timing-vs-work-hours overlaps cue_host_context.clustering and task_trigger_and_timing; dropped to hold count"
    - template: organizational_context.procedural_consistency
      reason: "GPO/SCCM-vs-direct-schtasks judgment overlaps host_user_context; dropped to hold count"
    - template: persistence_scope.file_based_persistence
      reason: "file_event Startup/Run-key drop is a distinct parallel mechanism; the rule's task is registered via schtasks, and process-based persistence stacking (persistence_scope.process_based_persistence) is the more direct scope question"
    - template: lolbin.renamed_binary_check
      reason: "expected-path check folded into masquerading.true_identity (Image vs OriginalFileName vs expected System32 path); separate question would duplicate the identity check"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1053.005) is not covered — pull in cue_registry_key.* or author a custom question with `category: registry_set`"
      disposition: declined
      reason: >-
        The registry footprint of task registration (Sysmon 13/14 writes under
        \Schedule\TaskCache\Tree and \Tasks) records the same registration moment
        the security 4698 question recovers with richer fidelity — the 4698
        question (run-as principal, author, full TaskContent) is the authoritative
        view of what was registered, and task_still_present confirms the
        TaskCache entry persists. A registry_set question would duplicate that
        registration-time evidence without adding a distinct investigative angle
        for this command-line-driven detection.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - scheduled_task
    - masquerading
    - obfuscation
    - persistence_scope
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 0
      llm_generated: 1
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 14
      security: 1
      sigma_alert: 1
