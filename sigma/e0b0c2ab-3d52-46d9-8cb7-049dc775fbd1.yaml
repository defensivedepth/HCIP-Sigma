name: Enumeration for Credentials in Registry
id: 82f27b8f-6722-4c56-b0ff-f63845da1bbe
description: |
  Detects reg.exe performing a recursive value search of the registry for string values, scoped to a hive (HKLM or HKCU) or to the PuTTY saved-sessions key. This is the command-line structure used to hunt for credentials, connection strings, and stored passwords left in the registry by applications and services. Legitimate administrators and software inventory tools occasionally run broad registry searches, so confirm the user, host role, and parent chain before escalating; an unexpected account or parent enumerating credential-bearing paths points to active credential discovery.
type: detection
detection_id: e0b0c2ab-3d52-46d9-8cb7-049dc775fbd1
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line match a recursive reg.exe value search for stored credentials?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires when reg.exe runs a query with the recursive flag and a string-value type filter (' query ', '/t ', REG_SZ, '/s') AND searches a hive (HKLM or HKCU with '/f ') or the PuTTY saved-sessions key. Verify all of these elements are present in the alerting command.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to perform broad registry credential searches?
  context: Legitimate administrative activity and inventory tooling can run recursive registry searches. Knowing whether the user role and host function are consistent with credential-discovery-style queries is often the fastest way to separate routine operations from credential access.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: What registry paths and search terms did this query target, and which credential stores do they expose?
  context: The full command line reveals which hive and search string were used. A '/f ' filter naming a credential-related term, or a query against HKCU\Software\SimonTatham\PuTTY\Sessions, targets application-stored secrets and connection details. The specific hive and search term determine which accounts, services, or saved sessions may be exposed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1552.002.command_analysis
  question: Did the same host run additional reg.exe query commands targeting other hives or credential terms in the same window?
  context: Credential discovery is often iterative -- an operator sweeps HKLM, then HKCU, then specific application keys with different search strings. Surfacing other reg.exe query invocations in a tight window shows whether this was a single lookup or a systematic registry credential sweep.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith: '\reg.exe'
        CommandLine|contains: ' query '
      condition: selection and selection_reg
    fields:
      - CommandLine
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned reg.exe and is that parent chain expected?
  context: The parent process reveals how the query was invoked. Interactive use from explorer.exe or a known admin shell differs from invocation by a script host, Office application, or an unfamiliar binary. An unusual parent such as wscript.exe, mshta.exe, or a freshly dropped executable points to scripted or operator-driven credential discovery.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      reg:
        Image|endswith: \reg.exe
        CommandLine|contains|all:
        - ' query '
        - '/t '
        - REG_SZ
        - /s
      hive:
      - CommandLine|contains|all:
        - '/f '
        - HKLM
      - CommandLine|contains|all:
        - '/f '
        - HKCU
      - CommandLine|contains: HKCU\Software\SimonTatham\PuTTY\Sessions
      _pb_scope:
        User|expand: '%User%'
      condition: (reg and hive) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      reg:
        Image|endswith: \reg.exe
        CommandLine|contains|all:
        - ' query '
        - '/t '
        - REG_SZ
        - /s
      hive:
      - CommandLine|contains|all:
        - '/f '
        - HKLM
      - CommandLine|contains|all:
        - '/f '
        - HKCU
      - CommandLine|contains: HKCU\Software\SimonTatham\PuTTY\Sessions
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (reg and hive) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user ran credential-discovery activity on several hosts, the scope of compromise may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      reg:
        Image|endswith: \reg.exe
        CommandLine|contains|all:
        - ' query '
        - '/t '
        - REG_SZ
        - /s
      hive:
      - CommandLine|contains|all:
        - '/f '
        - HKLM
      - CommandLine|contains|all:
        - '/f '
        - HKCU
      - CommandLine|contains: HKCU\Software\SimonTatham\PuTTY\Sessions
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (reg and hive) and _pb_scope
    fields:
    - host.name
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this registry-search pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even routine tools get abused. The query mirrors the rule's selection structure scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here -- not generic reg.exe usage. A new parent, an unexpected user account, or an unusual command structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      reg:
        Image|endswith: \reg.exe
        CommandLine|contains|all:
        - ' query '
        - '/t '
        - REG_SZ
        - /s
      hive:
      - CommandLine|contains|all:
        - '/f '
        - HKLM
      - CommandLine|contains|all:
        - '/f '
        - HKCU
      - CommandLine|contains: HKCU\Software\SimonTatham\PuTTY\Sessions
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (reg and hive) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this event, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, or follow-on credential use.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or initial execution -- may reveal the attack stages that preceded this credential-discovery activity and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts or saved sessions may be exposed, and do any grant elevated or remote access?
  context: The severity depends on what the registry search could surface. PuTTY saved sessions expose hostnames and connection details for remote systems; credential strings in HKLM may belong to services or domain accounts. Cross-reference the user and any discovered targets against directory group memberships to gauge the blast radius.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was reg.exe executed from its expected System32 path?
  context: reg.exe executed from outside System32 or SysWOW64 may indicate the binary was copied or renamed to evade path-based allowlists. A reg.exe running from a user's temp or download directory is more suspicious than one in System32.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.technique_class_lateral
  question: Were other discovery or credential-access tools run on this host recently?
  context: Operators who use reg.exe to hunt for credentials often pair it with other living-off-the-land utilities for discovery and proxy execution. Finding additional LOLBins active on the host strengthens the case that this registry search is part of hands-on-keyboard credential access rather than an isolated administrative query.
  range: -24h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_img:
        Image|endswith:
          - '\certutil.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
          - '\rundll32.exe'
          - '\regsvr32.exe'
          - '\mshta.exe'
          - '\findstr.exe'
          - '\nltest.exe'
          - '\whoami.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\schtasks.exe'
          - '\sc.exe'
      condition: selection and selection_img
    fields:
      - Image
      - CommandLine
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host fired this reg.exe credential-search pattern over the past 90 days, and what command-line structures are typical?
  context: A registry-search pattern with no prior history on a host is more suspicious than one used regularly under stable parents. The query mirrors the rule's selection structure scoped to this host, so the count reflects this specific firing pattern rather than generic reg.exe usage. The command-line projection lets the analyst compare typical invocation patterns against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      reg:
        Image|endswith: \reg.exe
        CommandLine|contains|all:
        - ' query '
        - '/t '
        - REG_SZ
        - /s
      hive:
      - CommandLine|contains|all:
        - '/f '
        - HKLM
      - CommandLine|contains|all:
        - '/f '
        - HKCU
      - CommandLine|contains: HKCU\Software\SimonTatham\PuTTY\Sessions
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (reg and hive) and _pb_scope
    fields:
    - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1552.002
      name: Credentials in Registry
      role: primary
    - id: T1552
      name: Unsecured Credentials
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: registry_credential_search
      description: "reg.exe performs a recursive string-value query against a credential-bearing hive or the PuTTY sessions key"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 2
      label: process_ancestry
      description: "Parent process driving the registry enumeration indicates scripted vs. interactive credential discovery"
      mapped_to: Q5
      source: template:cue_process_tool.process_chain
    - stage: 3
      label: credential_use_followon
      description: "Recovered credentials are used for lateral movement or remote access shortly after"
      mapped_to: Q13
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1552.002
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1552.002
      covered_by: Q4
      source: attack:T1552.002.command_analysis
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1552.002
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1552
      covered_by: Q4
      source: attack:T1552.002.command_analysis
      coverage_type: direct
    - dimension: process_identification
      technique: T1552
      covered_by: Q15
      source: template:lolbin.renamed_binary_check
      coverage_type: direct
    - dimension: user_context
      technique: T1552
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: KeywordMatch
      dimension: tuning_parameter
      specific_values: "password, credential, login (and the rule's PuTTY Sessions path)"
      disposition: context
      target_question: Q3
      notes: "Search terms read from the alerting CommandLine; credential_target_identification context directs the analyst to the '/f ' term and PuTTY path"
    - element: ParentProcessFilter
      dimension: parent_process_analysis
      specific_values: "rationale only (cmd.exe > reg.exe vs services.exe > reg.exe)"
      disposition: new_question
      target_question: Q5
      notes: "process_chain question surfaces the parent and its chain for ancestry tuning"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (chained registry reads then credential use)"
      disposition: new_question
      target_question: Q4
      notes: "command_analysis question correlates additional reg.exe queries in a +/-30m window"
    - element: RegistryHiveScope
      dimension: tuning_parameter
      specific_values: "HKLM vs HKCU vs PuTTY Sessions"
      disposition: context
      target_question: Q3
      notes: "Hive scope is read from the alerting CommandLine and explained in credential_target_identification context"
    - element: SuspiciousProcessList
      dimension: tuning_parameter
      specific_values: "reg.exe, cmd.exe, powershell.exe"
      disposition: query_injection
      target_question: Q4
      notes: "command_analysis query filters reg.exe query invocations on the host"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair -- non-deduplicable (subject vs. mechanism identity pivots)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair -- non-deduplicable (subject vs. mechanism identity pivots)"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (more specific decode of the credential target)"
    - template: cue_process_tool.baseline
      reason: "host-pattern baseline is covered more precisely by lolbin.historical_lolbin_baseline (rule-pattern scoped, not generic reg.exe volume)"
    - template: cue_process_tool.follow_on
      reason: "reg.exe query is a read operation and rarely spawns children; child-process angle adds no coverage for credential discovery and would dilute the 18-question budget"
    - template: cue_process_tool.clustering
      reason: "host-around-time context covered by cue_host_context.clustering (same aggregation shape, same window); avoids duplicate host-scoped query"
    - template: cue_process_tool.scope
      reason: "fleet-wide rule-pattern count is the same selection as lolbin.historical_lolbin_baseline scoped per-host; host history is the more actionable angle for a single alert and the budget is full"
    - template: registry.registry_key_context
      reason: "queries registry_set logsource via ProcessGuid; reg.exe QUERY reads the registry and emits no registry_set write event, so this returns nothing -- the credential target is read from the process_creation CommandLine instead (covered)"
    - template: registry.registry_related_modifications
      reason: "looks for registry WRITE operations (add/delete/import); this rule detects read-only queries, so write-side modification scope is out of scope for credential discovery"
    - template: persistence_scope.process_based_persistence
      reason: "persistence_scope set applied by binary heuristic, but this rule is a credential-read query with no persistence behavior; persistence-tool sweep is not the investigative question and budget is full"
    - template: persistence_scope.file_based_persistence
      reason: "no file-drop or persistence-location behavior in a registry read; out of scope for credential discovery"
    - template: universal.evidence_preservation
      reason: "reg.exe query reads registry values and drops no payload file to preserve; has_file_reference is set only because the PuTTY Sessions registry path appears in detection, not because a file artifact exists on disk"
    - template: universal.endpoint_protection_health
      reason: "cue_host_context.precursor is present and surfaces prior tamper alerts on the host; credential discovery via a registry read is not the execution/impact action that turns on prior-tamper sweeps, so the generic tamper sweep is redundant here"
  reflection_overrides: []
  hint_dispositions:
    - hint: "registry.registry_key_context replaces cue_process_tool.mechanism_decode"
      disposition: declined
      reason: >-
        registry.registry_key_context queries the registry_set logsource linked
        by ProcessGuid, but this rule fires on a reg.exe QUERY (a read), which
        emits no registry_set write event -- the question would always return
        empty. The mechanism_decode replacement that actually applies is
        credential_access.credential_target_identification (Q3), which decodes
        the targeted hive and search term from the process_creation CommandLine.
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: declined
      reason: >-
        The only path-like value in the detection is the PuTTY Sessions registry
        key (HKCU\Software\SimonTatham\PuTTY\Sessions), which is a registry path
        read by the query, not a filesystem path written to disk. No payload or
        output file is produced, so a %file_paths% / file_event question
        (evidence_preservation) returns nothing; the registry target is covered
        as a CommandLine value in credential_target_identification (Q3).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 14
    logsource_diversity:
      process_creation: 15
      sigma_alert: 2
