name: Invoke-Obfuscation Via Use Clip
id: b190f993-e4a0-436a-94bf-a10f16e5819f
description: |
  Detects a command-line pattern characteristic of the Invoke-Obfuscation CLIP+ launcher, where a payload is echoed into the
  Windows clipboard via clip.exe and then read back and invoked by PowerShell (echo ... | clip && powershell ...
  [Clipboard]::GetText().Invoke()). The technique bounces an obfuscated PowerShell command through the clipboard to evade
  command-line inspection, and the invocation itself runs in memory rather than from a file. Legitimate clipboard piping with
  clip.exe is common in scripting, but the combination of an echoed payload followed by a clipboard read that resolves to
  Clipboard or a backtick-broken Invoke is rarely benign. Investigation should decode the obfuscated payload, identify what the
  resolved command executes, and confirm whether the launch context and user are expected.
type: detection
detection_id: e1561947-b4e3-4a74-9bdd-83baed21bdb5
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the alerting command line match the clipboard-launcher obfuscation pattern this rule detects?
  context: >-
    Before investing investigation time, confirm the alert is a true positive. The rule fires on a single command line that
    echoes content into the clipboard via clip.exe and then reads it back with a Clipboard reference or a backtick-broken
    Invoke. Review the full command line of the triggering event and confirm both halves of the pattern are present.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run clipboard-piped command interpreters?
  context: >-
    Legitimate administrative scripting can pipe text through clip.exe, but a clipboard read that resolves to PowerShell
    invocation is unusual outside of obfuscation frameworks. Knowing whether the user role and host function include
    interactive scripting or automation that uses this pattern is often the fastest way to separate routine activity from
    operator tradecraft.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this clipboard-launcher execution part of an authorized penetration test or red team engagement?
  context: >-
    The Invoke-Obfuscation framework that produces this clipboard-launcher pattern is used in authorized security
    assessments. Before deeper analysis, check whether a penetration test or red team exercise is active on this host or
    network segment. An authorized engagement with matching timing and tooling resolves the alert without further work.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.evidence_preservation
  question: Are any files referenced or written by the decoded payload still on disk for analysis?
  context: >-
    Clipboard-launched payloads often resolve to download cradles or in-memory loaders that may drop a secondary file. If the
    decoded command references a payload path, decoded output, or downloaded file, checking whether it still exists on disk
    enables hash lookups, content review, and reputation checks.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: template:powershell.encoded_command_decode
  question: What does the clipboard-bounced payload decode to, and what operation does the resolved command perform?
  context: >-
    The clipboard launcher hides the real command by echoing it into the clipboard and resolving it back at runtime, often
    layered with backtick escaping, string concatenation, or base64. Reconstruct the obfuscated payload and identify what the
    resolved command does -- common outcomes are download cradles such as Net.WebClient.DownloadString, in-memory .NET
    assembly loading, or reverse shells. Focus on any payload source and what ultimately gets executed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1059.001.image_load
  question: Did the PowerShell process load the .NET automation assemblies that indicate in-memory script execution on this host?
  context: >-
    The clipboard launcher resolves its payload in memory through a Clipboard GetText invocation rather than from a file, so
    the in-memory invocation surfaces as image-load events for the PowerShell automation and CLR assemblies (such as
    System.Management.Automation and clr.dll) on the host. Correlate these loads in the alert window to confirm that an
    in-memory PowerShell invocation actually ran. If image-load telemetry is not collected in this environment, treat zero
    results as a visibility gap rather than evidence nothing executed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ImageLoaded|contains:
          - 'System.Management.Automation'
          - 'System.Management.Automation.ni.dll'
          - '\clr.dll'
          - '\clrjit.dll'
          - 'amsi.dll'
      condition: selection
    fields:
      - Image
      - ImageLoaded
      - Signed
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated content of the clipboard-resolved command?
  context: >-
    Script block logging records the deobfuscated script content that PowerShell actually executes, even when the original
    command line is obfuscated or resolved from the clipboard at runtime. This provides ground truth about what ran, bypassing
    the clipboard indirection. If PowerShell operational logs are not ingested in this environment, treat the absence of
    records as a visibility gap worth noting.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before this execution?
  context: >-
    Obfuscated launchers are frequently paired with control evasion. Common precursors include AMSI bypass by patching
    amsi.dll in memory, execution policy changes, constrained language mode escapes, and PowerShell version-2 downgrade
    attacks that lack logging. Finding these events in the hour before the clipboard launcher fired strengthens the case for
    malicious intent.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned the clipboard launcher, and is that parent chain expected?
  context: >-
    The parent process reveals how the launcher was invoked. A clipboard one-liner launched from explorer.exe or an
    interactive cmd session looks different from one spawned by mshta.exe, wscript.exe, an Office application, or a remote
    execution service. Walk up the tree to see what spawned the parent and what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this clipboard-launcher pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    Even on hosts where this pattern has appeared before, the parent process and user distribution reflects how the rule's
    specific firing pattern has historically run here, not generic clip.exe usage. A new parent process, an unexpected user
    account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|re: (?i)echo.*clip.*&&.*(?:Clipboard|i`?n`?v`?o`?k`?e`?)
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this clipboard-launcher pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    This query counts the rule's firing pattern fleet-wide with no host scope, so the result reflects the clipboard-launcher
    pattern rather than generic clip.exe usage. Many hosts firing the same pattern points to scripted deployment or vendor
    automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|re: (?i)echo.*clip.*&&.*(?:Clipboard|i`?n`?v`?o`?k`?e`?)
      condition: selection
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|re: (?i)echo.*clip.*&&.*(?:Clipboard|i`?n`?v`?o`?k`?e`?)
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|re: (?i)echo.*clip.*&&.*(?:Clipboard|i`?n`?v`?o`?k`?e`?)
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the
    same user triggered similar activity on multiple hosts, the scope of the incident may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|re: (?i)echo.*clip.*&&.*(?:Clipboard|i`?n`?v`?o`?k`?e`?)
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining activity around the alert reveals what led to this event and what happened
    after, and whether it is part of a larger sequence. Novel parent-child process pairs stand out against the repetitive
    background of agent telemetry.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or credential
    access -- may reveal the attack stages that preceded this clipboard launcher and help reconstruct the timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1027
      name: Obfuscated Files or Information
      role: primary
    - id: T1059.001
      name: PowerShell
      role: secondary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: clipboard_stage
      description: "Payload echoed into the Windows clipboard via clip.exe within a cmd one-liner"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: obfuscated_decode
      description: "Clipboard content read back and deobfuscated into a runnable PowerShell command"
      mapped_to: Q6
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: in_memory_invocation
      description: "Resolved command invoked in memory via [Clipboard]::GetText().Invoke() (reflective/.NET execution)"
      mapped_to: Q7
      source: attack:T1059.001.image_load
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q6
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1027
      covered_by: Q15
      source: template:cue_user_actor.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1027
      covered_by: Q8
      source: template:powershell.script_block_logging
      coverage_type: indirect
    - dimension: user_context
      technique: T1027
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q1
      source: template:universal.verify_trigger_match
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q10
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "echo/clip/&&/Clipboard|invoke regex (rule selection)"
      disposition: query_injection
      target_question: Q11
      notes: "Rule regex embedded verbatim in baseline_deviation and scope (REFLECTED) and adapted into user_actor baseline_svc/clustering_svc CommandLine|re for the CommandLine-only rule"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "abnormal parents (mshta, wscript, Office) per analytic"
      disposition: context
      target_question: Q10
      notes: "process_chain question text enumerates abnormal parents; ParentImage projected"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q15
      notes: "clustering windows scope the surrounding activity; no fixed value to inject"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rare/never-before-seen .NET assemblies tied to PS abuse"
      disposition: query_injection
      target_question: Q7
      notes: "Injected as ImageLoaded|contains automation/CLR/amsi assemblies in the new image_load question"
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "EID 4104 script block length"
      disposition: context
      target_question: Q8
      notes: "script_block_logging surfaces 4104 content; threshold is a tuning rationale, not injected"
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "entropy tuning is rationale; decode question addresses obfuscation directly"
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "LOLBins/admin tools misused for obfuscation"
      disposition: context
      target_question: Q10
      notes: "process_chain context covers abnormal/LOLBin parents"
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "Phase 2 within-phase dedup — powershell.encoded_command_decode is the more specific technique-set decode question for this PowerShell obfuscation rule"
      kept: powershell.encoded_command_decode
    - dropped: cue_process_tool.clustering
      reason: "within-phase dedup — cue_host_context.clustering covers host-around-time activity with richer Image+ParentImage grouping"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "within-phase dedup — cue_process_tool.baseline_deviation (REFLECTED) is the filter-correct host-historical view of the same firing pattern and also surfaces parent/user distribution"
      kept: cue_process_tool.baseline_deviation
    - dropped: hacking_tool.tool_capability_used
      reason: "within-phase dedup — re-displays the triggering event to identify the invoked capability, which the decode question already extracts for this CommandLine-pattern rule"
      kept: powershell.encoded_command_decode
    - dropped: cue_user_actor.baseline
      reason: "user_actor pair — non-deduplicable; kept (user-pivot retained alongside baseline_svc)"
      kept: cue_user_actor.baseline
    - dropped: cue_user_actor.clustering
      reason: "user_actor pair — non-deduplicable; kept (user-pivot retained alongside clustering_svc)"
      kept: cue_user_actor.clustering
  skipped_templates:
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by: powershell.encoded_command_decode (parent is powershell.exe — more specific decode)"
    - template: hacking_tool.tool_specific_artifacts
      reason: "default query scopes file_event by Image|endswith|expand %Image%, but this rule has no Image field (CommandLine-only); an Image-less host-wide file_event sweep would be unscoped. evidence_preservation covers file artifacts via %file_paths%"
    - template: cue_process_tool.follow_on
      reason: "low value for this rule — the clipboard launcher invokes its payload in memory via [Clipboard]::GetText().Invoke() (reflective load), so a generic child-process query is weaker than the new image_load question that captures the in-memory invocation; slot reallocated to the image_load hint"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - hacking_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 13
      file_event: 1
      image_load: 1
      powershell: 1
      sigma_alert: 1
