name: UAC Bypass via Windows Firewall Snap-In Hijack
id: 6d10433d-589e-4476-9788-bd1a392a2398
description: |
  Detects a child process spawned by mmc.exe when mmc.exe was launched with the Windows Firewall snap-in (WF.msc),
  excluding WerFault.exe error-reporting children. mmc.exe loading certain auto-elevated snap-ins runs at High
  integrity without a UAC prompt; an actor who preconditions the system to load attacker-controlled code into mmc's
  process space when it opens WF.msc obtains a Medium-to-High integrity elevation. Legitimate use of this exact
  parent-child pattern is rare, so the investigation should determine what the mmc child process did with the elevated
  token, whether registry or system state was modified to set up the bypass, and whether other escalation activity
  surrounds the event.
type: detection
detection_id: e52cb31c-10ed-4aea-bcb7-593c9f4a315b
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-06'
questions:
- source: template:universal.verify_trigger_match
  question: Was this process spawned by mmc.exe launched with the WF.msc Windows Firewall snap-in, and is the child something other than WerFault.exe?
  context: Confirm the alert is a true positive by reviewing the parent process and parent command line of the triggering event. The rule fires when the parent is mmc.exe AND its command line contains WF.msc AND the child is not WerFault.exe. A WerFault.exe child would have been excluded, so any child seen here is in scope.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to be managing the Windows Firewall through the MMC snap-in?
  context: Administrators do open the Windows Firewall snap-in through mmc.exe, but it spawning a child process at elevated integrity is unusual. Knowing whether this user routinely performs local firewall administration on this host helps separate legitimate management from an elevation attempt.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_parent_process.mechanism_decode
  question: What is the mmc.exe parent process and what was it doing when it spawned this child?
  context: The mmc.exe parent event shows how the management console was launched and what loaded the WF.msc snap-in. Retrieving the parent's own creation event reveals what spawned mmc.exe and the full parent command line, which establishes whether the snap-in load was an interactive admin action or driven by another process.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - ParentImage
- source: attack:T1548.tuning_parameter
  question: What did the mmc child process do at elevated integrity, and is the child binary signed and located where expected?
  context: The bypass succeeds when attacker-controlled code runs inside mmc's elevated process space. Decode the child command line and check the binary's path and signature; an unsigned binary, or one running from a user-writable directory such as a profile or temp path, points to hijacked execution rather than a normal snap-in component.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - CurrentDirectory
      - IntegrityLevel
- source: attack:T1548.user_context
  question: Was an elevated or special-privilege logon assigned around the time of this event?
  context: A successful UAC bypass yields a High integrity token without a normal consent prompt. Special-privilege assignment events on this host near the alert time corroborate that an elevated session was established; correlating the account and time with the process activity confirms the elevation took effect.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      product: windows
      category: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        winlog.event_id:
          - 4672
          - 4624
      condition: selection
    fields:
      - winlog.event_id
      - winlog.event_data.SubjectUserName
      - winlog.event_data.TargetUserName
      - winlog.event_data.LogonType
- source: template:uac_bypass.bypass_technique_registry_artifacts
  question: Were registry modifications made through reg.exe, powershell, or cmd to set up this UAC bypass?
  context: Many UAC bypass techniques write a command to a registry key that an auto-elevated process reads and executes. Common targets include ms-settings, mscfile, exefile and Folder shell-open-command keys, the Environment key, and DelegateExecute. Finding such a write shortly before the mmc child spawn confirms the bypass mechanism and reveals the payload that ran elevated.
  range: -15m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - '\reg.exe'
          - '\powershell.exe'
          - '\cmd.exe'
        CommandLine|contains:
          - 'ms-settings'
          - 'mscfile'
          - 'Classes\exefile'
          - 'Classes\Folder'
          - 'Environment'
          - 'Shell\Open\command'
          - 'DelegateExecute'
      condition: selection
    fields:
      - CommandLine
      - Image
- source: attack:T1548.tuning_parameter
  question: Were registry values written to the COM/snap-in keys that mmc.exe reads when loading WF.msc?
  context: The byeintegrity-style bypass preconditions the system by registering attacker-controlled code under the COM or snap-in registration keys that mmc consults when it loads the firewall snap-in. A registry_set event touching CLSID, InprocServer32, or snap-in registration paths shortly before the spawn is the setup step that process-creation visibility alone can miss.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - 'CLSID'
          - 'InprocServer32'
          - 'SnapIns'
          - 'NodeTypes'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: template:uac_bypass.elevated_action_after_bypass
  question: What action was performed with elevated privileges after the UAC bypass?
  context: UAC bypass is a means to an end. After elevating, actors install persistence, dump credentials, tamper with security tools, or access protected resources. Examining processes running at High or System integrity on this host shortly after the spawn reveals the objective behind the elevation.
  range: +30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        IntegrityLevel:
          - 'High'
          - 'System'
          - 'S-1-16-16384'
          - 'S-1-16-12288'
      condition: selection
    fields:
      - Image
      - CommandLine
      - IntegrityLevel
- source: template:cue_parent_process.follow_on
  question: What other child processes did mmc.exe spawn on this host?
  context: An actor who has hijacked mmc's elevated process space may use it to launch several tools in sequence. Seeing every child of mmc.exe on this host reveals the full scope of activity initiated through the compromised console, beyond the single child that triggered the alert.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_parent_process.baseline
  question: Has mmc.exe spawned this child binary on this host before?
  context: Some parent-child relationships are routine, but mmc.exe spawning an arbitrary child process is not part of normal snap-in operation. Historical frequency of this specific mmc.exe-to-child pairing on the host distinguishes a novel spawning relationship from an established one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \mmc.exe
        ParentCommandLine|contains: WF.msc
      filter:
        Image|endswith: \WerFault.exe
      _pb_scope:
        User|expand: '%User%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \mmc.exe
        ParentCommandLine|contains: WF.msc
      filter:
        Image|endswith: \WerFault.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spread across multiple endpoints. If the same user triggered similar activity on more than one host, the scope of compromise may be larger than the single alert suggests.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentImage|endswith: \mmc.exe
        ParentCommandLine|contains: WF.msc
      filter:
        Image|endswith: \WerFault.exe
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection and not filter) and _pb_scope
    fields:
    - host.name
- source: template:uac_bypass.other_escalation_attempts
  question: Were other privilege escalation techniques attempted on this host?
  context: Actors who attempt one UAC bypass often try several methods until one succeeds, especially when the OS version or configuration blocks certain techniques. Finding multiple bypass or escalation detections from the same user or process confirms deliberate privilege escalation rather than a false positive.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - rule.name
      - rule.uuid
      - event.severity_label
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or credential access — may reveal the stages that preceded the elevation and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1548
      name: Abuse Elevation Control Mechanism
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: bypass_setup
      description: "Actor preconditions the system (registry/COM snap-in keys) so mmc.exe loads attacker-controlled code when it opens WF.msc"
      mapped_to: Q7
      source: attack:T1548.tuning_parameter
    - stage: 2
      label: auto_elevated_load
      description: "mmc.exe launched with WF.msc loads the snap-in and spawns a child at High integrity without a UAC prompt"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: elevated_action
      description: "Attacker-controlled child runs at High integrity and performs the elevated objective"
      mapped_to: Q4
      source: attack:T1548.tuning_parameter
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1548
      covered_by: Q3
      source: template:cue_parent_process.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1548
      covered_by: Q6
      source: template:uac_bypass.bypass_technique_registry_artifacts
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1548
      covered_by: Q4
      source: attack:T1548.tuning_parameter
      coverage_type: direct
    - dimension: user_context
      technique: T1548
      covered_by: Q5
      source: attack:T1548.user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ElevatedProcessPath
      dimension: tuning_parameter
      specific_values: "rationale only (paths to monitor for unsigned/unexpected elevated binaries)"
      disposition: new_question
      target_question: Q4
      notes: "No fixed path list in the analytic; surfaced as Image/CurrentDirectory/IntegrityLevel projection on the child plus a signing/location prompt in context."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "mmc.exe (from rule detection)"
      disposition: query_injection
      target_question: Q9
      notes: "process.parent.name|expand %ParentName% (mmc.exe) drives the follow_on and baseline parent-child queries."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (time between registry modification and elevated process spawn)"
      disposition: context
      target_question: Q6
      notes: "Registry-setup question uses -15m backward window; COM-key registry_set question uses -1h; described in context, not a queryable literal."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent_process is primary cue — parent_process.mechanism_decode subsumes the process-chain lookup (hint acted on)"
      kept: cue_parent_process.mechanism_decode
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "host+binary decode of the child is covered more precisely by the ProcessGuid-scoped attack:T1548.tuning_parameter child-action question; the rule has no bare CommandLine selection so a host+Image sweep would surface unrelated binaries"
    - template: cue_process_tool.baseline
      reason: "host-level binary baseline duplicates cue_parent_process.baseline, which scopes by the mmc.exe parent and is more specific for this parent-driven rule"
    - template: cue_parent_process.clustering
      reason: "parent-associated activity in the alert window is covered by cue_host_context.clustering scope and cue_parent_process.follow_on; the +/-15m parent sweep adds no distinct dimension"
    - template: cue_parent_process.scope
      reason: "cross-host occurrence of this parent-child pattern is covered by cue_user_actor.clustering_svc, which pivots on the same parent+binary pair in the alert window"
    - template: cue_host_context.clustering
      reason: "host-wide activity-around-alert is covered by cue_parent_process.follow_on (mmc children) and the precursor alert sweep; a generic Image+ParentImage aggregation adds volume without a distinct angle for this parent-specific rule"
    - template: universal.endpoint_protection_health
      reason: "recommended-only generic tamper sweep; the precursor alert question and other_escalation_attempts already surface prior tamper/escalation alerts on the host, and this is a privilege-escalation rather than a payload-execution rule, so the dedicated sweep does not earn a slot under the 18-question cap"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - uac_bypass
  generation_stats:
    total_questions: 16
    source_counts:
      template: 13
      attack: 3
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 11
      security: 1
      registry_set: 1
      sigma_alert: 3
