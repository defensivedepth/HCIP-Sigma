name: Network Reconnaissance Activity
id: 4e600455-ba3c-46ff-8b86-f3e11638bdff
description: |
  Detects nslookup being used to resolve the DNS SRV record _ldap._tcp.dc._msdcs.<domain>, a query that enumerates a domain's domain controllers. The rule fires when a single command line contains both 'nslookup' and the LDAP DC SRV string. Administrators and AD tooling occasionally run this lookup for diagnostics, but it is also a common early-discovery step after a host is compromised, used to locate directory and authentication infrastructure before account enumeration or lateral movement. Investigation should confirm the exact command, determine which process and user launched it, and check whether other reconnaissance or follow-on directory/authentication traffic occurred on the host.
type: detection
detection_id: e6313acd-208c-44fc-a0ff-db85d572e90e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain both nslookup and the LDAP domain-controller SRV string _ldap._tcp.dc._msdcs.?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line. The rule fires only when both 'nslookup' and the '_ldap._tcp.dc._msdcs.' SRV-lookup string are present in the same command, so verify both appear and note the domain name that follows the SRV string.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this nslookup command do? Decode the full command line and identify the targeted domain and query options.
  context: The full command reveals the operation. nslookup against _ldap._tcp.dc._msdcs.<domain> returns the SRV records for that domain's domain controllers. Note the domain that follows the SRV string, and decode any options such as -querytype=ALL or -timeout that broaden or speed the lookup. The host scope returns every matching invocation in the window, so a sequence of related lookups appears together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
      - ParentImage
- source: attack:T1087.network_indicators
  question: After the SRV-record lookup, did this host connect to directory or authentication services on the resolved domain controllers?
  context: Resolving _ldap._tcp.dc._msdcs.<domain> returns the addresses of domain controllers. A discovery lookup that is part of an operation is often followed by connections to those controllers on LDAP (389/636), Global Catalog (3268/3269), Kerberos (88), or SMB (445). Follow-on traffic to newly resolved controllers shifts the disposition from idle diagnostics toward active enumeration or lateral movement; no follow-on traffic is consistent with a one-off lookup.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        DestinationPort:
        - 389
        - 636
        - 3268
        - 3269
        - 88
        - 445
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned this nslookup command, and is that parent chain normal for this host?
  context: Discovery commands run from cmd.exe under explorer.exe (an interactive operator) or from a known automation script are routine. The same lookup spawned by an Office application, a script interpreter (wscript.exe, cscript.exe, mshta.exe), wmiprvse.exe (WMI remote execution), or wsmprovhost.exe (WinRM remote execution) indicates automated post-exploitation enumeration rather than an administrator at a console.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user expected to run domain-controller discovery commands?
  context: Network administrators and IT staff legitimately run DNS and directory lookups for troubleshooting. Standard users and most service accounts rarely query _ldap._tcp.dc._msdcs. directly. The user's role and typical behavior determine whether this lookup is routine diagnostics or anomalous enumeration.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: A single DC lookup may be legitimate troubleshooting, but multiple reconnaissance commands in rapid succession -- whoami, net user, nltest, systeminfo, arp, netstat -- is a signature pattern of post-compromise enumeration. Three or more distinct recon binaries within fifteen minutes typically indicates automated reconnaissance before the next attack stage.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline
  question: Has this host run the nslookup DC SRV-record lookup before, and how often?
  context: A firing pattern that has never been seen on a host is more suspicious than one that recurs regularly. Zero or very low historical counts over ninety days indicate first-time or rare execution; a steady recurring count points to a scheduled diagnostic or monitoring job.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this lookup pattern, what parents and users drove it, and does the current alert fit that pattern?
  context: Even routine diagnostics leave a consistent shape. This query mirrors the rule's selection scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern has historically run here. A new parent process, an unexpected user account, or an unusual command shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally perform domain-controller discovery activity?
  context: Compare this user's current action against their historical behavior. A user who has never queried _ldap._tcp.dc._msdcs. is more suspicious than one who does so regularly as part of their role. User role and typical toolset should align with directory reconnaissance.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-process and lookup-pattern combination historically routine on this host or across the environment?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The meaningful pivot is the launch mechanism: which parent process spawned this lookup, and whether that pairing is historically common. A new or rare parent for this firing pattern is the deviation worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or enumeration spanning multiple endpoints. If the same user triggered similar activity on several hosts, the scope of the operation may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-process and lookup-pattern fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry noise, because every host runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism -- parent process plus the DC lookup pattern -- fired on other hosts in the alert window. Multiple hosts point to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led to this lookup, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, or persistence mechanisms running near the same time.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.scope
  question: Have other hosts fired this lookup pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: This query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of the firing pattern rather than generic nslookup usage. Many hosts firing the same lookup points to scripted deployment or monitoring automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - nslookup
        - _ldap._tcp.dc._msdcs.
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, initial execution, or credential access -- may reveal the attack stages that preceded this lookup and help reconstruct the timeline. Discovery commonly follows initial access and precedes lateral movement.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1087
      name: Account Discovery
      role: primary
    - id: T1082
      name: System Information Discovery
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: dc_srv_lookup
      description: "nslookup resolves _ldap._tcp.dc._msdcs.<domain> to enumerate the domain's domain controllers"
      mapped_to: Q2
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: invocation_context
      description: "The lookup is launched by a parent process under a user/service account"
      mapped_to: Q4
      source: template:discovery_recon.discovery_parent_chain
    - stage: 3
      label: follow_on_directory_access
      description: "Discovery is followed by connections to the resolved directory/authentication services"
      mapped_to: Q3
      source: attack:T1087.network_indicators
  dimension_coverage:
    - dimension: command_analysis
      technique: T1087
      covered_by: Q2
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: execution_context
      technique: T1087
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: network_indicators
      technique: T1087
      covered_by: Q3
      source: attack:T1087.network_indicators
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1087
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1087
      covered_by: Q6
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1087
      covered_by: Q2
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1087
      covered_by: Q5
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "net user, Get-ADUser, dsquery (T1087 enumeration variants)"
      disposition: context
      target_question: Q6
      notes: "Discovery-burst query already enumerates native recon binaries (net.exe, nltest.exe, dsquery.exe, etc.); AD PowerShell cmdlet variants noted as additional indicators in burst context."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "short burst of enumeration commands indicates automation"
      disposition: query_injection
      target_question: Q6
      notes: "Burst detection scopes a +/-15m window on the host with the recon-binary selection; 3+ distinct binaries flags automation."
    - element: UserContext
      dimension: user_context
      specific_values: "restrict to non-admin / unexpected users"
      disposition: context
      target_question: Q5
      notes: "discovery_user_context prompts role check; non-admin execution of DC SRV lookup is the anomaly."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "parent-process analysis covered by discovery_recon.discovery_parent_chain (technique-set version is more specific to discovery invocation)"
      kept: discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.clustering
      reason: "broad host-activity context covered by cue_host_context.clustering, which projects Image+ParentImage rather than Image alone"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.follow_on
      reason: "nslookup is a leaf process and does not spawn meaningful children; follow-on investigative weight is the network/directory pivot, covered by attack:T1087.network_indicators"
      kept: attack:T1087.network_indicators
    - dropped: universal.host_user_context
      reason: "overlaps discovery_recon.discovery_user_context; per template notes prefer the discovery-specific version"
      kept: discovery_recon.discovery_user_context
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair -- non-deduplicable"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair -- non-deduplicable"
      kept: both
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This is a discovery rule, not execution/credential-access; tamper-as-precursor is not the live disposition question. cue_host_context.precursor (-6h sigma alerts) already surfaces any prior tamper detections on the host. Dropped to stay within the question budget."
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - discovery_recon
  generation_stats:
    total_questions: 15
    source_counts:
      template: 14
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      network_connection: 1
      sigma_alert: 1
