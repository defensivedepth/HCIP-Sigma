name: Suspicious PowerShell Download and Execute Pattern
id: 57c70572-0b92-48be-8e82-ba851f326ce5
description: |
  Detects PowerShell command lines containing in-memory download-and-execute cradles -- IEX of a Net.WebClient DownloadString, or a DownloadFile call -- that pull remote script or payload content and run it without first writing a self-contained file to disk. Legitimately, some software installers and packaging tools fetch packages from remote systems and execute them this way. Maliciously, the same one-liner is the canonical loader for offensive PowerShell toolkits and second-stage payloads, often run directly in memory to limit on-disk evidence. The investigation should confirm the exact cradle that fired, identify the remote source and any payload written to disk, establish the launch context (parent process and user), and look for surrounding tampering, bypass, or follow-on activity.
type: detection
detection_id: e6c54d94-498c-4562-a37c-b469d8e9a275
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-06'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain the PowerShell download-and-execute cradle this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line against the rule's criteria -- an IEX of a Net.WebClient DownloadString, or a System.Net.WebClient DownloadFile call. Note which form fired, because an IEX cradle runs the fetched content in memory while DownloadFile writes a payload to disk first, and that distinction drives whether on-disk follow-up questions apply.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run command-line PowerShell downloaders that fetch and execute remote content?
  context: Legitimate administrative activity, software installers, and packaging tools can trigger this detection. Knowing whether the user role and host function are consistent with running PowerShell download cradles is often the fastest path to separating routine operations from operator activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:powershell.encoded_command_decode
  question: What remote source does the cradle fetch, and what does the fetched content do once executed?
  context: This cradle is a plain-text download pattern, so the command line is already readable -- the analytical task is to identify the URL being fetched and what runs after it loads. For an IEX of a Net.WebClient DownloadString, the fetched script executes directly in memory; for a DownloadFile call, note both the source URL and the local destination path. Check the reputation of the source URL and determine what cmdlet or payload the cradle invokes after the download.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_artifact.mechanism_decode
  question: If a payload was written to disk, what is in it -- is it obfuscated, encoded, or a known offensive script?
  context: When the cradle uses DownloadFile or the in-memory script drops a secondary file, read and analyze that file's actual content. Check for base64 encoding, obfuscated strings, embedded URLs or IP addresses, PowerShell commands, and known offensive-tool string patterns. Content analysis answers what is in the file, which is distinct from a hash reputation lookup that only answers whether the file is already known.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: The parent process reveals how the cradle was invoked. Legitimate usage often has predictable parents -- explorer.exe for interactive use, an installer or packaging tool for software deployment. Unusual parents such as office applications, mshta.exe, wscript.exe, or a web-server process point to macro execution, script-host abuse, or post-exploitation rather than routine administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script content?
  context: Script block logging (Event ID 4104) records the deobfuscated script content PowerShell actually executed, including the content pulled by an in-memory download cradle that never touched disk. It also exposes very long script blocks that a tuned length threshold might otherwise suppress, so it is the ground-truth source for what ran. If PowerShell operational logs are not ingested in this environment, treat the absence of results as a visibility gap rather than evidence that nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened before this execution?
  context: Operators frequently disable or bypass PowerShell controls before running a download cradle. Common precursors include AMSI bypass via in-memory patching, execution-policy changes such as an ExecutionPolicy Bypass or -ep bypass flag, and PowerShell version-2 downgrade to evade logging. Finding these events shortly before the cradle strengthens the case for malicious activity.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:download_tool.download_success_verification
  question: Was a payload actually written to disk, or did the download fail or stay in memory?
  context: A download command does not guarantee the file was retrieved -- network errors, blocked URLs, or security controls may have stopped the transfer, and an IEX cradle may execute entirely in memory with no file written. Confirming whether a file was created at the destination path establishes whether the activity progressed past delivery.
  range: +15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:download_tool.payload_analysis
  question: What is the hash of any downloaded payload, and does threat intelligence already know it?
  context: If a downloaded file is still on disk, submitting its hash to threat intelligence services or detonating it in a sandbox reveals whether it is a known malicious payload, a legitimate tool, or something novel that needs deeper analysis. This reputation check answers whether the file is already known, which complements reading the file's content directly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:download_tool.directory_activity
  question: What other file activity occurred in the download target directory?
  context: Download cradles often stage multiple files in the same directory, or the first file triggers further writes through extraction, decompression, or additional downloads. Examining directory-level file activity reveals the broader scope of the delivery operation beyond the single payload named on the command line.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains|expand: '%file_dirs%'
      filter_self:
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection and not filter_self
    fields:
      - TargetFilename
      - Image
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - IEX ((New-Object Net.WebClient).DownloadString
        - IEX (New-Object Net.WebClient).DownloadString
        - IEX((New-Object Net.WebClient).DownloadString
        - IEX(New-Object Net.WebClient).DownloadString
        - ' -command (New-Object System.Net.WebClient).DownloadFile('
        - ' -c (New-Object System.Net.WebClient).DownloadFile('
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - IEX ((New-Object Net.WebClient).DownloadString
        - IEX (New-Object Net.WebClient).DownloadString
        - IEX((New-Object Net.WebClient).DownloadString
        - IEX(New-Object Net.WebClient).DownloadString
        - ' -command (New-Object System.Net.WebClient).DownloadFile('
        - ' -c (New-Object System.Net.WebClient).DownloadFile('
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts in the alert window reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar download or execution activity on other hosts, the scope of the operation may be larger than this single alert.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - IEX ((New-Object Net.WebClient).DownloadString
        - IEX (New-Object Net.WebClient).DownloadString
        - IEX((New-Object Net.WebClient).DownloadString
        - IEX(New-Object Net.WebClient).DownloadString
        - ' -command (New-Object System.Net.WebClient).DownloadFile('
        - ' -c (New-Object System.Net.WebClient).DownloadFile('
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host -- defense tampering, reconnaissance, or credential access -- may reveal the attack stages that preceded the download cradle and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: attack:T1059.001.time_correlation
  question: Did this cradle run inside a normal working or maintenance window for this host?
  context: Legitimate installer and packaging activity tends to cluster in business hours or scheduled maintenance windows, while hands-on-keyboard operator activity often lands off-hours. Comparing when the cradle fired against the host's normal PowerShell activity pattern helps calibrate suspicion -- an off-hours fetch-and-execute on a host that is otherwise quiet at that time is harder to explain as routine.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        CommandLine|contains:
          - IEX ((New-Object Net.WebClient).DownloadString
          - IEX (New-Object Net.WebClient).DownloadString
          - IEX((New-Object Net.WebClient).DownloadString
          - IEX(New-Object Net.WebClient).DownloadString
          - ' -command (New-Object System.Net.WebClient).DownloadFile('
          - ' -c (New-Object System.Net.WebClient).DownloadFile('
      condition: selection
    fields:
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: powershell_invocation
      description: "PowerShell is launched with a download-and-execute cradle on the command line"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_analysis
      description: "Decode the cradle: identify the remote source URL and what the fetched content executes"
      mapped_to: Q3
      source: template:powershell.encoded_command_decode
    - stage: 3
      label: remote_fetch_and_payload
      description: "Network transfer of remote script/payload and any file written to disk"
      mapped_to: Q8
      source: template:download_tool.download_success_verification
    - stage: 4
      label: follow_on_execution
      description: "What the cradle invokes after load (in-memory execution, child processes, dropped payload)"
      mapped_to: Q3
      source: template:powershell.encoded_command_decode
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q3
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: attack:T1059.001.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q6
      source: template:powershell.script_block_logging
      coverage_type: partial
    - dimension: user_context
      technique: T1059
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop (regex for encoded/obfuscated/hidden args)"
      disposition: context
      target_question: Q3
      notes: "Rule fires on plain-text cradle, not on -enc; encoded_command_decode redirects to URL/payload analysis per template notes. Bypass-flag indicators (-ep bypass, -Version 2) injected into the powershell_security_bypass query (Q7)."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "Excel, WinWord, mshta spawning PowerShell"
      disposition: context
      target_question: Q5
      notes: "Abnormal-parent examples described in Q5 context; query is entity-scoped via ParentProcessGuid so the parent is returned directly."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "off-hours / non-maintenance windows (rationale, no fixed values)"
      disposition: new_question
      target_question: Q16
      notes: "Drove the attack:T1059.001.time_correlation question on working/maintenance-window timing."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rare/never-before-seen .NET assemblies (rationale only)"
      disposition: context
      target_question: Q6
      notes: "image_load logsource not queried (see hint_dispositions); script-block content (Q6) is the available evidence for in-memory .NET loading via process_creation/powershell logs."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "Event ID 4104 script-block length threshold (rationale only)"
      disposition: context
      target_question: Q6
      notes: "Q6 context notes that long script blocks a tuned threshold might suppress are the ground-truth source; no fixed threshold value to inject."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "Phase-2 analysis subsumed by the more specific powershell.encoded_command_decode (technique set wins over cue); both are ProcessGuid-class decode of the same triggering command, and the cue version scopes on Image|endswith on a CommandLine-only rule where powershell.exe host-scope would be noisy."
      kept: powershell.encoded_command_decode
    - dropped: cue_artifact.artifact_verification
      reason: "Hash/reputation lookup is the same investigation step as the more specific download_tool.payload_analysis (technique set wins over cue within-phase); cue_artifact.mechanism_decode (content analysis) is kept as the distinct angle."
      kept: download_tool.payload_analysis
    - dropped: universal.evidence_preservation
      reason: "Identical file-on-disk intent and query as download_tool.download_success_verification (same host+%file_paths% file_event scope); the download_tool version is more specific to the delivery stage."
      kept: download_tool.download_success_verification
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q11 user-pivot, Q12 parent+binary substitute)."
      kept: both
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q13 user-pivot, Q14 parent+binary substitute)."
      kept: both
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "Host historical-frequency baseline of the binary; the rule-pattern-specific user/parent baselines (cue_user_actor.baseline + _svc) and the time-window question cover the historical-deviation angle without an Image-scoped binary count that is noisy for powershell.exe."
    - template: cue_process_tool.baseline_deviation
      reason: "Reflected host-pattern baseline; coverage of how the rule's firing pattern has historically run on this host overlaps the parent+binary substitute baseline (Q12) and the time-correlation question (Q16); dropped to stay within the 18-question cap without losing a dimension."
    - template: cue_process_tool.follow_on
      reason: "Child processes spawned by the binary; for an in-memory IEX cradle the meaningful follow-on is what the script invokes (covered by encoded_command_decode Q3) and host-window activity (precursor Q15), so a separate ParentImage|endswith follow-on adds little."
    - template: cue_process_tool.clustering
      reason: "Host-window other-tools clustering overlaps cue_host_context coverage and the user clustering questions; dropped to stay within the cap."
    - template: cue_process_tool.scope
      reason: "Reflected fleet-pattern cross-host count; the parent+binary clustering substitute (Q14) already answers coordinated-rollout-vs-isolated across hosts in the window; dropped to stay within the 18-question cap."
    - template: cue_artifact.baseline
      reason: "File-seen-before baseline; the payload's novelty is established by hash reputation (Q9) and content analysis (Q4), so a separate 90d file_event baseline adds little for a download-cradle rule."
    - template: cue_host_context.clustering
      reason: "Host-window aggregated activity overlaps cue_host_context.precursor (Q15, prior alerts) and the user clustering questions; dropped to stay within the cap."
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1059.001) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule fires on the plain-text download-and-execute cradle on the
        powershell.exe command line, so the load-bearing evidence is the
        command line and the deobfuscated script content, not assemblies
        loaded by the process. Q3 (encoded_command_decode) and Q6
        (script_block_logging, EventID 4104) capture the in-memory
        fetch-and-execute via process_creation and PowerShell-log evidence
        sufficiently for triage; image_load (Event 7) on powershell.exe
        returns high-volume .NET-assembly noise without precision for which
        load corresponds to the cradle. No compensating
        attack:T1059.001.image_load question authored.
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "Keyword 'ransomware' appears only in a reference write-up, not in the rule's detection logic; the rule fires on a generic download cradle (an execution primitive), so ransomware-impact questions (encryption, shadow-copy deletion, ransom notes) would not match what this rule detects."
      - id: network_scope
        confidence: low
        reason: "Same incidental 'ransomware' keyword origin; the rule has no network_connection or DestinationIp logic, and cross-host scope is already covered by the user/parent clustering substitutes — a network-scope set would not align with the process_creation, CommandLine-only detection."
  technique_sets_applied:
    - universal
    - powershell
    - download_tool
  generation_stats:
    total_questions: 17
    source_counts:
      template: 16
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 11
      file_event: 4
      powershell: 1
      sigma_alert: 1
