name: WhoAmI as Parameter
id: 5d6f0b3e-7c4a-4f2a-9c1e-3a8e2b7d4f10
description: |
  Detects a process command line that contains the substring ".exe whoami" -- that is, an executable invoked with whoami passed directly as a parameter rather than whoami.exe being launched on its own. This pattern appears in token-impersonation exploit tooling, where an exploit binary runs whoami immediately after impersonating a privileged token to confirm the new identity, and in wrapper or launcher commands that chain user-discovery into a single line. Legitimate scripts and management tools occasionally embed whoami this way, so the investigation should determine which executable ran the command, what identity it resolved, what spawned it, and whether the activity sits inside a broader burst of enumeration on the host.
type: detection
detection_id: e9142d84-fbe0-401d-ac50-3e519fb00c89
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain an executable followed directly by the whoami parameter, as this rule detects?
  context: >-
    Confirm the alert is a true positive before investing investigation time. The rule fires when a command line contains
    the substring ".exe whoami" -- an executable invoked with whoami passed as a parameter, rather than whoami.exe running
    standalone. Read the full command line and identify which executable carried the whoami argument and how the rest of
    the line is structured.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: What was the full command, which executable carried the whoami argument, and what else did the line do?
  context: >-
    The full command line reveals the operation. Identify the executable that ran whoami as a parameter, any flags or
    chained subcommands on the same line (output redirection, additional enumeration, a follow-on command), and whether
    the executable is a known tool or an unfamiliar binary. Token-impersonation tooling typically runs whoami right after
    impersonating a privileged identity to confirm the switch succeeded.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - Image
      - CurrentDirectory
- source: template:discovery_recon.discovery_user_context
  question: Is this user and host expected to run user-identity discovery commands?
  context: >-
    Administrators and IT staff legitimately check identity during troubleshooting, and some management scripts embed
    whoami. Standard users and most servers rarely need to resolve their own token this way, and an executable passing
    whoami as a parameter is more unusual than an interactive whoami. The user's role and the host's function determine
    whether this is routine or anomalous.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned the command that ran whoami as a parameter, and is that parent chain expected?
  context: >-
    The parent process shows how the command was initiated and is the key signal for whether enumeration originated from
    an interactive shell, a service, or a non-interactive launcher. Discovery commands spawned by script interpreters
    (wscript.exe, cscript.exe, mshta.exe), Office applications, wmiprvse.exe, or wsmprovhost.exe point to automated or
    remote post-exploitation; a parent that is itself an unfamiliar executable matching the alerting command is the case
    that warrants the most weight.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: >-
    A single identity check may be benign, but multiple enumeration commands in rapid succession -- whoami, ipconfig,
    net user, systeminfo, nltest -- is a signature of post-compromise enumeration. An executable invoking whoami alongside
    a burst of native recon binaries strengthens the case that an operator is surveying the host before the next stage.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    Even routine tooling can embed whoami. The query mirrors the rule's selection scoped to this host, so the resulting
    parent and user distribution reflects how the ".exe whoami" pattern has historically run here -- not generic whoami
    usage. A new parent process, an unexpected user account, or an unfamiliar executable carrying the argument is the
    deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: .exe whoami
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the cross-host distribution look like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of the ".exe whoami"
    pattern. Many hosts firing the same pattern points to a script or management tool that embeds whoami; one host or a
    small unrelated set points to manual operator activity. Use the host distribution to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: .exe whoami
      condition: selection
    fields:
    - host.name
- source: template:cue_process_tool.follow_on
  question: What child processes did the command that ran whoami spawn afterward?
  context: >-
    The processes spawned by the alerting command reveal its actual impact. A line that resolves identity and then chains
    into credential access, lateral movement, or further enumeration is far more concerning than an isolated identity
    check. Walk the children of the alerting process to see whether discovery led directly to a follow-on action.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: .exe whoami
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: .exe whoami
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals whether the identity-discovery activity is isolated or part of
    movement across multiple endpoints. The same user driving similar activity on several hosts widens the potential
    scope of compromise.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains: .exe whoami
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Alerts rarely occur in isolation. Examining the activity surrounding the alert reveals what led to this event and what
    followed, and whether it is part of a larger sequence. Look for reconnaissance, privilege escalation, or persistence
    mechanisms appearing in the same window.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Earlier detections on the same host -- defense tampering, exploitation, credential access -- may reveal the attack
    stages that preceded this identity check and help reconstruct the timeline. Token-impersonation tooling that runs
    whoami to confirm a privilege switch is frequently preceded by an exploitation or service-abuse alert.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1033
      name: System Owner/User Discovery
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: enumeration_command_executed
      description: "An executable runs whoami as a parameter to enumerate the current user/token identity"
      mapped_to: Q2
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: enumeration_origin
      description: "Identify whether enumeration originated from an interactive shell, service, or non-interactive launcher (parent context)"
      mapped_to: Q4
      source: template:discovery_recon.discovery_parent_chain
    - stage: 3
      label: enumeration_clustering
      description: "Detect co-occurring native discovery commands (whoami + lateral-movement enumeration) in a tight window"
      mapped_to: Q5
      source: template:discovery_recon.discovery_burst_detection
  dimension_coverage:
    - dimension: command_analysis
      technique: T1033
      covered_by: Q2
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1033
      covered_by: Q5
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1033
      covered_by: Q4
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1033
      covered_by: Q3
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessContext
      dimension: tuning_parameter
      specific_values: "rationale only -- non-interactive shell or system service origin"
      disposition: context
      target_question: Q4
      notes: "Parent-origin guidance carried in discovery_parent_chain context; ParentImage/ParentCommandLine projected from the alerting event"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only -- temporal grouping of enumeration + lateral movement"
      disposition: context
      target_question: Q5
      notes: "Burst framing captured in discovery_burst_detection +/-15m; threshold not directly queryable"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only -- unexpected users / service accounts issuing enumeration"
      disposition: context
      target_question: Q3
      notes: "User-context scoping captured in discovery_user_context and cue_user_actor.baseline"
  dedup_decisions:
    - dropped: universal.host_user_context
      reason: "discovery_recon.discovery_user_context is more specific to discovery context and the template's notes_to_llm directs preferring it; merged to avoid a duplicate ProcessGuid->User question"
      kept: discovery_recon.discovery_user_context
    - dropped: cue_process_tool.process_chain
      reason: "discovery_recon.discovery_parent_chain (must_include) covers the parent-of-command angle more specifically for a discovery rule"
      kept: discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.baseline
      reason: "host historical frequency for the rule pattern is covered by cue_process_tool.baseline_deviation (host-scoped, filter-correct); user-side frequency covered by cue_user_actor.baseline. Template's %Image% scope is unusable -- rule has no Image/primary_binary"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_process_tool.clustering
      reason: "surrounding-tool activity intent already carried by cue_host_context.clustering and discovery_recon.discovery_burst_detection"
      kept: cue_host_context.clustering
    - dropped: cue_user_actor pair (baseline + baseline_svc), (clustering + clustering_svc)
      reason: "user_actor pair -- non-deduplicable; both variants emitted as Q9/Q10 and Q11/Q12"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc + cue_user_actor.clustering + cue_user_actor.clustering_svc
  skipped_templates:
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This is a discovery rule (T1033), not an execution/credential-access/impact rule, so tamper-as-precursor is not the live disposition question; the template's own notes_to_llm marks it safe to drop when the rule is not execution/credential-centric and cue_host_context.precursor is present (it is, as the prior-alert context question), which already carries any prior tamper signal on the host"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - discovery_recon
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 14
    source_counts:
      template: 14
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 1
      analysis: 1
      context: 12
    logsource_diversity:
      process_creation: 13
      sigma_alert: 1
