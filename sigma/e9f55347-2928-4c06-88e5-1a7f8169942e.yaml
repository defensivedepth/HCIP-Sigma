name: Invoke-Obfuscation VAR++ LAUNCHER OBFUSCATION
id: c3e4f541-edf8-4298-9358-5ef819514f8b
description: |
  Detects a cmd.exe command line that carries the signature of the Invoke-Obfuscation VAR++ launcher
  transform — the tokens &&set, cmd, /c, and -f appearing together alongside PowerShell format-string
  index markers ({0} through {5}). The transform builds a PowerShell expression at runtime by setting
  environment variables and reassembling a reordered format string, hiding the real command from casual
  inspection until it is executed. This obfuscation is almost never produced by legitimate scripting, so
  the investigation should reconstruct the format string into the actual PowerShell payload, confirm what
  spawned the cmd.exe shell, and determine what executed afterward.
type: detection
detection_id: e9f55347-2928-4c06-88e5-1a7f8169942e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain the &&set, cmd, /c, and -f tokens together with PowerShell format
    markers like {0} through {5}?
  context: Confirm the alert is a true positive by reviewing the exact command line. The rule fires only when
    the &&set, cmd, /c, and -f tokens all appear and at least one numeric format marker ({0}-{5}) is present,
    which together are the runtime-assembly signature of the VAR++ launcher transform. Verify the full command
    before investing further time.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run cmd.exe one-liners that build PowerShell commands from
    environment variables and format strings?
  context: Legitimate administration almost never assembles a PowerShell command at runtime from set commands
    and reordered format strings. Knowing whether the user role and host function include any tooling that
    produces this construction is often the fastest way to separate a true positive from a rare automation
    artifact.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:hacking_tool.authorized_engagement
  question: Is this Invoke-Obfuscation activity part of an authorized penetration test or red team engagement?
  context: The VAR++ launcher transform ships with an offensive PowerShell obfuscation framework used in
    authorized security assessments. Before investing analysis time, check whether a penetration test or red
    team exercise is currently active on this host or network segment. An authorized engagement with matching
    timing resolves the alert without further work.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: Reconstruct the format string and environment-variable assignments. What PowerShell command does
    the launcher actually build and run?
  context: The VAR++ transform stores fragments in environment variables with set, then reassembles them
    through a -f format operation whose {0}-{5} markers reorder the pieces. Resolve each set assignment, apply
    the format reordering, and read the reconstructed command. The decoded result is the real payload — look
    for a download cradle, an in-memory loader, or a reverse shell. Because cmd.exe is too common to scope by
    binary, the lookup is keyed to the alerting event itself.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: After the format string is reassembled, was the resulting PowerShell further encoded or obfuscated,
    and what does the final layer decode to?
  context: The VAR++ launcher is one obfuscation layer; the command it builds is frequently obfuscated again,
    using base64 -EncodedCommand, string concatenation, backtick escaping, or further variable substitution.
    Iterate the decode until the script is readable. The final layer typically reveals the real intent — a
    download cradle, a reverse shell, or in-memory execution of a .NET assembly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:hacking_tool.tool_capability_used
  question: Which Invoke-Obfuscation transform produced this command, and does the reconstructed payload point
    to download, in-memory loading, or recon?
  context: The framework offers many transforms, and the rebuilt command determines the scope of potential
    compromise. Identify the launcher style from the set and format-string construction, then read the
    reconstructed payload to classify the operation — a Net.WebClient or IWR download cradle, a
    Reflection.Assembly load, or a reconnaissance one-liner. The capability invoked drives the required
    response actions.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the deobfuscated script content?
  context: Script block logging (Event ID 4104) records the deobfuscated script content PowerShell actually
    executes, even when the launcher hides it behind environment variables and a reordered format string. This
    is ground truth that bypasses the obfuscation. If PowerShell operational logs are not ingested in this
    environment, treat zero results as a visibility gap rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before the obfuscated command
    ran?
  context: Obfuscated launchers are commonly paired with controls that reduce visibility or restrictions —
    AMSI bypass (patching amsi.dll in memory), execution policy changes, a downgrade to PowerShell v2 which
    lacks modern logging, or Defender preference changes. Finding these precursor events in the hour before the
    launcher strengthens the case for malicious intent.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this cmd.exe shell, and is that parent chain expected?
  context: The parent reveals how the launcher was invoked. Interactive use under explorer.exe or a console
    differs sharply from a spawn by mshta.exe, wscript.exe, rundll32.exe, an Office application, or a web
    server worker process — those parents point to a macro, exploit, or scripted delivery rather than an
    operator at the keyboard.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1027.obfuscation_check
  question: How many format markers and set assignments does the command line carry, and does its length and
    fragmentation look like a generated launcher rather than a hand-typed command?
  context: A VAR++ launcher is long and fragmented — multiple set assignments feeding a -f format operation
    with several reorder markers, well beyond what an operator types by hand. Count the {0}-style markers and
    set statements and gauge the overall length and randomness. A short command with one or two markers is
    more consistent with a benign coincidence than with the generated transform.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1027.time_correlation
  question: In the minutes around the obfuscated command, were encoded, compressed, or encrypted files created
    on this host?
  context: Obfuscated launchers frequently stage a second payload — an encoded blob, a .zip or .7z archive, or
    an .enc file written shortly before or after execution. A new encoded file in the same window ties the
    in-command obfuscation to a file artifact that can be hashed and analyzed. If file telemetry is not
    collected for this host, treat the absence of results as a visibility gap rather than evidence nothing was
    written.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_ext:
        TargetFilename|endswith:
          - '.zip'
          - '.7z'
          - '.gz'
          - '.rar'
          - '.enc'
          - '.b64'
          - '.dat'
      condition: selection and selection_ext
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.follow_on
  question: What child processes did this cmd.exe shell spawn afterward?
  context: The processes spawned after the launcher reveal its actual impact. A reconstructed payload that
    launches powershell.exe, another cmd.exe, certutil.exe, rundll32.exe, or an unexpected binary indicates
    post-execution activity rather than a self-contained command.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run cmd.exe one-liners matching this launcher pattern?
  context: Compare this user's current action against their historical behavior. A user who has never produced
    the VAR++ launcher construction is more suspicious than one for whom it appears repeatedly as part of a
    known routine. The query counts how often this user has triggered the same launcher pattern across all
    hosts; user role and typical toolset should align with the result.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        CommandLine|contains|all:
          - '&&set'
          - cmd
          - /c
          - -f
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is the parent-process and launcher-command pattern historically routine on this host or across the
    environment, or is the parent unusual for it?
  context: When the alert fires under a service principal, user identity does not attribute behavior; the
    meaningful pivot is the launch mechanism. The query counts how often this same parent process has driven
    the launcher pattern over 90 days, grouped by host. A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or operating-system
    automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
          - '&&set'
          - cmd
          - /c
          - -f
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated activity on
    multiple endpoints. If the same user drove similar activity elsewhere in the same window, the scope of the
    incident may be larger than this single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and launcher pattern fire on other hosts in the same window, or is the
    activity isolated to this host?
  context: Under a service principal, asking what else the user did returns agent-telemetry noise because every
    host runs system-owned agents constantly. The meaningful question is whether the same launch mechanism —
    the parent process driving this launcher pattern — fired on other hosts in the alert window. Multiple hosts
    point to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        CommandLine|contains|all:
          - '&&set'
          - cmd
          - /c
          - -f
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this launcher pattern in the past week, and does the cross-host distribution
    look like consistent automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of
    the launcher pattern rather than generic cmd.exe usage. Many hosts firing the same pattern points to
    scripted deployment or vendor automation; one host or a small unrelated set points to manual operator
    activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains|all:
        - '&&set'
        - cmd
        - /c
        - -f
        CommandLine|contains:
        - '{0}'
        - '{1}'
        - '{2}'
        - '{3}'
        - '{4}'
        - '{5}'
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering,
    reconnaissance, or credential access — may reveal the attack stages that preceded the obfuscated launcher
    and help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1027
      name: Obfuscated Files or Information
      role: primary
    - id: T1059.001
      name: PowerShell
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: obfuscated_command_execution
      description: "cmd.exe assembles a PowerShell command at runtime from set assignments and a reordered -f format string (VAR++ launcher transform)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: payload_reconstruction
      description: "The format string and environment variables are resolved into the real PowerShell command, then any further encoding is decoded"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: encoded_file_staging
      description: "Script execution correlates with creation of encoded/compressed/encrypted files (e.g. .zip, .7z, .enc) staged near the obfuscated execution"
      mapped_to: Q11
      source: attack:T1027.time_correlation
    - stage: 4
      label: follow_on_execution
      description: "The reconstructed payload spawns child processes (powershell.exe, certutil.exe, etc.) carrying out the operation"
      mapped_to: Q12
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1027
      covered_by: Q10
      source: attack:T1027.obfuscation_check
      coverage_type: direct
    - dimension: time_correlation
      technique: T1027
      covered_by: Q11
      source: attack:T1027.time_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1027
      covered_by: Q10
      source: attack:T1027.obfuscation_check
      coverage_type: partial
    - dimension: user_context
      technique: T1027
      covered_by: Q13
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q5
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: PayloadEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (tune entropy to distinguish obfuscation from legitimate compression)"
      disposition: context
      target_question: Q10
      notes: "Entropy is not directly queryable from CommandLine; folded into Q10 as length/fragmentation/randomness guidance"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (correlation window between script execution and encoded file creation)"
      disposition: query_injection
      target_question: Q11
      notes: "Realized as the +/-15m range on the encoded-file-creation question"
    - element: SuspiciousParentProcessList
      dimension: tuning_parameter
      specific_values: "rationale only (LOLBins/admin tools misused for obfuscation)"
      disposition: context
      target_question: Q9
      notes: "Abnormal-parent rationale folded into process_chain context (mshta, wscript, rundll32, Office)"
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop and obfuscation patterns"
      disposition: context
      target_question: Q5
      notes: "Encoded/obfuscated argument patterns drive the encoded_command_decode question; KEEP_EXACT template keeps ProcessGuid query, patterns described in context"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (Excel, WinWord, mshta spawning execution)"
      disposition: context
      target_question: Q9
      notes: "Abnormal-parent rationale folded into process_chain context"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only (rare/never-before-seen .NET assemblies)"
      disposition: context
      target_question: Q7
      notes: "Loaded-module visibility is an image_load concern; covered indirectly via script-block ground truth in Q7. See hint_dispositions for the image_load logsource decline."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (4104 script-block length filter)"
      disposition: context
      target_question: Q7
      notes: "Script-block length threshold folded into the script_block_logging question context"
  dedup_decisions:
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q13 user-pivot, Q14 service-principal substitute)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q15 user-pivot, Q16 service-principal substitute)
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "CommandLine-only rule has no Image field; the template's Image|endswith|expand placeholder would bind to cmd.exe and return host-wide cmd.exe noise. The host-frequency baseline this template provides is delivered by cue_process_tool.baseline_deviation, which is itself skipped in favor of the CommandLine-scoped user baselines (Q13/Q14) and the fleet scope question (Q17) that mirror the rule's actual pattern."
    - template: cue_process_tool.baseline_deviation
      reason: "REFLECTED host-pattern template overlaps Q13/Q14 (CommandLine-pattern baselines by user/parent) and Q17 (fleet pattern scope). With both user-actor pairs emitted plus fleet scope, the host-scoped parent/user distribution adds no new dimension and would push past the 18-question cap."
    - template: cue_process_tool.clustering
      reason: "host-wide cmd.exe Image aggregation adds no unique dimension; cue_host_context.precursor (prior alerts) and cue_user_actor.clustering (cross-host user activity) already cover around-time host context."
    - template: cue_host_context.clustering
      reason: "overlaps cue_host_context.precursor and cue_user_actor.clustering for host/around-time context; dropped to stay within the 18-question cap without losing a dimension."
    - template: universal.evidence_preservation
      reason: "applicability is has_file_reference, but file paths are only derived from the obfuscated CommandLine and there is no concrete TargetFilename to bind to %file_paths%. The encoded-file-staging angle is covered by Q11 (attack:T1027.time_correlation), which searches file_event by encoded-archive extension instead of an unresolved placeholder."
    - template: universal.endpoint_protection_health
      reason: "the powershell.powershell_security_bypass question (Q8) already surfaces prior tamper/bypass activity on the host (Defender preference changes, AMSI bypass, execution-policy and downgrade flags) for this execution-centric rule; the generic host-tamper sweep is more specific-superseded here and would push the count over 18."
    - template: hacking_tool.tool_specific_artifacts
      reason: "the template's file_event query uses Image|endswith|expand: '%Image%' which binds to cmd.exe and returns all files cmd.exe touches host-wide; for a CommandLine-only obfuscation rule there is no tool-specific on-disk artifact set to enumerate. Encoded/staged file artifacts are covered by Q11 by extension."
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by powershell.encoded_command_decode (Q5) — the payload the launcher builds is PowerShell, so the more specific PowerShell decode question supersedes the generic encoded-payload one."
  reflection_overrides: []
  hint_dispositions:
    - hint: "File paths derived from CommandLine — use %file_paths% placeholder"
      disposition: declined
      reason: >-
        %file_paths% has nothing to bind to here — the rule's detection is
        CommandLine-only and any file path is buried inside the still-obfuscated
        -f format string, so it is not resolved at alert time into a TargetFilename
        the placeholder could populate. universal.evidence_preservation (which
        consumes %file_paths%) was skipped for the same reason. The encoded-file
        staging angle the hint points at is instead covered by Q11
        (attack:T1027.time_correlation), which searches file_event by encoded /
        compressed / encrypted extension within the execution window rather than
        by an unresolved path placeholder.
    - hint: "logsource 'image_load' (recommended by techniques: T1059.001) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule fires on a cmd.exe command-line signature (set assignments + -f
        format string), not on a powershell.exe process or a .NET-hosted
        invocation, so System.Management.Automation assembly loads are not the
        firing surface. The deobfuscated PowerShell content is recovered with
        higher fidelity from script-block logging (Q7, EventID 4104) and the
        CommandLine reconstruction (Q4), which give the actual script text rather
        than the volume-without-precision signal an image_load query on cmd.exe
        or powershell.exe would return. No compensating attack:T1059.001.image_load
        question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - hacking_tool
    - obfuscation
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 14
    logsource_diversity:
      process_creation: 15
      powershell_script_block: 1
      file_event: 1
      sigma_alert: 1
