name: Potentially Suspicious Execution Of Regasm/Regsvcs With Uncommon Extension
id: 5062344c-f2c3-43f0-91fc-184e963bb64c
description: |
  Detects regasm.exe or regsvcs.exe (the .NET Framework assembly registration
  utilities) executed with a command line that references a file ending in an
  uncommon extension (.dat, .gif, .jpeg, .jpg, .png, .txt). These signed
  Microsoft binaries load a target .NET assembly into their own process and run
  its registration entry point, so an attacker can rename a malicious assembly
  to a non-.dll extension and pass it to regasm/regsvcs to execute code while
  bypassing application allow-listing and extension-based static scanning — the
  file is parsed by managed-assembly header, not by extension. Legitimate
  developer or installer activity registers .dll assemblies, so the combination
  of these binaries with an image/text extension on the command line is the
  pivot. The investigation should confirm what assembly was loaded, what it did
  after loading (registry, child processes, network), and whether the launch
  fits normal activity for the host and user.
type: detection
detection_id: e9f8f8cc-07cc-4e81-b724-f387db9175e4
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show regasm.exe or regsvcs.exe loading a file with an uncommon extension?
  context: >-
    Confirm the alert is a true positive by reviewing the triggering event. The
    rule fires when the image (or original file name) is RegAsm.exe or
    RegSvcs.exe AND the command line references a file ending in .dat, .gif,
    .jpeg, .jpg, .png, or .txt. Note which binary fired and the exact path and
    extension passed to it — a managed assembly renamed to an image or text
    extension is the abuse pattern, while a genuine .dll argument would not have
    triggered this rule.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to register .NET assemblies with regasm.exe or regsvcs.exe?
  context: >-
    Regasm/regsvcs registration is normally seen on developer workstations and
    build or installer hosts, driven by setup programs or SDK tooling. If the
    alerting host is not a development or software-deployment system, or the
    user is not a developer or installer account, the activity is harder to
    explain as routine. Pair this with the asset role and the user's normal
    toolset.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What assembly path and flags did regasm/regsvcs receive, and what does the full command line indicate?
  context: >-
    The command line names the file loaded into the registration utility and
    any switches. Decode the full argument set — the path and extension of the
    target file, and flags such as /u (unregister), /codebase, or /regfile that
    change how the assembly is registered. The /u variant still loads the
    assembly into the process, so an unregister flag does not make the execution
    benign. Scoping to all regasm/regsvcs runs on this host in the window
    captures multi-step registration sequences rather than a single invocation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned regasm/regsvcs and is that parent chain expected?
  context: >-
    The parent process shows how the utility was invoked. Genuine registration
    is usually launched by an installer (setup.exe, msiexec.exe), a build tool,
    or an interactive shell on a developer host. Parents such as Office
    applications, script hosts (wscript.exe, mshta.exe, powershell.exe), or a
    browser or mail-client download path point toward delivery of the renamed
    assembly rather than legitimate component registration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1218.009.image_load
  question: What modules did regasm/regsvcs load into its process, and were any from a user-writable or unusual path?
  context: >-
    After process creation, the abuse step is the target assembly being loaded
    into the signed regasm/regsvcs process. Look for a loaded module whose path
    matches the file named on the command line or that sits in a user-writable
    location (Downloads, %TEMP%, the user profile, OneDrive, or an SMB share),
    and check whether it is unsigned or signed by an unexpected publisher. A
    Microsoft-signed host process loading an unsigned managed module from a user
    directory is the load-event signature of this technique. If image-load
    telemetry is not collected in this environment, treat zero results as a
    visibility gap rather than evidence nothing loaded.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: image_load
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - ImageLoaded
      - Signed
      - Signature
- source: attack:T1218.009.registry_set
  question: Did regasm/regsvcs write COM registration keys under HKCR CLSID or ProgID around the alert time?
  context: >-
    The registration entry point invoked by these utilities normally creates or
    modifies COM class keys under HKCR\CLSID and ProgID entries. Review the keys
    written on this host in the window to see what CLSIDs or ProgIDs were
    registered and whether they point back to the loaded assembly. New or
    unfamiliar CLSID entries tied to a file in a user-writable path indicate the
    registration code ran with attacker-supplied content rather than a sanctioned
    install. If registry telemetry is not collected in this environment, treat
    zero results as a visibility gap.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains:
          - \CLSID\
          - \ProgID
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: attack:T1218.009.network_connection
  question: Did regasm/regsvcs or a process it spawned make outbound network connections after the alert?
  context: >-
    The assembly loaded through regasm/regsvcs commonly retrieves a later stage
    or opens a command-and-control channel. Network activity originating from
    the registration utility itself is unusual — these tools do not normally
    transfer data. Review destinations for external addresses, raw-IP endpoints,
    or non-standard ports, and exclude known enterprise update or telemetry
    destinations. If network telemetry from the host is not collected, treat
    zero results as a visibility gap.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - DestinationHostname
- source: attack:T1218.009.file_event
  question: Around the alert, was a file with one of the flagged extensions written to a user-writable path that matches the loaded assembly?
  context: >-
    The renamed assembly is typically dropped to disk shortly before
    regasm/regsvcs is run against it. Look for a file ending in .dat, .gif,
    .jpeg, .jpg, .png, or .txt written into a user-writable directory
    (Downloads, %TEMP%, the user profile, OneDrive, or an SMB share) whose path
    matches the argument on the command line, and identify the process that
    wrote it. Tying the drop to the executing command and to a delivery process
    (browser, mail client, archiver) reconstructs the file drop to execution
    sequence. The correlation window between drop, execution, and registration
    is typically a few minutes.
  range: +/-10m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - .dat
          - .gif
          - .jpeg
          - .jpg
          - .png
          - .txt
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.baseline
  question: Does this host normally run regasm.exe or regsvcs.exe?
  context: >-
    A registration utility that has never or rarely run on this host is more
    notable than one that runs as part of regular development or software
    deployment. Zero or very low historical counts indicate first-time or rare
    execution and raise the weight of the other findings.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously matched this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    The query mirrors the rule's selection — regasm/regsvcs with one of the
    flagged extensions on the command line — scoped to this host, so the
    resulting parent and user distribution reflects how this specific firing
    pattern has run here before, not generic use of the binary. A new parent
    process, an unexpected user account, or an unusual command-line structure
    relative to that history is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \Regsvcs.exe
        - \Regasm.exe
      - OriginalFileName:
        - RegSvcs.exe
        - RegAsm.exe
      selection_extension:
        CommandLine|contains:
        - .dat
        - .gif
        - .jpeg
        - .jpg
        - .png
        - .txt
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts matched this rule's pattern in the past week, and does the spread look like automation or one-off activity?
  context: >-
    The query mirrors the rule's selection with no host scope, giving a
    fleet-wide count of this firing pattern rather than generic use of the
    binary. Many hosts matching the same pattern points toward scripted
    deployment or vendor automation that renames assemblies; one host or a small
    unrelated set points toward manual operator activity. Use the host
    distribution and the consistency of parent and binary to calibrate the
    response.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \Regsvcs.exe
        - \Regasm.exe
      - OriginalFileName:
        - RegSvcs.exe
        - RegAsm.exe
      selection_extension:
        CommandLine|contains:
        - .dat
        - .gif
        - .jpeg
        - .jpg
        - .png
        - .txt
      condition: all of selection_*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \Regsvcs.exe
        - \Regasm.exe
      - OriginalFileName:
        - RegSvcs.exe
        - RegAsm.exe
      selection_extension:
        CommandLine|contains:
        - .dat
        - .gif
        - .jpeg
        - .jpg
        - .png
        - .txt
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \Regsvcs.exe
        - \Regasm.exe
      - OriginalFileName:
        - RegSvcs.exe
        - RegAsm.exe
      selection_extension:
        CommandLine|contains:
        - .dat
        - .gif
        - .jpeg
        - .jpg
        - .png
        - .txt
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts in the window reveals whether
    the registration is isolated or part of broader movement. The same user
    triggering similar activity on multiple endpoints widens the potential scope
    of the activity.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith:
        - \Regsvcs.exe
        - \Regasm.exe
      - OriginalFileName:
        - RegSvcs.exe
        - RegAsm.exe
      selection_extension:
        CommandLine|contains:
        - .dat
        - .gif
        - .jpeg
        - .jpg
        - .png
        - .txt
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Registration abuse is often a middle stage. Earlier detections on the same
    host — delivery, reconnaissance, defense tampering, or credential access —
    may reveal the steps that preceded the assembly load and help place this
    event on the timeline. The correlation window between a dropped assembly,
    its execution, and the registration writes is typically only a few minutes,
    so cluster events tightly around the alert.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1218.009
      name: Regsvcs/Regasm
      role: primary
    - id: T1218
      name: System Binary Proxy Execution
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: process_creation
      description: Process creation of regsvcs/regasm with a suspicious assembly path / uncommon extension
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: assembly_load
      description: Target assembly / DLL loaded inside the regsvcs/regasm process
      mapped_to: Q5
      source: attack:T1218.009.image_load
    - stage: 3
      label: com_registration
      description: Registry writes to HKCR\CLSID / ProgID during COM registration
      mapped_to: Q6
      source: attack:T1218.009.registry_set
    - stage: 4
      label: follow_on_activity
      description: Optional child process or network activity spawned by the registration code
      mapped_to: Q7
      source: attack:T1218.009.network_connection
  dimension_coverage:
    - dimension: parent_process_analysis
      technique: T1218.009
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1218.009
      covered_by: Q16
      source: template:cue_host_context.precursor
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1218.009
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: command_analysis
      technique: T1218
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: AssemblyPathRegex
      dimension: tuning_parameter
      specific_values: "%TEMP%, Downloads, OneDrive, SMB shares, user profile"
      disposition: context
      target_question: Q5
      notes: User-writable assembly-path examples injected into the image_load and file_event context as the locations to flag; environment-specific so kept as context not a literal selection.
    - element: SuspiciousFlags
      dimension: tuning_parameter
      specific_values: "/u (/unregister), /codebase, /regfile"
      disposition: context
      target_question: Q3
      notes: Specific flags injected into mechanism_decode context; mechanism_decode is host+binary scoped and returns the full command line, so analyst reads the flags directly without a flattening CommandLine|contains that would change the alert match.
    - element: ParentProcessAllowList
      dimension: parent_process_analysis
      specific_values: "setup.exe, msiexec.exe, Office, script hosts"
      disposition: context
      target_question: Q4
      notes: Legitimate vs. suspicious parents named in process_chain context.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "5-10 min between drop, exec, registry writes, child activity"
      disposition: context
      target_question: Q16
      notes: Few-minutes correlation window stated in precursor and file_event context; a window is not directly queryable.
    - element: SignedToUnsignedTransition
      dimension: tuning_parameter
      specific_values: "Microsoft-signed host loading unsigned assembly/children"
      disposition: query_injection
      target_question: Q5
      notes: Signed/Signature fields projected in image_load query so the analyst can spot the signed-host loads-unsigned-module transition.
    - element: KnownGoodAssemblies
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: Approved-assembly allow-listing is a tuning rationale; reflected as the automation-vs-one-off calibration in scope context.
    - element: RegistryKeyAllowList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: Approved CLSID/ProgID allow-listing is a tuning rationale; reflected as new vs. familiar CLSID framing in registry_set context.
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "URLs, script paths, DLL entrypoints"
      disposition: context
      target_question: Q3
      notes: Suspicious-argument analysis folded into mechanism_decode; query is host+binary scoped returning full CommandLine for manual decode.
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only"
      disposition: context
      target_question: Q4
      notes: T1218 parent-profiling rationale; covered by process_chain.
    - element: SignedBinaryList
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q11
      notes: Signed-binary allow-listing is the baseline this rule already narrows (regasm/regsvcs + uncommon ext); reflected in scope/baseline framing.
    - element: RemoteDomainAllowlist
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: Enterprise-domain suppression named as the exclusion step in network_connection context.
  dedup_decisions:
    - dropped: cue_process_tool.process_chain (vs attack network/registry chain)
      reason: kept — parent cue covers parent_process_analysis dimension; not dropped
      kept: template:cue_process_tool.process_chain
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: user_actor pair — non-deduplicable (user-pivot vs service_principal_substitute, different identity dimensions)
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: user_actor pair — non-deduplicable (user-pivot vs service_principal_substitute, different identity dimensions)
      kept: both
  skipped_templates:
    - template: cue_process_tool.follow_on
      reason: Child-process follow-on is more precisely covered by the T1218.009 chain questions (registry_set Q6, network_connection Q7) which target the specific post-load artifacts of this technique rather than a generic child-process sweep; dropped to stay within the 18-question cap.
    - template: cue_process_tool.clustering
      reason: Generic "what other tools ran on this host" overlaps the host-scoped precursor question (Q16, prior sigma alerts on host) for this investigation; the chain-stage questions already supply the around-the-time host context. Dropped to stay within cap.
    - template: cue_host_context.clustering
      reason: Near-duplicate of cue_process_tool.clustering (host-scoped process aggregation around the alert); only one host-clustering sweep is warranted and precursor (Q16) plus the chain questions cover the around-the-alert window. Dropped to stay within cap.
    - template: cue_user_actor.baseline (clustering layer note)
      reason: not skipped — recorded only to note all cue_user_actor patterns were evaluated; baseline and clustering pairs both included.
    - template: universal.endpoint_protection_health
      reason: This rule fires on a defense-evasion proxy-execution pattern, not a payload-execution or credential-access action where the disposition turns on whether AV/EDR was disabled first. The defense_tampering technique set is not confirmed, the rule is not execution/credential-centric, and cue_host_context.precursor (Q16) already surfaces any prior tamper alerts on the host, so the generic tamper sweep does not earn its slot under the cap.
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
  generation_stats:
    total_questions: 16
    source_counts:
      template: 12
      attack: 4
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 11
      image_load: 1
      registry_set: 1
      network_connection: 1
      file_event: 1
      sigma_alert: 1
