name: Suspicious Execution of Shutdown to Log Out
id: 2185b226-5de7-40c6-9f14-ea5c4fec9170
description: |
  Detects shutdown.exe invoked with the /l flag to log off the current interactive session. Legitimate logoff is
  normally user-initiated through the shell UI or logoff.exe, so a /l invocation from a script or command interpreter
  context is the pattern this rule targets. Forcing a logoff can clear a credential-cached session or trigger
  logon-based persistence (Run/RunOnce keys, ONLOGON scheduled tasks, Winlogon Notify), and the same binary used with
  /r /f or /a appears in impact and anti-recovery activity. The investigation should determine who and what drove the
  logoff, whether the parent chain is expected, and whether the host shows other impact-tactic or persistence activity
  around the same time.
type: detection
detection_id: ec290c06-9b6b-4338-8b6b-095c0f284f10
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show shutdown.exe run with the /l logoff flag?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line
    and process image against the rule's criteria — shutdown.exe with /l in the arguments. Confirm /l is present and is
    not a substring of an unrelated value.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to log off sessions from a command-line context?
  context: Routine logoff is normally driven from the shell UI or logoff.exe, not shutdown /l from a script. Knowing
    whether the user role and host function include scripted session management, kiosk reset, or automation that logs
    users off helps separate routine operations from operator-driven session clearing.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What were the full shutdown.exe arguments, and do they go beyond /l into reboot, force, abort, or remote flags?
  context: The full argument list reveals the actual operation. Decode every flag — /l (logoff, current user only),
    /r (restart), /s (shutdown), /f (force apps closed, no warning), /t (delay seconds), /a (abort a pending shutdown),
    /p (immediate power-off), /m \\target (remote machine), /d (reason code). A bare /l differs sharply from /r /f /t 0
    or a /m remote invocation in intent and impact.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:cue_process_tool.process_chain
  question: What process spawned shutdown.exe, and is that parent chain expected for a logoff?
  context: The parent reveals how the logoff was invoked. A user-driven logoff usually traces to explorer.exe or an
    interactive shell; shutdown /l descending from cmd.exe, powershell.exe, regsvr32.exe, rundll32.exe, wscript.exe, or
    an Office application points to scripted or automated invocation rather than a person choosing to log off.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1529.host_status_correlation
  question: Do host status change records on this host line up in time with the logoff, and do they fall during expected
    hours?
  context: Process execution of shutdown.exe can be correlated with host status change records (event IDs 1074 and 6006)
    that record shutdown, restart, and event-log-service start transitions. A logoff or shutdown with no corresponding
    status record, or one landing outside business hours when the user is not expected to be active, deserves closer
    review. If these Windows system event channels are not ingested in this environment, treat an empty result as a
    visibility gap rather than evidence nothing happened.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: system
    detection:
      selection:
        EventID:
          - 1074
          - 6006
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - host.name
      - EventID
      - User
- source: template:ransomware_impact.recovery_options_assessment
  question: What recovery-inhibiting commands were executed on this host?
  context: 'shutdown with /r /f or /a appears in impact chains where the operator forces a reboot into a pre-staged
    state or aborts a defender-initiated shutdown. Recovery destruction often accompanies it: deleting shadow copies
    (vssadmin, wmic), disabling boot recovery (bcdedit), deleting backups (wbadmin), and disabling the recovery
    environment (reagentc). This query surfaces those commands so the analyst can confirm whether the logoff sits inside
    a broader anti-recovery sequence, then verify host state with vssadmin list shadows, bcdedit /enum, and reagentc
    /info.'
  range: +24h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recovery:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\reagentc.exe'
          - '\diskshadow.exe'
      selection_cmdline:
        CommandLine|contains:
          - 'delete shadows'
          - 'shadowcopy delete'
          - 'recoveryenabled'
          - 'bootstatuspolicy'
          - 'delete catalog'
          - 'delete systemstatebackup'
          - 'boot.ini'
          - 'winre.wim'
          - '/disable'
      condition: selection and (selection_recovery or selection_cmdline)
    fields:
      - Image
      - CommandLine
- source: template:ransomware_impact.destruction_scope_on_host
  question: What other destructive or recovery-inhibiting actions occurred on this host?
  context: 'Impact activity typically chains several destructive actions: shadow copy deletion, recovery disabling,
    security service stopping, file permission changes, and event log clearing. Finding the full set of destructive
    commands around the logoff reveals whether this is an isolated administrative logoff or one step in a destruction
    sequence, and a shared parent process across those commands points to scripted execution.'
  range: +/-30m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_destructive:
        Image|endswith:
          - '\vssadmin.exe'
          - '\wmic.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\cipher.exe'
          - '\fsutil.exe'
          - '\icacls.exe'
          - '\takeown.exe'
          - '\attrib.exe'
          - '\sc.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\powershell.exe'
          - '\taskkill.exe'
      condition: selection and selection_destructive
    fields:
      - Image
      - User
- source: template:cue_process_tool.baseline
  question: Does this host normally run shutdown.exe?
  context: A binary that has never or rarely run on a host is more notable than one that runs regularly. Zero or very low
    historical counts indicate first-time or rare execution of shutdown.exe here, which raises the weight of the
    parent-chain and argument findings.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously run shutdown.exe with /l, what parents and users drove it, and does the current
    alert fit that pattern?
  context: Even routine tools can be abused. The query mirrors the rule's selection scoped to this host, so the
    parent/user distribution reflects how this exact logoff pattern has historically run here rather than generic
    shutdown.exe usage. A new parent process, an unexpected account, or an unusual argument shape is the deviation worth
    chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \shutdown.exe
        CommandLine|contains: /l
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did shutdown.exe spawn afterward?
  context: shutdown.exe itself spawns little under normal use, so any children are worth noting. More importantly, a
    forced logoff can fire logon-triggered persistence on the next sign-in; reviewing what executed in the window after
    the logoff helps catch persistence that activates on session change.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Does this user normally run shutdown.exe or perform scripted session management?
  context: Compare this user's current action against their historical pattern. A user who has never logged off sessions
    from a command line is more notable than one who does so routinely, and the user's role should align with managing
    or resetting sessions. If the alert fired under a service or computer account, the launch-mechanism view carries the
    weight instead of identity.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent-plus-shutdown.exe combination historically routine on this host or environment-wide, or is the
    parent unusual for it?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior — the meaningful
    pivot is the launch mechanism. Which parent process spawned shutdown.exe, and is that pair historically common? A new
    or rare parent for this binary is the deviation worth chasing; a pair already accounting for thousands of prior
    events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or coordinated action across endpoints.
    The same user driving similar logoffs or impact-tactic activity on multiple hosts widens the apparent scope. Under a
    service or computer account this view returns agent-telemetry volume, in which case the launch-mechanism view is the
    meaningful one.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-plus-shutdown.exe combination fire on other hosts in the same window, indicating a
    coordinated rollout rather than isolated activity?
  context: Under a service principal, network-wide user activity returns constant agent telemetry. The meaningful
    question is whether the same launch mechanism — parent process plus shutdown.exe — fired on other hosts in the alert
    window. Multiple hosts in the same window point to scheduled or pushed automation; a single host points to a
    host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the logoff?
  context: 'Alerts rarely occur in isolation. Examining activity around the logoff reveals the immediate context: what
    led up to it, what followed, and whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, persistence installation, or impact-tactic commands clustered in the same window.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before the logoff?
  context: Earlier detections on the same host — defense tampering, reconnaissance, credential access — may reveal the
    stages that preceded the logoff and help reconstruct the timeline. A logoff that follows tampering or anti-recovery
    activity reads very differently from one that stands alone.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:ransomware_impact.network_wide_impact
  question: Is this same logoff detection firing on other hosts in the environment?
  context: Impact-tactic activity is often executed across many hosts in a short span. The same alert firing on multiple
    hosts at once indicates coordinated or pushed execution and determines the scope for containment and response, rather
    than a single host-local action.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.uuid|expand: '%rule.uuid%'
      condition: selection
    fields:
      - host.name
      - rule.name
      - event.severity_label
- source: template:cue_process_tool.scope
  question: Have other hosts run shutdown.exe with /l in the past week, and does the cross-host distribution look like
    automation or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of this logoff
    pattern rather than generic shutdown.exe usage. Many hosts firing the same pattern points to scripted deployment or
    vendor automation; one host or a small unrelated set points to manual operator activity. Use host distribution and
    parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith: \shutdown.exe
        CommandLine|contains: /l
      condition: selection
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1529
      name: System Shutdown/Reboot
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: shutdown_command_execution
      description: Process execution of shutdown.exe with /l to log off the current interactive session.
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: argument_decode
      description: Decode the full shutdown argument set to distinguish logoff from reboot/force/abort/remote intent.
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: host_status_correlation
      description: Correlate shutdown/reboot execution with host status change records (1074, 6006) and expected-hours context.
      mapped_to: Q5
      source: attack:T1529.host_status_correlation
  dimension_coverage:
    - dimension: time_correlation
      technique: T1529
      covered_by: Q5
      source: attack:T1529.host_status_correlation
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1529
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: user_context
      technique: T1529
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: UserContext
      dimension: user_context
      specific_values: rationale only
      disposition: context
      target_question: Q2
      notes: >-
        UserContext is a tuning rationale (does the user have privileges/role to initiate logoff), not a queryable
        value. Surfaced via host_user_context (Q2) and the user-actor baseline (Q11). The security-log correlation
        question (Q5) also notes the absence-of-administrative-context angle from the analytic.
    - element: TimeWindow
      dimension: time_correlation
      specific_values: rationale only
      disposition: context
      target_question: Q5
      notes: >-
        TimeWindow (unexpected shutdowns during business hours warrant scrutiny) is a rationale, not a fixed value.
        Injected into Q5 context (host status records lining up in time, expected-hours check) and reinforced by the
        host-context clustering window (Q15).
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "near-duplicate of cue_host_context.clustering (same-window host activity); host_context version projects Image+ParentImage and is richer"
      kept: cue_host_context.clustering
    - dropped: network_scope.same_action_other_hosts
      reason: "Image-only cross-host count duplicates the rule-pattern fleet view from cue_process_tool.scope and the alert-correlation cross-host view from ransomware_impact.network_wide_impact"
      kept: cue_process_tool.scope
    - dropped: network_scope.actor_activity_network_wide
      reason: "duplicates cue_user_actor.clustering (user activity network-wide); user_actor version is paired with the service-principal substitute"
      kept: cue_user_actor.clustering
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q11 user-pivot, Q12 service-principal substitute)"
      kept: both
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable; both emitted (Q13 user-pivot, Q14 service-principal substitute)"
      kept: both
  skipped_templates:
    - template: ransomware_impact.encryption_or_ransom_indicators
      reason: >-
        Rule fires on shutdown /l (logoff), not file encryption. The encryption/ransom-note sweep would add a fourth
        ransomware-set slot without coverage that the destruction-scope (Q7) and recovery-options (Q6) questions don't
        already provide for this logoff context; dropped to stay within the 18-question cap.
    - template: network_scope.actor_authentication_trail
      reason: >-
        Recommended-priority Security-log auth-trail question. Authentication context for the logoff is better served by
        the host status correlation question (Q5) and the user-pivot clustering (Q13); a separate 4624/4625 query would
        push past the cap without distinct value for a logoff alert.
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - ransomware_impact
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 15
      windows_system: 1
      sigma_alert: 2
