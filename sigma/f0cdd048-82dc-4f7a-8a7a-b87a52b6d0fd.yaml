name: Potential Commandline Obfuscation Using Escape Characters
id: 26b880ef-0d7e-49bf-a2c5-b84525eade5c
description: |
  Detects command lines that obfuscate the literal string "http" using shell escape characters, where caret separators
  (h^t^t^p) or embedded double quotes (h"t"t"p) break up the token so signature-based detections that look for "http" do
  not match. Both forms are interpreted by the command-line parser back into the original characters at runtime, so the
  obfuscated string behaves identically to the plain text while evading naive string matching. The technique is most often
  used to hide download URLs or proxy-execution arguments inside otherwise normal-looking command lines. Read the full
  command line to reconstruct the de-obfuscated string, determine the binary and operation it drives, and check whether the
  surrounding activity looks like staging and payload execution.
type: detection
detection_id: f0cdd048-82dc-4f7a-8a7a-b87a52b6d0fd
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain "http" obfuscated with caret separators or embedded double quotes?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when a command line
    contains either the caret-escaped form h^t^t^p or the quote-broken form h"t"t"p. Read the full command line, confirm one
    of those exact patterns is present, and reconstruct what the obfuscated token resolves to once the escape characters are
    removed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run command lines that wrap URLs or arguments in escape characters?
  context: Caret and quote escaping inside command lines is rare in routine administration; legitimate scripts and installers
    usually write URLs and arguments in plain text. Determine whether the user role and host function plausibly produce escaped
    command lines (for example a developer testing parser behavior or a packaging tool that emits escaped paths) versus an
    ordinary endpoint and account where escaped "http" tokens have no benign explanation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:obfuscation.encoded_payload_decode
  question: Reconstruct the de-obfuscated command line. What URL, binary, and operation does the escaped string resolve to?
  context: The escape characters are the operator's way of hiding the real command from string matching. Remove the caret
    separators (h^t^t^p becomes http) or collapse the embedded double quotes (h"t"t"p becomes http) to recover the literal
    token, then read the rest of the command line in that light. Identify the full URL or argument the "http" fragment belongs
    to, which binary consumes it (a download cradle, a regsvr32 or rundll32 proxy-execution argument, a scripting host), and
    what operation the de-obfuscated command performs. Escaping is frequently combined with other layers such as environment-
    variable substitution or character concatenation, so iterate until the command reads cleanly.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this command, and is that parent chain expected?
  context: The parent process reveals how the obfuscated command was invoked. Escaped command lines launched by explorer.exe
    or an administration shell may be benign tooling, while invocation from Office applications, scripting hosts such as
    wscript.exe, cscript.exe, or mshta.exe, a PowerShell download cradle, wmiprvse.exe (WMI remote execution), or
    wsmprovhost.exe (WinRM) points to a macro or remote operator hiding a URL or proxy-execution argument.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.follow_on
  question: What child processes did this command spawn afterward?
  context: An obfuscated "http" token usually drives a download or proxy-execution step, and the impact shows up in what the
    process spawns next. Children such as cmd.exe, powershell.exe, certutil.exe, regsvr32.exe, rundll32.exe, or a freshly
    written binary indicate the escaped command reached an execution stage rather than failing harmlessly. Scoping to the
    alerting process by its own identifier returns exactly the processes it launched.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this escaped-http pattern, what parents and users drove it, and does the current
    alert fit?
  context: Even legitimate tooling occasionally emits escaped command lines. The query mirrors the rule's selection structure
    scoped to this host, so the resulting parent and user distribution reflects how this specific firing pattern - "http"
    broken up with carets or quotes - has historically run here, not generic command-line activity. A new parent process, an
    unexpected user account, or a first-ever appearance of the pattern is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - h^t^t^p
        - h"t"t"p
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - h^t^t^p
        - h"t"t"p
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - h^t^t^p
        - h"t"t"p
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If
    the same user ran escaped command lines or other tooling on several hosts in this window, the scope of the activity may be
    larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - h^t^t^p
        - h"t"t"p
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining the surrounding activity reveals the immediate context: what led to
    the obfuscated command, what followed, and whether it is part of a larger sequence. Look for file staging, download tools,
    or proxy-execution binaries appearing as novel parent-child pairs at low counts against the high-count background of
    routine processes.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Earlier detections on the same host - initial access, file staging, execution, or other defense-evasion attempts -
    may reveal the attack stages that preceded this obfuscated command and help reconstruct the timeline. Command-line
    obfuscation typically wraps an execution or download step, so prior alerts here help place the escaped command within an
    attack sequence.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
- source: template:universal.evidence_preservation
  question: Did the obfuscated command stage or write a file that is still on disk for analysis?
  context: Deobfuscation and download steps frequently land a payload on disk before execution - a decoded output file or a
    downloaded binary. Operators commonly disguise these with innocuous extensions such as .txt, .cer, .enc, .b64, or .zip in
    user-writable locations. If the de-obfuscated "http" token resolved to a download URL, check whether the fetched file
    still exists so it can be hashed, submitted to a sandbox, or reviewed.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection_self:
        ProcessGuid|expand: '%ProcessGuid%'
      selection_child:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: 1 of selection_*
    fields:
      - TargetFilename
      - Image
- source: attack:T1140.process_access
  question: Did the obfuscated process open a handle into another process's memory for in-memory decoding or injection?
  context: Beyond writing decoded content to disk, deobfuscation and loaders often run in memory - the process reconstructs a
    payload and reads or writes another process's address space rather than dropping a file. A cross-process handle request
    from the alerting process, especially with read/write/execute access into a system or browser process, indicates in-memory
    deobfuscation or injection rather than a benign command line. If process-access telemetry is not collected in this
    environment, treat zero results as a visibility gap rather than evidence nothing happened.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        SourceProcessGUID|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetImage
      - GrantedAccess
      - CallTrace
- source: template:cue_process_tool.scope
  question: Have other hosts fired this escaped-http pattern in the past week, and does the cross-host distribution look like
    consistent automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide count of the
    escaped "http" pattern rather than generic command-line activity. Many hosts firing the same pattern points to scripted
    deployment or a packaging tool that emits escaped command lines; one host or a small unrelated set points to manual
    operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        CommandLine|contains:
        - h^t^t^p
        - h"t"t"p
      condition: selection
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1140
      name: Deobfuscate/Decode Files or Information
      role: primary
  attack_chain_coverage:
    - stage: 1
      label: obfuscated_command_execution
      description: A command line executes with "http" obfuscated via caret separators or embedded double quotes
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: deobfuscation_decode
      description: Remove the escape characters to recover the literal command and identify the URL/binary/operation it drives
      mapped_to: Q3
      source: template:obfuscation.encoded_payload_decode
    - stage: 3
      label: staging_precedes_decode
      description: Deobfuscation typically follows file staging or download within a short window
      mapped_to: Q13
      source: template:universal.evidence_preservation
    - stage: 4
      label: chained_execution
      description: The deobfuscated command chains into script interpreters or further payload execution
      mapped_to: Q5
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1140
      covered_by: Q3
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: execution_context
      technique: T1140
      covered_by: Q5
      source: template:cue_process_tool.follow_on
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1140
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1140
      covered_by: Q11
      source: template:cue_host_context.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1140
      covered_by: Q13
      source: template:universal.evidence_preservation
      coverage_type: partial
    - dimension: user_context
      technique: T1140
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
  mutable_dispositions:
    - element: ToolName
      dimension: tuning_parameter
      specific_values: "rationale only - certutil, powershell, copy, expand, 7zip; varies by environment"
      disposition: context
      target_question: Q3
      notes: "Rule has no Image field (CommandLine-only), so no concrete binary to inject. Q3 (encoded_payload_decode) directs the analyst to identify which binary consumes the de-obfuscated token, and Q5 (follow_on) names the common decode/proxy binaries as expected children."
    - element: FileExtensionFilter
      dimension: tuning_parameter
      specific_values: ".txt, .cer, .enc, .b64, .zip"
      disposition: context
      target_question: Q13
      notes: "Disguised-extension list injected into Q13 (evidence_preservation) context so the analyst knows which staged-file extensions to look for; the query scopes on %file_paths% derived from the de-obfuscated command per the file-path hint."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rule's own pattern: h^t^t^p (caret), h\"t\"t\"p (quote)"
      disposition: query_injection
      target_question: Q6
      notes: "Rule's exact escape-char tokens injected into the non-entity-scoped CommandLine|contains queries (Q6 baseline_deviation, Q7 user baseline, Q8 baseline_svc, Q10 clustering_svc, Q16 scope) so each counts the rule's specific firing pattern, not generic activity. Q3 decode reconstructs the literal command from these tokens."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only - deobfuscation follows staging/download within a short timeframe"
      disposition: context
      target_question: Q11
      notes: "Q11 (host clustering, +/-15m) and Q13 (evidence_preservation, +/-15m) capture the short staging-to-decode window; Q12 (precursor, -6h) carries the prior-stage timeline. No numeric threshold supplied by the analytic."
  dedup_decisions:
    - dropped: cue_process_tool.mechanism_decode
      reason: "Phase 2 analysis is carried by the more specific obfuscation technique-set question (Q3 encoded_payload_decode), which targets escape-char/encoding reconstruction directly; per within-phase dedup the technique set supersedes the generic cue mechanism_decode. The cue's %Image% host-scope query is also non-functional here (rule has no Image)."
      kept: obfuscation.encoded_payload_decode
    - dropped: cue_process_tool.clustering
      reason: "Near-identical host.name +/-15m clustering query to cue_host_context.clustering (Q11); the host_context version projects Image and ParentImage and is retained."
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "Generic per-binary baseline relies on Image|endswith %Image%, but the rule is CommandLine-only with no Image to scope on; the reflected rule-pattern baseline (Q6 baseline_deviation) counts the rule's specific escaped-http pattern on this host and supersedes it."
      kept: cue_process_tool.baseline_deviation
    - dropped: none
      reason: "user_actor pair - non-deduplicable: cue_user_actor.baseline (Q7) + cue_user_actor.baseline_svc (Q8) and cue_user_actor.clustering (Q9) + cue_user_actor.clustering_svc (Q10) all emitted; user-pivot and parent-mechanism substitute pivot on different identity dimensions and degenerate under different conditions."
      kept: cue_user_actor.baseline, cue_user_actor.baseline_svc, cue_user_actor.clustering, cue_user_actor.clustering_svc
  skipped_templates: []
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - obfuscation
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 13
      sigma_alert: 1
      file_event: 1
      process_access: 1
