name: HackTool - Bloodhound/Sharphound Execution
id: b11d3f9d-39c9-4dcd-9315-c93eb7007a03
description: |
  Detects execution of the BloodHound/SharpHound Active Directory collector. The rule fires when EITHER the binary metadata or filename identifies the tool (Product, Description, Company, or Image naming SharpHound/BloodHound/SpecterOps) OR the command line carries a known collection pattern (a -CollectionMethod / --CollectionMethods value, the -JsonFolder + -ZipFileName output pair, the DCOnly + --NoSaveCache stealth pair, or an Invoke-Bloodhound / Get-BloodHoundData cmdlet) — only one branch needs to match.
  SharpHound enumerates domain users, groups, sessions, ACLs, and trust relationships and writes the result to a zip of JSON files for attack-path mapping; it is a legitimate component of authorized assessments and is also a routine precursor to lateral movement and privilege escalation. Determine which branch fired, whether the run is part of a sanctioned engagement, what collection scope was requested, and whether the activity sits inside a broader discovery or hands-on-keyboard sequence.
type: detection
detection_id: f376c8a7-a2d0-4ddc-aa0c-16c17236d962
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does this event match BloodHound/SharpHound either by binary identity or by a collection command line?
  context: >-
    Confirm the alert before investing time. The rule fires if any one branch matches — binary metadata or filename
    (Product, Description, Company, or Image naming SharpHound, BloodHound, or SpecterOps), a single collection flag (a
    -CollectionMethod / --CollectionMethods value, .exe -c All -d, or an Invoke-Bloodhound / Get-BloodHoundData cmdlet), the
    -JsonFolder plus -ZipFileName output pair, or the DCOnly plus --NoSaveCache stealth pair. Note which branch fired. A
    metadata or filename match identifies the tool even when its command line is benign or renamed, while a command-line-only
    match means the binary identity was not confirmed and could be a renamed or repackaged collector — that distinction drives
    how much weight the identity-versus-behavior questions carry downstream.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
      - Company
      - Description
      - Product
- source: template:hacking_tool.authorized_engagement
  question: Is this SharpHound/BloodHound run part of an authorized penetration test or red team engagement?
  context: >-
    Offensive AD collectors are standard tooling in sanctioned assessments. Before deeper analysis, check whether a pentest
    or red team exercise is active on this host or network segment. An authorized engagement whose scope, operator, host, and
    timing match the alert resolves it without further work; absence of any authorization, or a mismatch in host or window,
    keeps the investigation open.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:discovery_recon.discovery_user_context
  question: Is this user and account context expected to run domain enumeration tooling?
  context: >-
    SharpHound must run in the context of a domain user and enumerates directory objects broadly. Administrators, IT staff,
    and security testers may run AD collection legitimately; standard users and most service accounts have no reason to. Note
    whether the executing account is privileged, a help-desk or admin role, a standard user, or a service principal — that
    determines whether the collection is routine or anomalous.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - User
- source: template:hacking_tool.tool_capability_used
  question: What collection scope did this run request?
  context: >-
    SharpHound's collection methods control how aggressive and broad the enumeration is. CollectionMethod All pulls sessions,
    ACLs, group membership, and trusts in one pass; DCOnly with --NoSaveCache and stealth flags is a quieter, lower-footprint
    sweep; --Loop with --Loopduration runs repeated collection over time. The -CollectionMethod, --CollectionMethods, -c, and
    cmdlet arguments in the command line reveal which scope was used, which bounds how much directory data was exposed and
    shapes the response.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.mechanism_decode
  question: Decode the full BloodHound/SharpHound command line — what output paths, domain targets, and stealth options were set?
  context: >-
    The full command line on this host shows the complete operation, which may span more than the single triggering
    invocation. Read the output destination (-JsonFolder, -ZipFileName, --outputdirectory), the targeted domain or domain
    controller (-d, --domain, --domaincontroller), and any stealth or evasion options (--stealth, --randomfilenames,
    --memcache, --zippassword, --throttle, --jitter). These reveal where collected data was written and how much effort went
    into avoiding detection.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: If this fired on the PowerShell ingestor (Invoke-Bloodhound / Get-BloodHoundData), was the command encoded and what does it decode to?
  context: >-
    The PowerShell branch of the tool exposes Invoke-Bloodhound and Get-BloodHoundData, which are frequently launched through
    -EncodedCommand, string concatenation, or download cradles to hide the collection call and its parameters. Base64-encoded
    arguments are the most common form. Decoding shows the actual collection parameters, any in-memory assembly load
    ([Reflection.Assembly]::Load) that runs the collector filelessly, and any remote source the script pulled from.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1087.002.process_identification
  question: Does the binary's signing and metadata confirm it is really SharpHound, or could it be a renamed or repackaged collector?
  context: >-
    The metadata branch fires on Product, Description, or Company strings and the filename branch on Image, so a binary
    renamed away from SharpHound.exe can still match on embedded metadata, and a metadata-stripped copy can still match on a
    command-line pattern. Compare the on-disk name against the embedded Product/Description/Company and the signing chain
    (SpecterOps signs official releases; unsigned or mismatched signers on a SharpHound-named file, or SharpHound metadata
    inside an oddly named binary, point to a repackaged or relocated collector). This resolves whether the file identity and
    its claimed identity agree.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - Product
      - Description
      - Company
- source: template:discovery_recon.discovery_parent_chain
  question: What process spawned this collector, and is that parent chain expected?
  context: >-
    The parent reveals how the collection was launched. An interactive launch from cmd.exe or powershell.exe under
    explorer.exe is consistent with an operator at a console; launches from Office applications, script interpreters
    (wscript.exe, cscript.exe, mshta.exe), wmiprvse.exe (WMI remote execution), or wsmprovhost.exe (WinRM remote execution)
    indicate automated or remote post-exploitation rather than hands-on assessment work.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - ParentCommandLine
- source: template:hacking_tool.tool_specific_artifacts
  question: Did this collector write its output artifacts to disk on the host?
  context: >-
    SharpHound typically writes a zip of JSON files (commonly a timestamped *_BloodHound.zip plus per-object JSON) to the
    output directory unless --nozip or --memcache was used. Locating these artifacts confirms collection completed, shows
    where the data was staged for exfiltration or ingestion, and reveals the collection scope even after the process has
    exited. Absence of files alongside a stealth command line is consistent with an in-memory (--memcache) run.
  range: +1h
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:discovery_recon.discovery_burst_detection
  question: Did other reconnaissance commands run on this host around the same time?
  context: >-
    BloodHound collection rarely stands alone in an intrusion; it is commonly bracketed by native discovery — whoami, net
    user, net group, nltest, dsquery, setspn, adfind — and by AD PowerShell cmdlets like Get-ADUser and Get-ADGroupMember.
    Three or more distinct recon binaries inside a short window points to automated enumeration rather than a single
    troubleshooting command, and frames the collection as one stage of a broader discovery effort.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_recon:
        Image|endswith:
          - '\whoami.exe'
          - '\systeminfo.exe'
          - '\ipconfig.exe'
          - '\net.exe'
          - '\net1.exe'
          - '\nltest.exe'
          - '\dsquery.exe'
          - '\nbtstat.exe'
          - '\arp.exe'
          - '\netstat.exe'
          - '\tasklist.exe'
          - '\qprocess.exe'
          - '\query.exe'
          - '\quser.exe'
          - '\hostname.exe'
          - '\nslookup.exe'
          - '\adfind.exe'
          - '\csvde.exe'
          - '\ldifde.exe'
          - '\dnscmd.exe'
          - '\cmdkey.exe'
          - '\setspn.exe'
      condition: selection and selection_recon
    fields:
      - Image
      - User
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell security controls bypassed or weakened on this host before the collection ran?
  context: >-
    When the collector is launched through PowerShell (Invoke-Bloodhound) or a download cradle, operators frequently weaken
    PowerShell defenses first — AMSI patching, an execution-policy bypass (-ExecutionPolicy Bypass / -ep bypass), or a
    downgrade to PowerShell v2 to evade logging. Finding these precursor events in the hour before the run strengthens the
    case that the collection was deliberate and adversarial rather than an administrator's sanctioned use.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: >-
    Collection rarely occurs in isolation. The activity surrounding it on this host — what launched immediately before, what
    ran after — shows whether this is an isolated run or one step in a sequence that includes credential access, privilege
    escalation, or lateral movement. Aggregating by process and parent collapses routine telemetry into high-count rows so
    novel parent-child pairs stand out at low counts.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously fired this rule's pattern, what parents and users drove it, and does the current alert fit?
  context: >-
    The query mirrors the rule's full selection structure scoped to this host, so the parent and user distribution it returns
    reflects how this specific collection pattern — not generic binary usage — has historically run here. A new parent
    process, an unexpected account, or an unusual command-line shape relative to that history is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Product|contains: SharpHound
      - Description|contains: SharpHound
      - Company|contains:
        - SpecterOps
        - evil corp
      - Image|contains:
        - \Bloodhound.exe
        - \SharpHound.exe
      selection_cli_1:
        CommandLine|contains:
        - ' -CollectionMethod All '
        - ' --CollectionMethods Session '
        - ' --Loop --Loopduration '
        - ' --PortScanTimeout '
        - '.exe -c All -d '
        - Invoke-Bloodhound
        - Get-BloodHoundData
      selection_cli_2:
        CommandLine|contains|all:
        - ' -JsonFolder '
        - ' -ZipFileName '
      selection_cli_3:
        CommandLine|contains|all:
        - ' DCOnly '
        - ' --NoSaveCache '
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run BloodHound/SharpHound or other domain enumeration tooling?
  context: >-
    Compare the executing user's current action against their 90-day history across all hosts. A user who has never run this
    collector is more notable than one who runs it regularly as part of their role. Low or zero historical counts, combined
    with the user's role, indicate first-time or out-of-role use.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent+binary launch mechanism historically routine on this host or across the environment?
  context: >-
    When the alert fires under a service principal (SYSTEM, LOCAL SERVICE, NETWORK SERVICE, or a computer account), user
    identity does not attribute behavior, so the meaningful pivot is the launch mechanism — which parent process spawned this
    collector, and whether that pairing is historically common. A new or rare parent for this binary is the deviation worth
    chasing; a pair already responsible for thousands of prior events is automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the executing user's activity across all hosts in a tight window around the alert surfaces lateral movement or
    parallel collection on other endpoints. The same user driving similar activity on multiple hosts widens the apparent
    scope of the operation.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent+binary combination fire on other hosts in the alert window?
  context: >-
    Under a service principal, asking what the user did network-wide returns broad agent-telemetry noise because every host
    runs SYSTEM-owned agents constantly. The meaningful question is whether the same launch mechanism — parent process plus
    spawned binary — fired on other hosts in the same window. Multiple hosts point to scheduled or pushed automation; a single
    host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_process_tool.scope
  question: Have other hosts fired this rule's pattern in the past week, and does the spread look like automation or one-off activity?
  context: >-
    The query mirrors the rule's full selection structure with no host scope, giving a fleet-wide count of this collection
    pattern rather than generic binary usage. Many hosts firing the same pattern points to scripted deployment or vendor
    automation; one host or a small unrelated set points to manual operator activity. Use the host distribution and
    parent/binary consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Product|contains: SharpHound
      - Description|contains: SharpHound
      - Company|contains:
        - SpecterOps
        - evil corp
      - Image|contains:
        - \Bloodhound.exe
        - \SharpHound.exe
      selection_cli_1:
        CommandLine|contains:
        - ' -CollectionMethod All '
        - ' --CollectionMethods Session '
        - ' --Loop --Loopduration '
        - ' --PortScanTimeout '
        - '.exe -c All -d '
        - Invoke-Bloodhound
        - Get-BloodHoundData
      selection_cli_2:
        CommandLine|contains|all:
        - ' -JsonFolder '
        - ' -ZipFileName '
      selection_cli_3:
        CommandLine|contains|all:
        - ' DCOnly '
        - ' --NoSaveCache '
      condition: 1 of selection_*
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1087.002
      name: Domain Account
      role: primary
    - id: T1087.001
      name: Local Account
      role: secondary
    - id: T1482
      name: Domain Trust Discovery
      role: secondary
    - id: T1069.001
      name: Permission Groups Discovery (Local Groups)
      role: secondary
    - id: T1069.002
      name: Permission Groups Discovery (Domain Groups)
      role: secondary
    - id: T1059.001
      name: PowerShell
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: collector_invocation
      description: "Adversary launches the SharpHound/BloodHound collector via process or PowerShell cmdlet with collection parameters"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: collection_scope_selection
      description: "Collection method / scope flags determine breadth of directory enumeration (accounts, groups, trusts)"
      mapped_to: Q4
      source: template:hacking_tool.tool_capability_used
    - stage: 3
      label: enumeration_execution
      description: "Collector queries DCs for users, groups, sessions, ACLs, and trusts; may be bracketed by native recon"
      mapped_to: Q10
      source: template:discovery_recon.discovery_burst_detection
    - stage: 4
      label: output_staging
      description: "Results written as a zip of JSON files to an output directory for ingestion or exfiltration"
      mapped_to: Q9
      source: template:hacking_tool.tool_specific_artifacts
  dimension_coverage:
    - dimension: command_analysis
      technique: T1087.002
      covered_by: Q5
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1482
      covered_by: Q10
      source: template:discovery_recon.discovery_burst_detection
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1482
      covered_by: Q4
      source: template:hacking_tool.tool_capability_used
      coverage_type: direct
    - dimension: user_context
      technique: T1087.002
      covered_by: Q3
      source: template:discovery_recon.discovery_user_context
      coverage_type: direct
    - dimension: execution_context
      technique: T1087
      covered_by: Q8
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: network_indicators
      technique: T1087
      covered_by: Q9
      source: template:hacking_tool.tool_specific_artifacts
      coverage_type: partial
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q8
      source: template:discovery_recon.discovery_parent_chain
      coverage_type: direct
    - dimension: process_identification
      technique: T1087.002
      covered_by: Q7
      source: attack:T1087.002.process_identification
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-CollectionMethod All, --CollectionMethods Session, DCOnly, --NoSaveCache, -JsonFolder, -ZipFileName, Invoke-Bloodhound, Get-BloodHoundData"
      disposition: context
      target_question: Q4
      notes: "Values are the rule's own selection clauses; surfaced in Q4 collection-scope context and Q5 decode context. The rule-pattern queries (Q13 baseline_deviation, Q17 scope) carry them in their reflected detection blocks verbatim."
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only — non-admin / unexpected user or service principal"
      disposition: new_question
      target_question: Q3
      notes: "Drives discovery_user_context (Q3) and the user_actor pair (Q14/Q15 user-pivot, Q16/Q18 service-principal substitutes)."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only — bursts of enumeration in short succession (5-15m)"
      disposition: context
      target_question: Q10
      notes: "Burst-window framing applied to discovery_burst_detection (+/-15m) and host clustering (+/-15m)."
    - element: SourceHost
      dimension: tuning_parameter
      specific_values: "rationale only — non-DC / non-admin systems where collection is unexpected"
      disposition: context
      target_question: Q3
      notes: "Host expectation folded into user/host expectation framing (Q3) and host baseline_deviation (Q13)."
    - element: ParentImage
      dimension: tuning_parameter
      specific_values: "rationale only — abnormal script hosts / parents (Office, wscript, mshta, wmiprvse, wsmprovhost)"
      disposition: query_injection
      target_question: Q8
      notes: "discovery_parent_chain projects ParentImage/ParentCommandLine for parent legitimacy assessment."
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only — abnormal PowerShell parents"
      disposition: context
      target_question: Q8
      notes: "Same parent assessment via Q8; covered without a separate query."
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc, -nop, -ExecutionPolicy Bypass, -ep bypass, AmsiUtils, -Version 2"
      disposition: query_injection
      target_question: Q11
      notes: "PowerShell bypass indicators carried in powershell_security_bypass selection_bypass; decode angle in Q6."
    - element: API_Name
      dimension: tuning_parameter
      specific_values: "DSEnumerateDomainTrusts (T1482)"
      disposition: context
      target_question: Q4
      notes: "Trust-enumeration API is the analytic's native-tool angle; SharpHound performs trust collection via LDAP, surfaced as collection scope (Q4) rather than a process_access/image_load query — see hint_dispositions for the declined image_load/process_access/security logsource hints."
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only — rare .NET assemblies tied to in-memory execution"
      disposition: context
      target_question: Q6
      notes: "In-memory ([Reflection.Assembly]::Load) angle folded into the PowerShell decode context (Q6)."
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only — 4104 script-block length tuning"
      disposition: context
      target_question: Q6
      notes: "Tuning rationale only; no queryable value."
    - element: ProcessLineageDepth
      dimension: tuning_parameter
      specific_values: "rationale only — parent-child correlation depth"
      disposition: context
      target_question: Q8
      notes: "Lineage assessment via parent chain (Q8); depth is a tuning knob, not a queryable value."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain
      reason: "Parent-process coverage provided by discovery_recon.discovery_parent_chain (Q8), which is more specific to the discovery context and ProcessGuid-scoped; both are within-phase parent questions."
      kept: discovery_recon.discovery_parent_chain
    - dropped: cue_process_tool.follow_on
      reason: "SharpHound is a self-contained collector that does not spawn meaningful child processes; child-process angle adds no coverage. Slot reassigned to attack:T1087.002.process_identification to cover the uncovered process_identification dimension."
      kept: attack:T1087.002.process_identification
    - dropped: universal.host_user_context
      reason: "discovery_recon.discovery_user_context (Q3) supersedes it per the template's notes_to_llm — more specific to domain-discovery user context."
      kept: discovery_recon.discovery_user_context
    - dropped: cue_user_actor.baseline / cue_user_actor.baseline_svc pair non-dedup
      reason: "user_actor pair — non-deduplicable"
      kept: both (Q14 user-pivot, Q15 service-principal substitute)
    - dropped: cue_user_actor.clustering / cue_user_actor.clustering_svc pair non-dedup
      reason: "user_actor pair — non-deduplicable"
      kept: both (Q16 user-pivot, Q18 service-principal substitute)
  skipped_templates:
    - template: obfuscation.encoded_payload_decode
      reason: "replaced_by powershell.encoded_command_decode per hint (Q6)"
    - template: cue_process_tool.baseline
      reason: "Generic host-binary frequency is subsumed by the rule-pattern host baseline_deviation (Q13), which is filter-correct to this rule's firing pattern rather than generic binary usage."
    - template: cue_process_tool.clustering
      reason: "Overlaps cue_host_context.clustering (Q12); the host-context version aggregates by Image+ParentImage which is the more useful around-the-alert view."
    - template: powershell.script_block_logging
      reason: "Q6 (encoded_command_decode) covers the decode angle for the PowerShell branch; for the dominant process/.NET branches of this rule the script-block log adds little, and the slot was needed for the user_actor pair and process_identification coverage."
    - template: universal.endpoint_protection_health
      reason: "RECOMMENDED, not must_include. This is a discovery rule, not an execution/credential-access rule whose disposition turns on prior tamper; powershell_security_bypass (Q11) already surfaces prior PowerShell defense-weakening on the host, which is the relevant tamper angle for the PowerShell-cradle branch of this collector."
    - template: cue_host_context.precursor
      reason: "Tier 3 prior-alert timeline. Dropped to hold the 18-question cap; around-the-alert host context is carried by cue_host_context.clustering (Q12), and the host-pattern history by baseline_deviation (Q13). For a discovery-tool detection the prior-alert sweep is the least load-bearing context question."
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
    - discovery_recon
    - hacking_tool
    - cue_process_tool
    - cue_user_actor
    - cue_host_context
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1482,T1059.001) is not covered — pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        This rule fires on the SharpHound/BloodHound collector identified by binary metadata, filename, or collection
        command line — all process_creation evidence. The image_load angle for T1482/T1059.001 targets .NET/automation
        assemblies loaded by a generic host (e.g., System.Management.Automation in indirect PowerShell hosting); for a named
        collector whose identity is already established at process_creation time, an image_load sweep returns assembly-load
        volume without precision and does not advance triage. The in-memory/.NET-cradle concern this hint represents is
        instead covered by the PowerShell decode question (Q6), which surfaces [Reflection.Assembly]::Load and Invoke-Bloodhound
        cradles from process_creation CommandLine. No compensating attack:*.image_load question authored.
    - hint: "logsource 'process_access' (recommended by techniques: T1482) is not covered and no template family exists — author a custom question with `category: process_access`"
      disposition: declined
      reason: >-
        process_access (Sysmon EID 10) for T1482 targets API-level trust enumeration via handle access to LSASS or directory
        services. This rule does not detect handle access; it detects the SharpHound collector, which performs trust discovery
        over LDAP, not via cross-process memory access. A process_access query would surface unrelated handle activity. The
        collection-scope question (Q4) captures the trust-enumeration intent (CollectionMethod / Trusts) from the collector's
        own command line, which is the precise evidence for this rule. No process_access question authored.
    - hint: "logsource 'security' (recommended by techniques: T1482) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The security-log angle for T1482 is EID 4662 (directory-service object access), which records LDAP reads at the DC.
        This rule is endpoint process_creation on the collector host, and DC-side 4662 correlation requires DC audit logging
        that is environment-dependent and not addressable from the alerting event. The directory-enumeration behavior is
        instead evidenced endpoint-side by the collection-scope (Q4), decode (Q5), and output-artifact (Q9) questions. No
        security-logsource question authored.
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 3
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 17
      file_event: 1
