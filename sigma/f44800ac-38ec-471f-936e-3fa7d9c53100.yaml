name: PUA - CleanWipe Execution
id: 237ac186-e79a-4318-8f6d-b37786898b97
description: |
  Detects execution of CleanWipe, the vendor utility used to forcibly remove Symantec/Norton
  endpoint protection. The rule fires when at least one of its component binaries runs: SepRemovalToolNative_x64.exe,
  CATClean.exe with --uninstall, NetInstaller.exe with -r, or WFPUnins.exe with both /uninstall and /enterprise.
  A sanctioned migration or support engagement is the benign explanation; the same tool is also run by
  operators to strip endpoint protection before executing a payload, dumping credentials, or deploying
  ransomware, so removal of protective software is itself a defense-evasion action. Investigation should
  confirm which branch fired and the exact command, determine whether the removal was authorized and what
  protection was actually torn down, check whether the host is now unprotected, and look at what ran on the
  host before and after the removal and how widely the same pattern appears across the fleet.
type: detection
detection_id: f44800ac-38ec-471f-936e-3fa7d9c53100
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-09
questions:
- source: template:universal.verify_trigger_match
  question: Does this process match one of the CleanWipe component patterns this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive. The rule fires when
    EITHER the Image ends in SepRemovalToolNative_x64.exe, OR it ends in CATClean.exe with --uninstall in
    the command line, OR it ends in NetInstaller.exe with -r, OR it ends in WFPUnins.exe with both /uninstall
    and /enterprise. Review the alerting event and note which branch matched — the binary and flags that
    fired determine which downstream questions carry weight, since the four components remove different
    parts of the Symantec/Norton stack.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to remove endpoint protection software?
  context: Legitimate antivirus migration or vendor support can trigger this detection. Removing endpoint
    protection is a high-impact action normally restricted to IT or security staff during a planned change.
    Knowing whether the user role and host function are consistent with a sanctioned removal is often the
    fastest path to distinguishing an approved migration from an operator stripping defenses before a
    payload.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:remote_access_tool.tool_authorization
  question: Is CleanWipe on the organization's approved software list, and was its use sanctioned for this host?
  context: CleanWipe is a potentially unwanted utility — legitimate when run by IT to clean up a broken or
    migrating Symantec/Norton install, but not something that belongs on a normal endpoint. Even a vendor
    tool with a valid signature can represent an unauthorized teardown of protection if it was not part of
    a planned change. Check it against the approved software and change-management lists regardless of the
    tool's reputation.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
- source: template:remote_access_tool.active_session_context
  question: Is there a change ticket, maintenance window, or support session explaining this removal?
  context: Sanctioned removal of endpoint protection typically correlates with a change ticket, a scheduled
    migration window, or a vendor support engagement. If no such context exists for this execution, the tool
    may have been run by an operator to disable defenses or by a user circumventing security controls. Match
    the user, host, and timing against change records and helpdesk history.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.what_was_disabled
  question: Which CleanWipe component ran and what part of the Symantec/Norton stack did it remove?
  context: The four component binaries tear down different pieces of protection. SepRemovalToolNative_x64.exe
    is the native SEP removal driver; CATClean.exe --uninstall removes the content catalog; NetInstaller.exe -r
    reverses the network installer; WFPUnins.exe /uninstall /enterprise removes the Windows Filtering Platform
    callout driver that enforces network protection. Read the command line to identify which control was
    removed — this determines how exposed the host now is and what remediation is required.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.protection_status
  question: Is Symantec/Norton endpoint protection currently running on this host, or has it been removed?
  context: CleanWipe removal may have been completed or only partially applied. Check the current state of
    the endpoint protection services and drivers — if the agent, real-time scanning, or the network filter
    driver are gone, the host is unprotected and protection should be restored or re-imaged before further
    investigation. A host left without endpoint protection is an open window for follow-on activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after endpoint protection was removed?
  context: Protection is stripped for a reason — to execute malware, dump credentials, or perform actions
    that would otherwise be blocked or logged. Examining process activity in the window immediately after the
    removal reveals what the teardown was meant to enable. New or unusual binaries running shortly after the
    protection drop point to the payload the operator was clearing the way for.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense tampering actions performed on this host around the same time?
  context: Operators who remove one control often disable several — stopping additional security services,
    clearing event logs, deleting shadow copies, or changing the firewall — to maximize coverage before
    acting. Finding other tampering events from the same user or parent process near the removal indicates a
    deliberate teardown of multiple defenses rather than an isolated, sanctioned uninstall.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: attack:T1562.001.registry_set
  question: Were the Symantec/Norton service and driver registry keys modified or deleted around the removal?
  context: Removal of endpoint protection leaves registry evidence — the protection services and filter-driver
    keys under the service control hierarchy are deleted or disabled as the components are torn down.
    Registry-modification events targeting Symantec/Norton service keys (or the broader security-service hive)
    confirm the controls were actually removed at the system level rather than merely a removal tool being
    launched, and the writing process and account show who performed the teardown.
  range: +/-1h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection_host:
        host.name|expand: '%hostname%'
      selection_keys:
        TargetObject|contains:
          - '\Services\SepMasterService'
          - '\Services\SmcService'
          - '\Services\SAVService'
          - '\Services\Symantec'
          - '\Services\Norton'
          - '\Services\BHDrvx64'
          - '\Services\SymEvent'
          - '\Services\WFP'
      condition: selection_host and selection_keys
    fields:
      - TargetObject
      - Image
      - User
- source: template:cue_process_tool.process_chain
  question: What process spawned this CleanWipe component and is that parent chain expected?
  context: The parent process reveals how the removal tool was invoked and under which context. A managed
    deployment tool, an installer, or an interactive explorer.exe launch points to IT-driven removal; script
    hosts, archive utilities, or another remote access session as the parent point to delivery by a loader or
    staging from a prior foothold rather than a sanctioned migration. Examine the parent and what else it
    spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this pattern, what parents and users normally drive it, and does the current alert fit?
  context: Even sanctioned cleanup tools recur. This query mirrors the rule's selection structure scoped to
    this host, so the resulting parent/user distribution reflects how the rule's specific firing pattern — not
    generic binary usage — has historically run here. A new parent process, an unexpected user account, or an
    unusual launch shape is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection1:
        Image|endswith: \SepRemovalToolNative_x64.exe
      selection2:
        Image|endswith: \CATClean.exe
        CommandLine|contains: --uninstall
      selection3:
        Image|endswith: \NetInstaller.exe
        CommandLine|contains: -r
      selection4:
        Image|endswith: \WFPUnins.exe
        CommandLine|contains|all:
        - /uninstall
        - /enterprise
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (1 of selection*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.scope
  question: Have other hosts fired this pattern in the past week, and does the cross-host distribution look like consistent automation or one-off activity?
  context: This query mirrors the rule's selection structure with no host scope, so the result is a fleet-wide
    count of the rule's firing pattern rather than generic usage of the binaries. Many hosts running the same
    removal pattern points to a scripted or managed migration; a single host or a small unrelated set points
    to manual operator activity. Use the host distribution and parent consistency to calibrate severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection1:
        Image|endswith: \SepRemovalToolNative_x64.exe
      selection2:
        Image|endswith: \CATClean.exe
        CommandLine|contains: --uninstall
      selection3:
        Image|endswith: \NetInstaller.exe
        CommandLine|contains: -r
      selection4:
        Image|endswith: \WFPUnins.exe
        CommandLine|contains|all:
        - /uninstall
        - /enterprise
      condition: 1 of selection*
    fields:
    - host.name
- source: template:cue_user_actor.baseline
  question: Does this user normally run endpoint-protection removal tools?
  context: Compare this user's current action against their historical behavior. A user who has never run a
    CleanWipe component is more suspicious than one who does so regularly as part of IT cleanup work, and the
    user role should align with managing endpoint protection. Low or zero counts across the baseline window
    indicate this is not part of the user's routine.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and binary combination historically routine on this host or across the environment, or is the parent unusual for this binary?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior. The
    meaningful pivot is the launch mechanism: which parent process spawned this binary, and whether that pair
    is historically common. A new or rare parent for this removal binary — on this host or env-wide — is the
    deviation worth chasing; a pair that already accounts for many prior events is a managed deployment or
    cleanup job.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning
    multiple endpoints. If the same user removed protection or ran other tooling on several hosts, the scope
    of the operation may be larger than the single alerting host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and binary combination fire on other hosts in the same window, or is the activity isolated to this host?
  context: Under a service principal, asking what else the user did across the network returns agent-telemetry
    noise, since every host runs service-owned processes constantly. The meaningful question is whether the
    same launch mechanism — parent process plus spawned binary — fired on other hosts in the alert window.
    Multiple hosts point to a scheduled or pushed removal rollout; a single host points to a host-local
    trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: Alerts rarely occur in isolation. Examining activity around the removal reveals the immediate
    context — what led to it, what happened after, and whether it is part of a larger sequence. Look for
    reconnaissance, credential access, additional tampering, or new tooling installed near the removal time.
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — reconnaissance, credential
    access, or other defense tampering — may reveal the attack stages that preceded the removal of endpoint
    protection and help reconstruct the full timeline. Removing protection is often a preparatory step, so
    activity in the hours before it matters.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: removal_tool_execution
      description: "Execution of a security-tool removal/modification utility (one of the CleanWipe components)"
      mapped_to: Q5
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: protection_disabled_state
      description: "Security tool/service is left disabled or removed (process/service termination, registry edits)"
      mapped_to: Q9
      source: attack:T1562.001.registry_set
    - stage: 3
      label: post_tamper_activity
      description: "Abnormal activity following the disabling, including the action the teardown enabled"
      mapped_to: Q7
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q9
      source: attack:T1562.001.registry_set
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q10
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only (expected administrative tools to suppress false positives)"
      disposition: context
      target_question: Q3
      notes: "tool_authorization carries the allowlist framing — sanctioned removal vs. unauthorized teardown."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (window linking removal to post-removal activity / service stoppage)"
      disposition: context
      target_question: Q7
      notes: "activity_after_tampering uses a +1h forward window correlating removal to follow-on process activity."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "Symantec/Norton security service names (SepMasterService, SmcService, SAVService, etc.)"
      disposition: new_question
      target_question: Q9
      notes: "Injected concrete Symantec/Norton service-key names into the registry_set TargetObject|contains selection."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only (authorized admin tools that stop services during maintenance)"
      disposition: context
      target_question: Q10
      notes: "process_chain surfaces the parent so the analyst can judge managed deployment vs. ad-hoc launch."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only (which services count as security-critical)"
      disposition: context
      target_question: Q6
      notes: "protection_status asks the analyst to verify the Symantec/Norton services/drivers are still present."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "baseline_deviation (rule-pattern host history) and cue_user_actor.baseline (user history) cover the historical-frequency angle more precisely; generic host+binary baseline subsumed"
      kept: "cue_process_tool.baseline_deviation + cue_user_actor.baseline"
    - dropped: cue_process_tool.clustering
      reason: "cue_host_context.clustering covers the same what-else-ran-on-this-host angle; kept the supporting-cue version"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.follow_on
      reason: "defense_tampering.activity_after_tampering covers post-execution host activity (the children/follow-on the removal enabled) with a forward window; the parent-scoped follow_on adds no distinct angle for a one-shot uninstaller"
      kept: template:defense_tampering.activity_after_tampering
    - dropped: null
      kept: "cue_user_actor.baseline + cue_user_actor.baseline_svc"
      reason: "user_actor pair — non-deduplicable (subject identity vs. launch-mechanism identity)"
    - dropped: null
      kept: "cue_user_actor.clustering + cue_user_actor.clustering_svc"
      reason: "user_actor pair — non-deduplicable (subject identity vs. launch-mechanism identity)"
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: defense_tampering.what_was_disabled (Q5) — more specific control-targeting framing for a defense-evasion rule"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed for this rule; what_was_disabled (Q5), protection_status (Q6), and other_tampering_activity (Q8) are more specific than the generic host-tamper sweep and supersede it per the template's own drop condition (a)"
    - template: remote_access_tool.connection_destination
      reason: "CleanWipe components are local AV-removal binaries with no command-and-control / relay behavior; a network_connection question keyed to the removal binary would return no meaningful endpoint and does not fit this rule's behavior"
    - template: persistence_scope.process_based_persistence
      reason: "recommended, not must_include; this rule detects defense evasion (protection removal), not persistence installation. Persistence stacking is not the disposition driver for an AV-teardown alert, and the +1h activity_after_tampering (Q7) plus host clustering (Q17) already surface any tooling run near the removal"
    - template: persistence_scope.file_based_persistence
      reason: "recommended, not must_include; same rationale — this is a defense-evasion uninstaller, not a persistence drop. File-based autorun coverage adds no investigative angle the analyst needs for this rule, and including it would not improve disposition while pushing the count past 18"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - defense_tampering
    - remote_access_tool
  generation_stats:
    total_questions: 18
    source_counts:
      template: 17
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 16
      registry_set: 1
      sigma_alert: 1
