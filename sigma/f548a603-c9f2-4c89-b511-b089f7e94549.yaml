name: Potential Persistence Via Microsoft Compatibility Appraiser
id: c6159320-8775-444c-a926-0a004ef3bfb3
description: |
  Detects schtasks.exe being used to manually run the built-in "Microsoft Compatibility
  Appraiser" scheduled task, identified by an Image ending in schtasks.exe (or an
  OriginalFileName of schtasks.exe) running with a command line that contains both "run "
  and the task path \Application Experience\Microsoft Compatibility Appraiser. Manually
  triggering this task forces CompatTelRunner.exe to re-read the
  AppCompatFlags\TelemetryController registry key, where an attacker can register a Command
  value that then runs as SYSTEM — a registry-backed persistence and Admin-to-SYSTEM
  escalation primitive that autoruns does not surface. The investigation should confirm the
  exact command, determine whether the TelemetryController registry key was modified, and
  identify any child process CompatTelRunner.exe spawned after the task ran.
type: detection
detection_id: f548a603-c9f2-4c89-b511-b089f7e94549
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show schtasks running the Microsoft Compatibility Appraiser task — both the run verb and the Application Experience task path present?
  context: Before investing investigation time, confirm the alert is a true positive. The
    rule fires only when the process is schtasks.exe (by Image or OriginalFileName) AND the
    command line contains both "run " and \Application Experience\Microsoft Compatibility
    Appraiser. Confirm both elements are present and that this is a manual run of the
    existing task rather than the scheduler invoking it on its normal interval.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manually run the Compatibility Appraiser task?
  context: The Compatibility Appraiser task normally runs unattended on its own schedule.
    A human or script manually invoking it with schtasks /run is unusual. Knowing whether
    the account is an administrator and whether the host's function involves telemetry or
    compatibility tooling is often the fastest way to separate sanctioned maintenance from a
    deliberate trigger of registry-backed persistence.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:scheduled_task.task_configuration
  question: What exact schtasks command ran — confirm the /run verb, the targeted task name, and any additional flags?
  context: This re-displays the triggering event. The schtasks command line carries the
    operation (/run), the task name (/tn), and any run-as or extra flags. Unlike task
    creation, /run executes an already-registered task immediately. Identify the precise task
    name to confirm it is the canonical \Microsoft\Windows\Application Experience\Microsoft
    Compatibility Appraiser task and not a similarly named decoy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: attack:T1053.005.registry_set
  question: Was the AppCompatFlags\TelemetryController registry key modified to register a Command that CompatTelRunner will run?
  context: Manually running the Compatibility Appraiser task forces CompatTelRunner.exe to
    re-read subkeys under HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\AppCompatFlags\TelemetryController.
    A new subkey with a Command value and Nightly/Maintenance/Oobe DWORDs set to 1 is the
    persistence payload — it runs as SYSTEM at the next trigger. A registry-set event records
    the write directly even when it was made programmatically. Look for writes to that key
    path in the window around the task run.
  range: -24h
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetObject|contains: '\AppCompatFlags\TelemetryController'
      condition: selection
    fields:
      - TargetObject
      - Details
      - Image
- source: attack:T1053.005.compattelrunner_child
  question: After the task ran, did CompatTelRunner.exe spawn any child process, and is that child a signed Microsoft binary from System32?
  context: When the registered Command fires, CompatTelRunner.exe launches it via
    CreateProcessW as SYSTEM, with a command line of the form <Command> -cv <random> and
    sometimes -oobe or -fullsync appended. CompatTelRunner has no legitimate reason to spawn
    arbitrary child processes; any non-Microsoft-signed child, or a child outside System32,
    is the persistence payload executing with SYSTEM privileges.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\CompatTelRunner.exe'
      condition: selection
    fields:
      - Image
      - CommandLine
      - User
- source: template:scheduled_task.task_trigger_and_timing
  question: Did the manual run actually drive a payload execution, and did the scheduler also fire this task on its own?
  context: The /run verb triggers the task immediately, so CompatTelRunner should execute
    right after the alert; the scheduler also fires the same task periodically as a child of
    the Task Scheduler service. Reviewing processes spawned by the scheduler after the alert
    reveals whether the registered Command ran once or repeats on the task's normal interval,
    which determines how often the persistence reactivates.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith: '\svchost.exe'
        ParentCommandLine|contains: 'Schedule'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:scheduled_task.task_still_present
  question: Is the persistence still active — is the TelemetryController Command still registered and the Compatibility Appraiser task still enabled?
  context: This persistence is effective only while the TelemetryController Command value and
    its Nightly/Maintenance/Oobe DWORDs remain set. The Compatibility Appraiser task itself is
    a built-in task and will normally still be present, so remediation hinges on the registry
    key, not the task. Confirm via direct registry inspection of HKLM\SOFTWARE\Microsoft\Windows
    NT\CurrentVersion\AppCompatFlags\TelemetryController whether the malicious subkey is still
    in place and requires removal.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:obfuscation.encoded_payload_decode
  question: Is the registered TelemetryController Command encoded or obfuscated, and what does it decode to?
  context: The Command REG_SZ registered under TelemetryController is what runs as SYSTEM, and
    operators commonly hide it as a base64 or otherwise encoded payload, a download cradle, or
    a path to a dropped binary. The schtasks /run command line itself carries no payload — the
    operation here is triggering an existing task — so the content to decode is the registry
    Command value found in the TelemetryController key. Reconstruct it to identify the actual
    operation it performs.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this schtasks invocation, and is that parent chain expected?
  context: The parent reveals how schtasks was invoked. A user running it from an interactive
    shell (explorer-spawned cmd or powershell) differs from schtasks launched by a script
    host, a remote-execution service, or an Office application. An unexpected parent points
    toward an operator triggering the persistence rather than routine administration.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:lolbin.renamed_binary_check
  question: Was schtasks.exe executed from its expected System32 path?
  context: schtasks.exe legitimately lives in System32 (and SysWOW64). A copy running from a
    user-writable or temp directory, or a renamed binary whose OriginalFileName is schtasks.exe
    while its Image is something else, indicates an attempt to evade path-based allowlists.
    Compare the Image path against the expected location and check whether Image and
    OriginalFileName agree.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host run the Compatibility Appraiser task manually over the past 90 days, and what command-line structures are typical?
  context: A manual run of this task with no prior history on the host is more suspicious than
    one that recurs under a stable parent. The query mirrors the rule's selection scoped to
    this host, so the count reflects the rule's specific firing pattern rather than generic
    schtasks usage. The command-line projection lets the alerting invocation be compared
    against any prior patterns.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|all:
        - 'run '
        - \Application Experience\Microsoft Compatibility Appraiser
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts run the Compatibility Appraiser task manually in the past week, and does the spread look like automation or one-off activity?
  context: The query mirrors the rule's selection structure with no host scope, so the result
    is a fleet-wide count of this specific firing pattern rather than generic schtasks usage.
    Many hosts running the same pattern points to scripted deployment or vendor automation; a
    single host or a small unrelated set points to manual operator activity. Use the host
    distribution to calibrate how isolated the event is.
  range: -7d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|all:
        - 'run '
        - \Application Experience\Microsoft Compatibility Appraiser
      condition: all of selection_*
    fields:
    - host.name
- source: template:persistence_scope.process_based_persistence
  question: Were other process-based persistence mechanisms installed on this host around the same time?
  context: Operators rarely rely on a single persistence mechanism. Alongside the
    TelemetryController registry trigger, they commonly stack scheduled tasks, services,
    registry Run keys, BITS jobs, or WMI subscriptions as backups. Several persistence tool
    executions in the same timeframe indicate intentional stacking rather than isolated
    administrative work.
  range: +/-4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_persist:
        Image|endswith:
          - '\schtasks.exe'
          - '\at.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\bitsadmin.exe'
          - '\wmic.exe'
      condition: selection and selection_persist
    fields:
      - Image
      - CommandLine
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for manually running this task?
  context: Many organizations require change tickets for persistence-relevant or telemetry
    configuration changes. A corresponding ticket suggests authorized maintenance; absence of
    one when policy requires it suggests the action was unauthorized. This check can quickly
    close a false positive or escalate a true positive.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|all:
        - 'run '
        - \Application Experience\Microsoft Compatibility Appraiser
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|all:
        - 'run '
        - \Application Experience\Microsoft Compatibility Appraiser
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the account's activity across all hosts in the surrounding window reveals
    lateral movement or coordinated activity on multiple endpoints. If the same user triggered
    similar task or persistence activity elsewhere, the scope is larger than this one host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      product: windows
      category: process_creation
    detection:
      selection_img:
      - Image|endswith: \schtasks.exe
      - OriginalFileName: schtasks.exe
      selection_cli:
        CommandLine|contains|all:
        - 'run '
        - \Application Experience\Microsoft Compatibility Appraiser
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1053.005
      name: Scheduled Task
      role: primary
    - id: T1053
      name: Scheduled Task/Job
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: persistence_registration
      description: "Operator writes a Command value (with Nightly/Maintenance/Oobe DWORDs) under HKLM\\...\\AppCompatFlags\\TelemetryController — the registry-backed persistence payload"
      mapped_to: Q4
      source: attack:T1053.005.registry_set
    - stage: 2
      label: manual_task_trigger
      description: "schtasks /run forces the existing Microsoft Compatibility Appraiser task to fire immediately (the detection point)"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 3
      label: payload_execution_as_system
      description: "CompatTelRunner.exe re-reads TelemetryController and launches the registered Command as SYSTEM"
      mapped_to: Q5
      source: attack:T1053.005.compattelrunner_child
    - stage: 4
      label: recurring_execution
      description: "The task's normal schedule re-fires the registered Command on its interval (svchost-driven scheduler execution)"
      mapped_to: Q6
      source: template:scheduled_task.task_trigger_and_timing
  dimension_coverage:
    - dimension: obfuscation_check
      technique: T1053.005
      covered_by: Q8
      source: template:obfuscation.encoded_payload_decode
      coverage_type: direct
    - dimension: time_correlation
      technique: T1053.005
      covered_by: Q6
      source: template:scheduled_task.task_trigger_and_timing
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1053.005
      covered_by: Q3
      source: template:scheduled_task.task_configuration
      coverage_type: direct
    - dimension: user_context
      technique: T1053.005
      covered_by: Q15
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: command_analysis
      technique: T1053
      covered_by: Q5
      source: attack:T1053.005.compattelrunner_child
      coverage_type: direct
    - dimension: execution_context
      technique: T1053
      covered_by: Q5
      source: attack:T1053.005.compattelrunner_child
      coverage_type: direct
  mutable_dispositions:
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (grouping task trigger and associated execution in time proximity)"
      disposition: context
      target_question: Q6
      notes: "task_trigger_and_timing correlates the manual run with subsequent scheduler-driven execution within the +4h window; registry write correlated separately in Q4 over -24h."
    - element: UserContext
      dimension: user_context
      specific_values: "SYSTEM (CompatTelRunner runs the Command as SYSTEM); non-standard user running the manual trigger"
      disposition: context
      target_question: Q5
      notes: "SYSTEM execution context named in compattelrunner_child context and User projected in its fields; the manual-trigger user covered by user_actor baseline (Q15) and host_user_context (Q2)."
    - element: TaskNamePattern
      dimension: tuning_parameter
      specific_values: "\\Application Experience\\Microsoft Compatibility Appraiser (the canonical task path the rule keys on)"
      disposition: query_injection
      target_question: Q3
      notes: "Task path is part of the rule's selection_cli CommandLine|contains|all and is surfaced verbatim by the ProcessGuid-scoped task_configuration query; reflected scope/baseline queries (Q11, Q12) also enforce it."
    - element: CommandLineEntropyThreshold
      dimension: obfuscation_check
      specific_values: "rationale only (flagging base64/encoded payloads)"
      disposition: context
      target_question: Q8
      notes: "Encoding decode targets the registered TelemetryController Command value rather than the schtasks /run line, which carries no payload; encoded_payload_decode context redirects the analyst to the registry Command."
    - element: TaskAuthor
      dimension: tuning_parameter
      specific_values: "rationale only (unexpected user/account initiating the task)"
      disposition: context
      target_question: Q2
      notes: "Manual-trigger actor surfaced by host_user_context (Q2) and user_actor baseline (Q15); the task itself is a built-in Microsoft task so authorship of the task is fixed."
    - element: CommandLineRegex
      dimension: command_analysis
      specific_values: "rationale only (suspicious binaries/scripts tied to the scheduled job)"
      disposition: context
      target_question: Q5
      notes: "The job's actual payload is the CompatTelRunner-spawned child; compattelrunner_child projects Image and CommandLine of that child for inspection."
    - element: ExecutionWindow
      dimension: tuning_parameter
      specific_values: "rationale only (lookback to correlate execution after registration)"
      disposition: context
      target_question: Q4
      notes: "registry_set lookback set to -24h to catch the persistence registration that precedes the manual trigger; compattelrunner_child (+4h) and task_trigger_and_timing (+4h) catch execution after."
  dedup_decisions:
    - dropped: cue_process_tool.process_chain (drop of duplicate)
      reason: "kept the cue_process_tool.process_chain question itself; no parent_process cue is active so it is not subsumed"
      kept: cue_process_tool.process_chain
    - dropped: cue_user_actor.baseline / baseline_svc and clustering / clustering_svc emitted as pairs
      reason: "user_actor pair — non-deduplicable (subject identity vs launch-mechanism identity, degenerate under different conditions)"
      kept: both variants of each pair
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: scheduled_task.task_configuration (Phase 2 analysis covered by the task-config question)"
    - template: cue_process_tool.baseline
      reason: "superseded by lolbin.historical_lolbin_baseline (reflected host-scoped count of the rule's exact pattern is more precise than a generic Image|endswith schtasks baseline)"
    - template: cue_process_tool.baseline_deviation
      reason: "near-duplicate of lolbin.historical_lolbin_baseline — both reflect the rule's selection host-scoped over a long window; the lolbin variant is must_include and projects CommandLine, kept it"
    - template: cue_process_tool.follow_on
      reason: "follow_on (children of schtasks) adds little — schtasks /run hands off to the scheduler/CompatTelRunner, so the meaningful child is CompatTelRunner's child, covered by attack:T1053.005.compattelrunner_child"
    - template: cue_process_tool.clustering
      reason: "near-duplicate of cue_host_context.clustering (host-scoped process_creation aggregation around the alert); host_context version was dropped in favor of keeping question budget, see below"
    - template: cue_host_context.clustering
      reason: "host-scoped activity-around-alert aggregation overlaps persistence_scope.process_based_persistence (host-scoped, +/-4h, persistence-tool-filtered) which is more targeted for this persistence rule; dropped to stay within 18"
    - template: cue_host_context.precursor
      reason: "prior-alert sigma timeline question dropped to stay within the 18-question cap; this is a persistence rule where the registry/payload chain (Q4/Q5/Q6) and persistence-stacking (Q13) carry the multi-stage context"
    - template: cue_user_actor.clustering (note)
      reason: "kept — listed here only to note cue_process_tool.clustering overlap; user_actor clustering is a non-dedup pair and retained"
    - template: lolbin.technique_class_lateral
      reason: "the other-LOLBins sweep targets download/proxy-exec objectives; this rule's objective is persistence via a built-in task trigger, not LOLBin download/execution, so the certutil/bitsadmin/rundll32 sweep does not match the objective — persistence_scope.process_based_persistence covers the relevant cross-tool persistence stacking instead"
    - template: persistence_scope.file_based_persistence
      reason: "this persistence vector is registry-backed (TelemetryController Command value), not Startup-folder/Run-key file drops; file_event Startup/Run sweep does not cover the AppCompatFlags mechanism, which is covered by attack:T1053.005.registry_set (Q4)"
    - template: organizational_context.human_recall
      reason: "dropped to stay within 18; change_ticket (Q14) plus host_user_context (Q2) already cover the authorization/recall axis for this manual administrative action"
    - template: organizational_context.timing_alignment
      reason: "dropped to stay within 18; timing is covered behaviorally by task_trigger_and_timing (Q6) and the manual-vs-scheduled distinction in verify_trigger_match (Q1)"
    - template: organizational_context.procedural_consistency
      reason: "dropped to stay within 18; the 'is direct schtasks normal here' axis overlaps host_user_context (Q2) and user_actor baseline (Q15)"
    - template: universal.endpoint_protection_health
      reason: "this is a persistence-trigger rule, not an execution/credential-access rule; tamper-as-precursor is not the live question here, and per the template guidance it earns its slot only for hands-on-keyboard payload/cred actions — the registry/payload chain (Q4/Q5) is the disposition driver instead"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'security' (recommended by techniques: T1053.005) is not covered and no template family exists — author a custom question with `category: security`"
      disposition: declined
      reason: >-
        The 'security' analytic for T1053.005 keys on EID 4698 (task created) and 4702
        (task updated). This rule fires on schtasks /run of an already-registered built-in
        task — no task creation or modification occurs, so 4698/4702 would not record this
        activity and a security-channel query would return nothing relevant. The persistence
        is registered in the registry, not the task store; that write is covered by
        attack:T1053.005.registry_set (Q4) against the TelemetryController key, and the
        SYSTEM-context payload execution by attack:T1053.005.compattelrunner_child (Q5).
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - scheduled_task
    - organizational_context
    - obfuscation
    - persistence_scope
    - lolbin
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 17
      registry_set: 1
