name: Execution of Powershell Script in Public Folder
id: 5901e9af-4603-4d69-9d44-880227e54113
description: |
  Detects powershell.exe or pwsh.exe launching a script with a -File argument
  pointing at C:\Users\Public (including the %Public% variable and quoted and
  abbreviated -f / -fi / -fil forms). C:\Users\Public is world-writable and
  requires no elevation, which makes it a convenient staging directory for
  dropped scripts; legitimate software occasionally stages helper scripts there,
  but interactive or automated PowerShell execution of a script from that
  location is uncommon in normal operations. Determine what the script does,
  whether the command line was encoded or obfuscated, what launched PowerShell,
  and whether the host and user have a history of this pattern.
type: detection
detection_id: fb9d3ff7-7348-46ab-af8c-b55f5fbf39b4
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show powershell.exe or pwsh.exe running a script file out of C:\Users\Public?
  context: >-
    Confirm the alert is a true positive before investing time. The rule fires
    when PowerShell is launched with a -File argument (in its -f, -fi, -fil, or
    -file forms, quoted or unquoted, including the %Public% variable) that points
    into C:\Users\Public. Verify the exact Image and command line match that
    pattern and note the referenced script path.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to run PowerShell scripts staged in C:\Users\Public?
  context: >-
    Legitimate administrative or software-deployment activity can stage scripts
    in the public profile. Knowing whether the user role and host function are
    consistent with running PowerShell scripts from a world-writable directory is
    often the fastest way to separate routine operations from operator activity.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:universal.evidence_preservation
  question: Is the referenced .ps1 script still on disk for analysis?
  context: >-
    The command line names a script under C:\Users\Public. Checking whether that
    file still exists enables deeper analysis -- hash lookups, sandbox submission,
    or content review. Scripts staged in public directories are frequently
    deleted after execution, so an early check preserves the artifact.
  range: +/-15m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did this PowerShell invocation do? Decode the full command line and the script path it referenced.
  context: >-
    The full command line reveals the specific action taken. Decode any flags,
    the referenced script path, encoded content, and any inline arguments passed
    to the script. Because powershell.exe is too common to scope by Image alone
    on a busy host, this query narrows to executions carrying the rule's
    Public-folder -File pattern so the result reflects the firing behavior rather
    than all PowerShell on the host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith:
          - \powershell.exe
          - \pwsh.exe
        CommandLine|contains:
          - '-f C:\Users\Public'
          - '-f "C:\Users\Public'
          - '-f %Public%'
          - '-fi C:\Users\Public'
          - '-fi "C:\Users\Public'
          - '-fi %Public%'
          - '-fil C:\Users\Public'
          - '-fil "C:\Users\Public'
          - '-fil %Public%'
          - '-file C:\Users\Public'
          - '-file "C:\Users\Public'
          - '-file %Public%'
      condition: selection
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:powershell.encoded_command_decode
  question: Was the command encoded or obfuscated, and what does it decode to?
  context: >-
    PowerShell invocations frequently hide intent with -EncodedCommand (-enc),
    string concatenation, backtick escaping, or variable substitution. Even when
    the -File path is plain text, the script it launches or inline arguments may
    be encoded. Decoding reveals the actual payload -- download cradles, reverse
    shells, or in-memory loading of .NET assemblies.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_artifact.mechanism_decode
  question: What is inside the referenced .ps1 script? Check for obfuscation, encoded strings, and known malware indicators.
  context: >-
    Read and analyze the script's actual content. Check for base64 encoding,
    obfuscated strings, embedded URLs or IP addresses, download cradles, and
    reflective-load patterns. This content analysis answers "what is IN the
    script?" and is distinct from a hash or reputation lookup. Actual retrieval
    requires manual access to the endpoint or an EDR console.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Image
- source: template:cue_artifact.artifact_verification
  question: What is the script's file hash and does it have known AV or threat-intel detections?
  context: >-
    A file hash is a unique identifier for reputation lookup. Submit the hash to
    threat-intel platforms to determine whether the script is known malicious,
    known benign, or previously unseen. This is distinct from reading the
    content -- it answers "is this file KNOWN?" rather than "what is in it?".
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|endswith|expand: '%file_paths%'
      condition: selection
    fields:
      - TargetFilename
      - Hashes
- source: template:powershell.script_block_logging
  question: Did PowerShell script block logging capture the full deobfuscated script content?
  context: >-
    Script block logging (Event ID 4104) records the deobfuscated content
    PowerShell actually executes, even when the command line or script is
    encoded, giving ground truth about what ran. The logged script-block length
    threshold determines how much is captured. If PowerShell operational logs are
    not ingested in this environment, treat zero results as a visibility gap
    rather than evidence nothing ran.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: powershell
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID: 4104
      condition: selection
    fields:
      - ScriptBlockText
      - ScriptBlockId
      - Path
- source: template:powershell.powershell_security_bypass
  question: Were PowerShell or endpoint security controls bypassed or weakened before this execution?
  context: >-
    Operators often disable or bypass protection before running a payload. Common
    techniques include AMSI bypass, execution-policy changes (Set-ExecutionPolicy
    or -ep bypass), Defender preference changes, and PowerShell downgrade attacks
    that fall back to the v2 engine that lacks logging. Finding these precursor
    events on the host in the hour before the alert strengthens the case for
    malicious activity.
  range: -1h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_bypass:
        CommandLine|contains:
          - 'Set-MpPreference'
          - 'DisableRealtimeMonitoring'
          - '-ExecutionPolicy Bypass'
          - '-ep bypass'
          - 'AmsiUtils'
          - 'amsiInitFailed'
          - 'Invoke-Obfuscation'
          - '-Version 2'
          - '-v 2'
      condition: selection and selection_bypass
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this PowerShell instance, and is that parent chain expected?
  context: >-
    The parent process reveals how PowerShell was invoked. Legitimate usage has
    predictable parents (explorer.exe for interactive use, a service or scheduler
    for automation). Office applications, mshta.exe, wscript.exe, or a browser as
    the parent point toward a delivery mechanism such as a malicious document or
    download.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline
  question: Does this host normally run powershell.exe or pwsh.exe?
  context: >-
    A binary that has rarely or never run on a host is more notable than one that
    runs regularly. Zero or very low historical counts indicate first-time or
    rare execution; high counts suggest PowerShell is part of normal operations
    here, in which case the Public-folder script path is the distinguishing
    signal rather than the interpreter itself.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - User
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this rule's pattern, what parents and users normally drive it, and does the current alert fit?
  context: >-
    The query mirrors the rule's selection scoped to this host, so the resulting
    parent and user distribution reflects how the rule's specific firing pattern
    -- PowerShell running a script from C:\Users\Public -- has historically run
    here, not generic PowerShell usage. A new parent process, an unexpected user
    account, or an unusual command-line structure is the anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        CommandLine|contains:
        - -f C:\Users\Public
        - -f "C:\Users\Public
        - -f %Public%
        - -fi C:\Users\Public
        - -fi "C:\Users\Public
        - -fi %Public%
        - -fil C:\Users\Public
        - -fil "C:\Users\Public
        - -fil %Public%
        - -file C:\Users\Public
        - -file "C:\Users\Public
        - -file %Public%
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_process_tool.follow_on
  question: What child processes did this PowerShell instance spawn afterward?
  context: >-
    The processes spawned after PowerShell ran reveal its actual impact.
    Legitimate scripts have predictable children; malicious execution often
    spawns cmd.exe, additional PowerShell, certutil.exe, rundll32.exe, or network
    utilities for post-exploitation. Walk the children to see what the script
    actually did.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        ParentImage|endswith|expand: '%Image%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        CommandLine|contains:
        - -f C:\Users\Public
        - -f "C:\Users\Public
        - -f %Public%
        - -fi C:\Users\Public
        - -fi "C:\Users\Public
        - -fi %Public%
        - -fil C:\Users\Public
        - -fil "C:\Users\Public
        - -fil %Public%
        - -file C:\Users\Public
        - -file "C:\Users\Public
        - -file %Public%
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        CommandLine|contains:
        - -f C:\Users\Public
        - -f "C:\Users\Public
        - -f %Public%
        - -fi C:\Users\Public
        - -fi "C:\Users\Public
        - -fi %Public%
        - -fil C:\Users\Public
        - -fil "C:\Users\Public
        - -fil %Public%
        - -file C:\Users\Public
        - -file "C:\Users\Public
        - -file %Public%
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: >-
    Tracking the user's activity across all hosts reveals lateral movement or
    parallel activity on multiple endpoints. If the same user triggered similar
    PowerShell or staging activity on other hosts in the same window, the scope
    of the activity is larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        Image|endswith:
        - \powershell.exe
        - \pwsh.exe
        CommandLine|contains:
        - -f C:\Users\Public
        - -f "C:\Users\Public
        - -f %Public%
        - -fi C:\Users\Public
        - -fi "C:\Users\Public
        - -fi %Public%
        - -fil C:\Users\Public
        - -fil "C:\Users\Public
        - -fil %Public%
        - -file C:\Users\Public
        - -file "C:\Users\Public
        - -file %Public%
      _pb_scope:
        process.parent.name|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: >-
    Alerts rarely happen in isolation. Earlier detections on the same host --
    defense tampering, reconnaissance, credential access -- may reveal the attack
    stages that preceded PowerShell running a script from the public folder and
    help reconstruct the full timeline.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1059.001
      name: PowerShell
      role: primary
    - id: T1059
      name: Command and Scripting Interpreter
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: powershell_launch
      description: "PowerShell launched with a -File argument referencing C:\\Users\\Public"
      mapped_to: Q1
      source: template:universal.verify_trigger_match
    - stage: 2
      label: command_decode
      description: "Decode the full command line / encoded arguments to learn the operational intent"
      mapped_to: Q4
      source: template:cue_process_tool.mechanism_decode
    - stage: 3
      label: script_content
      description: "Analyze the dropped script content and reputation"
      mapped_to: Q6
      source: template:cue_artifact.mechanism_decode
    - stage: 4
      label: child_spawning
      description: "Network connections or child processes spawned by the PowerShell execution"
      mapped_to: Q13
      source: template:cue_process_tool.follow_on
  dimension_coverage:
    - dimension: command_analysis
      technique: T1059.001
      covered_by: Q5
      source: template:powershell.encoded_command_decode
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1059.001
      covered_by: Q10
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1059.001
      covered_by: Q16
      source: template:cue_user_actor.clustering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1059.001
      covered_by: Q8
      source: template:powershell.script_block_logging
      coverage_type: direct
    - dimension: user_context
      technique: T1059
      covered_by: Q14
      source: template:cue_user_actor.baseline
      coverage_type: direct
  mutable_dispositions:
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "-enc / -nop / encoded and obfuscated argument patterns"
      disposition: query_injection
      target_question: Q4
      notes: "mechanism_decode query carries the rule's Public-folder -File CommandLine|contains list; encoded_command_decode (Q5) decodes -enc/obfuscation from the alert event"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "abnormal parents (Office, mshta, wscript)"
      disposition: context
      target_question: Q10
      notes: "Abnormal-parent rationale described in process_chain context; no concrete parent values to inject"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (off-hours / non-maintenance windows)"
      disposition: context
      target_question: Q16
      notes: "Tuning rationale, not directly queryable; clustering question scopes the +/-1h window"
    - element: LoadedModuleList
      dimension: tuning_parameter
      specific_values: "rationale only (rare .NET assemblies)"
      disposition: context
      target_question: Q6
      notes: "image_load not in scope (see hint_dispositions); reflective-load patterns surfaced via script-content analysis"
    - element: ScriptBlockLengthThreshold
      dimension: tuning_parameter
      specific_values: "rationale only (4104 script-block length threshold)"
      disposition: context
      target_question: Q8
      notes: "Threshold rationale folded into script_block_logging context; not directly queryable"
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "around-this-time host activity is covered by user_actor clustering and host_context.precursor; process_tool.clustering is the broadest/least specific of the three"
      kept: cue_host_context.precursor
    - dropped: cue_user_actor.baseline + cue_user_actor.baseline_svc
      reason: "user_actor pair -- non-deduplicable"
      kept: both emitted (Q14 user-pivot, Q15 service-principal substitute)
    - dropped: cue_user_actor.clustering + cue_user_actor.clustering_svc
      reason: "user_actor pair -- non-deduplicable"
      kept: both emitted (Q16 user-pivot, Q17 service-principal substitute)
  skipped_templates:
    - template: cue_host_context.clustering
      reason: "host-around-the-time context overlaps user_actor.clustering and host_context.precursor; dropped to stay within the 18-question cap"
    - template: universal.endpoint_protection_health
      reason: "recommended (not must_include); powershell.powershell_security_bypass already surfaces prior AV/EDR/PowerShell tamper on the host in the -1h window for this execution rule"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'image_load' (recommended by techniques: T1059.001) is not covered -- pull in cue_image_load.* or author a custom question with `category: image_load`"
      disposition: declined
      reason: >-
        The rule constrains execution to powershell.exe / pwsh.exe running a
        script file from C:\Users\Public; the investigation centers on the
        command line, the dropped script's content, and the launching parent.
        image_load on powershell.exe returns assembly-load volume without
        precision for this file-staged execution, and the reflective-load /
        in-memory .NET angle that image_load would address is already covered by
        the script-content analysis question (cue_artifact.mechanism_decode) and
        the encoded-command decode question. No compensating
        attack:T1059.001.image_load question authored.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - powershell
  generation_stats:
    total_questions: 18
    source_counts:
      template: 18
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 4
      context: 12
    logsource_diversity:
      process_creation: 13
      file_event: 3
      powershell_script_block: 1
      sigma_alert: 1
