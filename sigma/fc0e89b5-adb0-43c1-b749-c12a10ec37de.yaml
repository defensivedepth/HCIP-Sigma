name: SafeBoot Registry Key Deleted Via Reg.EXE
id: 6312c5e4-75cb-46df-b851-d419949a021c
description: |
  Detects reg.exe executed with the 'delete' flag against a key under HKLM\SYSTEM\CurrentControlSet\Control\SafeBoot.
  The rule fires when both the reg.exe image (or OriginalFileName reg.exe) AND a command line containing both ' delete '
  and \SYSTEM\CurrentControlSet\Control\SafeBoot are present. Deleting SafeBoot\Minimal or SafeBoot\Network subkeys for a
  security service prevents that service from loading when the host boots into Safe Mode, a step that often precedes a
  reboot-into-safe-mode action used to defeat AV/EDR before impact. Legitimate use is rare; determine which service entry
  was removed, what else the same operator did on the host, and whether the host was being prepared for a Safe Mode reboot.
type: detection
detection_id: fc0e89b5-adb0-43c1-b749-c12a10ec37de
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line show reg.exe deleting a key under \SYSTEM\CurrentControlSet\Control\SafeBoot?
  context: Confirm the alert is a true positive before investing investigation time. The rule requires both the reg.exe image
    (or OriginalFileName reg.exe) and a command line containing both ' delete ' and the SafeBoot control path. Read the exact
    command line and verify which SafeBoot subkey was targeted.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to delete SafeBoot registry keys?
  context: Removing SafeBoot service entries is not a routine administrative action. Knowing whether the user role and host
    function involve boot-configuration or security-product management is often the fastest path to separating a true positive
    from sanctioned maintenance.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:defense_tampering.what_was_disabled
  question: Which SafeBoot service entry was deleted, and does removing it disable a security product in Safe Mode?
  context: The command line reveals the exact key path. Entries under SafeBoot\Minimal and SafeBoot\Network list the services
    allowed to start in Safe Mode; deleting the entry for an AV/EDR service means that product will not load if the host is
    booted into Safe Mode. Identify the specific service name in the deleted path to determine which protection was targeted.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:registry.registry_key_context
  question: What registry key and value were affected, and is the SafeBoot path a known evasion location?
  context: SafeBoot\Minimal\<svc> and SafeBoot\Network\<svc> control which drivers and services load in Safe Mode, so removing
    a security service entry there is an evasion action rather than persistence. A reg.exe key delete produces no registry value-set
    event, so the registry_set view may be empty; in that case the deleted path must be read from the command line instead.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: registry_set
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - TargetObject
      - Details
- source: template:defense_tampering.protection_status
  question: Is the affected security service still configured to load, or does the host remain unable to start it in Safe Mode?
  context: The deletion may have been reverted or may still be in effect. Confirm whether the SafeBoot entry for the targeted
    service has been restored and whether the service itself is running. If the entry is still missing, the host will fail
    to load that protection on a Safe Mode boot and the key should be restored before further analysis.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:defense_tampering.activity_after_tampering
  question: What activity occurred on this host after the SafeBoot key was deleted?
  context: SafeBoot entries are usually removed to prepare a host for a Safe Mode reboot where AV/EDR will not load, so the
    actions that follow reveal intent. Look for a forced reboot, additional service or recovery tampering, or staging of an
    encryptor or other payload in the window after the deletion.
  range: +1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - User
- source: template:defense_tampering.other_tampering_activity
  question: Were other defense-tampering actions performed on this host in the same window?
  context: Operators who delete SafeBoot entries often stack tampering actions in one operation, for example clearing event
    logs, deleting shadow copies, disabling recovery, or stopping security services. Multiple tampering events from the same
    user or parent process indicate a broader effort to weaken the host rather than an isolated change.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_tamper:
        Image|endswith:
          - '\wevtutil.exe'
          - '\vssadmin.exe'
          - '\bcdedit.exe'
          - '\wbadmin.exe'
          - '\netsh.exe'
          - '\sc.exe'
          - '\reg.exe'
          - '\powershell.exe'
        CommandLine|contains:
          - 'cl '
          - 'clear-log'
          - 'delete shadows'
          - 'recoveryenabled'
          - 'DisableRealtimeMonitoring'
          - 'advfirewall'
          - 'stop'
          - 'delete'
      condition: selection and selection_tamper
    fields:
      - Image
      - User
- source: template:registry.registry_related_modifications
  question: Were other registry write or delete operations made on this host in the same session?
  context: A SafeBoot deletion is frequently part of a cluster of registry changes, for example adding a Run key for persistence
    or disabling Defender policy keys alongside it. Finding additional reg.exe add, delete, or import operations from the same
    user within a short window reveals the full scope of configuration changes.
  range: +/-30m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_reg:
        Image|endswith:
          - '\reg.exe'
          - '\regedit.exe'
        CommandLine|contains:
          - ' add '
          - ' delete '
          - ' import '
          - '/s '
      condition: selection and selection_reg
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this reg.exe instance, and is that parent chain expected?
  context: The parent process shows how the deletion was launched. A reg.exe delete invoked by explorer.exe or a console an
    administrator opened looks different from one spawned by cmd.exe running a batch script, powershell.exe, or an unexpected
    application, which point to scripted or pushed tampering. Walk up to the parent's own parent if the immediate parent is
    itself a shell.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host previously deleted SafeBoot keys via reg.exe, what parents and users drove it, and does the current
    alert fit that pattern?
  context: The query mirrors the rule's selection scoped to this host, so the parent and user distribution reflects how this
    specific SafeBoot-delete pattern has run here before rather than generic reg.exe usage. A new parent process, an unexpected
    account, or a first-ever occurrence is the deviation worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: reg.exe
      - OriginalFileName: reg.exe
      selection_delete:
        CommandLine|contains|all:
        - ' delete '
        - \SYSTEM\CurrentControlSet\Control\SafeBoot
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Does this user normally run reg.exe against boot or security configuration keys?
  context: Compare the current action against the user's historical behavior. A user who has never run reg.exe to modify SafeBoot
    or related control keys is more notable than one whose role routinely involves boot-configuration management. User role
    and typical toolset should align with deleting SafeBoot service entries.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.baseline_svc
  question: Is this parent and reg.exe combination historically routine on this host or across the environment, or is the parent
    unusual for reg.exe?
  context: When the alert fires under a service principal, user identity does not attribute behavior, so the meaningful pivot
    is the launch mechanism. A new or rare parent for reg.exe on this host or environment-wide is the deviation worth chasing;
    a parent that already accounts for thousands of prior reg.exe events is vendor or OS automation.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or parallel tampering on multiple endpoints.
    If the same user deleted SafeBoot keys or performed other tampering on several hosts in the same window, the scope of the
    operation is broader than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent and reg.exe combination fire on other hosts in the same window, indicating a coordinated rollout
    rather than isolated activity?
  context: Under a service principal, a user pivot across the network returns agent-telemetry noise. The meaningful question
    is whether the same launch mechanism, parent process plus reg.exe, fired on other hosts in the alert window. Multiple hosts
    in the same window points to scheduled or pushed automation; a single host points to a host-local trigger.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        process.parent.name|expand: '%ParentName%'
        Image|endswith|expand: '%Image%'
      condition: selection
    fields:
      - host.name
- source: template:lolbin.renamed_binary_check
  question: Was reg.exe executed from its expected System32 or SysWOW64 path?
  context: reg.exe run from outside System32 or SysWOW64 may have been copied or renamed to evade path-based allowlists. The
    rule matches on OriginalFileName as well as Image, so a relocated or renamed reg.exe still fires; confirming the on-disk
    path distinguishes a normal system binary from a planted copy.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - OriginalFileName
- source: template:lolbin.historical_lolbin_baseline
  question: How often has this host deleted SafeBoot keys via reg.exe over the past 90 days, and what command-line shapes are
    typical?
  context: A SafeBoot-delete pattern with no prior history on a host is more notable than one seen regularly under stable parents.
    The query mirrors the rule's selection scoped to this host, so the count reflects this specific firing pattern rather than
    generic reg.exe usage. The command-line projection lets the analyst compare typical invocations against the alerting one.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_img:
      - Image|endswith: reg.exe
      - OriginalFileName: reg.exe
      selection_delete:
        CommandLine|contains|all:
        - ' delete '
        - \SYSTEM\CurrentControlSet\Control\SafeBoot
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - CommandLine
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Tampering rarely happens in isolation. Earlier detections on the same host, such as reconnaissance, credential access,
    or other defense-evasion activity, may reveal the attack stages that preceded the SafeBoot deletion and help reconstruct
    the timeline leading to it.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1562.001
      name: Disable or Modify Tools
      role: primary
    - id: T1562
      name: Impair Defenses
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: tamper_action
      description: "reg.exe deletes a SafeBoot service entry, disabling that security product on a Safe Mode boot"
      mapped_to: Q3
      source: template:defense_tampering.what_was_disabled
    - stage: 2
      label: registry_target
      description: "Identify the specific SafeBoot key/value affected and confirm it is an evasion location"
      mapped_to: Q4
      source: template:registry.registry_key_context
    - stage: 3
      label: post_tamper_activity
      description: "Activity enabled by the tampering — Safe Mode reboot, payload staging, further tampering"
      mapped_to: Q6
      source: template:defense_tampering.activity_after_tampering
  dimension_coverage:
    - dimension: time_correlation
      technique: T1562.001
      covered_by: Q6
      source: template:defense_tampering.activity_after_tampering
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1562.001
      covered_by: Q7
      source: template:defense_tampering.other_tampering_activity
      coverage_type: direct
    - dimension: user_context
      technique: T1562.001
      covered_by: Q11
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: process_identification
      technique: T1562
      covered_by: Q9
      source: template:cue_process_tool.process_chain
      coverage_type: direct
  mutable_dispositions:
    - element: ProcessNameExclusions
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Expected-tools tuning is a rule-side false-positive control; surfaced as the parent-chain expectation question rather than a query value."
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only"
      disposition: context
      target_question: Q6
      notes: "Correlation window between tamper, post-tamper activity, and other tampering is realized as the +1h / +/-1h ranges on activity_after_tampering and other_tampering_activity."
    - element: ServiceNames
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q3
      notes: "The targeted security-service name is read from the deleted SafeBoot path in what_was_disabled rather than a fixed query list."
    - element: ProcessWhitelist
      dimension: process_identification
      specific_values: "rationale only"
      disposition: context
      target_question: Q9
      notes: "Authorized-tool exclusion is the analyst judgment in the parent-chain question."
    - element: ServiceNamePatterns
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q7
      notes: "Security-critical service set is realized by the service-name and tamper-image filters in other_tampering_activity."
  dedup_decisions:
    - dropped: cue_process_tool.baseline
      reason: "lolbin.historical_lolbin_baseline (reflected, rule-pattern scoped) is the more specific 90d host baseline — generic Image|endswith count superseded"
      kept: lolbin.historical_lolbin_baseline
    - dropped: cue_process_tool.clustering
      reason: "host-around-time activity is covered by defense_tampering.activity_after_tampering (+1h) and other_tampering_activity (+/-1h); a bare host Image aggregation adds no distinct angle"
      kept: defense_tampering.activity_after_tampering
    - dropped: cue_host_context.clustering
      reason: "host-around-time Image/ParentImage aggregation duplicates defense_tampering.activity_after_tampering and other_tampering_activity on this host"
      kept: defense_tampering.other_tampering_activity
    - dropped: cue_process_tool.follow_on
      reason: "reg.exe is a terminal config tool that does not spawn children; child-process question is non-informative for this rule"
      kept: none
    - dropped: lolbin.technique_class_lateral
      reason: "other-LOLBin-for-same-objective overlaps defense_tampering.other_tampering_activity, which is the more targeted tamper-tool sweep for this defense-evasion rule"
      kept: defense_tampering.other_tampering_activity
    - dropped: persistence_scope.process_based_persistence
      reason: "rule is defense-evasion / inhibit-Safe-Mode, not persistence install; reg/sc/schtasks tamper coverage already in other_tampering_activity, and dropping holds the set at 17"
      kept: defense_tampering.other_tampering_activity
    - dropped: persistence_scope.file_based_persistence
      reason: "no file_reference applicability and rule is evasion not persistence; Startup-folder file_event sweep adds no coverage for a SafeBoot key delete"
      kept: none
    - dropped: cue_user_actor pair (baseline + baseline_svc, clustering + clustering_svc)
      reason: "user_actor pair — non-deduplicable"
      kept: all four emitted
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: registry.registry_key_context (and defense_tampering.what_was_disabled is the Phase-2 decode equivalent)"
    - template: universal.endpoint_protection_health
      reason: "defense_tampering technique set is confirmed; its what_was_disabled / other_tampering_activity questions are more specific and supersede the generic host-tamper sweep"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded:
      - id: ransomware_impact
        confidence: low
        reason: "keyword match on 'ransomware' in the reference is incidental; the rule detects a SafeBoot key delete, an evasion primitive, not an encryption/impact action"
      - id: network_scope
        confidence: low
        reason: "keyword match on 'ransomware' in the reference is incidental; the rule has no network indicator and no external entity"
  technique_sets_applied:
    - universal
    - defense_tampering
    - registry
    - lolbin
  generation_stats:
    total_questions: 17
    source_counts:
      template: 17
      attack: 0
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 2
      context: 13
    logsource_diversity:
      process_creation: 15
      registry_set: 1
      sigma_alert: 1
