name: User Added to Remote Desktop Users Group
id: c1aa9e53-36aa-470c-a805-df9bb97a6860
description: |
  Detects a command line that adds a user to the local Remote Desktop Users group, either through 'net localgroup ... /add' or 'Add-LocalGroupMember -Group', matched against the group name in English, French, or Spanish. Granting Remote Desktop Users membership enables interactive RDP logon for that account, so this is routinely an administrative action but is also a common way to establish remote-access persistence after a host is compromised. The investigation should confirm the group change actually succeeded, determine who ran it and whether that user and host normally perform account administration, and check whether the newly enabled account is subsequently used for remote logon or lateral movement.
type: detection
detection_id: ffa28e60-bdb1-46e0-9f82-05f7a61cc06e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: 2026-06-06
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line add a user to the Remote Desktop Users group via net localgroup or Add-LocalGroupMember?
  context: Confirm the alert is a true positive before investing further time. The rule fires when the command line both performs a group add ('localgroup ... /add' or 'Add-LocalGroupMember -Group') and references the Remote Desktop Users group name in English, French, or Spanish. Read the full command line and identify which account is being granted membership.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to manage local group membership for remote access?
  context: Legitimate administrative activity is the documented false positive for this rule. Knowing whether the acting user holds an admin role and whether this host is one where group administration normally happens is often the fastest way to separate routine maintenance from an attacker enabling remote access.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:cue_process_tool.mechanism_decode
  question: What exactly did the command do? Decode the full command line and identify which account was added to the group.
  context: The full command line reveals the target account and the exact method used. Identify the account name being added, whether it is a built-in account, a newly created local account, or a domain account, and whether any additional flags or chained commands ran in the same invocation. The query returns all matching group-add commands on this host in a tight window so multi-step operations are visible together.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_cmd:
      - CommandLine|contains|all:
        - 'localgroup '
        - ' /add'
      - CommandLine|contains|all:
        - 'Add-LocalGroupMember '
        - ' -Group '
      condition: selection and selection_cmd
    fields:
      - CommandLine
      - CurrentDirectory
- source: template:account_manipulation.change_success_verification
  question: Did the group change succeed? Is there a Security log event showing a member added to the Remote Desktop Users group?
  context: A command to modify group membership does not guarantee success; insufficient privileges or policy can block it. Windows Security event 4732 (member added to a security-enabled local group) confirms the change occurred and records the account that was added. Absence of a corresponding 4732 around the command time suggests the add failed or that group-membership auditing is not collected on this host.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      product: windows
      service: security
    detection:
      selection:
        host.name|expand: '%hostname%'
        EventID:
          - 4728
          - 4732
          - 4756
      condition: selection
    fields:
      - EventID
      - TargetUserName
      - SubjectUserName
- source: template:cue_process_tool.process_chain
  question: What process spawned this command and is that parent chain expected?
  context: The parent process reveals how the group-add was invoked. An interactive admin session (explorer.exe, a console host launched by a logged-on user) fits routine administration; an unusual parent such as a script host, an Office application, or a remote-execution service points to automated or attacker-driven invocation. Walk up to the parent to see what launched it and what else it spawned.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_main:
      - CommandLine|contains|all:
        - 'localgroup '
        - ' /add'
      - CommandLine|contains|all:
        - 'Add-LocalGroupMember '
        - ' -Group '
      selection_group:
        CommandLine|contains:
        - Remote Desktop Users
        - "Utilisateurs du Bureau \xE0 distance"
        - Usuarios de escritorio remoto
      _pb_scope:
        User|expand: '%User%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_main:
      - CommandLine|contains|all:
        - 'localgroup '
        - ' /add'
      - CommandLine|contains|all:
        - 'Add-LocalGroupMember '
        - ' -Group '
      selection_group:
        CommandLine|contains:
        - Remote Desktop Users
        - "Utilisateurs du Bureau \xE0 distance"
        - Usuarios de escritorio remoto
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the environment around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or activity spanning multiple endpoints. If the same user triggered similar administrative or remote-access activity on other hosts in the same window, the scope of the event may be larger than a single host.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection_main:
      - CommandLine|contains|all:
        - 'localgroup '
        - ' /add'
      - CommandLine|contains|all:
        - 'Add-LocalGroupMember '
        - ' -Group '
      selection_group:
        CommandLine|contains:
        - Remote Desktop Users
        - "Utilisateurs du Bureau \xE0 distance"
        - Usuarios de escritorio remoto
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (all of selection_*) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.follow_on
  question: What did this user do next on this host?
  context: Follow-on activity reveals the next steps after the group change. Look for privilege escalation, additional account or group modifications, persistence installation, or attempts to use the newly granted remote access by the same user on this host.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        User|expand: '%User%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_user_actor.follow_on_svc
  question: What other local-group or account-administration commands ran on this host in the next four hours, and does the parent or command-line structure change?
  context: 'Under a service principal, asking what the user did next is unbounded. A more focused follow-on for automation-driven alerts is to track repeat account-administration on the same host: routine configuration management repeats with stable parents and command-line structures, whereas a change in parent or command-line structure in the follow-on window is the signal worth chasing.'
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      selection_cmd:
        CommandLine|contains:
          - 'net user'
          - 'net localgroup'
          - 'Add-LocalGroupMember'
          - 'New-LocalUser'
          - 'Set-LocalUser'
      condition: selection and selection_cmd
    fields:
      - ParentImage
      - CommandLine
- source: template:cue_user_actor.scope
  question: Are there other detections for this user on other hosts in the past day?
  context: If the same user triggered alerts on multiple hosts, the activity may extend beyond a single endpoint. Check for lateral movement patterns or repeated account-administration across the environment by this user.
  range: -24h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - rule.name
      - event.severity_label
- source: template:cue_user_actor.scope_svc
  question: Did this same rule fire on other hosts in the past day, and does the cross-host pattern look like coordinated automation or an isolated detection?
  context: 'Under a service principal, alerts for the acting user match everywhere because the principal is omnipresent. The triage-useful question is whether this specific rule fires on multiple hosts: a rule firing across many hosts on the same day is probably tracking scheduled or pushed administration, while a rule firing on one host alone is the deviation worth chasing.'
  range: -24h
  query: |
    aggregation: true
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        rule.name|expand: '%rule.name%'
      condition: selection
    fields:
      - host.name
- source: template:account_manipulation.modified_account_post_activity
  question: Was the newly added account used for logon or process execution after the group change?
  context: Adding an account to Remote Desktop Users is a means to an end, granting that account interactive RDP logon. Process executions or new sessions attributed to the added account shortly after the change indicate the remote access was actually used. Substitute the account name from the decoded command line into the analysis when reviewing results.
  range: +4h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|contains:
          - 'Administrator'
          - 'guest'
          - 'DefaultAccount'
      condition: selection
    fields:
      - host.name
      - Image
      - User
- source: attack:T1133.external_remote_access
  question: Did remote-access gateway or VPN logs show external sessions for the added account after the group change?
  context: Granting Remote Desktop Users membership is frequently a step toward enabling external remote access. Remote-access gateway, VPN, and Citrix logs record external source addresses and session establishment for accounts, which process and Security logs do not capture. External sessions for the newly added account from uncommon source addresses or outside business hours strengthen the case that the change enabled unauthorized remote entry. If application-channel remote-access logs are not collected in this environment, treat zero results as a visibility gap rather than evidence nothing happened.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: application
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - User
      - EventID
- source: template:organizational_context.change_ticket
  question: Was there a change ticket, maintenance window, or documented approval for adding this account to the Remote Desktop Users group?
  context: Many organizations require change records for account and group modifications. A corresponding ticket or maintenance window suggests authorized administration; the absence of one when policy requires it suggests the change was unauthorized. This check is often the fastest way to close a false positive or escalate a true positive, and it must be answered from change-management systems outside of log data.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1136.001
      name: Local Account
      role: primary
    - id: T1133
      name: External Remote Services
      role: secondary
    - id: T1021.001
      name: Remote Desktop Protocol
      role: secondary
    - id: T1136
      name: Create Account
      role: secondary
    - id: T1021
      name: Remote Services
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: group_add_command
      description: "Built-in tooling (net localgroup /add or Add-LocalGroupMember) runs to add an account to the Remote Desktop Users group"
      mapped_to: Q3
      source: template:cue_process_tool.mechanism_decode
    - stage: 2
      label: account_change_confirmation
      description: "Security log records member-added event (4732) confirming the group change succeeded"
      mapped_to: Q4
      source: template:account_manipulation.change_success_verification
    - stage: 3
      label: remote_access_use
      description: "Added account is subsequently used for interactive/RDP logon or external remote access (T1133/T1021.001)"
      mapped_to: Q15
      source: attack:T1133.external_remote_access
  dimension_coverage:
    - dimension: time_correlation
      technique: T1136.001
      covered_by: Q4
      source: template:account_manipulation.change_success_verification
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1133
      covered_by: Q6
      source: template:cue_user_actor.baseline
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1136.001
      covered_by: Q5
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: user_context
      technique: T1136.001
      covered_by: Q2
      source: template:universal.host_user_context
      coverage_type: direct
    - dimension: command_analysis
      technique: T1021
      covered_by: Q3
      source: template:cue_process_tool.mechanism_decode
      coverage_type: direct
  mutable_dispositions:
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (cmd.exe, wscript.exe, renamed binaries)"
      disposition: new_question
      target_question: Q5
      notes: "Parent-chain question added to cover parent-process analysis for the group-add command"
    - element: UserContext
      dimension: user_context
      specific_values: "rationale only (SYSTEM, Administrator, untrusted account)"
      disposition: context
      target_question: Q2
      notes: "Service-vs-admin-vs-low-priv context folded into host_user_context and the user_actor service-principal pair"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "rationale only (5s-2m process-to-4720/4732 correlation)"
      disposition: context
      target_question: Q4
      notes: "Tight +/-5m window on the Security-log confirmation query correlates the command with the 4732 event"
    - element: ProcessList
      dimension: tuning_parameter
      specific_values: "rationale only (cmd.exe, powershell.exe, certutil.exe post-login)"
      disposition: context
      target_question: Q14
      notes: "Post-change process execution surfaced via modified_account_post_activity"
    - element: BusinessHours
      dimension: tuning_parameter
      specific_values: "rationale only"
      disposition: context
      target_question: Q15
      notes: "Outside-business-hours external access folded into the remote-access question context"
    - element: KnownRemoteIPs
      dimension: tuning_parameter
      specific_values: "rationale only (approved external IPs/VPN endpoints)"
      disposition: context
      target_question: Q15
      notes: "Uncommon-source-address framing folded into application-log remote-access question context"
  dedup_decisions:
    - dropped: cue_user_target.artifact_verification
      reason: "account_manipulation technique set is confirmed; its change_success_verification is more specific for an add-to-group rule (scopes 4728/4732/4756) and supersedes the generic cue_user_target version"
      kept: account_manipulation.change_success_verification
    - dropped: cue_user_actor.baseline (user_actor pair)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.baseline + cue_user_actor.baseline_svc
    - dropped: cue_user_actor.clustering (user_actor pair)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.clustering + cue_user_actor.clustering_svc
    - dropped: cue_user_actor.follow_on (user_actor pair)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.follow_on + cue_user_actor.follow_on_svc
    - dropped: cue_user_actor.scope (user_actor pair)
      reason: "user_actor pair — non-deduplicable; both user-pivot and service-principal substitute emitted"
      kept: cue_user_actor.scope + cue_user_actor.scope_svc
  skipped_templates:
    - template: cue_process_tool.baseline
      reason: "host-level tool baseline redundant with user_actor.baseline; CommandLine-only rule has no Image to scope a host tool-frequency query meaningfully"
    - template: cue_user_target.baseline
      reason: "target-account logon-history baseline (TargetUserName) overlaps modified_account_post_activity; budget reserved for user_actor pairs and logsource-coverage questions"
    - template: cue_host_context.clustering
      reason: "host-wide process clustering overlaps cue_user_actor.clustering (network-wide) and follow_on (host-local); dropped to stay within 18 and preserve the non-deduplicable user_actor pairs"
    - template: cue_host_context.precursor
      reason: "prior-alert precursor sweep dropped under the 18-question cap; scope question already surfaces other detections for this user, and the rule is not execution/credential-centric"
    - template: universal.endpoint_protection_health
      reason: "generic host-tamper sweep; this rule fires on account administration, not payload execution/credential access, so tamper-as-precursor is not the live question, and the cap is consumed by the four non-deduplicable user_actor pairs"
    - template: lateral_movement.source_host_identification
      reason: "rule fires on the group-add command, not on an inbound RDP connection; no source host to identify at detection time"
    - template: lateral_movement.authentication_method
      reason: "no authentication event in the triggering data; the rule precedes any RDP logon by the added account"
    - template: lateral_movement.target_activity_after_access
      reason: "overlaps modified_account_post_activity (post-change process execution on this host)"
    - template: lateral_movement.lateral_movement_scope
      reason: "lateral-movement-tool scope (psexec/mstsc/ssh) not yet relevant; rule fires before remote access occurs, and cross-host user activity is covered by user_actor.clustering"
    - template: initial_access.delivery_vector
      reason: "no email/document/web delivery vector; the rule detects local account administration, not initial payload delivery"
    - template: initial_access.campaign_scope
      reason: "overlaps cue_user_actor.scope_svc (same rule across hosts)"
    - template: initial_access.application_patch_status
      reason: "no exploited application in scope; the rule detects a built-in group-administration command"
    - template: network_scope.same_action_other_hosts
      reason: "overlaps cue_user_actor.clustering_svc (same group-add across hosts via parent+command scope)"
    - template: network_scope.actor_activity_network_wide
      reason: "duplicate of cue_user_actor.clustering (network-wide user activity)"
    - template: network_scope.actor_authentication_trail
      reason: "authentication-trail coverage folded into the application-log remote-access question (Q15); kept budget for user_actor pairs"
    - template: organizational_context.human_recall
      reason: "overlaps organizational_context.change_ticket and host_user_context for the legitimacy check; one organizational question kept"
    - template: organizational_context.timing_alignment
      reason: "timing-vs-work-hours legitimacy folded into change_ticket context; one organizational question kept under the cap"
    - template: organizational_context.procedural_consistency
      reason: "procedural-norm legitimacy overlaps change_ticket; one organizational question kept under the cap"
    - template: account_manipulation.modifying_user_other_changes
      reason: "other account changes by this user folded into follow_on_svc (account-administration commands on this host in +4h)"
    - template: account_manipulation.change_management_context
      reason: "duplicate of organizational_context.change_ticket"
    - template: persistence_scope.process_based_persistence
      reason: "process-based persistence tool sweep (schtasks/sc/reg) not the mechanism here; the rule's persistence is the group membership itself, confirmed via the 4732 Security event"
    - template: persistence_scope.file_based_persistence
      reason: "file-drop persistence not relevant; the rule's persistence is group membership, not a Startup/Run-key file"
  reflection_overrides: []
  hint_dispositions: []
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - account_manipulation
    - organizational_context
  generation_stats:
    total_questions: 16
    source_counts:
      template: 15
      attack: 1
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 13
    logsource_diversity:
      process_creation: 12
      security: 1
      sigma_alert: 2
      application: 1
