name: LSASS Dump Keyword In CommandLine
id: 5be00532-1702-4836-b594-9541a2484055
description: |
  Detects command lines containing keywords associated with dumping the LSASS process to disk, such as the literal strings
  lsass.dmp / lsass.zip / lsass.rar / nanodump / SQLDmpr or the combination of "lsass" and ".dmp" in a single command.
  LSASS process memory holds plaintext credentials and NTLM hashes when Credential Guard is not enabled, so a dump file is
  typically staged for offline extraction with Mimikatz or pypykatz. Legitimate hits are rare (a crash-analysis dump of an
  unrelated process, a file named for testing); the investigation should confirm whether LSASS memory was actually accessed
  and dumped, by whom, and whether the resulting credentials were used to move laterally.
type: detection
detection_id: ffa6861c-4461-4f59-8a41-578c39f3f23e
detection_category: ''
detection_type: sigma
contributors:
- SecurityOnionSolutions
created: '2026-06-06'
questions:
- source: template:universal.verify_trigger_match
  question: Does the command line contain the LSASS dump keywords this rule detects?
  context: Before investing investigation time, confirm the alert is a true positive by reviewing the exact command line against
    the rule's criteria. The rule fires on dump-file keywords (lsass.dmp, lsass.zip, lsass.rar, nanodump, SQLDmpr) or the
    pairing of "lsass" with ".dmp" in one command. Read the full command to see which keyword matched and whether it names
    a real LSASS dump or an unrelated artifact that happens to contain the string.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:universal.host_user_context
  question: Is this user and host expected to dump process memory or run credential-access tooling?
  context: Legitimate administrative or crash-analysis activity can produce a dump file, but a routine workstation user dumping
    LSASS is rarely expected. Knowing whether the user role (admin, incident responder, developer) and host function (a debug
    or DFIR workstation versus a general endpoint) fit the detected behavior is often the fastest path to distinguishing a
    sanctioned operation from credential theft.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - host.name
      - User
- source: template:credential_access.credential_target_identification
  question: Was LSASS memory actually accessed or dumped, and by what method?
  context: The command line reveals which LSASS dumping method was used and whether the dump targets memory or a file on disk.
    Identify the mechanism — procdump, comsvcs.dll MiniDump, nanodump, a SQLDumper variant, or a custom dumper — because the
    method indicates attacker sophistication and which evasion was attempted. A successful dump means the credentials cached
    in LSASS for every account logged on to this host should be treated as exposed.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.process_chain
  question: What process spawned this command, and is that parent chain expected?
  context: The parent process reveals how the dump command was invoked. A dump launched from explorer.exe or an admin shell
    during sanctioned work looks different from one spawned by office applications, script hosts (wscript.exe, mshta.exe),
    or a remote-execution service (WmiPrvSE.exe, services.exe from a scheduled task) — the latter point to hands-off or remote
    operator activity. Walk up the tree to see what spawned the parent and what else it launched.
  range: -7d
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ParentProcessGuid%'
      condition: selection
    fields:
      - ParentImage
      - Image
      - CommandLine
- source: attack:T1003.001.access_verification
  question: Did a process open a full-access handle to lsass.exe on this host around the alert time?
  context: The dump-file keyword in the command line is the on-disk artifact; the act of reading LSASS memory shows up
    separately as a handle open against lsass.exe. A handle requested with full access rights (GrantedAccess 0x1F0FFF) from
    a process that is not a known AV/EDR agent corroborates that credential scraping occurred rather than just a file being
    named. Note the source process and whether it matches the command identified in the alert.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_access
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetImage|endswith: '\lsass.exe'
        GrantedAccess|contains: '0x1f0fff'
      condition: selection
    fields:
      - SourceImage
      - GrantedAccess
      - CallTrace
- source: attack:T1003.001.tuning_parameter
  question: Was a dump file written to disk on this host, and where?
  context: A memory dump typically lands as a file before it is staged for exfiltration. Dump files are commonly written to
    operator-controlled staging paths such as %TEMP% or C:\Windows\Temp under names matching the rule keywords. Locating the
    file, its directory, and its creation time confirms the dump completed and shows where the credential material now sits;
    the gap between the file write and the LSASS handle open establishes the access-to-dump timing of the chain.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: file_event
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        TargetFilename|contains:
          - lsass
          - .dmp
          - .mdmp
          - SQLDmpr
          - nanodump
      condition: selection
    fields:
      - TargetFilename
      - Image
      - User
- source: template:cue_process_tool.follow_on
  question: What child processes did this command spawn afterward?
  context: The processes spawned after the dump reveal its actual impact. A dump followed by compression (a .zip or .rar of
    the output), an upload utility, or a parser like mimikatz/pypykatz indicates the credentials are being staged or extracted,
    not just captured. Predictable, no-child behavior is more consistent with a benign or isolated event.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ParentProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - Image
      - CommandLine
- source: template:cue_process_tool.baseline_deviation
  question: When this host has previously fired this LSASS-dump pattern, what parents and users drove it, and does the current
    alert fit?
  context: Even legitimate tooling can match these keywords. The query mirrors the rule's selection scoped to this host, so
    the resulting parent/user distribution reflects how this specific dump pattern (not generic process activity) has run
    here before. A new parent process, an unexpected user account, or a command structure with no precedent on this host is the
    anomaly worth chasing.
  range: -14d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains:
        - lsass.dmp
        - lsass.zip
        - lsass.rar
        - Andrew.dmp
        - Coredump.dmp
        - NotLSASS.zip
        - lsass_2
        - lsassdump
        - lsassdmp
      - CommandLine|contains|all:
        - lsass
        - .dmp
      - CommandLine|contains|all:
        - SQLDmpr
        - .mdmp
      - CommandLine|contains|all:
        - nanodump
        - .dmp
      _pb_scope:
        host.name|expand: '%hostname%'
      condition: (selection) and _pb_scope
    fields:
    - ParentImage
    - User
- source: template:cue_user_actor.baseline
  question: Has this user previously produced the rule's pattern, and does that history fit their role?
  context: The query mirrors the rule's selection structure scoped to this user — the result is the user's
    90-day history of the rule's specific firing pattern (not generic use of the binary), aggregated by
    host. A user who has never produced this pattern is more suspicious than one who does so regularly;
    weigh the count against the user's role and typical toolset.
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains:
        - lsass.dmp
        - lsass.zip
        - lsass.rar
        - Andrew.dmp
        - Coredump.dmp
        - NotLSASS.zip
        - lsass_2
        - lsassdump
        - lsassdmp
      - CommandLine|contains|all:
        - lsass
        - .dmp
      - CommandLine|contains|all:
        - SQLDmpr
        - .mdmp
      - CommandLine|contains|all:
        - nanodump
        - .dmp
      _pb_scope:
        User|expand: '%User%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.baseline_svc
  question: Has this parent process historically driven the rule's pattern on this host or across the
    environment, or is the parent new for this activity?
  context: 'When the alert fires under a service principal, user identity does not attribute behavior.
    The meaningful pivot is the launch mechanism: the query mirrors the rule''s selection structure scoped
    to the alerting parent process, so the result is the 90-day history of this parent driving this specific
    pattern (not generic parent+binary co-occurrence). A new or rare parent for this pattern is the deviation
    worth chasing; a parent that already accounts for thousands of prior events is vendor or OS automation.'
  range: -90d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains:
        - lsass.dmp
        - lsass.zip
        - lsass.rar
        - Andrew.dmp
        - Coredump.dmp
        - NotLSASS.zip
        - lsass_2
        - lsassdump
        - lsassdmp
      - CommandLine|contains|all:
        - lsass
        - .dmp
      - CommandLine|contains|all:
        - SQLDmpr
        - .mdmp
      - CommandLine|contains|all:
        - nanodump
        - .dmp
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_user_actor.clustering
  question: What else did this user do across the network around this time?
  context: Tracking the user's activity across all hosts reveals lateral movement or staging on multiple endpoints. If the
    same user triggered dump activity or other hands-on-keyboard actions on several hosts in the same window, the scope of
    compromise is larger than a single endpoint.
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        User|expand: '%User%'
      condition: selection
    fields:
      - host.name
      - Image
- source: template:cue_user_actor.clustering_svc
  question: Did this same parent-driven pattern fire on other hosts in the same window — i.e., is this
    a coordinated automation rollout, or is the activity isolated to this host?
  context: 'Under a service principal, "what else did this user do across the network" returns agent-telemetry
    noise (every host runs SYSTEM-owned agents constantly). The meaningful question is whether the same
    launch mechanism fired elsewhere: the query mirrors the rule''s selection structure scoped to the
    alerting parent process, fleet-wide, in the alert window. Multiple hosts in the same window points
    to scheduled or pushed automation; a single host points to a host-local trigger.'
  range: +/-1h
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains:
        - lsass.dmp
        - lsass.zip
        - lsass.rar
        - Andrew.dmp
        - Coredump.dmp
        - NotLSASS.zip
        - lsass_2
        - lsassdump
        - lsassdmp
      - CommandLine|contains|all:
        - lsass
        - .dmp
      - CommandLine|contains|all:
        - SQLDmpr
        - .mdmp
      - CommandLine|contains|all:
        - nanodump
        - .dmp
      _pb_scope:
        ParentName|expand: '%ParentName%'
      condition: (selection) and _pb_scope
    fields:
    - host.name
- source: template:cue_host_context.clustering
  question: What other activity occurred on this host around the time of the alert?
  context: 'Alerts rarely occur in isolation. Examining activity around the alert reveals the immediate context: what led
    to the dump command, what happened after, and whether it is part of a larger sequence. Look for reconnaissance, privilege
    escalation, or staging that brackets the credential-access event.'
  range: +/-15m
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - Image
      - ParentImage
- source: template:credential_access.post_access_lateral_movement
  question: Were outbound remote-access connections (RDP, SMB, WinRM, SSH) initiated from this host after
    this event?
  context: Credential theft is typically followed by lateral movement. The query returns outbound connections
    from this host to remote-access ports -- 3389 (RDP), 445 (SMB/PsExec), 5985/5986 (WinRM), 22 (SSH)
    -- in the hours after the event; the originating Image and User show what drove each connection. Two
    limits to keep in mind -- credentials exfiltrated and replayed from another machine will not appear
    here (check security 4624 logons on suspected targets for this account and any accounts with sessions
    on this host), and the query requires Sysmon network telemetry (EID 3); if that is not collected from
    this host, treat zero rows as a visibility gap, not as evidence no movement occurred.
  range: +4h
  query: |
    aggregation: false
    logsource:
      category: network_connection
      product: windows
    detection:
      selection:
        host.name|expand: '%hostname%'
        Initiated: 'true'
        DestinationPort:
          - 3389
          - 445
          - 5985
          - 5986
          - 22
      condition: selection
    fields:
      - DestinationIp
      - DestinationPort
      - Image
      - User
- source: template:credential_access.credential_exposure_assessment
  question: Which accounts may be compromised, and do any have elevated privileges?
  context: The severity of an LSASS dump depends on which accounts had sessions on this host when memory was captured. Every
    account logged on — interactive, service, or cached — may have credentials in the dump. Domain admin or service accounts
    with broad access require incident escalation; a standalone standard user may only need monitoring. Cross-reference the
    logged-on accounts with directory group memberships.
  range: +/-5m
  query: |
    aggregation: false
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
        ProcessGuid|expand: '%ProcessGuid%'
      condition: selection
    fields:
      - CommandLine
- source: template:cue_process_tool.scope
  question: Have other hosts fired this LSASS-dump pattern in the past week, and does the distribution look like automation
    or one-off activity?
  context: The query mirrors the rule's selection with no host scope, so the result is a fleet-wide count of this specific
    dump pattern (not generic process activity). Many hosts firing the same pattern points to scripted deployment or vendor
    automation; one host or a small unrelated set points to manual operator activity. Use the host distribution to calibrate
    severity.
  range: -7d
  query: |
    aggregation: true
    logsource:
      category: process_creation
      product: windows
    detection:
      selection:
      - CommandLine|contains:
        - lsass.dmp
        - lsass.zip
        - lsass.rar
        - Andrew.dmp
        - Coredump.dmp
        - NotLSASS.zip
        - lsass_2
        - lsassdump
        - lsassdmp
      - CommandLine|contains|all:
        - lsass
        - .dmp
      - CommandLine|contains|all:
        - SQLDmpr
        - .mdmp
      - CommandLine|contains|all:
        - nanodump
        - .dmp
      condition: selection
    fields:
    - host.name
- source: template:cue_host_context.precursor
  question: Did other detections fire on this host in the hours before this alert?
  context: Alerts rarely happen in isolation. Earlier detections on the same host — defense tampering, reconnaissance, or
    privilege escalation — may reveal the stages that preceded the dump and help reconstruct the timeline. Credential access
    sitting at the end of a chain of prior alerts is a stronger signal than an isolated keyword match.
  range: -6h
  query: |
    aggregation: false
    logsource:
      product: windows
    detection:
      selection:
        event.module: sigma
        host.name|expand: '%hostname%'
      condition: selection
    fields:
      - event.severity_label
      - rule.name
      - rule.uuid
_generation_meta:
  has_attack_brief: true
  techniques:
    - id: T1003.001
      name: LSASS Memory
      role: primary
    - id: T1003
      name: OS Credential Dumping
      role: secondary
  attack_chain_coverage:
    - stage: 1
      label: lsass_access
      description: "A non-privileged or abnormal process opens a full-access handle (0x1F0FFF) to lsass.exe"
      mapped_to: Q5
      source: attack:T1003.001.access_verification
    - stage: 2
      label: memory_dump
      description: "The process invokes a memory dump and writes a dump file to disk (the keyword the rule fires on)"
      mapped_to: Q6
      source: attack:T1003.001.tuning_parameter
    - stage: 3
      label: command_decode
      description: "Decode the dump command to identify the dumping method and confirm LSASS was the target"
      mapped_to: Q3
      source: template:credential_access.credential_target_identification
    - stage: 4
      label: post_dump_movement
      description: "Stolen credentials are exercised for lateral movement / follow-on logons"
      mapped_to: Q14
      source: template:credential_access.post_access_lateral_movement
  dimension_coverage:
    - dimension: access_verification
      technique: T1003.001
      covered_by: Q5
      source: attack:T1003.001.access_verification
      coverage_type: direct
    - dimension: command_analysis
      technique: T1003.001
      covered_by: Q3
      source: template:credential_access.credential_target_identification
      coverage_type: direct
    - dimension: parent_process_analysis
      technique: T1003.001
      covered_by: Q4
      source: template:cue_process_tool.process_chain
      coverage_type: direct
    - dimension: time_correlation
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.tuning_parameter
      coverage_type: direct
    - dimension: tuning_parameter
      technique: T1003.001
      covered_by: Q6
      source: attack:T1003.001.tuning_parameter
      coverage_type: direct
  mutable_dispositions:
    - element: AccessMask
      dimension: access_verification
      specific_values: "0x1F0FFF"
      disposition: query_injection
      target_question: Q5
      notes: "GrantedAccess|contains 0x1f0fff in the process_access query"
    - element: TimeWindow
      dimension: time_correlation
      specific_values: "5 minutes (LSASS access to dump file)"
      disposition: query_injection
      target_question: Q6
      notes: "Dump-file question range +/-5m brackets the access event; context calls out the access-to-dump gap"
    - element: ParentProcessName
      dimension: parent_process_analysis
      specific_values: "rationale only (allowlist AV/EDR)"
      disposition: context
      target_question: Q4
      notes: "process_chain surfaces the parent for the analyst to allowlist known AV/EDR"
    - element: DumpFilePath
      dimension: tuning_parameter
      specific_values: "%TEMP%, C:\\Windows\\Temp"
      disposition: query_injection
      target_question: Q6
      notes: "TargetFilename|contains dump keywords; staging paths named in context"
    - element: CommandLinePattern
      dimension: command_analysis
      specific_values: "rundll32, procdump, comsvcs.dll, Invoke-Mimikatz, nanodump"
      disposition: context
      target_question: Q3
      notes: "credential_target_identification is KEEP_EXACT (ProcessGuid-scoped); methods named in context"
  dedup_decisions:
    - dropped: cue_process_tool.clustering
      reason: "host-scoped same-time clustering subsumed by cue_host_context.clustering, which projects richer Image+ParentImage fields on the same host scope"
      kept: cue_host_context.clustering
    - dropped: cue_process_tool.baseline
      reason: "host historical frequency is covered more precisely by cue_process_tool.baseline_deviation, which scopes on the rule's CommandLine pattern rather than a (nonexistent) Image; CommandLine-only rule has no binary to baseline"
      kept: cue_process_tool.baseline_deviation
    - dropped: cue_user_actor.baseline (user-pivot) paired with cue_user_actor.baseline_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q9 user-pivot, Q10 parent+command substitute)
    - dropped: cue_user_actor.clustering (user-pivot) paired with cue_user_actor.clustering_svc
      reason: "user_actor pair — non-deduplicable"
      kept: both emitted (Q11 user-pivot, Q12 parent+command substitute)
  skipped_templates:
    - template: cue_process_tool.mechanism_decode
      reason: "replaced_by: credential_access.credential_target_identification (more specific Phase-2 question for credential-access rules)"
  reflection_overrides: []
  hint_dispositions:
    - hint: "logsource 'registry_set' (recommended by techniques: T1003.001) is not covered — pull in cue_registry_key.* or author a custom question with category: registry_set"
      disposition: declined
      reason: >-
        registry_set for T1003.001 covers the SilentProcessExit/WerFault registry
        trigger as an alternate dump mechanism. This rule's detection is strictly
        CommandLine keyword matching for dump-file artifacts (lsass.dmp, nanodump,
        SQLDmpr, etc.); a SilentProcessExit dump leaves no such keyword and would not
        fire this rule, so it is outside the analyzed event. The access_verification
        the registry path would otherwise corroborate is already covered by the
        process_access handle-open question (Q5). No compensating registry_set
        question authored at the 18-question cap.
    - hint: "logsource 'security' (recommended by techniques: T1003.001) is not covered and no template family exists — author a custom question with category: security"
      disposition: declined
      reason: >-
        The security-channel analytic (4673 privileged service / 4656 handle) for
        T1003.001 corroborates LSASS access at the audit layer. That same access is
        already directly verified by the Sysmon process_access handle-open question
        (Q5, GrantedAccess 0x1F0FFF against lsass.exe), which is the higher-fidelity
        source for this rule's host telemetry; the security audit channel would
        duplicate that coverage. No compensating security question authored at the
        18-question cap.
  contextual_sets:
    included: []
    excluded: []
  technique_sets_applied:
    - universal
    - credential_access
  generation_stats:
    total_questions: 18
    source_counts:
      template: 16
      attack: 2
      llm_generated: 0
    phase_distribution:
      triage: 2
      analysis: 1
      context: 15
    logsource_diversity:
      process_creation: 14
      process_access: 1
      file_event: 1
      sigma_alert: 2
